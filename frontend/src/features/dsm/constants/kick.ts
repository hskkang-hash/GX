/**
 * P-52 — **킥 문장 한 개.** 세 자리가 이 파일 하나에서 글자를 가져간다.
 *
 * 왜 상수인가
 * -----------
 * 판정은 「세 자리에 **같은** 글자」다(로그인 화면 · 「지금 처리할 것」 상단 ·
 * 월간 1쪽 첫 줄). 자리마다 손으로 적으면 세 문장은 반드시 갈린다 — 한 자리를
 * 고치는 날 나머지 둘이 옛말이 되고, **옛말이 된 것이 화면에서는 안 보인다.**
 * 그때 그것은 제품이 자기를 소개하는 문장이 아니라 **장식 셋**이다.
 *
 * ★ 사전(GX-COPY)에 등재된 글자다. 여기서 고치지 않는다 — 사전이 먼저 바뀐다.
 * ★ **마침표까지가 문장이다.** 자리마다 마침표를 떼면 그것도 갈린 것이다.
 */

/** 사전 등재 문장. 세 자리가 이것을 그대로 그린다. */
export const KICK_SENTENCE = 'GuardianX는 대응 시간을 잽니다.';

/**
 * 셋째 자리(월간 1쪽 첫 줄)는 **이 저장소의 프런트가 그리지 않는다.**
 *
 * [실측 2026-09-05] 월간 1쪽 「이번 달 우리 센터」는 서버가 낸다 —
 * 서식은 `docs/design/GX-REPORT_월간1쪽_이번달우리센터_서식_v0.1.md` 에 있고,
 * 치환 변수는 `backend/kernels/k4_report/schemas.py` 의 `MONTHLY_VARIABLES`
 * 서른여섯 개에 이름으로 잠겨 있다. 그 목록에 **머리 문장을 받는 변수가 없다**
 * (`tenant_name` · `period_label` 이 머리글의 전부다). 프런트에 월간 1쪽을
 * 그리는 화면도 없다.
 *
 * 즉 셋째 자리는 「우리 층에서 상수를 꽂으면 되는 자리」가 아니라 **서식과 커널
 * 튜플을 같은 커밋에서 고쳐야 하는 자리**다(그 규약은 그 파일이 스스로 적어 두었다).
 * 이번 턴의 프런트 차선은 서식 문서도 커널도 만지지 않는다 — 그래서 **배선을
 * 넘긴다.** 넘기는 것은 글자 하나다: 위의 `KICK_SENTENCE` 와 **같은 문장**을
 * 서식 첫 줄에 두고, 원하면 `kick_sentence` 라는 이름으로 등재한다.
 *
 * ⚠ 넘긴 채로 두면 셋 중 둘만 같은 글자다. 그것은 P-52 의 절반이고,
 *   절반은 초록이 아니다 — 보고에 「조율자 배선 필요」로 적는다.
 */
export const KICK_SENTENCE_THIRD_SLOT_PENDING = true;
