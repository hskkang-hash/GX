# GuardianX 회신 — 상황 대전환 + 정본 갱신을 이 문서 하나로
**발행** 2026-08-16 · **대상** Claude Code
**★ 이 문서가 정본 갱신 수단이다 (D-242).** zip 전달은 6회 연속 실패했다. 이제 갱신 원문을 이 문서에 담고,
**대표가 이 문서를 너에게 붙여넣는 행위 = 아래 §2·§3 적용에 대한 대표 승인**으로 정의한다.

---

## 0. 판정 — 이번 수행 2건 전부 인정

- **커밋 3분할(195888b · c38576f · faebd91)**: 훅 공백정리를 chore로 분리해 보안 diff 16줄을 깨끗하게 유지한 것,
  선례(7af88b6)를 따른 것, EXIT §2-1 커밋 해시를 채운 것 — 전부 옳다. glob 이 handover 3파일을 건드려
  되돌린 처리도 옳다. (커밋은 대표가 지시했으므로 금지 17 위반이 아니다 — 사후 승인으로 기록한다)
- **RUNBOOK 통합(3fcde0a)**: WP-0§8+WP-1§8+W0-1b를 한 장으로, §A 실패 시 중단 구조, §B-2 분기를 미리 박은 것 — 좋다.
  **다만 이 RUNBOOK 의 전제가 방금 무너졌다. §1 을 읽어라.**

## 1. ★ 새 사실 3가지 — 세계가 바뀌었다

| # | 사실 | 네 계획에 미치는 영향 |
|---|---|---|
| 1 | **사내망(192.168.0.22)은 더 이상 존재하지 않는다. 담당 개발자는 퇴사했다** | "사내망 방문"은 영원히 오지 않는다. RUNBOOK(3fcde0a)의 §A(사내 pip) 경로는 **사문화** |
| 2 | **운영 서버가 살아 있다**: `115.21.49.115:3002` 에서 GuardianX 가 실제 가동 중 | dj-core·rj-core·gcs-fe **설치본이 그 서버 안에 있다** — 회수 경로 B |
| 3 | 대표 증언: **운영환경은 `C:\GuardianX\GuardianX build` + `guardianx-source` 두 폴더만으로 100% 세팅됐다** | 사실이면 설치본이 **이 PC 안에 이미 있다** — 회수 경로 A (최우선) |

**따라서 다음 작업 = `docs/agent/RUNBOOK_로컬기동.md` 실행** (별첨 — 대표가 함께 붙여넣는다).
요지: `GuardianX build` 인벤토리 15분 → dj-core wheel/site-packages/node_modules/도커 tar 수색 →
발견 시 venv 이식 → `import core.base` → `manage.py check` → 프론트 기동 → **미검증 재고 전부 실행**.
네 RUNBOOK(3fcde0a)의 §B(확인 항목)·§C(검증 일괄)는 그대로 유효하다 — **실행 장소가 사내망에서 이 PC로 바뀔 뿐이다.**
§B-2(created_by__isnull 분기)도 dj-core 사본을 찾는 즉시 로컬에서 판정하라.
경로 A 실패(셋 다 없음) 시에만 경로 B(운영서버 SSH — 대표가 접근 확보 중)로 회귀한다.

## 2. 정본 수정 — 네가 직접 적용하라 (이 항목에 한해 금지 11 해제, 대표 승인부)

전부 **기계적 작업**이다. 판단이 필요한 것은 없다.

```
2-1. meta.manifest 재생성: tickets 배열 실측으로 total / by_phase / ids 를 생성해 meta 에 삽입.
     (네가 발견한 "manifest 블록 부재"의 해소 — 손으로 세지 말고 스크립트로 생성·검증)
2-2. tickets.sha256 삭제. meta 에 integrity: "manifest" 추가.  (D-228: 파일 전체 해시는 설계 자기모순)
2-3. meta.version 을 "3.7-local" 로. (내 v3.6 과 네 트리의 이력이 갈라졌으므로 새 이름)
2-4. AGENT_LOOP.md 머리에 개정 블록 추가:
     - STEP 0 첫 줄 형식: REGISTRY v… / total=… / decisions=… / loop=…  (4개 값 전부 — D-240)
     - sha256 검사 조항 삭제, manifest 대조로 대체
     - 금지 17 각주: 프로토타입 허용·작업트리 잔류 불허(패치 격리, D-234)
     - "사내망" 문구 전부 → "회수 경로 A(로컬 build 폴더) / B(운영서버)" 로 치환
2-5. C:\GuardianX\agent\ (v2.0 구판) 최상단에 DEPRECATED.md 생성:
     "이 폴더는 폐기. 정본은 guardianx-source/docs/agent/ (CANONICAL 규칙)"
2-6. 위 전부를 docs 커밋 1개로: "chore(D-242): registry self-maintenance (대표 승인부)"
```

## 3. decisions_pending 11건 — 전량 확정 (아래를 decisions.yaml 에 그대로 추가하라)

| pending | 확정 | 요지 (전문은 네가 D-2xx 항목으로 정서해 추가 — raised_by 원문 유지) |
|---|---|---|
| P-W0-2-1 | **D-221** | dod③(W0-14 참조) WP-1 EXIT 로 이월. W0-2 는 성능대조 기록으로 완료 가능 |
| P-W0-10-1 | **D-223** | done 소급 강등이 맞았고, settings.py 16곳 치환으로 재충족 — 현행 인정 |
| P-W0-4-1 | **D-218/222** | D-218 문구가 실효 DoD. dod 를 그 문구로 교체(이 문서가 승인부) |
| P-W0-3-1 | **D-224** | xfail 금지 유지 — 문서 등록부(coverage.md) 방식이 정본 |
| P-W0-10-2 | **D-225** | CORS 전면허용 제거 = 신규 티켓 W0-15(P1·S) 신설, WP-2 소속 |
| P-LOOP-1 | **D-228** | sha256 폐기 (§2-2) |
| P-LOOP-2 | **D-227** | manifest 는 작성자가 v3.0 개편 때 유실 — §2-1 로 복원 |
| P-LOOP-3 | **D-234** | 선행 구현은 패치 격리 원칙. 이번 건은 대표 지시 커밋으로 사후 승인 |
| P-W0-12-1 | **D-241** | print_format 표본 오열람은 W0-14 롤아웃 ②에서 함께 |
| P-W0-12-2 | **D-241** | dod③ → ServiceLayerGroupSelectionTest 로 정본 정정 |
| P-W0-14-1 | **D-241** | 데코레이터는 통합시험 2건 green 후 파일럿 1개부터. 466 일괄 부착 금지 |

적용 후 11건 전부 `status: answered` 로.

## 4. 검토서 승인 — 대표께 (아래 두 줄만 기입하시면 됩니다)

| 파일 | 첫 줄에 기입 | 근거 |
|---|---|---|
| `review/WP-0/ENTRY.md` | `APPROVED: 2026-08-16 강희석` | 계획 승인 (이미 이행됨 — 정합화) |
| `review/WP-1/ENTRY.md` | `APPROVED: 2026-08-16 강희석 (커밋 지시로 사후 승인)` | faebd91 커밋의 근거 |
| WP-0 / WP-1 **EXIT** | **승인하지 않는다 — 비워 둔다** | goal 이 아직 거짓(D-237). **로컬 기동 성공 후** 갱신 재제출 → 그때 승인 |

## 5. 실행 순서 (오늘)

① §2 정본 자체 정비 (커밋 1개) → ② §3 결정 정서·pending 정리 (커밋 1개) →
③ **RUNBOOK_로컬기동 실행** — 인벤토리 결과에 따라 A/B 판정 보고 →
④ 기동 성공 시: 미검증 재고 전부 실행, EXIT 두 장 갱신 재제출, **멈춘다**
⑤ 신규 기능 구현은 여전히 금지 — 기동·검증·정비까지만 (동결의 새 정의)
