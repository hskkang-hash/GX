# RESUME_NEXT.md — GuardianX 초효율 작업지시서 · 턴 F (2시간 · 사이드바 판정 · 배치는 명령 · RC-1 준비)

- 발행: **2026-09-05 22:15(기계) · 턴 F** · 발행자: 세종(CPO) · 수신: 영실(CPM · 조율자)
- 직전 보고(턴 E · 커밋 `cce7f7b` · 20:57) 읽음: **82.0/100**(133 · 직전 82.8/129 · 같은 분모면 82.8→82.8) · 손 안 **92.2%**(107/116) · ①82 ②83 ③100 ④78 ⑤71 ⑥92 ⑦84 ⑧50 · 손 밖 16 · 설계 1 · 단위 1,090 · E2E 23 · 게이트 13종 11·0·2 · 라우트 705(관문 없는 72 불변) · 화면 **28/28** · 5xx **0.083%** · **서비스되던 번들이 09-04 07:17 사본**(갈아 끼움) · **파기 진짜로 지움**(워커·beat · 행 0·객체 0·기록 1 · 감사 쓰기 3일치 12,468건 밀림 발견) · 거짓 초록 게이트 둘 · TOP 5 · Critical Path 3 · 남은 엔지니어링 2턴 — 영실 [실측]. `verify_ga_readiness` 재실행 82.0% [실측 기계 22:13].
- 규칙: 삭제·되돌리기 외 선택은 세종 판정으로 집행 · 멀티 에이전트 · 2시간 권고(짧은 턴은 이어서) · 사업 문서는 개발을 막지 않는다.

---

## §0 상용 기준 완료율 [실측 · cce7f7b]

| 관문 | 지금 | 열쇠 |
|---|---|---|
| G1 URL 도달 | ◐ 앞단 · 공개 0 | **스테이징 승인**(대표) |
| G2 로그인·계정 | ● — 단 **사이드바에 제품 화면 0장**(URL을 쳐야 한다) | **UX-25 판정 = §2 P-61 (이 문서)** |
| G3 실제 사용 | ⑦ 84% · 화면 28/28 새 번들 · 온보딩 63% | 점검표 · 셋째 술어 |
| G4 PRD·기능명세 | 계약 82% · 제품 84% | T-OUT |
| G5 운영 지속 | ④ 78% · 파기 ✓ · 5xx 0.083% ✓ · 경보 도달 0 · 로그 상한 재기동 대기 | SMTP · 재기동 15분 |

| | 지금 | RC-1 [산술] |
|---|---|---|
| Feature Completion (64절) | **83%** (53/64) | 88% |
| Production Readiness | **85%** 기술 · URL 0 | 90% · URL 1 |
| Commercial Readiness | **30%** | 60%(세종 문서) |
| 상용 / 손 안 / 온보딩 | **82.0 / 92.2 / 63** | ≈ 88 / 100 / ≥ 85 |

**−0.8은 잃은 것이 아니다** — 일하다 찾은 잠금 둘(다중 세션·사이드바)이 분모에 들어온 것이고, 그것을 「없는 문제」로 두지 않은 것이 이번 턴의 값이다.

---

## §1 인정 — 서비스되던 것은 우리가 만든 것이 아니었다

1. **3002가 내주던 것이 09-04 07:17 사본** — 그 뒤의 모든 화면 증거는 낡은 번들의 증거였다. 세종이 읽은 캡처 24장도 그것이다. **원칙 등재: 「빌드했다」와 「서비스된다」 사이에 게이트가 선다 — 서버가 내는 번들 해시 = HEAD.** 28/28 재촬영 인정.
2. **파기가 진짜로 지운다** — 워커·beat 세우고 행 0·객체 0·기록 1. 그리고 **감사 쓰기 12,468건이 밀려 있었다** — 정본은 안 지워졌지만 쓰이지도 않고 있었다. §2 P-65.
3. **예산 게이트 분리(응답 초록·오류 빨강)** · **SEC-11a 217→205** 인정.
4. **TOP 5·Critical Path·남은 2턴** — 옳은 지도. 이 문서는 그 셋(스테이징·재기동·UX-25)을 그대로 집행한다.
5. **가장 큰 위험 「낡은 입력이 초록을 만드는 자리」** — 옳다. 배치를 절차가 아니라 **명령**으로(§2 P-64).

---

## §2 판정 6 — Critical Path의 「판정」을 여기서 끝낸다 (집행)

### P-61 UX-25 사이드바 — 어느 화면을 어느 역할에 [판정]
메뉴 정본은 dj-core DB(131행)다 — **코드가 아니라 시드(데이터)로 넣는다**(§0.4 무수정). 이름은 GX-COPY. 순서는 **하루에 누르는 횟수 순**.

| 역할 | 사이드바 (위에서 아래로) |
|---|---|
| **U1 관제요원** | 지금 처리할 것 · 무슨 일 있었나(이벤트 목록) · 카메라(격자·순회) · 인계 메모 · **?** 처음이세요 |
| **U2 관제팀장** | U1 전부 + 요원별 현황 · 설정(임계값·알림 규칙·시험) · 보고서 · 훈련 모드 |
| **U4 재난안전과** | 무슨 일 있었나(7일) · 보고서(월간 1쪽·HWPX) · 이벤트 검색 · 열람·삭제 청구 · 감사 기록 |
| **U5 관리자** | 시스템(맥박·용량·생존) · 사용자·역할 · 카메라 일괄 등록 · 알림 규칙·채널 · 계량 · 백업·보존 · 훈련 모드 · 기존 화면(래핑) |
| 전 역할 하단 | **버전 `cce7f7b`** · 지원 창구(장애 신고 1줄) |

규칙: 역할에 없는 메뉴는 **렌더하지 않는다**(비활성 아님) · 관제 역할 9장 상한 · 시드는 `data_source=seed`가 아니라 **제품 메뉴 정본**이다(테넌트 생성 시 자동). 강제: `verify_sidebar.py` — 역할별 메뉴 행 수 · 링크 도달 · 드론·영문 0.

### P-62 UX-24 다중 세션 — 손 밖 인정, **쪼개서 손 안을 만든다**
dj-core 세션 자료구조(§0.4)가 갱신을 끊는다 — 손 밖 인정. 다만 **월 모드는 세션이 필요 없다**: **UX-24a 월 표시 토큰**(읽기 전용 · 12h · 화면 `/wall`만 · `authn_paths` 등재 · 쓰기 0) → 손 안. **UX-24b 데스크+휴대전화 동시** → 손 밖 유지(인수자에게 세션 구조 문의 — T-OUT 한 줄). 그리고 **지금 당장**: 휴대전화로 로그인하면 데스크가 끊긴다는 사실을 **끊긴 화면이 말한다**(「휴대전화에서 로그인해 이 화면이 닫혔습니다 · 다시 로그인」 1클릭) — 절 아님, C 차선 한 줄.

### P-63 재기동 창 15분 — 집행(P-55 그대로)
이번 턴 **첫 일**. OPS-07 상한 적용 + `gx-shell` 프런트 마운트(회색 둘 → 색). 차선 넷 정지 → 재기동 → V → 착수.

### P-64 배치는 절차가 아니라 명령이다
`scripts/deploy.sh` 하나: 빌드 → 다이제스트 → 배치 → **번들 해시 게이트**(서버가 내는 번들 = HEAD) → `walk_scenarios` 1회 → 실패 시 exit 1(배치 취소). RUNBOOK의 배치 절은 이 한 줄로 바뀐다. 스테이징도 같은 명령으로 간다.

### P-65 감사 쓰기 지연 — 감시 신호 12번째
`ops_monitor`에 「감사 큐 지연(초)·밀린 건수」 추가 · 5분 초과 경보(P-41 허용 도메인) · 생존 알림 본문에 한 줄. 정본이 있어도 쓰이지 않으면 감사에 답할 수 없다.

### P-66 온보딩 셋째 술어 · 점검표 — 보고에 없었다
턴 E 보고에 「셋째 술어 적용 · 내려간 행」과 「점검표 N/56」이 없다. **이번 턴 보고 첫 줄 옆에.** 없으면 그 둘은 미측정이다.

분모 133 → **134**(UX-24a). 손 안 116 → 117.

---

## §3 대표 승인·결정 (변동 없음 · 3 + 자원 1)
1. **SMTP 자격증명** 2. **삭제** `tasks/record_task.py` 3. **VLM·GPU 1장** · 자원: **스테이징 서버 1대** — **RC-1의 유일한 열쇠.** 승인 즉시 영실이 `deploy.sh`로 세운다(사람 손 0).

---

## §4 턴 F (2시간 · 동시 4) — Critical Path 셋 + 잔여

| 차선 | 절·일 | 첫 증거 |
|---|---|---|
| **S** | **UX-24a 월 표시 토큰**(대장 행 먼저 · 읽기 전용 · 12h) · SEC-11a 접두 계속(205→) · 끊긴 화면 한 줄(P-62) | `/wall` 12h 토큰으로 데스크 세션 유지 캡처 · authn_paths +1 |
| **Q** | **`deploy.sh`**(P-64 · 번들 해시 게이트 내장) · **점검표 56칸**(P-66) · 온보딩 셋째 술어(P-66) · 감사 지연 신호(P-65) 시험 | deploy 1회 exit 0 · 점검표 N/56 · 온보딩 N%(내려간 N) |
| **E** | **첫 일 재기동 15분 → OPS-07 닫기** · gx-shell 마운트 · OPS-16 계량 표(미완이면) · **스테이징 준비**(승인 전 가능한 것: 미러 스크립트 · 빈 환경 설치 스크립트 dry-run · 인증서 발급 절차 문서) · LAW-04·05 등재(세종 초안) | OPS-07 초록 · 계량 CSV · 스테이징 준비 3 파일 |
| **C** | **UX-25 사이드바 시드**(P-61 표 그대로) · `verify_sidebar` · 하단 버전·지원 창구 · 킥 문장 3자리 확인 | 역할 4 × 사이드바 캡처 · 관제 9장 |
| **V** | 번들 해시(첫) · `ui_secrets` · `ui_copy` · `send_allowlist` · `post-arg-style` · purge · sidebar · ga_readiness(134) · dormant · walk · 세 수 | 색만 |

**턴 G(다음 2시간)** — 승인 시: `deploy.sh` → 스테이징 → 공개 URL·인증서 → 신규 계정 발급 → **「URL로 들어가 로그인하고 첫날 일을 끝낸다」 완주 캡처(데스크+390px)** = **RC-1**. 미승인: 손 밖 재판정 · 문서 등재 · 회귀 전수.

**함정 넷**: ① 사이드바 시드는 테넌트 생성 경로에 붙는다 — 개발 테넌트에만 넣고 끝내지 않는다 ② 월 토큰은 쓰기 0 — 판정·접수 버튼이 `/wall`에 렌더되면 빨강 ③ `deploy.sh`가 실패해도 이전 번들이 계속 서비스돼야 한다(원자적 교체) ④ 재기동은 첫 일 한 번.

---

## §5 세종(CPO) 산출물 — 이번 턴 함께
LAW-04 GS 대조표 · LAW-05 SLA 초안(99.9 · 5xx ≤ 0.1 · RTO 30 · RPO 24h · 응답 예산) · 가격표 v0.1 · 데이터 이용 동의 조항 · 무료 진단 서식 · LAW-06b 자기평가서 서식 · **스테이징 승인 요청서 1쪽**(대표용 · 비용·목적·RC-1 정의). 영실은 등재·링크만.

## §6 순서 · 보고 · 불변
**순서(2h)**: ① 조율자 15분 — **재기동 창(P-63)** · UX-24a 등재(134) · 쓰기 면 선등록(월 토큰 · 메뉴 시드 · 감사 지연) ② S ∥ Q ∥ E ∥ C 착수 ③ 90분 병합 S→Q→E→C → V(첫 판정기 번들 해시) ④ `deploy.sh` 1회 → 세 수(134) · G1~G5 · FC/PR/CR · 점검표 · 온보딩(셋째 술어) — 보고.

**보고**: 첫 줄 게이트 + 번들 해시=HEAD + `ui_secrets`·`send_allowlist`·`post-arg-style`·purge·sidebar 초록 + 동시·착수·DuplicateDatabase · 세 수(134/117/48) · 8영역 · G1~G5 · FC/PR/CR · 「턴 F N절 중 닫힘 N」 · **점검표 N/56 · 온보딩 셋째 술어 적용·내려간 행 N** · 스테이징 준비 3 · 차선 한 줄 · V 빨강 · 세종 오류 N(누계 16+N) · Code 오판 N · 위임 이의 N · 기계 시각 · 소요.

**불변**: 전부 유지 + **서버가 내는 번들 = HEAD(배치는 명령)** + **역할에 없는 메뉴는 렌더하지 않는다** + **월 모드는 쓰기 0**.

## 대표께
- 턴 E: 서비스되던 화면이 **하루 전 사본**이었음을 잡고 갈아 끼웠고, 파기가 **진짜로 지웁니다**. 수는 82.0(분모 133) — 일하다 찾은 잠금 둘이 분모에 들어와 0.8 내려갔고 그것이 옳습니다.
- 영실의 Critical Path 셋 중 둘(재기동·사이드바 판정)은 이 문서로 끝냈습니다. **남은 하나가 스테이징 서버 승인**입니다 — 승인되면 다음 턴이 RC-1(URL로 들어가 첫날 일을 끝낸다 완주)입니다. 승인 요청서 1쪽을 세종이 씁니다.
- 관제실 월·데스크·휴대전화 동시 접속은 인수 자산의 세션 구조라 손 밖이지만, **월은 세션 없이(표시 토큰) 지금 엽니다.**

