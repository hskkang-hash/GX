# RESUME_NEXT.md — GuardianX 코드구현 지시서 (P-18 정정 · C∥D∥E2 동시 · A2 접두 한 개씩 · onboarding_48 텍스트 인계)

- 발행: 2026-09-21 · 발행자: 세종(Cowork) · 수신: Code(조율자)
- 직전 보고(2026-09-20) 기록 읽음: 게이트 8·0·0·대기 2 · 판정기 9종(+`verify_cache_frame`) · 계약 82%(눈 3)·절 39 · 커널 표 +2(K1 `close_as_false_positive` · K2 `notice_false_positive`) · **상용 59.9% (절 105: 구현 67 · 미착수 19 · 잠김 12 · 미측정 7)** · 8영역 표 · 커밋 `f78c93e`(A) `3670d19`(A3·A2·B) `2f2a47f` `af79ad8`(E ①④) · `BYPASS_PATTERNS` 쉼표 · `API_CONTRACT_PROMOTE_PATHS` · `docs/agent/evidence/P-3/promoted_11.md` · 단위 566/1 · E2E 23 · 진입면 21/21 · §0.4 0줄 — Code [실측].
- 번호: 이름으로. 대응표는 Code 보고에만.

---

## §0 인정 — 8영역이 처음으로 실측이 됐다, 그리고 세종 오류 둘

1. **8영역 표 [실측] 인정.** ①기능 32/39 82 · ②보안 10/15 67 · ③격리 3/4 75 · ④운영 6/13 46 · ⑤성능 2/6 33 · ⑥품질 7/11 64 · ⑦사용성 6/12 50 · ⑧법·인증 1/5 20 → 가중 59.9%. 이 표가 이 문서의 유일한 %다. 분모 105 = 구현 67 + 미착수 19 + 잠김 12 + 미측정 7 — 이 네 수가 100%의 지도다(§3).
2. **순차의 사유 — 옳다, 규약을 고친다.** 컨테이너 1대 · 시험 DB 1개(`test_database_guardianx`) · runserver 1개 · MinIO 1대 공유, 시험 DB 동시 실행 시 DuplicateDatabase/ObjectInUse [실측 09-10]. Code 정정 제안 「막는 것은 자원이 아니라 이름」 — 받는다. **P-18 정정**: 컨테이너 복제 없음. `DATABASES["default"]["TEST"]["NAME"] = env("DB_TEST_NAME")` · runserver 포트 · MinIO 버킷 접두 · 시드 테넌트 — 네 이름만 차선별로 가른다. 표에 「시험 DB 이름」 칸 추가(§2).
3. **세종 오류 E-10 — A2 폭발 반경.** promoted 11은 이미 승격 중(`API_CONTRACT_PROMOTE_PATHS` 기본 `/api/dsm/`)이었고, 전역 스위치가 바꾸는 것은 나머지 652 — 그 화면 상당수가 §0.4다. 내 「11 세기 → ON」은 반경을 60배 잘못 봤다. Code 권고 「접두 한 개씩 넣히고 마지막에 켠다」를 받는다(§4). 이 오류는 P-0의 사촌이다 — 세종은 저장소를 열지 않으므로 「스위치 하나」의 반경을 모른다. 앞으로 스위치·플래그·기본값을 바꾸는 지시에는 **「반경 먼저 실측 후 판정」**을 붙인다.
4. **세종 오류 E-11 — 온보딩 미측정의 원인이 나였다.** 48행 정본이 09-17 지시서 PDF 안에만 있어 Code가 텍스트로 꺼낼 수 없었다. 「없는 것을 지어내지 않았다」 — 옳다. 이번 턴에 `docs/agent/onboarding_48.md`를 **세종이 텍스트로 넘긴다**(같은 커밋에 함께 들어 있다). 규칙: 세종이 Code에게 넘기는 표는 PDF가 아니라 텍스트(md)로도 함께 낸다 — PDF는 대표용, md는 Code용.
5. **쉼표 하나가 게이트 하나를 삼켰다 — 인정, 착시 ⑥형 실사례 #1.** `BYPASS_PATTERNS` 쉼표 누락으로 `'media-datatask-status'`가 생겨 목록엔 있으나 아무 경로에도 맞지 않았고 `/api/task-status/…`가 조용히 캐시됐다. 「목록에 있다 ≠ 맞는다」 — ⑥ 스키마형(필드 있으면 기능 있다고 읽음)의 실사례로 등재. AST 원소 수 vs 토크나이저 STRING 수로 도구화한 것 옳다 — 눈과 grep은 못 잡는다.
6. **시드가 규칙을 흉내 내고 있었다 — 인정, 원칙 등재.** 직접 닫기를 빼자 전이 36→24, 자동 종결 4, 분포 동일(closed 8). 「수는 같은데 원인이 다르다」. **원칙: 시드는 규칙을 흉내 내지 않는다 — 규칙이 만든 결과를 확인만 한다.** 시드가 안 닫혀 있으면 멈추게 한 것 옳다.
7. 커밋 넷 · 쓰기 면 대장 셋 다 선등록→빨강→구현→초록(P-8) · Code 오판 0 — 인정.

---

## §1 이번 턴 — C∥D∥E2 동시 (B 끝 · 조건 충족)

B가 끝났으므로 C·D의 조건이 찼다. E의 ②순회·HWPX·P-11이 남았다. **셋을 동시에** 띄운다 — P-18 정정 위에서.

| 차선 | 내용 | 시험 DB · 포트 · 버킷 · 테넌트 | 쓰기 면(선등록) |
|---|---|---|---|
| **C 웹 화면** | P-13 W1 프리셋 3(미처리·지난 12시간·내 담당) + 요약 한 줄 「발생 N·확인 N·조치 중 N·종결 N·오탐 N」(오탐 N = `false_positive_rate` — **이미 있고 부르는 화면 0곳**) · W2 상세(오탐 버튼 = B 판정 라우트 · 전파 기록 · 상태 이력) · /handover 본문(열린 이벤트 첨부·메모·확인) · 검수 콘솔 16→21 | `test_gx_c` · 8300 · `c_` · tenant_c | 인계 메모 · 전파 기록 |
| **D 모바일** | M1 목록(배지 = response_state · verdict 태그) · M2 상세(스냅샷·구간 티켓·카메라 주소·접수 확인/조치 중/완료/오탐 = B 라우트) · M3 회신(한 줄·사진 1장·전파 기록) · 재알림 N분(기본 5 [추정] · 로그 어댑터) · 로그인 `/api/v1/auth/login` · 390×844 | `test_gx_d` · 8400 · `d_` · tenant_d | M3 회신 · 재알림 |
| **E2 운영·보고 잔여** | 평상 운영 ② 순회(설정 = 쓰기 면) · HWPX 일일 1종((a)(b)(c) 실측 · (d) 미검증 · samples/) · **P-11**: `docs/agent/onboarding_48.md`(세종 텍스트)의 48행에 경로 채우고 상태 실측 · 어긋난 행 N건 · 첫 온보딩 % | `test_gx_e` · 8500 · `e_` · tenant_e | 순회 설정 |
| **V 검증** | 병합마다: 화면 띄우기(data_source) · contract-route-reach · 격리 탐침 · ⑧⑩⑪ · cache_frame · 게이트 세 수 · 상용 가중 · 온보딩 | `test_gx_v` · 8900 · `v_` · tenant_v | 없음 |

병합 순서: C → D → E2, 각 뒤 V. 셋 다 B(끝)에만 의존하므로 **동시 착수**. 세 차선이 같은 파일을 만지면 조율자가 먼저 갈라 준다 — 예상 겹침: W1/W2와 M1~M3가 같은 이벤트 API 클라이언트를 쓰면 그 파일은 **B가 이미 끝낸 것**이라 읽기만 — 고치지 않는다.

**순차를 택하면 사유 한 줄.** P-18 정정 뒤에도 못 띄우면 그 이름을 적어라.

---

## §2 P-18 정정 — 이름 네 개

```
▣ P-18 (정정) 병렬 자원 분리 = 이름 분리 [판정 · Code 제안 수용]
   컨테이너 1대 그대로. 차선마다 네 이름: 시험 DB(DB_TEST_NAME=test_gx_<x>) · runserver 포트(8x00) · MinIO 버킷 접두(<x>_) · 시드 테넌트(tenant_<x>).
   settings.py DATABASES["default"]["TEST"]["NAME"] = env("DB_TEST_NAME") — 세 줄. .env.example에 DB_TEST_NAME 이름만(D-204).
   워크트리는 그대로(../gx-lane-<X> · lane/<X>) — 같은 컨테이너 안의 다른 디렉터리. 병합·main 커밋은 조율자.
   강제 도구: 두 차선 시험을 동시에 돌려 DuplicateDatabase 0건 [실측] — 이것이 P-18의 출생 표본이다.
```

---

## §3 상용 100% — 분모 105의 지도 [Code 실측 위에 세종 판정]

105 = 구현 67 + **미착수 19** + **잠김 12** + **미측정 7**. 100%로 가는 길은 셋이다.

```
▣ 미착수 19 → 차선이 닫는다. 이번 턴 C·D·E2가 그중 몇을 닫는지 절 이름으로 보고(「미착수 19 → N」).
▣ 미측정 7 → 이름과 측정 조건을 이번 보고에. 측정 조건이 손 안(도구·시드·환경 안)이면 V가 이번 턴에 잰다. 손 밖(GPU·상대방)이면 「손 밖」으로 옮긴다. 미측정은 회색이다 — 초록도 빨강도 아니고, 100%의 분모에서 빼지도 않는다.
▣ 잠김 12 → 둘로 갈라라 [판정]:
   (a) 설계 잠금(영구) — 계약 11조 CLIP_EXTRACTION처럼 「하지 않기로 한 것」. 이것은 기능이 아니라 규칙이다 → 분모에서 제외하고 대장에 「설계 잠금 N절 · 사유」로 남긴다. 100%의 대상이 아니다.
   (b) 손 밖 잠금(조건부) — SDN 5·정찰 1(상대방) · 알림 채널(대표) · GPU(환경) · 인증(기관). 분모에 남기고 「손 밖 N절」로 따로 센다. 조건 성립 시 열린다.
   그러면 두 수가 나온다: 「상용 가중(전체 분모) N% · 손 안 도달율 = 구현 / (105 − 설계 잠금 − 손 밖) N%」. 손 안 도달율 100%가 우리가 닫을 수 있는 100%다. 둘 다 적는다.
```

8영역 잔여(Code 실측 09-20 기준):

| 영역 | 실측 | 잔여 절 → 차선 |
|---|---|---|
| ① 기능 | 32/39 | 7 — 오탐 화면·모바일 3·인계·전파 기록·HWPX·순회 (C D E2) · 11조 설계 잠금은 (a)로 |
| ② 보안 | 10/15 | 5 — 봉투 승격 접두(A2) · 운영 자격증명 회전·9200/5601 절차 문서(T4) · 손 밖: 운영 작업 창 |
| ③ 격리 | 3/4 | 1 — 새 쓰기 면 5(C D E2) 탐침 초록 (V) |
| ④ 운영 | 6/13 | 7 — 순회·인계(C E2) · 백업 복구 실측(T3) · 손 밖: GPU |
| ⑤ 성능 | 2/6 | 4 — p95 게이트 상시(A2 뒤) · 부하시험 손 밖(GPU) |
| ⑥ 품질 | 7/11 | 4 — E2E 23→28(신규 화면 5 · C D 뒤) · 봉투 일관성(A2) |
| ⑦ 사용성 | 6/12 | 6 — 화면 21/21 · 프리셋 · 온보딩 실측 · 검수 콘솔(C D E2) · 손 밖: 알림 채널 |
| ⑧ 법·인증 | 1/5 | 4 — 문서 차선(T4) · 손 밖: 기관 |

---

## §4 A2 봉투 — 접두 한 개씩 (E-10 정정)

```
▣ 전역 스위치를 켜지 않는다. API_CONTRACT_PROMOTE_PATHS 에 접두를 한 개씩 더한다. 순서: 우리 화면이 부르는 접두(측정 가능·고칠 수 있음) → 마지막에 §0.4 화면이 부르는 접두.
▣ 접두 하나 더할 때마다: V가 그 접두를 부르는 화면을 다시 띄운다 → 깨진 화면은 HTTP 상태를 읽게 고친다(우리 화면) — 미들웨어를 끄지 않는다.
▣ §0.4 화면(우리가 못 고치는 자리)이 부르는 접두: 화면 코드를 고치지 않고 **프론트 인터셉터 1개**(우리 층)로 4xx/5xx를 그 화면이 기대하는 옛 봉투 모양으로 되돌려 준다 — 「밖에서 잇는다」(D-335/D-348). API는 승격되고 옛 화면은 깨지지 않는다. 인터셉터는 대장에 「봉투 어댑터 · §0.4 화면 N · 해제 조건 = 그 화면 교체」로 남긴다.
▣ 마지막에 전역 ON — 그때 promoted 652 전부가 이미 접두로 덮여 있어야 한다. ON은 확인이지 변경이 아니어야 한다.
▣ 강제 도구: 접두마다 「승격 전/후 화면 대조 N장」 · /api/config-management/list 가 200+본문 403 → 403 으로 바뀌는 순간을 [실측] 1건.
▣ 조율자 순차 작업(차선 아님). C·D 병합 뒤 시작 — Code 권고대로.
```

---

## §5 순서

1. **P-18 정정 적용**(세 줄 + .env.example) → 출생 표본(동시 시험 DuplicateDatabase 0) → **C∥D∥E2 동시 착수**
2. C 병합 · V → D 병합 · V → E2 병합 · V(첫 온보딩 %)
3. **A2 접두 한 개씩** (조율자) → V 화면 21 재촬영
4. **미측정 7 · 잠김 12 분류** → 두 수(상용 가중 · 손 안 도달율) 첫 보고
5. 시간이 남으면 E2E 23→28(신규 화면 5) — 품질 ⑥

## §6 보고 형식

- 첫 줄: 게이트 세 수 + 대기 + 빨강→초록 사유 + **병렬 여부(동시 N개 · DuplicateDatabase N건)** — 순차였으면 사유 한 줄.
- 계약: 「N% (눈 N) · 절 N(구현·잠김) · 상대방 대기 6절 · 커널 표 갱신 N」.
- **세 수**: 「상용 가중 N% (절 105) · 손 안 도달율 N% (분모 = 105 − 설계 잠금 N − 손 밖 N) · 온보딩 N% (48행 · 어긋난 행 N)」 + 8영역 표 + 「미착수 19 → N · 미측정 7 → N」.
- 차선: 차선별 석 줄 · 커밋 해시 · V 빨강 목록 · 충돌 N · 쓰기 면 대장 · 대응표 · 세종 오류 N · Code 오판 N.

## 대표께

- **상용 오픈 기준 완성율이 처음으로 영역별 실측으로 나왔습니다 — 59.9%.** 기능 82 · 보안 67 · 격리 75 · 운영 46 · 성능 33 · 품질 64 · 사용성 50 · 법·인증 20. 분모 105절 중 구현 67 · 미착수 19 · 잠김 12 · 미측정 7입니다. 잠김 12를 「하지 않기로 한 것(설계 잠금)」과 「조건이 열어 주는 것(손 밖)」으로 갈라, 우리가 닫을 수 있는 100%(손 안 도달율)를 따로 보이게 했습니다.
- 멀티 에이전트는 지난 턴 컨테이너 1대·시험 DB 1개 때문에 순차였습니다. 막는 것은 자원이 아니라 이름이어서 시험 DB 이름 등 넷을 차선별로 가르는 것으로 규약을 고쳤고, 이번 턴 C(웹 화면)·D(모바일)·E2(순회·HWPX·온보딩)를 동시에 띄웁니다.
- 제 오류 둘을 인정했습니다 — 봉투 스위치의 반경(11이 아니라 652)과, 온보딩 48행을 PDF에만 두어 Code가 못 읽게 한 것. 48행은 이번에 텍스트로 넘겼습니다.
- 대표 결정 셋 그대로: 알림 채널(문자/카톡/앱) · 운영 서버 작업 창 · HWPX 한컴 열림.

## 불변 제약 (변동 없음 + 1)

- 차선 에이전트에게 같은 글자로. `.env` 실제 값 금지(D-204 — `DB_TEST_NAME` 포함) · 권한 우회 목록 금지(D-103) · 격리 시험 비활성 금지(D-105) · 탐침 없는 쓰기 면 금지(P-8) · 무계정 링크 금지 · `_base_manager` 금지 · 죽은 인증 길 위 화면 금지(P-15) · dj-core/rj-core 직접 수정 금지(D-207) · 운영 서버 로그인 금지 · dry-run(D-209) · 구간 티켓·클립 잠금(11조) · 실제 화면+data_source(D-347·P-9) · 래칫(D-311) · lock(D-387) · 시험을 고쳐 초록 만들지 않는다(D-327) · 회색(P-12) · 거절≠도달(⑪) · 함수≠문(⑨) · 목록에 있다≠맞는다(⑥) · 대장 항목은 지우지 않고 사유를 바꾼다 · 상태·가용성 응답은 캐시를 지나지 않는다(P-19) · **시드는 규칙을 흉내 내지 않는다** · **스위치·기본값 변경은 반경 실측 먼저** · 조율자만 공용 등록부·병합·main 커밋 · 검증자는 색만 읽는다.
