# RESUME_NEXT.md — GuardianX 작업지시서 · 턴 AC

**WO-GX-20260921-04 파 1 턴 3** · 정본 지시서는 세종이 낸다. 이 문서는 **턴 AB 가 남긴 자리**다.
근거: `docs/workorders/WO-GX-20260921-04_report_wave1_turn1.md` · 규약 `docs/agent/checkpoints/turn-ab/_규약.md`

- 발행: **2026-09-21(기계 KST) · 턴 AB 마감** · 영실(CPM · 조율자)
- 직전 턴: 차선 **11** 동시 · 커밋 1 · **삭제 0** · §0.4 **0줄** · `--no-verify` **0**

## §0 첫 표

| 수 | 지금 [정본 · 턴 AB] | 비고 |
|---|---|---|
| 상용 /100 | **57.0** (절 155) | 턴 AA 54.1 · **+2.9** · 영역 ① 승격 5 |
| 닫힌 절 | **73/155** | 68 → 73 · 못 잼 77 → 71 |
| kind 표 정본 | **291** (절 155 + 별표 ② 136) | ★ 별표는 **8영역 밖** |
| 기능명세 포함 | **27.5 %** (분모 291) | 분모는 등재와 무관 · 분자만 움직인다 |
| 영역 ① | 도달 **18** · closed **15** · kind 15.50/39 | ★ **「도달 ≥ 24」는 반증됐다 — 정직한 천장 19** |
| FC / PR / CR | **62.5[2/3]** · **50.0[1/4]** · **18.75[8/8]** | CR 은 **잰 칸 8/8 · 회색 0** |
| MEASURED | **69/80** | 목표 ≥ 65 넘김 |
| 단위 시험 | **2,110 passed** | 1,935 → 2,110 · 0 failed |
| 타입검사 | **3234 = 기준선** · 우리 줄 0 | ★ `-p tsconfig.app.json` 으로만 잰다 |
| **evidence_chain** | ★ **rc 1 · 새 어긋남 6** | **턴 AC 첫 자리** |
| 커밋 | 턴 AB 커밋 1 · **push 0/4** | 대표 한 줄 |

## §0-1 V 가 잰 것 (보고 §15)

온보딩 **34.0/48**(U1 5.5 · U2 3.5 · U3 6.0 · U4 5.0 · U5 7.5 · U6 6.5 · **회색 0**) ·
48행 「누른 뒤」 **31/48** · 첫 10분 화면 **시험 장치 3/12**(목표 0 · **빨강**) · 손 안 **72.3 %**.

★★ **우리는 고객의 로캘로 한 번도 안 쟀다** — 판정기가 확인창 단추를 `Confirm` 영어로만 찾아
`ko-KR` 브라우저에서 로그인이 안 끝난다. **제품은 옳고 판정기가 틀렸다.**
**확인창을 지나야 하는 모든 측정**이 같은 그림자 아래 있었을 수 있다 — 고친 뒤 **다른 절의 색도 움직이는지** 본다.

## §0-2 ★★★ 턴 닫은 뒤에 찾았다 — **백업이 나흘째 실패하고 있다**

턴 AB 커밋 뒤 작업본에 남은 기계 산출물 셋을 보다가 찾았다[조율자 실측]:

```
docs/agent/evidence/D-373/backup_last.json
  verdict ALARM · RuntimeError: pg_dump 실패
  "server version: 18.1 … pg_dump version: 17.7 (version mismatch)"

이력 (커밋마다 그 판을 꺼내 봤다):
  09-18 ALARM · 09-19 ALARM · 09-20 ALARM · 09-21 ALARM   ← 나흘 · 다섯 턴
  그 앞은 UNKNOWN(09-15) · SKIPPED_UNDECLARED(09-06)
docs/agent/evidence/D-373/evidence_anchor_last.json  verdict **ALARM** (09-21 17:05)
```

**그런데 대장 `OPS-04`(백업)는 `status: 구현` · `kind: closed` 다** — 상용 점수에 **1.0 을 보태고 있다.**
★★ **그 절의 `note` 가 함정을 이미 적어 두었다**:
> 「★ 클라이언트 판이 서버 판과 맞아야 한다 — 앱 컨테이너의 pg_dump **17.7** 로는
>   서버 **18.1** 을 못 뜬다(실측). 그래서 **「어디서 뜨는가」가 인자다**.」

**적어 놓고, 그 인자 없이 도는 주기 작업이 나흘째 실패했고, 아무 색도 안 났다.**
사유는 **`gate:` 가 없다** — `proof:` 만 있다. **턴 AA 의 OPS-07 과 똑같은 모양이고 이번엔 백업이다.**

⇒ **턴 AC 의 0순위로 올린다**(위 「LAW-08 체인」보다 앞이다 — 체인은 증거가 안 남는 문제이고,
이쪽은 **복구할 것이 안 남는 문제**다).
① `OPS-04` 를 실물에 맞게 다시 적는다(`구현` 이 아니다) ② **`gate:` 를 붙인다** —
붙는 순간 조용할 수 없게 된다 ③ 주기 작업이 **pg_dump 18 이 있는 자리에서** 뜨게 고친다
④ `evidence_anchor_last` 의 ALARM 도 같이 본다.
⚠ **삭제·되돌리기가 아니므로 대표 결정이 아니다**(P-222) — 다만 **백업은 운영계 사실**이라 보고에 올린다.

## §1 턴 AC 의 순서 (보고 §13)

0. ★★★ **백업 ALARM 나흘째**(위 §0-2) — `OPS-04` 가 `closed` 인 채로 실패하고 있다.
0-b. ★★ **판정기 로캘 한 줄**(Q) — 위. 고치면 **다른 수도 같이 움직인다.**
1. ★ **LAW-08 체인** — `evidence_chain.py:774` 비대칭. **쓸 때마다 는다**(0→6). 한 차선이 고침·술어 둘 다.
2. **U4 사이드바** — 화면·권한 있는데 **가는 길이 없다.** `menu.Menu` 행 0건이라 `menuId` 를 못 지어냈다.
3. **청구 발급 순간 배선 다섯 자리** — 안 달면 새 씨앗이 다시 청구에 든다.
4. **「시드(검수용)」 셋째 낱말** — 청구·보고서가 같은 낱말 밭을 읽는다. 한 차선이 둘 다.
5. **캐시 A/A** — 눈금은 섰고 **팔 둘을 못 세웠다.** 제 창 + gunicorn 두 벌.
6. **회색 게이트 11** — **09-23 부터 빨강**(D-511). 면제가 아니라 **술어**가 필요하다.
7. **V 48행 `--measure`** — A 의 표로 **12절이 그대로 올라간다.** 가장 싼 한 수.

## §2 불변 — 턴 AB 가 더한 아홉 (보고 §12)

분모가 줄어 올라간 수는 수가 아니다 · 분모를 손으로 넣는 자기시험은 분모를 안 재는 자기시험이다 ·
**한 그물이 0건을 내면 「없다」가 아니라 「이 그물로는 안 잡힌다」이다** · 타입검사는 `-p tsconfig.app.json` 으로만 ·
지시도 입력이지 측정이 아니다(조율자에게도) · 로그인 창 없는 차선은 `--static` ·
「잰 적이 없다」와 「잰 것이 늙었다」는 다르다 · **동시 차선 수의 상한은 엔진이 정한다** · 거르지 못하면 말하게 한다.

## §3 세종 판정 청함 넷 (보고 §9)

① **CR 8칸의 합이 표 안에서 갈린다**(칸 합 1.5 ↔ 합 줄 2.0) — 반인 칸이 하나 더 있는가.
② `surveillance_*` 둘의 정본(지금 손해 0 이라 **조용히 안 맞췄다**).
③ **P-229 의 전제가 틀렸다** — 표 밖 역할에 실사용자 **8명**. 재배정할 것인가.
④ 별지 1호 문을 `monthly_report` 로 옮길 턴.

## §4 대표 — 손이 필요한 것

- **push** — 네 커밋. **두 턴째 분류기 거절 · 우회 0.** 보험 번들은 `_patches/`(저장소 밖).
- **Docker 자동 시작 + 재부팅** — 일곱 턴째. 이 턴에도 엔진이 죽었다.
- **VAPID 를 돌고 있는 서버에** — U3 가 금고에서 되살려 **훈련 도달 1** 을 냈으나
  `gx-gunicorn-e`·`gx-celery-e` 환경엔 아직 없다(**고객이 누르면 503**). 컨테이너 재생성이라 제 창이 필요하다.
- **09-28 가격 숫자 4** — 코드는 이미 **빈 설정 + 회색**으로 간다.

**대표께 갈 삭제·되돌리기 질문은 이 턴에 없었다.**
