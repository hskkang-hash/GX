# GuardianX 회신 — Gate 0 통과 확인 + WP-2 정정 반영·착수 승인
**발행** 2026-08-22(2차) · **대상** Claude Code
**★ 직전 RESUME_NEXT(WP-2 착수)를 대체한다.** D-242 프로토콜 유효: **붙여넣기 = §0 결정 확정 + WP-2 착수 승인.**
**너의 ENTRY 정정 4건은 전부 옳다. 수용한다.** 아래대로 정정해서 착수하라.

---

## 0. 먼저 — Gate 0 통과를 공식 확인한다 (중대한 이정표)

**GuardianX는 이 PC에서 살아난다.** dj-core 1.1.6 회수 → `manage.py check` 0 issues → 프론트 빌드 성공.
지금까지 12스프린트 0검증의 근본 원인(django.setup ImportError)이 **해소**됐고, 존속 가능성은 "시한부"에서 "회복 확인"으로 바뀌었다. D-245(probe DROP)·증거 커밋·시크릿 게이트 첫 작동(W0-1a)까지 전부 옳게 처리했다. 잘했다.

## 1. 결정 6건 추가 — decisions.yaml (너의 정정을 결정으로 승격)

```
D-246  티켓 ID 재부여 — W2-2/W2-3/W2-4 → W0-16/W0-17/W0-18
  - 사유: W2-2/3/4 는 정본에 AI 이벤트 센터(2단계 U1)로 이미 존재. 그대로 두면 U1 티켓 3개가 덮임.
  - AUTHOR-ERROR(작성자 오류 #14): 신규 티켓 부여 전 기존 정본 번호 미대조. (D-214 절차대로 기록)

D-247  R1 실행 순서 정정 — "역할 분리(W0-16)"를 "뷰 스코프(W0-14)" 앞으로
  - 사유: W0-14 현 구현이 superuser 우회를 그대로 복제함(tenant_scope.py:165 `if is_superuser: return`,
          tenant_filters.py:72, 그리고 _check·filter_by_group_field·filter_users_by_group·get_scoped_or_404 4곳).
          "전역 superuser = GAION 운영자 전용"을 먼저 정의하지 않고 뷰부터 고치면 두 번 고친다.
  - AUTHOR-ERROR(#15): 작성자가 뷰-우선 순서로 지정.

D-248  R3 재범위 — 상태코드는 소스 수정 불가, 응답 후처리 계층으로
  - 사유: 권한거부 200 은 core/role/permission.py:512 가 dict 반환해 생김 → §0.4 금지구역. 소스 수정 금지.
  - 정정: 응답 후처리 계층(미들웨어/렌더러)에서 200-body-403 → HTTP 403 으로 승격. 하위호환 계획 필수
          (기존 클라이언트가 200 을 기대할 수 있음 — 영향조사 → 단계적 전환/버전 플래그).
  - AUTHOR-ERROR(#16): 작성자가 저장소 코드로 고칠 수 있다고 가정.

D-249  WP-2 EXIT 판정 기준 정정 — 부착률이 아니라 "누출 0건"
  - 사유: 데코레이터는 표식일 뿐, 실제 좁힘은 핸들러가 함. 466(→실측 652) 라우트에 전부 붙여도 데이터는 샐 수 있음.
  - 정정: EXIT 격리 기준 = probe_tenant_isolation.py 재현 결과 **누출 0건** (현재 15·18·1건 → 0),
          격리 시나리오 **5/5** (현재 0/5). 라우트 수 실측값 652 로 갱신.
  - AUTHOR-ERROR(#17): 작성자가 부착률을 완결 기준으로 오정의 + 466 정적 추정.

D-250  P-LOCAL-2 픽스처 승인 — 활성화한다
  - 사유: 살리면 빨간불이 켜짐 = 결함을 "드러내는" 방향. 감추는 방향 아님. D-105 정신(격리시험 비활성 금지) 부합.
  - status: answered.

D-251  D-244 보강(긴급) — 운영서버 포트 노출 IP 제한 대상 추가
  - Code 확인: 115.21.49.115 의 9200(OpenSearch, 401 응답)·5601(Kibana 추정, 응답)이 인터넷에 열려 있음.
  - 조치: D-244 접근 IP 제한 대상에 9200·5601 포함. 자격증명 회전과 동급 긴급. 대표/운영자 수행.
```

## 2. 정정된 WP-2 "신뢰 봉인" — 이대로 착수

### R1 격리 봉인 (실행 순서 정정 — 역할 분리가 먼저)
```
W0-16  superuser 역할 분리 (D-243·D-247, 로컬/스테이징만) ── ★ 먼저
       (a) superuser 역할 보유 13계정 전수 목록화(테넌트별, 부여자/시각 unknown 표시)
       (b) "전역 superuser = GAION 운영자 전용" vs "고객 테넌트 운영 역할" 분리 정의  ← W0-14 의 전제
       (c) 고객 테넌트 계정에 "자기 테넌트 관리" 대체역할 부여
       (d) 대체역할 로그인 시 자기 테넌트만 보이는지 HTTP 시험
       (e) superuser 회수 절차서 작성 (운영 반영은 대표 승인 — Code 실행 안 함)
W0-14  뷰 레벨 테넌트 스코프 (역할 정의 후) ── 우회 4곳 교체
       · tenant_scope.py:165 / tenant_filters.py:72 / _check / filter_by_group_field /
         filter_users_by_group / get_scoped_or_404 의 "superuser 전부 통과"를
         "전역 superuser(W0-16 정의)만 통과, 나머지는 테넌트 스코프"로 교체
       · created_by IS NULL 도 소유 테넌트로 귀속. 파일럿 1개 → 통합시험 green 후 확대
W0-13  created_by 백필 (경로②) — 파괴적이므로 dry-run 리포트 먼저(D-209). 뷰 스코프와 독립
```

### R2 저하 운전
```
W0-17  외부 의존 부분 실패 격리 (실측: 외부 호출 29파일 / timeout 없는 49건)
       · 목록 API 가 스트리밍 상태조회 실패 시 500 대신 필드만 "일시 불가"+200
       · timeout 없는 49건에 타임아웃·폴백 부여. 시험: 외부 의존 다운 모킹 → 목록·이력·통계 200 유지
```

### R3 API 계약 정합 (재범위 — 후처리 계층)
```
W0-18  응답 후처리 계층 + 엔드포인트 문서 정정 (D-248)
       · 200-body-403 → HTTP 403 승격을 응답 후처리 계층에서(§0.4 소스 무수정). 하위호환 계획 첨부
       · /api/token/pair 미작동 → 문서를 실경로 /api/v1/auth/login 로 정정 + delete-session body 호출 수정
       · 계약 테스트(상태코드·엔드포인트) 추가
```

### EXIT 조건 (정정 — 누출 0건 기준. 전부 충족 시 제출하고 멈춘다)
```
1. probe_tenant_isolation.py 재현 누출 0건 (현재 15·18·1 → 0) · 격리 시나리오 5/5 (현재 0/5)   ← 부착률 아님
2. 대체역할 무중단 검증 완료 + superuser 회수 절차서 (운영 반영은 대표 승인 대기)
3. 외부 의존 다운 시 핵심 기능(목록·이력·통계) 200 유지 시험 green · timeout 없는 49건 처리
4. 응답 후처리 계층 + 하위호환 계획 + 계약 테스트 green + 연동 문서 갱신
5. 시크릿 0건 · §0.4 금지구역 무변경 · 파괴적 변경 전 dry-run 리포트 첨부
```

## 3. 불변 제약 (변함없음 — 위반 시 hard_stop)
D-103 권한우회목록 추가금지 · D-105 격리시험 비활성/수정/스킵 금지 · D-207 §0.4(dj-core/rj-core) 직접수정 금지 ·
D-209 파괴적 변경 dry-run 먼저 · D-204 .env* 실값 무기입 · D-002 백업 저장소 밖 · **운영 서버·운영 DB 직접 접속/변경 금지.**

## 4. 대표 승인란 (대표께 — 아래 한 줄)
| 파일 | 첫 줄에 기입 |
|---|---|
| `review/WP-2/ENTRY.md` | `APPROVED: 2026-08-22 강희석` |

## 5. 실행 순서
① §1 결정 6건 정서(D-246~251) — 커밋 1개 → ② WP-2 착수: R1(**W0-16 → W0-14 → W0-13**) → R2(W0-17) → R3(W0-18) →
③ EXIT 5조건(누출 0건 기준) 충족 시 제출하고 **멈춘다** → ④ 대표 EXIT 승인 후 U1~U4(가치 기능, W2-2/3/4 원래 티켓) 착수.

**긴급 병행(대표/운영자):** 자격증명 회전 + 9200·5601 포함 IP 제한(D-251) — 착수와 무관하게 지금.
