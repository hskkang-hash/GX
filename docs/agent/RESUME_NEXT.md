# RESUME_NEXT.md — GuardianX 다음 턴(AD) 착수 자리 · 조율자 갱신 2026-09-22

직전: **턴 AC** · 보고 `docs/workorders/WO-GX-20260922-05_report_wave1_turn1.md` · 지시서 `WO-GX-20260922-05_*.md`
전 턴 지시서의 차선 12 중 **셋만 돌았다** — 나머지는 §2 그대로 남아 있다.

## §0 첫 표 — 지금 [실측 2026-09-22]

| 수 | 값 | 비고 |
|---|---|---|
| 상용 /100 (`--static`) | **56.3** (57.0 에서 내려옴) | OPS-04 정직 재적 · **내려간 수가 정본** |
| 닫힌 절 | **72/155** · 못 잼 72 | |
| **백업 OPS-04** | `미착수 · unmeasurable` · **`gate:` 붙음 · rc 1(빨강)** | 회수증 1 섰다 · **주기 작업은 아직 죽어 있다** |
| **체인 LAW-08** | 비대칭 **제거** · 새 어긋남 **6 (안 늘었다)** | 옛 6 + 등재 23 은 등재만 |
| 회색 게이트(머리글) | **11** | 09-23 부터 빨강(D-511) |
| U4 사이드바 | **4줄** (`menuId` 143 · 144) | ⚠ **라이브 3002 는 아직 09-21 번들 — 배치 필요** |
| 판정기 로캘 | **`ko-KR` 기본** | U1 강제 회색 8/8 → 0/8 |
| 타입검사 | **3234 = 기준선** · 우리 파일 0 | `-p tsconfig.app.json` 으로만 |
| 안 움직인 수 | 온보딩 48 · 「누른 뒤」 31/48 · 영역 ① 16 · FC/PR/CR · 청구 배선 0/5 | **안 쟀으므로 안 적었다** |

## §1 턴 AD 순서

0. **★★ 창 2a (09-23 12:00 · 대표 승인됨)** — 백업 주기 작업을 **실제로 살린다.**
   갈래 ㉮ 워커 이미지 `postgresql-client` **18** (재생성) / ㉯ 주기 작업을 **호스트**로.
   ⚠ **`gx-celery-e` 안에는 docker 소켓도 CLI 도 없다**(실측) — P-236 의 `--via docker:postgres` 는
   **호스트에서만** 선다. 살아나면 `verify_backup_recovery.py` 가 **저절로 초록**이 되고 OPS-04 를 올린다.
   같은 창에서 `OPS-05`·`OPS-09`·`OPS-19`(`restore_drill_last.json` 09-19 이후 UNKNOWN)도 함께 본다.
1. **★ U4 사이드바를 라이브에 배치** — 지금 변경은 작업본에만 있고 **3002 는 09-21 번들**이다.
   배치 전에는 게이트도 사람도 그 4줄을 못 본다.
2. **회색 게이트 11** — 09-23 에 빨강이 된다. Q 가 남긴 분류 그대로:
   여섯은 **지금 잴 수 있는데 미배선**(deferred 아님 · 분모를 배선한다) ·
   `verify_seed_p20` 은 시간이 아니라 **결정 공백**(세종) · `verify_release_candidate`·`verify_perf_budget` 은 「언제」를 정해야 한다.
3. **안 돈 차선 일곱** — B(청구 배선 5) · U56(표 밖 8명 · `surveillance_*`) · U3(모바일 · VAPID 창 2b) ·
   N(승격 12 · 대장) · F(캐시 A/A) · A(영역 ① 천장 19) · K(카드 시험 6).
4. **첫 10분 화면 12 를 브라우저로** — U1 이 코드 경로는 0 으로 만들었으나 **띄워 보지 않았다**(회색).
   U24 가 쓴 「별도 포트 3003」 수법이 좋다 — 라이브를 안 건드리고 누른다.
5. **온보딩 48행 `--measure` (ko-KR)** — 로캘을 고쳤으니 **처음으로** 고객의 말로 잰다. 색이 움직이는 절을 전부 적는다.

## §2 ★ 차선 수 — 이 턴에 배운 것

> **상한을 정하는 것이 둘이다.** 엔진(동시 `docker exec` ≤ 8)과 **계정 주간 한도**.
> 엔진은 느려지는 것으로 말하고 **한도는 여덟이 한꺼번에 죽는 것**으로 말한다.
> **열둘을 반쯤 하는 것보다 셋을 끝까지 하는 것이 낫다.**

0·1순위는 **조율자가 직접** 쥔다. 나머지는 셋 안팎 · 가벼운 모델 · 「좁게, 끝까지」.

## §3 불변 — 이 턴이 더한 것
- **차선 수의 상한은 엔진과 한도가 함께 정한다.**
- **회수증 없는 백업은 백업이 아니다** — `gate:` 없는 `proof:` 는 증거가 아니다.
- **실패가 남긴 파일에 이름을 붙인다** — 0바이트 `.dump` 30개가 닷새를 「백업이 있다」고 말했다.
- **경보를 끄는 고침과 병을 고치는 고침은 다르다** — 체인은 「해시를 버린 뒤 값으로」가 아니라 **감싸서** 고쳤다.
- **불변은 적는다고 지켜지지 않는다** — 조율자가 턴 AB 에 세운 불변을 턴 AC 에 또 어겼다(§7).

## §4 세종 판정 청함 셋
① **부류 표에 「재서 빨강」 칸이 없다** — 못 잼 72 중 몇이 실은 재서 빨강인지 아무도 모른다
② P-236 을 창 2a 에서 ㉮ 로 하나 ㉯ 로 하나
③ 0바이트 덤프 30개의 처분 시점(지우는 것은 대표)

## §5 대표
- **재부팅 1회가 아직입니다** — 자동 시작 설정은 섰습니다(`Run` 키 · `autoStart=True`).
  그러나 uptime **4일 4시간**이라 PR 「재부팅 아침」 칸이 못 섭니다.
- **09-23 12:00 창 2a** 승인 받았습니다 — 그 창에서 **백업이 저절로 도는 것**을 세웁니다.
- 09-28 가격 숫자 4.
- **이 턴 삭제 결정 0건.**
