# GuardianX 회신 — WP-2 진척 확인 + 판정 4건 + 다음 순서
**발행** 2026-08-22(3차) · **대상** Claude Code
**★ 직전 RESUME_NEXT(WP-2 정정·착수)를 대체한다.** D-242 프로토콜 유효: **붙여넣기 = 판정 4건 확정 + 계속 진행 승인.**
질문에 답한다: **위 판정을 여기서 전부 주니, 배선 수정 → W0-13 → 652 확대 → W0-18 순으로 이어가라.**

---

## 0. 진척 확인 — 파일럿에서 누출이 실제로 0이 됐다

**이번이 상품화의 분수령이다.** 12스프린트 동안 못 하던 "격리를 증거로 닫는 것"을 파일럿 1개 라우트에서 실측으로 해냈다.

| 계정 | 착수(전) | W0-16 후 | 지금(W0-14 후) |
|---|---|---|---|
| man (Anyang·회수완료) | 15 | 1 | **0** |
| son (레거시 역할 보유) | — | 15 | **플래그 False → 0** |
| admin (Gaion·전역역할) | 18 | 18 | 18 (누출 아님 — 정의상 전역) |

- **W0-14(12468e6)**: `tenant_filters.py` 지역 `is_superuser()` 삭제 → 판정을 `tenant_roles.is_global_admin()` 한 곳으로 일원화. D-247 순서(판정 먼저) 옳았다. 파일럿 `GET /api/stream-monitors/stream-monitors`에 `@tenant_scoped()` + 명시 group 필터.
- **W0-17(b819fa6, done)**: 스트리밍 서버 죽인 상태에서 목록 API 500→200(`stream_status:"unavailable"`), 이력·통계·드론목록 200 유지 실측. before도 커밋본으로 되돌려 대조. pre-commit 훅 + 단위시험 11건 회귀 게이트.
  - **AUTHOR-ERROR #18**: 지시서의 "timeout 없는 49건"은 과대추정(grep 이름 매칭). AST 실측 17건(나머지는 redis14·OpenSearch2·Django테스트클라 5·dict1). 17→0 처리됨. (D-214 기록)

## 1. 판정 4건 — decisions.yaml 추가

```
D-252  격리 즉시 통제 = 플래그 우선 (중대 발견)
  - 발견: 역할 회수 없이 TENANT_TRUST_LEGACY_SUPERUSER=False 한 줄로 son 누출 15→0.
  - 결정: 격리 1차 통제는 "플래그"로 한다(즉시·가역·업무 무중단). 13계정 역할 회수(W0-16)는
          위험한 선행조건이 아니라 뒤따르는 "정리 작업"으로 재순위 → D-243 무중단 우려 대부분 해소.
  - 단서: 운영 플래그 전환은 W0-13 백필 완료 후(아래 D-254 — orphan 손실 방지). 로컬/파일럿은 이미 검증됨.

D-253  P-W0-14-2 승인 — 격리 시험 "배선"만 수정 (A안)
  - 상황: D-249 EXIT "시나리오 5/5"가 판정 자체 불가(404 4건·401 1건·필수 FK 24건으로 판정 전 죽음).
  - 결정: 배선·픽스처(실경로·메서드·필수필드)만 수정 승인. 격리 "단언(assert)"은 무변경 — D-105 준수.
          Code 가 근거(실경로·메서드·필수필드)를 전부 갖춰 뒀으니 그대로 배선만 고쳐 판정 가능 상태로.

D-254  P-W0-14-3 승인 — 652 확대 전 W0-13 백필 선행
  - 상황: 스코프 적용 시 group 빈 13건(33%)이 비전역 계정 화면에서 사라짐(orphan 손실).
  - 결정: created_by/group 백필(W0-13)을 652 확대 전에. 파괴적이므로 dry-run 리포트 먼저(D-209).

D-255  P-W0-17-1 승인 — 외부 의존 저하운전 분리 티켓 신설
  - Redis·OpenSearch·MinIO 저하운전을 신규 티켓 W0-19(P1)로 분리. W0-17(스트리밍)은 done 유지.
```

## 2. 다음 실행 순서 (이대로 이어가라)

```
① W0-14b  격리 시험 배선 수정 (D-253) — EXIT 를 "판정 가능" 상태로. 단언 무변경.
           목표: probe_tenant_isolation.py 5개 시나리오가 죽지 않고 실제 판정까지 도달.
② W0-13   created_by/group 백필 (D-254) — dry-run 리포트 먼저(D-209) → 대표 확인 후 적용.
           목표: orphan 33% 해소 → 플래그 전환·652 확대의 선행조건 충족.
③ W0-14c  652 라우트로 스코프 확대 — 파일럿 방식(@tenant_scoped + group 필터)을 전 라우트에.
           목표: 재현 누출 전 계정 0건, 격리 시나리오 5/5.
④ W0-18   API 계약 (D-248) — 응답 후처리 계층에서 200-body-403 → HTTP 403(§0.4 소스 무수정)
           + 하위호환 계획. /api/token/pair 문서를 실경로로 정정 + delete-session 수정 + 계약 테스트.
⑤ 신규 W0-19 는 WP-2 EXIT 후 별도(P1). 지금 착수 금지.
```

**W0-18 착수 질문 답:** ④ 순서로 한다 — 격리 체인(①②③)을 EXIT 판정 가능 상태까지 닫은 뒤 W0-18. 격리가 P0 입찰 자격의 핵심이고 판정 4건이 거기 몰려 있어서다. ①~④는 한 WP-2 안에서 연속 진행, EXIT는 넷 다 충족 후 한 번.

## 3. WP-2 EXIT (변함없음 — 누출 0건 기준)
```
1. 재현 누출 0건(전 계정) · 격리 시나리오 5/5 (배선 수정 후 판정 가능)
2. 대체역할/플래그 무중단 검증 + superuser 회수 절차서 (운영 반영은 대표 승인 대기)
3. 외부 의존(스트리밍) 다운 시 핵심 200 유지 — done(W0-17)
4. 응답 후처리 계층 + 하위호환 계획 + 계약 테스트 green + 연동 문서 갱신 (W0-18)
5. 시크릿 0건 · §0.4 무변경 · 파괴적 변경 dry-run 첨부
```

## 4. 불변 제약 (변함없음)
D-103 · D-105(격리 단언 무변경 — 배선만) · D-207 §0.4 · D-209 dry-run 먼저 · D-204 · D-002 · 운영 서버·DB 직접 변경 금지.

## 5. 대표 승인·병행
- 이 문서 붙여넣기 = 판정 4건 확정 + 계속 진행 승인. (별도 ENTRY 재승인 불필요 — WP-2 진행 중)
- **긴급 병행(대표/운영자, 착수 무관·지금):** 자격증명 회전 + 9200·5601 포함 IP 제한(D-251).
- **좋은 소식:** D-252로 13계정 즉시 회수는 불필요 — 플래그가 먼저 닫는다. 회수는 뒤에 여유 있게.
