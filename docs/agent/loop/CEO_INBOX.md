# CEO_INBOX — 대표가 답하는 자리 (한 줄씩 · 최신이 위 · `[ ]` → `[x]` 또는 문장)

> 세종·영실이 질문을 올리면 아래 형식으로 한 줄이 생긴다. 대표는 `[ ]` 를 `[x]` 로 바꾸거나 그 줄 끝에 문장을 적는다.
> 감시기가 5분 안에 읽어 영실에게 넘긴다. **값(비밀)은 여기 적지 않는다** — 금고 경로만.

## 열린 질문
- [ ] 2026-09-24 · 세종 · **감시기 등록(P-340 · 손 · 루프가 서는 유일한 자리)** — 관리자 PowerShell 한 줄: `schtasks /Create /TN "GuardianX-Yeongsil" /TR "powershell -NoProfile -ExecutionPolicy Bypass -File C:\GuardianX\guardianx-source\scripts\loop\yeongsil_watch.ps1" /SC ONLOGON /RL HIGHEST /F` → `schtasks /Run /TN "GuardianX-Yeongsil"` · `docs\agent\loop\logs\watch.log` 가 생기면 `[x]` — 등록 뒤 09-25 08:00 에 헤드리스가 턴 AI 를 연다(오늘 22:00 지나 착수 없음)
- [ ] 2026-09-24 · 세종 · **창 2b 문장 정정(P-335 · 세종 오류 ⑯ · 결정 ④)** — VAPID 3 은 턴 AE 창 2b(709a5aa)에 이미 실렸다(세종이 옛 창 표를 옮겨 적었다 · 자진). 대표 문장: **「창 2b 는 SMTP 5(금고 없으면 Mailpit 다섯) · WEBHOOK_SIGNING_KEYS · .env 세 줄 · 시험 DB — VAPID 는 빼라」** · 남은 VAPID 몫은 실기기 도달 1(대표 크롬 · 대화형 창)
- [ ] 2026-09-24 · 세종 · **09-26 14:00 대화형 창(P-324 · 손)** — 턴 AI 는 헤드리스로 착수하고 창 2b 는 턴 밖이다(LOOP_PROTOCOL §3). 14:00 에 대표 PC 에서 대화형 Claude Code 한 창 · 「창 2b 열어라 · 준비 표대로」 · 결정 ①②④⑤ 이 그때까지 이 파일에 있어야 창이 선다 · 열었으면 `[x]`
- [ ] 2026-09-24 · 세종 · **push 대기(P-337 · 손)** — 대표 push 뒤 로컬 1(`32e6ca5`) + 착수 첫 커밋(미커밋 9) → 조율자 표 뒤 `git push origin turn-q` · 올렸으면 `[x]`
- [ ] 2026-09-24 · 영실 · **시험 DB 「표 그대로」가 가리키는 것** — 09-18 표의 넷 중 **둘은 이미 없고**(전량 시험이 제 이름을 만들고 스스로 지운 것 · 몰래 지운 것 아님) **둘만 남았다**(`test_gx_lane_u24` · `test_gx_u3b`). 표 밖에 **14개**(462 MB · 전부 연결 0 · 다툼 가드 `_p<pid>` 찌꺼기 5 · 차선 이름 9)가 더 있다. 「**둘만**」이면 문장 그대로 · 「**열여섯 전부**」면 새 표 `evidence/P-168/창2b_사전점검_20260924.md` §3 그대로 지운다
- [ ] 2026-09-24 · 세종 · **시험 DB(P-329 · 결정 ①)** — 세종 권고 「열여섯 전부」(연결 0 · 시험 자료 · 차선이 다시 만든다 · 찌꺼기는 두면 는다). 대표 문장: **「열여섯 전부 · 사전점검 §3 이 새 표 · 창 안에서 연결 0 재확인 뒤 지워라」** (둘만이면 「둘만」)
- [ ] 2026-09-24 · 영실 · **비밀번호 찾기 메일을 Mailpit 으로** — `gx-gunicorn-e` 를 `SMTP_SERVER=mailpit` · `SMTP_PORT=1025` · `SMTP_TLS=0` 로 **재생성**(창 절차). 지금 그 다섯 값은 컨테이너를 만들 때 들어간 실키라 밖에서 못 바꾼다. ★ 조율자 정정: 처음에 `EMAIL_HOST` 세 줄이라고 올렸는데 **틀렸다** — dj-core 는 `django.core.mail` 을 안 쓰고 `SMTPEmailBackend` 이 `os.getenv` 로 `SMTP_*` 를 읽는다(실측). 재생성하라면 `[x]` · 창 2b 에 붙이시려면 「2b 에 붙여라」
- [ ] 2026-09-24 · 세종 · **비번 찾기 SMTP(P-328 · 결정 ④와 한 줄)** — 세종 판정 「창 2b 에 붙인다 · 금고 smtp.env 있으면 실값 5 · 없으면 Mailpit 다섯(mailpit·1025·TLS 0·빈 계정)으로 재생성 → Mailpit 1통으로 한 바퀴 닫고 실값 오는 날 다음 창」. 대표 문장: **「2b 에 붙여라 · 금고 없으면 Mailpit 다섯으로」**
- [ ] 2026-09-24 · 영실 · **K2 경보 메일**(위와 다른 문) — 이쪽은 장고 `send_mail` 이라 `backend/.env` 에 `EMAIL_HOST=mailpit` · `EMAIL_PORT=1025` · `EMAIL_USE_TLS=False` 를 넣고 재기동하면 선다(`docker-compose.yml` 628~630줄이 이미 그 세 줄을 적어 뒀다). 「수신자 0 테넌트 Mailpit 실채널 1통」이 이 문이다. 넣으라면 `[x]`
- [ ] 2026-09-24 · 세종 · **K2 `.env` 세 줄(P-327 · 결정 ⑤)** — `.env` 쓰기는 대표 손이라 창 2b 안에서(E · 대화형). 세종 권고 `[x]` — 위 영실 줄을 `[x]` 로
- [ ] 2026-09-24 · 영실 · **「보냈습니다」가 거짓말을 한다** — `forgot_password` 가 발송 실패를 `except` 로 삼키고 **성공 응답을 그대로** 돌려준다(§0.4 dj-core 라 못 고침). 지금 화면은 한 통도 안 나가는데 「메일 보냈습니다」라고 말한다. P-274 처럼 **길목에서** 잡을까요 — 잡으라면 `[x]` · 「Mailpit 붙고 다시 보자」도 답이다
- [ ] 2026-09-24 · 세종 · **「보냈습니다」 길목(P-325 · 세종 판정 · 대표 확인만)** — 길목에서 잡되 **주소를 보기 전에** SMTP 도달성(연결 시험 1회 · 60초 캐시)을 본다 → 안 되면 있는 주소·없는 주소 같은 답 + 로그 + K2 경보(있는 주소에서만 실패가 보이면 계정 존재가 샌다). 시험 3(있는 · 없는 · SMTP 죽음 = 같은 본문). 다른 뜻이면 이 줄에
- [ ] 2026-09-24 · 영실 · **재설정 링크 주소** — 메일에 실리는 주소가 `AdminConfig('System').settings['frontendUrl']` 인데 그 **칸이 없어** 기본값 `http://localhost:3001`(아무도 안 듣는 포트)로 나간다. `http://localhost:8500` 을 넣으면 링크가 산다(DB 한 칸 · 되돌리기 쉬움). 넣으라면 `[x]` · 다른 주소면 그 주소를 적음
- [ ] 2026-09-24 · 세종 · **`frontendUrl`(P-326 · 세종 판정 · 대표 확인만)** — 8500(대표가 브라우저로 여는 그 주소)을 넣는다 · 스테이징 오면 같은 칸을 `https://<도메인>` 으로 · 게이트에 「메일 링크 host == 설정 칸」 1행. 다른 주소면 이 줄에
- [ ] 2026-09-24 · 영실 · **`pk=6610` 카메라 표식** — `GX-ONB-V-20260922T124833`. 온보딩 계측기 잔여물인데 **표식이 아무 데도 없어** 격자에 남았고(14→5 중 하나), **지금 청구에 드는 유일한 온보딩 카메라**다. 곁표에 `probe` 한 줄을 적으면 화면·청구에서 함께 빠진다 — **청구 근거 변경**이라 여쭙는다. 이름으로 추측해 빼는 길은 **안 쓴다**(고객이 같은 이름을 쓰는 날 고객 카메라가 사라진다). 적으라면 `[x]`
- [ ] 2026-09-24 · 세종 · **`pk=6610`(P-330 · 결정 ③)** — 세종 권고 「probe 표식 적는다」(온보딩 계측기가 만든 것 · 곁표 한 줄이라 되돌리기 쉬움 · 청구는 덜 받는 쪽으로 · 훈련·탐침은 0원 줄 · S6 첫 실측). 대표 문장: **「probe 표식 적어라 · 청구서엔 0원 줄로 남겨라」**
- [ ] 2026-09-24 · 영실 · **`gx-portfwd` 컨테이너** — 3002 를 밖으로 내보내려고 급히 세운 **다리**다. P-286 으로 8500 한 주소가 섰으니 쓸 일이 없다. 삭제는 대표 결정이라 안 지웠다. 지우라면 `[x]` · 3002 와 8500 대조용으로 더 두시려면 「두어라」
- [ ] 2026-09-24 · 세종 · **`gx-portfwd`(P-331 · 결정 ②)** — 세종 권고 「지운다」(8500 정본 · 3002 참고 · 되돌리기 = docker run 한 줄 · 두면 재는 자리를 또 헷갈린다). 대표 문장: **「gx-portfwd 지워라 · 되돌리기는 docker run 한 줄 · 대장에 적어라」** (두시려면 「두어라」)
- [ ] 2026-09-24 · 세종 · **스테이징** — 「스테이징 왔다. 공인 IP … · 도메인 … · 키 경로 …」 오면 이 줄에 세 항목(값 아님 · IP·도메인·금고 경로) — 대표 09-24: 「오면 그 줄에」(아직 안 옴)
- [ ] 2026-09-24 · 세종 · **SMTP 5값** — 금고 `C:\GuardianX-vault\smtp.env` 에 넣었으면 `[x]` — ★ 조율자 확인 09-24 21:1x: `smtp.env` **아직 없다**. 09-26 13:50 까지 없으면 창 2b 에서 **SMTP 만 빠진다**(없는 값을 지어내지 않는다)

## 닫힌 질문
- [x] 2026-09-24 · 영실 · **스모크를 커밋 훅에 안 붙였다** — 세종 P-338: 옳다 · 재시작 뒤가 자리다 · 훅은 옛 코드를 잰다
- [x] 2026-09-24 · 영실 · **스모크 전용 계정 여부** — 세종 P-339: 만든다(`gx-smoke` · view_only · `is_probe` · V_LOCK 시 회색) · 대표 확인 불필요
- [x] 2026-09-24 · 세종 · **push** — 대표 완료(`785978f..62ab7d4 turn-q -> turn-q` · 태그 `rc-1-internal` → `290b603`) · 조율자 `git ls-remote` 로 원격 == 로컬 확인 · 미push 0
- [x] 2026-09-24 · 세종 · **창 2b 문장** — 대표 09-24: 「09-26 14:00 창 2b 열어라 · VAPID 3 · SMTP 5 · WEBHOOK_SIGNING_KEYS 이름 · 시험 DB 넷 지워라(표 그대로)」 · ⚠ 조율자 사전 점검(`evidence/P-168/창2b_사전점검_20260924.md`): VAPID 는 턴 AE(`709a5aa`)에 **이미 실림**(두 통 == `.env.vapid` · 다시 만들지 않는다 · 남은 것은 실기기 도달 1) · SMTP 금고 없음 · 시험 DB 표 어긋남(위 열린 줄) · **창은 헤드리스가 못 연다 — 14:00 대표 PC 에 대화형 세션 한 창**(LOOP_PROTOCOL §3)
- [x] 2026-09-24 · 영실 · **HUP → restart**(턴 AH 보고 §6 ㉠) — 대표 09-24: 「HUP 는 restart」 · 세종 P-321 과 같다 · `headless.settings.json` 에 `docker restart gx-gunicorn-e` · `gx-celery-e` 두 줄(세종 반영 · 미커밋)
- [x] 2026-09-24 · 세종 · **자동 로그인**(재부팅 세 조각 ③) — 대표 09-24: 「**손 재부팅 유지**」 · 아침 N/10 은 대표가 손으로 재부팅한 날만 +1(P-305)
- [x] 2026-09-24 · 세종 · `-old` 컨테이너 셋 — 「지워라 · 표 먼저 · 로그 상한 붙여 재생성」 (집행 완료 · 아침 2/10)
