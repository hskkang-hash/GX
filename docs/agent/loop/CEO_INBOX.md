# CEO_INBOX — 대표가 답하는 자리 (한 줄씩 · 최신이 위 · `[ ]` → `[x]` 또는 문장)

> 세종·영실이 질문을 올리면 아래 형식으로 한 줄이 생긴다. 대표는 `[ ]` 를 `[x]` 로 바꾸거나 그 줄 끝에 문장을 적는다.
> 감시기가 5분 안에 읽어 영실에게 넘긴다. **값(비밀)은 여기 적지 않는다** — 금고 경로만.

## 열린 질문
- [ ] 2026-09-24 · 영실 · **비밀번호 찾기 메일을 Mailpit 으로** — `gx-gunicorn-e` 를 `SMTP_SERVER=mailpit` · `SMTP_PORT=1025` · `SMTP_TLS=0` 로 **재생성**(창 절차). 지금 그 다섯 값은 컨테이너를 만들 때 들어간 실키라 밖에서 못 바꾼다. ★ 조율자 정정: 처음에 `EMAIL_HOST` 세 줄이라고 올렸는데 **틀렸다** — dj-core 는 `django.core.mail` 을 안 쓰고 `SMTPEmailBackend` 이 `os.getenv` 로 `SMTP_*` 를 읽는다(실측). 재생성하라면 `[x]` · 창 2b 에 붙이시려면 「2b 에 붙여라」
- [ ] 2026-09-24 · 영실 · **K2 경보 메일**(위와 다른 문) — 이쪽은 장고 `send_mail` 이라 `backend/.env` 에 `EMAIL_HOST=mailpit` · `EMAIL_PORT=1025` · `EMAIL_USE_TLS=False` 를 넣고 재기동하면 선다(`docker-compose.yml` 628~630줄이 이미 그 세 줄을 적어 뒀다). 「수신자 0 테넌트 Mailpit 실채널 1통」이 이 문이다. 넣으라면 `[x]`
- [ ] 2026-09-24 · 영실 · **「보냈습니다」가 거짓말을 한다** — `forgot_password` 가 발송 실패를 `except` 로 삼키고 **성공 응답을 그대로** 돌려준다(§0.4 dj-core 라 못 고침). 지금 화면은 한 통도 안 나가는데 「메일 보냈습니다」라고 말한다. P-274 처럼 **길목에서** 잡을까요 — 잡으라면 `[x]` · 「Mailpit 붙고 다시 보자」도 답이다
- [ ] 2026-09-24 · 영실 · **재설정 링크 주소** — 메일에 실리는 주소가 `AdminConfig('System').settings['frontendUrl']` 인데 그 **칸이 없어** 기본값 `http://localhost:3001`(아무도 안 듣는 포트)로 나간다. `http://localhost:8500` 을 넣으면 링크가 산다(DB 한 칸 · 되돌리기 쉬움). 넣으라면 `[x]` · 다른 주소면 그 주소를 적음
- [ ] 2026-09-24 · 영실 · **`pk=6610` 카메라 표식** — `GX-ONB-V-20260922T124833`. 온보딩 계측기 잔여물인데 **표식이 아무 데도 없어** 격자에 남았고(14→5 중 하나), **지금 청구에 드는 유일한 온보딩 카메라**다. 곁표에 `probe` 한 줄을 적으면 화면·청구에서 함께 빠진다 — **청구 근거 변경**이라 여쭙는다. 이름으로 추측해 빼는 길은 **안 쓴다**(고객이 같은 이름을 쓰는 날 고객 카메라가 사라진다). 적으라면 `[x]`
- [ ] 2026-09-24 · 영실 · **`gx-portfwd` 컨테이너** — 3002 를 밖으로 내보내려고 급히 세운 **다리**다. P-286 으로 8500 한 주소가 섰으니 쓸 일이 없다. 삭제는 대표 결정이라 안 지웠다. 지우라면 `[x]` · 3002 와 8500 대조용으로 더 두시려면 「두어라」
- [ ] 2026-09-24 · 세종 · **push** — `git push origin turn-q` 하셨으면 `[x]` (대기 4커밋 · 따라하기 ②)
- [ ] 2026-09-24 · 세종 · **창 2b 문장** — 「09-26 14:00 창 2b 열어라. VAPID 3 · SMTP 5 · WEBHOOK_SIGNING_KEYS 이름 · 시험 DB 넷 지워라(표 그대로). 되돌리기 10분 예비.」 그대로면 `[x]` · 일시가 다르면 문장을 고쳐 적음
- [ ] 2026-09-24 · 세종 · **스테이징** — 「스테이징 왔다. 공인 IP … · 도메인 … · 키 경로 …」 오면 이 줄에 세 항목(값 아님 · IP·도메인·금고 경로)
- [ ] 2026-09-24 · 세종 · **SMTP 5값** — 금고 `C:\GuardianX-vault\smtp.env` 에 넣었으면 `[x]`
- [ ] 2026-09-24 · 세종 · **자동 로그인**(재부팅 세 조각 ③) — 켬 + 화면 잠금이면 `[x]` · 안 켜면 「손 재부팅 유지」

## 닫힌 질문
- [x] 2026-09-24 · 세종 · `-old` 컨테이너 셋 — 「지워라 · 표 먼저 · 로그 상한 붙여 재생성」 (집행 완료 · 아침 2/10)
