# 창 2b 준비 표 — 09-26 14:00 · 대표 대화형 창 (P-324 · 턴 AI E 몫 · 조율자)

| 항목 | 값 |
|---|---|
| 누가 | **대표 PC 의 대화형 Claude Code 한 창**(영실 · 집행자). 헤드리스는 못 연다(LOOP_PROTOCOL §3 · P-324) |
| 언제 | 09-26 14:00~14:30. 13:50 에 도는 차선·게이트를 멈춘다. 창 동안 **아무도 재지 않는다** |
| 기초 | `docs/agent/evidence/P-168/창2b_사전점검_20260924.md`(09-24 21:1x) · `docs/agent/runbook/창2b_20260923_웹푸시새쌍.md`(재생성 걸음 · ㉡~㉥) · WO-13 P-327~P-331 · P-335 |
| 대표 결정 | CEO_INBOX 열린 줄 ①②③④⑤ — **창 전에 답이 있어야 그 걸음을 한다.** 답이 없는 걸음은 건너뛰고 「결정 없음」으로 적는다 |
| 되돌리기 | 옛 통은 **이름만 비킨다**(`-old-2c`) · 10분 안에 되돌림 · 지운 것은 결정 ①② 뿐 |
| 닫는 조건 | 두 통 새로 섬 · 건강 200 · celery ready · `restart_live` 초록 · 비번 찾기 Mailpit 1통 · K2 Mailpit 1통 · 실기기 웹푸시 1(대표 크롬) · 되돌리기 0 |

값은 이 문서에 **0** 이다. 이름·길이·sha256 앞 12자만 적는다.

## §0 창 전 사실 [실측 09-24 22:1x · 읽기만]

| 사실 | 실측 | 뜻 |
|---|---|---|
| 두 통 | `gx-gunicorn-e` · `gx-celery-e` — `:pg18` · `unless-stopped` · 로그 `json-file 10m×5` · 환경 **53** 이름 | 재생성할 때 로그 상한을 **꼭** 다시 준다(OPS-07-a · 턴 AE 창 2b 에서 빠뜨린 빚) |
| 마운트 | gunicorn **1**(`/app`) · celery **5**(`/app` · `/repo/backend` · `/repo/scripts` · `/backup` · `/docs/agent/evidence/D-373`) | 그대로 |
| VAPID | 두 통 == `.env.vapid`(09-23 새 쌍) | **건드리지 않는다**(P-335 · 새 쌍 = 구독이 또 끊긴다) |
| 비번 찾기 SMTP | dj-core `SMTPEmailBackend` 이 읽는 이름은 **여섯**: `SMTP_SERVER` · `SMTP_PORT` · `SENDER_EMAIL` · `SMTP_USERNAME` · `SMTP_PASSWORD` · **`SMTP_TLS`(기본 "1" = STARTTLS)** | 두 통에 다섯은 있다(옛 생성값) · `SMTP_TLS` 는 **없다** → Mailpit 으로 가려면 `SMTP_TLS=0` 을 **새로** 넣어야 닿는다. 「SMTP 5」가 실제로는 이름 여섯이다 |
| 금고 `smtp.env` | **없다** | 13:50 까지 없으면 결정 ④ 의 「Mailpit 다섯」 갈래 |
| Mailpit | `guardianx-source-mailpit-1` healthy · `gx-main-network` 별칭 **`mailpit`** | 두 통에서 `mailpit:1025` 로 닿는다 |
| K2 경보 메일 | 장고 `send_mail` → `EMAIL_HOST` · `EMAIL_PORT` · `EMAIL_USE_TLS` | ★ 두 통에 **`EMAIL_HOST` 가 없다** — 두 통은 `backend/.env` 가 아니라 금고 env-file 로 만들어졌다. **`backend/.env` 에만 넣으면 안 닿는다.** 재생성 env-file 에도 세 줄 |
| `WEBHOOK_SIGNING_KEYS` | 두 통 · 금고 모두 없음 · 모양 `sdn=<값>,agency=<값>`(`settings.py:1322`) | 창 안에서 만든다 |
| 시험 DB | 16개 · 전부 연결 0(09-24 21:1x) | 결정 ① |
| `gx-portfwd` | Up · `guardianx-backend:latest` | 결정 ② |

## §1 걸음 — 순서대로. 각 걸음의 확인이 서기 전에 다음으로 가지 않는다

| # | 걸음 | 확인 | 되돌리기 |
|---|---|---|---|
| ⓪ | 13:50 차선·게이트 정지 · `V_LOCK` 없음 확인 · `git status` 깨끗 · 태그 `pre-window-2b` | 미커밋 0 | — |
| ① | **백업 한 벌**: `pg_dump` → 금고(`C:\GuardianX-vault\gx_pre2b_20260926.dump`) | 크기 > 0 · 표 수 | — |
| ② | **값 만들기(금고로 곧장 · 화면 0)**: `WEBHOOK_SIGNING_KEYS` = `sdn=`·`agency=` 각 `secrets.token_urlsafe(32)` → `C:\GuardianX-vault\webhook_signing.env` · 결정 ④ 가 실값이면 `smtp.env` 읽기 | 이름 · 길이 · sha 앞 12자만 출력 | 파일 지움(아직 아무 데도 안 실렸다) |
| ③ | **env-file 둘 뜨기**(스크래치 · 저장소 밖): `docker inspect` 의 `Config.Env` − 이미지가 주는 여섯 이름 + 아래 표의 바꿈 | 줄 수 = 53 − 6 + 새 이름 수 · 값 화면 0 | 파일 지움 |
| ④ | 옛 통 **이름만 비킴**: `docker stop X && docker rename X X-old-2c` (둘) | `-old-2c` 둘 Exited | `docker rename X-old-2c X && docker start X` |
| ⑤ | 새 통: `docker run -d --name X --entrypoint python --env-file <③> --restart unless-stopped --network gx-main-network --log-opt max-size=10m --log-opt max-file=5` + §0 마운트 그대로 + 이미지 `:pg18` + Cmd 그대로(창 2b 실행서 §0 표) · ⚠ `MSYS_NO_PATHCONV=1` · ⚠ `-e KEY=VALUE` 금지 | — | ④ 의 되돌리기 |
| ⑥ | `python scripts/restart_live.py --reason "창 2b"` 대신 **확인만**: 건강 200 · celery ready · `verify_live_code` · `smoke_live` | 넷 다 초록 | ④ |
| ⑦ | 비번 찾기 한 바퀴 → **Mailpit 1통**(`http://localhost:8025` 또는 gx-shell 안 API) · 링크 host == `frontendUrl`(P-326 · 8500) | 1통 · host 일치 | — |
| ⑧ | K2 경보 → **Mailpit 1통**(수신자 0 테넌트 실채널) | 1통 | — |
| ⑨ | 결정 ① 시험 DB: 연결 0 **다시** 확인 → `DROP DATABASE` 목록 그대로(사전점검 §3) | 남은 시험 DB 0(또는 「둘만」이면 14) | 없음(그래서 대표 결정) |
| ⑩ | 결정 ② `gx-portfwd`: `docker inspect` 를 금고에 뜬 뒤 `docker rm -f` · 대장에 「임시 다리 · 09-26 제거 · 되돌리기 한 줄」 | 사라짐 | 뜬 inspect 로 `docker run` 한 줄 |
| ⑪ | **실기기 웹푸시 1** — 대표 크롬으로 8500 로그인 → 알림 받기 → 「내 기기로 한 통」 → `[훈련]` 제목이 뜨는가 | 도달 1 | — (훈련 채널만) |
| ⑫ | 임시 env-file 지움 · `-old-2c` 둘 **살려 둠**(지우는 것은 다음 결정) · 창 기록 | 삭제는 ⑨⑩ 뿐 | — |

### env-file 바꿈 표 (두 통 같음 · 값 0)

| 이름 | 지금 | 창 뒤 | 근거 |
|---|---|---|---|
| `SMTP_SERVER` | 있음(12자) | 실값 또는 `mailpit` | 결정 ④ · P-328 |
| `SMTP_PORT` | 있음 | 실값 또는 `1025` | 〃 |
| `SENDER_EMAIL` | 있음 | 실값 또는 `noreply@guardianx.local` | 〃 — Mailpit 은 보낸 주소를 따지지 않는다 · 비밀 아님 |
| `SMTP_USERNAME` · `SMTP_PASSWORD` | 있음 | 실값 또는 **빈 값**(P-328 그대로) | [실측 09-24 22:2x · 발송 0] redmail `EmailSender.get_server` 는 `if user is not None or password is not None: server.login(...)` — 빈 문자열이면 **login 을 부른다.** 그래서 Mailpit 이 login 을 받는지 쟀다: EHLO 에 `auth` **광고** · `login('', '')` → **235** · `login('x','y')` → 235. 빈 값으로 된다. ⚠ 조율자는 여기서 두 번 추정했다(「username 없으면 건너뛴다」 → 「Mailpit 은 AUTH 가 없어 죽는다」) — 둘 다 틀렸고, 잰 것만 남긴다 |
| `SMTP_TLS` | **없음**(기본 STARTTLS) | 실값이면 그 값 · Mailpit 이면 **`0`** | ★ 새 이름 |
| `EMAIL_HOST` · `EMAIL_PORT` · `EMAIL_USE_TLS` | **없음** | `mailpit` · `1025` · `False` | 결정 ⑤ · P-327 · ★ `backend/.env` 만으로는 두 통에 안 닿는다 |
| `WEBHOOK_SIGNING_KEYS` | 없음 | 금고 `webhook_signing.env` 에서 | ② |
| VAPID 셋 | 있음 | **그대로** | P-335 |

## §2 이 창에서 하지 않을 것
- VAPID 새 쌍 **0**(P-335). gx-shell 재생성 **0**. 0바이트 덤프 정리 **0**.
- 다른 통(`gx-beat-e` 등) 재생성 **0** — 두 통만. (`gx-beat-e` 는 `:latest` 로 갈려 있다 — 다음 창의 일.)
- 결정이 없는 걸음 **0** — 건너뛰고 「결정 없음」으로 적는다.

## §3 창 전 남은 대표 손
결정 ①~⑤(CEO_INBOX) · 금고 `smtp.env`(있으면) · 14:00 에 대표 PC 에 대화형 창 하나 · ⑪ 에 대표 크롬.
