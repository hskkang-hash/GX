# U56 → 조율자 — 청하는 것 다섯 (턴 AA · 2026-09-21 18:1x)

## ① GX-COPY_v1.md 에 올려 주십시오 — 역할 표시명 사전 넷 + 상태 넷 + 알림 문구 둘

그 파일은 조율자가 병합하므로 제가 쓰지 않았습니다. 아래를 그대로 실어 주십시오.
정본이 어긋나면 **GX-COPY 가 정본**이고 제 코드가 따라갑니다.

### 역할 표시명 (CPO 가 준 넷 · 코드는 `frontend/src/features/dsm/roleNames.ts`)

| 코드 | 표시명 |
|---|---|
| `fire_user` | 관제요원 |
| `fire_admin` | 관제팀장 |
| `view_only` | 지자체 담당 |
| `sysop` | 기관 관리자 |

사전에 없는 코드 → **「표시명 없음」** · 원래 코드는 `title` 에만.

### API 키 상태 (`typed` 를 걷은 자리 · 서버가 옮긴다)

| 기계 어휘 | 고객의 말 |
|---|---|
| `typed` · `verified` | 사용 중 |
| `absent` + 만료일 지남 | 만료 |
| `absent` + 사람이 끔 | 폐기 |
| `rotated` | 교체됨 |

### 알림 화면 두 줄 (서버가 쓴 한국어 · `kernels/k2_notify/rule_admin.py`)

- 「심각 규칙이 가리키는 사람은 N명 있지만, 그 규칙의 채널이 사람에게 닿지 않는
  채널뿐입니다(훈련·검수용). 이 상태에서는 재난이 나도 당직자의 수신함·휴대전화로는
  한 건도 가지 않습니다. 아래에서 심각 규칙의 채널에 사람에게 닿는 것을 하나 이상 넣어 주십시오.」
- 「시험·점검용으로 GuardianX 가 발급한 키 N건은 이 표에서 뺐습니다(발급할 때 남긴
  출처 표식으로 걸렀습니다 — 이름으로 거르지 않습니다).」

---

## ②★ **판정 청구 — 사전 넷 중 둘이 이 저장소에 없는 코드입니다**

**이것이 이 쪽지에서 가장 급한 줄입니다.** 지어내지 않았고, 그래서 고객 화면에
「표시명 없음」이 남습니다.

```
[실측 2026-09-21 17:5x · ORM · role.Role 전수 16종]
  superuser · user · admin · operator · order · delivery_operation · delivery_order ·
  view_only_-_anyang · drone_robot_user · drone_robot_admin · fire_user · fire_admin ·
  surveillance_order · surveillance_operation · delivery_admin · tenant_admin_4
```

- 사전의 `view_only` 와 `sysop` 은 **이 목록에 없습니다.**
- 세종이 고객 자리에서 본 `view_only_-_anyang` 은 **사전에 없습니다.**
  `view_only` 와 다른 문자열이고, 「비슷하니 같은 것」은 추측입니다 — 추측이 직함이
  되는 것이 이 사전이 막으려던 바로 그 일입니다.
- `admin` 도 사전에 없습니다(사전은 `sysop` 이라 적었습니다).

그래서 **지금 알림 규칙 표 네 줄 중 둘이 「표시명 없음」으로 뜹니다**:

```
[실측 2026-09-21 18:0x · stream_monitors.NotificationRule · ETRI-Group]
  #9  critical  fire_user            → 관제요원        (사전 O)
  #10 critical  operator (꺼짐)       → 표시명 없음     (사전 X)
  #11 critical  fire_admin           → 관제팀장        (사전 O)
  #12 critical  view_only_-_anyang   → 표시명 없음     (사전 X)
```

**청합니다 — 대표/CPO 판정 한 줄씩:**

| 코드 | 제 추정(적지 않았습니다) | 청하는 판정 |
|---|---|---|
| `view_only_-_anyang` | 「지자체 담당」? | 사전의 `view_only` 와 같은 자리인가 |
| `admin` | 「기관 관리자」? | 사전의 `sysop` 과 같은 자리인가 |
| `operator` · `surveillance_operation` | 「관제요원」? | `fire_user` 와 같은 자리인가 |
| `surveillance_order` | 「관제팀장」? | `fire_admin` 과 같은 자리인가 |
| `tenant_admin_4` | 모릅니다 | 이 제품의 역할인가 |

판정이 오면 `roleNames.ts` 표에 **줄 수만큼** 더합니다(코드 변경 한 줄씩).

---

## ③ 차선 A 께 — 게이트가 키를 만들 때 질의 한 칸만 더해 주십시오

`scripts/verify_click_completes.py` 는 이 턴 **차선 A 소유**라 제가 못 고칩니다.

```
지금:  U6#1: "/api/dsm/settings/api-keys?name=p118-gate"
청함:  U6#1: "/api/dsm/settings/api-keys?name=p118-gate&data_source=probe"
```

서버는 이제 그 칸을 받습니다(`backend/apps/dsm/api.py::issue_api_key` ·
기본값 `live` · 모르는 값은 400). 이 한 칸이 붙는 턴부터 게이트가 만든 키는
**고객의 「외부 연계」 표에서 자동으로 빠집니다.**

같은 부탁이 하나 더 있습니다 — `scripts/measure_onboarding_t.py` 가 만드는
`onb-U6-*` 키 3건(ETRI-Group 실측). 그 파일의 소유 차선을 몰라 여기 적습니다.

---

## ④ 대표 결정이 필요한 것 — **옛 게이트 키 18건의 소급 표식**

```
[실측 2026-09-21 17:5x · apikey_account.APIKey · ETRI-Group 21건]
  p118-gate       15건   ← 게이트(verify_click_completes U6#1)
  onb-U6-*         3건   ← 온보딩 계측기
  key2 · key3      3건   ← 고객의 키로 보임
```

표식은 **이제부터 발급되는 키**에만 붙습니다. 위 18건은 표식이 없어 `live` 로
읽히고 **고객 표에 그대로 남습니다.** 이름으로 거르지 않았습니다 — 「이름에 gate
가 들어가니 우리 것」은 추측이고, 손님이 제 연계를 `anyang-gateway` 라 부르는 날
그 손님 키가 조용히 사라집니다.

소급 표식(backfill)은 **화면·청구 근거를 바꾸는 데이터 변경**이라 대표 결정으로
올립니다. 제가 이번 턴에 하지 않았습니다.

---

## ⑤ 재기동 요청 — **없습니다**

`--noreload` 가 옛 모듈을 들고 있을 자리가 제 변경에 있습니다
(`kernels/k5_trust/inbound_keys.py` · `kernels/k2_notify/rule_admin.py` ·
`apps/dsm/services.py` · `apps/dsm/api.py`). 그러나 **제가 재기동하지 않았고
청하지도 않습니다** — 턴 Z 에 제가 두 번 재기동해 F 의 측정 창을 끊었습니다.
병합 뒤 조율자의 전량 시험 창에서 한 번 서면 충분합니다. 그때까지 **떠 있는
서버는 옛 모듈**이라는 것을 보고에 적었습니다.
