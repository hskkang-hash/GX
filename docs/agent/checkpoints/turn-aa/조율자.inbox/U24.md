# U24 → 조율자 쪽지 (턴 AA · 2026-09-21) — **GX-COPY 등재를 청합니다 (새 말 열둘)**

사전은 조율자가 병합합니다. **U24 는 `GX-COPY_v1.md` 를 한 글자도 안 건드렸습니다.**
아래 말은 **이미 화면·종이에 들어가 있고**(실물로 재서 확인했습니다), 등재만 남았습니다.

⚠ 사전 §4(절 ID·내부 경로·감사 채널 이름 금지)를 **전부 지켰습니다** — 아래 어느 문구에도
절 ID·파일 경로·영문 열거값이 없습니다. (턴 Z 에 「스냅샷 P-184」를 거절한 그 규율 그대로입니다.
Q 가 정규식을 고쳐 이제 게이트도 세 자리 절 ID 를 잡습니다 — 사전이 막던 것을 게이트가 함께 막습니다.)

## ① 종이 — 훈련 배너 (대표 결정 ⑨)

| 자리 | 문구 |
|---|---|
| 사건 보고서 1쪽 **맨 위** | `[GuardianX] 훈련 — 이 보고서는 훈련 상황의 기록입니다. 실제 재난 상황이 아닙니다.` |

「훈련」 낱말 자체는 사전 §2 에 이미 있습니다(실운영 / 시드(검수용) / **훈련** 배지).
새로 청하는 것은 **종이 배너의 한 문장**입니다.

## ② 종이 — 가린 칸 다섯 (대표 결정 ⑩)

| 자리 | 가리기 전(실측) | 가린 뒤 문구 |
|---|---|---|
| 조치 이력 「대상」 · 메일 | `geumsan@org.kr` | `(비표시)@org.kr` |
| 조치 이력 「대상」 · 기기 | 표식 + 기기 토큰 | `기기 등록 (식별자 비표시)` |
| 조치 이력 「대상」 · 전화 | 번호 전체 | `010-****-****` |
| 조치 이력 「대상」 · 모르는 모양 | 값 그대로 | `대상 비표시` |
| 설치 주소 | 번지까지 | `경기도 안양시 만안구 (이하 비표시)` |
| 판정자·발행자 | `이름 (계정명)` | `이름` · 실명이 없으면 `실명 미등록 (계정명은 적지 않습니다)` |
| 종이 맨 아래 **가림 고지** | (없었음) | `수신자 연락처 · 기기 등록 정보 · 상세 주소는 이 종이에 적지 않습니다 (가린 칸에는 그 사유를 적습니다). 원본은 알림 이력 화면에서 권한이 있는 사람만 볼 수 있습니다.` |

★ **가린 사실을 종이가 스스로 말합니다.** 가린 것을 안 적으면 빈 자리가 「없었다」로 읽히고,
그러면 「아무에게도 안 알렸다」가 됩니다.

## ③ 감사 화면 — 대상 갈래 둘이 늘었습니다

| 갈래 | 딱지 | 밑줄 |
|---|---|---|
| (기존) 살아 있음 | `사건` | — |
| (기존) 삭제된 사건 | `삭제된 사건` | `대표 결정 2026-09-20 · 스냅샷 있음` / `대표 결정 2026-09-21 · 스냅샷 있음` |
| **새로** | `열리지 않은 사건 번호` | `막힌 시도 — 그 번호로 열리지 않았습니다` |
| (기존) 사라진 사건 | `사라진 사건` | `기록된 결정 없음` |
| **새로** | `확인 못 함` | `이 쪽에서 확인하지 못했습니다` |

설명 줄 둘(그 딱지가 **실제로 뜬 쪽에서만** 뜹니다 — 0 건일 때 적으면 「이 화면에는 늘 그런
행이 있다」로 읽힙니다):

- `「열리지 않은 사건 번호」는 그 번호로 사건을 열지 못해 서버가 요청을 막은 기록입니다. 있던 사건이 없어진 것과는 다른 일입니다.`
- `이 쪽에는 사건을 가리키는 행이 많아 일부 행의 대상을 확인하지 못했습니다. 「확인 못 함」은 그 사건이 없다는 뜻이 아닙니다 — 기간을 좁혀 다시 보십시오.`

★ 「열리지 않은」으로 적은 까닭을 적어 둡니다: 서버가 그때 낸 404 는 「없다」와 「남의 조직 것이다」를
**같은 답**으로 냅니다(존재 여부를 안 흘리려고 일부러 그렇습니다). 그래서 「그때도 없는 번호였다」로
적으면 **우리가 모르는 것을 적는 것**이 됩니다.

## ④ 사건 목록 — 조합 검색 · 그리고 **주소는 회색입니다**

- `지금 걸린 조건` · `없음 — 기간만 봅니다` · `조건 모두 지우기`
- 주소 칸(회색): `주소로 찾는 기능은 아직 없습니다. 사건 목록을 내는 서버가 주소 조건을 아직 받지 않습니다. 화면에서 받아 온 50건만 훑으면 그 밖의 사건이 「없음」으로 보이므로 그렇게 만들지 않았습니다. 지금은 기간과 유형으로 좁힌 뒤 사건을 열어 주소를 확인하십시오.`

— 차선 U24
