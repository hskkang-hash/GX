# 턴 AA · 차선 **U24** (지자체 담당 · 종이) — 2026-09-21

브랜치 `turn-q` · **커밋 0** · **브라우저 0회**(누르는 것은 V) · **HTTP 0회** · §0.4 **0줄** ·
**서버 재기동 0회** · 운영 DB **읽기 + 종이 뽑기만**(쓰기 0)

★ 이 턴에 P-219 로 `backend/apps/dsm/audit.py` · `incident_report.py` 를 함께 쥐었습니다.
  **`audit.py` 는 결국 한 줄도 안 고쳤습니다** — 고칠 자리가 거기가 아니었습니다(아래 ③).

---

## ① 「훈련」 배너 — **실물에 보입니다. 실사건에는 안 붙습니다**

| | 잰 것 | 결과 |
|---|---|---|
| **양성** | 훈련 사건 **305027** 로 DOCX 를 다시 뽑음 | **「훈련」이 종이에 있다** · 배너가 **첫 줄** |
| **음성** | 실사건 **4798** 로 같은 길 | **「훈련」 0건** · `drill` 0건 |

```
[실측 2026-09-21 16:0x · gx-shell · gxseed_u4_official 자격]
 drill 305027  DOCX 37,900 B   word/document.xml 첫 줄:
   「[GuardianX] 훈련 — 이 보고서는 훈련 상황의 기록입니다. 실제 재난 상황이 아닙니다.」
 live  4798    DOCX 38,114 B   같은 자리에 「훈련」 0건 · HTML 에도 0건
```

- 양성본 `docs/agent/evidence/UX-30/incident_1page_drill_305027_20260921_turnAA.docx` (+ `.html`)
- 음성본 `docs/agent/evidence/UX-30/incident_1page_live_4798_20260921_turnAA.docx` (+ `.html`)
- 고친 자리 `backend/apps/dsm/incident_report.py` — `DRILL_BANNER` · `drill_banner()` ·
  `data_source_of()` · `build_html(..., data_source=)` · `build_incident_html` 이 정본을 묻는 세 줄

### ★ 중간에 **거짓 양성을 한 번 만들었다가 지웠습니다**

처음에 `_CSS` 에 `.banner.drill { … }` 규칙과 그 위 한글 주석을 뒀습니다. 그랬더니
**실사건 종이의 HTML 에도 「훈련」·`drill` 글자가 들어갔습니다** — 찍힌 종이에는 안 보이지만
(DOCX 변환이 `<style>` 을 떼어냅니다) **HTML 을 훑어 재는 사람에게는 실사건에 훈련 표시가
있는 것으로 보입니다.** 강조를 인라인으로 옮겨 지웠고, 그 사실을 함수 머리말에 적었습니다.
→ 그래서 시험을 **음성부터** 세웠습니다(`test_a_real_event_carries_no_banner`).

### 판정의 축은 **둘**입니다 — 하나만 보면 놓칩니다

`build_incident_html` 은 `apps/dsm/services.event_data_source` 를 묻습니다(행 표식 + 훈련 창).
`build_html` 만 직접 부르면 **행 표식 한 축**으로 떨어집니다 — 훈련 창에서 난 사건을 놓칩니다.
놓치는 쪽이 실운영이라 안전한 방향이지만(실경보에 「훈련」이 붙으면 관제요원이 손을 늦춥니다),
**놓친다는 사실을 함수 머리말에 ⚠ 로 적었습니다.**

### ⚠ **별지 1호(상황보고서)에는 못 붙였습니다 — 그 서식이 저장소에 없습니다**

```
[실측 2026-09-21] grep 「별지」·「제N보」·「13항목」·form1·situation_report → backend 에 0건
  명세서(DSM v1.1 DSM-U4-01)가 incident_report.py::form1 이라 적어 둔 그 함수가 없다.
  지금 서는 서식은 셋: 사건 1쪽(build_html) · 월간(build_monthly_html) · 상급 제출용(build_upper_html)
```

배너는 **공용 함수**(`drill_banner`)로 냈으므로 별지 1호가 서는 날 **한 줄로 붙습니다.**
상급 제출용(`build_upper_html`)에는 **안 붙였습니다** — 그 종이가 읽는
상급 보고 체크(`DsmUpperReportFlag`)가 **지금 0건**이라 분모 0입니다. 분모 0인 초록은
초록이 아니고, 부르는 사람 없는 가지는 잠든 코드입니다(틀려도 아무도 모릅니다).

---

## ② 가린 칸 다섯 — **가린 판과 「무엇을 가렸는지」 표**

가린 판: `docs/agent/evidence/UX-30/incident_1page_live_4798_20260921_turnAA.docx`
(가리기 **전** 판은 턴 Z 의 `incident_1page_live_4798_20260921.docx` 로 같은 폴더에 남아 있습니다 —
두 장을 대 보면 무엇이 가려졌는지 한눈에 갈립니다. **전 판은 반출 금지입니다.**)

| # | 칸 | 가리기 전 (실측) | 가린 뒤 (실측) | 남긴 사실 |
|---|---|---|---|---|
| 1 | ④ 조치 이력 「대상」 · 메일 **5줄** | `geumsan@org.kr` · `oper_chi1@yopmail.com` · `chungnam_oper@org.kr` · `chungnam002@org.kr` · `chungnam01@org.kr` | `(비표시)@org.kr` ×4 · `(비표시)@yopmail.com` ×1 | **도메인은 남깁니다** — 「어느 기관에 알렸나」는 결재가 읽어야 할 사실이고, 도메인은 사람이 아니라 기관입니다. 아이디는 **한 글자도 안 남깁니다**(앞 두 자를 남기는 방식은 여전히 그 사람의 조각입니다) |
| 2 | ④ 같은 열 · **웹푸시** | `drill:webpush:dc716379fa14` | `기기 등록 (식별자 비표시)` | **한 글자도 안 남깁니다.** 토큰이 기기를 가리키고, 앞머리는 우리 표식이라 사전 §4 가 금합니다 |
| 3 | ③ 「판정자」 | `이름 (gxseed_u1_operator)` | 이름만. 실명이 없으면 `실명 미등록 (계정명은 적지 않습니다)` | 계정명은 로그인에 쓰는 **내부 식별자**입니다 |
| 4 | 머리줄 「발행자」 | `[시드] U4 재난안전과 담당 공무원 (gxseed_u4_official)` | `[시드] U4 재난안전과 담당 공무원` | 위와 같음. **종이 전체에 `gxseed_*` 0건**(실측) |
| 5 | ① 「설치 주소」 | `경기도 안양시 만안구 안양천서로 100 (시드 카메라)` | `경기도 안양시 만안구 (이하 비표시)` | 행정구역 마디까지만. **모양을 못 가르면 통째로 가립니다**(빗나가는 쪽이 여는 쪽이면 안 됩니다) |
| — | (더함) 전화 | — | `010-****-****` | 지금 표본엔 없지만 모양이 오면 새지 않습니다 |
| — | (더함) 모르는 모양 | — | `대상 비표시` | **닫는 쪽이 기본값** |
| — | (더함) **종이 맨 아래 가림 고지** | (없었음) | 「수신자 연락처 · 기기 등록 정보 · 상세 주소는 이 종이에 적지 않습니다 (가린 칸에는 그 사유를 적습니다). 원본은 알림 이력 화면에서 권한이 있는 사람만 볼 수 있습니다.」 | **가린 사실을 숨기면 그것도 거짓말입니다** |

★ 조건 칸을 **안 만들었습니다.** 「대외본일 때만 가린다」는 스위치를 두면 그 스위치를 안 켠
종이가 언젠가 나가고, 나간 종이는 되돌릴 수 없습니다. **한 모양으로 나갑니다.**

★ 계정명 → **표시명**: 이 턴에 U56 이 사전을 세웠지만 **종이는 그것을 못 씁니다**
  (자리가 화면 쪽이고 열쇠가 역할 코드입니다 — 아래 ⑥ 에 실측으로 적었습니다).
  그래서 **지어내지 않고 가렸습니다.** 쪽지로 청했고(`turn-aa/U56.inbox/U24.md`),
  사전이 종이에서 읽히는 날 고칠 자리는 `incident_report._role_display_name` **한 곳**입니다 —
  부르는 쪽은 한 줄도 안 바뀝니다. **대표 지시의 「가리기」는 섰고 「표시명으로」는 회색입니다.**

### ⚠ 가리고 나서 **새로 보인 것 하나** — 표시명에 「[시드]」가 붙어 있습니다

```
[실측] 발행자 칸 = 「[시드] U4 재난안전과 담당 공무원」
```
계정명은 지웠는데 **이름 자체에 시험 표식이 들어 있습니다.** 이 턴의 한 줄
(「고객 화면에 시험 장치 0」)이 **종이에도** 걸리는 자리입니다. 값은 코드가 아니라 **씨앗 데이터**라
U24 가 고칠 자리가 아니고, 제가 이름을 바꿔 적으면 그것은 추측입니다. **재서 넘깁니다.**

---

## ③ 감사 화면 침묵 — **전 180행 → 후 0행**

```
[실측 2026-09-21 · gxseed_u4_official 테넌트 좁힘 · 커널 문으로 한 건씩]
  전   읽는 행 2,194 · 사건을 가리키는 행 273 · 이름 붙는 행 2 · 대상이 없는데 침묵 180
  후   읽는 행 2,231 · 사건을 가리키는 행 282 · 이름 붙는 행 282 · 침묵 0
       (2,194 → 2,231 의 차는 그 사이에 운영이 쌓은 행입니다 — 제가 만든 행이 아닙니다)
```

### 갈래별로 — **하나로 뭉뚱그리지 않았습니다**

| 갈래 | 행 | 사건 | 화면이 주는 말 | 근거 |
|---|---|---|---|---|
| 살아 있는 사건 | **100** | 22 | `사건 #N` | 커널 문이 200 |
| 대표 결정으로 지워진 사건 (**09-20**) | **29** | 10 | `삭제된 사건 #N` · 「대표 결정 2026-09-20 · 스냅샷 있음」 | 09-20 스냅샷에 id 16 |
| 대표 결정으로 지워진 사건 (**09-21** · 이번에 새로) | **16** | 4 | `삭제된 사건 #N` · 「대표 결정 **2026-09-21** · 스냅샷 있음」 | 09-21 스냅샷에 id 4 (295402~295405) |
| **없는 번호를 가리킨 막힌 시도** | **2** | 1 | `열리지 않은 사건 번호 #999999999` · 「막힌 시도 — 그 번호로 열리지 않았습니다」 | 그 **행 자신**이 막힘 + 서버 404 |
| 그 밖 — 사라졌고 기록된 결정이 없다 | **135** | 65 | `사라진 사건 #N` · 「기록된 결정 없음」 | 표에 없음(하드 삭제 확인) + 두 스냅샷에 없음 |
| 확인 못 함 (상한에 닿은 쪽) | 0 | — | `확인 못 함` · 「이 쪽에서 확인하지 못했습니다」 | 아래 상한 |

★ **턴 Z 의 거절이 옳았고, 이번에 그 행이 제 이름을 얻었습니다.** 조율자가 「대상 없음이니
  삭제된 사건이라 적어라」고 한 그 2 행은 `upper_report:set:999999999` — **없는 번호를 가리킨
  막힌 시도**였습니다. 거절만으로는 행이 이름을 못 얻으므로 이번에 **갈래를 하나 더 세웠습니다.**
  ⚠ 다만 「그때도 없는 번호였다」고까지는 **안 적습니다**: 서버의 404 는 「없다」와
  「남의 조직 것이다」를 **같은 답**으로 냅니다(존재 여부를 안 흘리려고 일부러 그렇습니다).
  우리가 아는 사실은 **그 번호로 열리지 않았다**까지입니다.

★ **09-21 삭제 넷을 이번에 넣은 까닭**: 턴 Z 에 안 넣은 이유는 **분모 0** 이었기 때문입니다
  (그때 화면은 `action` 한 모양만 읽었고 그 모양으로 그 넷을 가리키는 행은 0 이었습니다).
  `event_ref` 를 읽기 시작하면서 분모가 **16 행**이 됐습니다. 분모가 생겼으므로 이름을 줍니다.
  ⚠ 두 스냅샷을 **한 상수로 합치지 않았습니다** — 합치면 09-21 에 지워진 사건에
  「대표 결정 2026-09-20」이 찍힙니다. **없던 날짜를 종이에 만드는 것**입니다.

### 고친 자리 — **`audit.py` 가 아니었습니다**

U56 이 턴 Z 에 `read_page` 에 `event_ref` 를 이미 실어 뒀습니다. 그리는 자리만 없었고,
그 자리는 `backend/apps/dsm/api_u24.py::_with_target_column` 입니다.

- `api_u24.row_event_id()` **신설** — 축 둘을 한 답으로. `action` 이 답하면 `action` 이 답이고
  (**대체가 아니라 보완**), 아니면 `event_ref`. 판정식을 **새로 쓰지 않았습니다**
  (`common/evidence_chain.event_ref_of` 가 정본이고 체인도 같은 함수를 부릅니다 —
  화면이 세는 수와 체인이 세는 수가 같은 자에서 나옵니다).
- `api_u24.deleted_event_decision()` **신설** — id 가 **어느 결정**으로 지워졌나(날짜·스냅샷).
- `api_u24._is_denied_attempt()` **신설** — 추측이 아니라 **행에 적힌 사실**(막힘 **그리고** 404).
- `frontend/.../AuditLog.tsx` — 갈래 낱말 둘 추가 · 분모 다섯 칸 · 설명 줄 둘(그 딱지가 **뜬 쪽에서만**).
- `backend/apps/dsm/audit.py` — **0줄**.

### ★ 이 칸이 **비싸졌습니다. 상한을 두고 닿았다고 말합니다**

```
[실측 2026-09-21 · 50행 한 쪽 · 서로 다른 사건 9] 질의 45 · 0.06초  → 사건 하나에 약 다섯 질의
```
쪽 크기 상한이 200 이므로 상한이 없으면 한 쪽이 **질의 1,000**까지 갑니다.
그래서 `TARGET_SCAN_MAX_EVENTS = 60` 을 두고, 닿으면 `target_scan_capped` 로 **말합니다**
(체인 칸의 `chain_scan_capped` 와 같은 규약). 안 물어본 행은 「사라짐」이 아니라 **「확인 못 함」**입니다 —
안 물어본 것을 「사라짐」으로 적으면 **없어지지 않은 사건이 사라진 것**이 됩니다.

---

## ④ U4#8 검색 조합 — **셋 다 섰습니다. 다만 주소는 아직 안 재 봤습니다**

| 조건 | 어디서 거르나 | 이 턴 |
|---|---|---|
| **사건번호** | 서버 문(`GET /api/dsm/events/{id}` · 없으면 404) | 이미 있었음 — 「거르기」가 아니라 **「열기」** |
| **유형** | 서버(`event_type` · 쉼표로 여럿) | 이미 있었음 |
| **주소** | 서버(부분 일치) | ★ **이 턴에 섰습니다 — 그러나 실물로 못 쟀습니다** |
| 등급 · 기간 · 대응 단계 | 서버 | 이미 있었음 |
| (새로) **지금 걸린 조건** | — | 걸린 조건을 낱말로 모아 적고 낱개·일괄로 지웁니다 |

★ **조합의 절반은 「지금 무엇으로 좁혀져 있는지」가 한 줄로 읽히는 것**입니다. 조건이 네 자리에
흩어져 있으면 사람은 「0건」을 **사건이 없다**로 읽는데, 실제로는 자기가 건 조건이 겹친 것입니다.
기간 딱지를 닫으면 주소의 **세 칸**(`period`·`since`·`until`)이 같이 빠집니다 — 하나만 지우면
화면은 「기간 조건 없음」이라 말하면서 **서버로는 여전히 그 창을 보냅니다.**

### 주소 — **아침엔 세 가지가 다 없었고, 저녁에 둘이 섰습니다**

```
[실측 2026-09-21 아침] ① 목록 응답에 address 칸이 없다
                       ② 목록 커널 query_events 인자 열둘에 주소가 없다
                       ③ 주소로 카메라를 찾아 주는 문도 없다
```
그래서 아침엔 **안 지었습니다** — 받은 50건을 화면에서 훑으면 상한 밖 사건이
「그 주소에 사건 없음」이 되고, 그것은 검색이 아니라 **거짓말하는 검색**입니다.
커널은 U1 의 파일이라 한 줄도 안 건드리고 **쪽지로 청했고**(`turn-aa/U1.inbox/U24.md`),
U1 이 같은 턴에 세웠습니다(`U24.inbox/U1.md`).

제가 이은 것은 **딱 두 줄**입니다:
```
backend/apps/dsm/api.py::events   address: str | None = None,   (서명)
                                  services.recent_events(..., address=address, ...)
frontend/.../EventList.tsx        주소 칸 · 질의로 내보냄 · 걸린 조건 딱지 · 안내 한 줄
```
⚠ **U1 의 P-220 을 덮지 않았습니다** — 작업본을 먼저 읽고 `include_probe` 위에 얹었습니다
(`include_probe` 6건 그대로 · 실측).

★ **「주소를 모르는 사건」과 「그 주소에 사건이 없다」를 가르는 말**을 화면이 적습니다
(U1 이 청한 그 한 줄). **주소로 좁혔을 때만** 뜹니다 — 늘 적으면 「이 검색은 원래 안 맞는다」가
되고, 안 적으면 0 건이 「그 주소에 사건이 없다」로 읽힙니다:

> 주소는 사건에 적힌 주소로 부분 일치 검색합니다. 카메라 주소가 아직 확인되지 않은
> 사건은 이 검색에 걸리지 않습니다 — 0건이 「그 주소에 사건이 없다」는 뜻은 아닙니다.

⚠⚠ **U1 이 「이으실 때 먼저 재 주십시오」라고 했는데 못 쟀습니다.** 도커가 죽어 있습니다.
쟀다: `py_compile` 네 파일 **exit 0** · 화면 문구 게이트 **exit 0**.
**안 쟀다: 주소로 실제로 걸러지는지 · 빈 칸이 안전한지 · 화면이 그 질의를 보내는지.**
**U4#8 을 초록으로 적지 마십시오** — 아무도 안 잰 초록이 됩니다.

---

## 시험 · 자원

```
tests/test_u24_audit_target.py + tests/test_incident_report.py
  전 30 passed  →  후 53 passed (exit 0) · 새 시험 23
  새로 세운 것: 훈련 배너 음성(실사건에 0) · 배너가 첫 줄 · 낱말 한 벌 ·
                가림 다섯 + 모르는 모양은 닫는다 + 「가렸다고 적혀 있다」 ·
                두 스냅샷이 날짜를 안 섞는다 · 축 ①이 ②를 안 밀어낸다 ·
                막힌 404 는 사라진 사건이 아니다 · 상한에 닿으면 닿았다고 적는다
```

- **잰 시각(분)**: 15:5x~16:0x 종이 실물 둘 · 15:1x·15:3x·16:1x 감사 전수 · 16:2x~16:3x 시험
- **로그인 0회** — HTTP 를 한 번도 안 때렸습니다(전부 ORM·커널 문). 남의 창을 안 건드렸습니다.
- **서버 재기동 0회.**

## 못 한 것 · 재지 못한 것 — **회색입니다. 초록으로 안 적습니다**

- **별지 1호(상황보고서) 서식이 저장소에 없습니다.** 배너를 붙일 자리가 없어 **못 붙였습니다.**
  공용 함수로 내 뒀으니 서식이 서는 날 한 줄입니다. 서식을 제가 새로 짓지 않았습니다 —
  13항목·제N보 채번은 이 턴의 일이 아니고, 지어 놓고 안 부르면 잠든 코드입니다.
- **상급 제출용 종이의 훈련 표시**: 분모 0(상급 보고 체크 0건)이라 **안 지었습니다.**
- **주소 검색**: 커널 한 줄이 남의 파일입니다. **화면은 회색 그대로** 둡니다.
- **프런트 타입검사**: 격리 사본으로 돌리는 중 도커 엔진이 500 을 냈습니다(아래 갱신 참조).
- **브라우저로 안 눌렀습니다**(규약 — V 의 일). ③의 282 도 ①의 두 종이도 **문과 코드**에서 잰 값입니다.
- **CSV 면을 이 턴에도 안 쟀습니다** — 새 갈래 둘이 `target_state` 열로 나가지만 **파일을 안 열어 봤습니다.**
- **감사 화면의 새 상한(60)이 실제로 닿는 쪽을 못 봤습니다** — 지금 데이터로는 안 닿습니다(분모 0).
  시험에서 상한을 좁혀 갈래를 갈랐을 뿐입니다.
- **「[시드]」가 종이에 남아 있습니다** — 재서 넘깁니다(값은 씨앗 데이터 · 제 자리가 아닙니다).

## 보낸 쪽지 셋

- `turn-aa/U56.inbox/U24.md` — 역할 표시명 사전 · `event_ref` 받았다는 회신
- `turn-aa/조율자.inbox/U24.md` — GX-COPY 등재 열둘(**사전 파일은 안 건드림**)
- `turn-aa/U1.inbox/U24.md` — 목록 커널에 주소 인자 한 줄

## 고친 파일

```
backend/apps/dsm/incident_report.py    훈련 배너 · 가림 셋 · 표시명 갈고리 · 가림 고지
backend/apps/dsm/api_u24.py            축 둘 · 갈래 둘 · 두 스냅샷 · 상한과 「닿았다」
frontend/src/features/dsm/pages/AuditLog.tsx    갈래 낱말 둘 · 분모 다섯 · 설명 줄 둘
frontend/src/features/dsm/pages/EventList.tsx   걸린 조건 · 조건 지우기 · 주소 회색
backend/tests/test_incident_report.py           +14
backend/tests/test_u24_audit_target.py          +9
docs/agent/evidence/UX-30/*_turnAA.{docx,html}  종이 실물 넷(양성·음성)
backend/apps/dsm/audit.py                       0줄 (쥐었지만 고칠 자리가 아니었습니다)
```

---

# ★ 늦게 잰 것 · 늦게 안 것 (같은 턴 · 덧붙임)

## ⑤ 화면 문구 게이트 — **통과했고, 빈 자리 하나를 메웠습니다**

```
scripts/verify_ui_copy.py   (호스트 · docker exec 0건이라 밖에서 돈다)
  전   exit 0 · 본 비율 100% · 잔여 84(기준선 84) · 새 위반 0
       [입력] 표시명 사전 **2/3** — ? 사전이 아직 없다: **채널 이름 (U24)**
  후   exit 0 · 본 비율 100% · 잔여 84(기준선 84) · 새 위반 0
       [입력] 표시명 사전 **3/3** — 역할 표시명(U56) · **채널 이름(U24)** · 상태 표시명
```

판정기가 **제 이름으로 부른 빈 자리**였습니다. 채널 우리말 표는 턴 Z 에 제가
`AuditLog.tsx` **안에** 세웠는데, 판정기가 표시명 사전을 찾는 자리는 `copy.ts` 입니다 —
**사전이 있는데도 「없다」로 읽히고 있었습니다.** 낱말은 한 글자도 안 바꾸고 표를
`copy.ts::CHANNEL_LABEL` 로 **옮겼습니다**(새로 짓지 않았습니다 · 두 벌 금지).
`copy.ts` 는 이 턴 조율자가 이미 고친 파일이라 **덮지 않고 뒤에 덧붙였습니다.**

## ⑥ U56 의 역할 표시명 사전 — **섰습니다. 그런데 종이는 못 씁니다**

```
[실측 2026-09-21] frontend/src/features/dsm/roleNames.ts::ROLE_DISPLAY_NAMES (U56 · 이 턴)
  ① 자리가 화면(TypeScript)이고 종이를 찍는 곳은 backend(Python)이다
  ② 열쇠가 **역할 코드**(fire_user …)이고 종이가 든 열쇠는 **계정명**(gxseed_u1_operator)이다
```

**파이썬으로 베끼지 않았습니다** — 베끼면 두 벌이 되고, 갈리는 날 화면과 종이가 같은
사람을 다른 직함으로 부릅니다. 그래서 이 턴의 종이는 **계정명을 가린 채** 나갑니다
(대표 지시의 「가리기」쪽은 집행됐고 「표시명으로」쪽은 **아직 회색**입니다).
U56 쪽지에 **둘 중 하나를 골라 달라**고 적었습니다 — 고를 자리가 제 자리가 아닙니다.

## ⑦ ★ **프런트 타입검사를 끝내 못 했습니다 — 그리고 도커 엔진이 죽었습니다**

```
16:3x  gx-fe-build 안에 frontend/src 를 격리 복사(/tmp/u24check) · 별도 tsconfig · node_modules 심링크
       node /app/node_modules/typescript/bin/tsc -p tsconfig.app.json --noEmit
16:4x  600초 안에 안 끝나 배경으로 넘김
17:0x~ 이후 **모든 docker 명령이 500** — `docker version` · `docker ps` 까지 전부
       (v1.54 API · dockerDesktopLinuxEngine 파이프)
```

- **TSC_EXIT 를 못 받았습니다. 출력 파일은 0줄이었습니다.**
  ⚠ 그 0줄을 **「오류 0」으로 읽지 않습니다** — 턴 Z 에 제가 빠질 뻔한 바로 그 함정이고,
  이번엔 명령이 **안 끝난 것**이지 깨끗한 것이 아닙니다. **기준선 3234 와 대조하지 못했습니다.**
- **도커를 재기동하지 않았습니다** — 규약대로 조율자께 맡깁니다. 다른 차선의 창이 같이 끊깁니다.
- ★ **원인은 제가 아니었습니다** — 조율자가 알려 왔습니다: 차선 F 가 `gx-shell` 안에
  gunicorn 네 벌(프로세스 20개)과 1.7GB DB 를 띄웠고, 그것이 엔진을 굶겼습니다.
  F 가 스스로 신고했고 재시작을 안 눌렀습니다. 제 tsc 와 시각이 겹쳤을 뿐입니다.
  ⚠ **제가 처음 이 칸에 「겹쳤다」고만 적고 원인을 단정하지 않은 것이 옳았습니다** —
  단정했으면 제 이름으로 남의 원인을 적을 뻔했습니다.
- 대신 할 수 있는 것만 했습니다: 파이썬 넷 `py_compile` **exit 0** · 화면 문구 게이트 **exit 0**.
  **TypeScript 는 한 번도 안 돌았습니다. 초록으로 적지 않습니다.**
- ★ 백엔드 시험 **53 passed** 는 도커가 살아 있을 때 잰 값이고, **그 뒤 백엔드는 한 줄도
  안 고쳤습니다**(그 뒤 고친 것은 화면 파일 셋뿐). 그래서 그 수는 지금도 유효합니다.

## 이 턴에 고친 파일 (늘어남)

```
frontend/src/features/dsm/copy.ts      CHANNEL_LABEL · channelLabel · hasChannelLabel (덧붙임)
frontend/src/features/dsm/pages/AuditLog.tsx   표를 copy.ts 로 옮기고 그쪽을 부른다
```

---

# ★★ 조율자께 — **실물로 확인한 것 / 도커가 죽어서 못 잰 것**

도커가 **18:20 경**부터 죽어 있고 원인은 차선 F 라고 알려 주셨습니다. 그 뒤로는
`docker exec` 를 **한 번도 더 안 시도했습니다**(나오는 답이 뜻 없는 회색이라).

## 쟀다 — **도커가 살아 있을 때 실물로**

| 무엇 | 잰 값 | 시각 |
|---|---|---|
| 훈련 배너 **양성** | drill 305027 DOCX **37,900 B** · 「훈련」이 첫 줄 | 16:0x |
| 훈련 배너 **음성** | live 4798 DOCX **38,114 B** · 「훈련」 0건 · HTML 에도 0건 | 16:0x |
| 가린 칸 다섯 | 종이 실물에서 메일·토큰·계정명 **0건** · 주소는 동까지 | 16:0x |
| 감사 갈래 | 273 → 282 행에 이름 · 침묵 **0** · 갈래 다섯의 수 | 15:1x·16:1x |
| 사라진 65 사건이 **정말 없는가** | `DetectionEvent._base_manager` 전수 조회 → **0건 발견**(하드 삭제) | 16:1x |
| 막힌 2 행이 **무엇인가** | `upper_report:set:999999999` · 막힘 · 404 · 사유 그대로 읽음 | 15:3x |
| 백엔드 시험 | `test_u24_audit_target` + `test_incident_report` **53 passed · exit 0** (전 30) | 16:3x |

★ 알려 주신 ③(「대시보드 첫 줄이 probe 가 아니라 drill 이었다 — 갈래 판정도 실물로 보라」)
  그대로 했습니다: 65 사건은 **표에 없는 것**을 확인했고, 막힌 2 행은 **사유 글자까지 읽었습니다.**
  추정으로 이름 붙인 갈래가 **하나도 없습니다.**

## 못 쟀다 — **「안 된다」가 아니라 「못 쟀다」입니다**

| 무엇 | 왜 |
|---|---|
| **프런트 타입검사** | 격리 사본으로 돌리다 600초 초과 → 배경 → 엔진 500. **TSC_EXIT 를 못 받았습니다** · 출력 0줄을 「오류 0」으로 **안 읽습니다** · 기준선 3234 와 **대조 못 했습니다** |
| **주소 검색이 실제로 거르는가** | U4#8 의 마지막 두 줄을 저녁에 이었고 그 뒤로 도커가 없습니다 |
| **백엔드 시험 재실행** | 주소 두 줄(`api.py`)을 얹은 **뒤**로는 한 번도 안 돌렸습니다 |
| **CSV 면** | 새 갈래 둘이 `target_state` 열로 나가지만 파일을 안 열어 봤습니다 |
| **상한(60)이 닿는 쪽** | 지금 자료로는 안 닿습니다(분모 0). 시험에서 상한을 좁혀 갈래만 갈랐습니다 |

★ 53 passed 는 **백엔드를 마지막으로 고치기 전**이 아니라 **`api_u24` 상한까지 넣은 뒤**의 값입니다.
  그 뒤에 백엔드에서 바뀐 것은 **`api.py` 두 줄뿐**입니다(서명 한 칸 + 넘기기 한 줄).

## 도커가 나으면 — **한 줄씩 그대로 돌려 주십시오**

```bash
# ① 내 시험 (전 30 → 53 · exit 0 을 기대)
MSYS_NO_PATHCONV=1 docker exec -e DJANGO_SETTINGS_MODULE=config.settings -e DB_TEST_NAME=test_gx_u24 \
  gx-shell python -m pytest tests/test_u24_audit_target.py tests/test_incident_report.py -q --nomigrations -p no:randomly

# ② 주소 인자가 실제로 거르는가 (U1 이 세우고 내가 이은 자리 · 아무도 아직 안 쟀습니다)
MSYS_NO_PATHCONV=1 docker exec -e DJANGO_SETTINGS_MODULE=config.settings -e DB_TEST_NAME=test_gx_u24 \
  gx-shell python -m pytest tests/test_f05_event_api.py -q --nomigrations -p no:randomly

# ③ 프런트 타입검사 — 기준선 3234 와 대조. ★ 종료 코드를 출력과 함께 찍어 주십시오
MSYS_NO_PATHCONV=1 docker exec -w /tmp/u24check gx-fe-build sh -c \
  'node /app/node_modules/typescript/bin/tsc -p tsconfig.app.json --noEmit > out.txt 2>&1; \
   echo TSC_EXIT=$?; echo LINES=$(wc -l < out.txt); grep -c "features/dsm" out.txt'
#   (/tmp/u24check 는 이미 서 있습니다: frontend/src 격리 사본 + 별도 tsconfig + node_modules 심링크.
#    ⚠ 그 사본은 **오늘 아침 것**입니다 — 저녁 고침을 재려면 docker cp 를 다시 해 주십시오.)
```

## S 의 fail-closed 27건 — **제 쪽은 위험이 없습니다**

`audit.py` 를 쥐었지만 **한 줄도 안 고쳤습니다**(고칠 자리가 `api_u24` 였습니다).
제가 이 턴에 더한 감사 관련 코드는 **읽기뿐**이고 `apps.get_model(...)` 로 감사 표를
집는 줄이 **0건**입니다 — `audit.read_page` 가 내준 사전을 읽기만 합니다.

## 알려 주신 ④(역할 표시명 사전) — **읽었고, 종이에는 못 댔습니다**

```
[실측 2026-09-21] roleNames.ts::ROLE_DISPLAY_NAMES
  ① 자리가 화면(TypeScript) · 종이를 찍는 곳은 backend(Python)
  ② 열쇠가 역할 코드(fire_user …) · 종이가 든 열쇠는 계정명(gxseed_u1_operator)
```
**파이썬으로 베끼지 않았습니다** — 베끼면 두 벌이 되고, 갈리는 날 화면과 종이가 같은
사람을 다른 직함으로 부릅니다. 말씀하신 *「「표시명 없음」이면 가리십시오」* 는 그대로
집행했습니다 — **종이는 계정명을 가린 채 나갑니다.** 정본을 어디 둘지는 제 자리가
아니라 U56·조율자의 자리라 **쪽지로 청해 두었습니다**(`U56.inbox/U24.md` 덧붙임).
사전이 종이에서 읽히는 날 고칠 자리는 `incident_report._role_display_name` **한 곳**입니다.
