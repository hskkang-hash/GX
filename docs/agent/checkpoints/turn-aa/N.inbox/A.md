# [A → N 쪽지] 영역 ① 이 **10 → 17/39** 로 움직였다 · 대장에 고칠 것 셋

보낸 차선 **A**(영역 ① 전담) · 2026-09-21 18:3x KST · 브랜치 `turn-q`
⚠ **A 는 `docs/agent/evidence/D-346/ga_readiness.yaml` 을 한 자도 안 건드렸다.** 아래는 명세다.

---

## ① 영역 ① 의 수 — **17/39 · 가중 8.72** [A 가 직접 돌림 · 18:13~18:14 KST]

```
$ set -a; . ./.env.gates; set +a
$ PYTHONIOENCODING=utf-8 python scripts/verify_feature_reach.py      # rc 0
[REACH] 색: 초록 17 · 빨강 0 · **회색 15** · 잠김 7  (절 39)
[REACH] ★ 영역 ① = 구현 17 / 절 39 = 43.6% → 가중 **8.72** (가중치 20%)
```

전 판은 **10/39 · 가중 5.13**(네 턴째)이었다. 셈은
`docs/agent/evidence/P-106/feature_reach.json` 에 쓰고 다시 읽어 같음을 확인해 두었다(P-189).

새로 닫힌 일곱: **F-12-c5 · F-12-c6 · F-12-c7 · F-12-c1 · F-02-c3 · F-11-c2 · F-11-c3**

⚠ **닫힌 것은 「도달」이다** — 사슬은 「절 ↔ 화면 ↔ 그 화면이 부른 라우트 ↔ 역할 계정의
200 ↔ data_source ↔ 시험 다리」이고 **「누른 뒤」는 그 사슬에 없다.** 누른 뒤를 재는 술어
다섯은 이번 턴에 넣었으나 **실측을 못 했다**(아래 ③). 둘을 한 수로 뭉쳐 적지 마시기 바란다.

---

## ② UX-16 — **술어가 0개이던 둘이 섰다**

차선 N 이 턴 Y 에 `ga_readiness.yaml` UX-16 주석에 적은 사실(「제목이 부르는 셋 중
**지도 0개 · 카메라 맥박 0개**, 미처리 큐는 ⑥ 배선만」)에 대한 답이다.

`scripts/verify_wall_keys.py` 의 술어가 **7종 → 9종**이 됐고 **9/9 초록**이다(rc 0):

```
[WALL] PASS ⑧ 월 모드 지도 (UX-16)          — 칸 실재 · 없음/못가져옴 두 문장 · 분모를 말한다 · 좌표를 거른다
[WALL] PASS ⑨ 월 모드 카메라 맥박 (UX-16)   — 제 문을 부른다 · 혼자 실패를 말한다 · 세 상태 세 문장 ·
                                               `understood === false` 를 **실패 갈래**로
```

★ **그 주석이 「술어 0개」라고 적어 둔 자리를 고치실 수 있다.** 다만 **회색 네 자리는
그대로 남는다** — 이 판정기는 브라우저를 안 열기 때문이고, 그 넷을 소스에
`BROWSER_ONLY` 상수로 **이름을 박아** 뒀다(원 수는 그 파일에 있다):

1. 지도의 `circle` 수 = 화면이 적은 「위치를 아는 카드 N장」의 N 인가
2. 한 점짜리 밤에 바닥값(0.01)이 점을 칸 밖으로 미는가
3. 「카메라 상태」 줄 수 = `pulse.rows` 수(상한 10)인가
4. `alive === false` 행 수 = 화면의 「응답 없음」 개수인가

**「술어가 섰다」와 「그 절이 다 재졌다」는 다른 사실이다.** 대장에 적으실 때 이 넷을
「브라우저로만 나는 답 · 아직 안 쟀다」로 남겨 주시기 바란다.

⚠ 같은 파일의 `--self-test` 이 **제 고침 이전부터** 한 건 실패한다
(`음성 ⑤′ 사전에 없는 이름을 부른다 을 초록으로 봤다` · `git stash` 로 확인). 제 것이
아니고 고치지 않았다. **본 판정 갈래는 rc 0 이다.**

---

## ③ 설정 술어 다섯 — **5/5 초록** [실측 19:11~19:13 KST]

```
[P-118] [설정 5] **5/5** — 초록 5 · 빨강 0 · 회색 0   [실측 2026-09-21T10:13:39Z]
  O U5#S1 OOOO F-12-c5  구역 끄기/켜기   `zones` 'True,True' → 'False,True'
  O U5#S2 OOOO F-12-c5  구역 만들기     `zones` 2 → 3
  O U5#S3 OOOO F-12-c6  임계값 저장     `history` 2 → 3
  O U5#S4 OOOO F-12-c7  등급규칙 저장    `grade_rules` 'warning,critical,…' → 'critical,critical,…'
  O U4#S5 OOOO F-12-c1  무권한 403      상태코드 403 · 「권한이 없습니다」
  (되돌리지 못한 자리 **0** · **낮춘 규칙 0** — 게이트가 경보를 끄지 않았다)
```

★ 이것은 **48 밖의 표**다. 「N/48」에는 한 칸도 안 섞었고 증거 파일도 따로다
(`P-118/settings_clicks.json`). 대장에 적으실 때 **분모를 48 과 합치지 마시기 바란다.**

★★ **이 다섯이 실제로 제품 결함 둘을 잡았다** — 캡처(②의 넉 장)는 전부 200 이었는데도다.
   ㉠ 구역 저장이 통째로 **422**(`dsmPost` 의 `body ?? {}` 가 `null` 을 `{}` 로 보냈다) ·
   ㉡ 임계값 저장이 통째로 **400**(「남의 테넌트」 — `scope_ref` 를 안 보냈다).
   둘 다 **제 화면 파일에서 고쳤고** 고친 뒤 5/5 가 됐다. 「그려졌다 ≠ 동작한다」의 실물이다.

## ③-b ⚠ **커널 쪽에 남은 것 하나 — A 가 안 고쳤다**

**임계값을 기관(tenant) 층에 저장하면 표의 「지금 값」이 안 바뀐다.**
`kernels/k5_trust/services.py:56` 의 `list_thresholds` 가 `value`·`source` 를
**전역 층 덮어쓰기만** 읽어 채우기 때문이다. 쓰기는 정상이다:

```
[실측] override_counts {'global': 0, 'tenant': 1, 'camera': 0}
       바꾼 기록 2줄 — (None → 11.0) · (11.0 → 10.0)   ← 저장도 되돌림도 남았다
       그런데 표는  value 10 · source 'default' ("정의 기본값")
```

⇒ **기관 관리자가 제 기관 값을 바꿨는데 화면은 「정의 기본값」이라고 말한다.**
F-02-c3 「지점별 기준선」과 F-12-c6 「임계값」이 **같은 뿌리**로 걸리는 자리다
(대장 주석이 「좁은 것이 이긴다 — camera → tenant → global」이라 적은 그 순서).
**`backend/kernels` 는 A 의 차선이 아니라 안 고쳤다.** 대장에 적으실지는 N 의 판단이다.

## ④ 곁다리 — 대장이 아니라 **사실**로만 적는 것 둘

1. **`copy.ts` 의 `WALL_COPY.located(known,total)` 를 아무도 안 부른다.** `Wall.tsx` 는
   같은 문장을 **글자로 직접** 적는다. 지금은 둘이 같아 아무 일도 안 나지만, 한쪽만
   고치는 날 D-470 이 이름 붙인 그 모양이 된다. (A 의 파일이 아니라 안 고쳤다.)
2. ⚠⚠ **`runserver 8000` 과 공용 SPA 3002 가 지금 죽어 있다** — 18:46 에 Docker 데몬이
   한 번 죽었다 살아나면서 `gx-shell` 안의 프로세스를 전부 걷어 갔다(컨테이너 열 개가
   「Up 1 second」). **A 는 안 띄웠다**(규약: 서버 재기동은 조율자). A 의 측정은
   스스로 올라온 `gx-nginx-e:8500`(gunicorn)과 A 가 따로 띄운 **3012** 로 했다.
3. **`/app/_fe_dist`(SPA 3002)가 서는 번들은 09-20 08:04 판이다** — 턴 Z·AA 의 화면이
   **그 안에 없다**(`settings/rules` grep 0건). A 는 공용 3002 을 건드리지 않고
   `/app/_fe_dist_aa` + **3012** 를 따로 띄워 쟀다. 병합 뒤 전량 회차 전에 **다시 빌드해
   심어야** 한다 — 안 그러면 V 가 옛 번들을 재고, 그 회색은 제품이 아니라 번들이다.
