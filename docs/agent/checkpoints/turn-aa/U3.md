# 차선 U3 — 턴 AA 체크포인트 (현장요원 모바일)

마지막 갱신: 2026-09-21 17:55 KST · 브랜치 `turn-q`

---

## ★★ 이 보고의 뼈대 한 줄 — **세종이 본 M1 은 이미 없는 화면이다**

세종이 §3 5번에 적은 M1(「노란 사과 배너」 + 「같은 사건 #268496 카드 **4장**」)은
**09-19 15:22 의 캡처**이고, 그 캡처가 찍은 번들은 **빌드 `dd77b48`** 입니다.
접기(㉡ · P-188)와 `mine` 손잡이(㉠)는 **그 다음 턴(W)** 에 들어갔고,
**지금 3002 가 내주는 번들에는 둘 다 들어 있습니다.**

⚠ **그러니 ①과 ②는 「제가 이번 턴에 고친 것」이 아닙니다.** 그렇게 적으면
안 한 일을 한 일로 적는 것이고, 다음 사람이 「턴 AA 가 접었다」를 믿고
턴 W 를 못 찾습니다. **이번 턴에 제가 한 일은 「이미 서 있는 것을 실물로 확인한 것」**입니다.
고친 것은 ③과 ⑤뿐입니다.

```
세종이 본 것   :  _fe_dist_c / _fe_dist_cJ3 / _fe_dist_w8010 계열 (턴 W 이전)
                   「줄이지 않고 전부 보여 드립니다」 1건 · 「이 사건에 나간 발송」 0건
지금 서 있는 것 :  /app/_fe_dist/assets/MobileInbox-CBR0z5XH.js  (09-20 08:04 UTC = 17:04 KST)
                   「줄이지 않고 …」 **0건** · 「이 사건에 나간 발송」 **1건** · 「내게 온 것만」 **1건**
```

---

# ① M1 — 같은 사건 카드 **1장**

## 어떻게 확인했나 — **셋을 대 봤습니다**(코드 · 서버가 내주는 번들 · 눌러 찍은 그림)

| 무엇 | 어떻게 | 결과 |
|---|---|---|
| **코드** | `MobileInbox.tsx` 의 `groups` — `Map<event_id, EventGroup>` 로 묶고 `groups.map` 이 **카드 한 장**을 그린다 | 사건당 `<Card key={group.eventId}>` **1장** |
| **서버가 내주는 번들** | `gx-shell` 안 `/app/_fe_dist/assets/MobileInbox-CBR0z5XH.js` 를 직접 grep | 「이 사건에 나간 발송」 **1건** · 「줄이지 않고」 **0건** |
| **눌러 찍은 그림** | `docs/agent/evidence/U3-Y/04_mine_built.png`(09-20 **17:33** KST · 워터마크 **버전 `0acf8d1`**) | 「**사건 1건 · 발송 기록 2건**」 · 카드 **1장** · 그 안에 채널 줄 **2줄** |

★ ③의 그림이 **지금 서 있는 번들의 그림**입니다 — 번들 mtime 은 09-20 08:04 **UTC**(= 17:04 KST)이고
캡처는 **17:33 KST** 입니다. 캡처가 번들보다 **나중**이므로 같은 판입니다.
(`docker` 안 시계는 UTC, 호스트는 KST — 이걸 안 맞춰 보면 「캡처가 더 낡았다」로 잘못 읽습니다.)

## 오늘 잰 수가 그림과 **글자까지 같습니다** [U3 실측 · 게이트 8000 · 17:44 KST]

```
GET /api/dsm/deliveries?limit=50&mine=true   →  200 · total 2 · 행 2 · **사건 1건** · 채널 {log: 2}
```
그림의 「사건 1건 · 발송 기록 2건」과 **같은 수**입니다. ⇒ 카드는 **1장**입니다.

## 접으면서 **잃은 것 0** — 네 자리를 하나씩 짚었습니다

| 잃을 뻔한 것 | 어디에 남아 있나 | 실물 |
|---|---|---|
| **어느 채널로 갔나** | 카드 안 채널 줄 — **접지 않는다**(배지만 그리고 감추지 않는다) | 04 그림에 발송 2건 → 줄 **2줄** |
| **어떻게 됐나** | 줄마다 결과(성공 / 훈련 + 시각 / 실패 + **사유 그대로**) | 05 그림: 16건 사건의 줄 **16줄 전부** 보인다 |
| **실패가 성공 뒤에 숨나** | `sort((a,b) => Number(a.succeeded) - Number(b.succeeded))` — **실패가 맨 위** + 머리줄 빨강 배지 「발송 실패 N건」 | 코드 실측 |
| **모수** | 카드 안 「이 사건에 나간 발송 N건」 + 머리 띠 「사건 N건 · 발송 기록 M건」 | 04·05 그림 둘 다 |

★ **셈의 분모가 발송인 것도 그대로입니다** — `failed` 는 카드가 아니라 **발송 행**으로 셉니다.
카드로 세면 한 사건에서 채널 셋이 실패해도 「1건」이 됩니다.

## ⚠ 접기가 **못 막는 것 하나** — 안 고쳤고, 이름만 답니다

`05_tenant_built.png` 에서 사건 **#295402 한 장 안에 「log」 줄이 16줄**입니다
(08:20 넷 · 05:50 넷 · 05:31 넷 · 04:57 넷 — **같은 채널 같은 시각이 네 벌씩**).
접기는 **사건 사이의 되풀이**를 없앴지만 **한 사건 안의 되풀이**는 그대로입니다.
⇒ 고객은 이제 「한 사건을 4번」 보지는 않지만 **「한 카드에서 16줄」**을 봅니다.

★ **이번 턴에 안 고쳤습니다.** 접는 열쇠를 `(channel, sent_at)` 으로 한 겹 더 두면
줄지만, 그것은 **「접기가 정보를 가리면 안 된다」의 반대편 벼랑**이고(되풀이 발송이
실제로 네 번 나간 사실을 지우게 됩니다) 이번 턴의 물음이 아닙니다.
**색을 적지 않고 이름만 답니다** — 다음 턴 후보.

---

# ② 배너 — **내려가 있습니다. 제가 내린 것이 아니라 턴 W 가 내렸고, 이번 턴에 「진짜 도는지」를 쟀습니다**

## 근거 — **mine 갈래를 실제로 봤습니다** [U3 실측 · 17:44 KST · 로그인 1회]

```
계정 gxseed_u4_official (턴 Z 와 **같은 계정** — 수를 비교하려면 같은 자리에서 재야 한다)
자격 대조:  GX_SEED_ROLE_PASSWORD sha256 앞12 = de08e1f17eb3   ← 턴 Y·Z 가 적은 값과 같다
POST /api/v1/auth/login (end_previous_session=true)  →  **200 · access_token 길이 300**
```

| 질의 | `total` | 행 | **사건 수** | 채널 |
|---|---|---|---|---|
| `deliveries?limit=50&mine=true` | **2** | 2 | **1** | log 2 |
| `deliveries?limit=50&mine=false` | **50**(꽉 참) | 50 | 5 | log 49 · webpush 1 |
| `deliveries?limit=500&mine=true` | **2** | 2 | **1** | log 2 |
| `deliveries?limit=500&mine=false` | **225** | 225 | **15** | log 222 · email 2 · webpush 1 |

★ **`limit` 을 500 으로 열어도 `mine=true` 는 2 입니다.** 이것이 「화면이 페이지를
잘라 낸 2」가 아니라 **서버가 수신자로 좁힌 2** 라는 증거입니다 — 화면이 걸렀다면
`limit` 을 키울 때 수가 늘어야 합니다. **2 · 50 · 2 · 225.**
★ 발송 분모 **225** — 지시가 준 수와 같습니다(턴 Z 에 probe 씨앗 61행이 빠진 뒤).
★ **턴 Z 의 「2 / 50」과 같은 수**입니다. 씨앗이 지워진 뒤 다시 쟀고 **안 바뀌었습니다.**

## 그래서 배너는?

**이미 없습니다.** 그 자리에는 지금 **사과문이 아니라 손잡이**가 있습니다:

```
[ 내게 온 것만 ] [ 기관 전체 ]                    ← Segmented (기본값 = 내게 온 것만)
지금 보는 것: 나에게 간 발송만 (서버가 골라 줍니다).   ← 범위 띠 (늘 적는다)
```

★ 지우고 손잡이를 단 것은 **턴 W** 입니다. 제가 이번 턴에 한 일은
**「안 도는데 배너만 지운 것이 아닌지」를 확인한 것**이고, 위 네 수가 그 답입니다.
★ **배너를 새로 지우지 않았습니다** — 지울 배너가 없습니다. 그 사실을 그대로 적습니다.

---

# ③ 「서버가 404」 → **「저장된 구간이 없습니다」** (고쳤습니다)

```
전:  이 사건에는 영상 구간 참조가 없습니다 (서버가 404 로 답했습니다 — 고장이 아닙니다).
후:  저장된 구간이 없습니다 — 이 사건은 영상이 저장되지 않았습니다. 위의 「스냅샷」으로 확인하십시오.
     저장된 구간이 없습니다 — 이 사건은 영상이 저장되지 않았습니다. 현장에서 본 것을
                                아래 「현장 회신」에 적어 주십시오.
```

- **원인과 다음 손이 한 줄에** 있습니다(이번 턴 불변).
- **갈래가 둘인 이유**: 턴 AA 불변 「실제 값이 없는 문구는 **비표시**」. 스냅샷이 없는
  사건에서 「위의 스냅샷으로」는 **없는 것을 가리키는 손가락**이고, 그것은 P-121 이
  지운 「관제 화면에서 확인하십시오」와 **같은 병**입니다. 다음 손은 **이 화면에 실제로
  서 있는 손잡이**만 가리킵니다.
- **접는 규율은 안 바꿨습니다** — 404 만 빈 것으로 접고 **403·500 은 그대로 오류**입니다.
  바뀐 것은 **빈 자리의 글자**뿐입니다.
- 문구가 **GX-COPY 에 없어서** 조율자에게 청했습니다 → `조율자.inbox/U3.md` ①.

# ④ 지도 단추 — **좌표가 없으면 주소로 엽니다. 이미 그렇습니다**

세종의 「지금은 좌표가 없으면 아무것도 안 합니다」는 **실물과 다릅니다.**
`mapLinkUrl(lat, lng, address)` 은 **턴 Q(`86d08b2` · P-141)** 부터 세 갈래입니다:

```
좌표 있음        →  map.kakao.com/link/map/사건위치,{lat},{lng}     (점)
좌표 없고 주소만  →  map.kakao.com/link/search/{주소}                (검색)   ← 이 갈래
둘 다 없음       →  null  (단추를 아예 안 그린다 — 없는 칸에 손잡이를 안 단다)
```

**서버가 내주는 번들에도 들어 있습니다** [U3 실측]:
`/app/_fe_dist/assets/MobileEventDetail-qscAv11U.js` 에 `link/map` **1건** · `link/search` **1건**.

★ **한 줄도 안 고쳤습니다.** 모바일 전체에서 지도 링크를 만드는 자리는 **이 함수 하나**이고
(grep 실측 · 부르는 곳도 한 곳), 폴백이 빠진 두 번째 자리는 **없습니다.**
★ 안 고친 것을 **안 고쳤다고 적습니다** — 여기에 「고쳤다」를 적으면 P-141 이 지워집니다.

# ⑤ `log` 채널 → **「훈련」** (고쳤습니다)

```
전:  ▲ log     로그에 기록됨(사람에게 안 감) 08:20
후:  ▲ 훈련    훈련 기록 08:20              ← 툴팁에 「… 사람에게 가는 발송 수단은 아직 연결되지 않았습니다」
```

- **색과 표식은 안 바꿨습니다** — 주황 · `▲` 그대로. 초록으로 그리면 그 순간 **F-10 이
  거짓으로 달성**됩니다(`deliveryOutcome.tsx` 머리말의 그 자리).
- **계약 값도 안 바꿨습니다** — `d.channel` 은 그대로 두고 **라벨에서 매핑**합니다(GX-COPY 규칙 2).
  판정기가 읽는 `data-gx-channel` 도 **`log` 원래 값**입니다. 화면의 말이 바뀌었다고
  술어가 읽는 값까지 바꾸면 게이트가 제 것을 못 찾습니다.
- 「사람에게 안 감」을 **버리지 않고 툴팁으로 옮겼습니다** — GX-COPY 머리말
  「사유는 「?」 아이콘 뒤에 산다」가 이 자리입니다.

## ⚠ 넣고 보니 **배지와 툴팁이 다른 말을 합니다** — 조율자 판정을 청했습니다

`log` 의 실물은 **「훈련용으로 고른 채널」이 아니라 「아직 안 이어진 채널」**입니다.
실제 화재 사건의 발송 줄에 「훈련」이 붙으면 당직자가 **「진짜가 아니었구나」**로 읽을
자리가 생깁니다. 시킨 대로 넣되 **그 위험을 적어 올렸습니다** → `조율자.inbox/U3.md` ③.

## ⚠ 데스크와 말이 **이 턴 동안 갈립니다** (알고 갈라 뒀습니다)

말의 정본 `deliveryOutcome.tsx` 와 데스크 `EventDetail.tsx` 는 **U1 의 자리**라
**한 줄도 안 건드렸습니다.** D-212 에 걸린 자리를 제가 **알고** 만들었고,
**한 턴짜리로 끝내 달라**고 조율자에게 올렸습니다 → `조율자.inbox/U3.md` ④.

---

# ★ 편리성 #5 (`u3_receive_to_ack`) — **아직 못 잽니다. 씨앗이 안 심겼습니다**

## **세 번** 쟀고 **세 번 다 0행**입니다 [U3 실측 · ORM 직독 · **17:44** · **17:51** · **17:54** KST]

```
DetectionEvent 27  ·  response_state:  closed 17 · in_progress 9 · **occurred 1**
occurred 사건:  #326411 (data_source=drill;run=20260921T102945 · warning)
   └ 그 사건의 DeliveryRecord :  **0행**        ← 턴 Z 12:31 과 **똑같다**
DeliveryRecord 전량 225  ·  실패 1  ·  채널 log 222 · email 2 · webpush 1
```

## **안 심겼습니다.** 그대로 적습니다

턴 Z 에 제가 「U1 이 발송 한 행만 붙이면 한 번 눌러 끝난다」고 넘겼고, 이번 턴 지시도
「U1 이 심습니다」였습니다. **17:54 현재 안 심겼습니다** — 열한 분에 걸쳐 세 번 쟀고
세 번 다 같은 수였습니다(한 번만 재고 「없다」고 적으면 **심는 중이었을 수도** 있습니다).
- 배선은 **여전히 완전합니다**(턴 Z 에 네 자리를 짚었고 이번 턴에 **고칠 것 0**).
- 발송이 0행이면 사건은 **M1 에 아예 안 뜨고** `startReceiveToAck` 이 안 불립니다.
  상세로 바로 들어가 접수해도 `finishReceiveToAck` 은 **시작 없는 끝**이라 `null` 입니다.
- ⇒ **없는 자료로 잰 수는 수가 아닙니다.** 대장 `convenience_u3.json` 은 **한 자도 안 고쳤습니다.**
- U1 에 쪽지를 다시 보냈습니다(필요한 행의 모양 · 붙일 계정 후보까지) → `U1.inbox/U3.md`.

★ **브라우저를 한 번도 안 띄웠습니다.** 자료가 없어 띄워 봐야 잴 것이 없고,
띄우면 남의 세션을 끊습니다. **안 잰 것을 안 쟀다고 적습니다.**

---

# 프런트 타입검사 — **우리 변경이 더한 오류 0건** · 종료 코드 함께

`gx-fe-build` 안 `/tmp/u3check` 에 **격리 복사**(작업본 `frontend/src` 통째 + `/app/tsconfig*.json`
+ `node_modules` 심링크). 원본 `/app` **무손상** · 호스트에는 아무것도 안 만들었습니다.

```
node /app/node_modules/typescript/bin/tsc -p tsconfig.app.json --noEmit
  TSC_EXIT=2 · 총 오류 줄 **3234**            ← 기준선 3234 와 **같다**
  MobileInbox.tsx  0줄  ·  MobileEventDetail.tsx  0줄
```

★ 이것은 **제 파일 둘만의 수가 아니라 17:4x 시각 작업본 통째의 수**입니다 — 그 시각
`frontend/src` 에는 다른 차선 열다섯 파일의 이번 턴 변경이 이미 들어 있었고
(`App.tsx` · `copy.ts` · `AuditLog.tsx` · `ControlDashboard.tsx` · `FocusQueue.tsx` ·
`NotifySettings.tsx` · `SettingsRules.tsx` · `time.ts` · `types.ts` …),
**그 합본이 기준선 3234 그대로**였습니다. 다만 **17:4x 의 판**이라
그 뒤 들어온 변경은 안 들어 있습니다 — 병합 뒤 한 번 더 재야 합니다(조율자 몫).

★ **침묵을 안 믿고 A/B 로 확인했습니다.** 제가 새로 넣은 상수 바로 옆(150행)에
일부러 형 오류를 심으니 **3234 → 3237**(TS2322 · TS2448 · TS2454, 전부 `MobileInbox.tsx`).
되돌리니 **다시 3234 · 모바일 0줄.** ⇒ **판정기가 제 줄을 실제로 봅니다.**
심은 것은 컨테이너 안 사본이고 되돌렸습니다(컨테이너 잔여 0).

★ **첫 판은 3234 가 아니라 15374 였습니다** — `node_modules` 심링크를 안 걸어
`Cannot find module 'react'` 가 파일마다 났습니다. 그 판의 「모바일 33줄」은
**제 오류가 아니라 모듈 해석 실패**였고, 심링크를 걸자 **0줄**이 됐습니다.
종료 코드와 **총량**을 같이 안 봤으면 33줄을 제 것으로 적을 뻔했습니다.

# 금지구역

```
bash docs/agent/verify_gates.sh --gate forbidden-zone   →  PASS · 금지구역 변경 0건 · rc=0
```

# 고친 파일 — **둘뿐**

| 파일 | 무엇 |
|---|---|
| `frontend/src/features/mobile/pages/MobileInbox.tsx` | ⑤ `log` → 「훈련」 라벨 매핑(상수 셋 + 배지·결과 두 자리) |
| `frontend/src/features/mobile/pages/MobileEventDetail.tsx` | ③ 구간 티켓 빈 자리 문구 둘(상수 셋 + `emptyText` 한 자리) |

**새 라우트 0 · 새 쓰기 문 0 · 제품 쓰기 0 · 시험 0개 · 대장 0자 · 서버 재기동 0회.**
`deliveryOutcome.tsx` · `EventDetail.tsx` · `copy.ts` · `GX-COPY_v1.md` **전부 0줄**(남의 자리).

# 잰 시각(분 단위) · 로그인

| 시각(KST) | 무엇 | 상태를 바꿨나 |
|---|---|---|
| 17:3x~17:4x | 소스·번들 grep(호스트 `backend/_fe_dist*` 8벌 + `gx-shell` 안 `/app/_fe_dist`) | 안 바꿈 |
| **17:44** | **ORM 직독** — 사건/발송/채널/`response_state`/수신자 분포 | 안 바꿈 |
| **17:44** | **로그인 1회** — `gxseed_u4_official` + `deliveries` 네 갈래 · `events` 한 갈래 | 세션만 |
| 17:4x | `gx-fe-build` 격리 타입검사(기준선 · A/B · 되돌림) | 컨테이너 사본만 |
| 17:5x | `verify_gates.sh --gate forbidden-zone` | 안 바꿈 |
| **17:51** | **ORM 재독**(씨앗 확인 — 여전히 0행) | 안 바꿈 |
| **17:54** | ORM 3독(마지막 확인 — 여전히 0행) | 안 바꿈 |

- **로그인 총 1회.** 턴 Z 에 일곱 번 들어가 Q 의 창과 겹쳤던 병을 **한 번으로 줄였습니다** —
  재야 할 것을 먼저 다 적고 **한 세션 안에서 다섯 갈래를 연달아** 쟀습니다.
- **429 0건.** `anon = 5/minute` 한도에 안 닿았습니다.
- **비밀은 쿼리·argv 에 0** — 전부 `docker exec -e 이름만` 으로 넘겼습니다.
  자격 대조는 sha256 **앞 12자**로만 적습니다(`de08e1f17eb3`).
- **서버 재기동 0회** · **브라우저 0회** · **저장소에 남긴 임시 파일 0**
  (측정 스크립트는 스크래치패드 → `docker cp` → `/tmp`).

---

# 못 한 것 · 재지 못한 것

1. **편리성 #5 의 둘째 값 — 못 쟀습니다.** 씨앗(발송 1행)이 **안 심겼습니다**(U1 몫).
   두 번 재서 두 번 다 0행이었고, **대장은 한 자도 안 고쳤습니다.**
2. **바뀐 화면을 눌러서 못 봤습니다.** ③·⑤ 는 **소스에만** 들어갔고, 3002 가 내주는
   번들은 아직 **`0acf8d1`**(09-20 17:04 KST) 입니다. 빌드·배포는 이번 턴 제 소유가
   아니고(`deploy.sh` 는 `_fe_dist` 를 통째로 갈아 끼웁니다) **남의 창을 끊습니다.**
   ⇒ **조율자가 병합 뒤 한 번 지을 때** 확인돼야 합니다. 그 전까지 ③·⑤ 는
   **「소스에 섰다」이지 「고객 화면이 그렇다」가 아닙니다.** 색을 적지 않습니다.
3. **①·②를 새 캡처로 못 찍었습니다.** 대신 **번들 grep + 09-20 17:33 캡처 + 오늘 API 수**
   셋을 대 봤고 셋이 같은 수를 냅니다. 그래도 **오늘 찍은 그림은 아닙니다** — 그렇게 적습니다.
4. **한 카드 안 16줄**(①의 ⚠) — 이름만 댔고 **안 고쳤습니다.**
5. **데스크 화면의 「로그에 기록됨(사람에게 안 감)」** — U1 의 자리라 **안 고쳤습니다.**
   이 턴 동안 두 화면의 말이 갈립니다(알고 갈라 뒀고 조율자에게 올렸습니다).
6. **「훈련」이 참말인지** — 제가 정할 일이 아니라 **판정을 청했습니다.** 지금 실물은
   시킨 대로입니다.
7. **웹푸시 자격 소실 · 실카메라 0/4** — 턴 W~Z 이월 그대로. **안 건드렸습니다.**

---

# 자진 오판

### ① **세종의 「지금은 …」 넷 중 **셋**이 이미 고쳐져 있었는데, 하마터면 넷 다 다시 지을 뻔했습니다**

지시문을 그대로 읽으면 다섯 가지가 전부 「지금 안 되는 것」입니다. 그런데 ①·②·④ 는
**턴 Q·W 에 이미 섰고**, 세종이 본 것은 **09-19 캡처**였습니다.
소스를 먼저 읽지 않고 손을 댔으면 **이미 있는 것을 다시 지어 놓고 「고쳤다」고 적었을** 것이고,
그것은 턴 W 의 공을 지우는 일입니다.
⇒ **지시서의 「지금은 …」도 입력이지 측정이 아닙니다.** 턴 Z 에 제가 판정기의 사유를
믿을 뻔한 것과 **같은 병**이고, 이번엔 손대기 전에 쟀습니다.

### ② **타입검사 첫 판에서 「모바일 33줄」을 제 오류로 적을 뻔했습니다**

`node_modules` 심링크를 빠뜨린 첫 판이 **15374줄**을 냈고 그중 모바일 두 파일이 33줄이었습니다.
`TSC_EXIT=2` 는 **양쪽 판에서 똑같이** 나와서 종료 코드만으로는 안 갈렸습니다.
갈라 준 것은 **총량(3234 대 15374)** 이었습니다.
⇒ 규약은 「종료 코드를 출력과 함께 찍어라」인데, 이 자리에서는 **기준선 총량까지
대 봐야** 갈립니다. 다음 사람을 위해 적어 둡니다: **`ln -sfn /app/node_modules <격리자리>/node_modules`**.

### ③ **컨테이너 시계가 UTC 인 것을 하마터면 「캡처가 더 낡았다」로 읽을 뻔했습니다**

`/app/_fe_dist` 는 `Sep 20 08:04`, 캡처는 `Sep 20 17:33` — 앞의 것이 **UTC** 라
실제로는 번들이 **17:04 KST** 이고 캡처가 **나중**입니다. 안 맞춰 봤으면
「지금 서 있는 번들은 저 그림보다 뒤」라는 **반대 결론**을 적을 뻔했습니다.

---
---

# ★★ 덧붙임 — **조율자 판정을 받고 ⑤를 한 턴 안에 두 번 고쳤습니다** (2026-09-21 18:0x KST)

**앞 내용은 한 자도 안 지웠습니다.** 판정 전의 제 글자가 무엇이었는지가 남아 있어야
다음 사람이 「왜 한 턴 안에 말이 두 번 바뀌었나」를 되짚습니다.

## 조율자가 제 손으로 확인해 준 것

```
backend/kernels/k2_notify/rule_admin.py:91
  "log": "훈련 채널 — **사람이 아니라 로그에 도달한다.**
          운영 규칙에 넣으면 당직자가 못 받는다"
```

⇒ 제가 넣었던 「훈련」은 **거짓말이 아니었습니다** — 제품 자신이 그렇게 부릅니다.
**그런데 붙는 자리가 틀렸습니다.**

## 판정 한 줄 — **배지는 사건이 아니라 발송을 말해야 한다**

`log` 는 **사건의 성질이 아니라 그 발송이 간 곳**입니다. 화재 사건 카드 줄에 「훈련」이
붙으면 사람은 그것을 **사건에 붙은 말**로 읽고 「이건 진짜가 아니었구나」로 읽습니다.
채널 하나를 고객 말로 바꾸려다 **사건 전체를 가짜로 만드는** 더 큰 거짓말을
새로 만들 뻔했습니다.

★ 그리고 **세종의 읽기가 옳았습니다 — 실제로 안 갑니다.** 제가 잰 수가
그것을 말합니다: **발송 225건 중 `log` 가 222건.** 위 커널 주석 그대로라면
**진짜 사건의 알림이 거의 전부 당직자에게 안 가고 있습니다.** 그런데 화면에
**빨강이 없습니다** — `succeeded=True` 라 전부 「성공」 쪽입니다.

⇒ 고칠 것은 **그 사실을 가리는 것이 아니라 고객 말로 정확히 말하는 것**입니다.
이번 턴 불변은 **「정직한 회색을 고객 말로」이지 「정직한 회색을 지우기」가 아닙니다.**

## 고친 글자

```
09-19 (세종이 본 것) :  ▲ log     로그에 기록됨(사람에게 안 감) 13:01
이 턴 첫 판          :  ▲ 훈련    훈련 기록 08:20                        ← 물렀다
이 턴 지금           :  ▲ 기록만  사람에게 안 갔습니다 — 기록에만 남았습니다 08:20
                        + 카드마다 한 줄:
                          이 사건의 알림 N건이 기록에만 남았습니다 —
                          관리자에게 알림 채널 등록을 요청하십시오.
```

- **「훈련」을 이 자리에서 뺐습니다.**
- **`data_source=drill` 의 「훈련」 배지는 안 건드렸습니다** — 그건 **사건의 성질**이 맞습니다.
  이 화면엔 아직 그 배지가 없고(모바일의 「훈련」은 `MobileSettings.tsx` 의 **시험 발송**뿐),
  언젠가 한 카드에 둘이 같이 서는 날 **절대 같은 낱말로 안 그린다**를 주석에 표로 박았습니다:

  | 배지 | 무엇을 말하나 |
  |---|---|
  | 사건 배지 「훈련」 | 이 **사건**이 훈련이다 (사건의 성질) |
  | 채널 배지 「기록만」 | 이 **발송**이 사람에게 안 갔다 (발송이 간 곳) |

- **계약 값 그대로** — `data-gx-channel` 은 여전히 `log`. 색·표식도 그대로(주황 · `▲`).
- **`logOnlyCount` 를 `failedCount` 와 따로 셉니다.** 실패는 「보내려다 못 보낸 것」,
  이쪽은 「보냈는데 갈 곳이 사람이 아니었던 것」 — **다른 수**입니다.
  한 수로 합치면 둘 중 하나가 거짓말이 됩니다.

## ⚠ 판정을 **한 군데 좁혀 실행했고, 그것을 적어 올렸습니다**

판정문은 「**그 줄의 설명** + 다음 손」이었는데 저는 **다음 손만 줄에서 떼어 카드 머리로**
올렸습니다. 사유 둘: ① 다음 손(「알림 채널을 등록하라」)은 **발송이 아니라 기관**에 대한
손이라 발송마다 되풀이하면 **같은 부탁을 열여섯 번** 한다 · ② [실측] 한 카드에 `log` 줄이
**16줄**인 자료가 실재한다(`evidence/U3-Y/05_tenant_built.png` · #295402) — 거기 한 문장을
열여섯 벌 깔면 **카드가 사과문으로 덮이고**, 그건 이번 턴이 걷어내기로 한 바로 그 모양이다.

⇒ **원인은 줄마다 · 원인+다음 손 한 줄은 카드마다 한 번.** 어느 쪽에서도 사실이 안 빠집니다.
**「줄마다」를 고집하시면 한 줄짜리 고침**이라고 조율자에게 적어 올렸습니다.

## 시킨 것 둘 — 했습니다

1. **계약 값 그대로 뒀습니다**(`data-gx-channel` = `log`).
2. **U56 쪽지** → `docs/agent/checkpoints/turn-aa/U56.inbox/U3.md`.
   **222/225 를 분모째** 넘겼고, ORM·HTTP 두 갈래로 같은 수가 나온 것,
   그리고 **「발송 실패 1건」이 이 병을 못 잡는 이유**(실패 ≠ 사람 아닌 곳에 도달)를 적었습니다.
   제 카드의 다음 손이 「관리자에게 알림 채널 등록을 요청하십시오」이고 **그 요청을 받는
   화면이 U56 의 `NotifySettings.tsx`** 라, 말이 안 맞으면 제 카드가 사람을
   **막다른 곳으로** 보낸다는 것도 함께 적었습니다.

## (a)(c) 도 처리했습니다 → `조율자.inbox/U3.md` 뒤에 덧붙임

- **(a)** 「기록만」 묶음 셋을 등재 청구에 **추가**했고, **「훈련 / 훈련 기록」은 철회**했습니다.
- **(c)** 갈린 줄을 **파일·줄번호로** 적었습니다:
  `deliveryOutcome.tsx` **56 · 68 · 74 · 87~96 · 104~116** · `EventDetail.tsx` **54 · 729**.
  ★ **정본은 `deliveryOutcome.tsx:74`(`outcomeLabel`) 하나**라 거기만 고치면 데스크는 따라옵니다.
  ★ **덤으로 찾은 셋** — `NotifySettings.tsx` **153 · 445 · 500** 에도 「사람에게 안 감」이
    있습니다(U56 파일 · **안 건드렸고** U56 에 알렸습니다).
  ⇒ 이 턴 뒤 「사람에게 안 감」 계열이 **세 파일 · 일곱 자리**에 흩어져 있습니다.

## 고친 뒤 다시 잰 것

```
gx-fe-build · /tmp/u3check 격리 · ln -sfn /app/node_modules
TSC_EXIT=2 · 총 오류 줄 **3234**(기준선 그대로) · 모바일 두 파일 **0줄**
```

★ A/B(일부러 심어 3234→3237)는 **이 세션 앞 판에서** 같은 격리 자리·같은 파일 경로로
이미 했습니다. 판정기가 이 파일을 본다는 것은 그때 확인됐고, **이번 판에서 다시 심지는
않았습니다** — 그 사실을 그대로 적습니다.

## ★ 이 덧붙임이 바꾸는 것 — **앞의 「못 한 것」 2번은 그대로입니다**

③·⑤ 는 **여전히 소스에만** 들어갔습니다. 3002 가 내주는 번들은 아직 `0acf8d1` 이고,
**이번에 두 번째로 고친 글자도 고객 화면에는 아직 없습니다.**
**「소스에 섰다」이지 「고객 화면이 그렇다」가 아닙니다 — 색을 안 적습니다.**

## 자진 오판 ④ — **제품 주석 한 줄을 안 읽고 낱말을 골랐습니다**

「훈련」이 참말인지 **의심은 했고 판정을 청한 것까지는 옳았지만**, 그 판단에 쓸 증거가
`rule_admin.py:91` 에 **한 줄로 이미 있었습니다.** 조율자가 그것을 찾아 줬습니다.
제 쪽지에는 「`log` 의 실물은 훈련 채널이 아니라 아직 안 이어진 채널」이라고 적었는데,
**제품은 그것을 「훈련 채널」이라 부르고 있었습니다** — 제 전제가 틀렸고,
**틀린 전제로 물은 물음이 우연히 옳은 답을 불렀습니다.**
⇒ 낱말을 고를 때는 **화면·사전만이 아니라 그 값을 만드는 커널의 글자까지** 봐야 합니다.
턴 Z 의 「판정기가 적은 사유는 입력이지 측정이 아니다」와 **같은 자리**입니다.
