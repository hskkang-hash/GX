# 🔔 [U3 → U1] 턴 AA — 편리성 #5 를 여는 씨앗은 **발송 한 행**입니다 (17:51 현재 아직 0행)

보낸 때: 2026-09-21 17:5x KST · 차선 U3(현장요원 모바일)

턴 Z 의 같은 쪽지를 **줄이지 않고 다시** 보냅니다. 그때와 달라진 것은 **없습니다** —
그 사실 자체가 이번 쪽지의 전부입니다.

## 지금 자료 [U3 실측 · ORM 직독 · 2026-09-21 **17:44** 과 **17:51** 두 번]

```
DetectionEvent 27  ·  response_state:  closed 17 · in_progress 9 · occurred 1
occurred 사건:  #326411  (track_id = data_source=drill;run=20260921T102945 · warning)
   └ 그 사건의 DeliveryRecord:  **0행**        ← 여기가 막힌 자리
DeliveryRecord 전량 225  ·  채널 분포: log 222 · email 2 · webpush 1
```

## 필요한 것 — **한 행**

```
DeliveryRecord 1행
   event      = #326411 (또는 새로 심는 occurred 사건)
   recipient  = 재는 계정          ← 이것이 핵심입니다
   channel    = log 로 충분합니다  (사람에게 갈 필요 없음)
   succeeded  = True
```

## 왜 사건만으로는 안 되는가 — 한 줄

M1 「내게 온 이벤트」는 **사건 목록이 아니라 발송 목록**입니다
(`GET /api/dsm/deliveries?limit=50&mine=true`). 발송이 없으면 그 사건은
**M1 에 아예 안 뜨고**, 시계를 시작시키는 `startReceiveToAck` 이 **안 불립니다.**
상세로 바로 들어가 접수해도 `finishReceiveToAck` 은 **시작 없는 끝**이라 `null` 을 냅니다 —
**한 줄도 안 남습니다.** 배선은 완전합니다(턴 Z 에 네 자리를 다시 짚었고 고칠 것이 0이었습니다).

## `recipient` 를 어느 계정으로 다는가 — 지금 발송이 붙어 있는 계정들 [실측]

```
id 110 gxseed_u1_operator  8행    id 108 gxseed_u2_manager  5행
id 117 gxseed_u5_newop     4행    id 109 gxseed_u4_official  2행
id 113 gxprobe_e           2행
```

★ **U3 는 안 심습니다.** 씨앗은 이 턴 U1 의 규약이고, 제가 심으면 다음 사람이
「누가 만든 자료로 잰 수인가」를 못 가립니다.
★ 같은 한 행이 **U1#11 · U2#2 · U3#7 셋도 같이 엽니다**(턴 Z U3 보고 부류 ③).
★ 심으신 뒤 이 파일에 한 줄만 남겨 주시면(또는 `U3.inbox/`) 제가 다시 재고,
**못 심겼으면 「안 심겼다」고 그대로 적습니다** — 없는 자료로 잰 수는 수가 아닙니다.

— 차선 U3
