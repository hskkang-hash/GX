# Q 체크포인트 (턴 W · 파 3 턴 3 「이어서」) — 2026-09-19 · **마지막**

## 닫음 (눌러서 본 것만)

### ㉠ P-189 증거 재읽기 — **게이트 17종 → 18종**
- 새 게이트 `evidence-roundtrip` · `scripts/verify_evidence_roundtrip.py`.
  `verify_gates.sh` 셋 다 같은 변경에 넣었다(`ALL_GATES` · `gate_evidence_roundtrip()` · dispatch).
  **러너로 돌렸다 · exit 0** · `[입력] 4건`. 증거 4종(FC P-118 · 온보딩 P-159 ·
  도달 P-106 · 걷기 UX-WALK) 전부 「다시 읽어도 같은 수」.
- `verify_gate_counts` 초록(**18종**) · `verify_tool_selftest` 초록(표본 없이 태어난 새 도구 0).
- **출생 표본**: 살아 있는 FC 증거를 cp949 왕복으로 깨뜨리면 같은 판정기가 **29/48 → 8/48**.
  예외 0 · 둘 다 「판정 끝」이라 말한다. 깨진 증거는 파싱되고 **수만 틀린다**.
- **utf-8 못 박고 쓴 즉시 다시 읽어 대는 자리 넷**: `verify_click_completes.py`(증거 쓰기 —
  `decode("utf-8","replace")` 를 엄격으로 · 깨지면 **안 쓰고 회색**) · `measure_onboarding_t.py`
  (+ 적힌 수 대조) · `walk_scenarios.py`(회차본·대장) · `verify_feature_reach.py`.
- ★★ **2차 — 내가 만든 거짓 회색을 V 가 잡았다. 고쳤다.** 되읽기가 「**가리기 전** 원문」과
  「쓴 파일」을 견줘, `appkey=` **18곳**이 언제나 가려지는 탓에 `--measure` 가 **매번 회색**
  이었다. 칸을 **둘로** 갈랐다: ① 되읽기 = **쓴 것 vs 다시 읽은 것** · ② 가리기 =
  **가리기 전후의 판정 수가 같은가**(+ 가린 자리 수). ②를 빼지 않은 이유 — 빼면
  「가리기가 수를 바꿔도 아무도 안 본다」가 된다.
  **진짜 증거 162KB 로 되짚었다**: 가린 자리 18 · 수 (29,4,15) → (29,4,15) · **초록**.
  종전 판으로 같은 것을 먹이면 **언제나 회색**. 변이(종전 판을 도로 심기)로 자기시험이
  **빨강**을 내는 것도 확인했다. **끄지 않았다** — cp949 왕복 판은 여전히 빨강(29→8).
  증거: `docs/agent/evidence/P-189/false_grey_redaction_20260919.md`
  ⚠ `--measure` 는 **안 돌렸다** — V 의 증거를 내 측정으로 덮게 된다.
- ★ **세종 전제 정정**: 턴 U 증거(커밋 `7f90ec5`)는 **안 깨졌다** — U+FFFD 0 · 한글 13,313자 ·
  나이를 무시하면 **28/48** 그대로 재현. 28→0 의 원인은 `MAX_AGE_HOURS = 12` 다.
  깨진 옛 증거는 **다시 쓰지 않았다**(메모리 안에서만 깨뜨려 먹인다).

### ㉡ P-190 두 도구 한 행 한 정본 — **빨강을 먼저 보고 고쳤다**
- 판정 자리: **`verify_click_completes.py` 자기시험 ⑧**(`canon_disagreements()`).
  `click-completes` 게이트가 이미 그 자기시험을 부르고, 깨지면 `--measure` 도 멈춘다.
- **고치기 전 빨강 11행** · 재현 절차까지 적었다 →
  `docs/agent/evidence/P-190/before_fix_20260919.md`. 조율자가 지목한 **U5#15 가 그 안에 있다.**
- **U24 문안 받았다** — `onboarding_48.md:1101` 에 **글자 그대로** 넣었다.
- **U1#1·#2 기대식**을 화면이 부르는 것으로: `auth/profile` → `user/get-user-detail` ·
  `link-state` 따로 호출 → `frame.link` 하나. 도구 쪽 「기대식 오류」 문구도 같이 지웠다.
- `onboarding_48.md:595`(턴 T 촬영 기록)는 **지우지 않고** 턴 W 정정을 덧붙였다.
- 분모·측정 행은 안 건드렸다 — `--check` 여전히 **48 · 두 칸 35 · 정본 없음 13**.

### ㉢ 걷기 로그아웃 닫힘 — **2차까지 갔다. 살아 있는 걷기로 닫았다**
- 1차: 등록부 + `atexit` 그물. 죽는 길 셋(`exception`·`sysexit`·`KeyboardInterrupt`)
  전부 닫힘 — **열린 채 0/3** · 음성 대조(등록 없이 죽이기)는 **열린 채**.
- **1차는 모자랐다.** 조율자가 `deploy.sh` 자격 누락을 고쳐 W5(u2)·W6(u5)가 처음 걸리자
  **연 3 · 닫은 1 · 열린 채 2** 가 났다. 내 죽는 길 시험이 세션을 **하나만** 열었기 때문에
  못 잡았다 — 걷기는 사람 **셋**으로 로그인한다.
- 원인 **둘**(사유를 적게 해 둔 덕에 한 번 돌려서 둘 다 글자로 나왔다):
  ㉠ 브라우저 로그아웃이 미덥지 않다 — `localStorage.userInfo` 에 토큰이 없다(쿠키에 든다).
     `walk_states.py:774` 가 **턴 G 에 이미 적고 정본으로 옮겼는데 이 파일만 옛 길이었다**(D-369).
  ㉡ 정본(`capture_screens._release_session`)을 `sync_playwright()` 안에서 부르면
     `SynchronousOnlyOperation` — 비동기 문맥이다. **딴 실에서** 부르니 선다.
- 고침: 등록부가 **이름까지** 든다 · 닫기는 **제품 길 → 정본** 두 단 · 정본은 딴 실 ·
  **어떻게 닫혔는지**를 `session_log` 에 남긴다.
- **눌러서 본 것 (살아 있는 걷기 · 대장 7회차 `2026-09-19T09:48:22`)**:
  `연 3 · 닫은 3 · **열린 채 0**` · `session_closed=True` · **walk exit 0** · 완주 5/5.
  조율자가 열려 있다고 한 **u2·u5 세션도 이 회차가 정리했다.**
- **변이 시험**: 「브라우저 길이 지면 정본으로 안 넘어간다」를 심으면 자기시험이
  `닫은 0 · 열린 채 3` 으로 **그날 모양 그대로** 빨강을 낸다.
- 판정 줄 「**세션이 열린 채 끝난 것 0**」 — 분모(연 수)를 같이 적는다. 이 분모 덕에
  조율자가 「닫은 1」을 볼 수 있었다.
- 증거: `docs/agent/evidence/UX-WALK/session_close_on_death_20260919.md` (1차 + 2차)

### ㉣ W4 `NOT_WALKED` 사유 등재
- `evidence-roundtrip` **게이트 출력**에 이름과 사유로 올라간다(러너에서 실제로 찍혔다):
  `[안 걸음] 1건 — 초록이 아니다. 안 걸은 것이다 / W4 부하용 혼합 시나리오(← perf_load.S4) …`
- `count_walk` 가 「안 걸음」을 **수로** 센다 — 목록에서 지우면 수가 변하고 재읽기가 말한다.
- `walk_scenarios.judge` 에도 같은 줄을 세웠고, **사유가 빈 이름은 빨강**(면제 금지 · D-264).

### ㉤ probe 필터 — 부른 자리
- `measure_onboarding_t.exclude_probe_rows()` → **U2#2 미처리 셈**(`n_srv`).
  `probe_events.is_probe_track` + `PROBE_CAMERA_HINT` 둘 다 **그 파일에서 빌렸다**.
  ⚠ `/api/dsm/events` 응답에 `track_id` 칸이 없어서 첫째 셈법만으로는 못 거른다.
  뺀 수를 evidence 문장에 적는다(조용히 빼지 않는다).

## 손 위 / 다음 한 줄
- 없음. 살아 있는 걷기 한 회차만 남았고 그것은 **창 문제**다(아래).

## 안 한 것 (그리고 왜)
- (해소) 살아 있는 걷기 — 18:45~19:05 창을 `_조율자.md` 에 적고 **돌렸다.** 위 ㉢ 참조.
- FC `--measure` 재측 안 했다 — 재는 것은 V 다.
- **내가 고친 정본으로 내가 잰 수는 하나도 적지 않았다.** FC·온보딩 수는 V 가 낸다.
