# U56 체크포인트 (턴 W) — 18:25 · **끝낼 때 판** (조율자 요청 셋 반영)

## 닫음 (눌러서 확인한 것만)

- **㉠ `/dsm/system` 404 — 닫혔다.** 브라우저로 `gxseed_u5_sysop` 로그인 →
  `/dsm/system` [실측 2026-09-19 17:41 KST]:
  **분모 11건 · 404 0건** · `GET /api/dsm/ops/backup/declaration` → **200**.
  네트워크를 **두 출처**로 셌다(playwright `response` · 브라우저 `performance` 자원 목록) —
  둘 다 11 로 같다. 증거: `docs/agent/evidence/U5-BACKUP-404/`(README · PNG · 원본 JSON).
  · **404 URL 은 하나였다.** 선등록 ㉡ 은 「읽기 문 둘」로 봤지만 `backup`·`declaration`
    은 한 경로의 두 조각이다 → **읽기 문 1 로 닫았다.**
  · **쓰기 면 안 났다** — 같은 경로 POST 는 **405**. 시험이 지킨다
    (`test_post_is_not_opened_on_this_path`).
  · ⚠⚠ **가장 큰 함정**: `gx-shell` 안 `runserver` 는 `--noreload` 라 백엔드를 고쳐도
    안 들어간다. **화면 번들이 부르는 곳이 바로 그 8000 이다.** 고친 직후 같은 경로를
    두 출처로 재니 nginx:8500 **401**(문 있음) / runserver:8000 **404**(옛 코드)로
    갈렸다. 17:38 에 재기동(pid 38479 → 91933)하고서야 눌러 볼 수 있었다.
    `docker restart gx-gunicorn-e` 만으로는 **모자라다**.

- **㉡ WS-26 저장 상한 — 화면이 선언과 이름을 말한다.** 같은 판에서 눈으로 봤다:
  상한 **50 GB**(**값 안 바꿨다** · 세종 이의 #2) · 쓰는 중 0.0006 GB ·
  어디서 선언하나 `GX_STORAGE_CAPACITY_GB` + 「**환경 선언** — 컨테이너 환경에
  적혀 있습니다」. 더한 것은 값이 아니라 **출처**다(`storage_capacity_source()`).
  백업 카드에도 같은 규약으로 「어디서 선언하나 = `OPS_BACKUP_*` 넷」을 붙였다.

- **시험** — `tests/test_u56_backup_declaration.py` **9건 새로**, U56 전체 **94/0**,
  `test_api_contract` 포함 재확인 **82/0**. tsc `--noEmit` 저장소 전체 **오류 0**.
  ⚠⚠ **그 94/0 은 참이지만 좁았다.** 새 라우트는 `/api/dsm/` **진입면 계약**을 건드리는데
  그 계약을 지키는 시험은 `test_f05_event_api.py`(차선 F 파일)에 있다 — **내 시험만
  돌리면 내 문만 보인다.** 옆 차선의 넓은 회귀가 그것을 멈춰 세웠고, 그것이 옳다.
  **다음에 라우트를 내는 차선은 `test_f05_event_api.py` 를 같은 판에서 돌려라.**

- **㉤ 조율자 요청 둘 — 닫았다** [실측 18:0x]
  · **라우트 분류**: `route_classes.yaml` 에 `GET /api/dsm/ops/backup/declaration:
    session_only`. 코드가 `inbound_key=True` 를 **선언하지 않으므로** 좁은 쪽이 맞다.
    **고치기 직전에 다시 읽고** U3 의 `cameras/pulse: inbound_key_allowed` 줄이 살아
    있는지 확인한 뒤 한 줄만 끼웠다 — 덮어쓰지 않았다.
    게이트 재측: **라우트 763 · 대장 763 · 미분류 0**. `--freeze` 는 **안 눌렀다**
    (남은 FAIL `declared 1→2` 는 U3 의 pulse 개방 · 조율자가 D-501 로 한 번 누른다).
  · **넓은 회귀의 빨강**: 재현하니 **둘이 아니라 셋**이었고, **둘만 내 것**이었다.
    내 것 둘(`EntrySurfaceIsLockedTest` 의 `pinned_by_name` · `count_is_not_zero`
    108≠109)은 `test_f05_event_api.py::EVENT_ENTRY_SURFACE` 에 한 줄 + 사유로 닫았다
    (명세 §9 가 「새 진입면은 문을 낸 차선이 같은 변경에서 갱신한다」로 정한 자리 ·
    F 의 턴 V 줄이 살아 있는지 확인 후 삽입). 재측 **102 passed / 1 failed**.
    셋째(`test_f05_inbound_api_key::test_the_key_reaches_only_the_declared_routes`)는
    **U3 것이다** — 실측으로 못 박았다: 런타임 `inbound_key=True` 는 `/cameras/pulse` ·
    `/events` **둘뿐**이고 내 라우트는 **그 집합에 없다**. `INBOUND_KEY_ALLOWED` 는
    한 칸도 안 건드렸다(넓힌 사람이 선언하는 자리다).

## 손 위
- **㉥ 「재시작 요청」 0건 문구 (조율자 요청 2 · 문안은 U24)** — **코드는 섰고 화면은 못 눌렀다.**
  넣은 것: `requests` 자원에 `isEmpty` · 그 `StateBoundary` 에 `emptyText`/`emptyNext`
  (U24 문안 그대로) · 그리고 **표가 숨기던 분모** 한 줄(`전체 N건 중 최근 M건만 보입니다`
  — 서버가 `limit` 20 으로 자르는데 `pagination={false}` 라 21건째부터 조용히 사라지고
  있었다). tsc 저장소 전체 **오류 0**.
  **같은 눈으로 본 것** — 「가만히 두면 생기는 자리인가」: **아니다.** 이 표에 행을 만드는
  자리는 저장소 전체에서 **하나**다[실측 · 시험으로 고정]. 그래서 U24 의 「누르면 생깁니다」
  문안이 참이고, 사전 기본 문구(「새 자료가 생기면 나타납니다」)는 **이 자리에서 거짓말**이다.
  시험 셋으로 못 박았다(`test_u56_backup_declaration.py` · 12/12):
    · 0건 테넌트 → **200 + `requests: []` + `total: 0`**(화면이 `isEmpty` 로 보는 그 값)
    · 0 → 누름 → **1**(「눌러야 생긴다」가 참인가)
    · **행을 만드는 자리가 `apps/dsm/api_u56.py` 하나뿐인가** — 늘면 빨개진다.
      ⇒ 배치가 이 표를 쓰기 시작하면 **문구가 먼저 거짓이 되고 시험이 먼저 멈춘다.**
  **다음 한 줄: 새 테넌트의 admin 계정을 받아 19:00 뒤에 build+deploy 하고 0건 화면을 누른다.**
  못 누른 이유 셋(전부 실측 · `_조율자.md` 에 같은 내용):
    ㉠ `gxseed_u5_sysop` 을 U24 가 18:15~19:00 쓰고 있다(동시 접속 1개) ·
      `admin` 역할 씨앗 계정은 **그 하나뿐**이다.
    ㉡ **그 계정으로는 0건이 안 나온다** — u5 테넌트(group 4)에 **이미 1건**이다
      [`count(*) WHERE group_id=4` → 1]. 지우는 것은 대표 결정이라 안 지웠고,
      계정·테넌트를 새로 만드는 것은 이 차선이 할 일이 아니라고 봤다.
    ㉢ 떠 있는 dist 에 이번 변경이 **없다**[`_fe_dist` grep → 0]. 마지막 배치 17:41:33.
  ⚠ **그래서 「닫음」에 적지 않았다.** 회색은 초록이 아니다.
(없다 — 이 턴 몫은 다 손을 뗐다. 아래 「안 한 것」만 남았다.)

## 안 한 것
- **㉢ P-187 — dry-run 까지만. 실제 `GRANT`/`REVOKE` 0줄.**
  증거로 못 박았다: `SELECT rolname FROM pg_roles WHERE rolname IN ('gx_app','gx_migrate')`
  → **빈 결과**. 한 일은 ① 전제 재측(231표·superuser 둘·시험 DB 12 그대로) ②
  `remaining_40.md` §2-2 에 **OPS-23·OPS-24 등재** ③ `.env.example` 에 이름 넷
  (**전부 주석 · 값 없음**) ④ `docker-compose.yml` backend 절에 **주석만** 배선 안내
  (붙이면 `:?` 가 compose 를 멈추게 하므로 지금 붙이면 안 된다) ⑤ `P-178/dry_run.md`
  에 턴 W 절. 전환·값·시험 DB 역할 선택은 **창 2 · 대표 결정**.
- **㉦ 조율자 알림** — U24 의 17:3x 빨강 둘이 **내가 대장을 갱신한 뒤 18:16 에 12/12
  초록**으로 풀렸다. 내 문 때문에 옆 차선이 빨갰고, 내가 닫으니 같이 닫혔다 —
  「자기 시험만 돌리면 자기 문만 보인다」의 반대쪽 증거다.
- **㉣ API-03·04** — 턴 V 의 「손 위」 둘을 닫았다(명세 §3·§7·§8·§9 · `authn_paths` §1
  U6 행 정정). **남은 한계는 그대로**이고 이 차선 것이 아니다: `inbound_key=True` 를
  선언한 라우트는 저장소 전체에서 **`GET /api/dsm/events` 하나**다. `stats/*` 여덟
  (`api_u24.py`) · `cameras/pulse`(`api.py:1343`)는 키에게 **401** — U24·U3 파일이라
  안 고쳤다. **두 턴째 같은 요청**이라 조율자에게 이름으로 넘겼다.
- **못 잰 것** — `verify_tenant_scope` · `verify_write_auth` 는 **판정 불가**로 끝난다
  (`/repo/docs` 가 `gx-shell` 에 안 붙어 있어 등재부 셋을 못 읽는다). HEAD 본으로 재도
  같은 자리에서 멈춘다 → **이 턴이 만든 것이 아니다.** 회색으로 둔다.
- **넘김** — `scripts/verify_click_completes.py` U5#15 주석과 `onboarding_48.md:595`
  행이 「읽는 라우트가 없다」로 남아 있다. 이제 거짓이다(둘 다 200). 남의 파일이라
  안 고쳤다 — 조율자 대장에 적었다.
