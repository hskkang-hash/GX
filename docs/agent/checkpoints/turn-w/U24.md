# U24 체크포인트 (턴 W · 파 3 턴 3 「이어서」) — 2026-09-19

## 닫음 (눌러서 본 것만)

### ㉠ P-185 LAW-07′ 권한 — **authz 표만 고쳤다. 새 문·새 인증기 0.**

다섯 문 × 역할 3 — **실서버 실측** (nginx `:8500` → `gx-gunicorn-e` · 18:12 재시작 뒤 ·
실제 로그인 3 · 접수 번호 `GX-PR-20260919-6D9C81`). 단위시험(`test_u24_law07_authz.py` 21/21)이
낸 표와 **한 칸도 안 어긋난다** — 두 출처가 같은 값을 냈다.

| 문 | 메서드 · 경로 | U4 `view_only_-_anyang` | U2 `fire_admin` | U5 `admin` |
|---|---|---|---|---|
| 목록 | `GET /api/dsm/law/privacy-requests` | **200** | **403** | **200** |
| 접수 | `POST /api/dsm/law/privacy-requests` | **200** | **403** | **200** |
| 상세 | `GET …/{receipt_no}` | **200** | **403** | **200** |
| 마스킹본 | `GET …/{receipt_no}/masked` | **200** | **403** | **200** |
| 회신 | `POST …/{receipt_no}/reply` | **200** | **403** | **200** |
| **(대조)** | `POST /api/dsm/events/999999999/upper-report` | **403** `read_only_role` | **404** | **404** |

마지막 줄이 **안 넓힌 쪽의 증거**다: U4 는 청구 면 밖의 쓰기에서 **지금도 관문에 막히고**,
U2·U5 는 관문을 지나 핸들러가 돌아 404(없는 사건)를 낸다. 실재 사건 id 로는 안 두드렸다.
계정 비밀번호 대조: `sha256[:12] = de08e1f17eb3` (세 계정이 같은 `GX_SEED_ROLE_PASSWORD`).

- 「눌러도 403 나는 줄」 **0** (U4 축). 넓힌 쪽(U4 200)과 **안 넓힌 쪽(U2 403) 둘 다** 눌렀다.
- U2 거절 본문에 「지자체 담당관」이 들어 있고 `read_only_role` 은 **없다** — 사유를 안 섞었다.
- **막힌 자리가 둘이었다**(턴 V 는 하나만 봤다):
  ① 라우트의 `law_api._admin` (`tenant_admin` 만) → `_privacy_officer` 로 갈랐다.
  ② **미들웨어** `common/role_gate` 의 읽기 전용 쓰기 금지 — U4 의 역할 코드가 글자 그대로
     `view_only_-_anyang` 이라 **접수·회신(POST 둘)이 핸들러에 닿지도 못했다.**
     ②를 안 보면 ①만 고치고 「고쳤다」가 된다.
- **자격은 고치지 않았다.** 계정·역할·그룹에 한 줄도 쓰지 않았다(조율자가 물은 그 칸).
  `_privacy_officer` 는 `config/k3_roles` 표를 **읽기만** 한다 — 표를 두 벌 두지 않았다(D-212).
- **안 넓힌 것**을 눌러서 확인: U4 로 `POST /api/dsm/events/{id}/upper-report` **403**(`read_only_role`) ·
  `POST /api/dsm/law/retention/sweep` **403**. `_admin` 은 손대지 않았다(파기·사용량 종전 그대로).
- **새 인증 경로 없음**을 런타임 레지스트리로 쟀다(`enumerate_operations`):
  `privacy-requests` 아래 문은 **정확히 다섯** · 다섯 다 `has_auth=True` · 다섯 다 `apps.dsm.law_api`.
  익명은 여전히 401/403 · 역할 0 은 여전히 `role_required`.

### ㉠″ **넓히기 전의 403 을 지금도 본다** — 같은 순간 · 같은 토큰 A/B
조율자 경고(「넓히기 전 403 을 못 봤으면 넓힌 뒤 200 은 모르는 200 이다」)에 답한다.
서버를 되돌려 다시 잴 필요가 없었다 — **옛 문지기 `_admin` 이 이웃 문들에 그대로 살아 있다**:

| | 문 | 문지기 | U4 |
|---|---|---|---|
| A | `GET /api/dsm/law/purge/tenants` | 옛 `_admin` | **403** |
| B | `GET /api/dsm/law/privacy-requests` | 새 `_privacy_officer` | **200** |

계정·토큰·서버가 같고 **다른 것은 문지기 하나**다. 그리고 옛 조건은 **여전히 거짓**이다 —
`is_global_admin(U4)=False` · `is_tenant_admin(U4)=False`. 즉 **자격을 고쳐서 통과시킨 것이
아니라 표를 고쳐서 통과시켰다.** 대조로 `is_tenant_admin(U5)=True`(종전에도 지나갔다) ·
`is_tenant_admin(U2)=False`(지금도 못 들어간다).

### ㉠′ 마스킹본 조회 감사 1행 — **남은 것을 봤다** (실서버 DB 에서)
위 실서버 실측에서 마스킹본 문을 **셋이 눌렀다**(U4 200 · U5 200 · U2 403).
그 뒤 개발 DB 를 그대로 세니 `logger_name=guardianx.law07.privacy_request` ·
`api_name=law07:masked_view` 행이 **정확히 둘**이다:

```
#268416  09:12:55Z  누가=gxseed_u4_official  청구=GX-PR-20260919-6D9C81  본건수=20  분모=34
#268441  09:13:06Z  누가=gxseed_u5_sysop     청구=GX-PR-20260919-6D9C81  본건수=20  분모=34
```

- **누른 사람 200 둘 = 행 둘.** 행마다 **누가 · 어느 청구 · 몇 건을 봤나 · 분모**가 있다.
- **음성 대조**: U2 의 403 은 **행을 안 남겼다**(셋이 눌렀는데 행은 둘) — 막힌 것은 조회가 아니다.
- 단위시험도 같은 것을 잰다(1회 = +1 · 403 = +0).
- ⚠ 이 감사 1행은 **내가 만든 것이 아니다.** `privacy_request.masked_view` 가 이미 쓰고 있었다
  (`ACTION_VIEW`). 나는 **남는지 눌러서 확인**했을 뿐이다.

### ㉠‴ 잰 동안 개발 DB 에 남긴 것 (지우지 않았다 · 표기만 한다)
실서버로 다섯 문을 재면 **접수·회신은 실제로 쓴다** — 그것이 그 문의 일이다. 남은 것:
청구 **3건**(U5 사전 1 + U4·U5 의 「접수」 누름 각 1 · 청구인 이름 `P185측정`·`P185눌림`) ·
회신 **2건**(U4·U5) · 마스킹본 조회 감사 **2행**. 지우지 않는다 — **감사 행은 그 일이
있었다는 유일한 증거**이고, 지우면 잰 사실이 사라진다(`role_gate.py` #197207 과 같은 규율).

### ㉡ 403 모달/상태 칸 겹침 — **상태 칸을 남겼다**
- `PrivacyRequests.tsx` 가 `ownDenialPaths(['/api/dsm/law/privacy-requests'])` 로 임자를 선언한다.
  앞머리 하나가 다섯 문을 다 덮는다.
- **왜 상태 칸인가** (셋):
  ① **남는다.** 맨 위 띠는 사람이 닫을 수 있고 화면을 옮기면 사라진다 — 나중에 보면 증거가 아니다.
  ② **누를 것이 있다.** 「다시 시도」가 상태 칸에만 있고 방금 실패한 그 요청을 다시 낸다.
  ③ **막힌 자리에 붙어 있다.** 접수가 막혔는지 회신이 막혔는지가 칸의 위치로 보인다.
- 숨긴 것이 아니다 — 403 은 `deniedPaths()` 에 그대로 남고 상태 칸에 그대로 적힌다.

### ㉣ 빈 상태 — **0건을 실제로 만들어서 눌렀다** (`tests/test_u24_empty_states.py` 3/3)
카메라 0대 · 보고서 0건인 **새 테넌트**를 세워 세 문을 두드렸다:
- `GET /api/dsm/cameras/address-gap` → 200 · `total=0` · `measurable=False` · `coverage=None`
- `GET /api/dsm/reports/runs` → 200 · `runs=[]`
- `GET /api/dsm/law/privacy-requests` → 200 · `items=[]` · `total=0` · `unanswered=0`

고친 문구 **둘**:
- **보고서 0건** (`Reports.tsx`) — 「아직 만든 보고서가 없습니다. **(요청은 성공했고 0건입니다)**」
  → 괄호를 뺐다(바로 아랫줄이 이미 그 말을 한다 · 중복이자 응답 상태의 말이다).
  그리고 사전 기본 「새 자료가 생기면 이 자리에 나타납니다」가 **이 자리에서는 거짓말**이라
  화면이 제 다음 줄을 적게 했다: 「위 서식에서 「만들기」를 누르면 여기에 한 줄이 생깁니다.
  매월 1일 자동으로 만들어진 것도 이 자리에 「만든 쪽 · 자동」으로 옵니다.」
- **카메라 일괄 등록** (`CameraImport.tsx`) — **분모 0 을 초록으로 그리고 있었다.**
  카메라가 0대면 「주소 없는 카메라 **0 / 0대**」가 **초록**으로 떴고, 이 제품에서 초록 0 은
  「다 채웠다」다. 실제는 「아직 한 대도 없다」였다. → 분모가 0이면 비율 카드를 안 그리고
  「아직 등록된 카메라가 없습니다. 요청은 성공했고 카메라가 0대입니다 — 못 불러온 것이
  아닙니다. … 아래 ① 칸에 CSV 를 붙여넣고 「② 표 먼저 보기」를 누르면 …」 한 장을 그린다.

### ㉤ `stats/*` 들어오는 키 — **열지 않는다** (두 턴째 요청 · 이 턴에 닫는다)
사유는 `api_u24.py` 의 `DsmU24API` 머리에 **코드로** 적었다(떠다니지 않게).
실측이 요청의 전제 둘을 **뒤집었다** [런타임 레지스트리 763자리 · 2026-09-19]:
- **`stats/*` 는 여덟이 아니라 아홉**이다 — GET 8 + **POST 1**(`/stats/thresholds/simulate`).
  그 POST 는 규약 ③ 「읽기 전용부터」라 **애초에 열 대상이 아니다.**
- **`inbound_key=True` 선언 라우트는 하나가 아니라 둘**이다 —
  `GET /api/dsm/events`(F-05) · `GET /api/dsm/cameras/pulse`(UX-23 · 범위 `pulse:read`).
  즉 「하나뿐이라 좁다」가 사실이 아니고, **여는 방법의 정본이 이미 있다**: 데코레이터 한 줄이
  아니라 **이름 붙은 범위**를 세우고 그 범위를 일부러 받은 키만 들인다.
- 그래서 여는 일의 본체는 **`stats:read` 범위를 세우는 일**이고 그 범위는 아직 없다
  (턴 V · U56 실측 「`stats`·`pulse` 미선언」). 범위 없이 열면 기본 범위(`events:read`) 키가
  통계에 **전부** 닿는다 — `inbound_api_key.py` 머리말이 이름으로 금지한 그 모양이다.
- 부르는 외부 App **0**. 필요를 못 본 채 여는 것은 D-300 부작위의 반대다.
- **여는 조건**을 코드에 남겼다: ⓐ `stats:read` 범위 ⓑ 그 범위를 부르는 외부 계약 절
  ⓒ **키로 실제로 눌러 200 을 본 뒤** — 그때 **GET 8 개만**. POST 는 열지 않는다.

## 손 위 / 다음 한 줄
- **손 위에 열린 것 없다.** 네 갈래 + 조율자 요청 ㉤ 까지 닫았다.
  다음 한 줄(다음 사람에게): 조율자 요청 **2**(U56 · 관리·설정 빈 상태)와 **1**(Q · U4#15 정본)이
  넘어간 상태다 — 그 둘이 들어가면 P-185 ㉠㉡㉢㉣ 가 전부 닫힌다.
- 배치는 **안 돌렸다**(`scripts/deploy.sh` 0회). 조율자 공지대로 정본 배치는 병합 뒤 조율자가 한다.

## 안 한 것 (그리고 왜)
- **「관리·설정」 빈 상태 문구** — `/dsm/system` `SystemSettings.tsx` 는 **이번 턴 차선 U56 이
  쓰고 있다**(작업 트리에 U56 의 `api_u56.py`·`ops_tasks.py`·`SystemSettings.tsx` 변경이 살아 있다).
  **한 파일은 한 차선**이라 고치지 않았다. 문안은 아래에 적어 U56 에게 넘긴다.
- **U4#15 정본 파일** — 「정본은 고치는 사람과 재는 사람이 다르다」. 증거만 만들고 문안을 Q 에게 넘겼다.
- **gx-shell `runserver:8000` 으로는 안 쟀다** — 실서버 축을 nginx:8500(gunicorn)으로 잡았다.
  그 runserver 는 U56 이 17:38 에 다시 띄워 새 코드를 들고 있지만, 손으로 띄운 프로세스라
  **정본 축이 아니다.** 두 출처(실서버 + 단위시험)가 이미 같은 값을 냈다.

## U4#15 정정 문안 — **Q 차선에 넘김** (글자 그대로 · U24 는 정본 파일을 안 고쳤다)

대상: `docs/agent/onboarding_48.md` **1101행** (「채운 9행」 표의 `U4 15` 줄).
그 줄의 **셋째 술어 칸**을 아래로 **통째로** 바꾼다:

> ★★ **이 행은 U4 로 ● 가 될 수 없다 — U2 축에서 잰다** (세종 P-190 · 증거 U24 턴 W).
> U4 의 역할 코드는 글자 그대로 `view_only_-_anyang` 이고, 읽기 전용 관문
> (`common/role_gate` P-119 / SEC-20)이 **쓰기 메서드 전부**를 막는다. 그래서
> `POST /api/dsm/events/{id}/upper-report` 는 U4 로 **언제나 403**이고, 그 403 은
> **제품이 옳게 막은 자리**다 — 고칠 흠이 아니다. [실측 2026-09-19 턴 W · U24 ·
> `tests/test_u24_law07_authz.py::U4CannotBeGreenOnUpperReportTest`] 같은 문을 같은 순간
> U4 로 누르면 **403 `read_only_role`**(관문이 낸다 · 핸들러까지 못 간다), U2(`fire_admin`)로
> 누르면 **관문을 지나 핸들러가 돈다**(없는 사건 id 로 404 — P-83 의 그 눈금).
> 그러므로 셋째 술어는 **U2 축**으로 읽는다: [서버 기록] `POST /api/dsm/events/{id}/upper-report`
> **200**(U2 `gxseed_u2_manager`) → 재조회에서 그 행의 표시가 **서버 값으로** `보고함`
> + [화면 상태] `보고 표시`/`보고함` 두 말이 같은 화면에 동시에 있지 않을 것.
> ⚠ **U4 계정으로 이 행을 다시 재지 않는다.** 다시 재면 또 403 이 나오고, 그 403 을
> 빨강으로 세면 **제품이 옳게 한 일에 벌점을 주는 것**이다(P-159 가 그렇게 쟀다).
> ⚠ 읽기 전용에 열어 준 쓰기는 **열람 청구 면 둘**뿐이다(P-185 · 접수·회신).
> `upper-report` 는 **거기 없다** — 이 행을 초록으로 만들려고 관문을 넓히지 않는다.

## 차선 간 요청 (조율자에게)

1. **→ Q** : 위 「U4#15 정정 문안」을 `docs/agent/onboarding_48.md:1101` 에 넣는다. (U24 는 안 고쳤다.)
2. **→ U56** : `SystemSettings.tsx` 의 **「재시작 요청」 표**가 0건일 때 antd 기본
   (「데이터 없음」)만 뜬다 — `requests` 리소스에 `isEmpty` 가 없어서 `empty` 상태로 안 간다.
   U24 가 `StateBoundary` 에 **선택 prop `emptyNext`** 를 더해 두었으니(안 주면 종전 그대로),
   아래 두 줄이면 닫힌다:
   ```tsx
   const requests = useDsmResource<DsmSystemRequests>(
     () => dsmGet<DsmSystemRequests>(dsmU56AdminEndpoint.systemRequests), [],
     { isEmpty: (v) => (v?.requests?.length ?? 0) === 0 },
   );
   // …그리고 그 StateBoundary 에:
   emptyText="아직 올린 재시작 요청이 없습니다."
   emptyNext="위 칸에 사유를 적고 「재시작 요청」을 누르면 여기에 한 줄이 생깁니다. 사유 없는 요청은 다음 사람에게 「왜 내렸는지 모르는 정지」입니다."
   ```
3. **알림(닫혔다 · 내 빨강이 아니었다)** : 17:3x 에 `tests/test_f05_event_api.py::
   EntrySurfaceIsLockedTest` 가 **2건 빨강**이었다 — 진입면 108 → 109, 새로 난 자리는
   `GET /api/dsm/ops/backup/declaration`(**차선 U56** 의 이번 턴 새 문 · `api_u56.py:803`).
   **18:16 재측 12/12 초록** — U56 이 대장을 갱신했다. U24 의 변경은 라우트를 **0개** 더했다
   (런타임 레지스트리로 확인). 적어 두는 이유는 하나다 — 내 빨강으로 셀 뻔했다.

## 고친 파일
- `backend/apps/dsm/law_api.py` — `_privacy_officer` 신설 · LAW-07 다섯 문만 교체(`_admin` 은 그대로)
- `backend/common/role_gate.py` — `READONLY_WRITE_ALLOWED_PATTERNS`(모양 **둘**) · `is_readonly_allowed_path`
  ⚠ **공유 파일이다.** 조율자 배정에 없었지만 U4 의 POST 가 막히던 실제 자리라 손댔다 —
  `import re` + 상수 1 + 함수 3줄이고, 역할 0 목록은 **안 건드렸다.**
- `backend/apps/dsm/api_u24.py` — `stats/*` 들어오는 키를 **열지 않는다**는 답과 사유(주석만 · 라우트 0 변경)
- `backend/tests/test_u24_law07_authz.py` (신설 · 21) · `backend/tests/test_u24_empty_states.py` (신설 · 3)
- `frontend/src/features/dsm/pages/PrivacyRequests.tsx` — 임자 선언 한 줄
- `frontend/src/features/dsm/pages/Reports.tsx` — 0건 문구
- `frontend/src/features/dsm/pages/CameraImport.tsx` — 분모 0 빈 상태
- `frontend/src/features/dsm/components/StateBoundary.tsx` — 선택 prop `emptyNext` (안 주면 종전 그대로)

## 기계 메모
- 시험: `DB_TEST_NAME=test_gx_u24` · `--nomigrations -p no:randomly`. 내 시험 **24/24**.
- 회귀: `test_l_privacy_request` · `test_p119_readonly_role` · `test_role_gate` · `test_s_wall_token`
  전부 초록. `test_f05_event_api` 2 빨강은 **U56 의 새 문** 때문이다(위 요청 3).
- 타입: `tsc -p tsconfig.app.json --noEmit` 전체 **3,234줄**(턴 V 와 같은 인수 자산 빚) ·
  **내 파일 오류 0**. 호스트에 `node_modules` 가 없다 — `docker exec gx-fe-build` 로 돌린다.
  ★ **그 0 이 거짓 0 이 아님을 분모로 확인했다**(조율자 경고 · 루트 `tsconfig.json` 은
  `files: []` 라 아무것도 안 잰다). `--listFiles` 에 내 다섯 파일이 **전부 들어 있다**:
  `PrivacyRequests.tsx` · `Reports.tsx` · `CameraImport.tsx` · `StateBoundary.tsx` ·
  `permissionDenied.ts`. 검사 목록에 있는 파일의 오류 0 이다.
- 정규식 백슬래시 함정: `role_gate.py` 의 패턴 둘은 **백슬래시가 0자**다(`[^/]+` 뿐).
  컨테이너에서 파일을 다시 읽어 글자가 그대로인 것을 확인했고, 순수 함수 시험이 모양을 잰다.
- **`runserver` pid 38479 → 91933** (U56 이 17:38 에 다시 띄웠다). 내 `law_api` 변경은
  17:2x 이므로 **그 프로세스에는 새 코드가 들어 있다.** 눌러서 확인하는 것이 다음 한 줄이다.
- `gx-gunicorn-e` 가 17:27 에 **다른 차선이** 재시작했다. 내 백엔드 변경 뒤로는 아직 안 돌렸다.

## 자진 오판
1. **`_admin` 을 한 번에 치환해서 파기·사용량 문 셋까지 넓혔다.** 바로 되돌렸고
   `git diff` 로 다섯 문만 바뀐 것을 확인했지만, 커밋됐으면 파기 문이 U4 에게 열릴 뻔했다.
   넓히는 작업에서 **일괄 치환은 도구가 아니라 사고**다.
2. **안 나는 갈래에 문구를 썼다.** `CameraImport` 에 `plan.total === 0` 빈 상태를 먼저 지었는데,
   `parse_csv` 를 눌러 보니 머리줄만 있으면 **`fatal`** 로 빠져 그 갈래는 **영영 안 난다**.
   눌러 보기 전에 화면부터 고친 것이고, 그대로 뒀으면 **아무도 못 보는 문구**가 남았다. 지웠다.
