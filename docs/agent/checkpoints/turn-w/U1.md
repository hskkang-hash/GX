# U1 — 턴 W 체크포인트 (P-176) · **마지막**

마지막 갱신: 2026-09-19 18:0x KST · 차선 U1(**관제요원** · `gxseed_u1_operator`)

## 닫음 (눌러서 확인한 것만)

**① 큐 카드 1클릭 — 대기 카드를 HTTP 로 눌렀고, 누른 뒤를 다시 읽었다**
`gxseed_u1_operator`(자격 sha256[:12]=`de08e1f17eb3`) · **앱이 부르는 밑동**
`http://localhost:8000`(번들에 박힌 그 주소) · `gx-shell` 안에서.

| 잰 것 | 값 |
|---|---|
| `GET /events/queue?limit=200` | 200 · `total_events=34` · `card_total=18` · focus `4805` · 대기 17장 |
| `GET /queue/field-signals`(18건) | 200 · `asked=18 read=18` · **`allowed_next` 를 18/18 받았다** |
| 카드의 `allowed_next` == 상세의 `allowed_next` | **8/8 같다 · 갈림 0** ← 표는 하나다(D-399) |
| **대기 카드** 231075 누르기 전 | `occurred` · `['acknowledged']` |
| `POST /events/231075/review-and-acknowledge` | **200** |
| **누른 뒤 다시 읽었다** | `acknowledged` · `['in_progress']` ← 상세에 **안 들어가고** 옮겼다 |
| 감춘 갈래 `occurred`·`closed` 를 눌러 봤다 | **409** 둘 다 · 재조회 `acknowledged` 그대로 |
| 익명 `GET /queue/field-signals` | **401** — 토큰이 실제로 뭔가를 바꿨다 |

**② 「8분 경과 — 가장 오래 기다린 사건」이 거짓이었다는 실측**
`focus=4805` 인데 **실제로 가장 오래 기다린 사건은 4819**(경과 1,369,919초 · 동률 1건).
둘은 **다른 사건**이다 — 옛 고정 문구는 단계 4 인 카드마다 자기가 1등이라고 적었다.

**③ 순위·단계 문구를 돌려서 봤다 (0건 갈래 포함 · 분모를 먼저 보인다)**
`esbuild` 로 `features/dsm/time.ts` 만 묶어 node 로 실행:
- **분모 0**: 「시계가 도는 사건이 0건입니다 — 경과 순위를 매기지 않습니다.」
- 1장: 「시계가 도는 사건이 이 하나뿐입니다 — 견줄 상대가 없습니다.」
- 3장: 「가장 오래 기다린 사건 (시계가 도는 3건 중 1번째)」 / 「경과 2번째 (시계가 도는 3건 중)」
- 동률: 「… — **동률 2건** (시계가 도는 3건 중 1번째)」
- 종결 섞임: 분모가 2가 아니라 **1**(멈춘 시계는 순위에 안 든다) · 그 카드의 순위 줄은 **빈 문자열**
- 단계 이름이 서버 문턱을 따라간다: 서버표 `[30,120,300,480]`→「8분 넘음」 ·
  다른표 `[60,180,600,1200]`→**「20분 넘음」** (옛 문구는 둘 다 「8분 경과」였다)

**④ 되돌림 갈래 — 시험이 내 1차판을 잡았고, 실물로 확인했다**
`closed` 의 `allowed_next` 는 **빈 목록이 아니다**(되돌림이 들어 있다). 사건 268497:
사유 없이 → **400** 「되돌림에는 사유가 필수다」 · 사유를 채워 → **403** 「관제팀장(K3
MANAGER 이상)만 한다」 · 재조회 `closed` **그대로**. → 화면이 400/403/409 에 **다른 일**을 한다.

**⑤ 단위시험 38 passed / 0 failed** (`test_u1_queue_card_actions.py` 7 신설 포함)

**⑥ 타입검사 — 내 파일 5개 오류 0 · 분모를 봤다**
`tsc --noEmit -p tsconfig.app.json` (루트 `tsconfig.json` 은 `files: []` 라 0 을 낸다 —
안 썼다). `--listFiles` 로 **내 5파일이 검사 목록 2,528개 안에 있는 것**을 확인했다.

**⑦ 번들에 실렸다** (임시 배치 2회차 17:54:17 · 다이제스트 `a3461169b273fd44` · exit 0)
`gx-shell:/app/_fe_dist/assets/*.js`: 낡은 문구 **0건** · 새 문구 9종 각 1건 이상.

## 손 위

없음.

## 안 한 것 (넘긴다)

- **㉢ `stats`/`pulse` `inbound_key=True` — U1 소유분 0.** 조율자 대장 「차선 간 요청」에
  근거와 **부수 항목 넷**(access_gate 집합 · route_classes.yaml · `reason` 필수 ·
  쓰기 면 금지의 **실제 문지기는 시험이다**)을 적어 넘겼다.
- 숫자 키 1·2·3 을 대기 카드로 넓히는 것 — **안 했다**(대상이 움직이는 키는 오조작).
- 브라우저 걷기·캡처 — V 의 일. `rankNote`·`card-actions` 에 `data-gx` 를 달아 뒀다.

## 자진 오판 (둘 · 둘 다 보고 전에 잡았다)

1. **`gx-fe-build:/app` 을 저장소 마운트로 알고 `tsc` 를 돌려 「초록」을 봤다.** 그 `/app` 은
   **사본**이고(`deploy.sh` 머리말 ⚠ 가 적어 둔 함정) 내가 본 초록은 **직전 턴 코드**의
   것이었다. `docker cp frontend/src/. gx-fe-build:/app/src` 뒤 다시 재서 바로잡았다.
2. **시험 1차판에 「종결된 사건은 `allowed_next` 가 빈 목록」이라고 적었다 — 틀렸다.**
   되돌림이 들어 있다. 이 오판이 없었으면 **누르면 반드시 실패하는 단추**를 대기 카드
   17장에 그대로 그릴 뻔했다. 시험이 먼저 빨개져서 잡혔다.
