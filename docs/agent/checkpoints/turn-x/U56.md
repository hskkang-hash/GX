# U56 체크포인트 (턴 X) — 14:1x · **끝낼 때 판**

## 닫음 (눌러서 확인한 것만)

- **★ 창 2a 절차서** — `docs/agent/evidence/P-178/RUNBOOK_창2a.md`.
  **16단계**(A부 5 · B부 3 · C부 8) · **★ 되돌릴 수 없는 자리 셋** ·
  **「여기서 멈춤 — 대표 결정 필요」 한 자리(C0)에 결정 세 칸(ⓐ`.env` 값 넷 ⓑ 시험 DB 역할
  ⓒ 남은 시험 DB 13개 회수)**. 되돌림 절차를 **단계마다 한 칸씩** 표로 붙였다.
  ★ 셋: **★A2** `docker rm -f gx-shell`(컨테이너 레이어 소실 · A0 보존본이 유일 원본) ·
  **★A3-함정** `--entrypoint` 누락(이미지 entrypoint 가 개발 DB 에 migrate+초기화 스물몇 개를
  쓴다 — 되돌릴 수 없다) · **⛔★C7** `ALTER ROLE … NOSUPERUSER`(**이번 창에서 안 한다**).

- **조율자 요청 · LAW-02a 빨강 — 닫았다.** `ops_tasks.py` 의 기본 `0` 을 뽑았다.
  `verify_retention_declared.py` → **⑤ 「수 기본값 0개」 · 통과 · EXIT=0**.
  `settings` 로 **안 옮겼다**. 미선언 `None` · 0 선언은 `0` 그대로(둘은 다른 사실).
  **A/B 음성 대조**: 같은 `scan_defaults` · 읽은 파일 3/3 — `9d8edae` **1건** / 지금 **0건**.
  ⚠ 덤: 턴 W 시험 하나가 **결함을 지키고 있었다**(`…invents_nothing` 이 `0` 을 주고 `None` 기대).

- **P-187 — dry-run 그대로. 실 `GRANT`/`REVOKE` 0줄.**
  `pg_roles WHERE rolname IN ('gx_app','gx_migrate','gx_test')` → **빈 결과** [실측 13:2x].
  231표 · 로그인 superuser 둘 · 만든 역할 0 · 바꾼 `.env` 0 · 재생성한 컨테이너 0.

- **OPS-23·24 — 등재가 아니라 증명을 냈다.**
  · OPS-23 증명 = `pg_roles` 빈 결과 + **compose diff 가 집행 수단이 아니라는 실측**
    (컨테이너 10개 compose 라벨 없음 · 뿌리 `.env` 에 `DB_USER` **0줄** ·
     DB_USER 넷이 `sha256("postgres")` 앞 12자 `a942b37ccfaf` 로 같다).
  · OPS-24 증명 = **순서 제약**. 시험 DB **13개 소유자 전부 `postgres`** → `CREATEDB` 를 줘도
    `gx_migrate` 는 못 지운다 ⇒ **회수가 앱 전환보다 먼저**다. 회수 규약 세 근거
    (`numbackends` 두 출처 · `xact_commit`(기동 이래) · `pg_stat_file` 시각) ⇒ **후보 3 · 95MB**.
  · ★ **한 턴 안 40분 A/B**: 13:2x 13개 / 14:0x 13개 — **총수는 같은데** `test_gx_s` 사라지고
    `test_gx_u56` 생기고 `test_gx_u24` 재생성(`xact_commit` 3,894→691). **후보 셋은 두 판 동일.**

- **API-03·04 — 반은 풀렸고 반은 늘었다** (명세 §3.2 신설).
  닿는 문 **2/4**: `events:read`·`pulse:read`(U3 가 이번 턴에 `api.py:1388` 을 열었다) ·
  `stats:read`(`stats/*` **아홉** — 턴 W 엔 여덟) · `webhooks:manage`(여섯) 는 **401**.
  시험 `tests/test_u56_key_scope_reachability.py` **3/0** 이 그 수를 지킨다.

- **`--max-requests` 값 — `200` · jitter `50` · 그대로.** 창에서 바꿀 값이 **없다**(턴 C 가
  폭 넓히기를, 턴 D 가 끄기를 **실측으로 기각**했다 · 키우는 것도 같은 이유로 금지).
  ★ **진짜 물음은 값이 아니라 「걸려 있나」였다**: 도는 명령줄에 `-c` 가 **없고**,
  `--print-config` 로 재니 `config = ./gunicorn.conf.py` — **cwd 가 우연히 `/app` 이라** 걸렸다.
  떨어지면 기본값 `max_requests=0` · `keepalive=2`(= 502 를 만든 그 수)다.
  ⇒ 창에서 `-c /app/gunicorn.conf.py` **명시**. `gunicorn.conf.py` 는 **주석만 25줄** 더했고
  **값은 한 자도 안 바꿨다**(`git diff` 에 주석 아닌 줄 **0**).

- **시험** — U56 전체 **105 passed / 0 failed**(파일 목록 명시 실행) · 새 시험 **8**.

## 손 위
(없다)

## 안 한 것
- **`pulse:read` 범위 밖 403 을 HTTP 로 못 쟀다 — 회색이다.** F 의 잰 창(12:45~14:30) 안이라
  **로그인 0회**로 일했다. 선언은 실측했지만 「눌러 봤다」로 적지 않는다.
- **`.pre-commit-config.yaml` 을 안 고쳤다**(조율자 소유). 구멍의 정체는 「훅에 없다」가 아니라
  **`files:` 가 `^docs/agent/evidence/(D-346|D-309|DA-05)/` 라 코드 커밋엔 안 뜬다**이다
  [실측: 최근 5커밋 391파일 중 걸린 커밋 **1**]. 넓히면 매 커밋 로그인 판정기 여섯이 돌고
  이 저장소는 동시 접속 1개다 — 대가를 아는 쪽이 정해야 한다. 대안(정적 갈래만 분리)을 적었다.
- **`/stats/*` 를 안 열었다** — U24 파일이고 `inbound_key=True` 는 사유 한 줄이 필요한 개방 결정.
- **`docker-compose.yml` 을 한 자도 안 고쳤다**(턴 W 자진 이후 이번엔 절차서에만 적었다).
- **`verify_ga_readiness` 전량을 안 돌렸다** — 로그인 판정기 여섯을 부른다(F 의 창).
- **지운 시험 DB 0개 · 만든 것 1개**(`test_gx_u56` · 표적 pytest).

## 자진 오판
- **턴 V·W 의 `dry_run.md` §3 compose 배선 diff 는 집행 수단이 아니다.** 「지금 넷은
  `env_file: ./backend/.env` 를 읽고 그 `DB_USER` 는 postgres」가 **틀렸다** — 뿌리 `.env` 에
  `DB_USER` 가 **0줄**이고 자격은 `C:/GuardianX-vault/recreate/*.new.env` 에서 온다.
  「compose 를 고치는 것은 선언이지 집행이 아니다」(위임 이의 #3)가 **내 산출물에도 걸렸다.**
- **턴 W 에 내가 넣은 기본값 `0` 이 LAW-02a 를 빨갛게 만들었고**, 같은 턴에 내가 쓴 시험이
  그 뒤섞임을 「맞다」고 못 박고 있었다. 둘 다 이 턴에 고쳤다.
