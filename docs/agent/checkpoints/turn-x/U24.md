# U24 체크포인트 (턴 X · 파 3 턴 4 「이어서」) — 2026-09-20

## 닫음

### ★ ㉠ 파기·집행 문 U4 403 유지 시험 — **변이로 빨개지는 것을 봤다**

새 파일 `backend/tests/test_u24_admin_doors_stay_403.py` (**16 passed**).

**양쪽을 같은 파일에서** 잰다(한쪽만 재면 「전부 403」인 코드도, 「전부 200」인 코드도 통과한다):

| | 잰 것 | U4 |
|---|---|---|
| 안 넓힌 쪽 | `GET /api/dsm/law/purge/tenants` · `GET …/purge/history` · `POST …/purge` · `POST /law/retention/sweep` | **403 넷** |
| 넓힌 쪽 | `privacy-requests` 다섯 문 | **200 다섯** |
| 분모 | 같은 네 문을 **U5 로** | **403 아님** (문이 살아 있다) |

- **이름으로 박았다**(D-285 ②). 개수가 아니라 `(메서드, 경로)` 집합이고, 원본 AST 로
  「`_admin` 이 지키는 문의 이름 집합」·「`_privacy_officer` 가 지키는 문의 이름 집합」을
  통째로 대 본다. 어느 쪽으로 번져도 **어느 문이 번졌는지 이름으로** 빨강이 난다.

**★ 변이 시험 — 턴 W 의 사고를 그대로 재현했다**
`law_api.py` 의 호출부 `_admin(request)` **넷을 일괄 치환**(`def` 둘은 그대로 — 그때와 같은 모양):

```
변이 → 8 failed (내 파일 7 + 기존 test_u24_law07_authz 1)
  AssertionError: {'파기 대상 목록': 200, '파기 집행': 403, '파기 기록': 200, '보존기간 집행': 403}
  AssertionError: 빠진 문={('POST','/law/retention/sweep'), ('POST','/law/purge'),
                           ('GET','/law/purge/history'), ('GET','/law/purge/tenants')}
되돌림 → 40 passed (admin_doors 16 + law07_authz 21 + empty_states 3)
git status backend/apps/dsm/law_api.py → 출력 0줄 (HEAD 와 같다)
```

**★ 변이가 가르쳐 준 것 — 겹이 둘이고, 한 겹만 재면 사고를 못 잡는다**
위 표를 보라: 치환했는데 **POST 둘은 그대로 403** 이었다. P-119 읽기 전용 관문이
앞에서 끊어 `_admin` 까지 가지도 않기 때문이다. 즉 **쓰기 문만 재는 시험은 이 사고를
못 잡는다** — 관문이 사고를 덮는다. 그래서 이 파일은 겹을 갈라서 잰다:
  ① `GET` 두 문으로 `_admin` 을 **직접** 잰다(읽기는 관문이 안 본다)
  ② 쓰기 둘은 `READONLY_ROLE_GATE_ENABLED=False` 로 **관문을 끄고** 한 번 더 눌러
     그 아래의 `_admin` 도 U4 를 막는지 본다 ← 이 줄도 변이에서 빨개졌다

### ㉡ 빈 상태 **3/3** — 셋째를 **눌러서** 닫았다

`tests/test_u24_empty_states.py` 에 ③ 을 더했다(`RestartRequestsEmptyStateTest`).
`GET /api/dsm/system/requests` 를 **0건에서 한 번 · 1건을 만든 뒤 한 번** 두드린다.

- **`.tsx` 를 한 줄도 안 고쳤다.** U56 이 턴 W 의 내 요청 2 를 그대로 넣었다
  (`isEmpty` · `emptyText` · `emptyNext`). 남은 일은 **누르는 것**이었고 그것을 했다.
- **0건만 재지 않았다.** 0건에서 빈 상태가 뜨는 것만 재면 **언제나 빈 상태인 화면**도
  초록이다 — 「분모 0 인 초록은 초록이 아니다」의 뒤집힌 얼굴이다. 1건을 만들어
  빈 상태를 **벗어나는 것**까지 봐야 첫 번째가 「0건이라서」 뜬 것이 된다.
- 그 1건도 `objects.create` 로 **심지 않고 문으로 만들었다**(아래 자진 오판 1).

**★ 「분모 0 을 초록으로」를 그 화면 전수로 찾았다 — 턴 W 의 카메라 일괄 등록 같은 자리.**
`SystemSettings.tsx` 의 수를 그리는 칸 **넷을 다 봤고, 새로 난 자리는 없었다.**
U56 이 이미 같은 규율로 지어 뒀다:

| 칸 | 분모 0 일 때 | 판정 |
|---|---|---|
| 저장 용량 % | 상한 미선언이면 **% 를 안 그린다** (`used_pct === null` 갈래 · 「분모를 코드가 지어내면 그 추측이 초록이 된다」) | 옳다 |
| 백업 회수증 | 한 장도 못 찾으면 `verdict:"UNKNOWN"` — **0 을 초록으로 안 적는다**(D-301) | 옳다 |
| 재시작 요청 표 | 0건은 빈 상태 · 1건 이상일 때만 「전체 N건 중 최근 M건」 | 옳다 |
| 「복구를 판정할 수 있는가」 | `rows` 값이 **하나라도 `None` 이 아니면** 초록 | **경계 — 흠 아님.** 초록 문구가 「행 **수**가 있습니다」이지 「행이 있습니다」가 아니다. `rows={"x":0}` 이면 **0 은 잰 값**이고 None 이 「안 잼」이다 — D-301 을 그대로 지킨 문구다. 적어만 둔다 |

### ㉢ `stats` 「안 연다」 사유 — **사람이 읽는 자리에 섰다**

`docs/agent/authn_paths.md` **§3-1** 신설(새 절). 턴 W 에는 `api_u24.py` 머리의
**코드 주석에만** 있었다 — 주석만 있으면 **대장만 읽는 다음 사람이 또 묻는다**
(실제로 두 턴 그랬다). 코드 주석은 **지우지 않았다**: 고치는 사람이 보는 자리와
읽는 사람이 보는 자리는 다르고, 두 자리가 같은 말을 한다고 §3-1 에 적어 뒀다.

**오늘 다시 쟀다**(대장에 적는 수는 오늘 잰 수여야 한다) [2026-09-20 · 런타임 레지스트리]:

```
전체 라우트 763 · stats/* = 9  (GET 8 + POST 1 /stats/thresholds/simulate)
inbound_key=True 선언 라우트 = 2  (api.py:189 F-05 events · api.py:1390 cameras/pulse)
```

**그리고 대장의 낡은 줄 둘을 정정했다** — 그 둘이 내 새 절 바로 옆에서 반대말을 하고 있었다:
- §1 「외부 API U6」행: 「키가 닿는 문은 **아직 events 하나**」 → **둘**(pulse 는 턴 W 차선 U3).
- §5 정정 문단: 「`stats/*`(**여덟**)」 → **아홉**, 「선언 라우트 **하나**」 → **둘**.
  그리고 한 줄을 더했다 — **「안 열렸다」와 「안 열기로 했다」는 다른 사실이다.**
  사유 없이 「401」만 적어 두면 다음 사람이 그것을 **못 고친 빚**으로 읽고 열러 온다.

## 손 위

- **없다.** 셋 다 닫았다.

## 안 한 것 / 창

- **F 창 지켰다**: 13:40~14:10 에는 pytest·빌드·로그인·`runserver` 재기동 **0회**.
  그 앞 13:2x~13:3x 창(F 의 옛 창이 끝나고 새 창이 서기 전)에 표적 pytest 만 돌렸다.
- **로그인 0회 · `deploy.sh` 0회 · 프런트 빌드 0회 · 커밋 0회.**
- **§0.4 0줄.** 새 라우트 **0개**. **프런트 0줄**(그래서 `tsc` 를 돌리지 않았다 —
  안 고친 것에 대고 「타입 초록」을 적지 않는다).
- **화면 브라우저로는 안 눌렀다.** 셋째 빈 상태를 잰 축은 턴 W 와 같은 축이다 —
  **0건을 실제로 만들어 그 문을 두드리는 것**(`GET /api/dsm/system/requests`).
  화면 쪽 한 줄(`isEmpty`)이 보는 값을 서버에서 그대로 재고, 1건을 만들어
  **빈 상태를 벗어나는 것**까지 봤다. SPA 를 띄우는 축은 F 의 측정 창과 겹쳐 안 했다.

## 시험 · 게이트

```
tests/test_u24_admin_doors_stay_403.py   16 passed   (신설)
tests/test_u24_empty_states.py            6 passed   (3 → 6 · ③ 셋째 빈 상태)
회귀 112 passed  (+ law07_authz 21 · p119_readonly_role · role_gate · l_privacy_request)
scripts/verify_cache_bypass.py  새 위반 0건 (우회 44 · 비움 4 · 해당 없음 1)
```

⚠ `test_u24_empty_states.py` 의 수가 **9 → 6 으로 줄었다.** 고장이 아니다 —
셋째 클래스가 `ZeroDenominatorTest` 를 상속해서 **같은 시험 셋이 한 번 더 돌고
있었다.** 자리를 `_ZeroTenant`(시험 없는 자리)로 갈라 그 중복을 없앴다.
늘어난 3 은 잰 자리가 는 것이 아니라 **같은 자리를 두 번 센 것**이었다.

## 고친 파일 (셋 · 프런트 0 · 제품 코드 0)

- `backend/tests/test_u24_admin_doors_stay_403.py` — **신설** (★ 음성 대조 · 16)
- `backend/tests/test_u24_empty_states.py` — 셋째 빈 상태 + 자리/시험 분리
- `docs/agent/authn_paths.md` — §3-1 신설 · §1·§5 낡은 수 정정

## 자진 오판

1. **없는 결함을 요청으로 올릴 뻔했다 — 「`system/requests` 가 테넌트를 안 좁힌다」.**
   `api_u56.py:713~` 이 `DsmSystemRequest.objects.all()` · `.objects.count()` 를 쓰는
   것을 보고 「저장소 전체를 센다 · 남의 테넌트 요청이 보인다」로 읽었고, 체크포인트에
   차선 간 요청으로 **적었다가 지웠다.** 읽기를 한 겹 더 갔더니 틀렸다:
   `DsmSystemRequest(TenantModel)` 이고 그 `objects` 는 dj-core `CustomManagerGroup` —
   **스레드의 요청자 group 으로 이미 좁혀진다**(`models.py:1014`). `.all()` 이 전부가
   아니다. **모델의 관리자를 안 보고 질의만 보고 판단했다.**
   ⚠ 이 오판이 시험을 **거짓 빨강**으로 만들 뻔도 했다: 처음에 1건을 `objects.create` 로
   직접 심었는데, 스레드를 비워 둔 시험에서 그렇게 심으면 **소속 없는 행**이 생겨
   화면 질의에 안 잡힌다. 그 빨강은 제품의 흠이 아니라 **시험이 만든 것**이다.
   → 행을 **문으로**(`POST /api/dsm/system/restart-request`) 만들게 고쳤다.
   제품이 제 손으로 소속을 붙이고, 「누른 뒤를 본 것」이 된다.

2. **시험 수를 3 늘렸다고 셀 뻔했다.** `test_u24_empty_states.py` 가 처음엔 **9 passed**
   였다. 셋째 클래스가 `ZeroDenominatorTest` 를 상속해 **같은 시험 셋이 한 번 더
   돌고 있었다** — 잰 자리는 셋 늘었는데 수는 여섯이 늘었다. 자리를 갈라 **6** 으로
   되돌렸다. 수가 커진 것을 그대로 보고했으면 **분모를 부풀린 보고**가 됐다.
