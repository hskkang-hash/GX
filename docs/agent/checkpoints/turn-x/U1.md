# 턴 X · 차선 U1 (관제요원 화면 · 집계) — 체크포인트

갱신: 2026-09-20 13:4x (기계 시각) · 계정 `gxseed_u1_operator` · **로그인 0회**(F 측정 창)

## 닫음 (눌러서 확인한 것만)

**① 거르는 곳이 한 곳 — `backend/common/probe_marker.py`**
`PROBE_MARKER` · `is_probe_track()` · `exclude_probe(qs)`. 부르는 자리는 **둘뿐**이고
둘 다 ORM 이 있는 커널이다: `kernels/k1_event/services.py::query_events`(모든 셈의 외길) ·
`kernels/k6_feedback/services.py::false_positive_rate`(오탐 분모).
App 층은 ORM 을 못 만지므로(`test_dsm_app::AppStaysThinTest`) 거르는 실행이 커널에 있어야
했고, 커널이 둘이라 뜻은 L1(`common/`)에 두었다(`verify_layers` 허용: kernels→common. **통과 · 위반 0**).

**② 셈만 바꿨다 — 제품은 안 감췄다**
`GET /api/dsm/events`(운영자 목록)만 `include_probe=True` 로 연다. 기본값이 「안 센다」라
요약 `unhandled` · 초점 큐 · 인계 초안 · 온보딩 술어 · F-14 통계 · 대응 시간이 **한 줄도
안 고치고** 빠졌다. 시험이 「목록에는 그대로 있다」를 따로 잰다.

**⓪ SEC-13 `verify_cache_bypass` — 내 파일이 내던 빨강을 닫았다**
선언 한 줄이 `**우회**`(굵게)라 판정기 정규식(`캐시\s*처리\s*[:：]\s*(우회|…)`)에 안 걸렸다.
굵기를 빼고 **무엇이 거짓 초록이 되는지**를 적었다: 캐시가 적중하면 적중 본문이 앞 요청의
답이므로 **필터가 없어도 「변화 0」이 나온다** — 이 파일이 캐시를 타는 순간 음성 대조는
아무것도 안 재면서 초록이고, P-193 은 「고쳤다」로 보고된 채 살아 있다. 우회는 이 파일의
**판정 조건**이지 위생이 아니다.
`PYTHONIOENCODING=utf-8 python scripts/verify_cache_bypass.py` → **EXIT=0** ·
새 위반 0 · 내 파일 분류 **「우회」**(`classify_source` 로 직접 확인) · 시험 **7 passed** 유지.

**⓪′ 발송 축 구멍을 닫았다 (U3 가 화면에서 찾아 조율자가 넘긴 것)**
「`query_events` 가 세는 외길」은 **사건 축에서만** 참이었다. 발송은 다른 커널
(`k2_notify.list_deliveries`)을 지나고 표식은 발송 행이 아니라 `event` FK **너머**에 있다.
`exclude_probe(qs, via="event")` 한 줄을 같은 한 곳에 더해 K2 가 부르게 했다(정의는 그대로 하나).
**가른 축은 「보는가 / 누르는가」** — `mine=false` 발송 **대장**은 열어 두고(빼면
「보낸 적 없음」이 된다 · `DeliveryRecord` 머리말이 금지한 그 혼동), `mine=true` M1
「나에게 온 것」은 **일감**이라 닫았다(사건 축의 초점 큐와 같은 자리).
음성·양성 대조 둘 다 실제 라우트로: probe 발송 4건 → M1 **변화 0** · 비probe 1건 → **+1** ·
같은 행이 대장에는 **있다**. 변이 A/B: K2 필터를 끄면 2 failed · 「전부 0」으로 바꾸면
**양성 대조가 잡는다**(1 failed). 시험 **9 passed** · 발송 축 회귀 7파일 **149 passed**.

**인구조사(실 DB · 내 시험은 실 DB 를 안 쓴다 — 매번 `test_gx_u1*` 을 만들고 지웠다)**
| | 재기 전 13:5x | 재기 후 14:4x |
|---|---|---|
| 사건 / probe 사건 | 26 / **4** | 26 / **4** |
| 발송 / probe 발송 | 229 / **4** | 233 / **8** |
발송만 **+4 (전부 probe)** — 내 시험이 아니다. 8건 전부 **사건 #295402 하나**에 달렸고
04:57Z 4건 · 05:31Z 4건 **두 무리**(채널 `log` · `retry_count=0`)다. 즉 **씨앗 하나가
제 발송을 계속 낳고 있다** — P-193 이 왜 발송 축까지 가야 했는지의 실물이다.

**③ 음성 대조 + 양성 대조 — 실제 라우트를 실제 자격으로**
`tests/test_u1_probe_not_counted.py` (**7 passed** · 캐시 우회 `X-No-Cache`):
probe 4건 심음 → `unhandled` **변화 0** · 이어서 비probe 1건 → **+1**.
**변이 A/B 로 시험이 사는 것을 확인**: 커널 필터를 끄면 3 failed(음성 대조 포함) ·
`exclude_probe` 를 「전부 0」으로 바꾸면 **양성 대조가 잡는다**(1 failed).

**④ 새 문 0** — 라우트 추가 0. `NoNewDoorForProbeTest` 가 `api.py`·`api_u1.py` 의
`@route.` 줄에 probe 가 얹히면 빨강을 낸다(선등록 ㉠).

**⑤ 회귀** — `focus_queue` 「가장 오래 기다린」이 **실측 최고령(비probe)**과 일치 ·
대기 카드 `allowed_next` 에 되돌림(`in_progress`) **없음**(화면은 이 목록을 그대로 단추로 그린다).
9파일 172 passed · 금지구역 변경 **0건**.

## 손 위

없음. 보고 작성 중.

## 안 한 것 (넘긴다)

- ⚠ **세 번째 구멍을 찾았지만 안 고쳤다 — `backend/apps/dsm/metering.py`** (내 파일이 아니다).
  `_events`(183행 위) · `_notifications` · `_notifications_failed` 가 **커널을 안 거치고**
  `DetectionEvent` · `DeliveryRecord` 를 직접 세고, 그 수가 **청구서로 간다.**
  즉 지금 구조에서는 **게이트가 심은 씨앗에 돈을 청구한다.** 고치는 법은 같다 —
  `common.probe_marker.exclude_probe(qs)` / `(qs, via="event")` 한 줄씩.
  조율자 대장에 적었다.

- **프런트 변경 0** — 의도적이다(감추지 않는다). 다만 씨앗이 있는 동안 EventList 의
  배지(`summary.unhandled`)와 목록 건수가 **다를 수 있다**. 그것이 P-193 의 모양이다
  (배지=일감 · 목록=사실). 표기하는 UI 는 **만들지 않았다**(선등록 ㉠).
- `docs/agent/verify_gates.sh --gate dormant` **빨강 — U1 것이 아니다**:
  `backend/common/config_read_cache.py::cached_reads`·`uninstall`(차선 F).
- `route-alive`·`contract-route-reach` 는 **못 잼**(서버 필요 · F 측정 창이라 배치 안 함).
- 브라우저·캡처는 V 몫.

## 자진 오판 2

**㉠** 9파일 합본에서 `test_dsm_app::SettingGuardTest` 2건이 빨갰다(`101 != 100` —
감사 읽기 상한 100 포화). **내 변경 탓으로 적을 뻔했다.** 격리 A/B 로 기각: 같은 9파일을
다시 돌리니 **172 passed**, 필터를 켠 채/끈 채 둘 다 초록.

**㉡** 그 빨강을 처음엔 「낡은 test DB」로 적었다. **그것도 틀렸다** — 매 실행이 DB 를
새로 만들고 지운다. 더 그럴듯한 설명은 **같은 작업 나무에서 다른 차선이 그 순간
`backend/common/audit_writer.py` 를 고치고 있었다**는 것이다(P-191 · LAW-08 체인 ·
`git diff` 에 +63 · `tests/test_law08_chain_race.py` 신설). `/app` 은 호스트 파일을 그대로
보므로 **실행 중에 코드가 바뀐다.** 확정하지 않는다 — 내 것이 아니고, 재현되지 않았다.
