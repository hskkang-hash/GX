# 턴 X · 차선 Q (정본·도구·걷기) — 체크포인트

갱신: 2026-09-20 14:18 KST · 3차(끝낼 때) · 창 13:45~15:00 (F 가 3차 체크포인트로 닫은 뒤 앞당겨 씀)

## 닫음 (눌러서 확인한 것만)

### ① ★★ P-193 의 **진짜 시험 — 통과. 그리고 공허하지 않다**
그냥 재면 공허했을 시험이다: 재기 전 저장소에 `response_state='occurred'` 사건이
**0건**이라 `unhandled` 는 이미 0 이었고, `0 → 0` 은 필터가 아니라 **잴 것이 없었던 것**이다.
그래서 **씨앗을 실제로 심어** A/B 로 세웠다(`capture_screens.seed_events` · K1 생성 경로 ·
`track_id=data_source=probe;run=20260920T045111` · 그림 4/4장 제품 경로 업로드).

| | probe 인구조사 | 그중 미처리 | `summary.unhandled` (X-No-Cache 켬/끔) |
|---|---|---|---|
| 심기 **전** | **0건** (표식 0 · 카메라 0) | 0 | **0 / 0** |
| 심은 **뒤** | **4건** (표식 4 · 카메라 4 · 두 셈이 같다) | **4** | **0 / 0** |

**심은 4건이 전부 `occurred` 인데 `unhandled` 가 한 칸도 안 움직였다** — P-193 이 없었으면
4가 됐을 자리다. 캐시가 덮은 것이 아니다(`X-No-Cache` 켠 판과 끈 판이 **같은 0**).
**숨긴 것이 아니라 안 센 것**도 눌러서 봤다: 운영자 목록 `GET /api/dsm/events`
**26건**(22+4) — 심은 4건이 **전부 보인다**. `?response_state=occurred` 문도 **4건**을 낸다.

### ② ★ FC 재측 — **27/48 이다. 29 도 32 도 아니다**
`--measure` exit 0 · 실측 `2026-09-20T04:47:45Z` · 되읽기 초록 · 가리기 18곳에 수 불변.
**초록 27 · 빨강 6 · 회색 15** (턴 W: 초록 32 · 빨강 1 · 회색 15).
회색 15는 **한 행도 안 바뀌었다**. 바뀐 것은 **초록 5행이 빨강으로** 간 것뿐이다.

| 행 | 턴 W | 턴 X | **원인을 이름으로** |
|---|---|---|---|
| U1#11 판정 | 초록 | **빨강** | 미판정 사건 **0건**(verdict: confirmed 18 · rejected 4) → 드라이버 `unv` 가 비어 **이미 판정된 4798** 을 골랐다(`verify_click_completes.py:1977`) → `verdict` confirmed→confirmed |
| U1#19 교대 인계 | 초록 | **빨강** | `summary.unhandled` 0→0 · **`after==0` 을 판정기가 「값 없음」으로 읽는다**(아래 ③) |
| U2#1 이벤트 목록 | 초록 | **빨강** | 같음 (`summary?hours=12` `unhandled` 0) |
| U2#2 미처리 보기 | 초록 | **빨강** | 같음 (`/events?response_state=occurred` `events` 0) |
| U3#1 알림 보내기 | 초록 | **빨강** | **F-04 5분 중복 억제** — `kernels/k2_notify/services.py:478 suppress()`. 억제창 기준이 **발송 시각이 아니라 사건 발생 시각**(`occurred_at__gte=event.occurred_at - SUPPRESS_WINDOW`)이라, 발생 3분 뒤 성공 발송(#249~252)이 있는 4798 은 **16일이 지나도 영구히 억제**된다. 직접 두드려 확인: `POST /events/4798/notify` → **200 `{"total":0,"deliveries":[]}`**. 제품이 옳다 |
| U6#4 웹훅 | 빨강 | 빨강 | 그대로 — 아래 ⑤ |

**다섯 행 중 어느 것도 제품이 나빠져서가 아니다.** 셋은 **데이터 상태**(씨앗 삭제로
미판정·미처리가 0이 됐다), 하나는 **제품의 규율**(5분 억제), 하나는 **환경**(서명키).
⚠ 조율자 예상 **29** 는 「probe 위에 선 초록 3이 빠진다」였다. 증거로 세어 보면
씨앗 문자열을 딛고 있던 행은 **21행**(초록 20)이었고, 실제로 내려간 것은 **5행**이다.
**예상과 수도 다르고 이유도 다르다.**

### ③ ★ 발견 — 판정기가 **「0」을 「값 없음」으로 읽는다** (거짓 빨강 · **내 파일**)
`verify_click_completes.py:683` `if after in (None, "", [], {}, 0): "서버가 아무것도 안 냈다"`
· 드라이버 `:1917` `st["on_screen"] = bool(a) and …` — **`0` 이 두 자리에서 「없음」이 된다.**
그런데 화면은 「미처리 **0건**」이라 **정직하게 그리고 있었다**(관측 `text_after` 에 그 글자가 있다).
즉 U1#19·U2#1·U2#2 의 빨강은 **제품이 아니라 판정기**다.
**고치지 않았다.** 빨강 셋을 초록으로 바꾸는 판정 규칙 변경을 **내가 재는 턴에** 하면
그것이 이 턴의 거짓 초록이다(★★ 규약). **판정 요청으로 넘긴다.**

### ④ 정본 술어 좁힘 **2차 회귀 — 새로 어긋난 행 0. 그러나 「0」의 뜻이 달랐다**
`--self-test` exit 0 · `[P-190] 갈린 행 0`. 분모를 세니 **갈릴 수 있는 행이 0** 이었다:
`읽은 행 48 · 양쪽이 다 말한 행 33 · 정본「정본 없음」 0 · 제 입으로 회색 3 ·
이 파일 문 있다 34 · 「정본 없음」 2`. 다툼 두 갈래 중 `(fc_has & canon_none)` 은
`canon_none=0` 이라 **구조적으로 죽었고**, 다른 갈래 후보 2행(U1#4·U3#14)은 둘 다 정본이
제 입으로 「회색」이라 다툼이 못 된다 → **살아 있는 정본으로는 빨개질 수 없다.**
종전 출력은 「**48행**에 같은 정본」이라 적었다 — 48 은 대 본 수가 아니라 표의 행 수다.
**고쳤다**: `canon_compare_census()` 신설 · 실수를 찍고 · 갈릴 수 있는 행이 0이면 ⚠ 로
「이 줄을 초록으로 만드는 것은 정본이 아니라 출생 표본이다」를 말한다.
**변이 A/B**: 정본 한 칸(U6#2)을 「정본 없음」으로 바꾼 사본 → `capable 0→1` · 갈린 행 `['U6#2']`.

### ⑤ U6#4 원인 — **이름으로. 제품 결함이 아니다**
`POST /webhook-subscriptions?...signing_key_ref=p118-gate` → **422**
← `common/webhook_outbox.py:219 UnknownSigningKey` ← `signing_secret()` 빈 문자열
← `settings.WEBHOOK_SIGNING_KEYS` 빈 표 ← `config/settings.py:1323` 이 읽는 환경변수가 **없다**.
`gx-gunicorn-e`·`gx-shell` 두 컨테이너에서 **이름 존재 여부만** 확인(값은 안 찍었다) — **둘 다 없다**.
행이 안 생기니 `total` 0→0. 고치는 길 둘(환경에 키를 준다 / 정본을 새 문으로 옮긴다)을
`_조율자.md` 에 판정 요청으로 올렸다. **내가 정하지 않았다.**

### ⑥ 게이트 문안 — **손으로 적은 분모를 걷어냈다**
`verify_gates.sh` `click-completes` 의 칭찬이 「양성 **38** · 식 자기수용 **38**」인데
도구가 내는 수는 **34** 였다. 사람이 적은 수는 갈린다 → **도구가 낸 줄을 그대로 옮긴다**
(`O`·`⚠` 줄). 게이트 재실행 확인. (U3 가 같은 턴에 더한 `camera-secret-logs` 와 안 부딪힌다 ·
`verify_gate_counts` **19종** 초록.)

### ⑦ 온보딩 재측 — **26.0/48 · 잰 행 35 · 못 잼 0** (턴 W 27.0 에서 한 행)
`rows_attempted=35 · rows_measured=35 · gray_measured_fail=**0**` ·
초록 24 · 반 4 · 빨강 7 · 분모 48(두 칸 35 · 정본 없음 13 — 손으로 안 적었다).
증거 `P-159/onboarding_measure_20260920T045202.json`.
내려간 행은 **하나뿐**이고 이름이 있다:

**U1#11 초록 → 빨강 · 원인은 「P-193 이 제대로 섰기 때문」이다.**
도구가 적은 말: 「큐 화면에 『실제로 확인 · 접수』 가 보이지 않는다 — 보이는 단추 `['종결하기']`
· 초점 사건이 미처리가 아니면 이 단추는 없다(**데이터 상태**)」.
**눌러서 확인**: `GET /api/dsm/events/queue` → **5건 전부 `in_progress`** 이고 내가 심은
`occurred` probe 4건(295402~295405)은 **한 건도 없다.** 초점 큐가 `query_events` 를 지나
probe 를 빼기 때문이다(P-193 설계대로).
→ **P-193 의 값(비용)이 여기 있다**: 게이트가 심은 씨앗을 안 세게 만들었더니 **게이트가
그 씨앗으로 잴 수도 없게** 됐다. 「미처리 흐름」을 재려면 이제 **probe 아닌 씨앗**이 있어야 한다.
이건 결함이 아니라 **갈림길**이고, 제가 정하지 않습니다 — 판정 요청으로 올립니다.

### ⑧ 걷기 W1~W6 — **5/5 유지 · 세션 열린 채 0**
`walk exit 0` · 완주 **5/5** · **세션 연 3 · 닫은 3 · 열린 채 0**(`POST /auth/logout 200`) ·
분류되지 않은 콘솔 오류 **0건**(알려진 잡음 118건은 따로 센다) ·
**W4 는 「안 걸음」 1건** — 사유가 이름으로 등재돼 있고 **초록이 아니다**.
대장 `walk.json` 옛 7회 + 이번 1회 → **8회**(줄지 않았다).
[다음 사람이 볼 자리] 화면이 부른 API 중 실패: `422 GET /api/user-groups/gen-schema?raw=false`
(W1·W2·W3 · dj-core §0.4 · 이미 등재된 결함).

### ⑨ 게이트 재실행 — **거짓 회색이 걷히고 빨강이 드러났다**
- `click-completes`: 턴 시작엔 **SKIP(회색 · 증거 17.5시간 낡음)** 이었다 → 재측 뒤 **FAIL** ·
  `27/48` · 빨강 6. **회색이 초록이 아니었듯, 지금의 빨강은 정직한 상태다.**
- `evidence-roundtrip`: **PASS** · 넷 다 다시 읽어 같은 수
  (FC 27/6/15 · 온보딩 26.0 · 도달 39절 · 걷기 8회차).
- `verify_gate_counts` **19종** 초록 · `verify_tool_selftest` 표본 없이 태어난 새 도구 **0종**.

## 손 위
- 없다. 넷 다 쟀다(FC · P-193 A/B · 온보딩 · 걷기).

## 안 한 것 (넘긴다)
- ③ 「0은 값이다」 판정 규칙 **안 고쳤다**(판정 요청).
- **심은 probe 4건(295402~295405)을 안 지웠다** — 「지우지 않고 세지 않는다」(P-184)가
  규약이고 지우는 것은 대표 결정이다. 셈에는 안 들어간다(①에서 눌러서 확인).
  ⚠ 다음 사람이 알 것: 최신 씨앗 명세가 `P-157/runs/20260920T045111/seed.json` 로 바뀌었다.
  직전 것(`20260919T103803`)이 가리키던 285641·285642 는 **삭제돼 없었고**, 그대로 두면
  온보딩이 **없는 사건으로 재서** 씨앗 탓 빨강을 냈을 자리다.
- U6#4 **안 고쳤다**(판정 요청).
- **내가 고친 정본(④·⑥)으로 내가 잰 수를 「좋아졌다」고 적지 않았다** — ④·⑥ 은 FC 수에
  한 칸도 영향을 주지 않는다(판정 규칙 0줄 · 흐름 표 0줄 · 출력 문안만 바꿨다).
