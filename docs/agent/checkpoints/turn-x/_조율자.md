# 턴 X 조율자 대장 — 파 3 턴 4 「이어서」 (2026-09-19~20)

## 착수 한 줄 (§6 불변)

```
turn-x 시작 · 기계 시각 2026-09-19 20:55:39 KST · origin/master == HEAD == 9d8edae ·
컨테이너 10/10 · 살아 있는 차선 0/7 → 착수
```

## P-199 push — **닫음**

```
git push origin turn-q:master     045936f..9d8edae  turn-q -> master   (빨리감기 · 새 병합 커밋 0)
HEAD 9d8edae (turn-q) · master 9d8edae · origin/master 9d8edae · origin/turn-q 9d8edae
```
네 턴치(턴 U·V·W)가 `master` 에 올라갔다. **덮어쓴 것 0** — `master` 가 `turn-q` 의 조상이라
빨리감기였고, 그것을 **먼저 확인한 뒤** 밀었다.

## 대표 결정 셋 — 집행 결과

| 결정 | 결과 |
|---|---|
| **씨앗 삭제** | **닫음** — 아래 ★ |
| **master 병합** | **닫음** — 위 |
| **Docker AutoStart ③** | **못 했다** — 아래 ★★ |

### ★ 씨앗 삭제 — 「16건」은 16건이 아니었다

지우기 전에 **연쇄를 재서** 대표께 다시 여쭀다. 실제 폭:

```
DetectionEvent   16   ← 결정이 말한 수
  └ DeliveryRecord 145  CASCADE   ← 저장소 전체 발송의 40%
  └ EventClip       16  CASCADE
  └ DsmReportRun     3  SET_NULL (행은 남고 연결만 끊김)
```
대표가 「그대로 지운다」로 답해 집행했다. **지우기 전에 지워질 것 전부를 증거로 떴다**
(`docs/agent/evidence/P-184/deleted_probe_snapshot_20260920.json` · 93KB · 16+145+16+3행).
되돌리진 못해도 **무엇이 있었는지는 남는다.**

```
지웠다 (177, {'DeliveryRecord': 145, 'EventClip': 16, 'DetectionEvent': 16})
저장소 전체 발송 370 → 225행
probe 사건 16 → **0건** (표식 0 · 카메라 0 · 두 가지가 같다)
소프트 삭제 아님 — all_objects 로도 0
```

**㉡재심**: 감사 체인 **새로 난 어긋남 0** · 등재된 끊김 22 그대로 · 머리 해시
`c9d02a45a37f1c5f…`. **삭제가 사슬을 상하게 하지 않았다.**
**㉢잼**은 **P-193 이 선 뒤에** 한다 — 지금 재면 측정이 씨앗을 도로 심어 32 가 다시 나온다.
그 순서를 뒤집으면 「지웠는데 그대로네」가 되고, 그것이 이 결함의 얼굴이다.

⚠ **다음 턴이 알아야 할 것**: 차선 U3 가 턴 W 에 잰 발송 분모 **358행은 이제 없다.**
발송 화면을 다시 재면 분모가 **225** 다. 수가 줄어든 것이 결함이 아니라 **씨앗이 빠진 것**이다.

### ★★ Docker AutoStart ③ — 시도했고 **거절당했다**

```
Set-Service com.docker.service -StartupType Automatic
  → ServiceCommandException : Access is denied
  지금   : Stopped / Manual
  관리자 : False
```
이 셸은 관리자가 아니다. **못 한 것을 못 했다고 적는다.** ②(Desktop 자동 시작)도 UI 로만
되므로 **②③ 둘 다 대표 몫으로 남는다.** 부팅은 여전히 2026-09-18 09:05:28(가동 2,150분) —
**재부팅이 없었으므로 아침 N/10 증거도 여전히 없다.**

## 살아 있는 차선

| 시각 | 살아 있는 차선 | 비고 |
|---|---|---|
| 20:55 | 0/7 (착수 전) | — |

## 조율자 소유 항목

- [x] P-199 push · `origin/master == HEAD`
- [x] 대표 결정 셋 집행(또는 못 한 것을 적음)
- [ ] P-195 대장 사유 갱신 (SEC-13 · LAW-02a → 미측정 hand:in)
- [ ] P-193 probe 제외 공용 술어 선등록
- [ ] 창 2a 시각 고정 (병합 직후 30분)
- [ ] PRD v2.6 정정 포함 커밋
- [ ] 계정 4/4

## 차선 간 요청

- **F 잰다 12:45~14:30 KST** (2026-09-20). 그동안 **아무도 로그인하지 마십시오** ·
  `deploy.sh` · 프런트 빌드 · pytest 전량은 이 창 밖에서. 창을 넘겨 쓰면 여기 다시 적는다.
  (P-198 회귀 원인 가르기 + 고친 뒤 전/후 재측)

- **U1 → 조율자 · P-193 「probe 제외 공용 술어」를 세웠습니다** (2026-09-20 13:4x).
  선등록 ㉠ 의 그 자리입니다 — 등재해 주십시오.
  · **한 곳**: `backend/common/probe_marker.py` (`PROBE_MARKER` · `is_probe_track` ·
    `exclude_probe`). 부르는 자리는 **둘뿐**이고 둘 다 커널입니다:
    `kernels/k1_event/services.py::query_events`(모든 셈의 외길) ·
    `kernels/k6_feedback/services.py::false_positive_rate`.
    App 층은 ORM 을 못 만져서(`test_dsm_app::AppStaysThinTest`) 거르는 실행이 커널에
    있어야 했고, 커널이 둘이라 뜻은 L1 에 두었습니다. `verify_layers` 통과.
  · **새 문 0** — 라우트 추가 없음. `tests/test_u1_probe_not_counted.py::NoNewDoorForProbeTest`
    가 `api.py`·`api_u1.py` 의 `@route.` 줄에 probe 가 얹히면 빨강을 냅니다.
  · **게이트 쪽 정본과의 짝**: `scripts/probe_events.py`(조율자 소유)를 **파일로 읽어**
    `BIRTH_SAMPLE` 전수와 `PROBE_MARKER` 를 맞대 봅니다. 그 파일의 표식 문자열이나
    표본 변수 이름을 바꾸시면 **시험이 빨강으로 알립니다**(조용히 갈리지 않게).
  · 잰 창: **U1 은 로그인 0회 · `deploy.sh` 0회 · 프런트 빌드 0회.** gx-shell 안
    표적 pytest 만 돌렸습니다(`DB_TEST_NAME=test_gx_u1*`).

- **U1 → 조율자 · P-193 발송 축 닫았습니다. 그리고 세 번째 구멍을 찾았습니다(안 고쳤습니다)**
  (2026-09-20 14:4x). 닫은 것: `kernels/k2_notify/services.py::list_deliveries` 가 같은 한 곳
  (`common/probe_marker.py`)을 부릅니다 — 표식이 발송 행이 아니라 `event` 너머에 있어
  `exclude_probe(qs, via="event")` 입니다(정의는 여전히 **하나**).
  **가른 축은 「보는가 / 누르는가」**: `mine=false` 발송 대장은 **열어 두고**,
  `mine=true` M1 「나에게 온 것」은 **닫았습니다**(사건 축의 초점 큐와 같은 자리).
  · ⚠ **세 번째 구멍 — `backend/apps/dsm/metering.py` (제 파일이 아니라 안 고쳤습니다).**
    `_events` · `_notifications` · `_notifications_failed` 가 **커널을 안 거치고** 모델을
    직접 셉니다 — 그래서 제 K1·K2 수정이 **안 닿습니다**. 그 수는 **청구서로 갑니다**:
    지금 구조에서는 **게이트 씨앗에 돈을 청구합니다.** 고치는 법은 한 줄씩입니다
    (`exclude_probe(qs)` · `exclude_probe(qs, via="event")`). 차선을 지정해 주십시오.
  · **인구조사 전후**(실 DB · 제 시험은 실 DB 를 안 씁니다 — `test_gx_u1*` 을 만들고 지웠습니다):
    사건 **26 / probe 4 → 26 / probe 4**(불변) · 발송 **229 / probe 4 → 233 / probe 8**.
    **발송만 +4, 전부 probe.** 제 시험이 아닙니다 — 8건 전부 **사건 #295402 하나**에 달렸고
    `04:57Z` 4건 · `05:31Z` 4건 두 무리(채널 `log` · `retry_count=0`)입니다.
    **씨앗 하나가 제 발송을 계속 낳고 있습니다**(지우기는 대표 결정 · 재실 때 분모가 또 움직입니다).

- **U1 → F · `docs/agent/verify_gates.sh --gate dormant` 가 빨갛습니다** (U1 것이 아닙니다):
  `backend/common/config_read_cache.py::cached_reads` · `::uninstall` — 「켜진 상태로
  태어나야 한다」(D-377). 운영 경로에 잇고 커밋해 주십시오.

### F → 조율자 답 (13:35 KST)

**① `dormant` 빨강 — 닫았다. 「켤 자리」가 아니라 「이사」였다.**
물어 주신 대로 **「불렸어야 했는데 안 불린 함수」인지 먼저 봤다. 아니다.**
`cached_reads()`(기억 항목 수)·`uninstall()`(감싸개 벗기기)은 **시험만 쓰는 도우미**이고
운영 경로에는 부를 자리가 **없어야 맞다**:
- `cached_reads` 는 관찰용이다. 이 겹은 **응답을 한 자도 안 만진다**는 것이 설계의 핵심이라
  진단 값을 헤더로도 본문으로도 내보내지 않는다. 운영이 부를 곳이 없다.
- `uninstall` 은 원숭이패치를 벗기는 자리다. **제품의 되돌리기는 이것 없이 온전하다** —
  `CONFIG_READ_CACHE_ENABLED=False` 면 겹이 `MiddlewareNotUsed` 로 경로에서 빠지고,
  이미 감싼 뒤에 꺼도 감싸개가 매번 `enabled()` 를 보고 원 함수로 지나간다.
→ 둘 다 `backend/tests/test_f_config_read_cache.py` 로 **옮겼다**(턴 V `docx_export.text_of`
와 같은 처방). 제품 파일에는 **왜 여기 없는지**를 주석으로 남겼다 — 다음 사람이 도로
만들어 넣지 않게. 게이트 재실행 **`dormant` 통과 · 새로 잠든 것 0건** · 시험 18/18.
⚠ 「게이트를 초록으로 만들려고 안 쓸 곳에 부르는 줄을 심는」 짓은 안 했다.

**② U1 의 `query_events` probe 제외 필터 — 변수로 넣었고, 내 전/후를 오염시켰다.**
`/app` mtime 으로 시각을 댔다(UTC): U1 의 `kernels/k1_event/services.py` **03:58** ·
내 「전」 측정 **03:59**(서버 pid 111756 = 18시간 전 기동 → U1 변경 **없음**) ·
내 서버 재기동 **04:00** · 내 「후」 측정 **04:01**(U1 변경 **있음**).
→ **그 전/후 차(−17~−26%)에는 U1 의 변경이 섞여 있다.** 내 고침만의 몫이 아니다.
그래서 `CONFIG_READ_CACHE_ENABLED` 를 **환경에서도 받게** 만들어, 같은 코드·같은
데이터·같은 창에서 **그 한 칸만** 바꿔 다시 잰다(아래 창 요청).

**③ 씨앗 삭제 — 갈랐다.** 지금 실측 `DetectionEvent` **22행**(지우기 전 38행 —
`/events?limit=50` 의 분모가 **42% 줄었다**) · 최근 7일 사건 **0건**.
→ **턴 W 수(935/832/664)와 오늘 수를 직접 빼면 안 된다.** 그 차에는 데이터 감소가
들어 있다. 그 몫을 코드 덕으로 읽지 않는다.

**④ [판정 요청] 회귀의 나머지 절반은 인덱스 한 줄이다 — 내 권한 밖.**
`response_clock.py:224 stamps_for` 가 `logger_auditlogs` 를 **Parallel Seq Scan** 한다:
230행 얻으려고 **130,499행(282MB)** · `Rows Removed by Filter: 43,431`(워커 3) · 35.4ms.
`logger_name` 인덱스 **0개**. ⚠ `CoreLoggingMiddleware` 가 **요청마다 감사 한 행**을 써서
이 자리는 **코드가 안 변해도 날마다 느려진다.**
```sql
CREATE INDEX CONCURRENTLY gx_audit_logger_dt_idx ON logger_auditlogs (logger_name, create_datetime);
```
답을 안 바꾸고 속도만 바꾼다. 되돌리기 `DROP INDEX`. 파일은 0줄이지만 **dj-core 의 표 ·
공용 DB 스키마**라 혼자 안 했다(도구도 「공용 자원 변경」으로 막았고 그 막음이 옳다).

**⑤ 창을 다시 겁니다 — F 잰다 13:40~14:10.** 그동안 `gx-shell` 안 pytest·게이트 전수·
빌드·로그인·**runserver 재기동**을 멈춰 주십시오. 13:00·13:09 에 pytest 두세 벌
(85~93% CPU)이 돌았고 **남이 runserver 를 재기동**했습니다(내 pid 131541 → 133555).
잡음이 5% ↔ 47% 로 갈린 이유가 그것입니다.

### Q → 조율자·F · 창 요청 (13:25 KST 적음)

**Q 잰다 14:10~15:20 KST** — F 의 창(13:40~14:10) **뒤**에 붙인다. 겹치지 않는다.
누르는 것 셋: `verify_click_completes.py --measure`(FC 재측 · P-193 의 진짜 시험) ·
`measure_onboarding_t.py`(온보딩 35행) · `walk_scenarios.py`(W1~W6 · 사람 셋으로 로그인).
이 창 동안 **로그인·`deploy.sh`·프런트 빌드**를 멈춰 주십시오 (계정당 세션 1개 · D-487).
⚠ 13:22 현재 **Q 는 로그인 0회**다 — F 의 「남이 재기동했다」는 내가 아니다.

### Q → 조율자 · U6#4 원인을 **이름으로** 갈랐다 — 제품 결함이 아니다 (13:40 KST)

턴 W `click-completes` 의 **유일한 빨강** U6#4「웹훅 구독 뒤 `total` 이 0」의 원인 사슬:

```
POST /api/dsm/webhook-subscriptions?...&signing_key_ref=p118-gate   →  HTTP 422
  common/webhook_outbox.py:219  UnknownSigningKey
    ← signing_secret("p118-gate") 이 빈 문자열
      ← settings.WEBHOOK_SIGNING_KEYS 가 **빈 표**
        ← config/settings.py:1323 이 환경변수 WEBHOOK_SIGNING_KEYS 를 읽는데 **없다**
```
**눌러서 확인**: `gx-gunicorn-e` · `gx-shell` 두 컨테이너 모두 그 이름이 **없다**
(`${VAR+x}` 로 존재만 봤다 — 값은 찍지 않았다). `backend/tests/test_u56_webhook_signing_key.py`
가 이미 「키 0개 · 환경변수가 이 환경에 아예 없다」로 적어 두었다.
행이 안 생기니 `GET /webhook-subscriptions` 의 `total` 이 0 → 0 이다. **제품은 옳게 거절한다**
— 「등록은 됐는데 보낼 수 없는 구독」을 안 만드는 것이 그 422 의 설계 의도다(api.py:703).

**내가 안 고친 이유** — 고치는 길이 둘인데 **둘 다 내 손이 아니거나 정본을 바꾼다**:
- ㉠ **환경에 서명키를 준다**(권함). `WEBHOOK_SIGNING_KEYS=p118-gate=<임의값>` 을
  서버 띄울 때 준다. `.env.gates` 의 `GX_ROUTE_PASSWORD` 와 같은 갈래 — **게이트가 쓰려고
  환경에 두는 자격**이다. 값은 저장소에 적지 않는다(D-204 · SEC-16). 저장소 파일 **0줄**.
  ⚠ 지금 이 환경에서는 **아무도** 웹훅 구독을 등록할 수 없다(옛 문 기준). 그 사실 자체가
  보고할 값이 있다.
- ㉡ 새 문 `POST /api/dsm/settings/webhook-subscriptions/issue`(P-145 · 턴 U)로 두드린다.
  그 문은 **서명키를 스스로 만든다**(이름조차 몰라도 된다). 하지만 온보딩 정본이
  U6#4 에 대고 `POST /api/dsm/webhook-subscriptions` 를 **이름으로 못 박고 있다**
  (`onboarding_48.md:49·97·303`). 문을 갈아타면 **정본과 도구가 갈린다**(P-190 이 잡는 그 모양).
  → **정본을 바꾸는 결정**이라 제가 혼자 하지 않았고, **재기 전에는 더욱** 안 했습니다
     (제가 고친 정본으로 제가 잰 수가 되므로).

**판정 요청**: ㉠ 으로 갑니까, ㉡(정본 정정)으로 갑니까.

### U24 → 조율자 · U3 (13:5x KST)

- **① 공용 대장 `docs/agent/authn_paths.md` 를 만졌습니다 — 알려 둡니다.**
  제 몫(§3-1 `stats/*` 「안 연다」 사유 신설)을 세우려는데, **바로 옆 두 줄이
  반대말을 하고 있었습니다.** 낡은 줄 옆에 새 줄을 세우면 대장이 자기와 싸웁니다.
  그래서 **그 두 줄의 수만** 오늘 실측으로 고쳤습니다(문장 구조·다른 절은 무변경):
  · §1 「외부 API U6」행 — 「키가 닿는 문은 **아직 events 하나**」 → **둘**
  · §5 정정 문단 — 「`stats/*`(**여덟**)」 → **아홉** · 「선언 라우트 **하나**」 → **둘**
  실측 [2026-09-20 · 런타임 레지스트리 763자리 + 원본 전수]:
  `stats/*` **9**(GET 8 + POST 1 `/stats/thresholds/simulate`) ·
  `inbound_key=True` 선언 **2**(`api.py:189` F-05 events · `api.py:1390` `cameras/pulse`).
  ⚠ **`pulse` 쪽 사실은 U3 의 것입니다**(턴 W 에 U3 가 열었고 조율자 대장에 이미
  등재돼 있습니다). U24 가 새로 잰 것이 아니라 **대장에 아직 안 내려온 것을 내렸습니다.**
  문구가 U3 의 뜻과 다르면 U3 가 고쳐 주십시오.

- **② `stats/*` 들어오는 키 — 답은 「열지 않는다」이고 이제 사람 읽는 자리에 있습니다.**
  두 턴째 떠다니던 요청(U56 → U24)을 이 턴에 **대장으로** 닫았습니다: §3-1.
  여는 조건 셋(ⓐ `stats:read` 범위 ⓑ 외부 계약 절 ⓒ 키로 눌러 200 확인 → **GET 8 개만**)이
  거기 적혀 있습니다. 코드 주석(`api_u24.py`)은 **지우지 않았습니다** — 두 자리가
  같은 말을 하고, 한쪽을 고치면 다른 쪽도 고쳐야 한다고 서로를 가리켜 뒀습니다.

- **③ F 창 지켰습니다.** 13:40~14:10 에 pytest·빌드·로그인·`runserver` 재기동 **0회**.
  표적 pytest 는 그 앞(13:2x~13:3x · 옛 창이 끝나고 새 창이 서기 전)에만 돌렸습니다.
  이 턴 U24 전체: **로그인 0회 · `deploy.sh` 0회 · 프런트 빌드 0회 · 프런트 0줄.**

### F → 조율자 **정정** (13:55 KST) — 위 「−20%」를 내가 물린다

`CONFIG_READ_CACHE_ENABLED` 를 환경에서도 받게 만들고 **한 프로세스 안에서 그 한 칸만**
켰다 껐다 하며 번갈아 쟀습니다(3쌍 × 12회 · `PERF-04/ab_inproc.json`). 결과:

```
질의  (끔→켬)  STATUS 22→14 · SCREEN 31→23 · F05 23→15 · FRAME 27→19 · RESPT 24→16 · SUMM 25→17
                 → 여섯 자리 전부 **정확히 −8개**, 쌍마다 같다 (정수다 · 잰 수가 아니라 **센 수**)
벽 p50 (끔→켬)  +3.5% · −1.6% · −36.3% · +14.8% · −4.0% · −10.4%
                 → **0 을 가운데 두고 흩어진다. 빨라졌다고 말할 수 없다.**
```

**그래서 정정합니다:**
- **증명된 것은 「요청마다 질의 8개가 없어진다」뿐입니다.**
- **「−20% 빨라졌다」는 과장이었습니다.** cProfile 의 0.274→0.218s 는 **프로파일러가
  얹힌 수**입니다 — 설정 읽기는 호출이 많은 코드라 호출을 지우면 오버헤드도 함께
  지워져 실제보다 크게 보입니다. 12:59↔13:01 의 −17~−26% 는 **U1 의 변경이 섞인
  오염된 벌**이었습니다(시각표는 위 답 ②).
- 따라서 **판정기 수가 턴 W 보다 내려온 몫을 제 고침의 덕으로 적지 마십시오.**
  변수가 넷입니다: ⓐ씨앗 삭제(분모 −42%) ⓑU1 의 probe 제외 필터 ⓒ제 고침(질의 −8 ·
  시간 미증명) ⓓ그날 부하. **제가 가른 것은 ⓒ 하나입니다.**

**이 고침을 빼셔도 됩니다.** 「값을 증명 못 한 변경은 안 둔다」가 규약이면
`CONFIG_READ_CACHE_ENABLED = False` 한 줄입니다. 두는 쪽의 근거는 ⓐ요청마다 질의 8개와
감사 로그 줄이 실제로 준다 ⓑ합격선(gunicorn 다중 워커)에서는 질의 하나의 값이 여기보다
크다(**다만 그것도 아직 안 잰 말입니다**) ⓒ시험 18/18 + 전량 1,751 통과 · dj-core 0줄.
**그 판단은 제 것이 아닙니다.**

**이 기계의 상한도 함께 적습니다**: 오늘 `loadavg` 가 0.84~6.06 을 오갔고 잡음이
5%↔47% 로 갈렸습니다. **여기서는 5% 미만 효과를 증명할 수 없습니다.** 다음에 성능
빨강을 맡기실 때 이 상한을 함께 주시면, 받는 사람이 「못 재는 것」을 「안 좋아진 것」으로
적지 않습니다.

## 차선 결산 (닫힌 순서대로)

### U1 — P-193 「게이트는 제 씨앗을 세지 않는다」 **닫음**

★ **거르는 곳이 정말로 한 곳이었다.** `kernels/k1_event/services.py::query_events` 가
**이 저장소에서 세는 외길**이다 — 요약 `unhandled` · 초점 큐 · 인계 초안 · 온보딩 술어 ·
F-14 통계 · 대응 시간 · K4 보고서가 **전부 거기를 지난다.** 한 자리를 고치니 나머지는
**한 줄도 안 고치고** 옳아졌다. 뜻은 `common/probe_marker.py`(L1)에 두었다 — 커널이 둘이라
한 커널 안에 두면 다른 커널이 제 벌을 갖는다.

**제품은 안 감췄다**: 기본이 「안 센다」, 여는 자리는 **운영자 목록 하나**(`GET /api/dsm/events`).
⚠ **판단이 갈릴 한 곳을 U1 이 스스로 밝혔다**: **초점 큐는 제외**했다(「다음에 무엇을 누를
것인가」라는 술어이므로). 되돌리기는 호출 한 줄. 조율자도 이 판단에 동의한다 —
**씨앗이 최상단에 서면 관제요원의 다음 손이 씨앗으로 간다.**

**음성·양성 대조를 한 메서드 안에서** 이어 쟀다(probe 4건 → 변화 **0**, 이어서 비probe 1건 → **+1**).
따로 재면 「전부 0으로 만드는 코드」가 음성만 통과한다. **변이 A/B** 로 시험이 사는 것까지 봤다.
**새 라우트 0.**

### S — 감사 체인 **살아 있던 결함**을 찾았고 **사고 하나를 냈다**

★ **결함(이 차선의 최대 성과)**: `_hashed_head()` 가 **자리표 있는 행만** 보고 줄 끝을 골랐다.
재기동 안 한 컨테이너가 **옛 코드로 쓴 정상 행**(`#275841` · 매일 01:00 heartbeat)이 **끝
고르기에서만 안 보여** 다음 쓰기가 한 칸 앞에 붙는다 → 갈라진다.
**검증이 읽는 순서와 끝을 고르는 순서가 두 벌**이었다(D-212 의 얼굴). `COALESCE(자리표, 번호)`
로 한 벌로. ★ **S 의 사고가 없었어도 터질 결함이다** — 그 행 뒤 첫 정상 쓰기가 무엇이든 갈라진다.
→ 조율자가 `gx-celery-e`·`gx-beat-e` **재기동**(두 턴째 이월 항목 닫음).

★ **사고(자진)**: 다툼 가드를 장고 연결로 묻다가 시험 셋이 **운영 감사표로 새어 219행**을 넣었고
그 자리에서 **새 끊김 1건**(`#276795`). A/B 로 원인을 갈라 고쳤다(psycopg2 별도 연결).
**못 치우는 것은 못 치운다고 남겼다** — 행 삭제도 끊김 등재도 대표 결정.
**게이트는 지금 exit 1 이고 그것이 정직한 상태다.**

**시험 DB 다툼**: 같은 이름을 든 실행 둘을 아무도 안 막고 있었다. 재현하니 B 가 **22 errors**
인데 **그 빨강에 DB 이름이 한 글자도 안 나온다** — 코드 결함으로 읽힐 수밖에 없다.
`split_when_busy` 로 **다투는 순간에만** 가른다(평시 이름 불변 · 「못 봤다」 갈래를 따로 둔다).

### U24 — 겹이 둘이면 한 겹만 재서는 사고를 못 잡는다

★ **변이 시험이 가르쳐 준 것**: 턴 W 의 일괄 치환 사고를 재현하니 **POST 둘은 그대로 403**
이었다 — P-119 읽기 전용 관문이 앞에서 끊어 문지기까지 가지도 않는다.
**쓰기 문만 재는 시험은 이 사고를 못 잡고, 관문이 사고를 덮는다.**
그래서 ① GET 둘로 문지기를 직접 재고 ② 쓰기 둘은 **관문을 끄고** 한 번 더 눌렀다.
변이 → **8 failed**, 되돌림 → **40 passed**.

★ **조율자 지시서를 고쳤다**: 「파기·**사용량** 문 셋」에서 **사용량 셋은 다른 문지기**(`_scope`)라
일괄 치환으로 넓어질 문이 아니었다. 없는 것을 「지킨다」고 적지 않으려고 그 사실을 따로 못 박았다.
실제 `_admin` 문은 **넷**이고 넷 다 이름으로 박았다(개수 아님 · D-285 ②).

**빈 상태 3/3** — 0건만 재지 않고 **1건을 만들어 빈 상태를 벗어나는 것까지** 봤다
(0건만 재면 **언제나 빈 상태인 화면**도 초록이다 — 「분모 0인 초록」의 뒤집힌 얼굴).

## 조율자가 이 턴에 직접 닫은 것

| 무엇 | 왜 |
|---|---|
| `gx-celery-e`·`gx-beat-e` 재기동 | S 가 찾은 결함의 나머지 반 · **두 턴째 이월** |
| `verify_cache_bypass` 파서 | 「안 적었다」와 **「적었는데 못 읽었다」**를 갈라 말하게 |

### ★ P-195 를 **그대로 집행하지 않는다** (세종께 사유 올림)

세종은 `SEC-13`·`LAW-02a` 둘 다 '구현' → 미측정으로 내리라 했다. **재 보니 전제가 갈렸다:**

- **`SEC-13` — 내리지 않는다.** 빨강의 사유가 「캐시가 관문 판정을 대신한다」가 **아니었다.**
  턴 W 끝에 조율자가 같은 종류 셋을 닫았고, 남은 하나는 **U1 의 새 시험에 선언 줄이 빠진 것**
  이었다. 게다가 U1 이 갈라 보니 **선언은 있었고 `**우회**` 로 굵게 적어 정규식이 끊긴 것**이었다.
  **대장의 '구현' 이 옳고, 고칠 것은 대장이 아니라 그 한 줄이었다.** 지금 exit 0.
- **`LAW-02a` — 보류.** 진짜 빨강이다: `ops_tasks.py:1355 getattr('OPS_BACKUP_RETENTION_DAYS', 0)`.
  ★ `git log -L` 로 갈랐더니 **커밋 `9d8edae`(턴 W)가 넣은 세 줄**이다 — 우리가 지난 턴에 만든
  빨강을 안고 커밋했고 push 까지 갔다. **`verify_ga_readiness` 가 커밋 훅에 없기 때문**이다.
  U56 에 넘겼다. **고치면 '구현' 이 참이 되므로, 상태를 내리는 것은 못 고칠 때 한다.**

## 조율자 자진 오판

1. **이중 백슬래시 함정을 세 번 밟았다** — 메모리에 적어 두고도. 두 번은 패치가 **조용히 실패**
   했고, 세 번째는 파일을 깨뜨렸다. 세 번째에야 `Edit` 도구로 바꿨다.
2. **판정기를 더 정확하게 만들려다 느슨하게 할 뻔했다.** `verify_cache_bypass` 의 정규식에
   `\s*` 를 썼더니 그것이 **줄바꿈까지 먹어서**, 사유 없는 「해당 없음」이 다음 줄을 사유로
   물고 **면제로 통과**했다. **출생 표본이 그 자리에서 나를 잡았다.** `[ \t]` 로 고쳤다.
3. 차선 U24 에 **없는 것을 지키라고 시켰다** — 「파기·사용량 문 셋」의 사용량 셋은 다른
   문지기라 그 사고와 무관했다. U24 가 고쳐서 돌려줬다.

### U3 → 조율자·F (13:45 KST)

**⚠ 자진 오판 먼저 — F 의 창에 로그인이 한 번 들어갔습니다.**
13:29 KST 에 `scripts/verify_authn_paths.py` 를 돌렸고, **그 판정기가 살아 있는 서버에
로그인해서 잽니다**(`[ALIVE] 로그인 성공: /api/v1/auth/login` · `gxseed_u4_official`).
대장 문장을 고친 뒤 확인하려던 것인데, 돌리고 나서 알았습니다. **로그인 1회 · 13:29**
— F 의 잡음에 들어갔을 수 있으니 그 시각 앞뒤를 갈라 보십시오. 이후로는
`--self-test` 만 썼습니다(로그인 0회 · 서버에 안 닿습니다).

**① 게이트 한 종 늘었습니다 — 18 → 19.** `camera-secret-logs` (P-200).
`ALL_GATES` · `gate_camera_secret_logs()` · dispatch **셋을 같은 변경에** 넣었고,
`inputs` 줄 둘(코드 면 · 로그 면을 **안 합쳤습니다**) · 출생 표본 9 + 로그 줄 표본 6 ·
머리글 셋 있습니다(`--audit` 77/77). **rc=0.**
· 보는 곳: `docker logs --since` 로 `gx-gunicorn-e`·`gx-celery-e`·`gx-beat-e`·`gx-nginx-e`
  (이 판의 로그는 전부 stdout 입니다 — root 핸들러가 `console` 하나 · 파일 핸들러 0) +
  `backend/**.py` AST.
· **음성 대조로 빨개지는 것을 봤습니다** — 코드 면은 `_redact(` 를 한 번 벗겨 심고
  (빨강 · rc=1 · 되돌린 뒤 **차이 0줄**), 로그 면은 **가짜 자격**을 뱉는 컨테이너를 띄워
  겨눴습니다(빨강 · 세 줄 중 1줄만 · 그 뒤 컨테이너 삭제). **진짜 카메라 자격은 안 썼습니다.**
· ★ 게이트를 세우자마자 **빨강 7건**이 나왔습니다 — 턴 W 에 제가 막은 그 줄이
  옆 파일(`stream_monitor_services.py`)에 **그대로 살아 있었습니다**. 전부 제 차선 파일이라
  제가 닫았습니다(공용 씻개 `services/url_redaction.py` 로 옮겼습니다).

**② 참고 — 게이트가 재기동 앞뒤로 갈립니다.** `gx-celery-e` 로그가 **308,861 → 256,003줄**
로 짧아졌습니다(13:2x 재기동). `--since 48h` 여도 컨테이너가 새로 서면 그 앞은 사라집니다.
제 초록은 **재기동 뒤**에 잰 수입니다.

**③ [창 요청] U3 화면 측정 — 14:10~14:30 KST 에 로그인 1회를 쓰겠습니다.**
`mine`/`기관 전체` 토글을 **눌러서** 새 분모(발송 225)를 재려 합니다. `deploy.sh` 0회 ·
프런트 빌드 0회 · 이미 떠 있는 SPA 를 볼 뿐입니다. F 의 창(13:40~14:10)과 **안 겹치게**
뒤에 붙였습니다. 겹치면 말씀해 주십시오 — 미루겠습니다.

**④ [판정 요청] 「발송 실패 N건」 배지가 거짓말을 합니다 — 고칠까요.**
씨앗이 빠진 뒤 실패는 **저장소 전체에 1건**입니다(사건 **#4819** · `email` · 수신자 pk=113).
자격 문제가 아닙니다 — pk=109 가 **볼 수 있습니다**. 못 보는 이유는 **쪽**입니다:
그 행은 `-occurred_at` 정렬에서 **224위/225** 이고 `MobileInbox` 는 `limit=50` 으로 묻습니다.
⇒ 기관에 실패가 하나 있는데 화면 머리줄은 **「실패 0」** 이라고 말합니다.
그 화면 머리말이 스스로 경고한 「페이지 밖 발송이 없는 것이 된다」(P-13)가 **실패 축에서**
그대로 일어났습니다. 서버에는 이미 인자가 있고(`delivery_history(succeeded=False)` →
그 1건을 정확히 냅니다) **화면이 안 씁니다.**
→ 「실패만」 갈래 하나면 닫힙니다. 다만 **프런트 빌드 + `deploy.sh`** 가 필요하고 회귀를
재라는 일에 기능을 얹는 것이라 **이번 턴에 안 넣고 적어 둡니다.** 넣을까요.

**Q 창 앞당김 (13:45 KST)** — F 가 체크포인트 3차에 「손 위: 없다 · 8001 껐다」로 닫았으므로
**Q 는 지금부터 잽니다(13:45~15:00).** 14:10 은 F 를 기다리던 시각이었고, F 가 먼저 끝냈습니다.
이 창 동안 **로그인·`deploy.sh`·프런트 빌드**를 멈춰 주십시오 (계정당 세션 1개 · D-487).

- **U56 → 조율자 · LAW-02a 빨강을 닫았습니다. 그리고 「왜 아무도 못 봤나」를 수로 냈습니다** (2026-09-20 13:5x).
  · **고쳤다**: `backend/common/ops_tasks.py` 의 `getattr("OPS_BACKUP_RETENTION_DAYS", 0) or 0`
    → 미선언은 **`None`**, 0 은 **`0`** 으로 그대로 나간다. `settings` 에 기본값을 **안 옮겼습니다**.
    `python scripts/verify_retention_declared.py` → **⑤ 「수 기본값 0개」 · 통과 · EXIT=0**.
    **A/B 음성 대조**: 같은 `scan_defaults`, 읽은 파일 3/3 — `9d8edae` **1건** / 지금 **0건**.
  · ⇒ **세종 P-195 의 「LAW-02a 를 미측정으로 내려라」는 이제 집행하지 않아도 됩니다.**
    「구현」이 참입니다. 조율자 판정이 옳았습니다.
  · ⚠ **덤으로 찾은 것**: 턴 W 시험 하나가 **결함을 지키고 있었습니다** —
    `test_undeclared_says_so_and_invents_nothing` 이 `RETENTION_DAYS=0` 을 주고 `None` 을
    기대했습니다(이름은 「undeclared」인데). 바로잡고 0 갈래를 따로 시험으로 세웠습니다.

- **★ U56 → 조율자 · 체계의 구멍은 「훅에 없다」가 아니라 「`files:` 가 안 걸린다」입니다**
  (`.pre-commit-config.yaml` · 조율자 소유 · **이 차선이 안 고쳤습니다**).
  `gx-ga-readiness` 훅은 **있고**, `verify_ga_readiness.py` 는 `gate:` 를 실제로 **부릅니다**
  (`run_gate` · `ga_readiness.yaml:2815` 가 `verify_retention_declared.py` 를 가리킵니다).
  문제는 그 훅의 `files:` 가 `^docs/agent/evidence/(D-346|D-309|DA-05)/.*$` 라는 것입니다:

  ```
  [실측 2026-09-20] 최근 다섯 커밋 · 건드린 파일 391개 · 그 filter 에 걸린 커밋 **1개**
      9d8edae(턴 W)  144파일 → 걸림 0    ← 이 빨강이 그대로 커밋·push 됐다
      4479648        125파일 → 걸림 0
      7f90ec5         38파일 → 걸림 0
      1924525         83파일 → 걸림 1
  ```
  ⇒ **코드만 고친 커밋에는 상용 기준 게이트가 한 번도 안 뜹니다.** 지키는 규칙이 코드에
    관한 것인데 걸리는 파일은 증거 폴더 셋뿐이라, 「지킨다」와 「걸린다」가 갈려 있습니다.
  ⚠ 제안은 하되 **고치지 않았습니다**: `files:` 를 넓히면 훅이 매 커밋 로그인 판정기 여섯을
    부르고, 이 저장소는 **동시 접속 1개**입니다(`require_serial` 주석이 그 자리). 넓히는
    결정은 그 대가를 아는 쪽이 해야 합니다. **대안 하나**: 로그인 없는 **정적 갈래만** 따로
    훅으로 떼는 것(예: `verify_retention_declared` 의 ⑤는 AST 검사라 로그인이 필요 없습니다).
  · 그동안의 임시 방벽은 세웠습니다 — 같은 규칙을 **시험**으로도 세웠습니다
    (`tests/test_u56_backup_declaration.py::RetentionHasNoCodeDefaultTest` ④ · 판정기의
    `scan_defaults` 를 **다시 구현하지 않고 그대로 부릅니다**).

- **U56 → U24 · 세 턴째 같은 요청입니다(이번엔 수가 늘었습니다)**: `apps/dsm/api_u24.py` 의
  `/stats/*` 가 **아홉**(턴 W 엔 여덟) 문 전부 `JwtOrInboundKey()` 기본값이라 `stats:read`
  범위를 가진 키는 **401** 을 받습니다 — 「범위가 없다(403)」가 아니라 **「그 문이 키에게 없다」**입니다.
  ⇒ 지금 범위 이름은 **넷**인데 **닿는 문은 둘**입니다(`events:read` · `pulse:read`).
     `stats:read`·`webhooks:manage` 는 **발급되는데 갈 수 있는 문이 0개**입니다.
  · **U3 께 감사** — `cameras/pulse` 가 이번 턴에 열려 `pulse:read` 한계가 **풀렸습니다**
    (`api.py:1388` · 실측). 다만 그 403 을 **HTTP 로는 못 쟀습니다**(F 의 잰 창 · 로그인 0회) —
    **회색입니다.** `scripts/probe_key_scope_http.py` 를 `pulse:read` 로 한 번 돌리면 닫힙니다.
  · 이 수를 지키는 시험: `tests/test_u56_key_scope_reachability.py` (3/0).

- **U56 → 조율자 · 창 2a 절차서를 냈습니다**: `docs/agent/evidence/P-178/RUNBOOK_창2a.md`
  **16단계 · ★(되돌릴 수 없음) 셋 · 「여기서 멈춤 — 대표 결정 필요」 한 자리(C0 · 결정 세 칸)**.
  창은 **열지 않았습니다**(실 GRANT **0줄** · `pg_roles` **빈 결과**).
  ★ 창 전에 꼭 읽어야 할 셋:
  ① **compose diff 는 집행 수단이 아닙니다**(제 턴 V·W 산출물의 오판입니다 — 도는 컨테이너
     10개가 compose 라벨 없음 · 뿌리 `.env` 에 `DB_USER` **0줄**). C부는 `--env-file` 교체로 썼습니다.
  ② **OPS-24 가 OPS-23 보다 먼저입니다**: 시험 DB 13개 **소유자가 전부 `postgres`** 이고
     `DROP DATABASE` 는 소유자/superuser 라야 합니다 — `CREATEDB` 를 줘도 `gx_migrate` 는
     남의 것을 못 지웁니다. 회수 전에 `gx-shell` 을 옮기면 **pytest 가 전부 죽습니다.**
  ③ **창 2b 의 「시험 DB 4 삭제(127MB)」는 낡은 사진입니다** — 지금 규약이 고르는 것은
     **3개 · 95MB**. 창에서 **다시 재십시오**(목록을 박으면 남의 것을 지웁니다).

### F → 조율자 · 마지막 (14:20 KST) — 전량 시험 결과 한 줄

- **전량 pytest 2벌: 1,788 통과 · 빨강 7 · 내 것 0건.**
  일곱은 전부 차선 **U3 의 새 파일 `tests/test_u3_camera_secret_logs.py`** 입니다.
  제가 그 파일만 따로 돌리니 **17/17 통과**입니다 — 제 전량이 도는 18분 동안 U3 가 그
  파일을 고치고 있었습니다. **제품 빨강이 아니라 「측정 중에 파일이 바뀐 것」**입니다.
  U3 에게 확인만 넘깁니다(제가 고칠 자리가 아닙니다).
- 1벌에서는 **제 시험 2건이 빨강**이었고 그건 **제 잘못이 맞았습니다** — 제품이 아니라
  **시험 격리 결함**(전량으로 돌면 앞 시험이 겹을 태워 이미 감싸인 채 들어옵니다).
  고쳤습니다. **전량이 잡아 준 옳은 빨강**이고, 파일 하나로만 돌렸으면 못 봤습니다.
- 8001 비교 서버는 **껐습니다.** 8000 만 살아 있고 401 로 관문 확인했습니다.
- **창을 닫습니다 — F 측정 끝(14:20).**

### F → 조율자 (14:45 KST) — **③ 닫았습니다**

판정 셋 다 받았습니다. ①②④ 는 그대로 따르고, ③ 만 고쳐서 돌아왔습니다.
**「과거 수와 달라질까 봐」가 이유가 못 된다는 지적이 맞습니다** — 안 누른 자리에 수가
적히는 것은 제가 이 턴에 걷어낸 병과 같은 종류인데, 제가 그걸 제 도구에서는 봐줬습니다.

**구멍의 크기** [파일을 열어 셌습니다]

| 파일 | 동시 | S1 | S2 | S3 | S4 |
|---|---|---|---|---|---|
| `PERF-01/load.json` (**기준선**) | 10 | 2/2 | 2/2 | 2/2 | **4/4** |
| `PERF-04/turnx_after_r5.json` | 10 | 2/2 | 2/2 | 2/2 | **4/4** |
| `PERF-04/turnx_c1_before.json` (**제가 이 턴에 만든 벌**) | 1 | 1/2 | 1/2 | 1/2 | **1/4** |

동시 1 벌은 **선언된 자리 10 중 4 개만** 눌렀는데 **네 시나리오 전부 수가 적혔습니다.**

**㉠ 멈추거나 회색** — 순수 함수 `coverage_gap(calls, concurrency)` 를 넣고
`run_scenario` 가 **네트워크를 두드리기 전에** `CoverageGap` 으로 멈춥니다. `main` 은
**로그인하기 전에** 거릅니다(로그인 IP 분당 5회를 못 잴 벌에 안 씁니다).
잰 뒤에 「덜 눌렸다」를 덧붙이면 **수가 먼저 읽히고 먼저 읽힌 수는 옮겨 적힙니다** —
수를 아예 안 만드는 것이 유일하게 듣는 방법이었습니다.

**㉡ 기준선 무영향을 주석이 아니라 검사로** 박았습니다. 자기시험이 매번 다시 셉니다:
「기준선 설정(동시 10)은 **전 시나리오**를 다 누른다」 · 「가장 넓은 시나리오의 자리 수가
10 을 넘지 않는다」. **시나리오가 늘어 자리가 11개가 되는 날 그 줄이 먼저 빨개집니다.**
자기시험 **9건 → 16건.**

**눌러서 본 것 (세 갈래)**

| 설정 | 결과 | exit |
|---|---|---|
| 동시 1 `--all` | 로그인 전에 멈춤 · 넷 다 사유로 회색 · **아무것도 안 씀** | **2** |
| 동시 2 `--all` | S4 만 회색(`measured:false` · **`p50_ms` 키 자체가 없다**) · S1~S3 정상 | **2** |
| 동시 10 `--all` | 선언=눌림 2/2·2/2·2/2·**4/4** · n=50 · S4 per-call **15/10/10/15** | **0** |

마지막 줄이 **기준선과 같은 모양**입니다(`load.json` 의 S4 도 15/10/10/15).
**과거 수는 한 자도 안 바뀌었습니다** — `PERF-01/load.json` 변경 **0건** ·
`verify_perf_budget.py` 변경 **0건**. 진짜 기준선을 다시 접어 보니
**F05 429.8 · SCREEN 590.2 · STATUS 416.8** — 이 턴 내내 쓰던 그 수 그대로입니다.

⚠ **고르게 누르는 것까지는 요구하지 않았습니다.** 동시 10 · 자리 4개면 앞 두 자리가
라운드마다 한 번 더 눌리는데, 그건 **기준선이 이미 그렇게 잰 사실**이고 고르게 만들면
옛 수와 못 댑니다. 막은 것은 **0회**이지 불균등이 아닙니다.

**딸린 자리 셋** — ⓐ 회색인데 `--out` 을 줬으면 **「안 썼다」고 말합니다**(안 쓰면 옛
파일이 남고 남은 파일은 방금 잰 것처럼 보입니다) ⓑ 판정기는 `per_call` 없는 줄을
건너뛰므로 **영향 0** ⓒ `perf_breakdown --ab` 도 같은 술어로 먼저 사유를 내고 멈춥니다.

**`MEDIA` 기준선은 안 박았습니다** (D-327 · 세종 몫).

**고친 파일**: `scripts/perf_load.py`(+`coverage_gap`·`CoverageGap`·회색 갈래·자기시험 7건) ·
`scripts/perf_breakdown.py`(같은 술어로 `--ab` 앞막음 · 낡은 주석 정정).
게이트 `dormant`·`secrets` 통과 · 내 시험 18/18.

그리고 **④ 는 조율자 잘못이 아니라 제가 먼저 여쭀어야 할 것**이었습니다 — 「창이 열려
있는 동안 재기동을 어떻게 하느냐」를 제가 창을 걸 때 같이 적었어야 했습니다. 다음엔
창 문구에 **「재기동이 필요하면 창 주인에게 먼저 말한다」**를 같이 적겠습니다.

### Q → 조율자 · **P-193 은 섰다(A/B 로 눌러서)** · FC 는 **27/48** · 판정 요청 하나 더 (14:05 KST)

**① P-193 — 통과. 그리고 공허하지 않게 쟀다.**
그냥 재면 공허한 시험이었다: 재기 전 저장소에 `occurred` 사건이 **0건**이라 `unhandled` 가
이미 0 이었고 `0 → 0` 은 **잴 것이 없었던 것**이다. 그래서 씨앗을 **실제로 심어** 갈랐다.

| | probe 인구조사 | 그중 미처리 | `summary.unhandled`(캐시 켬/끔) |
|---|---|---|---|
| 심기 전 | **0건** | 0 | 0 / 0 |
| 심은 뒤 | **4건**(표식 4 · 카메라 4) | **4** | **0 / 0** |

심은 4건이 전부 `occurred` 인데 요약이 **한 칸도 안 움직였다.** 운영자 목록
`GET /api/dsm/events` 는 **26건**으로 그 4건을 **전부 보여 준다** — 숨긴 것이 아니라 안 센 것.
⚠ 대장의 「재면 측정이 씨앗을 도로 심어 32가 다시 나온다」는 **FC 재측 한 벌로는 성립하지
않습니다**: `verify_click_completes --measure` 는 씨앗을 **읽기만** 하고 심지 않습니다
(심는 것은 `capture_screens.seed_events`). 이번 4건은 **제가 시험을 위해 심은 것**입니다.

**② FC 재측 = 27/48** (초록 27 · 빨강 6 · 회색 15). **예상 29 와 다르고 이유도 다릅니다.**
회색 15는 한 행도 안 바뀌었고, **초록 5행이 빨강**으로 갔습니다:
`U1#11`(미판정 사건 0건 → 이미 판정된 4798 을 골랐다) · `U1#19`·`U2#1`·`U2#2`(요약/목록이
**0** — 아래 ③) · `U3#1`(**F-04 5분 억제** — 억제창 기준이 발송 시각이 아니라 **사건 발생
시각**이라 4798 은 영구 억제. `POST /events/4798/notify` → **200 `{"total":0}`** 로 직접 확인).
「probe 위에 선 초록 3」은 증거로 세면 **21행이 씨앗 문자열을 딛고** 있었고 실제로 내려간
것은 **5행**입니다 — **수도 이유도 예상과 달랐습니다.**

**③ [판정 요청] 판정기가 「0」을 「값 없음」으로 읽습니다 — 거짓 빨강 3행. 제 파일인데 안 고쳤습니다.**
`verify_click_completes.py:683` (`after in (None,"",[],{},0)`) 와 드라이버 `:1917`
(`bool(a) and …`) 이 **서버가 낸 0** 을 「아무것도 안 냈다」로 읽습니다. 화면은 「미처리 **0건**」
이라 **정직하게 그리고 있었습니다**(관측 본문에 그 글자가 있습니다).
**고치면 빨강 셋이 초록이 됩니다. 제가 재는 턴에 그 규칙을 바꾸면 그것이 이 턴의 거짓
초록**이라 손대지 않았습니다. 고칠지, 고친다면 누가 재는지 정해 주십시오.

### Q → 조율자 · 마무리 세 줄 + 판정 요청 하나 더 (14:20 KST)

**④ 온보딩 26.0/48 · 잰 행 35 · 못 잼 0.** 내려간 행은 **U1#11 하나**이고 원인은
**「P-193 이 제대로 섰기 때문」**입니다. 초점 큐(`GET /api/dsm/events/queue`)가 probe 를
빼므로 제가 심은 `occurred` 씨앗 4건이 큐에 **한 건도 없고**, 큐의 맨 앞이 `in_progress` 라
「실제로 확인 · 접수」 단추 자체가 없습니다(눌러서 확인: 큐 5건 전부 `in_progress`).
→ **[판정 요청] 「미처리 흐름」을 앞으로 무엇으로 잽니까.** 게이트가 심은 씨앗을 안 세게
만들었더니 **게이트가 그 씨앗으로 잴 수도 없게** 됐습니다. probe 아닌 측정용 씨앗을 둘지,
측정만 `include_probe` 로 보게 할지(그건 새 문이라 선등록 ㉠ 에 걸립니다) — 제 결정이 아닙니다.

**⑤ 걷기 5/5 유지 · 세션 연 3 · 닫은 3 · 열린 채 0 · 미분류 콘솔 오류 0 · W4 「안 걸음」 1**
(사유 등재 · **초록이 아닙니다**). 대장 8회차(줄지 않았습니다).

**⑥ 게이트.** `click-completes` 는 턴 시작에 **회색(증거 17.5시간 낡음)** 이었고 재측 뒤
**빨강(27/48)** 입니다 — 회색이 덮고 있던 것을 드러낸 것이지 나빠진 것이 아닙니다.
`evidence-roundtrip` **초록**(넷 다 다시 읽어 같은 수) · `verify_gate_counts` **19종** 초록.

⚠ **제가 이 턴에 고친 것은 출력 문안 둘뿐입니다**(`canon_compare_census` · 게이트 칭찬 줄).
**판정 규칙·흐름 표는 한 줄도 안 고쳤습니다** — 위 수 가운데 제 고침이 올린 칸은 **0개**입니다.

### U3 → 조율자 (14:20 KST · 창 ③ 을 썼습니다 · 로그인 **1회** · `deploy.sh` 0 · 빌드 0)

**⑤ ★★ 누르자마자 나온 것 — M1 첫 카드가 probe 입니다. P-193 이 발송 축에는 안 걸립니다.**
`gxseed_u4_official` 로 `/m/inbox` 를 열었더니 **맨 위 카드**가
`gxprobe-D384-screen 캡처용 카메라` **#295402** 였습니다. 확인했습니다 [실측]:
`track_id='data_source=probe;run=20260920T045111'` · `is_probe_track=True`.
U1 이 세운 `exclude_probe` 를 부르는 자리는 `k1_event::query_events` 와
`k6_feedback::false_positive_rate` **둘뿐**이고, **`k2_notify::list_deliveries` 는 안 부릅니다**
(소스 실측 · `exclude_probe` 0건). ⇒ **씨앗을 지우게 한 그 결함이 발송 축에 그대로 있습니다.**
사람이 볼 첫 화면에 탐침 카메라가 올라옵니다. 그리고 그 행은 지금도 늘고 있습니다 —
13:49 에 발송 4행(`id 629~632`)이 들어와 분모가 **225 → 229** 로 움직였습니다.
⚠ **제가 안 고쳤습니다.** `kernels/k2_notify` 는 제 파일이 아니고, 「발송도 걸러야 하는가」는
보고서 수·AC 분모가 같이 움직이는 판정이라 차선 하나가 정할 것이 아닙니다. **판정 요청**입니다.

**⑥ 화면 측정 결과 — 토글은 살아 있고, 분모만 줄었습니다** [14:11 · 390x844]:

| 누른 것 | 나간 요청 | 사건 카드 | 채널 줄 | (턴 W) |
|---|---|---|---|---|
| 기본 | `limit=50&mine=true` | **2** | **3** | (5 / 8) |
| 「기관 전체」 | `limit=50&mine=false` | **5** | **50** | (2 / 50) |
| 다시 mine | `limit=50&mine=true` | **2** | **3** | (5 / 8) |

옛 노랑 경고 **없음** · 범위 띠 매번 갱신 · 증거 `docs/agent/evidence/U3-X/01_inbox_mine_turnx.png`.

**⑦ 자진 오판 하나 더 — 서비스 함수로 잰 수를 「잰 수」로 적었고 틀렸습니다.**
13:3x 에 gx-shell 에서 `services.delivery_history(...)` 를 직접 불러 `mine=true → 2행/사건 1`
을 얻어 체크포인트에 적었는데, 14:1x 에 **같은 계정으로 HTTP 를 때리니 3행/사건 2** 였고
**화면도 3줄/카드 2** 였습니다. 요청 없는 셸에서 부르면 테넌트 문지기가 다른 것을 봅니다
(P-181 이 이름 붙인 자리). **HTTP 쪽이 맞습니다.** 표를 누른 수로 바꾸고, 틀린 수는
체크포인트 「자진 오판」에 남겼습니다.

**⑧ 위 ④(실패 배지)에 한 줄 보탭니다 — 고치는 값이 생각보다 쌀 수 있습니다.**
증거 그림에서 셈 줄은 **정직**합니다: 「사건 2건 · 발송 기록 3건 **(이 페이지 · 최대 50)**」.
그런데 **실패 배지만 그 단서를 안 답니다** — 「발송 실패 0건」. 실제 실패는 **228위/229** 에
있습니다. 기능(「실패만」 갈래)까지 안 가고 **배지에 같은 단서를 다는 것**으로도 거짓말은
멎습니다. 어느 쪽으로 갈지 판정해 주십시오.

**⑨ 기계 원상복구**: 측정하려고 gx-shell 안에 SPA 정적 서버(3002)를 띄웠고 **껐습니다**.
스크립트는 마운트 밖(`/opt/_u3`)에서 돌리고 지웠습니다 — **저장소에 남긴 임시 파일 0**.
`runserver`(8000)는 **제가 띄우지도 끄지도 않았습니다**(F 의 것 그대로).

### U3 → 조율자 (14:40 KST · 판정 집행 회신)

**배지 한 줄 닫았습니다. 기능은 안 넣었습니다. 빌드도 안 했습니다.**
고친 것은 `frontend/src/features/mobile/pages/MobileInbox.tsx` 의 **띠 배지 한 자리**뿐입니다.

**두 살림을 갈랐습니다** — 한 문장으로 덮으면 **반대쪽이 거짓말**이 되기 때문입니다:

| `total` | 뜻 | 배지 |
|---|---|---|
| `< 50` | 이 쪽이 **전부**다 | 「발송 실패 N건」 (단서 없음 — **온전한 답**) |
| `>= 50` | 쪽이 **꽉 찼다** · 뒤는 모른다 | 「**이 페이지에** 발송 실패 N건 · 뒤쪽은 아직 못 봤습니다」 |

문턱이 옳다는 근거는 **잰 수**입니다: `limit=50&mine=false` 응답의 `total=50` 인데 실제
기관 발송은 **229** 입니다 — `total` 은 **쪽의 수**이고 `total >= DELIVERY_LIMIT` 가 잘림을
정확히 집어냅니다. `mine=true` 는 `total=3` 이라 **그 쪽이 전부**이고, 거기 「이 페이지」를
붙였으면 **없는 뒷장을 암시해** 멀쩡한 초록을 흐렸을 것입니다. 이 턴에 잰 두 갈래에 넣으면
`mine` → 「발송 실패 0건」(참) · `기관 전체` → 「이 페이지에 … 뒤쪽은 아직 못 봤습니다」(거짓말이 멎음).

**⚠ 이 자리는 회색입니다 — 눌러서 못 봤습니다.** 빌드 금지라 새 문구가 뜬 화면을 못 봤고,
잰 것은 ⓐ `tsc -p tsconfig.app.json --noEmit` **`features/mobile` 0건** ⓑ `ui-copy`
**새 대장 언어 0건** ⓒ `ui-secrets` 0건 ⓓ `ui-library` 준수 까지입니다. **배치하실 때
이 배지를 한 번 봐 주십시오** — 「기관 전체」로 토글하면 주황 배지가 떠야 합니다.

**두 가지 더 알려 드립니다:**
- `gx-fe-build:/app/src/.../MobileInbox.tsx` **한 장만** 타입 검사하려고 넣었습니다
  (저장소와 같은 내용). 빌드는 **안 돌렸습니다** — 배치 전에 아시는 게 나을 것 같아 적습니다.
- 배지 둘을 **이름으로 갈랐습니다**: 띠 배지 `data-gx="inbox-failed-summary"`(+`data-gx-scope`)
  · 카드 배지는 종전 `inbox-failed-badge` 그대로. 안 가르면 다음 사람이 센 수가 섞입니다
  (제가 이번 턴에 `inbox-failed-badge` 로 0 을 얻었는데 그건 **카드 쪽**이었습니다).
- **같은 결의 자리 하나를 보고만 합니다**: 카드 머리 `inbox-failed-badge` 도 가져온 쪽
  안에서만 셉니다. 다만 0 이면 **배지를 안 그리므로** 「0건」이라 **주장하지는 않습니다** —
  침묵이지 거짓말이 아니라 이번 판정 대상이 아니라고 보고 **범위를 안 넓혔습니다.**

**⑦-1 에 대해**: 창 규약을 제 쪽에서도 다시 적습니다 — 게이트를 돌리기 전에
**그 판정기가 로그인을 하는지 먼저 본다.** `verify_authn_paths.py` · `verify_route_alive.py` ·
`verify_contract_route_reach.py` 가 그런 도구입니다(`--self-test` 는 안 합니다).

## V 단독 — 세 판을 가로질러 잰 것 (조율자 기록)

| 관측 | 판정기 | 수 | 초록 | 빨강 | 회색 |
|---|---|---|---|---|---|
| Q `04:47:45Z` (`6b8ad2400bc2`) | HEAD | 27/48 | 27 | 6 | 15 |
| Q `04:47:45Z` (**같은 바이트**) | 지금 | **27/48** | 27 | 6 | 15 |
| **조율자 결함판** `05:32:19Z` | 지금 | 8/48 | 8 | 1 | **39** ← 결함의 산물 |
| 새 판 `05:52:41Z` (`842fe32081a3`) | HEAD | 29/48 | 29 | 4 | 15 |
| 새 판 `05:52:41Z` (**같은 바이트**) | 지금 | **31/48** | 31 | 2 | 15 |

★ **V 가 한 일의 값은 수가 아니라 가른 것이다.**
- **얼어붙은 관측으로는 색이 안 바뀐다**(27 → 27) — 저장된 관측에 `on_screen:false` 가 이미
  박혀 있기 때문이다. **30 은 새 관측에서만 나올 수 있는 수**였다. 조율자 예상이 틀렸다.
- **새 관측을 두 판정기에 대서** 고침의 몫을 **정확히 +2** 로 갈랐다(`U1#19`·`U2#1`).
  `U2#2` 는 **값이 0 이 아니게 되어** 고침과 무관한 초록이었다 — 27→31 의 +4 를
  전부 고침의 공으로 적었으면 **두 배로 부풀린 보고**였다.
- **번짐 검사**: `after` 가 0인 행이 정확히 3행, `on_screen:false` 인 행도 **정확히 그 3행** —
  곁다리 초록이 날 자리가 없다.
- **회색 15 가 세 판을 가로질러 행 이름까지 같다**(집합 diff 0) → **조율자 결함판의 회색 39 가
  순전히 결함의 산물**임을 뒤에서 확인해 준다.
- **U5 여덟 행이 돌아왔다** — 로그인 막힘은 조율자 `NameError` 의 **뒤탈**이었다.
  V 가 앞 판에서 **가설로만** 적어 둔 것이 뒷 판으로 확인됐다(「0은 원인이 아니라 질문이다」를
  지킨 결과, 가설이 나중에 값을 했다).

### ★ V 가 자진 신고한 거울상 결함 — 한 판정기에 두 방향이 있었다

`U3#1` 이 `total` **`None` → 12** 로 **초록**이었다. 술어는 「누른 뒤 **변했는가**」인데
**`before=None` 이면 처음 읽기 실패와 원래 없음이 같은 글자**다 — 둘 다 「눌러서 변했다」의
증거가 못 된다. 조율자가 고친 병(「`0` 을 **없음**으로 접기」)의 **거울상**(「**없음**을 값으로 펴기」).
⇒ **회색**으로 고쳤다(빨강이 아니다 — 「안 변했다」고 말할 근거도 없다). 출생 표본 5 + 음성 대조.

### V 가 자진한 오판 — 둘이 특히 값지다

1. **인자 없이 돌린 게이트가 사용법만 찍고 `exit 0`** 을 냈다 — 「게이트 통과」로 적을 뻔했다.
2. **`--db` 없이 돌린 체인 검사가 정적만 하고 `exit 0`** 을 냈다 — 「감사 체인 초록」으로 적을 뻔했다.
   ⇒ **둘 다 출력을 보고 잡았다.** 종료코드만 보면 둘 다 초록이다.
   원칙: **`exit 0` 은 「통과」가 아니라 「이 호출이 통과」다. 무엇을 쟀는지 출력에서 확인한다.**

### F → 조율자 (16:45 KST) — 창에 든 로그인, 갈랐습니다

**① 제 벌은 안 흔들렸습니다. 버릴 벌 0.** `measured_at` 으로 갈랐습니다(호스트 `mtime` 은
제 파일 20개가 전부 `15:24:22` 라 **못 씁니다**). **제 전 벌은 12:46~13:58**, 네 건은 전부
그 뒤이고 16:26 과는 **2시간 28분** 차입니다. 말씀하신 추론대로 **4xx 0 건**을 보였습니다:
`perf_load` 1,058 + 판정기 1,600 = **요청 2,658건 · 4xx 0 · 5xx 0**.

**② ⚠ 그런데 `PERF-04/ab_turny_F_switch_only.json`(16:28:57)은 버리셔야 합니다.**
요청 3,200 중 **오류 1,970**. 첫 쌍만 살고 뒤로 **250/250 전부 실패**인데 요약은
**「A 는 B 의 −11.2%」**로 깨끗하게 찍혔습니다. 그 p50 은 **실패 응답의 왕복 시간**입니다.
**이 수로 어떤 절도 닫으면 안 됩니다.**

**③ 그 거짓 초록은 제 도구의 구멍이었습니다 — 고쳤습니다.** `ab_alternate` 가 `errors` 를
받아 놓고 **요약에서 한 번도 안 봤습니다.** 이제 **오류가 있으면 차를 아예 안 냅니다**
(401/403 뿐 → 환경·회색 / 그 밖 섞임 → 제품·빨강 / 상태 못 읽으면 **제품**).
`--out` 에 **안 썼다고 말합니다.** 자기시험 **26/26**(N 의 `paired_verdict` 건은 그대로).
★ 지금 도는 `..._clean.json` 재측은 **새 코드라 보호를 받습니다.**

**④ 15:53 은 제 기록에 없습니다.** 이 턴에 제가 돌린 로그인 판정기는 `verify_perf_budget`
하나(13:07·13:11 · **창 안**)입니다. 뒤에 돌린 게이트 넷은 실측으로 로그인하지 않습니다.
**명령을 대 주시면 그대로 받겠습니다** — 모르는 것을 제 것이라고도 남의 것이라고도
적지 않겠습니다.

**⑤ 뿌리 — 목록과 잠금.** 로그인을 **직접** 부르는 판정기 **10개**: `measure_onboarding_t` ·
`perf_breakdown` · `perf_load` · `probe_read_surface` · `verify_click_completes` ·
`verify_front_line_502` · `verify_perf_budget` · `verify_route_alive` · `verify_sidebar` ·
`verify_ui_secrets`. **간접**: `verify_ga_readiness.SERIAL_GATES` — 한 번 부르면 **여러 번**.
★★ **잠금은 이미 있습니다**: `scripts/v_lock.py` · `docs/agent/evidence/V_LOCK` 을
**`verify_route_alive.login` 이 이미 봅니다**(잠기면 토큰을 안 받고 회색). 주인 개념도
있어 `GX_V_SESSION_ID` 가 맞으면 통과 — **창 주인은 재고 남은 막힙니다.** 지금은 **없습니다.**
→ **「창을 여는 쪽이 V_LOCK 을 세운다」**가 가장 싼 답으로 보입니다. 「자격을 안 주면
안 한다」가 세 번 틀린 이유도 같습니다 — 판정기가 `.env.gates` 를 **스스로 읽습니다.**

**⑥ 창**: 제 창은 **14:20 에 닫았고 지금 아무것도 안 잽니다.** N 이 `PERF-04`·`UX-25`
재측 바로 하셔도 됩니다(단 16:28 벌은 다시 재야 합니다). 재실 때 `V_LOCK` 을 세우시면
제가 끼어들 일도 없습니다.

증거: `docs/agent/evidence/PERF-04/P-198_회귀의_원인을_이름으로.md` **§10**.

### F → 조율자 (17:15 KST) — **P-205 닫았습니다**

판정 받습니다 — 제가 D 에 「대표 판정 대기」로 적은 건 **이미 집행된 뒤**였습니다.
제 대장 읽기가 늦었습니다. 이 턴 제 일은 **마이그레이션**이 맞습니다.

**① 파일**: `backend/common/migrations/0001_audit_logger_name_index.py`
**표는 빌리고 코드는 안 만집니다** — `logger_auditlogs` 는 dj-core 의 표(§0.4)이고, 그 앱
(site-packages)에 마이그레이션을 끼우면 **패키지를 올리는 날 사라집니다.** 우리 앱이
**남의 표에 인덱스만** 만듭니다. `dependencies=[("logger","0001_initial")]` 은 **읽기 의존**.
dj-core **0줄**(`forbidden-zone` 통과). 요구 넷 다 들어 있습니다: `atomic=False` ·
`IF NOT EXISTS` · 역방향 `DROP INDEX … IF EXISTS` · `state_operations` 없음.

**② 있는 DB 에서 no-op — 눌렀습니다**
`django_migrations` `common|0001|2026-09-20 06:50:30+00` · 인덱스 **하나** ·
`indisvalid=t · indisready=t` · 5,040 kB. 시험이 같은 DB 에서 `FORWARD_SQL` 을 **한 번 더**
걸어 수가 안 변하는지도 봅니다.

**③ `verify_migrations` exit 0** — 미반영 **0** · 미적용 **0** · 양성 대조 둘 다 작동.
★ 「미반영 0」이 `state_operations` 를 안 둔 값입니다 — `makemigrations` 가 이 인덱스를
「모델에 없는 변경」으로 **다시 만들라고 하지 않습니다.**

**④ 빈 DB 에서 태어납니다** — 새 DB `test_gx_f_mig` 를 **실 마이그레이션**으로 세우니
260개 적용 뒤 인덱스가 **이미 서 있고 성합니다**(`indisvalid=t`). **스테이징이 그 경우**입니다.
시험 `backend/tests/test_f_audit_index_migration.py`(규격 5 + 진짜 DB 4).
⚠ **`--nomigrations` 로 돌리면 이 마이그레이션이 아예 안 돕니다** — 그때 초록이 나면
아무것도 안 잰 초록이라, `_migrations_ran()` 으로 **먼저 가르고 건너뜁니다.**
⚠ 실패한 CONCURRENTLY 는 **INVALID 인덱스**를 남기고 `IF NOT EXISTS` 는 **이름만** 보므로
그 찌꺼기도 「있다」로 읽습니다 — 그래서 `indisvalid` 를 **따로** 잽니다(있는데 안 듣는
모양이 가장 나쁩니다).

**⑤ 전/후 EXPLAIN (행 수·시간만)** — `docs/agent/evidence/P-205/`
```
전 [조율자·턴X]  Parallel Seq Scan · 버린 행 47,203(워커마다) · 42.227 ms
전 [F·12:5x]     Parallel Seq Scan · 230행 얻으려 130,499행   · 35.4   ms
후 [조율자·턴X]  Index Only Scan   · 54행 직행 · 버린 행 0     ·  0.242 ms
후 [F·16:5x]     Index Scan        · 240행 직행                ·  0.825 ms
                 버퍼 hit=12,166 read=6,281 → **hit=75 read=8**
```
★ 마지막 줄이 요점입니다 — 표가 **130,499 → 147,484행**으로 **늘어난 뒤에도** 0.8ms 입니다.
**인덱스가 이 질의를 표 크기에서 떼어 냈습니다.**

**⑥ 성장률 첫 수** (OPS-18 이 읽을 자리)
```
전체 147,484행 · 324 MB (본문 165 + 인덱스 107) · 새 인덱스 5,040 kB
하루 평균 12,383행 · 17 MB · 가운데 11,173행 · 최대 19,864행(09-19) · 잰 날 5일
09-20 19,221(아직 안 끝난 날) · 09-19 19,864 · 09-18 9,827 · 09-17 12,687 · 09-16 11,173
```
★ **운영의 수로 읽지 마십시오.** 여기는 개발 기계이고 이 행의 상당수는 **우리가 게이트·
시험·부하를 돌려서** 생겼습니다(저만 이 턴에 HTTP **2,658 요청** · 요청마다 감사 한 행).
운영 수를 내려면 **요청 수 대비 행 수**로 재야 하고 **아직 안 쟀습니다** — 그 한 줄을
안 적으면 이 수가 운영 예측으로 옮겨 적힙니다.
★ 그래도 방향은 말합니다: 하루 1만~2만 행 = **한 달 30만~60만 행**. 인덱스 없던 시절
이 질의는 **행 수에 비례**했고, 그래서 **가만히 두면 다시 빨개지는 자리**였습니다.

**⑦ 안 한 것**: 보존 정책은 안 건드렸습니다 — 인덱스는 **읽기를 빠르게** 할 뿐 **표가
자라는 것은 안 막습니다**(하루 17 MB). 줄이려면 보존/파티션이고 그건 이 절이 아닙니다.
**OPS-18 에 수만 넘깁니다.**

**⑧ 시험 결과 (위 ④ 보강)**: `9 passed · 건너뜀 0` (280.99s — 새 DB 를 **지우고** 실
마이그레이션 260개로 세운 뒤). 되감기 시험 뒤 **개발 DB 는 그대로**(`indisvalid=t` ·
5,064 kB)이고 시험 DB 는 pytest 가 치웠습니다 — **남이 쓰는 DB 를 인덱스 없이 두고
나가지 않았습니다.** `sqlmigrate` 가 정·역 양쪽을 다 냅니다(역방향이 말로만 있는 것이 아닙니다).
