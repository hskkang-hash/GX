# U56 → 조율자 (턴 AD · 2026-09-22 저녁)

**이것 하나만 · 끝까지 — 끝났다.** P-238 집행(표 밖 역할 8명 처리) + P-239 확인(코드 정본
그대로 서 있음, 안 건드림) + 인수 6종 최소 메뉴(RoleNavFilter.tsx) + U5#2 회귀 시험 승격.
넓히지 않았다 — U5#15·U6#3·U6#4 등 §3 표 밖 다른 항목은 손 안 댐(이번 지시서 프롬프트에
없다).

## ① 실측(명령 + 결과)

```
[역할 전수 16종 — 턴 AB 와 같다. 재확인]
MSYS_NO_PATHCONV=1 docker exec -e DJANGO_SETTINGS_MODULE=config.settings gx-shell python -c "...Role.objects.all()..."
→ 16종. 표에 있는 것 7(fire_user·fire_admin·operator·surveillance_operation·
  surveillance_order·view_only_-_anyang·admin) · 표 밖 9(superuser·user·order·
  tenant_admin_4·delivery_operation·delivery_order·delivery_admin·drone_robot_user·
  drone_robot_admin) — roleNav.ts ⑤ 머리말과 한 자도 안 틀린다.

[테넌트 재측 — UserProfileLink.group 로 조인. Role.group 은 테넌트가 아니라 역할이
 "정의된" 스코프(예: fire_user·fire_admin 의 role.group=Anyang)라 사람의 테넌트가 아니다
 — 처음에 이걸로 헷갈릴 뻔했다. 실사용자 테넌트는 UserProfileLink.group 이 정본이다.]

GX 테넌트(ETRI-Group) · is_active=True · deleted IS NULL · 역할이 표 밖뿐인 계정:
  14 order_user · 17 order_chi · 18 order_user_1 · 19 order_user_2 · 20 order_user_3 ·
  21 order_user_4 · 22 order_user_5 · 63 order_chungnam   = 정확히 8명 (턴 AB 와 일치)

  ⚠ is_active 만 보면 9번째로 58 order_chungnam_001 이 잡힌다 — deleted 값이
    2025-09-18 06:02(소프트삭제)이라 "살아 있는 계정"이 아니다. deleted 도 걸러야
    턴 AB 의 8과 맞는다. (아래 ⑦)
  ⚠ GeumSan-ETRI 에 102 order_geumsan(role=order) 1명 더 있다 — ETRI-Group 이
    아니라서 턴 AB 도 이 턴도 "8"에 안 넣었다. GeumSan-ETRI 는 role='operator'(GX
    표 코드) 보유자도 있어(GX 계열일 가능성) 판정을 안 내리고 남겨 뒀다(§7·§8).
  ⚠ 112 gxprobe_q(role=tenant_admin_4·user) — Q 차선 게이트 탐침 계정(2026-09-05
    생성), 실사용자가 아니라서 뺐다.

[집행 — order → fire_user, 8명]
docker exec ... python -c "... u.roles.remove(order); u.roles.add(fire_user) ..." (transaction.atomic)
→ 8/8 성공. commit ok.

[독립 재조회로 검증 — 같은 세션이 아니라 새 python 프로세스로 다시 읽음]
14 order_user ['fire_user'] · 17 order_chi ['fire_user'] · 18 order_user_1 ['fire_user'] ·
19 order_user_2 ['fire_user'] · 20 order_user_3 ['fire_user'] · 21 order_user_4 ['fire_user'] ·
22 order_user_5 ['fire_user'] · 63 order_chungnam ['fire_user']
role('order').users == ['order_chungnam_001', 'order_geumsan']  (미처리·제외분만 남음)
role('fire_user').users 에 8명 신규 등재(기존 fire_user1·fire_user2·firedrone01·02·
  gxseed_u1_operator·gxseed_u5_newop·hansF·ldkF 와 나란히)

[영어 사이드바 — 인수 6종 역할별 실제 RoleMenu(permit_read=True) 전수 · 한글 유무로 분류]
order                  granted=4  → hideAcquired 뒤 영어 줄 4  → 이번 코드 뒤 0
delivery_operation     granted=33 → 영어 줄 30 → 0
delivery_order         granted=5  → 영어 줄 5  → 0
delivery_admin         granted=38 → 영어 줄 32 → 0
drone_robot_user       granted=30 → 영어 줄 25 → 0
drone_robot_admin      granted=30 → 영어 줄 25 → 0
  합계 121 → 0 (정지 상태 · 어떤 화면에도 안 서 있을 때)

[실사용자 최종 집계 — 8명 재배정 뒤]
is_active=True · deleted IS NULL · 표 밖 뿐인 살아있는 계정 = 63
  ├ 인수 6종뿐(이번 최소메뉴 코드 적용 대상)      = 43명
  ├ gxprobe_*(게이트 탐침 계정 · 범위 밖)          = 6명
  └ 그 외(superuser·user 단독/혼합 · 이번 범위 밖) = 14명 — 기존 hideAcquired 그대로 둠

[순수 함수 회귀 시험]
node scripts/repro_u5_2_roles_zero_rows.mjs
→ §1~6(U5#2 재현+고침, 턴 AB 원본) 전부 PASS · §7~8(신설 — 인수 6종 판정·최소 메뉴)
  전부 PASS · rc 0 (총 실패 0)

[타입검사 — gx-fe-build 격리 복사(원본 /app 무손상) · A/B]
  ① /app 그대로(베이스라인)         tsc -p tsconfig.app.json → 3234줄
  ② 내 앞판 2파일만 덮어씀           tsc -p tsconfig.app.json → 3234줄
  diff(①,②) = 0줄 · 내 파일이 낸 오류 0
```

## ② 고친 파일 : 줄

- `frontend/src/features/nav/RoleNavFilter.tsx`
  - 29·31~40 — `acquiredOnlyRoles` 모듈 import·머리말 갱신(정본 위치 설명)
  - 70~92 — 새 갈래: `bucketOf` 가 `null` 이고 `isAcquiredOnlyRoles(codes)` 가 참이면
    **빈 목록 + `withCurrentPath`** 만 쓴다(P-238 최소 메뉴). 그 아래 기존
    `hideAcquired` 갈래(P-220)는 그대로 두되 이제 인수 6종은 여기 안 온다(주석만 갱신).
- `frontend/src/features/nav/acquiredOnlyRoles.ts` (신설)
  - `ACQUIRED_ONLY_ROLE_CODES`(인수 6종 정본 집합) · `isAcquiredOnlyRoles()`(순수 함수).
    `RoleNavFilter.tsx` 가 아니라 **JSX 없는 `.ts` 로 따로 둔 이유**는 노드가 `.tsx`
    (react·rj-core import 있음)를 형 벗기기로 못 읽어서다 — 회귀 시험이 이 파일을
    직접 부른다. 정본은 여기 하나뿐(`RoleNavFilter.tsx` 는 재수출만).
- `scripts/repro_u5_2_roles_zero_rows.mjs`
  - 2~4 머리말에 "회귀 시험으로 승격" 한 줄 · import 에 `acquiredOnlyRoles.ts` 추가
  - 파일 끝 §7(인수 6종 판정 9건) · §8(최소 메뉴 4건) 신설 — 전부 PASS
- `docs/agent/evidence/P-238/reassignment_before_after.json` (신설) — 8명 전/후 표 +
  되돌리는 법 + 미처리 3명(58·102·112) 사유
- `docs/agent/evidence/P-238/english_sidebar_before_after.json` (신설) — 역할별 영어
  줄 전/후 · 방법 · 못 잰 것(브라우저 미실측) 명시
- **`roleNav.ts` · `product_menus.py` · `menu` 는 0줄** — U24 소유, 안 건드렸다(확인:
  `git status`에 안 뜸).

## ③ 8명 처리 표 — GX 테넌트(ETRI-Group) · order → fire_user

| id | username | 테넌트 | 전 | 후 | 되돌리는 법 |
|---|---|---|---|---|---|
| 14 | order_user | ETRI-Group | order | **fire_user** | `u.roles.remove(fire_user); u.roles.add(order)` |
| 17 | order_chi | ETRI-Group | order | **fire_user** | 〃 |
| 18 | order_user_1 | ETRI-Group | order | **fire_user** | 〃 |
| 19 | order_user_2 | ETRI-Group | order | **fire_user** | 〃 |
| 20 | order_user_3 | ETRI-Group | order | **fire_user** | 〃 |
| 21 | order_user_4 | ETRI-Group | order | **fire_user** | 〃 |
| 22 | order_user_5 | ETRI-Group | order | **fire_user** | 〃 |
| 63 | order_chungnam | ETRI-Group | order | **fire_user** | 〃 |

삭제 0 · 비활성화 0 · 행 자체는 안 건드렸다(M2M 역할 연결만 바꿈). 전부 되돌릴 수 있다.
전/후·되돌리는 법 원본: `docs/agent/evidence/P-238/reassignment_before_after.json`.

**인수(외부) 테넌트 — 손 안 댐, `RoleNavFilter` 최소 메뉴로만 처리**: Anyang·Gaion·
Thailand·Gongju·DRONE-ROBOT 등의 delivery_*·drone_robot_* 실사용자 **43명**(§①).
사람도 행도 역할도 0줄 안 바뀌었다 — 사이드바만 코드로 줄였다.

## ④ 영어 사이드바 N(전/후)

역할별 표는 ①에 있다. 요약: **정지 상태 기준 121 → 0**(인수 6종 실사용자 43명이 보는
화면). GX 8명은 `fire_user` 로 넘어가 U1 표(전부 한글 5줄)를 탄다 — 거기도 0.
⚠ **서 있는 화면 자체는 예외다** — `withCurrentPath` 가 지금 주소 줄 하나를 되돌리고,
그 줄은 대개 영어 이름이다(예: `Delivery Dashboard`). "정지 상태(사이드바를 펼쳤을 때)"
가 0 이라는 뜻이지, 어느 순간에도 영어 글자가 화면에 0 이라는 뜻은 아니다 — 이 차이를
숨기지 않는다(roleNav.ts ④ 와 같은 규율).
⚠ **브라우저로 실측 못 했다.** 로그인 IP 5회/분·계정 전환 61초 제약과 다른 차선의 동시
로그인을 피하려고 ORM(서버가 실제로 주는 RoleMenu 전수)으로만 쟀다 — 이것은 "서버가
줄 수 있는 것의 상한"이고 실제 화면 값과 같거나 더 크다(작을 수 없다). 초록이 아니라
"쟀다·상한"으로 적는다.

## ⑤ U5#2 회귀 시험

`scripts/repro_u5_2_roles_zero_rows.mjs` 를 **회귀 시험으로 승격**했다(§7·§8 신설,
①에 rc 0 실측). 이 파일이 도는 한 ① `/roles` 가 다시 0행으로 안 깨지고(§1~6, 턴 AB
원본 그대로 보존) ② 인수 6종 최소 메뉴 규칙이 안 깨진다(§7~8, 이번 턴 신설)를 **한 번에**
잰다. CI·pre-commit 배선은 **안 했다** — `.pre-commit-config.yaml` 은 내 소유가 아니고
(§0.4 소유표 밖) 이번 지시서에도 없다. 다음 턴 Q·조율자 청함.

## ⑥ 못 한 것과 왜

1. **브라우저 실측 0회** — 로그인 요금(5회/분·61초 전환)과 다른 차선 충돌을 피하려고
   ORM·순수 함수로만 잰다. "누른 뒤"가 아니므로 ④를 초록이 아니라 상한으로 적는다.
2. **GeumSan-ETRI(order_geumsan, id 102)를 처리 안 함** — 세종이 이 테넌트를 GX 로
   명시적으로 판정한 적이 없다(턴 AB 도 "참고"로만 따로 셈). 재배정하면 남의 판정을
   내가 대신 내리는 것이라 손을 안 댔다 — 대신 ACQUIRED_ONLY 코드에 걸려 최소 메뉴는
   이미 받는다(영어 0). 다음 턴 판정 청함.
3. **U5#15·U6#3·U6#4 등 §3 표의 다른 U56 항목은 이번에 안 봄** — 이 턴 프롬프트가
   "8명 처리 + surveillance_* 정본 확인 + U5#2 회귀"만 냈다. 넓히지 않았다.
4. **CI/pre-commit 배선 안 함**(위 ⑤).
5. **superuser·user·tenant_admin_4 단독 보유 계정(14명)은 그대로 hideAcquired** — 이번
   지시서의 "인수 6종"에 없어 손 안 댔다. 필요하면 별도 판정 청함(범위 확장 필요).

## ⑦ 지시서 전제 중 틀린 것 / 다시 잰 것

- **"표 밖 역할 8명"은 다시 재도 8이 맞다** — 단, `is_active` 만으로는 **9**가 잡힌다
  (soft-delete 된 order_chungnam_001 이 섞여 들어온다). `deleted IS NULL` 을 같이
  걸러야 턴 AB 의 8과 정확히 일치한다. 지시서엔 이 함정이 안 적혀 있었다 — 이번에
  실측으로 확인하고 적는다(P-246 스타일: "재서 8" 이 아니라 "재서 8, 단 그물이 성기면
  9로 보인다").
- **GeumSan-ETRI 는 세 턴째 판정 공백이다** — P-229→P-238 은 항상 "ETRI-Group" 만
  말했지 GeumSan-ETRI 를 명시한 적이 없다. 이번에 그 공백을 처음으로 적는다(⑥-2).

## ⑧ 모르는 것

1. **GeumSan-ETRI 가 GX 테넌트인지 인수 테넌트인지** — role 조합(operator+order)은
   ETRI-Group 과 닮았지만, 다른 차선의 옛 자료(`W0-16/superuser_accounts.md`)는
   GeumSan-ETRI 를 Thailand·Gongju 등과 한 줄에 "고객·시험"으로 묶어 적었다. 갈린다.
2. **"로그인·내 정보" 최소 메뉴의 정확한 모양** — `/dsm/me`·`/profile` 둘 다 dj-core
   `Menu` 행이 **없다**(ORM 실측, `Menu.objects.filter(path=...)` 0건). 그래서 이번
   구현은 "최소 메뉴 = 서 있는 자리 하나 + 그 외 0"으로 냈다 — "로그인·내 정보" 두
   글자를 사이드바 줄로 넣으라는 뜻이었다면, 그 두 화면에 먼저 실재하는 `menu.Menu`
   행(id)을 세워야 한다(U24 소유 `product_menus.py` 영역 — 내가 못 고친다). 조율자
   판정 청함.
3. **43명 중 실제로 로그인해 화면을 보는 사람이 몇 명인지** — `last_login` 값은 봤지만
   "지금도 쓰는지"는 이번 턴 범위 밖이라 안 쟀다.
