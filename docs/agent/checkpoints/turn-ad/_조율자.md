# 턴 AD 조율자 기록 (영실 · 2026-09-22 저녁 · 파 1)

## §0 이 턴은 지시서와 **시각이 다르다**

지시서 `WO-GX-20260923-06` 은 **09-23 10:00~13:30** 자리다. 그런데 대표가
**오늘 저녁 재부팅 1회**를 하고 **창 2a 는 내일 12:00** 에 「계속」을 주었다.
그래서 **둘로 쪼갰다**: 오늘 = 차선 넷 + 커밋(파 1) · 내일 = 창 2a + 배치 + V(파 2).

**한도 규약(P-249) 지켰다**: 차선 **넷** · **전원 가벼운 모델** · Opus 차선 0. 429 **0건**.

---

## §1 N — 부류 여섯이 섰다 (P-246)

`measured_red` 신설. 자기시험 **74건 0 실패** · 출생 표본 = **OPS-04**(합성 아님).

**★ N 이 옳게 판단한 자리 하나**: P-216 선례는 「판정기 목록 갱신과 한 커밋」인데,
N 은 `KIND_SCORE` 를 그대로 앨리어스하면 **Q 의 자기시험(`len==5`)이 오늘 밤 빨개진다**는 것을
코드로 확인하고 **다섯으로 거른 사본**을 대신 냈다. 「다른 판정기는 만지지 말라」를
선례보다 위에 뒀다 — **조율 없이 남의 차선을 빨갛게 만들지 않는 것**이 맞다.

### ★ 조율자가 이어서 한 것 — 후보 셋을 **읽고 옮겼다**
N 은 `OPS-07` · `OPS-10` · `SEC-18` 셋이 rc 1 임을 **찾아 놓고 안 옮겼다**
(「사람이 먼저 읽어야 한다」). 읽었다. 셋 다 자기 `kind_why` 에 이미 이렇게 적혀 있다:

> 「상태가 '미착수' 이고 **게이트도 이번 턴 exit 1** — **색이 같다**」
> 「**잰 초록이 없다 → `unmeasurable`**」

**어휘가 없어서 그 칸에 있었던 것이다.** 새 칸을 만든 이유가 정확히 이것이므로 옮겼다 —
새 판단이 아니라 **이름을 마저 고친 것**이다.

| | 전 | 후 |
|---|---|---|
| measured_red | 1 (OPS-04) | **4** (+ OPS-07 · OPS-10 · SEC-18) |
| unmeasurable | 71 | **68** |
| 상용 | 56.3 | **56.3 (안 움직였다)** |

**수가 안 움직인 것이 옳다** — 둘 다 0.0 이다. 움직였다면 그것이 오히려 사고다.
영역별로 보면 **운영 3 · 보안 1** 이 「못 잼」에서 「재서 빨강」으로 갈렸다.

### ⚠ 남은 흠 — **머리글 줄은 아직 거짓말한다**
```
[입력] 「초록」 부류 155절 — closed 72 · ratchet 3 · rule_only 3 · unmeasurable 72 · gate_only 5   ← 옛 5키(Q 호환)
[입력] ★ P-246 여섯째 부류 — closed 72 · ratchet 3 · rule_only 3 · unmeasurable 68 · measured_red 4 · gate_only 5
```
아랫줄이 정본인데 **윗줄이 먼저 나온다.** 윗줄만 읽는 사람은 「못 잼 72」로 읽는다 —
**우리가 이번에 없애려던 바로 그 뭉침**이다. 지금 지우지 않는 이유는 `verify_readiness_scores.py`
(Q 소유)가 그 줄을 읽기 때문이고, 오늘 밤 조율 없이 남의 판정기를 깨지 않는다.
**세종 판정 청함**: 그 줄을 언제 걷나 — 걷는 커밋에 Q 의 소비자도 같이 간다.

---

## §2 Q — 회색 게이트 **11 → 2** (조율자 검산함)

차선의 보고는 입력이지 측정이 아니므로 직접 댔다:
```
MEASURED_LINE  게이트 81개 중 **79개가 「무엇을 · 분모 N」을 말한다** · **회색 2**
               ['verify_perf_budget.py', 'verify_release_candidate.py']
```
둘 다 **의도한 `deferred:RC-1 2026-09-25`** 다. 분모를 손으로 안 적었다 — 전부 `len(...)`.

### ★ 내일 이 칸은 **빨강이 된다** — 그리고 그것이 옳다
판정기가 스스로 말한다: 「D-511 의 기한은 **2026-09-23** 다 — 그날부터 이 칸은 **red**」.
`deferred:` 의 「언제」가 **09-25** 라 **이틀 동안 빨강**이다.
**지연 신고는 면제가 아니다**(P-243) — 「언제」가 사는 이유는 기한을 미루기 위해서가 아니라
**빨강 옆에 갚을 날을 적기 위해서**다. 「빨강 2, 이유 모름」과 「빨강 2, 09-25 에 갚기로 적혀 있음」은 다르다.

### ★ Q 가 「예측이지 실측 아님」으로 남긴 자리 — **조율자가 재니 빨강이었다**
```
docker exec -e DJANGO_SETTINGS_MODULE=config.settings gx-shell python /repo/scripts/verify_seed_p20.py
[P20] [입력] 시드 이벤트 23건 · 알림 규칙 8건
[P20] ✗ 씨앗 23건 중 청구 셈에 **남은 것 22건** — seed 사건이 청구·월간 KPI 로 샌다
      씨앗 카메라는 `track_id` 표식도 없고 `StreamMonitor.data_source` 도 기본값 `live` 그대로라
      `exclude_unbillable` 의 세 갈래 중 **어느 것도 안 걸린다**                          rc=1
```
**`exclude_unbillable` 에 씨앗을 거르는 갈래가 아예 없다.** B 에 그대로 넘겼다 —
B 의 「새 씨앗 1 → 청구 0 증가」 시험은 **배선 전에는 반드시 빨강**이어야 그 A/B 가 증명이 된다.

---

## §3 U56 — 표 밖 8명 처리 (조율자 독립 재측 8/8)

`order` → `fire_user` 재배정. **M2M 연결만** 바뀌었다 — 삭제 0 · 비활성화 0 · 전부 되돌릴 수 있다.
조율자가 새 프로세스로 다시 읽어 **8/8 전부 `fire_user` · `order` 0** 확인.
인수(외부) 테넌트 43명은 **사람·행·역할 0줄** 안 바꾸고 `RoleNavFilter` 코드로만 사이드바를 줄였다.

### ★ U56 이 잡은 함정 — 지시서에 없던 것
> `is_active` 만 거르면 **소프트삭제 계정이 섞여 9로 보인다**(`order_chungnam_001`).
> **`deleted IS NULL` 을 같이 걸어야** 8이다.

「8명」이라는 수는 맞았지만 **그 수에 닿는 길이 하나가 아니었다.** 지시서는 길을 안 적었고,
잘못된 길로 가면 **남의 소프트삭제 계정을 되살릴 뻔했다.**

### 영어 사이드바 121 → 0 — **다만 정지 상태다**
ORM 으로 `RoleMenu` 전수를 세어 121 → 0. ⚠ **브라우저 실측 0회**(로그인 요율·차선 충돌 회피).
U56 이 스스로 「이는 **상한**이지 「누른 뒤」의 초록은 아니다」라고 적었다 — **그대로 받는다.**
⚠ `withCurrentPath` 가 지금 주소 줄 하나는 되돌리므로 **그 한 줄은 여전히 영어일 수 있다.**

### 조율자 판정 — 최소 메뉴는 **지금 모양으로 둔다**
U56 이 물었다: 「로그인·내 정보」를 사이드바 줄로 세우려면 `menu.Menu` 에 `/dsm/me`·`/profile`
행이 있어야 하는데 **ORM 으로 재니 0건**이고, 그 표는 U24 소유다.
→ **행을 새로 세우지 않는다.** P-238 은 「손대지 않고 최소 메뉴」이고, 없는 행을 세우는 것은
**인수 테넌트에 새 제품 면을 여는 일**이다. 지금의 「서 있는 자리 하나 + 그 외 0」이 최소다.
행이 필요하다면 **U24 와 같은 변경**으로 턴 AE 에 한다.

---

## §4 B — 씨앗이 청구에서 **22 → 0** (앞뒤 둘 다 조율자가 쟀다)

| | 전 [16:45 조율자 실측] | 후 [17:00 조율자 실측] |
|---|---|---|
| `verify_seed_p20` | **rc 1** | **rc 0** |
| 씨앗 23건 중 청구 셈에 남은 것 | **22건** | **0건** |

★ 이 앞뒤는 **차선의 말이 아니다.** Q 가 「예측이지 실측 아님」으로 남긴 자리를 조율자가 먼저 재서
빨강임을 확인했고(16:45), B 에 넘겼고, 고친 뒤 같은 명령으로 다시 쟀다(17:00). **같은 그물로 두 번.**

### 고친 모양 — 새 칸이 아니라 **상속**
`exclude_unbillable` 에 갈래 **㉣** 신설: 제 `data_source` 칸이 없는 표가 카메라로 이어지면
**그 카메라의 출처를 물려받는다.** 씨앗 사건은 `track_id` 표식도 제 칸도 없어서
㉠㉡㉢ 어느 것에도 안 걸리던 자리다.

### ★ 고객이 다치지 않았는가 — 세어서 확인했다
갈래 ㉣ 이 고객을 다치게 하는 **유일한 길은 고객 카메라가 잘못 표식되는 것**이다. 전수를 셌다:
```
카메라 52대 — live 40 · probe 8 · seed 1 · drill 3
live 아닌 12대 전수: GX-SEED-DSM(seed) · gxprobe-D384-screen-CAM + GX-ONB-V-* 7(probe)
                    · GX-DRILL-ANYANG-01~03(drill)
```
**열둘이 전부 우리가 만든 것이다. 고객 카메라 40 대는 전부 `live` 로 남았다.**
그리고 B 는 `NONBILLABLE_SOURCES` **집합 자체는 한 글자도 안 바꿨다**(diff 확인) —
훈련이 청구에 드는지 마는지는 **이 턴에 움직이지 않았다.**

> **청구를 줄이는 변경은 「무엇이 빠졌나」가 아니라 「무엇이 남았나」로 증명한다.**
> 22 가 빠진 것보다 **40 이 남은 것**이 이 변경의 안전을 말한다.

### 마이그레이션 둘 — 운영 DB 에 이미 먹었다 (`[X]`)
`common/0004_p224_seed_account_marks_v_residual` · `stream_monitors/0032_p224_camera_data_source_v_residual`
- 둘 다 **되돌리는 함수가 있다**(`RunPython(정, 역)`). 역함수의 `delete()` 는 **자기가 만든 표식만** 지운다 — 데이터 삭제가 아니다.
- 계정 표식은 `pk + username` 을 **대조한 뒤에만** 얹는다 — id 가 밀려도 엉뚱한 사람을 안 잡는다.

### ★ 지시서 전제가 또 틀렸다 — 「다섯 자리」는 다섯이 아니었다
B 가 태어나는 자리를 전수로 세니 **7곳(6파일)** 이었고, 그중 **다섯은 `transaction.atomic()` +
`raise _Rollback` 으로 스스로 되돌려 DB 에 안 남는다** — 청구 위험 0 이라 배선할 대상이 아니다.

| 지시서 | 실측 |
|---|---|
| ① 게이트 탐침 카메라 | 생성 지점 7곳 중 **남는 것 2곳**만 배선 |
| ② 온보딩 계측기 | **못 했다** — Q 소유 파일 + ORM 접근이 아예 없는 순수 Playwright. 걸 신호가 없다 |
| ③ 훈련 창 | **태어나는 자리 자체가 없다**(create/save 0건) |
| ④ 씨앗 계정 | 배선함 |
| ⑤ 탐침 계정 | 손으로 한 번 만든 고정 픽스처 — 반복 생성 0 |

**그러므로 「5/5」라고 적지 않는다. 배선한 자리 셋 · 대상 아님 둘 · 못 한 것 하나다.**
V 잔여도 「계정 226 하나」가 아니라 **다섯**(118·119·224·225·226)이었다.

---

## §5 조율자 오판 — **경합 중에 잰 수를 적을 뻔했다**

타입검사가 **3239** 로 나왔다(기준선 3234). 우리 파일 언급은 0 인데 다섯이 늘었다.
「기준선이 오른 것은 이유를 대야 한다」이므로 A/B 로 갈랐다 — 두 파일을 HEAD 로 되돌리니 **3234**,
되돌린 것을 다시 넣으니 **3234**, 연달아 두 번 더 **3234 · 3234**.

**3239 는 내가 tsc 두 벌과 게이트 하나를 동시에 돌리는 중에 잰 수였다.**
윈도 바인드 마운트 위에서 읽기가 겹치면 파싱이 흔들린다.

> **경합 중에 잰 수는 수가 아니다** — 이 저장소가 이미 적어 둔 불변인데 **내가 또 걸렸다.**
> 이 턴 조율자 오판은 이것 하나이고, **A/B 를 안 했으면 「U56 이 기준선을 5 올렸다」고
> 거짓으로 적을 뻔했다.** 차선에게 없는 죄를 씌우는 것도 거짓 빨강이다.

정본: **타입검사 3234 = 기준선 · 우리 파일 언급 0**(고립 3회 연속).

---

## §6 전량 시험 — **2,122 통과 · 1 실패** (숨기지 않는다)

```
FAILED tests/test_sec21_login_ratelimit.py::LoginRateLimitBodyTest::test_sixth_login_is_429_json_korean_with_seconds
1 failed, 2122 passed, 6 skipped  (13:57)
혼자 다시 재면: 3 passed (2:42)
```

### 진단 — 시험은 잘 격리돼 있는데 **시계에 기댄다**
그 파일은 오염을 이미 막고 있다: 전용 주소 `10.255.255.77` · 전용 캐시
(`override_settings(CACHES=locmem, LOCATION="sec21-lane-f")`) · **없는 계정**(실계정 잠금 0).

그런데 **locmem 은 LOCATION 으로 묶인 프로세스 전역 저장소**이고 율제한 창은 **1분**이다.
계수를 **test 사이에 치우지 않는다**(`cache.clear` 0건). 그래서 전량 시험 안에서
이 파일이 **몇 시 몇 분에 서느냐**에 따라 앞 회차의 계수가 남아 있기도, 1분이 지나 비어 있기도 하다.
턴 AC(2,116)에서는 통과했고 턴 AD(2,122)에서 실패했다 — **시험 수가 늘어 시각이 밀린 것**이 맞는 설명이다.

### 오늘 밤 안 고치는 이유
혼자 재면 **어차피 통과**한다. 그러므로 「고치고 혼자 재서 초록」은 **고침을 증명하지 못한다.**
증명하려면 「계수를 일부러 채워 둔 뒤에도 통과하는가」라는 **음성 대조**가 있어야 하고,
그것은 차선이 소유를 들고 할 일이지 재부팅 20분 전 조율자가 손댈 자리가 아니다.

**턴 AE 로 넘긴다**: `setUp` 에서 그 캐시를 치우고, **음성 대조**(계수를 미리 채워도 통과)를 붙인다.
**「혼자 재면 통과」를 「고쳤다」로 적지 않는다.**
