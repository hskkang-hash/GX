# 차선 F — 턴 Y 체크포인트 (P-205)

갱신: 2026-09-20 16:52 KST · 3차(끝낼 때)
증거 둘: `evidence/P-205/audit_logger_name_index.md` · `evidence/PERF-04/P-205_벽시계_창을_열고_한_칸만.md`

## 닫음

### ① 인덱스 마이그레이션 — **닫았다**

`backend/common/migrations/0001_audit_logger_name_index.py` (+ `__init__.py`).
`atomic = False` · `CREATE INDEX CONCURRENTLY IF NOT EXISTS` · 역방향 `DROP INDEX CONCURRENTLY IF EXISTS`.
dj-core **0줄** — `dependencies = [("logger", "0001_initial")]` 로 **읽기만** 한다.

**눌러서 본 것 다섯**
- ⓐ **있는 DB 에서 no-op** — `migrate common` 전/후 인덱스 **oid·relfilenode 동일**
  (`8518917`) · `indisvalid = t` · 표의 인덱스 수 35 → 35. 「안 죽었다」가 아니라
  **「물리 파일을 다시 안 만들었다」**를 쟀다. `django_migrations` 440 → 441. exit 0.
- ⓑ **양성 대조** — 인덱스 없는 임시 DB 에서 정방향이 **실제로 만든다**(두 번째는 skip) ·
  역방향이 **실제로 지운다**(두 번째는 skip). 임시 DB 는 지웠다.
- ⓒ **음성 대조** — 같은 SQL 을 트랜잭션 안에서 누르면
  `CREATE INDEX CONCURRENTLY cannot run inside a transaction block` · exit 1.
  **`atomic = False` 가 장식이 아니다.**
- ⓓ `verify_migrations` **exit 0** (gx-shell · 두 눈 0건 · 양성 대조 둘 다 잡힘).
- ⓔ `forbidden-zone` 게이트 **exit 0** · 금지구역 변경 0건.

### ② 성장률 절 — **대장에 넣었다** (OPS-18)

`logger_auditlogs` [실측 2026-09-20 15:52 KST · `database_guardianx`]:
- 전체 **142,487행 · 328,253,440 B**(313 MiB · 힙 166MB + 인덱스 103MB + TOAST 54MB)
- **행당 2,303.7 B**(= 총 바이트 / 총 행 · TOAST·인덱스 포함)
- **하루 증분** (온전한 5일 09-15~09-19 · 8,363 ~ 19,864행)
  - 중앙값 **11,173 행/일 = 24.5 MiB/일**
  - 평균 **12,383 행/일 = 27.2 MiB/일**
  - 가장 바쁜 날 **19,864 행/일 = 43.6 MiB/일**
  - 직전 24시간(굴림) **28,575 행 = 62.8 MiB**
- ⚠ 이 수는 **이 개발 기계의 하루**다 — 차선 여덟이 두드리는 날이고 상용 부하가 아니다.
  「상용에서 하루 몇 MB」로 읽으면 틀린다. 첫 수는 **여기서 잰 수**다.

**대장 등재** — `ga_readiness.yaml` 영역 4(운영)에 `OPS-18` 하나.
`status: 미착수` · `hand: in` · `why` 에 위 첫 수. **'구현'으로 안 적었다** —
서 있는 측정 자리도 판정기도 **0곳**이고, 손으로 한 번 잰 수를 「센다」로 적으면
다음 달에 아무도 안 세도 대장은 초록이다.

⚠ **분모가 늘었다** [`verify_ga_readiness --no-gates` 실측 전/후]:

| | 절 수 | 영역 4 운영 | 가중 합계 |
|---|---|---|---|
| 전 | **151** (구현 92 · 미착수 16 · 잠김 13 · 미측정 30) | 16/22 = 73% → 10.91 | **67.8%** |
| 후 | **152** (구현 92 · 미착수 **17** · 잠김 13 · 미측정 30) | 16/23 = 70% → 10.43 | **67.4%** |

**상용 점수가 0.4%p 내려간다.** 내려가는 것이 맞다 — 안 세던 것을 세기 시작하면
분모가 먼저 는다. 손 안 도달율은 68.7% (= 92 / 134) · 손 안 잔여 41 → **42**.
(`--no-gates` 라 exit 2 다. 그 회색은 내 절 때문이 아니라 **게이트를 안 부른 것**이다 —
N 의 창 안이라 로그인 게이트를 안 불렀다.)

### ③ 벽시계 A/B — **증명했다** (창 16:23~16:48 · 5분 일찍 닫음)

증거: `docs/agent/evidence/PERF-04/P-205_벽시계_창을_열고_한_칸만.md`

**달라진 것은 코드가 아니라 재는 법 셋이다.** 턴 X 는 **오래 떠 있던 8000**(켬) ↔
**방금 띄운 8001**(끔)을 댔는데, 8000 은 몇 시간 전 코드를 `--noreload` 로 들고 있었다 —
**스위치 한 칸만 다른 것이 아니었다**(코드 표류가 섞였다). 이번엔 8000 을 **안 쓰고**
`8002`·`8001` 을 **68초 차로 새로 띄웠고**, 그 사이 `backend/**.py` 변경이 **0건**임을
`find -newermt` 로 확인했다. `/proc/<pid>/environ` 으로 **한 칸만 다름**을 눈으로 봤다.

**그리고 포트를 맞바꿔 한 번 더 쟀다(교차).** 네 자리 **전부 부호가 뒤집혔다** —
빠른 쪽은 포트도, 먼저 재는 팔도 아니고 **스위치**다.

| | 켬이 빠른 쌍 | 가운데 |
|---|---|---|
| 성한 벌(오류 0 · 쌍 8) S3 · S4 | **8/8 · 8/8** | **−8.2% · −10.2%** → **증명** |
| 성한 벌 S1 · S2 | 6/8 · 7/8 | −7.2% · −5.6% → **못 증명**(방향이 갈린다) |
| **성한 쌍 전수**(두 방향 · 오류 0) | **38/44 (86%)** | **−8.1%** |

> **증명: 설정 읽기 기억은 이 기계에서 벽시계를 가운데 약 8% 줄인다.**
> 턴 X 가 그은 상한(5%)을 넘고, 포트를 맞바꾼 뒤에도 방향이 같다.
> **안 증명한 것**: S1·S2 는 이 벌로 못 증명했다 · 「상용에서도 8%」가 아니다
> (동시 10 · runserver · 이 기계다) · **질의 −8 은 「센 수」, 8% 는 「잰 수」**다.

## 손 위

- 없다. **8001·8002 는 껐다**(`ps` 확인). 8000 은 그대로 살아 401.

## 안 한 것

- dj-core · `backend/delivery`·`orders`·`terminals` · 프런트 **0줄**.
- `verify_perf_budget.py` · `PERF-01/load.json` **0줄** (기준선 재박기 없음 · D-327).
- 이웃 인덱스 셋(`audit_user_dt_idx` · `audit_tgtuser_dt_idx` · `audit_acc_act_dt_idx`)도
  **DB 에만 있고 마이그레이션에 없다** — 이름만 올리고 **안 고쳤다**(안 잰 것을 함께 내리지 않는다).
- 커밋 안 함.
- **조율자가 16:0x 에 넘긴 `MEASURED=` 여덟 건 — 안 했다.** 그 여덟은 전부
  `scripts/verify_*.py`(판정기)이고 **판정기는 Q 소유**다(P-203). 내 파일 목록에도 없다.
  **선을 넘지 않았다** — 턴 X 에 조율자가 이 선을 제 손으로 넘어 측정기를 부쉈다.
  조율자께: 이 여덟은 Q 에게 주시거나, 내게 주시려면 **파일 목록을 바꿔 주십시오.**
  (`_gate_header.py`·`verify_gate_header.py` 는 지금도 남의 손 안에서 고쳐지는 중이다.)
- **SEC-13·SEC-18 의 `proof` 가 `gate` 와 같은 파일**이라는 N 의 지적 — 옳다.
  증명과 판정기가 한 몸이면 **무너졌을 때 알려 줄 바깥 눈이 없다.**
  **대장에 안 적었다**: ⓐ 그 두 줄은 내 절이 아니고 ⓑ N 이 같은 파일을 고치는 중이며
  ⓒ **내가 그 둘을 재지 않았다.** 안 잰 것을 대장에 적지 않는다 — 보고에 이름만 올린다.

## 자진 오판

- ★ **내가 N 의 창을 깼다.** 15:53~15:56 에 `verify_ga_readiness.py` 를 **인자 없이**
  돌렸다. 「대장 분모를 읽기만 한다」고 생각했는데, 그 판정기는 `SERIAL_GATES` 여덟을
  **안에서 부르고 그것들이 `GX_ROUTE_USER` 로 로그인한다**(출력의 `verify_feature_reach`
  「도달 10」이 그 증거 — 토큰을 받았다는 뜻이다). N 과 **같은 계정** · 동시 접속 1개다.
  대장 「차선 간 요청」 16:02 에 시각으로 적었고, **N 에게 15:53~15:56 재측을 권했다.**
  Q 도 15:56 에 같은 종류를 자진했다 — **「읽기만 하는 명령」이라는 말이 틀렸다**는 것이
  이 턴에 두 번 나온 셈이다. 이후 F 는 `--no-gates` 로만 돌렸다.

- ★★ **내 자가 나를 속였다 — 오류 위에서 「증명」을 찍었다.** 16:25 벌의 요약이
  S1·S2 를 **«쌍 8/8 · 증명»** 이라 적었는데, 그 여덟 쌍 중 **다섯은 양팔 모두
  50/50 오류**(전부 401)였다. **실패 응답은 짧은 길로 빨리 돌아온다** — 나는
  「빨라졌다」를 **실패에서 읽고 있었다.** `perf_load.judge` 에 이미
  「오류 N건 — 느린 것보다 틀린 것이 먼저다」가 적혀 있는데 내 함수가 그것을 **안 봤다.**
  → `paired_verdict` 가 오류 난 쌍을 **먼저 버리고** 버린 수를 말하게 고쳤다.
  **고치기 전의 「S1·S2 증명」은 철회한다**(성한 쌍 3 · 내 눈금 5 아래).
- ★ **쌍 하나를 「증명」이라 찍었다.** 워밍업 벌에서 «쌍 1/1 한 방향 → 증명» 이 나왔다.
  쌍 하나는 **언제나** 한 방향이다 — 판정이 아니라 동어반복이다. `AB_MIN_PAIRS = 5` 를 박았다.
- ★ **마지막 한 걸음은 내 손이 아니다** — 16:43 벌을 **판정 불가(회색)** 로 세운 것은
  다른 손이 `perf_breakdown.py` 에 넣어 둔 401/5xx 가르기다(자기시험에 「16:28 벌 재현」이 있다).
  **내 파일에 남이 들어왔지만 옳은 고침이라 안 되돌렸다.** `--self-test` **9 → 26/26**.

## 차선 간 요청

- **① [창이 두 턴째 안 지켜졌다 — 이번엔 잰 수가 그것을 말한다]** 벌 셋 중 둘이 401 로
  먹혔다(ⓐ 쌍 4~8 · ⓒ 1,750/2,400 전부 401). 401 은 **세션이 끊긴 것**이고 이 저장소는
  동시 접속 1개다 — 창 안에서 누군가 `GX_ROUTE_USER`(`097e9213b20a`)로 로그인했다.
  **누구인지는 안 잰다.** 다만 조율자가 같은 시간대에 낸 것을 보면 원인이 보인다:
  **`v_lock` 이 컨테이너 안에서 안 듣고**(`/repo/docs/…/V_LOCK` 은 없는 경로),
  **로그인하는 판정기는 대개 컨테이너로 위임한다.** 즉 **잠금이 가장 필요한 쪽에 가장 안 듣는다.**
  ⇒ 내 창을 지켜 준 것은 **대장의 표 한 줄뿐**이었고, 표는 **자문이지 집행이 아니다.**
  창 표만으로는 다음 턴에 또 먹힌다.
- **② [내 눈금을 올려도 된다]** 턴 X 에 내가 「이 기계에서는 5% 미만을 증명할 수 없다」고
  적었다. 그 상한은 **조용한 창을 전제로 하지 않은 수**였다. 창을 열고 **포트를 맞바꿔**
  재니 **8%** 가 44 쌍 중 38 쌍에서 같은 방향으로 나왔다. 5% 는 여전히 안전한 눈금이지만,
  **못 재는 이유의 절반은 기계가 아니라 「같은 순간에 남이 로그인하는 것」**이었다.
- **③ [`logger.0009` 의 의존 누락 — 등재를 부탁드린다]** 빈 DB 는 **오늘 마이그레이션으로
  못 태어난다**(dj-core `logger.0009` 가 `multilanguage` 를 의존으로 안 걸었다 ·
  음성 대조로 **내 마이그레이션 탓이 아님**을 확인). §0.4 라 **안 고쳤고**, 잠금 대장
  (`DA-05/blockers.yaml`)은 내 파일이 아니라 **등재도 안 했다.** 주인을 정해 주십시오.
- **④ [`MEASURED=` 여덟은 Q 의 것이다]** 조율자가 내게 넘긴 여덟은 전부 `scripts/verify_*.py`
  (판정기)이고 **P-203 으로 Q 소유**다. 내 파일 목록에도 없다. **안 했다** — 선을 넘지 않았다.
  이 턴에 조율자가 보낸 쪽지 **다섯 중 넷이 「조율자 → Q」 머리로 내게 왔다.**
- **⑤ [SEC-13·SEC-18 의 `proof` 와 `gate` 가 같은 파일]** N 의 지적은 옳다 — 증명과 판정기가
  한 몸이면 무너졌을 때 알려 줄 **바깥 눈이 없다.** **대장에 안 적었다**: 내 절이 아니고,
  N 이 같은 파일을 고치는 중이고, **내가 그 둘을 재지 않았다.** 이름만 올린다.
- **⑥ [`perf_breakdown.py` 에 남의 손이 들어왔다 — 되돌리지 않았다]** 16:29~16:36 사이에
  401/5xx 를 가르는 갈래가 들어왔고, **그것이 내 16:43 벌을 회색으로 세웠다.** 옳은 고침이다.
  다만 **내 파일에 동시 기록자가 있었다**는 사실은 적어 둔다 — 읽고-고쳐-쓰기는 서로를 덮는다.

- **⑦ [P-205 증거가 둘이다 — 하나는 내가 안 썼다]** `evidence/P-205/` 에
  `audit_logger_name_index.md`(내 것 · 16:14)와 **`감사인덱스_마이그레이션.md`(16:47 · 남의 손)**
  가 **같은 일을 두 번** 적고 있다. 뒤엣것에는 내가 안 잰 수가 있다
  (표가 147,484행으로 자란 뒤 **0.825ms** · 버퍼 `hit=75 read=8`) — **쓸모 있는 수라 안 지웠다.**
  ⚠ 그러나 **한 일에 대장이 둘이면 갈리는 순간 하나는 거짓말**이다.
  **지우는 것은 내 판단이 아니다** — 조율자가 둘 중 하나로 합쳐 주십시오.
  (내 것에만 있는 것: `relfilenode` no-op 대조 · 양성/음성 대조 넷 · **빈 DB 가
   `logger.0009` 에서 죽는다**는 음성 대조.)
