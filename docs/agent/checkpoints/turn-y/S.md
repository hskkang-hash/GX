# 턴 Y · 차선 S (LAW-08 감사)

## 닫음

### ① 끊김 #276795 등재 확인 — **맞게 섰다** (`--db` 로 쟀다)
- `python scripts/verify_evidence_chain.py --db` → `감사 행 2897 · 체인 2608 · **등재 23** ·
  **새로 난 어긋남 0건** · 머리 1fb6e9a77bf198ad… · exit 0`.
- 열쇠(id + 종류 + got 64자 + want 64자)가 산 표의 그 행과 **한 글자도 안 어긋났다**:
  컨테이너에서 직접 물어 `recorded` 23개 id 를 전부 받았다 — `#276795` 포함 · `breaks` 빈 목록.
  (열쇠가 한 글자라도 틀리면 그 자리는 **새 끊김**으로 떨어진다 → 등재가 빨강을 가린 것이 아니다.)
- ⚠ `--db` **없이** 돌리면 정적 검사만 하고 `exit 0` 이다. 위 수는 전부 `--db` 로 잰 것이다.
- 곁가지 고침: `RECORDED_BREAKS` 머리 주석이 아직 「지금 든 22건 … 전부 같은 원인」이었다.
  23건이고 **원인은 둘**이다(22 = P-191 경합 · 1 = 시험이 샜다) → 주석만 고쳤다. 값은 안 건드렸다.

### ② 가드 — **쓰기도 묻기도 운영 alias 로는 못 간다** (P-202)
- 자리: `common/evidence_chain.py` — `audit_db_isolated`(순수 술어) · `guard_audit_db`(fail-closed) ·
  `audit_db_name` · `AuditDbNotIsolated`. 부르는 자리 셋 —
  `evidence_chain._model()`(묻기 · 이 파일의 질의가 전부 여기를 지난다) ·
  `audit_writer._model()`(묻기 · `read` 도 지난다) ·
  `audit_writer.write()` **트랜잭션 열기 전**(쓰기 — 운영 DB 에 트랜잭션도 advisory lock 도 INSERT 도 없다).
  켜는 자리: `backend/conftest.py::pytest_configure`.
- ⚠ **가드가 DB 에 묻지 않는다.** `connections[alias].settings_dict["NAME"]` 을 읽을 뿐이다 —
  턴 X 사고의 원인이 「묻는 행위」였으므로 가드가 물으면 **가드가 곧 사고다.**
  시험으로 박았다: 커서를 못 열게 막아 놓고 가드를 부른다(`TheGuardDoesNotAskTheDatabaseTest`).
- ⚠ 바닥(`test_`)을 `DB_TEST_NAME` 접두와 **함께** 본다. 접두만 보면 그 환경변수에 운영 이름을
  넣는 순간 가드가 통째로 열린다 — 끌 수 있는 규칙은 결국 꺼진다(D-353).
- 시험 `backend/tests/test_law08_prod_writes.py` **14건**.
  **A/B 로 갈랐다**: 가드를 일부러 꺼 두니 **5 failed**(쓰기 · 묻기 · 체인검증 · 메시지 · 커서) ·
  되돌리니 14 passed. 분모 갈래(`test_the_normal_write_still_works`)를 같이 둔다 —
  「다 막는 가드」는 `raise` 한 줄로도 초록이고, 그 초록은 아무것도 안 재는 초록이다.
- 내 네 파일 + `test_evidence_guard` → **88 passed**.

### ★ 자진 — 가드의 첫 판이 **복사본**이었다 (남이 세워 둔 시험이 잡았다)
- 첫 판은 「지금 시험이 도는가」를 **제 자리에서 따로 물었다**(깃발 하나). 그 술어는
  `common/evidence_guard.py` 한 자리에 있고(D-369), `tests/test_evidence_guard.py::GuardLivesInOnePlaceTest`
  가 **바로 잡았다** — 내 시험이 아니라 **남이 이미 세워 둔 시험**이다.
- 잡히고 보니 **저쪽이 더 좋았다**: `under_pytest()` 는 깃발 **둘**을 보아 수집·픽스처·
  세션 마무리까지 덮는다. 턴 X 사고가 난 자리가 **픽스처 안**이라, 내 첫 판은
  복사본이면서 동시에 **그 창이 비어 있었다.** 정본 술어를 부르는 것으로 고쳤다.
- ⚠ 그래서 **전량을 한 번 더 쟀다**(아래 ③ 재측 ②). 「재고 **뒤에** 고쳐 놓고 재기 전 수를
  보고하는 것」은 재지 않은 것과 같다.

### ③ 게이트 `test-writes-prod-zero` — 세웠고 **전량으로 재서 초록** (두 번)
- `scripts/verify_test_writes_prod_zero.py` · 자기시험 **15/15**(출생 표본 = 턴 X 의 219행 유출 →
  빨강 · 분모 0 → 회색 · 행이 줄면 빨강 · 한 파일에 볼 줄이 둘인 자리를 따로 셈).
  ⚠ 자기시험 표본이 **한 번 거짓 빨강**을 냈다 — 어긋난 것은 제품이 아니라 표본이었다(주석에 적음).
- `docs/agent/verify_gates.sh` **세 자리 같은 변경**: `ALL_GATES` · `gate_test_writes_prod_zero()` ·
  dispatch case. `verify_gate_counts.py` → `게이트 20종 · 건수 출력 24곳 · 등재 20종` exit 0.
  (그 뒤 U24 가 `admin-doors` 를 더해 지금 목록은 **21종**이다.)
- `MEASURED:` 줄 있다(P-204). ⚠ **분모를 먼저 센 뒤에** 머리글을 찍는다 — 분모 없이 찍은
  MEASURED 줄은 그 자리에서 거짓말이 된다. `--db` 없는 호출은 **exit 2**(안 잰 호출).
- **재측 ①** (가드 첫 판 · 21분 27초): 운영 DB `database_guardianx` · 전 **219** → 후 **219** ·
  **새 행 0** · 분모 **1850** · exit 0.
- **재측 ②** (고친 가드 · 16분 21초): 전 **219** → 후 **219** · **새 행 0** · 분모 **1867** · exit 0.
  그 실행의 시험 빨강 **3건**(`test_s_webhook_outbox` · `test_tenant_isolation` ·
  `test_u56_notify_repeat_count`) — **이 게이트의 색이 아니고 내 파일도 아니다.**
  그래도 **수와 이름을 게이트가 인쇄한다**: 안 적으면 다음 사람이 이 초록을 「전량 초록」으로 읽는다.

### ④ 체인 행 29 — **사유 등재** (지우지 않는다)
- `evidence_chain.RECORDED_DANGLING` 두 줄 + `event_ref_of`(순수) + `dangling_event_refs`(재측 함수).
- **다시 쟀다**: 체인 행 **32** · 감사표 전체 **180**(차이의 대부분은 체인 시작 전의 두 칸 없는 행 144).
  턴 X 의 29/43 은 **세는 식이 안 남아 재현 못 한 수**다 — 그 사실을 그 줄 안에 적었다.
  두 판을 **직접 빼지 말라**고 적었다(원인이 둘 섞여 있고, 어느 쪽이 얼마인지 모른다).
- 사유: 사건을 지운 것은 **대표 결정**(P-184 · 177행 · 스냅샷
  `docs/agent/evidence/P-184/deleted_probe_snapshot_20260920.json`). 감사 행은 지우지도 고치지도
  않는다 — `event_id` 를 비우는 것도 **과거 행 수정**이다. 체인은 이 32행에 대해 **여전히 초록**이고,
  「가리키는 곳이 아직 있다」는 애초에 이 체인의 주장이 아니다.
- ⚠ **판정을 안 낸다**(빨강을 안 만든다). 여기서 색을 내면 대표 결정으로 지운 사건이 매일
  빨강이 되고, 매일 나는 빨강은 아무도 안 본다. 화면 쪽은 U24(P-208).
- 대장: `remaining_40.md` 2-2 절에 **OPS-25** 한 줄 + 등재 문단(`hand: in`).

## 손 위
- 없음.

## 안 한 것
- 게이트를 `verify_gates.sh` **러너로** 한 번 더 통과시키지 않았다 — 같은 명령을 또 16분 돌리는
  값이고, 스크립트는 이미 두 번 전량으로 쟀다. 러너의 `grep` 두 개는 **실제 출력에 대 봤다**
  (`가드 배선 자리 4곳` → 4 · `전량 단위 시험 1867건` → 1867).
- 전량 실행의 시험 빨강 3건은 **고치지 않았다** — 내 파일이 아니다.
- `--db` 없이 `exit 0` 을 내는 형제 게이트(`verify_evidence_chain.py`)는 **안 고쳤다** — Q 소유다(P-203).
