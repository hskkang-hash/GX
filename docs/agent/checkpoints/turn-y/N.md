# 차선 N 체크포인트 — 턴 Y · 파 3 턴 5 (2026-09-20 · 16:32 갱신 ⑦ · **마감**)

작업: **P-207 — 대장 절 닫기(증명 채우기)**. 브랜치 `turn-q` · HEAD `0acf8d1` · 커밋 안 함.
**후보 23 을 끝까지 돌았다.**

## 결산 — 후보 23 전체

**초록으로 닫음 20 · 회색(증거는 남김) 1 · 일부러 안 닫음 2 · 빨강 0**
→ `ga_readiness.yaml` 에 `evidence:` **21절**. 대장 diff **421줄 가산 · 0줄 삭제.**

| # | 절 | exit | `[입력]` 분모 | 결과 · 한 줄 사유 |
|---|---|---|---|---|
| 1 | SEC-01 | 0 | 라우트 **763** · 대장 763 | 🟢 ⚠ 신선도 **오늘 18:18 만료** |
| 2 | SEC-04 | 0 | 번들 **58** + 산 서버 **8콜** | 🟢 **재측 1회**(겹침 둘) · 수 불변 |
| 3 | SEC-05 | 0 | 파일 면 **107** · 이력 0 | 🟢 자기시험이 심은 키를 잡았다 |
| 4 | SEC-06 | 0 | **모수 795파일** · 표② 4 | 🟢 출생 표본(juso 키) 포함 |
| 5 | SEC-12 | 0 | 주석 **39** → 열린 문 0 | 🟢 **면제 0 — 전수 0** |
| 6 | SEC-13 | 0 | 시험 **132** · 스택 **49** | 🟢 **면제 0 — 전수 0** |
| 7 | SEC-18a | 0 | **판 12개 실제 기동** · 7/7 | 🟢 음성 대조 넷 |
| 8 | SEC-16 | 0 | backend 파이썬 **780** | 🟢 ⚠ **규약만** — 나가는 웹훅 0개 |
| 9 | SEC-17 | 0 | 프런트 **832** + **API 5라우트** | 🟢 ★ **거짓 초록을 한 뼘 차이로 비껴감** |
| 10 | ISO-02 | 0 | 커널 함수 **64/64** · 등재 66종 | 🟢 ⚠ `unguarded 346` 은 **보고한 수**(판정 아님) |
| 11 | ISO-03 | 0 | 라우트 **576** | 🟢 **래칫 — 면제 461(80%)** |
| 12 | OPS-07b | 0 | 시드 1 · **되돌림 했다** · 8수 | 🟢 U56 의 새 줄도 초록 |
| 13 | OPS-15 | 0 | **활성 구역 0개 → 합성** | 🟢 ⚠ **현장 분모 0** |
| 14 | **PERF-04** | **2** | 예산 **4/4 초록** · 회귀 **4/4 판정불가** | **⬜ 회색** — 잡음 25~30% > 문턱 20% |
| 15 | QA-03 | 0 | **대상 223** / 전체 319 | 🟢 **래칫 — 면제 96(30%)** |
| 16 | QA-12 | 0 | **7일 이벤트 4건** | 🟢 ⚠ **현장 분모 얇음**(시간당 0.0건) |
| 17 | UX-15 | 0 | 술어 **3종**(①②⑦) | 🟢 ⚠ `J/K`·`1/2/3`·`Enter`·`M`·「3종」 **미측정** |
| 18 | **UX-16** | 0 | ③④⑤⑥ PASS | ⛔ **안 닫음** — **지도 0술어 · 맥박 0술어** |
| 19 | UX-20 | 0 | 화면 **70파일** · 패턴 7 | 🟢 **기준선 0 · `--strict` 도 0 — 전수 0** |
| 20 | UX-25 | 0 | **역할 4** · 두 눈 대조 | 🟢 ⚠ **잰 때 오염 — 내 사고**(아래) |
| 21 | **LAW-01** | 0 | 자기시험 5 | ⛔ **안 닫음** — **잠금 게이트**(`READY=False`) |
| 22 | LAW-02a | 0 | 정적 3/3 · 백업 5 · RTO **0.14분** | 🟢 **코드에 보존 기본값 0개** |
| 23 | LAW-08 | 0 | **240파일** + **감사 2996/체인 2707** | 🟢 ⚠ **등재 끊김 23** · 종이 앵커는 **사람 몫** |

## ★★★ 안 닫은 둘 — 이유는 하나다: **그 게이트의 초록이 그 절의 닫힘이 아니다**(P-85)

**UX-16** — `exit 0` 이고 이 절 몫 넷이 다 PASS 인데도 비웠다.
제목이 부르는 셋 중 **지도 0술어 · 카메라 맥박 0술어**. 넷이 재는 건 월 모드의 **표시 방식**이다.

**LAW-01** — `exit 0` 인데 같은 실행이 `CLIP_EXTRACTION_READY=**False** · F-09 추출 무결성 AC
시험 없음` 을 낸다. 통과 문장이 **「잠긴 것은 사유가 있고, 열린 것은 시험이 있다」**다.
즉 **초록의 뜻이 「잠겨 있다」**이다. 판정기 머리말이 스스로 경고한다:
「약한 것을 약하다고 말하지 않으면 **「영상 연결 됩니다」가 초록으로 나가고 실제 재생은 영영
아무도 안 만든다.**」 **잠금 게이트가 `status: 구현` 인 계약 절에 붙어 있다.**
둘 다 **판정기도 상태도 안 건드렸다.** 사유만 적었다.

## ★★ 자진 — **내가 F 의 창을 깼다 (16:26)**

`verify_sidebar.py`(UX-25)를 **자격을 일부러 안 주고** 불렀다. F 의 A/B 창(16:25~16:55)이
열려 있었고 그 자격이 내 것과 **같은 것**이었기 때문이다.
**그런데 로그인이 됐다 — 판정기가 스스로 `.env.gates` 를 읽는다.** 시드 계정 넷으로 들어갔고
U4 `gxseed_u4_official` 은 F·Q·U3 와 **같은 계정**이다. 조율자 대장 16:26 에 자진으로 적었다.
★ **이 턴에 같은 사고가 셋째다**: F 15:53 · Q 15:56·16:01 · **N 16:26.**
  「인자 없이/자격 없이 부르면 로그인 안 한다」는 가정이 **세 번 틀렸다.**
  **자격을 안 주는 것으로는 로그인을 못 막는다.**
★ 그리고 그 셋 중 **내 것이 가장 나쁘다** — 나는 **창이 열린 것을 알고도** 「자격을 안 주면
  안전하다」고 **스스로 판단**해서 눌렀다. F·Q 는 몰랐고 나는 알았다.

## 두 번째 자진 — 증거 파일을 한 번 망쳤다
`echo` 안에 역따옴표를 넣어 셸이 그것을 **명령으로 실행**했다(`command not found` 3줄이
증거 파일에 박혔다). 파이썬으로 머리글을 다시 써서 고쳤다. **기억에 적어 둔
「Bash 도구가 이중 백슬래시를 벗긴다」와 한 식구**인데 또 밟았다.

## 고친 판정기 — **0줄.** 건드린 §0.4 — **0줄.** 커밋 — **0.**

## 이 턴에 모은 눈 (결산)

**「머리글이 위임한다 적고 안 한다」 넷**: SEC-17(**0 을 낸다 · 가장 위험**) · OPS-07b(2) ·
OPS-15(2) · QA-12(2).
**「현장 분모가 아직 안 찬 초록」 둘**: OPS-15(구역 0) · QA-12(7일 4건).
**래칫 — 면제를 업은 초록 vs 안 업은 초록**:
  업지 않음 = SEC-12(0) · SEC-13(0) · **UX-20(0)** → 전수 0
  업음 = **ISO-03(461·80%)** · **QA-03(96·30%)** · **LAW-08(끊김 23 + 체인 밖 289)**
**고쳐진 것 둘 (이 턴에 실측으로 확인)**:
  · `verify_evidence_chain.py` — **V 의 자진이 닫혔다.** `--db` 없는 실행이 이제 **회색(2)**.
  · `verify_authn_paths.py` 외 — `MEASURED=` 선언이 **0개 → 11/79**.

## 남은 것 / 넘김
- **PERF-04 재측** — F 의 A/B 창(16:25~16:55) 뒤. 조율자가 알려 주기로 했다. **대기 중.**
- **SEC-01 신선도** — **오늘 18:18** 만료. 조율자가 병합 시점에 `--refresh`.
- **UX-25 재측 여부** — F 가 16:26 벌을 다시 잰다면 그때 같이.
- 세종 판정 대기 둘: **UX-16**(판정기를 붙일지 제목을 좁힐지) · **LAW-01**(잠금 게이트 교체 ·
  상태 재검) · 그리고 **ISO-03 의 461 옆에 선 영역 ③ 100% 표시**.

## 고친 파일 (누적 28 · 전부 문서·증거)
`docs/agent/evidence/D-346/ga_readiness.yaml`(**421줄 가산 · 삭제 0**) ·
`docs/agent/evidence/` 아래 증거 **24개**(21절 + SEC-17 정적/API 2벌 + SEC-04 재측 추가) ·
`docs/agent/checkpoints/turn-y/_조율자.md`(로그인 창 셋 + **16:26 자진**) ·
`docs/agent/checkpoints/turn-y/N.md`

---

# 추가 ⑧ — 재측 둘 + **V_LOCK 첫 실측** (16:45)

## ★★★ `V_LOCK` 은 **반만 듣는다** — 내가 첫 표본이다

조율자가 「이 잠금이 몇 개 판정기에 듣는지 아직 아무도 안 셌다」고 했다. **세 팔로 갈랐다.**
잠근 뒤(`--lock --session-id turn-y-n` · `--status` 로 「잠김」 확인), **주인이 아닌 자격**으로:

| 팔 | 부른 법 | 결과 |
|---|---|---|
| ㉠ | **호스트**에서 `verify_ui_secrets --api` | **듣는다** — `[ALIVE] 로그인 건너뜀 — V 단독 중` → **exit 2 회색** ✔ |
| ㉡ | **컨테이너로 위임**하는 `verify_sidebar` | **안 듣는다** — **로그인 성공 4회** ✘ |

**왜인가 — 증명했다.** gx-shell 안에서 `v_lock` 을 직접 불렀다:
```
LOCK_PATH = /repo/docs/agent/evidence/V_LOCK
파일 있나 = False          ← 잠금 실물은 /docs/agent/evidence/V_LOCK 에 있다
is_locked() = False
```
`/repo/docs` 는 디렉터리로 존재하지만 **비어 있다.** 잠금은 `/docs` 쪽에 붙는다.
**턴 W 에 ISO-02 를 회색으로 만든 그 `/repo` 대 `/docs` 갈림과 같은 뿌리다.**

**⇒ 가장 나쁜 대목**: 로그인하는 판정기는 **대개 컨테이너로 위임한다**(호스트에 8000 포트가
없으므로). **즉 잠금이 가장 필요한 쪽에 가장 안 듣는다.**

**잠금이 닿는 범위 (셌다)**: 잠금을 **직접 아는 파일 11개**(그중 `verify_*` 는 4개) ·
`verify_route_alive.login` 을 빌려 **간접으로 닿는 판정기 11개**(`verify_sidebar` ·
`verify_perf_budget` · `verify_ui_secrets` · `verify_authn_paths` 등) → 합쳐 **약 14/79.**
**그러나 그 14 도 컨테이너로 들어가는 순간 0 이 된다.**

**고칠 자리는 한 곳**: `verify_authn_paths._ledger()` 가 이미 쓰는 **「두 자리를 다 본다」**
(`ROOT/docs` 와 `/docs`)를 `v_lock.LOCK_PATH` 에 넣으면 닫힌다.
★ **안 고쳤다**(P-203 · Q 소유). 조율자 대장 16:45 에 전 차선 앞으로 적었다.
★ **그때까지는 창 표가 여전히 유일한 보호막이다** — 잠금을 믿고 로그인하면 안 된다.

## 재측 ① PERF-04 — **색도 수도 안 바뀌었다** (여전히 회색)
F 가 안 쟀기 때문이 **아니다.** F 의 A/B 창은 `ab_*.json` 만 냈고, 이 판정기가 기본으로 읽는
자리는 **`PERF-04/budget.json`** 인데 그 파일이 **아직 09-15 자 그대로**다.
**A/B 와 예산 판정은 서로 다른 파일을 본다.** 회색을 걷으려면 **`budget.json` 을 새로 떠야**
하고, 회귀 칸이 살려면 **gunicorn+nginx 뒤에서** 떠야 한다(OPS-13).
★ **버리라 한 파일은 안 읽었다** — `ab_turny_F_switch_only.json` 이 출력에 **0회**.
  기본 증거 자리가 `budget.json` 이라 A/B 파일은 애초에 분모에 안 들어온다.

## 재측 ② UX-25 — **같은 수 · 같은 초록**
`V_LOCK` 주인으로, F 창이 닫힌 뒤. U1 5 · U2 7 · U4 2 · U5 4 · 금지 낱말 0 · 영문 메뉴명 0.
**16:26 의 오염은 수를 바꾸지 않았다.** 재측본을 증거 파일 끝에 붙였다.

## 귀속 — 15:53 을 내가 어떻게 알았나 (조율자 물음에)
**추론이 아니다. `docs/agent/checkpoints/turn-y/F.md` 80줄을 읽었다.** 그 줄은 F 의 자진 절
안에 있고 글자 그대로 이렇다:
> `- ★ **내가 N 의 창을 깼다.** 15:53~15:56 에 `verify_ga_readiness.py` 를 **인자 없이** 돌렸다.`
84줄에는 `**N 에게 15:53~15:56 재측을 권했다.**` 도 있다.
**나는 그 두 줄을 그대로 옮겼을 뿐이다** — 로그도 내 관측도 아니고 **F 의 체크포인트 자기
기술**이 출처다. F 가 지금 기록이 없다고 하면 **그 파일의 그 줄과 F 의 기억이 갈리는 것**이고,
어느 쪽이 맞는지는 내가 못 가른다. **귀속 미상이 옳다.**

## 잠금 뒷정리
`--unlock` 했고 `--status` 가 **「안 잠김」**, 파일도 없다. `scripts/v_lock.py` **0줄 고침.**
