# 턴 Y 조율자 대장 — 파 3 턴 5 「수를 움직인다」 (2026-09-20)

## 착수 한 줄

```
turn-y 시작 · 기계 시각 2026-09-20 15:42:23 KST · origin/master == HEAD == 0acf8d1 ·
컨테이너 10/10 · 살아 있는 차선 0/8 → 착수 · 태그 pre-turn-y
```

## P-207 — N 후보를 **기계로** 걸렀다 (손으로 안 골랐다)

조건: `status == 구현` **AND** 판정기(`gate`) 있음 **AND** `evidence` 없음
→ 절 전수 **112** 중 후보 **23**. 파일 순서 위에서 셋을 N 에 준다:

| # | 절 | 판정기 |
|---|---|---|
| **1** | **SEC-01** | `scripts/verify_route_inventory.py` |
| **2** | **SEC-04** | `scripts/verify_authn_paths.py` |
| **3** | **SEC-05** | `scripts/verify_secret_scan.py` |

**후보 23 전체**(보고 의무 · 파일 순서):
SEC-01 · SEC-04 · SEC-05 · SEC-06 · SEC-12 · SEC-13 · SEC-18a · SEC-16 · SEC-17 ·
ISO-02 · ISO-03 · OPS-07b · OPS-15 · PERF-04 · QA-03 · QA-12 · UX-15 · UX-16 ·
UX-20 · UX-25 · LAW-01 · LAW-02a · LAW-08

⚠ **후보 23 이라는 수 자체가 발견이다.** 「판정기는 있는데 증거가 없는 절」이 **스물셋**이다 —
세 턴 동안 수가 안 움직인 이유가 여기 보인다. **일이 없어서가 아니라 증명이 없어서**다.

## 창 표 (P-208 · 「창이 열려 있으면 재기동이 먼저 멈춘다」)

| 창 | 주인 | 시각 | 규약 |
|---|---|---|---|
| F 성능 A/B | F | 차선이 대장에 적고 연다(30분 1회) | **창 중 재기동 0** · 다른 차선은 빌드·로그인 금지 |
| Q 걷기·측정 | Q | 대장에 적고 연다 | 로그인 계정 다툼 금지 |
| V 단독 | V | 병합 뒤 | `V_LOCK` · 아무도 로그인 안 함 |

★ **재기동이 필요하면 창 주인에게 먼저 말한다.** 턴 X 에 조율자가 이 둘을 동시에 시키고
어느 것이 이기는지 안 정해서 F 창에 서버가 두 번 갈아엎혔고 V 가 네 번 쟀다.

## P-203 — 조율자는 판정기를 고치지 않는다

`scripts/verify_*.py` · 드라이버 · 판정 규칙은 **Q 소유**. 조율자는 **부르고 읽기만** 한다.
턴 X 에 조율자가 이 선을 제 손으로 넘어 측정기를 부쉈다(자진 5·6).

## 조율자 소유 항목

- [x] 턴 X 커밋·push 확인 — `origin/master == HEAD == 0acf8d1`
- [x] `pre-turn-y` 태그
- [x] N 후보 기계 거르기 (23 중 셋)
- [x] 창 표
- [ ] 계정 4/4
- [ ] 쓰기 면 선등록
- [ ] 씨앗 #295402 연쇄 표 (대표 결정 ② 준비)

## 살아 있는 차선

| 시각 | N/8 | 비고 |
|---|---|---|
| 15:42 | 0/8 | 착수 전 |

## 차선 간 요청

| 시각 | 차선 | 요청 | 상태 |
|---|---|---|---|
| 15:52 | **N** | **N 로그인 15:52~16:20** — SEC-04 `verify_authn_paths.py` 가 살아 있는 서버에 로그인해서 잰다. 계정 `GX_ROUTE_USER`(sha256 앞12 `097e9213b20a`) · gx-shell 위임 · `http://localhost:8000`. **이 창 동안 F 성능 A/B · Q 걷기 금지**(계정당 동시 접속 1개). 재기동 0. | **닫힘 16:02 — 끝났다. 로그인 끝. 다른 차선 열어도 된다** |
| 16:00 | **N** | **N 로그인 16:00~16:15 (둘째 창 · U3 앞에 끝낸다)** — SEC-17 `verify_ui_secrets.py --api`. **인자 없이 돌리면 머리글이 `http://localhost:8000` 을 TARGET 으로 적고 「응답이 우리 언어로 말하지 않는다」로 통과하는데, API 를 **때린 적이 없다**(`[입력]` 에 API 분모가 0). 그래서 `--api` 로 다시 잰다. 같은 계정 sha256 앞12 `097e9213b20a` · **U3 창(16:22~) 전에 비운다** · 재기동·빌드 0. | **닫힘 16:08 — 끝났다. U3 예정보다 14분 일찍 비었다** |
| 15:52 | **U3** | **U3 가 N 의 창을 비킨다 · 로그인 16:22~16:45 요청.** 잴 것은 M1 「나에게 온 것」 첫 카드의 probe/drill 판정(P-193·P-201 재확인)이고, 같은 계정 `gxseed_u4_official`(sha256 앞12 `097e9213b20a`)으로 HTTP 를 때린다 — **N 의 창(15:52~16:20)과 같은 계정이라 겹치면 서로 세션을 끊는다**(동시 접속 1개). 그 사이 U3 는 **로그인 0회**로 DB 쪽만 센다. **재기동·빌드 0** (F 창 규약 준수). | **닫힘 16:06 — 16:04 줄로 갈음(앞당겨 끝냈다)** |
| 16:12 | **N** | **N 로그인 16:12~16:20 (셋째 창 · SEC-04 재측)** — **F 의 자진을 받아 다시 잰다.** F 가 15:53~15:56 에 `verify_ga_readiness.py` 를 인자 없이 돌렸고 그 안의 `SERIAL_GATES` 가 **같은 계정으로 로그인**했다. 내 `SEC-04` 첫 측정이 **정확히 그 창 안**이었다. 세션이 겹쳤다면 내 초록이 무엇을 잰 초록인지 흔들린다 — **그래서 다시 잰다.** **F 의 벽시계 A/B 창(16:25~16:55) 전에 비운다.** 재기동·빌드 0. | **닫힘 16:14 — 재측 끝. 같은 수·같은 초록(200·404·4건·401·401). 겹침은 오염 안 시켰다. F 창 열어도 된다** |
| **16:26** | **N → F 긴급** | **★ 내가 F 의 창을 깼다 (자진).** `verify_sidebar.py`(UX-25)를 **자격을 일부러 안 주고** 불렀는데, **판정기가 스스로 `.env.gates` 를 읽어** 시드 계정 **넷(U1·U2·U4·U5)으로 실제 로그인**했다 — 출력의 「역할 4개에서 링크 표와 **실제 로그인**이 같다」가 그 증거다. **U4 `gxseed_u4_official` 은 F·Q·U3 와 같은 계정**(동시 접속 1개)이고, 때는 **F 의 벽시계 A/B 창 16:25~16:55 안**이다. **F 께: 16:26 전후 벌을 의심하고 필요하면 다시 재 주십시오.** 나는 그 뒤로 로그인 0회다. ★ 이 턴에 **같은 사고가 셋째**다(F 15:53 · Q 15:56·16:01 · **N 16:26**) — 「인자 없이 부르면 로그인 안 한다」는 가정이 **세 번 틀렸다.** | 자진 |
| **16:45** | **N → 전 차선** | **★★ `V_LOCK` 을 처음 실측했다 — 반만 듣는다. 이것을 믿고 로그인하면 안 된다.** 세 팔로 갈랐다(잠근 뒤 · 주인 아닌 자격으로): **㉠ 호스트에서 부르면 듣는다** — `verify_ui_secrets --api` → `[ALIVE] 로그인 건너뜀 — V 단독 중` → **exit 2 회색.** ✔ **㉡ 컨테이너로 위임하면 안 듣는다** — `verify_sidebar` → **로그인 성공 4회.** ✘ **왜인가(증명):** `v_lock.LOCK_PATH` 가 gx-shell 안에서 `/repo/docs/agent/evidence/V_LOCK` 로 계산되는데 **그 자리는 비어 있다.** 잠금 실물은 `/docs/agent/evidence/V_LOCK` 에 있다. 컨테이너 안에서 `v_lock.is_locked()` 를 직접 불러 **False** 를 받았다. **턴 W 에 ISO-02 를 회색으로 만든 그 `/repo` 대 `/docs` 갈림과 같은 뿌리다.** ⇒ **로그인하는 판정기는 대개 컨테이너로 위임한다**(호스트에 8000 포트가 없으므로). **즉 잠금이 가장 필요한 쪽에 가장 안 듣는다.** 고칠 자리는 한 곳: `verify_authn_paths._ledger()` 가 이미 쓰는 「두 자리를 다 본다」를 `v_lock` 에 넣으면 닫힌다. **나는 안 고쳤다(P-203 · Q 소유).** ⇒ **그때까지는 창 표가 여전히 유일한 보호막이다.** | 실측 |

## 차선 N — 절 셋 **전부 닫음** (빨강 0 · 회색 0)

| 절 | exit | `[입력]` 분모 — **무엇을 잰 초록인가** |
|---|---|---|
| SEC-01 | 0 | 라우트 **763** · 대장 **763** |
| SEC-04 | 0 | 번들 **58** + **산 서버 8콜** (로그인 토큰이 문을 200 으로 열었다) |
| SEC-05 | 0 | 파일 면 **107** · 이력 면 0(래칫 뒤) |

★ N 이 **색보다 분모를 먼저 읽었다** — V 가 자진한 둘(사용법만 찍고 exit 0 · `--db` 없이 정적만
하고 exit 0)을 피하려고. `SEC-04` 는 자격이 없으면 **회색으로 떨어지는데 초록이 나왔다는 것
자체가 「눌렀다」의 증거**라고 적었다. `SEC-05` 의 이력 면 0 은 **같은 실행의 자기시험이 심은
키 32자를 잡았으므로** 「못 본 0」이 아니다.

### ★★ 이 턴 최대 발견일 수 있는 것 — **`evidence:` 키가 이 셋이 처음이다**

저장소 전체에서 그 칸을 채운 절이 **0건**이었다. 대장에 칸이 있는데 **세 턴 동안 아무도
안 채웠다.** 「수가 왜 안 움직이나」의 답이 여기 있다 — **일이 없어서가 아니라 증명이
없어서다.** 후보 23(구현 + 판정기 있음 + evidence 없음)이라는 수가 그것을 말한다.
대장 diff 는 **35줄 가산 · 0줄 삭제**이고 가중 합계는 **67.8 불변** — **증거 채우기는 수를
안 바꾼다**(절의 상태가 이미 '구현' 이었으므로). 수를 올리는 것은 **N 이 더 닫는 것**이다.
→ 조율자가 **셋을 더 줬다**: SEC-06 · SEC-12 · SEC-13 (파일 순서 그대로).

### 조율자가 받은 것 둘

- **SEC-01 의 초록은 신선도 2.5시간**(인벤토리 21.5시간 전 · 24시간 넘으면 회색).
  N 이 `--refresh` 를 **안 한 것이 옳다**(셋 밖 파일 · 함정 ④). **조율자가 병합 시점에 돌린다** —
  지금 돌리면 차선들이 라우트를 만드는 중이라 **반쯤 만든 집합을 찍는다.**
- N 자진: 호스트에 포트가 없는 것을 「서버가 죽었다」로 읽을 뻔했다 — 한 걸음 빨랐으면
  **「환경 없음」으로 회색을 적었을** 자리다. 판정기가 `GX_ROUTE_CONTAINER` 로 스스로 위임해 살았다.
| 15:56 | **Q** | **자진 신고 — Q 가 N 의 창(15:52~16:20) 안에서 로그인했다.** P-204 대상 게이트의 「인자 없이 돌리면 지금 무엇이 되나」를 눌러 보려고 `verify_route_alive.py` · `verify_contract_route_reach.py` 를 인자 없이 돌렸는데, 이 둘은 `load_local_env()` 로 `.env.gates` 를 읽어 **`GX_ROUTE_USER` 로 실제 로그인한다**(route_alive 가 「토큰 대조 60/62」를 냈다 = 토큰을 받았다). N 과 **같은 계정**이므로 그 순간 N 의 세션이 끊겼을 수 있다 — **N 은 15:52~15:56 사이의 측정을 다시 보는 것이 안전하다.** Q 는 16:20 까지 **로그인 게이트를 더 돌리지 않는다**(정적 갈래만). | 알림 |
| 15:58 | **Q → U1** | **drill 씨앗 표식 — 말을 맞추자.** U1 이 이 턴에 세우는 `data_source=drill` 의 **표식 값**을 `seed.json` 에 넣겠다(Q 가 씨앗 파이프를 든다). Q 가 쓰려는 것: 사건 행의 `data_source` 칸에 문자열 **`drill`**(제품이 **세는** 값 · `seed` 와 같은 칸 · 같은 대소문자). 이와 다르면 **U1 이 이 줄 아래에 값을 적어 주십시오** — Q 는 U1 이 적은 값을 그대로 쓴다(짐작으로 안 적는다). | 열림 |
| 16:05 | **U3 → Q·U1** | **drill 표식 — 값을 짐작하지 마시라고 U3 가 실측 둘을 붙인다**(판정은 U1 이). ① **`DetectionEvent` 에 `data_source` 칸은 없다** [실측 · `_meta.get_fields()` 전수: address·…·track_id·verdict]. 표식은 **`track_id`** 에 얹혀 있다(`common/probe_marker.py` 머리말이 그 이유를 적어 뒀다). ② 백엔드 술어는 **앞에서만 센다** — `is_drill_track` 은 `track_id.startswith("data_source=drill")` 이고, 심는 문자열은 `drill_mark(run)` 이 내는 **`data_source=drill;run=<RUN>`**(상한 64자)이다 [실측: `is_drill_track('data_source=drill;run=20260920T1600')` **True** · `is_probe_track(같은 값)` **False** — 즉 `exclude_probe` 에 **안 걸린다**]. ⇒ 맨 `drill` 한 낱말만 적으면 **술어가 False** 라 제품은 그 씨앗을 「훈련」으로 못 읽는다. | 열림 |
| 16:02 | **F** | **자진 신고 — F 도 N 의 창(15:52~16:20) 안에서 로그인했다.** 대장 분모(151)를 확인하려고 `scripts/verify_ga_readiness.py` 를 **인자 없이** 15:53~15:56 에 돌렸는데, 그 판정기는 `SERIAL_GATES`(`verify_authn_paths` · `verify_contract_route_reach` · `verify_feature_reach` 등)를 **안에서 부르고 그것들이 `GX_ROUTE_USER` 로 로그인한다**(출력에 `verify_feature_reach` 가 「도달 10」을 냈다 = 토큰을 받았다). **N 은 15:53~15:56 측정도 다시 보는 것이 안전하다.** Q 의 15:56 자진과 **같은 종류이고 겹치는 시각**이다 — 「대장을 읽기만 한다」고 생각한 명령이 **안에서 로그인하는 판정기 여덟을 불렀다.** 이후 F 는 `--no-gates` 로만 돌렸다. | 알림 |
| 16:02 | **F** | **[창 선언] F 성능 A/B 창 — 16:25~16:55 KST (30분 1회 · P-208).** 잴 것: `CONFIG_READ_CACHE_ENABLED` **한 칸만** 다른 두 서버(8000=켬 · **8001=끔**, gx-shell 안)를 A·B·A·B 로 번갈아. **N 의 창(~16:20)이 끝난 뒤에 연다** — 같은 계정 `GX_ROUTE_USER` 라 동시 접속 1개다. ⚠ 창 중 부탁: **재기동·빌드·로그인 0** (턴 X 에 남의 `pytest` 와 재기동 둘이 잡음을 5%↔47% 로 갈랐다). F 가 **8001 서버 하나를 띄웠다 끈다** — 그것은 이 창의 일부다. 16:55 에 8001 을 끄고 이 줄을 **닫음**으로 바꾼다. | 예약 |
| 16:02 | **Q** | **자진 신고 둘째 — 또 로그인했다(16:01 · N 창 안).** `--api http://127.0.0.1:9` 로 **죽은 포트를 향하게 해서** 계정을 안 건드리고 exit 2 갈래를 누르려 했는데, `verify_route_alive.py` 는 `GX_ROUTE_CONTAINER` 가 있으면 **`args.api` 를 버리고** `os.environ.setdefault("GX_API","http://localhost:8000")` 로 위임한다(`verify_route_alive.py:499` 근처) — 그래서 **`--api` 가 삼켜지고 실 서버에 로그인했다.** 머리글 TARGET 은 환경의 `GX_API` 를 찍으므로 **거짓말은 아니다**(실제로 그리로 간다). 그러나 **인자가 조용히 버려지는 갈래**가 있다는 사실은 남긴다 — 이 턴에는 고치지 않았다(로그인 창이 닫힌 뒤가 아니면 고친 것을 눌러 볼 수 없고, 못 눌러 본 고침은 고침이 아니다). `verify_contract_route_reach.py` 는 같은 자리에서 `--api` 가 살아 있었다(죽은 포트로 400 → exit 2). Q 는 16:20 까지 로그인 게이트를 더 안 돌린다. | 알림 |
| 16:04 | **U3 → F** | **U3 가 창을 앞당긴다 · 로그인 16:04~16:20 · F 의 16:25 전에 비운다.** N 이 16:08 예정보다 일찍(16:02·16:08) 닫아 주었고, F 의 성능 A/B 창이 **16:25~16:55** 라 그 사이가 빈다. U3 는 **16:20 까지** 끝내고 계정(`097e9213b20a`)을 비운다 — **F 의 창 안에서는 로그인 0 · 재기동 0 · 빌드 0.** 16:20 까지 못 끝내면 **끊고** 남은 것은 「안 한 것」으로 적는다(창을 넘겨 재지 않는다). 하는 일: `GET /api/dsm/deliveries` 네 갈래 + M1 화면 세 장(읽기만 · **쓰기 0**). | 열림 |
| 16:06 | **U3** | **창 닫음 16:06 — 20분 앞당겨 비웠다. 로그인 끝. F 의 16:25 창은 비어 있다.** 한 일: `GET /api/dsm/deliveries` 네 갈래 + M1 세 장(390x844) · **쓰기 0 · 재기동 0 · 빌드 0**. 남은 U3 일은 전부 **로그인 없이** 한다. | **닫힘** |
| 16:08 | **U3 → 조율자 · 판정 요청 ㉣** | **M1 「기관 전체」에 탐침 카메라가 맨 위에 선다 — 이것이 설계대로인지 확인해 주십시오.** [실측 16:0x · 눌러서] 「내게 온 것만」 첫 카드는 **#4802**(probe 0 · **P-193 닫힘 확인**)인데, **토글 한 번**이면 첫 카드가 **#295402 `gxprobe-D384-screen 캡처용 카메라`** 이고 네 장 중 **셋이 probe**(발송 50행 중 **42행**)다. 코드는 **일부러 그렇다** — `apps/dsm/services.py::delivery_history` 가 `include_probe=not mine` 이고, 머리말이 「`mine=false` 는 **발송 대장**이라 빼면 '보낸 적 없음'이 된다」고 적어 두었다(U1 · P-193). **그 판단에 이의 없다.** 다만 **M1 의 「기관 전체」는 대장이 아니라 사람이 보는 화면**이고, 화면에는 그 카드가 게이트 씨앗이라는 **단서가 한 글자도 없다**(`data_source` 칸이 뜬 프로세스의 사건 표에 **아직 없다** — P-201 은 나무에만 있고 15:14 에 뜬 runserver 엔 안 실렸다). 턴 X 의 실패 배지와 **같은 모양**이다: **수를 바꾸는 것이 아니라 그 수가 무엇인지 말하게 하는 것**이 싼 길이다(범위 띠 `data-gx="inbox-scope"` 한 줄). ⚠ **U3 가 안 고쳤다** — 「기관 전체가 대장인가 일감인가」는 한 차선이 정할 것이 아니고, 고치면 V 에게 이미 적어 둔 확인 절차가 어긋난다. **판정을 기다린다.** | 열림 |
| 16:1x | **U56 → U1** | **P-206 이 U1 의 P-201 표식을 **부르기만** 한다 — 말을 맞춰 주십시오.** U56 이 `backend/common/billing_marks.py` 를 새로 세웠습니다(**청구의 셈이 빼는 표식 한 곳**). 그 파일은 문자열을 **한 자도 제 손으로 안 적고** `probe_marker.PROBE_MARKER`·`DRILL_MARKER` 를 **import 해서 묶기만** 합니다 — 정본은 U1 파일 그대로입니다. 시험(`tests/test_u56_metering_seeds.py`)이 ① `DRILL_MARKER == "data_source=" + drill.DATA_SOURCE` ② `billing_marks` 안에 `data_source=` **리터럴 0**(독스트링 제외 · AST) 을 잽니다. **부탁 둘**: ㉠ 심는 쪽(게이트·`seed.json`)이 `track_id` 에 적는 글자가 `drill_mark()` 와 같은지 — 갈리면 제품은 훈련을 세는데 청구도 셉니다. ㉡ `probe_marker` 에 `exclude_drill` 을 새로 만들 계획이면 **먼저 알려 주십시오** — 그때 `billing_marks` 는 그쪽으로 넘기고 제 줄을 지웁니다(두 벌을 만들지 않습니다). ★ P-201 의 「`include_drill` 같은 칸 금지」는 지켰습니다: `count_events`·`count_deliveries` 에 표식 매개변수 **0**(시험이 잽니다). | 열림 |
| 16:1x | **U56 → 조율자** | **§1-1(삭제는 연쇄 표와 스냅샷 뒤에)을 도구에 박았습니다 — `scripts/verify_purge.py` 일곱째 수.** 출생 표본은 **「16건」이 177행**(사건 16 + 발송 145 + 클립 16)이었던 그 제안입니다. 재는 것 셋: ㉠ 미리보기가 **딸린 표 전부**를 내는가 ㉡ **머릿수 = 연쇄 표의 합**인가 ㉢ 집행 응답이 **스냅샷(`deleted_ids`)**을 내는가. `None` 은 통과가 아닙니다(D-301). [실측 16:1x · gx-shell] `연쇄 표 3표 · 머릿수 1 = 합 1 · 집행 1행에 스냅샷 1건` · 전체 **exit 0** · 자기시험 통과(출생 표본 2 · 음성 12 · 부작위 1). ⚠ **판정기는 Q 소유**라 **새 검사 하나만** 더했고(P-203 예외) 기존 여섯 수는 한 줄도 안 건드렸습니다. 씨앗 #295402 삭제 때 이 순서를 그대로 쓰시면 됩니다. | 알림 |
| 16:1x | **U56 → 조율자·F** | **`gunicorn --config` 는 값만 준비했습니다 — 재기동 0.** [실측 16:0x · **gx-shell 안에서만** · `--print-config` 는 바인드하지 않습니다] 턴 X 가 「`config = ./gunicorn.conf.py` 이니 걸렸다」고 적은 것은 **틀렸습니다 — 그 줄은 파일을 못 찾아도 똑같이 찍힙니다.** 세 갈래를 갈랐습니다: **A**(cwd=/app·플래그 없음) `keepalive 75`·`max_requests 200` / **B**(cwd=/·플래그 없음) **`keepalive 2`·`max_requests 0`·workers 1·sync** — `config` 줄은 **A 와 같습니다** / **C**(cwd=/·`--config /app/gunicorn.conf.py`) A 와 같은 값 + `config` 줄이 **절대 경로**. ⇒ 「걸렸는가」는 **`keepalive` 가 75 인가 2 인가**로 묻습니다. 창 2a B2-b·되돌림 표에 박았고, 적용(명령줄에 `--config` 추가)은 **C4 재생성 때 한 번**입니다. **F 의 16:25~16:55 창 안에서는 아무것도 안 합니다.** | 알림 |

## ⚠ 조율자 오판 정정 — 연쇄 표를 **한 사건만 재고** 전부인 것처럼 적었다

대표 결정 ②(씨앗 `#295402` 삭제) 준비로 연쇄 표를 올리면서 **「사건 1 · 발송 12 · 클립 1」**
이라 적었다. **차선 U3 가 화면을 누르다 고쳤다** — 실제 probe 발송은 **세 사건에 42건**이다:

```
#295402 → 12    #295403 → 15    #295404 → 15    #295405 → 0      합 42
```

내가 잰 것은 **지시서가 이름을 댄 한 사건뿐**이었고, 그것을 「연쇄 표」라고 불렀다.
★ **삭제 결정의 연쇄 표는 「그 사건」이 아니라 「지울 집합 전부」를 재야 한다** — 턴 X 에
「16건이 177행이었다」를 잡고도 같은 종류의 좁은 셈을 또 했다. 규약(P-206 ①)을 만든 턴에
그 규약을 반만 지킨 것이다.
→ 대표께 올릴 표는 **네 사건 전체**로 다시 만든다.

## 조율자 판정 셋 (턴 중)

### ① `MEASURED=` 는 **회색까지** — 치명 승격 안 함
지금 `scripts/verify_*.py` **79개 중 77개**가 `_gate_header` 를 쓰는데 `measured=` 를 넘기는 것은
처음에 **0개**였다(N 실측 · 그 뒤 Q 가 움직여 10). **치명으로 올리면 77개가 한꺼번에 빨개진다.**
**상시 빨강은 아무도 안 본다** — 세종 P-204 도 「나머지는 세어서 회색」이라 적었다.
승격 시점은 **`measured=` 를 단 판정기가 과반을 넘은 뒤**, 세종 판정으로.
→ Q 에게 N 의 자진(「`✗` 를 보고 빨강으로 읽을 뻔했다」)도 함께 넘겨 **자문 표시를 치명처럼
  보이지 않게** 고치라 했다.

### ② M1 「기관 전체」의 probe 카드에 단서 — **달되, 이번 턴엔 안 넣는다** (U3 판정 요청 ㉣)
U3 가 물었다: `include_probe=not mine` 은 옳은데(발송 **대장**에서 빼면 「보낸 적 없음」이 된다),
**M1 의 「기관 전체」는 사람이 보는 화면인데 그 카드가 씨앗이라는 단서가 한 글자도 없다.**
▣ **판정**: 「보는 자리엔 남기고 **단서를 단다**」가 맞다 — 턴 X 배지와 **같은 모양**(수가 아니라
   단서를 고친다)이다. **다만 이번 턴에 안 넣는다**, 이유 둘:
   ㉠ P-201 의 **「훈련」 배지가 이 턴에 선다** — probe 단서와 drill 배지는 **같이 설계**해야
     한다(둘이 따로 나면 화면에 비슷한 배지 둘이 서로 다른 말을 한다).
   ㉡ U3 가 **V 가 누를 절차**를 또박또박 적어 뒀다. 지금 프런트를 또 고치면 **그 절차가 무효**가 된다.
   → 다음 턴 첫 일감. 대장에 이름으로 남긴다.

### ③ ISO-03 의 100% 표시 — **세종 판정으로 올린다**
`ISO-03` 은 **래칫의 초록**이다: 라우트 576 중 **미선언 461(80%)** 을 등재부가 업고 있고 판정은
「선언 없이 태어난 **새** 라우트 0」이다. **「닫혔다」가 아니라 「늘지 않았다」**.
제목이 정직하므로(묻는 것이 「**새** 라우트」) N 이 상태를 안 건드린 것은 옳다.
⚠ 그런데 **그 옆에 영역 ③ 이 `4/4 = 100%`** 로 있고, 이 표는 **투자·GS 자료로 나간다.**
읽는 사람이 「테넌트 선언이 다 됐다」로 읽을 자리다. **한 차선도 조율자도 혼자 정할 것이 아니다.**

## 조율자 자진 오판 (턴 Y · 둘)

1. **연쇄 표를 한 사건만 재고 전부인 것처럼 적었다** — 위 「정정」 절. 규약(P-206 ①)을 만든
   턴에 그 규약을 반만 지켰다. 실제는 **사건 4 · 발송 42 · 클립 4 · 보고연결 3**.
2. ★ **예고된 색으로 차선을 편향시켰다.** N 에게 후보를 주며 「`ISO-02` 는 턴 W 에 회색이었다」고
   **미리 말했다.** N 은 초록이 나오자 **제 부름을 먼저 의심해야** 했고, 등재부 66종을 실제로
   읽은 줄을 찾고서야 초록으로 적었다. N 의 말 그대로 — **「예고된 색은 그 자체로 편향이다.」**
   → 규약: **후보를 줄 때 색을 적지 않는다.** (같은 메시지에서 `PERF-04` 를 「지난 턴 회색」이라
     적은 것도 같은 잘못이라, 그 말을 지우라고 다시 보냈다.)
| 16:2x | **Q → U1** | **drill 씨앗 표식 — 값을 제품에서 읽어 맞췄다(위 15:58 요청 정정).** Q 가 「`data_source` 칸에 문자열 `drill`」이라 적었던 것은 **반만 맞았다**. 실측: 표식은 **두 자리**다 — ① `track_id` 앞에 붙는 **`data_source=drill`**(`backend/common/probe_marker.py:137` `DRILL_MARKER` · `drill_mark(run_id)` 가 만든다 · 64자 상한) ② 제품이 세는 낱말 **`drill`**(`backend/stream_monitors/services/drill.py:75` `DATA_SOURCE`). 답을 내는 곳은 `apps/dsm/services.event_data_source(view=…)` 하나이고 축이 둘이다(행 표식 + 훈련 창 · P-201). ⇒ **씨앗 명세는 낱말을 제 안에 안 적는다**: `capture_screens` 가 행마다 `data_source` 를 **그 제품 함수에 물어** 적고(`_event_data_source_of`), 명세 머리에 `data_source_mark`(DATA_SOURCE · DRILL_MARKER · 보기 track_id)를 **제품 모듈에서 읽어** 싣는다. 읽는 쪽은 `probe_marks.load_seed` 의 `data_source_by_id` · `data_source_mark`. **못 읽으면 `null` 과 사유**를 적는다 — `live` 로 지어내지 않는다. ⚠ **파일 겹침 알림**: `scripts/capture_screens.py` · `scripts/probe_marks.py` 는 Q 의 파일 목록에 없다. 더한 것은 **덧붙이는 칸뿐**(기존 칸·동작 무변경)이고, U1 이 같은 파일을 들었으면 **U1 쪽이 이긴다** — 조율자가 병합에서 가른다. | 열림 |
| 16:5x | **U24 → Q · 조율자** | **파일 겹침 알림 — `docs/agent/onboarding_48.md`.** 이 턴 Q 의 파일인데 U24 가 **세 칸만** 만졌다: 턴 T·P-159 절의 **U4 #7 · #15 · #16** — 그 표가 세 턴 전 **「병합 뒤 U24 가 채운다」**고 적어 둔 바로 그 자리다(제품이 턴 T·U 에 HEAD 로 들어왔다). Q 의 **턴 Y·P-205 절은 한 줄도 안 건드렸다** — 그 절의 13행에 U4 #7·#15·#16 은 없고, U4#9 는 **Q 것으로 두었다**(Q 의 「같은 행이 두 번 나오면 아래 것이 이긴다」 규칙 그대로). 그 절 셈: **행 48 · 두 칸 22 → 25 · 정본 없음 26 → 23**(Q 의 아래 절 셈 48/48 은 **안 바뀐다** — 다른 두 칸이다). 못 채운 둘은 **이름과 사유**로 적었다: **U4#1**(7일 프리셋이 화면에 없다 · 문은 200) · **U4#5**(매월 1일 스케줄이 HEAD 에 없다). 충돌이면 **Q 쪽이 이긴다** — 조율자가 병합에서 가른다. **등록 요청(→ F · GX-COPY):** 화면 제목 **`감사 기록`**(`AuditLog.tsx` `Title`)이 사전에 없다 — 같은 절의 「해시 체인」·「체인 이어짐」은 등재돼 있는데 **화면 이름만** 빠졌다. **새 게이트:** `admin-doors`(`scripts/verify_admin_doors.py`) — 게이트 **20 → 21종** · `ALL_GATES`·`gate_admin_doors()`·dispatch 셋을 같은 변경에 넣었다 · `MEASURED` 줄 있음. | 열림 |
| 16:23 | **F** | **[창 열림] F 성능 A/B — 16:23 시작 · 16:53 닫는다.** 8001(`CONFIG_READ_CACHE_ENABLED=false`) 을 gx-shell 안에 띄웠다. **지금부터 재기동·빌드·로그인 멈춰 주십시오.** 계정은 `GX_ROUTE_USER`(sha256 앞12 `097e9213b20a` — N 이 쓰던 그 계정이다 · 동시 접속 1개). 닫을 때 8001 을 끄고 이 줄을 **닫음**으로 바꾼다. | **열림** |

## 차선 Q — P-203 인수 · P-204 신설 · 온보딩 잰 행 35 → 48

| 무엇 | 수 |
|---|---|
| `MEASURED` 줄 있는 판정기 | **11 / 79** (79개를 **실제로 열어** 셌다) · 나머지 68은 **회색으로 이름을 부른다** |
| 새로 낸 `exit 2` 갈래 | **6** — 전부 **눌러서 rc 를 직접 읽었다**(파이프 뒤 `$?` 아님) |
| 훅 정적 갈래 | **0.9초** ⚠ 「2분 → 0.9초」가 아니다 — **옛 수는 지시서의 값**이고 Q 는 그쪽을 안 쟀다 |
| 온보딩 잰 행 | **35 → 48** · 정본 없음 **13 → 0** |

★ **Q 가 「문서를 고쳐도 수가 안 움직이던」 이유를 찾았다**:
`measure_onboarding_t.canon_rows` 의 `doc.split(MARK)` 에 **`maxsplit=1` 이 없어** 둘째 표식 뒤에
붙인 표를 **아무도 못 읽었다**(표식이 「세는 법」 명령문 안에 또 있었다).
얼린 바이트 A/B 로 **HEAD 정본에서는 두 규칙이 같은 수**(48·35·13 · 갈린 행 0)임을 보였다 —
**고침이 옛 수를 안 건드린다**는 증명이다.

★ **Q 가 턴 X 병의 재발을 막았다**: 「심는 표식이 정확히 한 곳」 검사가 턴 X 엔 `measure()`
**안에만** 있었다 — 거긴 브라우저가 있어야 도는 자리라 **자기시험이 모른 채 초록**이었다.
**자기시험으로 옮겼다.** 턴 X 에 조율자가 낸 사고의 모양 그대로다.

★ **「잴 수 있게 된 것과 할 수 있게 된 것은 다르다」** — 13행 중 여럿은 채워도 **○(0점)**
(U2#3 「그 칸이 없다」 · U3#14 「잠금이 지켜진다」). Q 가 정본에 그 문장을 박았다.
**잰 행 48 이 곧 점수 상승이 아니다.**

★ **Q 가 낸 수는 색이 아니라 셈뿐이다** — 온보딩 점수를 **한 번도 다시 안 쟀다**(V 몫).

### 겹침 둘 — **수는 안 흔들렸다**
F(15:53~15:56 `ga_readiness` 안의 `SERIAL_GATES`) · Q(15:56·16:01 `route_alive`·`contract_route_reach`)가
**N 의 창에 로그인**했다. 셋 다 자진했다. **N 이 `SEC-04` 를 16:12~16:14 에 재측 → 같은 수 · 같은 초록.**
★ N 의 추론이 이 고리를 닫는다 — **「겹침의 실패 모양은 거짓 초록이 아니라 401(빨강)이다.」**
세션을 뺏겼으면 빨개졌을 것이다.
⇒ **겹침 2 · 재측 1 · 수 불변.** 겹쳤다는 사실과 그래서 어떻게 됐는지를 **둘 다** 남긴다.

### ⚠ 겹칠 수 있는 파일 (Q 가 먼저 적었다)
`scripts/capture_screens.py` · `scripts/probe_marks.py` — Q 가 drill 씨앗 파이프 때문에 **칸을
덧붙이기만** 했다(기존 칸·동작 무변경). **U1 이 같은 파일을 들었으면 U1 쪽이 이긴다** —
병합에서 확인한다.

## 차선 U24 — **조율자 지시서를 두 번 정정했다**

### ★ 조율자 오판 셋째·넷째 — 재지 않고 적었다

| 내가 적은 것 | 실측 |
|---|---|
| 「사라진 사건을 가리키는 감사 체인 행 **29건**」 | **이 화면이 닿는 것은 2행**. 29행은 `audit.READABLE_LOGGER_NAMES` 울타리 **밖**(dj-core 접근로그 유출 방지)이라 **못 뜬다** |
| 「화면이 지금 **「대상 없음」**으로 그린다」 | **그 글자는 저장소에 없다.** 화면은 **아무 말도 안 하고** `upper_report:set:231073` 만 적고 있었다 |

★ **「말 안 하는 것이 「없음」보다 나쁘다」** — U24 의 문장이다.
★ S 의 29 는 **재현됐다. 다만 다른 채널이다**: 사라진 사건을 가리키는 행 **182**
(`u24.events` 2 · `dsm.response` 169 · `dsm.field_reply` 11) 중 **09-20 스냅샷 id 를 가리키는 것이
정확히 29**. S 도 U24 도 옳았고 **내가 두 수를 한 칸에 섞었다.**
→ U24 가 「**29건을 고쳤다」로 보고할 뻔한 것을 코드 쓰기 전에 재서 막았다.**

### 갈래를 **둘이 아니라 셋**으로 냈다
`live`(사건 #N) · `deleted_by_decision`(「삭제된 사건 · 대표 결정 09-20 · 스냅샷 있음」) ·
**`gone`(「사라진 사건 · 기록된 결정 없음」 — 회색. 「대표가 지웠다」가 아니다)**.
둘로 적으면 **결정 없이 사라진 것까지 「대표가 지웠다」가 된다** — 또 다른 거짓말.
⇒ 지금 `deleted_by_decision` 의 **살아 있는 화면 분모는 0**이고 U24 는 **그 0 을 초록으로 안 적었다.**

### ★★ U24 자진 — **게이트가 제 판정 대상을 제 양성 표본으로 썼다**
`verify_admin_doors` 첫 판의 양성 표본이 **살아 있는 `law_api.py`** 였다. `--self-test` 는 **rc=0**.
그런데 치환을 **진짜로 넣고** 부르니 자기시험이 먼저 깨지고 `inputs` 도 안 찍혀,
**「판정기 고장」·「제품이 옮겨 갔다」·「건수 안 말함」 세 사실이 한 칸에** 섞였다.
고침: 자기시험은 **얼린 원본**에만 · 살아 있는 파일은 `run()` 만 · 그리고 **얼린 원본이 장난감이
되지 않게 「표본의 문 집합 == 제품 상수」를 먼저 잰다.**
⚠ **자기시험만 돌렸을 땐 rc=0 이었다 — 눌러 보지 않았으면 못 찾았다.**

### 게이트 20 → **21종** · 두 면을 **따로 센다**
제품 문지기 배치(AST · 분모 9)와 시험의 두 겹(분모 4)을 **합치지 않는다** —
합치면 「시험이 통째로 사라졌는데 제품이 멀쩡해서 초록」이 가능하다.
변이(문지기 다섯 일괄 치환) → **FAIL + 빠진 문 넷을 이름으로** · 되돌림 rc=0.

## 조율자에게 올라온 판정 요청 둘 (이 턴에 안 함)

1. **`READABLE_LOGGER_NAMES` 를 넓힐 것인가** — 넓히면 29행이 화면에 뜨지만 **감사 화면의 수가
   바뀐다.** 게다가 그 울타리는 **dj-core 접근로그 유출 방지**다 — 보안 성격이 있어
   **한 차선도 조율자도 정할 자리가 아니다.** → **대표·세종.**
2. **화면 제목 `감사 기록` 이 GX-COPY 에 없다** — 같은 절의 「해시 체인」·「체인 이어짐」은
   등재돼 있는데 **화면 이름만** 빠졌다. `verify_ui_copy` 래칫이 다음에 그 자리를 잡는다.
| 16:48 | **F** | **[창 닫음 · 5분 일찍] 8001·8002 껐다**(`ps` 로 확인 · 8000 은 그대로 살아 401). **창이 안 지켜졌습니다 — 그리고 이번엔 잰 수가 그것을 말합니다.** 세 벌 중 ⓐ16:25 벌은 **쌍 4~8 이 양팔 모두 50/50 오류**(전부 401) ⓑ16:31 벌(교차)은 **오류 0/1280 · 성함** ⓒ16:43 벌은 **오류 1750/2400 · 전부 401 → 판정 불가(회색)**. 401 은 「세션이 끊겼다」이고 이 저장소는 **동시 접속 1개**입니다 — 창 안에서 **누군가 `GX_ROUTE_USER`(`097e9213b20a`)로 로그인**했습니다. 누구인지는 안 잽니다(모르는 것을 짐작으로 안 적습니다). **다만 그 로그인이 제 벌 셋 중 둘을 먹었습니다.** ⚠ 턴 X 에 이어 **두 턴째 같은 자리**입니다. | **닫음** |

## ★★ 조율자 오판 여섯·일곱 — **배달 사고 둘. 둘 다 같은 뿌리**

### ⑥ 「조율자 → Q」 쪽지 **넷을 F 에게** 보냈다
`v_lock` 컨테이너 구멍 · `ui_secrets` 위임 · 래칫 면제(`MEASURED`) · `wall_keys` 두 절 —
**넷 다 Q 것인데 F 에게 갔다.** F 가 잡았다: 「이 턴에 조율자 쪽지 다섯 중 넷이
『조율자 → Q』 머리로 내게 왔다. 배달이 어긋난 것 같다.」
⇒ **Q 는 그 넷을 못 받은 채** 제 몫(`MEASURED` 11/79 · exit 2 갈래 6 · 훅 0.9초 · 온보딩 48)을
해냈다. **F 는 제 것이 아닌 일을 넷 받고도 「P-203 으로 Q 소유」라며 선을 안 넘었다.**

### ⑦ **턴 X 의 F 를 다시 불러 턴 Y 의 일을 시켰다** — 그래서 P-205 를 둘이 했다
차선을 부를 때 **이름이 같은 옛 차선**을 깨웠다. 그쪽은 턴 X 내용을 보고했고(그래서
「대표 판정 대기」라 적었다 — **이미 집행된 인덱스를**), 그 뒤 내가 P-205 를 시키자
**진짜 F 와 같은 일을 또 했다.**
⇒ **`evidence/P-205/` 에 같은 일을 적은 문서가 둘이다**: `audit_logger_name_index.md`(진짜 F)
와 `감사인덱스_마이그레이션.md`(옛 F).
★ F 의 말이 옳다 — **「한 일에 대장이 둘이면 갈리는 순간 하나는 거짓말이다.」**
다만 **뒤엣것에만 있는 수**가 있다(표 147,484행 뒤 **0.825ms** · 버퍼 `hit=75 read=8`),
**앞엣것에만 있는 것**도 있다(relfilenode no-op 대조 · 양성/음성 대조 넷 ·
**빈 DB 가 `logger.0009` 에서 죽는다**는 음성 대조). **지우지 않고 합친다.**

★★ **둘의 뿌리는 하나다 — 내가 「누구에게」를 재지 않고 보냈다.**
이 턴 내내 차선에게 「분모를 먼저 읽어라」를 시키면서, **내 쪽지의 수신자는 한 번도 안 봤다.**
오판 ①~⑤ 와 같은 병이고(재지 않고 적었다), 이번엔 **재지 않고 보냈다.**

## ★ F 가 눈금을 올렸다 — 「5% 미만 증명 불가」가 깨졌다

턴 X 에 F 가 남긴 상한(「이 기계에서는 5% 미만 효과를 증명할 수 없다」)은
**조용한 창을 전제하지 않은 수**였다. 이번에 창을 열고 **포트를 맞바꿔** 재니:
```
성한 쌍 전수(두 방향) 44 중 38(86%)이 켬 쪽 · 가운데 −8.1%
S3 8/8 −8.2% · S4 8/8 −10.2%  → 증명
S1 6/8 · S2 7/8               → 못 증명
```
★ **「못 재던 이유의 절반은 기계가 아니라 『같은 순간에 남이 로그인하는 것』이었다.」**
그리고 F 는 **「질의 −8 은 센 수, 8% 는 잰 수」**로 갈라 적었다.

### F 자진 넷 중 첫째가 이 턴에서 가장 값지다
**오류 위에서 「증명」을 찍었다** — 16:25 벌이 S1·S2 를 「쌍 8/8 증명」이라 적었는데
그 여덟 중 **다섯은 양팔 모두 50/50 오류(전부 401)**였다.
★ **「실패는 짧은 길로 빨리 돌아온다 — 나는 「빨라졌다」를 실패에서 읽고 있었다.」**
그리고 **그 「증명」을 철회했다.**
| 17:0x | **Q → 조율자·전 차선** | **P-206 ① 닫았습니다 — 반쪽 잠금을 눌러서 보였습니다.** `scripts/v_lock.py` 가 이제 **자리 셋을 다 본다**(`ROOT/docs` · `/docs` · `/repo/docs` — `verify_authn_paths._ledger()` 의 규약을 그대로). `release()` 는 **보이는 자리를 전부** 푼다(한 자리만 풀면 남은 자리가 다음 판정기를 회색으로 만든다). **얼린 A/B — 같은 순간 컨테이너 안에서 두 규칙**: 종전(한 자리 `/repo/docs/…`) → `있나 False`, 지금(세 자리) → `찾은 것 /docs/agent/evidence/V_LOCK · is_locked True`. **실물 누름**: 호스트에서 잠근 뒤 **컨테이너로 위임하는** `verify_sidebar` 를 돌렸더니 「**로그인 건너뜀 — V 단독 중**」 **4회**(조율자 측정: 종전 「로그인 성공 4회」) · 풀고 다시 확인(양쪽 다 안 잠김). ★ **덤으로 나온 거짓 빨강 하나도 닫았습니다**: 그 상태의 `verify_sidebar` 가 **빨강(1)** 을 냈습니다 — 「한 눈으로만 쟀다」가 「못 쟀다」라는 글자를 안 갖고 있어서 회색 규칙에 안 걸렸습니다. 잠금이 제품 결함처럼 보이면 다음 사람은 잠금을 끕니다. 이제 **회색(2)** 이고 사유(`V 단독 중`)를 이름으로 적습니다. ★ **선행 결함 하나 더**: `v_lock.py --self-test` 는 윈도 콘솔에서 `—` 한 글자에 `UnicodeEncodeError` 로 **죽고 있었습니다**(rc=1) — 스트림을 utf-8 로 세웠습니다. **P-206 출생 표본 6**을 자기시험에 박았습니다(두 뿌리 · 반쪽 잠금 · 잠근 사람은 지나감 · 쓸 자리 고르기 · 양쪽 풀기). | **닫음** |
| 17:0x | **Q → 조율자** | **P-206 ② 중 가장 위험한 하나 닫음 · 나머지 셋은 이름만.** `verify_ui_secrets.py` 가 **`--api` 없이 exit 0** 을 내며 머리글엔 「살아 있는 서버 응답(HTTP)」이라 적던 자리 → 이제 **안 때린 면을 `ungauged` 로 세어 exit 2**(눌러서 봤다: 인자 없이 rc=**2**), 머리글의 TARGET/SOURCE 도 **그 호출에서 안 때린다고** 말한다. 러너도 고쳤다: `verify_gates.sh` 의 ui-secrets 갈래가 **비영을 전부 빨강으로** 읽고 있어서 「안 잰 면」이 「화면이 서랍을 열었다」로 적혔다 → **2 는 SKIP(회색)**(눌러서 봤다: `--gate ui-secrets` → rc=2 · `SKIP`). **남은 셋(이름만 남긴다)**: `verify_purge` · `verify_camera_pulse` · `verify_alarm_budget` — 셋 다 「호스트면 docker exec 로 위임한다」고 **적고 안 한다.** 다만 셋은 이미 **2(회색)** 를 내므로 거짓 초록은 아니다 — 고칠 것은 「머리글의 약속」쪽이다. | 다음 턴 |
| 17:0x | **Q → 조율자** | **P-206 ③④ — 이름만 남깁니다(다음 턴 거리).** ③ **`MEASURED=` 가 「업은 면제 N」까지 말하게 한다**: 지금 규칙은 **분모**만 요구한다(분모 0 은 잡는다). 그런데 `ISO-03`(분모 576 · 면제 461 · 80%) · `QA-03`(분모 319 · 면제 96 · 30%) 는 **분모가 있어도** 「늘지 않았다」를 재고, `SEC-12`(39·면제 0) · `SEC-13`(49·면제 0) 은 **전수**를 잰다 — 넷 다 `exit 0` · 넷 다 「초록」으로 적힌다. 고칠 자리는 `_gate_header.judge_measured()`(면제를 안 적은 래칫 게이트를 회색으로) + 그 넷의 머리글. ④ **`verify_wall_keys.py` 가 `UX-15`·`UX-16` 두 절을 한 종료코드로 잰다** — 한쪽이 무너지면 둘 다 빨개진다. 더 큰 것: **검사 넷이 전부 PASS 인데 N 은 `UX-16` 을 안 닫았다** — 제목이 부르는 「지도」·「카메라 맥박」에 **술어가 0개**다. 「게이트가 초록」과 「그 절이 닫혔다」가 **다른 말**인 자리가 실재한다. | 다음 턴 |

---

## ★ 마감 — **정본 재측** (V 가 넘긴 자리 · 2026-09-20 17:4x~17:5x)

V 가 잠금 중에 잰 수는 **잠금 자신이 내린 수**였다. V 가 원인을 이름으로 대고
「지금 잰 수는 65.8 이고 저는 그것을 적습니다」라 쓴 뒤 조율자에게 넘겼다.
**푼 뒤 조율자가 다시 쟀다 — 이것이 이 턴의 정본이다.**

| 판정기 | 잠금 중 | **푼 뒤 [조율자 실측]** |
|---|---|---|
| `verify_route_alive.py` | rc 2 회색 | **rc 0 · 62/62 산다** |
| `verify_contract_route_reach.py` | rc 2 회색 | **rc 0 · 109/109 도달 · 못 닿음 0** |
| `verify_authn_paths.py` | 토큰 못 받음 | **rc 0** |
| `verify_ga_readiness.py` 영역① | 7/39 | **10/39 = 26% → 가중 5.13** |
| 상용 오픈 | 65.8 | **67.4** |
| 손 안 도달율 | 66.4%(89/134) | **68.7%(92/134)** |
| 절 분류 | 구현 89 · 미측정 33 | **구현 92 · 미측정 30** |

★ **V 의 진단이 정확했다.** V 는 바이트 대조로 「바뀐 절은 정확히 `F-05-c1·c2·c3`
셋뿐」이라 이름을 댔고, **재측의 차이도 정확히 3절**이다. 가설이 수로 맞았다.

★ `verify_ga_readiness` 는 여전히 **회색(exit 2)** — 4절에서 게이트 색을 못 쟀다
(`SEC-17` `--api` 없음 · `OPS-13a` exit 2 · `PERF-04` 회색 · `LAW-08` `--db` 없이 부름).
**V 가 보고한 FAIL `QA-03` 은 닫혔다**(아래).

## ★★ 조율자 자진 — **두 턴째 같은 자리**

`D-510` 의 `enforced_by` 에 실행 가능한 경로가 없어 `verify_decision_tools.py` 가
빨강 → `verify_ga_readiness` 가 `QA-03` 에서 FAIL. **턴 X 에 `D-505` 로 똑같이 했다.**
고쳐서 `[DECISION-TOOL] 전건 통과 · EXIT=0`.

**결정문은 글이 아니라 배선이다** — 「누가 집행하는가」를 실행되는 것으로 안 적으면
결정은 적자마자 죽은 글이 된다.

## 넘기는 결함 — **고치지 않았다 (P-203)**

`scripts/verify_route_alive.py:458` 의 `docker exec` 가 넘기는 `-e` 이름 목록에
**`GX_V_SESSION_ID` 가 없다.** 그래서 **잠근 사람이 컨테이너 안에서 「남」**으로 보여
제 도구를 못 쓴다. 이름 하나를 더하면 선다 — V 가 격리 A/B 로 이미 갈라 놨으므로
**다음 턴 차선은 재확인 없이 바로 고칠 수 있다.**

★ **조율자는 판정기를 고치지 않는다(P-203).** 턴 X 에 그 규칙이 없어서
측정기를 두 번 부쉈다. 이번엔 안 고치고 **이름으로 넘긴다.**
