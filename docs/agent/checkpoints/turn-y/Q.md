# 턴 Y · 차선 Q (정본 · 판정기 · 걷기) — 체크포인트

기계 시각 기준 · 브랜치 `turn-q` · HEAD `0acf8d1` · **커밋하지 않는다**

---

## 닫음

### ① 조율자 고침 둘 **인수** (P-203) — 자기시험 안으로 들어왔다

`scripts/verify_click_completes.py --self-test` 이 이제 넷을 전부 **브라우저 없이** 묻는다:

- 출생 표본 **12** — 「`0` 은 값이다」(양성 7 · 음성 5)
- 출생 표본 **7** — 「없음 → 값」은 회색 · **세는 수와 갈래값을 가른다**
- 드라이버 **원문 심기 + AST** — 「부르는데 정의 없는 이름」 **0** (부르는 이름 30개)
- **★ 새로 더함**: 심는 표식이 **정확히 한 곳**인지 — 턴 X 에는 이 검사가 `measure()`
  **안에만** 있었다. 그 자리는 브라우저가 있어야 도는 자리라, 자리가 둘이 되거나
  사라진 사실을 **자기시험이 모른 채 초록**이었다(턴 X 병의 바로 그 모양).

### ② P-204 — `exit 0` 은 「이 호출이 통과」다

**머리글 네 번째 줄**을 `scripts/_gate_header.py` 에 세웠다:

    [P-107] MEASURED=무엇을 · **분모 N**

- `judge_measured()` — ① 없음 ② 분모 없음 ③ **분모 0** 이면 어긋남. `judge_header` 에는
  **안 넣었다**: 넣으면 P-204 이 생긴 순간 머리글을 옳게 단 게이트 일흔여덟이 전부
  붉어져 **「머리글이 없다」와 「분모를 안 말한다」가 한 칸에 섞인다.**
- 선언 안 한 게이트는 머리글에 **「(선언 없음 …)」을 스스로 찍는다** — 조용히 빠지면
  세는 쪽이 「없는 것」과 「말 안 한 것」을 못 가른다.
- `verify_gate_header` 에 **넷째 수 `MEASURED_LINE`** — 빨강이 아니라 **회색(`None`)** 을
  낸다(「제품이 무너졌다」가 아니라 「우리가 그 게이트를 모른다」이므로). 그 수 하나가
  회색이면 이 게이트는 **exit 2**.

**MEASURED 선언: 11 / 79** [실측 · 79개를 **실제로 열어** 받았다 · `FINAL_RC=2`]
— 내가 단 것 아홉 + 차선 S 의 새 게이트 둘(S 가 이 규약을 그대로 집어 썼다).
**회색 68** 은 이름으로 불린다. `verify_gate_header` 는 그래서 **exit 2**(회색)다 —
러너(`verify_gates.sh:1136`)가 그것을 `skip` 으로 받는다.

**얼린 관측 A/B**(HEAD 의 바이트 · 게이트 77개를 연 관측 한 벌에 판정기 두 벌):

| 수 | 고침 전 | 고침 후 |
|---|---|---|
| HEADER_DECLARED | True | **같음** |
| HEADER_LIVE | True | **같음** |
| PIPE_EXIT | True | **같음** |
| MEASURED_LINE | (없는 수) | 회색 |

**고침의 몫 1행**(새 수 하나) · **무관한 변화 0행**. 기존 세 수는 같은 바이트에서 **글자까지 같다.**
**exit 2 갈래**(눌러서 봤다 · rc 를 직접 읽었다 — 파이프 뒤 `$?` 로 읽지 않았다):

| 게이트 | 갈래 | rc |
|---|---|---|
| `verify_evidence_chain` | `--db` 없음 (정적만) | **2** ← 종전 0 |
| `verify_onboarding_walk` | `--api` 없음 (구조만) | **2** ← 종전 0 |
| `verify_onboarding_walk` | `--list` | **2** ← 종전 0 |
| `verify_click_completes` | `--list` | **2** ← 종전 0 |
| `verify_gate_header` | `--list` | **2** ← 종전 0 |
| `verify_feature_reach` | `--no-gates` | **2** ← 종전 0 |
| `verify_contract_route_reach` | 토큰 못 받음 | 2 (종전부터) |
| `verify_authn_paths` · `verify_route_alive` | 자격 없음 | 2 (종전부터 · 소스로 확인) |

### ③ 훅 정적 갈래 — **0.9초** (목표 20초)

`verify_ga_readiness.py --static` 을 새로 냈다 — **게이트를 한 벌도 안 부른다**(로그인 0 ·
계정 다툼 0). 대장 구조만 본다: 증명이 가리킨 시험 파일의 실재 · 사유 없는 잠김 · 분류.
`.pre-commit-config.yaml` 에 **`gx-ga-readiness-static`** 을 `^backend/.*$` 로 걸었다.
**언제나 회색(2)** 이고 **exit 1 일 때만 막는다.** 로그인 갈래(`gx-ga-readiness`)는
`docs/agent/evidence/(D-346|D-309|DA-05)` 에 그대로 남는다 — 이 훅이 그것을 대신하지 않는다.

### ④ 온보딩 — **잰 행 48 · 정본 없음 0**

- `docs/agent/onboarding_48.md` 에 P-205 절: 13행에 **누를 자리 · 기대**를 채웠다.
- `scripts/measure_onboarding_t.py` 가 그 13행을 **실제로 잰다**(U1#4 · U2#3 · U3#3 ·
  U3#14 · U4#9 + **기계 여덟 `rows_u6`**). `--check` 가 문서 48 ↔ 도구 48 로 맞는다.
- **판정기 병 하나를 찾아 고쳤다**: `canon_rows` 의 `doc.split(MARK)` 에 `maxsplit=1` 이
  없어 **둘째 표식 뒤에 붙인 표를 아무도 못 읽었다**(표식이 「세는 법」 명령문 안에 또
  있다). 얼린 바이트로 A/B — **HEAD 의 정본에서는 두 규칙이 같은 수**(48·35·13 · 갈린 행 0).
- ⚠ **점수는 내가 내지 않는다.** 정본과 도구를 내가 고쳤으므로 **수는 V 가 낸다.**

### ⑤ drill 씨앗 파이프

`seed.json` 이 표식 값을 나른다 — **낱말을 파일에 안 적고 제품 모듈에서 읽는다**
(`DATA_SOURCE` · `DRILL_MARKER` · `event_data_source(view=…)`). 못 읽으면 `null` + 사유.
읽는 쪽은 `probe_marks.load_seed` 의 `data_source_by_id` · `data_source_mark`.
U1 과 대장에서 말을 맞췄다(15:58 요청 → 16:2x 정정 — 값을 **실측으로** 고쳐 적었다).

### ⑥ P-206 — **반쪽 잠금** (조율자 쪽지 ① · 늦게 받았다)

`scripts/v_lock.py` 가 잠금을 **한 자리**에서만 봤다. 컨테이너 안에서 `ROOT` 는 `/repo` 이고
`/repo/docs/agent/evidence/V_LOCK` 은 **없다** — 실물은 `/docs/agent/evidence/V_LOCK` 이다.
**로그인하는 판정기는 대개 컨테이너로 위임한다**(호스트에 8000 포트가 없다) —
즉 **잠금이 가장 필요한 쪽에 가장 안 들었다.** 차선 F 의 성능 벌 둘이 그래서 401 로 먹혔다.

- 고침: **자리 셋을 다 본다**(`find_lock` · `lock_candidates` · `write_target`),
  `release()` 는 **보이는 자리를 전부** 푼다. `read_lock()` 은 부를 때마다 **다시 찾는다**
  (러너가 먼저 뜨고 V 가 나중에 잠그므로, 불러올 때 굳힌 자리는 그 뒤의 잠금을 못 본다).
- **얼린 A/B — 같은 순간 · 컨테이너 안 · 같은 파일계**:

      종전(한 자리 /repo/docs/…) → 있나 False
      지금(세 자리)              → 찾은 것 /docs/agent/evidence/V_LOCK · is_locked True

- **눌러서 봤다**: 호스트에서 잠근 뒤 **컨테이너로 위임하는** `verify_sidebar` →
  「**로그인 건너뜀 — V 단독 중**」 **4회**(조율자 측정으로는 종전 「로그인 성공 4회」).
  풀고 양쪽이 다 안 잠김인 것까지 확인했다.
- ★ **덤으로 나온 거짓 빨강**: 그 상태의 `verify_sidebar` 가 **빨강(1)** 이었다 —
  「한 눈으로만 쟀다」에 **「못 쟀다」라는 글자가 없어** 회색 규칙에 안 걸렸다.
  **잠금이 제품 결함처럼 보이면 다음 사람은 잠금을 끈다**(D-353). 이제 **회색(2)** 이고
  사유를 이름으로 적는다(`eyes_why`).
- ★ **선행 결함**: `v_lock.py --self-test` 가 윈도 콘솔에서 `—` 한 글자에
  `UnicodeEncodeError` 로 **죽고 있었다**(rc=1). 스트림을 utf-8 로 세웠다.
  **못 도는 자기시험은 빨강이 아니라 없는 것이다.**
- **P-206 출생 표본 6**을 자기시험에 박았다(두 뿌리 · 반쪽 잠금 · 잠근 사람은 지나감 ·
  쓸 자리 고르기 · 양쪽 풀기). `v_lock --self-test` rc=0 · `verify_sidebar --self-test` rc=0.

### ⑦ P-206 ② — **머리글이 약속을 지키게 한다** (가장 위험한 하나만)

`verify_ui_secrets.py` 는 머리글에 「`TARGET=… :8000`」 · 「`SOURCE=살아 있는 서버 응답`」 을
적으면서 **`--api` 가 없으면 API 를 한 번도 안 때리고 exit 0** 이었다.
→ 안 잰 면을 **`ungauged` 로 세어 exit 2**(눌러서 봤다 · rc=**2**), 머리글의 TARGET/SOURCE 도
그 호출에서 **안 때린다고** 말한다. MEASURED 도 달았다(패턴 분모 6종 · 면 둘).
러너도 같이 고쳤다 — `verify_gates.sh` 의 ui-secrets 갈래가 **비영을 전부 빨강**으로 읽어
「안 잰 면」이 「화면이 서랍을 열었다」로 적혔다 → **2 는 SKIP(회색)**
(눌러서 봤다 · `--gate ui-secrets` → rc=2 · `SKIP`. 처음 쓴 회색 사유에 역따옴표가 있어
셸이 `--api` 를 **명령으로 실행**했다 — 그것도 같이 고쳤다).

---

## 손 위

- (없음 — 이 턴의 다섯 항은 닫았다. 아래 「안 한 것」이 남은 전부다)

## 대 본 것

- `verify_tool_selftest` 전수 — **검사 102종 · 자기시험 91 · 출생 표본 87 ·
  표본 없이 태어난 새 도구 0** → 통과 (머리글 넷째 줄이 다른 게이트를 안 깨뜨렸다)
- 무관한 게이트 다섯(`dormant` · `envelope` · `seed_p20` · `gate_counts` ·
  `measure_repro`) 자기시험 **rc=0**
- `verify_gates.sh:1134` 의 출력 그물에 **`?` 를 더했다** — 종전 `[OX]` 만 떠서
  **회색 줄이 출력에서 통째로 사라졌다.** 안 보이는 회색은 초록처럼 읽힌다.
  (⚠ 차선 S 가 16:08 에 이 파일을 고쳤다 — **고치기 직전에 다시 읽고** 그 한 줄만 바꿨다)

## 안 한 것 · 못 한 것 (P-206 ②③④ 는 대장에 **이름으로** 남겼다)

- **P-206 ② 나머지 셋** — `verify_purge` · `verify_camera_pulse` · `verify_alarm_budget`:
  셋 다 「호스트면 `docker exec` 로 위임한다」고 **적고 안 한다.** 다만 셋은 이미 **2(회색)**
  를 내므로 **거짓 초록은 아니다** — 고칠 것은 「머리글의 약속」 쪽이다.
- **P-206 ③** — `MEASURED=` 가 **「업은 면제 N」**까지 말하게 한다. 지금 규칙은 분모만
  요구한다(분모 0 은 잡는다). `ISO-03`(576·면제 461) · `QA-03`(319·면제 96) 은 분모가 있어도
  「늘지 않았다」를 재고 `SEC-12/13`(면제 0)은 전수를 잰다 — **넷 다 `exit 0`**.
- **P-206 ④** — `verify_wall_keys` 가 `UX-15`·`UX-16` 을 **한 종료코드**로 잰다.
  더 큰 것: 검사 넷이 전부 PASS 인데 `UX-16` 의 「지도」·「카메라 맥박」에 **술어가 0개**다.
  **「게이트가 초록」과 「그 절이 닫혔다」가 다른 말**인 자리가 실재한다.

- **나머지 68개 게이트의 MEASURED 줄** — 이 턴 대상은 여덟(+ 머리글 게이트 자신)이었다.
  나머지는 `verify_gate_header` 가 **회색으로 세어** 이름을 부른다.
- **온보딩 48행을 실제로 걷기** — V 의 몫이고, 규약상 내 몫이 아니다.
  13행은 **이번에 처음 도는 코드**다: 행마다 `guarded()` 울타리를 쳐서 터져도
  **그 행만 회색**이 되게 했다(사람 하나가 통째로 날아가지 않는다).
- `verify_route_alive` 의 **`--api` 가 위임 갈래에서 삼켜지는 자리** — 고치지 않았다.
  로그인 창이 닫힌 뒤가 아니면 고친 것을 눌러 볼 수 없고, **못 눌러 본 고침은 고침이 아니다.**

## 자진 오판 둘 (대장에 적었다)

1. **15:56** — N 의 로그인 창(15:52~16:20) 안에서 `verify_route_alive` ·
   `verify_contract_route_reach` 를 인자 없이 돌렸다. 둘은 `.env.gates` 를 읽어
   **실제로 로그인한다.** N 과 같은 계정이라 그 순간 N 의 세션이 끊겼을 수 있다.
2. **16:01** — 죽은 포트(`--api http://127.0.0.1:9`)로 계정을 피하려 했는데
   `verify_route_alive` 는 `GX_ROUTE_CONTAINER` 가 있으면 **`args.api` 를 버리고** 위임한다.
   그래서 **또 로그인했다.** 그 뒤로 16:20 까지 로그인 게이트를 돌리지 않았다.
