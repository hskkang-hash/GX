# 턴 Y · 차선 U56 체크포인트 (P-176 규약 · 이 파일 하나만 덮어쓴다)

마지막 갱신: 16:2x KST · 2026-09-20

## 닫음 — **눌러서(돌려서) 확인한 것만**

- **P-206 ① 계량이 커널을 거친다.** `apps/dsm/metering.py` 의 `_events` ·
  `_notifications` · `_notifications_failed` → `kernels.k1_event.count_events` ·
  `kernels.k2_notify.count_deliveries`. 그 셋의 앱 층 ORM 직접 셈 **0**.
  - 커널 쪽은 **셈 갈래**다: `_events_queryset` / `_deliveries_queryset` 를 뽑아
    `query_events`·`list_deliveries` 와 **필터 한 벌**을 공유한다. 갈리는 것은
    표식 한 줄뿐(보는 갈래 `exclude_probe` · 세는 갈래 `exclude_unbillable`).
  - 셈 갈래에만 더한 셋: **반열린 구간**(`until` 이 `__lt`) · **소프트 삭제 제외** ·
    **전역 관리자여도 제 테넌트만**(`own_tenant_only`).
- **P-206 ② `AppStaysThinTest` 가 이제 `metering.py` 를 본다** (`tests/test_dsm_app.py`).
  - `test_the_bill_is_not_counted_by_the_app` — **함수 단위 AST**. 남은 빚 넷
    (`_alive`·`_cameras`·`_users`·`_storage`)은 **이름으로 못박았고 늘어날 수 없다**
    (줄어드는 방향만 초록 — 낡은 예외도 빨강이다).
  - `test_the_billing_counts_come_from_the_kernels` — **부르는가**를 따로 잰다.
    (안 만지면서 **아무것도 안 세는** 코드가 앞 시험을 통과하기 때문.)
- **P-206 ③ 시험** `backend/tests/test_u56_metering_seeds.py` (**14건 전부 초록**).
  probe 4·drill 4 전/후 **불변** · 실사건 1 → **+1** · 두 축(사건·발송) · 실패 칸 ·
  테넌트 양방향 · 달 경계 반열린 · 표식 짝 맞추기 · **남은 빚 1건(표식 없는 훈련)**.
- **§1-1 을 도구에 박았다** — `scripts/verify_purge.py` **일곱째 수**
  「연쇄 표 → 스냅샷 → 집행」. 자기시험 통과(출생 표본 2 · 음성 12 · 부작위 1) ·
  실측 `연쇄 표 3표 · 머릿수 1 = 합 1 · 집행 1행에 스냅샷 1건` · 전체 **exit 0**.
- **`gunicorn --config` 값 준비 · 재기동 0.** [실측 · gx-shell · `--print-config`]
  **`config =` 줄은 거짓말한다** — 안 걸려도 `./gunicorn.conf.py` 로 찍힌다.
  A(cwd=/app) 75/200 · B(cwd=/) **2/0/1/sync** · C(`--config` 절대) 75/200 + 절대 경로.
  ⇒ 「걸렸는가」는 **`keepalive` 75 인가 2 인가**. `gunicorn.conf.py` 주석 ①-b ·
  창 2a B2-b·B3·되돌림 표에 박았다.
- **창 2a 절차서** — 집행 순서 표 신설(**OPS-24 회수 → C4 앱 전환**, 왜 그 자리인지
  포함). **16단계 · ★셋 · ⛔★C7 그대로.**
- 게이트: `verify_layers` **0건** · `forbidden-zone` **0건** · `dormant` 내 새 항목 **0**.

## 손 위 — 지금 고치던 파일과 **다음 한 줄**

- 손 위에 없다 — P-206 · §1-1 · gunicorn · 창 2a 넷 다 닫았다.
- 다음 한 줄: **U1 의 답을 기다린다**(대장 16:1x 요청) — 심는 쪽이 `track_id` 에
  적는 글자가 `drill_mark()` 와 같은가. 갈리면 제품은 훈련을 세는데 **청구도 센다.**

## 전 시험 — 돌렸고, **내 빨강 0**

`pytest tests --ignore=tests/e2e` → **12 failed / 1832 passed** (21분).
그중 **둘은 내 것이었고 둘 다 고쳤다**:

- `test_f05_event_api::EntrySurfaceIsOneTest` **2건** — 계량이 `kernels.k1_event` 를
  **직접** import 해서 「K1 의 App 소비자는 하나」(F-05 「진입면 하나」의 집행)가
  깨졌다. **게이트를 느슨하게 하지 않고** `apps/dsm/services.py` 에 얇은 문 둘
  (`count_billable_events` · `count_billable_deliveries`)을 두어 그리로 돌렸다.
  ★ 내 시험도 **사슬을 두 칸으로** 재도록 고쳤다(계량→문 · 문→커널).
  한 칸만 재면 문이 커널을 안 부르는 날에도 초록이다.

나머지 빨강은 **내 헉크가 아니다** — `git diff -U0` 로 갈랐다:
`suppress`·`send` 의 변경은 **U1 의 F-04 재작업**(채널별 억제 · P-208)이고,
내 헉크는 import 한 줄과 `list_deliveries` 이하뿐이다. 남은 빨강 셋
(`test_k2_notify_kernel::SuppressionTest` 2 · `test_u1_probe_not_counted` 의 발송 축 1)은
전부 **억제 가족**이다. 막판 대조 [실측]: 내 범위 재측 **170 passed · 3 failed**
(그 셋만) · `test_u56_metering_seeds` **14/14** · `test_be_metering` · `test_dsm_app` ·
`test_f05_event_api` · `test_tenant_isolation` · `test_k1_event_kernel` **전부 초록**.

## 안 한 것 — 넘길 것

- **계량의 남은 직접 셈 셋**(`_cameras`·`_users`·`_storage`). 카메라·계정·미디어
  장부를 세는 **커널 면이 저장소에 없다**(W4-1 계량 모델이 선행). 없는 것을 엉뚱한
  커널(K1=이벤트·K2=발송)에 밀어 넣으면 다음 사람이 그 자리를 정본으로 읽는다 —
  **남기고 시험에 이름으로 못박았다.** 갚는 날: 계량 커널 면.
- **표식 없는 옛 훈련 사건은 아직 청구에 든다.** 훈련의 원래 정본은 **창(감사)**이고,
  창 판정을 셈에 쓰면 한 달 셈이 감사 전수 조회가 된다. `billing_marks` 머리말 ⚠ 절과
  `WhatWeStillCannotSubtractTest` 가 그 빚을 적어 두었다(0으로 안 덮었다).
- **`--config` 적용은 안 했다** — 창 일(F 창 중 재기동 금지). 창 2a C4 재생성 때 한 번.
- `apps/dsm/services.py` 에 문 둘을 더했다(`count_billable_*`) — 내 파일 목록 밖이지만 F-05 「진입면 하나」가 요구한 자리다. 다른 차선과 겹치지 않게 **파일 끝에만** 붙였다.
- DA-04 §2 K1·K2 표에 `count_events`·`count_deliveries` 두 줄 추가(문서 소유 밖).
  커널 `__init__` 과 `SURFACE` 시험은 **깨지지 않는다**(둘 다 「없는 이름」만 잡는다).
