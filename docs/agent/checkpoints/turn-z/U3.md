# 차선 U3 — 턴 Z 체크포인트

마지막 갱신: 2026-09-21 12:5x KST

**이 턴에 U3 가 고친 제품 코드 0줄 · 새 라우트 0 · 제품 쓰기 0.** 사유를 규명한 턴이다.
(상태를 바꾼 것: 없음. 아래 「잰 것」의 로그인 일곱 번과 422 하나가 전부이고,
그 422 는 **검증에서 떨어져 아무것도 안 만들었다**.)

---

## ★ 먼저 — 수가 내려간 것을 뒤집지 않는다

온보딩 **24.5/48 · 잰 행 43**(전 턴 26.0 · 잰 행 35)은 **옳게 내려간 수**다.
안 재서 안 보이던 빨강이 드러난 것이고, 이 보고는 그 판단을 건드리지 않는다.

---

# 본 일 ① — 잰 48행 중 **M 축 회색 5행**의 사유

## 결론 한 줄 — **세 부류 중 어느 것도 아니었다. 「문을 잘못 두드렸다」다**

회색 5행은 **U6#1 · U6#2 · U6#3 · U6#4 · U6#9** 다
(`evidence/P-159/onboarding_measure_20260920T081440.json` · `measured=false`).
판정기가 적어 둔 사유는 **「기계 자격이 없다 — 환경에 `GX_PROBE_PASSWORD` 를 주면 잰다」**
였는데, **그 문장이 거짓이다.** 자격을 줘도 안 재진다. 사유는 자격이 아니다.

| 행 | 판정기가 적은 사유 | **실물로 확인한 진짜 사유** | 부류 |
|---|---|---|---|
| U6#1 | 관리자 토큰이 없다 | `api_token()` 이 **3002(정적 SPA 서버)** 에 POST 했다 | 우리가 안 잰 것 — **거짓 회색** |
| U6#2 | 기계 자격이 없다 | 〃 | 〃 |
| U6#3 | 기계 자격이 없다 | 〃 | 〃 |
| U6#4 | 기계 자격이 없다 | 〃 | 〃 |
| U6#9 | 기계 자격이 없다 | 〃 | 〃 |

### 실물 — **넷을 따로 세워 「자격 없음」 가설을 기각했다** [U3 실측 2026-09-21 12:2x~12:3x KST]

1. **계정은 있다.** ORM 직독 — `gxprobe_q`(id 112) · `gxprobe_v`(id 116) ·
   `gxseed_u5_sysop`(id 115) 전부 `EXISTS active=True`. 씨앗 삭제에 안 쓸렸다.
2. **자격도 맞다.** `.env.gates` 의 `GX_PROBE_PASSWORD`(sha256 앞12 **`a7c0ebcbb722`**)로
   `POST http://localhost:8000/api/v1/auth/login` → **200 · `user.access_token` 길이 300**.
   관리자(`gxseed_u5_sysop` · 비밀번호 sha12 **`de08e1f17eb3`**)도 **200 · 토큰 300**.
3. **그 토큰은 실제로 문을 연다.** `Bearer` 로 `GET /api/dsm/events?limit=1` → **200** ·
   `GET /api/dsm/health` → **200**. 「자격이 없어 못 쟀다」가 설 자리가 없다.
4. **3002 은 API 가 아니다.** 같은 경로를 3002 에 익명으로 두드리면 **전부 404 + `text/html`**
   (정적 서버가 `index.html` 을 낸다). 8000 에 두드리면 **401**(events 등) · **200**(health).

★ **차선 Q 가 오늘 같은 자리를 독립적으로 찾아 이미 고쳤다.**
`scripts/measure_onboarding_t.py::api_token` 머리말 [턴 Z · Q] 에 사유가 박혔고
(`web` → `api_base`), Q 의 재측 `onboarding_measure_20260921T032527.json` 이
**U6 8행 · 잰 행 8 · 회색 0 · 초록 4 · 빨강 4** 를 냈다.
⇒ **M 축 회색 5행은 이 턴에 0 이 되었다.** 제 실측이 Q 의 재측과 **글자까지 같은 수**를 냈다:

| 내가 8000 에서 잰 것 | Q 의 재측이 적은 것 |
|---|---|
| 익명 읽기 5건 전부 **401** | 「전부 401=True」 → U6#12 **초록** |
| `health` **200** · `X-GX-Schema: 1.1` | 「X-GX-Schema=1.1」 → U6#14 **초록** |
| `health` 검사 이름 `db·cache·queue` | 「검사 이름 ['db','cache','queue']」 → U6#15 **초록** |

## ★ 그래서 **U6#12 · #14 · #15 의 빨강 셋도 거짓 빨강이었다**

같은 뿌리다. `rows_u6` 가 `web`(3002)을 두드려 **정적 서버의 404** 를 받고
「익명 읽기 5건 전부 404 — 401 아님」 · 「X-GX-Schema 없다」 · 「health 404」로 **빨강**을 적었다.
**제품이 낸 빨강이 아니다.** Q 의 고침 뒤 셋 다 초록이다.
⇒ 다만 **24.5/48 이라는 수 자체는 아직 안 고쳐졌다** — 48행 전량 재측이 한 번 더 필요하다(§⑤).

## 세 부류로 가른 표 — 회색 5행 **+** 「잴 것이 0」으로 빨강이 된 행들

지시가 준 세 부류를 그대로 쓰면, **M 축 회색 5행에는 ①도 ③도 0** 이고 전부 ② 다.
대신 세 부류는 **빨강으로 적힌 행들** 쪽에 살아 있다 — 그쪽을 같이 적는다.

### 부류 ① 「환경(로그인·세션·속도 제한)」 — **회색 5행 중 0행**

다만 **함정은 실재한다** — 제가 오늘 직접 밟았다.
`POST /api/v1/auth/login` 을 1분 안에 네다섯 번 부르면 **429** 가 온다
(`NINJA_DEFAULT_THROTTLE_RATES` 의 `anon = "5/minute"` · `backend/config/settings.py:1111`).
그리고 `api_token()` 에는 **429 를 다시 기다려 주는 길이 없다**
(`verify_route_alive.login` 에는 `_rate_waited` 가 있다). 429 가 나면 결과는
**똑같은 `None`** 이고 증거줄은 또 「자격이 없다」라고 적는다 —
**다음 번에 같은 거짓말이 나올 자리다.** → Q 쪽지 ②.

### 부류 ② 「우리가 안 잰 것(술어 없음)」 — **회색 5행 전부 + 빨강 3행**

U6#1·2·3·4·9(회색) · U6#12·14·15(거짓 빨강). 전부 **판정기가 제품이 아닌 것을 쟀다.**
술어가 없어서가 아니라 **술어를 엉뚱한 문에 물어서**다.

### 부류 ③ 「잴 것이 0(자료 상태가 비었다)」 — **빨강 여섯 행** [U3 실측 12:2x KST · ORM 직독]

이 여섯은 **판정기도 제품도 옳고, 자료가 없어서 0점**이다. **drill 씨앗으로 채우면 잰다.**

| 행 | 판정기가 못 본 것 | 지금의 자료 상태 | 심어야 할 것 |
|---|---|---|---|
| **U1#11** | 큐에 「실제로 확인 · 접수」 단추 | `response_state='occurred'` 사건 **0건** (closed 17 · in_progress 9) | `occurred` 사건 1건 |
| **U2#2** | `preset=unhandled` 목록 ≥1행 | 서버 0건 (같은 이유) | 〃 |
| **U3#7** | 접수→조치 전이 + 감사 1행 | 초점 사건이 이미 `closed` | `occurred` 또는 `acknowledged` 사건 1건 |
| **U4#11** | `/device` 표 ≥1행 | 장비 표 0행 | 장비 1행 |
| **U5#2** | `/roles` 표 ≥1행 | 역할 표 0행 | 역할 1행 |
| **U2#6** | `/report-template` 표 ≥1행 | `DsmReportRun` **9행은 있다** — 아래 ★ | 자료 아님 · 화면 축 |

★ **U2#16 과 U2#6 은 「자료가 없다」로 적으면 안 된다.**
판정기는 U2#16 에 「서버 규칙 0건」이라 적었지만 **실물은 `NotificationRule` 9행(group 4)**이다.
판정기의 `net.ok(...)` 는 **화면이 그 요청을 실제로 쏘았는지**를 보는 자리라,
「200=False」는 **자료 0 이 아니라 「화면이 그 문을 안 불렀다」**는 뜻이다. U2#6 도 같은 모양이다.
⇒ 이 둘은 **화면 축의 사실**이고, 씨앗을 심어도 안 바뀐다.
(`NotifySettings.tsx` · `Reports.tsx` 는 U3 소유지만 **이 턴엔 안 고쳤다** —
회귀를 재는 턴에 새 변수를 얹지 않는다. 다음 턴 후보로 **이름만** 남긴다. 색은 안 적는다.)

### 부류 ④ — 새로 이름을 붙인다: 「제품이 그렇게 생겼다(○)」 · **빨강 네 행**

U2#3(등급을 다시 매기는 자리 0개) · U3#14(모바일 실시간 자리 0 · 계약 11조 설계 잠금) ·
U4#15(「보고 표시」 토글이 화면에 없다) · U5#10(채널 값이 `['log']` 뿐).
**심어도 안 바뀐다.** 자료를 넣어 초록으로 만들려 하면 그것이 거짓 초록이다.

---

# 본 일 ② — 편리성 #5 (`u3_receive_to_ack`)

## **못 닫았다. 다만 사유가 넷에서 하나로 줄었다 — 「발송이 달린 `occurred` 사건이 0건」**

지난 턴에 사유가 넷이었다(접수 가능한 사건 0 · 첫 값 대상 소멸 · 대장 규약 · 계정 한 세션).
이 턴에 **둘은 사라졌고**(계정 경합은 안 일어났다 · 배선은 다시 확인해 완전하다),
**하나는 자료로 옮겨 붙었다.**

### 배선은 완전하다 — 고칠 것이 없다 [U3 소스 실측]

| 자리 | 무엇 | 파일 |
|---|---|---|
| 시계 **시작** | M1 목록에 발송이 뜰 때 `startReceiveToAck(event_id)` | `features/mobile/pages/MobileInbox.tsx:231` |
| 탭 **셈** | 상세를 열면 `markOpened(id)` (끝이 아니라 탭 한 번) | `features/mobile/pages/MobileEventDetail.tsx:237` |
| 시계 **멈춤** | `toState === 'acknowledged'` 가 **서버에 기록된 뒤** `finishReceiveToAck(id)` | `MobileEventDetail.tsx:277` |
| 꺼내기 | `window.__gxMobileConvenience.export()` | `features/mobile/metrics.ts` |

턴 V 에 옮겨 둔 「열기가 아니라 접수에서 멈춘다」가 그대로 서 있다.

### 막은 것 — **자료 하나** [U3 실측 2026-09-21 12:2x KST · ORM 직독]

```
DetectionEvent 26건 → response_state:  closed 17 · in_progress 9 · occurred 0
전이표:  occurred → acknowledged → in_progress → closed   (kernels/k1_event/response_flow.py)
```

화면은 **서버가 준 `allowed_next` 만 그린다**(`MobileEventDetail.tsx:384`).
`occurred` 가 0건이면 **「접수하기」는 어디에도 안 뜬다.**
막은 것은 제품이 아니라 자료다 — 제품은 옳다.

### ★ 12:31 KST 에 자료가 움직였다 — 그런데 **아직도 못 잰다**

같은 자리를 다시 물었더니 `occurred` 가 **1건** 생겼다:
`#326411 · track_id='data_source=drill;run=20260921T102945' · severity=warning`
(U1 차선이 지금 훈련 자료를 심고 있다.)

**그런데 그 사건의 `DeliveryRecord` 가 0건이다.** 발송 총계도 **225 그대로**다.
M1 은 **사건 목록이 아니라 발송 목록**(`GET /api/dsm/deliveries?limit=50&mine=true`)이라,
발송이 없으면 그 사건은 **M1 에 안 뜨고** `startReceiveToAck` 이 **안 불린다.**
⇒ 상세로 바로 들어가 접수해도 `finishReceiveToAck` 은 **시작 없는 끝**이라 `null` 을 낸다.
**한 줄도 안 남는다.**

### 다음 사람이 재려면 필요한 것 — **씨앗 한 줄** (U1 몫 · U3 는 안 심는다)

```
필요한 것:  response_state='occurred' 인 사건 1건
            +  그 사건에 붙은 DeliveryRecord 1행, recipient = 재는 계정
               (M1 이 부르는 질의:  GET /api/dsm/deliveries?limit=50&mine=true)

지금 있는 것:  occurred 사건 1건(#326411)  ·  그 사건의 발송 0행   ← **발송이 빠졌다**
```

지금 drill 씨앗은 **사건만 만들고 발송을 안 만든다.**
발송 한 행만 더 붙으면 **#5 는 한 번 눌러 끝난다** (목록 뜸 → 카드 탭 → 「접수하기」 = 2탭).
같은 씨앗 한 줄이 **U1#11 · U2#2 · U3#7 셋도 같이 열어 준다**(위 부류 ③).

### 대장(`convenience_u3.json`)은 **한 자도 안 고쳤다**

- 대장의 규칙 4: 「**브라우저에서만 잰다 — 차선은 브라우저를 재지 않는다. 채우는 것은 단독 세션(V)**」.
- 첫 값의 대상 `subject=274948` 은 **DB 에 없다**(P-184 삭제). 둘째 값을 다른 사건으로 재면
  **두 값은 같은 대상이 아니다** — 이어 붙이면 그 표가 거짓이 된다. 그 판단도 V 의 몫이다.
- 못 잰 칸은 **`null` 이지 0 이 아니다.**

---

# 본 일 ③ — 발송 실패 배지 「실패만」 갈래는 **v1.2 그대로**

> **왜 v1.2 인가 — 한 줄.** 서버에는 이미 `succeeded=false` 인자가 있고
> [실측 오늘 `GET /api/dsm/deliveries?limit=500&mine=false&succeeded=false` → **total 1** ·
> 사건 #4819 · `email`] **그 1건을 정확히 내준다.** 못 만드는 게 아니라,
> 「배지가 하던 거짓말 하나를 닫는 턴」에 **새 변수를 얹지 않기로** 조율자가 2026-09-20 에
> 미룬 것이다 — 사유는 `frontend/src/features/mobile/pages/MobileInbox.tsx` 의 주석에
> 이미 박혀 있다(「지금 닫는 것은 거짓말 하나뿐이다」).
> **막힌 것이 아니라 미룬 것이고, 서버 쪽은 이미 다 서 있다.**

이 턴에 **안 고쳤다.** 다음 턴이 열기로 하면 드는 비용은 화면 한 곳뿐이다(단추 + 질의 인자 하나).
**다음 턴은 이 절을 읽고 다시 묻지 않아도 된다.**

---

# 본 일 ④ — 발송 분모 **225** 재측 [U3 실측 2026-09-21 12:2x~12:3x KST]

**수가 줄어든 것은 결함이 아니라 씨앗이 빠진 것이다.** 조율자의 삭제(286 → 225 · −61) 그대로다.

| 무엇 | 지난 턴(Y · 16:05) | **이 턴 실측** | 어떻게 쟀나 |
|---|---|---|---|
| `DeliveryRecord` 전량 | 267 (뒤에 씨앗이 286까지 낳음) | **225** | ORM 직독 |
| 〃 HTTP `mine=false&limit=500` | 267행 | **225행 · `total` 225** | 게이트 8000 · `gxseed_u4_official` |
| `DetectionEvent` | 26 (probe 4) | **26 → 27** (probe **0** · drill **4 → 5**) | ORM · 12:2x 와 12:31 두 번 |
| `EventClip` | 30 | **26** | ORM |
| probe 발송 | 42 | **0** | ORM (`track_id` 에 `data_source=probe` 0건) |
| 발송 실패 | 1 (#4819 `email`) | **1 (#4819 `email`)** — 그대로 | ORM · HTTP 둘 다 |

★ **probe 사건 4건이 「사라진」 것이 아니라 자리를 바꿨다.** 이 턴의 사건 26건 중
`data_source=drill` 이 **4건**(#305027~305030 · 전부 `closed` · 발송 0행)이고
`data_source=probe` 는 **0건**이다. 조율자가 지운 것은 `#295402~295405` 쪽이 맞다.

## 배지 두 갈래는 **분모가 바뀌어도 그대로다** [HTTP 실측]

| 누를 칸 | 질의 | `total` | `data-gx-scope` | 화면 글자 |
|---|---|---|---|---|
| ① 내게 온 것만 | `deliveries?limit=50&mine=true` | **2** | `all` | **`발송 실패 0건`** (초록) |
| ② 기관 전체 | `deliveries?limit=50&mine=false` | **50**(꽉 참) | `page` | **`이 페이지에 발송 실패 0건 · 뒤쪽은 아직 못 봤습니다`** (주황) |

⚠ **② 의 주황은 여전히 「괜찮다」가 아니다** — 기관 전체에 실패는 **1건 있고**(#4819),
`-occurred_at` 정렬에서 뒤쪽이라 50건 쪽에 안 잡힌다. 분모가 267 → 225 로 줄어도 그 사실은 같다.
「2 · 50」 은 **턴 Y 와 같은 수**이고, 바뀐 것은 뒤쪽 전량(267 → 225)뿐이다.

---

# ⑤ 못 한 것 · 재지 못한 것

1. **온보딩 48행 전량 재측을 안 했다.** Q 가 오늘 U6 8행만 다시 쟀고(회색 0),
   **24.5/48 은 아직 어제 판**이다. 전량 재측은 **Q 의 파일이고 Q 의 창**이라 U3 가 안 돌렸다 —
   같은 도구를 두 차선이 동시에 돌리면 로그인 5/분 한도에서 서로의 세션을 끊는다.
   ⚠ 다음 전량 재측에서 U6 세 행이 어떻게 바뀔지는 **예상이지 측정이 아니다. 색을 적지 않는다.**
2. **편리성 #5 의 둘째 값** — 위 본 일 ②. 씨앗(발송 1행)이 없어 못 쟀다. **안 심었다**(U1 몫).
3. **브라우저 화면을 한 장도 안 찍었다.** 이 턴의 물음(회색의 사유 · 분모)은 전부 ORM·HTTP 로
   답이 났고, 화면을 띄우면 남의 세션을 끊는다. **안 찍은 것을 안 찍었다고 적는다.**
4. **U2#6 · U2#16 의 화면 축**(화면이 `reports/templates` · `notify-rules/list` 를 안 부른다) —
   **이름만 댔고 안 고쳤다.**
5. **U6#1 · U6#4 의 빨강**(422) — 사유는 규명했다(Q 쪽지 ①). **안 고쳤다 — Q 의 파일이다.**
6. **실카메라 0/4 · `capture_service._capture_frame` 의 `None.is_external`** — 턴 W·X·Y 이월 그대로.

---

# 잰 것 (이 턴) · **창**

| 시각(KST) | 무엇 | 상태를 바꿨나 |
|---|---|---|
| 12:2x | ORM 직독 — 계정 6 · 발송/사건/클립/규칙 전수 · `response_state` 분포 | 안 바꿈 |
| 12:21~12:23 | `gxprobe_q` 세 번 · `gxprobe_v` 한 번 로그인 (마지막이 **429**) | 세션만 |
| 12:27~12:28 | `gxprobe_q` · `gxseed_u5_sysop` 로그인 + `Bearer` 읽기 세 갈래 | 세션만 |
| 12:30 | `gxseed_u4_official` 로그인 + `deliveries` 다섯 갈래 | 세션만 |
| 12:31 | ORM 재독 (`occurred` 1건이 새로 보임) | 안 바꿈 |
| 12:3x | `gxseed_u5_sysop` 로그인 + `POST api-keys` **422** (검증에서 떨어짐 · 키 0개 생성) | **안 바꿈** |

- 자격 대조: `GX_SEED_ROLE_PASSWORD` sha256 앞12 **`de08e1f17eb3`** — 턴 Y 가 적어 둔 값과 **같다**.
  `GX_PROBE_PASSWORD` sha12 **`a7c0ebcbb722`** (이 턴에 처음 적는다).
- **비밀은 쿼리·argv 에 0** — 전부 `docker exec -e 이름만` 으로 넘겼다.
- 게이트 서버 상태: `gate_servers.sh status` → api 8000 · spa 3002 **둘 다 대답한다**
  (호스트에서 재지 않았다 — 호스트의 「없다」는 뜻 없는 회색이다).
- **저장소에 남긴 임시 파일 0** — 측정 스크립트는 전부 표준입력으로 흘려보냈다.

## 자진 오판

### ① **Q 가 재는 시각과 내 로그인이 겹쳤다**
Q 의 U6 재측이 **12:25:27 KST**(`…20260921T032527.json`)인데, 나는 12:21~12:23 에
`gxprobe_q`·`gxprobe_v` 로 네 번 로그인해 **마지막에 429 를 받았다.**
Q 의 결과는 `ok=true · admin_ok=true` 라 **해는 없었지만 그건 운이었다** —
창을 먼저 대장에 대 보고 잡았어야 했다.
턴 Y 에 내가 F 와 겹칠 뻔한 것과 **같은 병이고, 두 턴 연속**이다.

### ② **회색의 사유를 판정기가 적은 대로 믿을 뻔했다**
판정기의 문장(「`GX_PROBE_PASSWORD` 를 주면 잰다」)이 너무 구체적이어서,
`.env.gates` 에 그 이름이 실제로 있는 것을 보고 **「그럼 안 넘긴 것이구나」로 닫을 뻔했다.**
자격이 있는데도 안 되는지를 직접 재서야 문이 틀렸다는 것이 나왔다.
**판정기가 적은 사유는 입력이지 측정이 아니다.**

### ③ **`GX_PROBE_USER` 와 판정기의 계정 이름이 다르다** (오판은 아니나 적어 둔다)
`.env.gates` 는 `GX_PROBE_USER=gxprobe_v` 인데 `measure_onboarding_t.py` 는
**`"gxprobe_q"` 를 손으로 박아 두었다.** 오늘은 둘 다 살아 있고 같은 비밀로 열려서
아무 일도 안 났지만, **자격이 한 번 돌면 판정기만 다른 계정을 재게 된다.** → Q 쪽지 ③.

### ④ **새 쪽지 규약의 첫 사고를 내가 겪었다 — 같은 파일에 쪽지 둘**

12:37 에 U3 가 `turn-z/Q.md` 를 **신설**해 쪽지를 적었는데, 12:4x 에 **차선 A 의 쪽지가
같은 이름으로 들어와 U3 의 글이 통째로 덮였다.**
U3 는 **A 의 글을 지우지 않고 제 절을 뒤에 다시 붙였다**(A 340줄 + U3 145줄 = 485줄).

⚠ **규약이 바뀐 첫 턴에 바로 부딪혔다.** 턴 Y 의 병은 「쪽지를 보낸 차선 파일에 썼다」였고,
턴 Z 는 그것을 「받는 차선 파일에 쓴다」로 고쳤다 — 그런데 **받는 차선이 하나인데 보내는
차선이 여럿**이면 나중 사람이 앞사람을 덮는다. **고침이 새 병을 하나 만들었다.**

**제안(조율자 판정 몫 · U3 가 정하지 않는다)**: 쪽지 파일은 **보내는 차선 이름을 붙인다** —
`turn-z/Q.md` 하나가 아니라 `turn-z/Q_from-A.md` · `turn-z/Q_from-U3.md`.
그러면 「받는 차선 파일에 쓴다」를 지키면서 **한 파일 한 차선**도 같이 선다.
지금 턴은 **덮지 말고 뒤에 붙이고, 절 제목에 보낸 차선을 적는다**로 U3 가 넘긴다.

## 고친 파일

- `docs/agent/checkpoints/turn-z/U3.md` (이 파일 · 신설)
- `docs/agent/checkpoints/turn-z/Q.md` (**A 의 쪽지 뒤에 덧붙임** · U3 쪽지 넷 · A 의 글 0줄 손댐)
- `docs/agent/checkpoints/turn-z/U1.md` (신설 · 씨앗 쪽지 하나 — 발송 한 행이 빠졌다)

**제품 코드 0줄 · 새 라우트 0 · 시험 0개 · 대장 0자.**
`bash docs/agent/verify_gates.sh --gate forbidden-zone` → **rc=0 · 금지구역 변경 0건**.

---

## 🔔 [U24 → U3] 2026-09-21 — **U4#15 는 「단추가 없어서」 빨강이 아닙니다**

`EventList.tsx` 는 이 턴 **당신 파일**이라 저는 **한 줄도 안 건드렸습니다.** 잰 것만 넘깁니다.

### V 의 증거(2026-09-20T08:30:35Z)

```
U4#15  빨강 0.0  ·  「보고 표시」·「보고함」 둘 다 화면에 없다
```

### 제가 잰 것 (코드 · 문 · 자료 — **브라우저 0회**)

```
[코드] EventList.tsx:975-1000 — 「상급 보고」 열은 **조건 없이 행마다** 그린다.
       ⚠ 다만 서버 값이 안 오면(flags.state 가 data/empty 가 아니면) 그 칸은
         「확인 중」/「확인 못 함」이고 — **그때도 두 낱말이 화면에 없다.**
         즉 V 의 증거 문장은 「열이 없다」와 「열은 있는데 서버 값이 없다」를 **못 가른다.**
[문]   GET /api/dsm/events/upper-report/flags 에 역할 거절이 **없다**
       (api_u24.py:335 — tenant_scoped 만. U4 읽기 전용도 GET 은 통과가 설계다)
       그러므로 「읽기 전용이라 403」은 **후보가 아니다.**
[자료] 2026-09-21 03:41 실측 — U4 계정으로 커널이 내는 사건 **20행 이상**
       (기본 기간은 「전체 기간」 — EventList.tsx:294 parsePeriod 의 기본값 'none')
       ⚠ 오늘 probe 씨앗 4건이 지워졌지만 **목록은 여전히 안 비었습니다.**
```

### 남은 후보는 **둘**이고, **둘 다 눌러야 갈립니다**

1. 목록이 아직 안 그려졌는데 본문을 읽은 것 — **타이밍**
   (`flags` 는 `enabled: idsKey.length > 0` 이라 **행이 선 뒤에야** 나갑니다. 즉 이 칸은
   표보다 **한 박자 늦게** 글자가 생깁니다.)
2. 그 순간 `flags` 문이 실패해 칸이 「확인 못 함」이었던 것.

★ **더 싼 확인 하나를 제안합니다** — 지금 증거 문장은 두 사실을 한 글자로 덮습니다.
그 칸이 「확인 중」/「확인 못 함」일 때를 **증거에 따로 적게** 하면(문구 셋을 각각 세면),
다음 측정에서 이 두 후보가 **한 번에 갈립니다.** 판정기는 Q 의 파일이니
「보고 표시/보고함/확인 중/확인 못 함 중 무엇이 보였나」를 적어 달라고 **Q 에 청하시면** 됩니다.

저는 **「고쳤다」고 적지 않았습니다** — 제 보고(`U24.md` ②)에 이 행은 **빨강 그대로** 뒀습니다.

— 차선 U24
