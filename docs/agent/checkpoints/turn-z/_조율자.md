# 턴 Z — 조율자 (영실) · 2026-09-21

## 대표 결정 여섯 — 받은 그대로

```
① DOCKER 자동시작 켰
② 씨앗 그대로 지워라 (4·42·4·3)
③ 창 2a 09-23 12:00 열어라. C0 에서 물어라.
④ 준비되면 대표가 다시 얘기한다
⑤ 넓혀라 — 접속 로그는 빼고
⑥ 훈련 구역으로 울려라
```

★ 지시서 별지(따라하기)는 ⑤를 「이번 턴은 표만 · 다음 턴 결정」이라 적었는데 **대표가 이미
답을 줬다.** 그러므로 U56 은 **표를 만들고 그 표대로 집행**한다 — 조건 「접속 로그는 빼고」.

★ ③ 의 일시는 별지 예시(14:00)가 아니라 **대표가 준 12:00** 이다.

---

## ① Docker 자동 시작 — **세 겹 중 둘만 섰다** [조율자 실측 2026-09-21]

| 겹 | 지금 | 근거 |
|---|---|---|
| ① 컨테이너 재시작 정책 `unless-stopped` | **10/10** | `verify_autostart.py` |
| ② Docker Desktop 자동 시작 | **섰다** | `settings-store.json` 의 `AutoStart = True` · HKCU `Run` 에 `Docker Desktop` |
| ③ `com.docker.service` StartType | **`Manual` · Stopped** | `Get-Service` |

`python scripts/verify_autostart.py` → **rc 1 (빨강)**.
판정기 문장 그대로: 「Run 키에 Docker Desktop 이 있어도 **엔진 서비스가 수동이면 올라오지
않는다**」 — 이것이 09-18 아침 **0/10** 의 뿌리였다.

★ 제 셸로 한 번 더 시도했고 **또 거절당했다**:
```
Set-Service com.docker.service -StartupType Automatic
  → Access is denied            (지금: Manual / Stopped)
```
**못 한 것을 못 했다고 적는다.** ③ 은 **대표 계정의 관리자 PowerShell** 로만 된다.

⚠ 그리고 **재부팅이 아직 없다** — 「재부팅 아침 N/10」 실측은 여전히 **없다**. G5 는 ◐ 그대로.

---

## ② 씨앗 삭제 — **집행했다** [조율자 실측 2026-09-21]

### 지우기 전에 다시 쟀다 — 수가 달랐다

| 지워지는 것 | 대표께 올린 수 | **집행 시점 실측** |
|---|---|---|
| DetectionEvent (probe) | 4 | **4** (#295402~295405 · 같은 4건) |
| DeliveryRecord (CASCADE) | 42 | **61** (+19) |
| EventClip (CASCADE) | 4 | **4** |
| DsmReportRun (연결만 끊김) | 3 | **4** (+1) |

발송이 +19 인 이유는 **씨앗이 그 사이 19건을 더 낳았기 때문**이고, 그것이 바로 대표가
삭제를 승인한 사유 그 자체다. 되돌릴 수 없는 일이라 **그냥 넘기지 않고 대표께 다시 물었다**
→ 「**같은 4건 그대로 지워라**」.

### 스냅샷 먼저

`docs/agent/evidence/P-184/deleted_probe_snapshot_20260921.json` · **73행**
(사건 4 + 발송 61 + 클립 4 + 보고 링크 4). 되돌리진 못해도 **무엇이 있었는지는 남는다.**

### ★ 집행하다 제품의 가드에 막혔다 — 두 가지를 알았다

```
core.base.DependencyCheckError: This record cannot be deleted,
  because there is already data in use on the system
  [DEBUG can_delete] DetectionEvent has 1 dependencies: dsm report runs.event: 1 records
```

1. **dj-core 가 지우기 전에 의존성을 본다.** `DsmReportRun.event` 는 `SET_NULL` 인데도
   가드는 그것을 「쓰이는 자료」로 세어 **거절한다.**
2. **이 표들은 SafeDelete 다.** 그냥 `delete()` 는 **소프트 삭제**다 —
   지난 턴에 「all_objects 로도 0」이라 적었던 것은 강제 정책을 줬기 때문이다.

dj-core 는 **§0.4 금지구역**이라 고치지 않는다. 대신 **대표가 승인한 결과 그대로**
(「보고 링크는 연결만 끊고 행은 둔다」)를 **명시적으로 먼저** 한 뒤 하드 삭제했다.

### 결과

```
LINKS_BROKEN 4                       (DsmReportRun.event → None · 행은 남음)
DELETED 69  {DeliveryRecord: 61, EventClip: 4, DetectionEvent: 4}
probe 사건  4 → **0**                 (all_objects 로도 0 — 소프트 아님)
발송       286 → **225**  (−61)
클립        30 → **26**   (−4)
보고 행      9 → **9**    (안 줄었다 · event_null 4 → 8)
```

⚠ **다음 턴이 알아야 할 것**: 발송 분모는 이제 **225** 다. 턴 X 가 끝난 자리와 같은 수인데,
그 사이 씨앗이 61 을 낳아 286 이 됐다가 돌아온 것이다. **줄어든 것이 결함이 아니다.**

### ㉡ 감사 체인 재심 — 삭제가 사슬을 상하게 하지 않았다

```
verify_evidence_chain.py --db   →  EXIT 0
① 정적 — 체인 밖에서 태어나는 감사 행 0건 (쓰는 손 1곳)
② 표  — 감사 행 3320 (체인 3031 · 등재된 끊김 23) · **새로 난 어긋남 0건**
        머리 해시 2a10f5d596474534…   (sha256 앞 16자)
```

★ **제가 이 판정기를 거꾸로 불렀다** — 먼저 gx-shell **안에서** 불러 회색(exit 2)을 받았다.
이 판정기는 **호스트에서** 불러야 한다(제가 `docker exec` 로 위임한다). 안에서 부르면
docker 를 못 찾는다. `gate_servers.sh` 는 **안에서**, 이것은 **밖에서** — **방향이 반대다.**
→ 조율자 오판 ① (아래)

---

## ③ 창 2a — **2026-09-23 12:00 · C0 에서 대표께 묻는다**

일시가 정해졌다. U56 이 절차서(`evidence/P-178/RUNBOOK_창2a.md`)에 박는다.
**이번 턴에 창을 열지 않는다.** 오늘은 준비만.

창이 하는 것 셋: **A** `gx-shell` 재생성(★되돌릴 수 없음 · 보존 5분 먼저) · **B** gunicorn
재활용 값(OPS-13a) · **C** DB 역할 전환(회수가 앱 전환보다 먼저) — **C0 에서 한 번 멈춘다.**
`⛔C7 NOSUPERUSER` 는 **이번 창에서 안 한다.**

## ④ 스테이징 · 운영 주소 — 대기

「준비되면 대표가 다시 얘기한다」. **일곱 턴째**다. 없으면 RC-1(09-25)에 `CR ①` 과 `P-90`
을 **회색 채로** 낸다 — 회색을 초록으로 적지 않는다.

---

## 조율자 오판 — 자진

### ① `verify_evidence_chain.py` 를 컨테이너 **안에서** 불렀다
회색(exit 2)을 받고 「못 쟀다」가 나왔다. 이 판정기는 **호스트에서** 부르는 것이다.
지난 턴 오판 ⑨(`gate_servers.sh` 를 호스트에서 부름)와 **정확히 반대 방향의 같은 병**이다.
두 턴 연속 「어디서 부르는가」를 틀렸다.
→ 기억에 **방향을 둘 다** 적는다: `gate_servers.sh` 는 **안**, `verify_evidence_chain.py` 는 **밖**.

### ② 스냅샷을 뜨며 `Collector.field_updates` 의 모양을 **추측으로** 세 번 고쳤다
판마다 구조가 다른 내부 자료를 읽으려 했다. 세 번째에 **표에 직접 물어서** 한 번에 됐다.
「내부를 읽지 말고 표에 물어라」 — 처음부터 그랬어야 했다.

## 창 표 (겹침 0 을 대조할 자리)

| 차선 | 잰 시각 (차선이 적어 낸다) | 겹쳤나 |
|---|---|---|
| F — 설정 캐시 · OPS-18 | (F 보고에서) | |
| V — 단독 40분 | (V 보고에서) | |
| 전량 시험 20분 | (조율자) | |

---

## 병합 중 조율자가 직접 한 것 · 조율자 오판 추가

### ③ 제 차선 지시문이 틀렸다 — 「프런트 빌드는 호스트에서 됩니다」
`frontend/node_modules` 가 **없다**. A·U24·U3 세 차선에 그렇게 적어 보냈다.
A 가 알아채고 `gx-fe-build` 안 `/tmp/acheck` 에 격리 복사해 돌렸고(원본 `/app` 무손상)
**침묵을 안 믿고 A/B 로 확인**(일부러 형 오류를 넣으니 3건이 떴다)했다 — 옳은 처신이다.
제 지시문이 그 수고를 만들었다.

### ④ 화면을 걸지 않으면 다섯 절이 전부 회색이다 — 조율자가 걸었다
A 는 `routes.ts` 에 경로만 등재했고 `App.tsx` 는 **남의 파일**이라 등록 요청만 남겼다.
소유표에 `App.tsx` 가 없어 **조율자가 병합 자리에서 걸었다**(제품 코드 8줄):
`DsmSettingsRules` lazy 선언 + `dsm2Routes.settingsRules.path` 등록.
형제(`/dsm/system` 등)와 같은 모양이고 **변수 조각이 없어 삼키지 않는다.**
⚠ 이것은 **판정기가 아니라 제품 배선**이다 — P-203 은 판정기를 겨눈다.

### ⑤ U3 의 쪽지 ①을 되살렸다
A 가 `Q.md` 를 덮어 U3 의 쪽지 ①이 사라졌고, A 는 **지어내지 않고 「못 되살렸다」고 신고**했다 —
옳다. 그 원문은 **U3 가 조율자에게 낸 보고에 남아 있어** 조율자가 옮겨 적었다.
내용: `U6#1`·`U6#4` 의 `422` 는 **「본문 대신 쿼리」**(`loc: ["query","name"]`).
★ **A 가 제 일에서 독립적으로 같은 병에 부딪혔다** — 「임계·등급규칙은 인자가 전부 질의,
구역만 `camera_ids` 가 본문」. **두 차선이 같은 자리를 따로 밟았다.**

## 차선이 낸 것 중 조율자가 눈여겨본 것

- **N**: 미측정 후보가 **엄밀히 0건** — 미측정 8절은 회색 판정기를 가진 게 아니라 **`gate:` 가 비었다.**
  「판정기가 회색」이 아니라 **판정기가 없다**. 세 턴째 수가 안 움직인 또 하나의 얼굴.
- **N**: 같은 판정기가 **부르는 자리에 따라 색이 달랐다** 셋(`route_inventory`·`tenant_scope`·`retention_declared`).
  뿌리는 컨테이너의 `/repo/docs` 가 비어 있다는 것 — **조율자가 두 턴 연속 틀린 그 병과 같은 뿌리다.**
- **N**: `OPS-15` 의 초록은 **합성 구역 위에서 난 것**이었다.
- **S**: 머리 해시를 종이 대조 칸으로 삼으면 **매일 「다름」이 나고 매일 나는 빨강은 아무도 안 본다.**
  종이에 박는 값은 그날의 **앵커**여야 한다.
- **U3**: M 축 회색 5행의 사유가 **판정기가 적은 것과 달랐다**(「자격이 없다」→ 거짓).
  실물은 **3002 이 API 가 아니라 정적 SPA 서버** — 문을 잘못 두드렸다.
  ★ **U6#12·#14·#15 의 빨강 셋도 같은 뿌리의 거짓 빨강.**
- **A**: `F-02-c3`·`F-12-c6` 의 row_map 사유가 **이미 낡아 있었다** — `CameraTuning.tsx`(턴 T)가
  벌써 그 문을 부른다. 「부르는 화면 0」이 아니었다.
- **A**: `F-03-c3` 폴리곤은 **지금 문으로는 화면이 못 한다**(`geometry` 가 질의에 실리는 객체 칸).
  못 하는 일을 **누르기 전에 한 줄로 적었다** — 새 문 0개.
- **A**: `F-14-c2` 오탐 줄이 **없다.** 넘어가는 `axes` 에 `verdict` 축은 있는데 종이에 안 그려진다.
  **없는 것을 있다고 적지 않았다.**

---

## ★ 조율자 오판 ⑥ — **창 표를 안 세웠다. 그래서 경합이 났다** [자진]

지시서 §3 은 창을 **시각으로 못 박아** 줬다(F 10:00~10:30 · 전량 시험 11:20~11:40 ·
V 11:45~12:25 · 「겹침 0」). **나는 그것을 그대로 쓰지 않고** 차선들에게
「잰 시각을 적어 내라, 겹침은 내가 대조한다」고만 했다. **대조는 사후이고 경합은 사전에 막는 것이다.**

### 실제로 겹쳤다 [차선들이 적어 낸 것을 조율자가 맞춰 봄]

| 시각 | 누가 | 무엇 |
|---|---|---|
| 12:21~12:23 | U3 | 로그인 4회 |
| ~12:23 | N | `verify_feature_reach` (★ `--no-gates` 를 줘도 영역 ① 게이트가 불린다) |
| 12:25 | Q | 온보딩 재측 |
| ~12:28 | N | `verify_feature_reach --json` |
| **12:29~12:40** | **F** | **설정 캐시 벽시계 창 — 로그인 4회** |
| 12:34:05 | **알 수 없음** | `gxseed_u5_sysop` 로그인 (F 것이 아니다) |

**F 의 세션이 창 안에서 세 번 끊겼다.** 한 계정 한 세션(UX-24)이라 남이 들어오면 끊긴다.
U3 도 「제 로그인이 Q 의 재측과 겹쳤다 — 해는 없었지만 **운이었다**」고 자진했다.
**두 차선이 같은 병을 자진했고 원인은 내 창 표다.**

### 그런데 F 의 결론은 살아남았나 — **설계가 경합을 견디게 돼 있었다**

F 는 경합을 **탐지했고**(끊긴 구간이 전부 401 로 드러남) **판정에서 뺐다.** 그리고
한 팔로 재지 않았다 — **같은 순간 두 벌을 짝지어** 재고, 쌍마다 순서를 뒤집고,
**A/A 대조**(두 팔 모두 켬 → 전부 못 증명 · 옳은 널)와 **맞바꿈**(부호가 전부 뒤집힘)을 붙였다.
앱을 안 믿고 **DB 쪽에서 스캔 수를 따로 셌고 그 수도 함께 뒤집혔다.**
경합은 **양쪽 팔에 똑같이** 들어오므로 쌍차를 체계적으로 밀지 못한다.

⇒ 조율자 판정: **PERF-04 의 방향(캐시가 벽시계를 줄인다)은 경합에도 불구하고 선다.**
   **크기(7~11 ms · 14~19%)는 경합 창에서 잰 수**이므로 `[차선 보고 · 경합 창]` 으로 적고,
   **정본으로 삼지 않는다.** 다시 재는 것은 다음 턴 — 그때는 창 표를 먼저 세운다.
   F 가 스스로 적은 「안 한 말」 넷(운영 서버 아님 · 동시 1 의 수 · 상용 아님 · 401 자리 없음)도 그대로 싣는다.

### 다음 턴 규약
**창은 조율자가 시각으로 나눠 준다 — 차선이 고르지 않는다.** 그리고 창을 여는 차선은
**열기 전에 「지금 로그인한 사람 있나」를 감사 표로 한 번 본다.**

---

## ★ 조율자 오판 ⑦ — **없던 삭제를 만들 뻔했고, 차선이 막았다** [U24 가 거절 · 옳다]

나는 U24 에게 이렇게 시켰다:

> 감사 화면의 **「대상 없음」 2행**에 「삭제된 사건 · 대표 결정 2026-09-20 · 스냅샷 P-184」를 적어라.

**그 2행은 삭제된 사건이 아니었다.** U24 실측:
```
upper_report:set:999999999   막힘 · HTTP 404   「사건이 없거나 남의 테넌트다」
```
**없는 번호를 가리킨 막힌 시도**다. 거기에 「삭제된 사건 · 대표 결정」을 적었으면
**일어나지 않은 삭제를 화면이 증언**하게 된다 — 감사 화면에서 그것은 가장 나쁜 종류의 거짓말이다.
U24 는 **거절하고 턴 Y 의 글자(「사라진 사건 · 기록된 결정 없음」)를 그대로 뒀다.**

**내 잘못의 모양**: 나는 「2행」이라는 **수만 보고 그 행이 무엇인지 안 봤다.** 지난 턴 오판 ④
(「연쇄 표를 한 건만 재고 전부라 적었다」)와 **같은 병**이다 — 수를 옮겨 적고 실물을 안 봤다.
U24 는 「**스냅샷 P-184**」도 거절했다(GX-COPY §4 가 절 ID·내부 경로를 사용자 본문에서 금한다).
⚠ 그런데 **막은 것은 게이트가 아니라 사전**이었다: `verify_ui_copy.py` 의 절 ID 정규식이
숫자 **1~2자리**만 봐서 `P-184` 를 **못 잡는다.** 판정기가 놓친 자리다 — 다음 턴 Q.

## ★ U24 가 찾은 것 — **종이가 훈련이라 말하지 않는다**

`drill` 사건으로 1쪽을 실물로 뽑아 확인: 완성 HTML 에 **「훈련」 0건 · `drill` 0건**,
`incident_report.py`·`docx_export.py` 에 grep **0건**.
**청구는 훈련을 빼는데 종이는 훈련이라 말하지 않는다.**
⇒ **파 4 검수(09-24~25)에서 drill 사건을 고르면 안 된다.** 정본 DOCX 는 실사건 4798 로 뽑았다.
⇒ 그리고 **U1 이 오늘 만든 훈련 구역의 사건도 같은 병을 갖는다.**

**DOCX 에서 가려야 할 칸(실제로 찍힌 값)**: 수신자 메일 5건 · `drill:webpush:…` 기기 토큰 ·
판정자 계정명 · 발행자 계정명 · 설치 주소(번지). **앞의 둘은 가리기 전에 밖으로 내면 안 된다.**

## ★ U56 의 넓힘이 U24 의 턴 도중에 일어났다 — 둘이 전후를 갈라 쟀다

| 잰 시점 | 채널 | 화면이 읽는 행 | 사건을 가리키는 행 | **화면이 침묵하는 행** |
|---|---|---|---|---|
| 넓힘 **전** | 2 | 1,678 | 2 | **0** |
| 넓힘 **후** | 15 | 2,066 | 273 | **271** |

대표 결정 ⑤ 를 집행하자 **화면이 아무 말도 못 하는 행이 0 → 271** 로 드러났다.
**넓힘이 빚을 만든 것이 아니라 덮여 있던 빚을 드러냈다** — 온보딩이 내려간 것과 같은 모양이다.
U24 가 그중 **채널 이름 15개**에 우리말을 줬고, 나머지 **180행은 못 줬다**(고칠 자리가
`audit.py` = U56 의 파일). **다음 턴에 한 차선이 둘 다 쥔다.**

---

## 프런트 컴파일 — **조율자 실측** (세 차선 + 조율자의 변경이 쌓인 뒤)

아무도 이것을 못 했다. `frontend/node_modules` 가 **호스트에도 컨테이너에도 없고** gx-shell 엔
node 자체가 없다. U24 는 `npx tsc` 의 빈 출력을 **한 번 「오류 0」으로 읽을 뻔했다**고 자진했다 —
**거짓 초록 직전이었다.** A 만 `gx-fe-build` 안에 격리 복사해 돌리는 길을 찾아냈다.

조율자가 같은 길로 **합쳐진 판**을 쟀다 (`gx-fe-build` · node 18.20.8 · `/tmp/zcheck` 격리):

```
node /app/node_modules/typescript/bin/tsc -p tsconfig.app.json --noEmit
  TSC_EXIT=2 · 총 오류 줄 **3234**   ← A 가 잰 기준선과 **같다**
  우리가 만진 넷(App.tsx · SettingsRules.tsx · AuditLog.tsx · routes.ts) = **17줄**
    → 17 전부 App.tsx 의 `rj-core` import(15~36 · 121). **우리가 만진 줄이 하나도 없다.**
    → SettingsRules.tsx 0 · AuditLog.tsx 0 · routes.ts 0
```

★ **첫 판은 `TSC_EXIT=127` 이었다** — `./node_modules/.bin/tsc: not found`(심링크가 끊겼다).
그때 출력은 「우리가 만진 파일 **0건**」이었다. **명령이 안 돌았는데 초록으로 보였다.**
종료 코드를 같이 안 찍었으면 나도 U24 가 빠질 뻔한 그 자리에 빠졌다.

★ **침묵을 안 믿고 A/B 로 확인**: 우리가 넣은 줄 바로 옆(269행)에 일부러 형 오류를 심으니
App.tsx 오류가 **17 → 18**. **판정기가 우리 줄을 실제로 본다.** 심은 것은 컨테이너 안 사본이고
되돌렸다(컨테이너 잔여 0 · **호스트 파일은 애초에 안 만졌다**).

⇒ **우리 변경이 더한 컴파일 오류 0건.** 다만 이것은 **형 검사**이고 **화면이 실제로 뜨는지는 V 가 잰다.**
