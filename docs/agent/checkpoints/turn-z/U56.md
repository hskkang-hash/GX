# 턴 Z · 차선 **U56** — 2026-09-21

> 소유: `backend/apps/dsm/audit.py` · `backend/common/billing_marks.py` · 계량 커널 경유
> 이 보고의 수는 전부 **[실측]**이고, 잰 시각을 붙였다. **색은 안 적는다**(대장은 N 의 파일).

---

## ① 대표 결정 ⑤ — `READABLE_LOGGER_NAMES` 를 넓혔다 (접속 로그는 빼고)

### 먼저 표 — **화면이 닿는 수 N → M**

[실측 2026-09-21 14:0x · gx-shell ORM · 테넌트로 안 좁힌 전건]

| | 열린 이름 | 행 수 | 화면이 닿는 합 |
|---|---|---|---|
| **전(N)** | 2개 | — | **1,698** |
| **후(M)** | 15개 | — | **3,369** (**×1.98**) |
| 안 여는 것 | 접속 로그 5개 | 152,884 | — |
| 표 전체 | 20개 | 156,253 | — |

⚠ 이 수는 **흐른다** — 게이트가 도는 동안 표가 계속 자란다(같은 이름들을 13:1x 에는
1,678 / 3,346 으로 쟀다). **비율(약 2배)과 이름 목록이 사실이고, 절대수는 잰 시각의 사진**이다.

### 넣은 이름 — 15 (기존 2 + 턴 Z 신규 13)

| 로거 이름 | 행 | 무엇이 새로 보이나 |
|---|---|---|
| `guardianx.f12.settings` | 1,696 | (기존) F-12 설정 변경 허용·차단 |
| `guardianx.u24.events` | 2 | (기존) 상급 보고 체크·해제 |
| `guardianx.law02a.retention` | 1,049 | **LAW-02a 보존기간 파기** — 미리보기·실제 파기가 처음 화면에 |
| `guardianx.dsm.response` | 254 | **대응 상태 전이**(접수→진행→종결) — 누가 언제 무엇을 닫았나 |
| `guardianx.test.law08_race` | 219 | LAW-08 체인 경합 시험(P-191)이 **운영 표에 남긴 행** |
| `guardianx.dsm.notify` | 84 | **알림 규칙 변경** · 오탐 고지 |
| `guardianx.dsm.field_reply` | 36 | **현장 회신** |
| `guardianx.k2.heartbeat` | 14 | OPS-14 **일일 맥박 요약**(「수신자 16명 중 0명 도달 · 실패 16건」 같은 경고가 여기 있다) |
| `guardianx.law07.privacy_request` | 7 | **LAW-07 열람·삭제 청구** 접수·마스킹본 조회·회신 |
| `guardianx.u1.event_note` | 2 | 사건 메모 |
| `guardianx.dsm.push_subscription` | 2 | 웹푸시 구독·시험발송 |
| `guardianx.sec.otp_reset` | 1 | P-113 OTP 재설정 |
| `guardianx.role_request` | 1 | P-105 역할 부여 요청(거부) |
| `security.otp_reset` | 1 | ↑의 **옛 이름**으로 태어난 행 |
| `gx.role_request` | 1 | ↑의 **옛 이름**으로 태어난 행 |

★ **여는 목록이지 거르는 목록이 아니다.** 「접속 로그만 빼라」를 `exclude` 로 쓰면
내일 dj-core 가 로거를 하나 더 쌓을 때 그것이 **자동으로 화면에 뜬다.** 여는 쪽으로 적으면
새 이름의 기본값은 「안 보임」이고, 보이게 하려면 사람이 한 줄을 고쳐야 한다.

### 뺀 이름 ①: **접속 로그 5** (대표 조건) — 이름으로 뺐다

| 로거 이름 | 행 | 무엇인가 · 왜 빼나 |
|---|---|---|
| `db` | 81,704 | dj-core 요청 로그. **컨테이너 사설 IP 225행**(`172.18.0.7`)과 **우리 대장 이름 `D-290` 3행**이 본문에 있다 — `verify_ui_secrets` 의 「결정 번호」 패턴 그 자리 |
| `jwt` | 46,897 | dj-core 요청 로그(인증 경유) |
| `application` | 20,445 | dj-core 요청 로그(주로 401/403 본문) |
| `security` | 3,106 | 로그인 로그. **`note` 3,012행이 "User (계정) logged in successfully from (IP)."** — `read_page` 는 `note` 를 `reason` 칸으로 화면에 **그대로** 낸다 ⇒ **접속자 IP 노출** |
| `user_update` | 732 | dj-core 사용자 조회·수정 로그 |
| **합** | **152,884** | `db`+`jwt` 만 127,807 — 대표께 보고된 「약 13만」이 이 둘이다 |

빼는 사유는 **둘**이다: ㉠ 대표 조건(접속 로그) · ㉡ **누출**(위 `security` · `db`).
그리고 수로도 그렇다 — 넣으면 감사 화면은 **접속 기록 뷰어**가 되고,
상한 200(`PAGE_SIZE_MAX`)짜리 표에서 우리 감사 행은 98% 의 요청 로그 밑에 묻힌다.

### 뺀 이름 ②: **0행이라 못 잰 넷** — 재지 못한 것은 초록이 아니다 (D-400)

| 로거 이름 | 행 | 사유 |
|---|---|---|
| `guardianx.k5.credentials` | **0** | **자격증명 접근 감사.** 첫 행이 어떤 글을 담을지 **실물로 못 봤다.** 안 보고 여는 것은 특히 이 채널에서 나쁘다 |
| `guardianx.k5.thresholds` | **0** | K5 임계값 변경 — 실물 0 |
| `guardianx.sec.key` | **0** | 키 회전 — 실물 0 |
| `guardianx.dsm.drill` | **0** | 훈련 창 — 실물 0 |

**화면에 올리는 것은 되돌리기 어렵다.** 첫 행이 생긴 날 그 행을 눈으로 보고 넣는다.
이 넷은 `audit.py` 에 **이름으로** 적어 두었다 — 모르는 것이 아니라 **아는 빚**이다.

### ★ 누출 울타리 — 무엇을 어떻게 쟀나

**㉠ 행 안을 직접 훑었다** [실측 2026-09-21 · gx-shell ORM · **후보 20개 전수**]
`note` · `api_name` · `username`(= 화면의 `reason` · `action` · `actor` 칸)을
`verify_ui_secrets` 의 **금지 패턴 6종** + **IPv4 · 이메일 · 전화 · 주민번호** 로 훑었다.

- 새로 여는 13개 채널: **IPv4 0 · 이메일 0 · 전화 0 · 주민번호 0 · 금지 패턴 0**
- `client_ip` 칸: 새로 여는 13개는 전부 **비어 있다**(접속 로그 5개만 차 있다).
  그리고 `read_page` 는 애초에 `client_ip` 를 **안 낸다**.
- LAW-07 은 구조적으로 위험한 자리라 7행을 **눈으로 전수** 봤다: `note` 에는
  접수번호(`GX-PR-20260919-6D9C81`)만 있고 **청구인 이름·연락처는 `data_after` 페이로드**에
  산다 — `read_page` 가 내는 10칸에 그 칸이 **없다**.

**㉡ 살아 있는 API 면을 훑었다** [실측 2026-09-21 12:3x · gx-shell 안 · runserver 재기동 뒤]
`/api/dsm/audit` 을 `gxseed_u4_official`(U4 · 읽기 전용)로 **11쪽 2,066행 전수** 받아
응답 JSON 의 **문자열 필드 전수**를 같은 6종으로 훑었다 → **0건**. 채널 15개 확인.

  ⚠ **처음 잰 판은 거짓이었다** — 채널 2 · 1,694행이 나왔다. 8000 의 runserver 가
    `--noreload` 라 **09-20 08:02 에 올라온 옛 모듈**을 들고 있었다. `gate_servers.sh restart`
    뒤에 다시 재서 15채널 · 2,066행을 얻었다(메모리 「응답 캐시가 장애를 덮는다」 계열).

**㉢ 판정기 두 면** — `scripts/verify_ui_secrets.py`

| 면 | 어디서 | 결과 |
|---|---|---|
| `--self-test` | 호스트 | **통과 — 양성 1 · 음성 3** (exit 0) |
| ① 렌더 문자열 | 호스트 | **833개 파일 · 패턴 6종 · 0건** |
| ② API 본문 (`--api`) | **gx-shell 안** | **6개 라우트 · 0건 [실측]** · exit **0** |

★ **게이트를 넓힌 곳에 물렸다.** `API_PATHS` 에 **`/api/dsm/audit` 을 더했다**(5→6).
  안 더하면 「API 면 초록」이 **넓힌 그 면을 한 번도 안 본 초록**이다.
  읽을 권한이 없는 자격(U1·U3)으로 돌면 403 본문을 재고, 그 본문에도 패턴은 없다.

**㉣ 울타리 자체를 시험으로 묶었다** — `backend/tests/test_u56_audit_channels.py` (신설)

결정문은 배선이다. 글로만 있는 울타리는 다음 사람이 한 줄로 무너뜨린다. 네 가지를 묻는다:

1. 울타리 ∩ 접속 로그 = **∅** (글자)
2. 울타리에 **중복 이름 0** — 있으면 「열다섯」이라는 셈이 거짓이다
3. ★ **본체**: 접속 로그 다섯 이름으로 행을 **심어 놓고** `read_page` 를 두드려
   **안 뜨는가**를 본다. 글자만 보면 「목록은 옳은데 화면에는 뜬다」를 못 잡는다.
   같은 시험이 **열어 둔 채널의 행은 뜨는가**도 함께 본다 — 분모 0 인 초록을 막는다.
4. 화면이 내는 `channels` 가 **울타리 그 자체인가**(두 벌이면 한쪽이 거짓말한다) ·
   턴 Z 에 새로 연 이름 하나(`law02a.retention`)의 행이 **정말 뜨는가**
   (아무것도 안 열어도 3번은 초록이기 때문)

⚠ **0행이라 안 연 넷은 시험으로 안 잠갔다.** 그것은 「오늘 실물을 못 봤다」라서 뺀
  **바뀔 결정**이고, 바뀔 결정을 시험으로 못박으면 다음 사람이 결정을 못 바꾸거나
  시험부터 지우고 바꾼다. 그 넷은 `audit.py` 머리말에 **글로** 있다.

**[실측] 체인·대장 칸이 안 망가졌다** — 넓힌 뒤 `/api/dsm/audit` 첫 쪽(50행):
`chain_states = {linked 50 · broken 0 · unchained 0 · unknown 0}` ·
`chain_scan_capped` 안 걸림. 열린 채널이 많아져 한 쪽의 id 폭이 **좁아졌기** 때문이다.

### 안 고른 것 — 판단을 적어 둔다

- `guardianx.test.law08_race` **219행을 넣었다.** 시험 기구가 남긴 행이라 「운영 행위가
  아니다」로 뺄 수도 있었다. 넣은 이유: 그 행들은 **LAW-08 체인의 칸을 차지하고 있고**,
  화면이 못 보는 체인 칸은 화면에서 못 고친다. 빼기로 결정되면 이름 한 줄이다.
- 옛 이름 둘(`security.otp_reset` · `gx.role_request`)을 **넣었다.** 지금 코드는 이 이름으로
  안 쓴다(각 1행). `logger_name__in` 은 정확 일치라 dj-core 의 `security` 와 안 섞인다.
- **「우리 언어」는 이번 판단 기준이 아니다.** 새로 여는 행의 상당수가 `P-67` · `S-16` ·
  `턴 Y` 같은 개발 대장의 말을 담는다(`law02a.retention` 1,038/1,049 · `dsm.response`
  219/254). ⚠ 다만 이것은 **새로 난 일이 아니다** — 이미 열려 있던 `f12.settings` 도
  1,696행 중 **289행**이 그렇다. 금지 패턴 6종에는 안 걸린다(0건). **U24 에 쪽지**로 넘겼다.

---

## ② 계량의 직접 셈 셋 → **커널 경유** · 글자 상수 0

### 어디로 보냈나 — **K6**, 그리고 그것은 추측이 아니다

`metering.py` 가 적어 둔 빚은 *"카메라·계정·미디어 장부를 세는 커널 함수가 저장소에
없다 … 갚는 날: 계량 커널 면(W4-1)"* 이었다. **그 말이 틀렸다** [실측]:

- `DA-04 §2 K6` 표: `usage_snapshot(group, period)` → **채널수·이벤트수·저장량 등 (W4-1 UsageSnapshot)**
- `DA-04 §4` 티켓표: **`W4-1` 이 K6 줄에 있다**
- `kernels/k6_feedback/__init__.py` · `tests/test_k6_feedback_kernel.SURFACE`:
  `usage_snapshot` 이 **이미 공개 면 4개 중 하나**

없던 것은 **커널 면이 아니라 그 면의 구현**이었고, 빚 문서가 그 둘을 같은 말로 적었다.
그래서 **새 커널을 세우지도, K1(이벤트)·K2(발송)에 장부 셈을 얹지도 않았다** — 둘 다
그 빚이 경고한 바로 그 일이다.

### 배선 (두 칸)

```
apps/dsm/metering.py   _cameras · _users · _storage  →  _ledgers (한 달에 한 번만 두드린다)
apps/dsm/services.py   count_billable_ledgers        ←  「K1 의 App 소비자는 하나」와 같은 규약
kernels/k6_feedback    usage_snapshot                ←  common/billing_marks 한 곳에서 거른다
```

- `_alive` 를 **지웠다.** 그 세 줄은 `billing_marks.exclude_soft_deleted` 와 **두 벌**이었고
  (그 함수 주석이 *"이 세 줄이 `metering.py::_alive` 였다"* 라고 적어 둔 그 자리),
  두 벌이던 동안 어느 쪽이 정본인지 아무도 안 물었다.
- **한 달에 한 번만 두드린다.** 칸마다 문을 만들면 최근 6달 표가 커널을 18번 두드린다.
- `billing_marks.has_marker_field(model)` 하나를 더했다. **매개변수가 아니라 사실이다** —
  부르는 쪽이 고를 것이 없고 답은 모델이 준다(`exclude_soft_deleted` 가 `deleted` 칸을
  모델에 묻는 것과 같은 규약). `exclude_unbillable` 은 **한 글자도 안 바꿨다**:
  그 함수가 칸을 스스로 묻게 만들면 누가 `track_id` 를 이름 바꾼 날 K1 의 거름이
  **조용히 통과**가 된다. 큰 소리로 죽는 쪽이 낫다.

### 글자 상수 0 — AST 로 쟀다 (지난 턴과 같은 자)

```
[실측 2026-09-21 · gx-shell] apps.dsm.metering 의 모든 함수 본문(독스트링 뺀)에서
  "apps.get_model(" · "_base_manager" · ".objects.filter(" · "models.Model"
  → offenders = []          (턴 Y: {_alive, _cameras, _users, _storage})
AppStaysThinTest.METERING_ORM_DEBT = frozenset()   ← 빈 집합으로 줄였다(늘리는 것은 금지)
```

파일 전체 날것(주석·독스트링 포함)에는 4건이 남는데 **전부 「왜 그렇게 생겼나」를 적은 글**이다.
설명은 죄가 아니다 — 호출이 죄다(그 시험이 AST 로 묻는 이유).

### A/B — **수가 안 바뀐 것을 쟀다**

[실측 2026-09-21 · 같은 순간 · `ETRI-Group` · 2026-09 · 옛 구현을 손으로 재현해 대조]

| 칸 | 옛 구현 | 커널 경유 |
|---|---|---|
| 카메라 | 15 | **15** |
| 쓰는 사람 | 30 | **30** |
| 저장 바이트 | 1,493,917,814 | **1,493,917,814** |
| 파일 수 | 115 | **115** |

`metering.usage(scope=…, month="2026-09")` 도 다섯 칸 전부 `state=ok` 로 나온다.

### ⚠ 커널로 갔다고 씨앗이 빠진 것은 아니다 — **수로 적었다**

[실측 2026-09-21] 세 표에 **표식 칸(`track_id`)이 없다**:

```
stream_monitors.StreamMonitor   track_id 없음 · deleted 있음
user.CoreUser                   track_id 없음 · deleted 있음
file_management.UserMediaFile   track_id 없음 · deleted 있음
```

⇒ `exclude_unbillable` 을 **걸 자리가 없다**(걸면 FieldError). 그래서 이 세 장부에서는
**씨앗이 지금도 청구에 든다**: `ETRI-Group` 의 살아 있는 카메라 12대 중 **8대**가 우리가
심은 것(`GX-ONB-V-*` 6 · `GX-SEED-DSM` 1 · `gxprobe-*` 1), 계정 105개 중 **11개**가
`gxseed_*` · `gxprobe_*` 다. **다른 테넌트 넷은 0이다.**

  ★ **이번 턴에 새로 난 구멍이 아니다** — 앱이 직접 셀 때도 똑같이 들어 있었다.
    달라진 것은 **고칠 자리가 하나**라는 것이고, 표식 칸이 생기는 날
    `has_marker_field` 가 저절로 참이 되면서 이 셈이 같이 고쳐진다.
  ★ **이름으로 거르지 않았다.** `username__startswith("gxseed")` 는 표식이 아니라
    **추측**이고, 고객 이름 하나가 우리 접두와 겹치는 날 그 계정이 조용히 공짜가 된다(D-280).
  ★ 0으로 덮지 않았다 — `common/billing_marks.py` 머리말 ⚠ 에 수로 적혀 있다.

  ⚠ **카메라 12 → 15 는 40분 사이에 일어났다.** 낮 12시대에 12대(씨앗 8)였고
    오후에 15대다 — 다른 차선의 온보딩 게이트가 `GX-ONB-V-*` 를 세 대 더 심었다.
    **씨앗 비율은 잰 시각의 사진이다.** 다시 셀 때는 다시 재라.

---

## ③ 창 2a 절차서 — 일시를 박았다

`docs/agent/evidence/P-178/RUNBOOK_창2a.md`

- **머리에 박은 것**: `★ 창 2a — 2026-09-23 (수) 12:00 KST · C0 에서 대표께 묻는다`
- **여는 날 순서 여덟 칸 표**를 맨 위로 올렸다(각 칸에 링크·소요·멈춤 조건):
  ① §1 네 조건 → ② §2 기준선 → ③ A0 보존 5분 → ④ A1~A4 → ⑤ B →
  **⑥ ★★C0 멈춤** → ⑦ C0-ⓒ집행 → C1~C6 → ⑧ 되돌림 표
- ⚠ **⑦ 안의 순서 제약 하나**를 순서표에 같이 올렸다: **C0-ⓒ(시험 DB 회수)가 C4 보다 먼저**다
  (앱이 아직 `postgres` 로 돌 때만 남의 DB 를 지울 수 있다. 미루면 되돌릴 수 없는 막다른 길).
- **★ 되돌릴 수 없는 자리 셋**을 머리에 표로 올렸다(본문 §4 와 같은 셋):
  **★A2** `docker rm -f gx-shell` · **★A3-함정** `--entrypoint` 빼먹음 · **⛔★C7** `NOSUPERUSER`
- **⛔ C7 은 이번 창에서 안 한다** — 머리 표와 인용 줄에 **두 번** 적혀 있고, 본문 §C7 절이
  그대로 금지 표지로 남아 있다. 단계 셈 16 에도 안 들어 있다.
- **C0 멈춤 문장**을 그대로 쓸 수 있게 적어 두었다(대표께 물을 한 문장 + 세 칸 ⓐⓑⓒ).
- 맨 아래 §5 에 **오늘 안 한 일**을 수로 적었다: 만든 역할 0 · GRANT/REVOKE 0줄 ·
  지운 DB 0 · 바꾼 `.env` 0 · 재생성한 컨테이너 0 · `docker rm -f` 0회.
- **본문(A·B·C 절 · 되돌림 표 · §4)은 한 줄도 안 고쳤다.** 순서를 위로 끌어올린 것이지
  절차를 바꾼 것이 아니다.

**절차서를 실행하지 않았다.** 창은 09-23 이다.

---

## ④ 못 한 것 · 재지 못한 것

1. **0행 채널 넷을 못 쟀다** — `k5.credentials` · `k5.thresholds` · `sec.key` · `dsm.drill`.
   행이 없어 화면에 무엇이 뜰지 실물로 못 봤고, 그래서 **안 넣었다**. 이름은 코드에 적혀 있다.
2. **미래의 자유 문장은 못 잰다.** `field_reply` · `u1.event_note` 는 관제요원이 **자유 문장**을
   쓰는 칸이다. 오늘 있는 38행에는 개인정보가 0건이지만, 내일 쓰는 문장은 잴 수 없다.
   ⚠ 다만 그 글은 **이미 사건 상세 화면에 뜨고**, 감사 화면의 독자(U2·U4·U5)는 같은
   테넌트 안이다 — **독자를 넓히지는 않았다**. 입력 쪽 마스킹은 이 차선 밖이다.
3. **CSV 내보내기(`/audit/export.csv`)는 안 쟀다.** 같은 `read_page` 를 쓰므로 같은 칸이
   나가지만, **「같은 함수니까」는 측정이 아니다.** 파일 면은 U24 의 자리이기도 하다.
4. **화면(3002)에서 눈으로 안 봤다.** 15채널이 뜬 것은 **API 본문으로** 쟀다.
   `AuditLog.tsx` 가 채널 15개를 어떻게 그리는지(필터·배지)는 U24 의 파일이라 안 건드렸다.
5. **`_storage` 는 커널로 갔지만 여전히 미디어 장부를 믿는다.** 버킷을 안 훑는 이유는
   그대로이고(이 환경은 `minio.invalid`), `unsized` 가 0이 아닌 달의 값은 **하한**이다.
6. **`usage_snapshot` 의 `since` 는 받지만 안 쓴다.** 세 칸이 「그 달 끝 시점의 잔량」이기
   때문이고, 시그니처를 K6 의 다른 함수와 같게 두려고 남겼다 — 독스트링에 적었다.
7. **시험은 부분만 돌렸다** — 아래 ⑤. 특히 `tests/test_u24_audit_target.py` 를
   **제 손으로 안 돌렸다**(같은 시험 DB 를 두 판이 동시에 쓰면 서로를 끊는다).
   [차선 보고 · U24 2026-09-21] 넓힘 뒤 **13 passed** 라 적혀 있다 — 그것은 **입력이지
   제 측정이 아니다**. `event_ref` 칸을 더한 뒤로는 **아무도 안 돌렸다**
   (칸 추가는 앞뒤 대조의 양쪽에 똑같이 붙으므로 안 깨질 것이라 **예상**한다 — 예상이다).
8. **전체 시험(`pytest backend/tests`)은 안 돌렸다.** 한 판이 4~5분이고 차선 넷이 같은
   DB 이름 공간을 쓴다. 제가 만진 면 셋(`test_dsm_app` · `test_k6_feedback_kernel` ·
   `test_u56_audit_channels`)만 돌렸다.

---

## ⑤ 고친 파일 · 돌린 것

| 파일 | 무엇 |
|---|---|
| `backend/apps/dsm/audit.py` | `READABLE_LOGGER_NAMES` 2 → **15** · `ACCESS_LOG_LOGGER_NAMES` 신설(아는 것을 적는 자리) · `read_page` 에 `event_ref` 칸 · 사유 전문 |
| `backend/tests/test_u56_audit_channels.py` | **신설** — 울타리를 시험으로 묶었다(위 ①-㉣) |
| `backend/tests/test_k6_feedback_kernel.py` | 낡은 `usage_snapshot raises` 시험 교체 · `UsageSnapshotLedgersTest` 4건 신설 |
| `backend/common/billing_marks.py` | `has_marker_field` 신설 · 머리말에 **표식 칸 없는 표 셋**과 씨앗 수 |
| `backend/kernels/k6_feedback/services.py` | `usage_snapshot` **구현**(`NotImplementedYet` 제거) + `_billable_count` · `_billable_storage` |
| `backend/apps/dsm/services.py` | `count_billable_ledgers` 문 신설 |
| `backend/apps/dsm/metering.py` | `_alive` 삭제 · `_cameras` · `_users` · `_storage` 를 `_ledgers` 경유로 · 머리말 정정 |
| `backend/tests/test_dsm_app.py` | `METERING_ORM_DEBT` → **빈 집합** · 사슬 시험에 `_ledgers` / `count_billable_ledgers` / `usage_snapshot` 등재 |
| `scripts/verify_ui_secrets.py` | `API_PATHS` 에 **`/api/dsm/audit`** (5→6) |
| `docs/agent/evidence/P-178/RUNBOOK_창2a.md` | 일시 박음 · 여는 날 순서 8칸 · ★셋 표 · C0 멈춤 문장 · §5 |

돌린 것:

```
verify_ui_secrets --self-test        exit 0 (양성 1 · 음성 3)
verify_ui_secrets  (렌더 면)          833파일 · 6종 · 0건
verify_ui_secrets --api (gx-shell)   6라우트 · 0건 · exit 0        ← event_ref 추가 뒤 다시 돌렸다
verify_layers.py                     분모 83 · 위반 0 · exit 0
verify_tenant_scope.py               통과 · exit 0
pytest tests/test_dsm_app.py              exit 0  (AppStaysThinTest 포함)
pytest tests/test_k6_feedback_kernel.py   **23 passed** · exit 0
pytest tests/test_u56_audit_channels.py   **5 passed** · exit 0   ← 울타리 시험(신설)
감사 API 전수 훑기                     11쪽 2,066행 · 6종 · 0건
A/B 대조 (옛 구현 vs 커널)            네 칸 전부 일치
```

---

## ⑥ **지시서 밖에서 한 것 하나** — `read_page` 에 `event_ref` 칸 (U24 요청)

지시서의 세 일 밖입니다. **먼저 적습니다** — 돌려놓으라 하시면 세 줄입니다.

**왜 했나**: U24 가 같은 턴에 쟀습니다 — 제가 넓힌 뒤 「사건을 가리키는 행」이
**2 → 273** 이 됐는데, 화면의 「대상」 칸은 `action` 문자열
(`^upper_report:(set|clear):\d+$`) **한 모양만** 읽어서 **180행이 침묵한다**고.
그 행들은 사건 번호를 `action` 이 아니라 `data_*` 페이로드에 들고 있고,
**싣는 자리가 `apps/dsm/audit.read_page` — 제 파일**입니다.
넓혀 놓고 「보이는데 이름을 못 붙인다」를 남기는 것은 결정 ⑤ 를 반만 집행한 것입니다.

**무엇**: `items` 의 각 행에 칸 하나 추가(**기존 10칸은 이름도 값도 그대로**).

```python
"event_ref": evidence_chain.event_ref_of(
    {"data_before": r.data_before, "data_after": r.data_after})
```

- **판정식을 새로 안 썼다** — `common/evidence_chain.event_ref_of`(차선 S 의 순수 함수)를
  그대로 부른다. `dangling_event_refs` 도 같은 함수를 부르므로 **화면이 세는 수와 체인이
  세는 수가 같은 자에서 나온다.**
- **질의 0건 증가** — `rows` 는 이미 모델 인스턴스라 두 칸이 같이 실려 와 있다.
- **누출 아님** — 내는 것은 **사건 번호 하나**다. 페이로드의 나머지(LAW-07 청구인
  이름·연락처가 사는 그 자리)는 이 함수가 내는 칸에 **없다**.
  `verify_ui_secrets --api` 를 추가 뒤 다시 돌려 **0건 · exit 0**(정수라 문자열 훑기에 안 걸린다).
- **[실측 2026-09-21]** 열린 15채널 3,386행 중 `event_ref` 를 든 행 **292**.

⚠ **이 칸은 아직 아무것도 안 그립니다.** `_with_target_column` 은 U24 의 `api_u24.py` 이고
**한 줄도 안 건드렸습니다.** 그리는 것은 U24 의 판단입니다 — 그리고 번호가 생겼다고
「삭제된 사건 · 대표 결정」 글자를 **자동으로 주면 안 됩니다**(`gone` 151행은 여전히 회색).
제가 낸 것은 **번호지 사유가 아닙니다.**

⚠ **서버를 한 번 재기동했다**: `gate_servers.sh restart`(2026-09-21 12:35~12:36 · api 8000 ·
spa 3002). `--noreload` runserver 가 옛 모듈을 들고 있어서다. 다른 차선이 그 창에 걸렸다면
제 탓이다 — 둘 다 「대답한다」로 돌아온 것을 확인했다.

---

## 🔔 [U24 → U56] 2026-09-21 — 쪽지 잘 받았습니다. **셋 다 답하고, 수를 하나 돌려드립니다**

### 0. 먼저 — **당신의 넓힘이 제 화면의 수를 바꿨고, 저는 전후를 둘 다 쟀습니다**

| 잰 시점 (UTC) | `READABLE_LOGGER_NAMES` | `gxseed_u4_official` 이 보는 행 | 사건을 가리키는 행 | 화면이 **이름 붙이는** 행 | **침묵하는 행** |
|---|---|---|---|---|---|
| 2026-09-21 ~03:2x (넓힘 **전**) | 2 | 1,678 | 2 | 2 | **0** |
| 2026-09-21 03:41:12 (넓힘 **후**) | 15 | **2,066** | **273** | 2 | **271** |

당신이 12:3x 에 적은 **2,066 과 제 수가 같습니다.** 두 사람이 다른 길로 같은 수를 냈습니다.

### 1. 채널 셀렉트 — **없습니다. 하지만 당신 말이 맞았습니다**

이 화면의 필터는 기간 · 행위자 번호 · 행위 앞머리 셋이고 `channels` 배열을 **안 그립니다**.
그러니 15개가 셀렉트로 쏟아지는 일은 없습니다.

★ **기계 말이 새던 곳은 표의 「채널」 칸이었습니다.** 종전 그 칸은 이름 **둘**만 우리말로
바꾸고 나머지는 서버 글자를 그대로 적었습니다. 채널이 둘일 때 그 「나머지」는 **0 건**이라
아무도 안 봤는데, 넓힌 지금은 담당관 한 사람의 화면에서 **368 행**이 그 길로 떨어집니다:

```
[실측 2026-09-21 03:45 UTC · U4 테넌트 좁힘 · 전체 2,076행]
  f12.settings 1,706 · dsm.response 233 · dsm.notify 84 · dsm.field_reply 36 ·
  law07.privacy_request 7 · u24.events 2 · u1.event_note 2 · dsm.push_subscription 2 ·
  sec.otp_reset 1 · role_request 1 · security.otp_reset 1 · gx.role_request 1 ·
  law02a.retention 0 · k2.heartbeat 0 · test.law08_race 0
```

→ **`CHANNEL_WORD` 열다섯**을 세우고 GX-COPY 에 올렸습니다(「턴 Z 추가 ②」 절).
규율 둘을 지켰습니다:
- **모르는 채널을 지어내지 않습니다** — 표에 없으면 「우리말 이름 없음」, 원래 이름은 `title` 에만.
  (「기타」로 적으면 **모르는 것이 분류된 것처럼** 보입니다.)
- **`guardianx.test.law08_race` 를 꾸며 적지 않습니다** — 화면 글자는 **「시험이 남긴 행」**입니다.
  U4 테넌트에서는 지금 **0 행**이지만, 이름을 안 주면 뜨는 날 기계 말이 뜹니다.
  ⚠ 그 채널을 화면에 여는 것이 옳은지는 **제 판단 자리가 아닙니다** — 다만 열려 있다는
    사실과 화면이 그것을 뭐라 부르는지는 적어 뒀습니다(전역 관리자 면에서는 행이 있습니다).

> ★ **채널을 새로 여는 일에는 「화면에 우리말 한 줄을 올리는 일」이 포함됩니다.**
> 안 올리면 그 채널은 담당관 화면에서 우리 폴더 구조를 읽어 줍니다(사전 §4).

### 2. CSV — **안 쟀습니다. 회색입니다**

「같은 함수니까」로 초록을 적지 않습니다. ⚠ 다만 하나 적어 둡니다: CSV 의 `channel` 열은
**서버가 내는 기계 이름 그대로**입니다. 파일이라 사전이 금한 자리는 아니지만, 이제
**화면의 낱말과 파일의 낱말이 갈립니다** — 두 장을 대조하는 사람이 「대응 처리」와
`guardianx.dsm.response` 를 같은 것으로 읽어야 합니다. 상한(`limit=1000`) 쪽도 안 쟀습니다.

### 3. ★★ `target_states` 의 네 갈래 — **여기가 당신 쪽지의 ③에 대한 답이고, 제 이번 턴의 수입니다**

넓힘 후 273 행의 갈래(커널 문 `services.event_detail` 로 한 건씩 물어 본 값):

```
live  91  ·  deleted_by_decision  29  ·  gone  151        (합 273)
  그중 화면이 이름 붙이는 것 = 2      ← 둘 다 upper_report:set:999999999 (막힌 시도 · 404)
  화면이 아무 말도 못 하는 것 = 271   ← 그중 대상이 없는 것 180 (dsm.response 163 · field_reply 17)
```

★ 당신이 쪽지에 적은 **「턴 Y 의 29건이 화면에서는 2행이었던 까닭」이 정확히 맞았습니다.**
그리고 넓힌 지금 그 **29 가 화면 위로 올라왔습니다** — 차선 S 가 턴 Y 에 잰 수와 **정확히 같습니다.**

### ⚠ 그런데 **저는 그 180 행에 글자를 못 줍니다 — 당신의 파일이 막고 있습니다**

「대상」 칸은 `api_u24.target_event_id(action)` 이 정하고 그 함수는
`^upper_report:(set|clear):([0-9]+)$` **한 모양만** 읽습니다. 새로 들어온 180 행은 사건 번호를
`action` 이 아니라 **`data_before`/`data_after` 의 `event_id`** 에 들고 있습니다.
읽는 **정본 함수는 이미 있습니다** — `common/evidence_chain.event_ref_of()`(순수 함수 · 차선 S).

> **막힌 곳은 딱 하나입니다: `audit.read_page` 의 `items` 가 `data_before`/`data_after` 를 안 싣습니다.**
> 그 파일이 당신 것이라 **한 줄도 안 건드렸습니다.**

제안(집행은 조율자·N 의 몫으로 넘깁니다): 다음 턴에 **그 두 자리를 한 차선에** 주십시오.
넓힌 쪽과 말하는 쪽이 갈려 있으면, **넓힘이 만든 침묵 180 행**은 다음 턴에도 그대로 남습니다.

— 차선 U24 (보고 전문 `docs/agent/checkpoints/turn-z/U24.md`)
