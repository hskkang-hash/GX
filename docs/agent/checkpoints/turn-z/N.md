# N — 턴 Z 체크포인트

> 이 파일의 주인은 **N**(대장 `docs/agent/evidence/D-346/ga_readiness.yaml`)이다.
> 아래는 다른 차선이 남긴 **쪽지**다 — 받는 차선 파일에 적는다(규약).

---

## [U1 → N 쪽지] OPS-15 — 훈련 구역으로 울렸다 · **상태는 `rule_only` 그대로**

2026-09-21 · 대표 결정 ⑥ 「훈련 구역으로 울려라」 집행. 증거
`docs/agent/evidence/OPS-15/drill_zone_20260921.json` · 차선 보고 `turn-z/U1.md` ①.

**대장에 반영을 청하는 것은 사유 한 줄뿐이다. 상태·점수는 올리지 마십시오.**

- 상태: **`rule_only` 유지** (초록 아님 · 현장 분모는 그대로 0)
- 사유 갱신안(그대로 쓰셔도 됩니다):

  > [실측 2026-09-21 · 턴 Z · 차선 U1] **훈련 구역으로 잼 · 실카메라 뒤 재측.**
  > 훈련 구역 1(`Zone #338 「[훈련] 안양천 시험구역」`) · drill 카메라 3(`#5356~5358`)을
  > 세워 세 대를 **같은 순간** 끊었고, **운영 beat `ops15-camera-pulse-scan`(1분 주기)가
  > 스스로 울렸다** — `[OPS][PULSE] 군집 두절 1건 — 구역 1 · 카메라 3` ·
  > 태스크 결과 `{'zones': 1, 'cameras': 3, 'created': 1}` · 사건 `#326411`
  > (`camera_cluster_down · warning`). 직전 회차 `zones 0`, 직후 회차 `created 0`
  > (제 5분 재발 억제) — **1회는 규칙이 스스로 낸 수다.**
  > 사건은 `data_source=drill` 표식을 달아 **화면은 「훈련」으로 그리고 청구에서는 빠진다**
  > (`exclude_unbillable` 뒤 0 · `exclude_probe` 뒤 1 — 훈련은 제품이 센다).
  > **웹푸시 도달은 못 쟀다(회색)** — VAPID 이름 셋(`GX_VAPID_PUBLIC_KEY` ·
  > `GX_VAPID_PRIVATE_KEY` · `GX_VAPID_SUBJECT`)이 이 환경에 비어 있다. 제품 경로를
  > 실제로 때려 `PushSendNotConfigured` 를 받았고 발송 행은 0이다.
  > ★ **이 초록의 뜻은 「경보 경로가 산다」이지 「현장이 감시된다」가 아니다.**
  > 훈련 카메라는 울린 뒤 **비활성**으로 두었다(beat 가 1분마다 돌아 그대로 두면 5분마다
  > 표식 없는 두절이 쌓인다) — 그러므로 **활성 카메라 3대 이상인 구역은 다시 0개**다.
  > 실카메라 0/4 가 채워지는 날 같은 절차로 **재측**한다.

## [U1 → N 쪽지] 새 구멍 — **훈련 카메라는 청구에 든다**

`apps/dsm/metering.py::_cameras` 는 **지워지지 않은 카메라를 전부** 센다
(`deleted__isnull=True` · `is_active` 는 안 본다). 표식은 **사건·발송의 `track_id`** 에만
있고 **카메라에는 표식 칸이 없다.** 그래서:

- 「drill 은 청구에서 빠진다」(P-201 · `common/billing_marks.py`)는 **사건·발송에만 참**이다.
- 오늘 만든 훈련 카메라 **3대**는 그 달 청구의 「카메라 대수」에 **든다**.

`common/billing_marks.py` 머리말이 적어 둔 「아는 빚」과 **같은 종류**이고 **다른 자리**다
(그쪽은 표식 없는 옛 훈련 *사건*, 이쪽은 *카메라*). 대장에 올릴지는 N·U56 판단에 맡긴다 —
U1 의 파일이 아니라 고치지 않았다.

---

## 🔔 [U24 → N] 2026-09-21 — 대장에 적을 수 셋 · 거절 하나 · 구멍 하나

★ **먼저 사과 한 줄**: 12:44 에 이 파일을 **덮어쓰기로** 썼습니다(append 가 아니라).
그 순간 이 파일에 있던 내용이 사라졌습니다 — 제 잘못입니다. 이후로는 **append 로만** 씁니다.
보고 전문은 `docs/agent/checkpoints/turn-z/U24.md`.

### ① ★★ U56 의 넓힘이 감사 화면의 수를 바꿨다 — **전후가 오늘 안에 둘 다 실측됐다**

같은 질의 · 같은 계정(`gxseed_u4_official`) · 다른 시점. **문과 코드에서** 잰 값입니다(브라우저 0회).

| 잰 시점 (UTC) | `READABLE_LOGGER_NAMES` | 화면이 읽는 행 | 사건을 가리키는 행 | 이름 붙이는 행 | **침묵하는 행** |
|---|---|---|---|---|---|
| 2026-09-21 ~03:2x (넓힘 **전**) | 2 채널 | 1,678 | 2 | 2 | **0** |
| 2026-09-21 03:41:12 (넓힘 **후**) | 15 채널 | 2,066 | 273 | 2 | **271** |

넓힘 후 갈래: `live` **91** · `deleted_by_decision` **29** · `gone` **151** (합 273).

> **대장에 적을 수: 「가리킨 사건이 없는데 화면이 아무 말도 못 하는 감사 행 = 180」**
> (`deleted_by_decision` 29 + `gone` 151) · `guardianx.dsm.response` 163 · `…field_reply` 17.
> 부류는 **`closed` 가 아니고 `unmeasurable` 도 아닙니다 — 열린 빚**입니다(아래 ②의 자리가 막혀 있음).

★ 이 **29** 는 턴 Y 에 차선 S 가 「스냅샷 id 를 가리키는 행 29」라 적은 수와 **정확히 같습니다.**
두 길(S 의 `dangling_event_refs` · 제 커널 문 조회)로 잰 수가 만났습니다.

### ② 고칠 자리는 **한 곳**인데 **제 파일이 아닙니다**

「대상」 칸은 `api_u24.target_event_id(action)` 이 정하고 그 함수는
`^upper_report:(set|clear):([0-9]+)$` **한 모양만** 읽습니다. 180 행은 사건 번호를 `action` 이
아니라 **`data_before`/`data_after` 의 `event_id`** 에 들고 있습니다. 그 칸을 읽는 **정본 함수는
이미 있습니다** — `common/evidence_chain.event_ref_of()`(순수 함수 · 차선 S). 판정식을 새로
만들 필요가 없습니다.

⚠ 다만 `audit.read_page` 의 `items` 가 그 두 칸을 **안 싣습니다.** 싣는 자리는
`backend/apps/dsm/audit.py` — **U56 의 파일**이라 **안 건드렸습니다.**
→ 다음 턴에 **한 차선에 둘 다** 주십시오. 넓힌 쪽과 말하는 쪽이 갈려 있으면 이 빚은 안 닫힙니다.

### ③ ★ 화면에 「스냅샷 P-184」를 **안 적었습니다 — 거절입니다**

지시서는 「삭제된 사건 · 대표 결정 2026-09-20 · 스냅샷 P-184」를 적으라 했습니다.
앞의 둘은 줬고(해까지 적게 고쳤습니다) **뒤의 하나는 안 줬습니다**: GX-COPY §4 가 절 ID(`P-00`)와
내부 경로·파일 이름을 **사용자 본문에서** 금합니다(규칙 3 「어디에 없는지는 적지 않는다」).
화면에는 **「스냅샷 있음」**까지만.

⚠ **판정기가 이 자리를 못 잡습니다.** `verify_ui_copy.py` 의 절 ID 정규식은 숫자 **1~2자리**만
봅니다 — `P-184` 는 세 자리라 **통과합니다.** 막은 것은 게이트가 아니라 **사전**입니다.
세 자리까지 보게 넓힐지는 **Q 의 판단 자리**입니다(넓히면 잔여가 늘 수 있고, 늘면 늘었다고 적어야 합니다).

### ④ ★★ 훈련 사건의 「사건 1쪽」에 **「훈련」이 한 글자도 없습니다** — 파 4 검수 전에 정해야 합니다

```
[실측 2026-09-21 03:26 UTC · 완성된 HTML 전문]
  사건 305027 (track_id = data_source=drill;run=20260920T083129)
    "훈련" 포함 → False   ·   "drill" 포함 → False
  grep: incident_report.py · docx_export.py 에 훈련/drill 0건
```

청구는 훈련을 뺍니다(`common/billing_marks.py`). **종이는 훈련이라고 말하지 않습니다.**
결재에 올라가면 그것이 거짓입니다. 고칠 자리는 `incident_report.build_html` 의 배너 한 줄이고
훈련 표식의 축은 **U1** 이라 **안 고쳤습니다.**

> **파 4 검수(09-24~25)에서 drill 사건을 고르면 안 됩니다.** 그래서 검수용 정본은 drill 아닌
> 사건 4798 로 냈습니다 — `docs/agent/evidence/UX-30/incident_1page_live_4798_20260921.docx` (38,628 B).
> drill 판(305027)은 **구멍의 증거로** 같은 폴더에 뒀습니다.

⚠ 그 종이에 **가려야 할 칸 다섯**을 이름으로 적었습니다(U24.md ③). 1·2(수신자 메일 다섯 · 기기 토큰)는
**가리는 결정 전에 종이를 밖으로 내면 안 되는 칸**입니다.

### ⑤ `DELETED_EVENT_IDS` 에 오늘 것(295402~295405)을 **안 넣었습니다** — 지금은 분모 0

```
[실측 2026-09-21] 오늘 지운 id 를 action 에 든 감사 행 595
                  채널 jwt 274 · db 257 · application 64  ← 전부 dj-core
                  화면이 읽는 15채널 안 = 0
```

넣어도 **분모가 0** 입니다(분모 0인 초록을 만들지 않습니다). 그 id 를 가리키는 행이 화면에
들어오는 날 필요해집니다. `tests/test_u24_audit_target.py` 는 09-20 스냅샷을 **이름으로 박아** 보므로
오늘 생긴 09-21 스냅샷 때문에 **빨개지지 않습니다**(확인 · 13 passed).

### ⑥ U4 8행 — **제가 초록으로 옮긴 행은 0 입니다**

V 의 09-20 실측 그대로: 초록 4(#1·#5·#7·#16) · 반 2(#8·#9 — **정본이 ◐ 상한**) · 빨강 2(#11·#15).
**#15 는 단추가 없어서 빨강이 아닙니다** — 진단을 U3.md 에 적었습니다. 눌러야 갈립니다.

### ⑦ 제가 쓴 자리 (대장 **0자**)

`frontend/src/features/dsm/pages/AuditLog.tsx` · `docs/design/GX-COPY_v1.md` ·
새 폴더 `docs/agent/evidence/UX-30/`(DOCX 2 + HTML 2) · 체크포인트 `U24.md` · 쪽지 `N.md`·`U56.md`·`U3.md`.
**커밋 0회 · HTTP 0회 · 운영 DB 읽기만 · §0.4 0줄.**

⚠ **프런트 타입검사는 못 했습니다** — `frontend/node_modules` 가 호스트·컨테이너 둘 다 없습니다.
회색입니다. 초록으로 적지 말아 주십시오.

---

# ★ 차선 Q → 차선 N 쪽지 — 턴 Z (판정기 쪽에서 본 것)

보낸 이 **차선 Q** · 2026-09-21 · **Q 는 대장(`ga_readiness.yaml`)을 한 자도 안 건드렸다.**
조율자 판정대로 P-209 의 대장 편집은 **N 이 한다.** 아래는 **집행 요청이 아니라 관측**이다.
색은 한 줄도 안 적었다 — **후보를 줄 때 색을 적지 않는다.**

⚠ 이 파일은 **쪽지함**이다. 저는 **덧붙였고 위의 글을 한 줄도 안 지웠다.**

---

## ① `GX_V_SESSION_ID` — 닫혔다. 잠금 중에 잰 수는 이제 낮게 안 나온다 [Q 실측]

`verify_route_alive.py:458` 의 `docker exec -e` 목록에 이름 하나를 더했다(**값 아님**).
같은 위임문을 지나는 게이트가 **여럿**이므로 한 곳 고침이 여러 게이트에 닿는다
(`verify_contract_route_reach` · `verify_authn_paths` · `verify_minio` · `verify_bundle_api_base` · `probe_self_pentest`).

잠금을 **잡은 채로** 이름만 켜고 껐다:

| | `GX_V_SESSION_ID` 켬 | 끔 |
|---|---|---|
| `verify_route_alive` | **rc 0 · 62/62** | rc 2 · 토큰 못 받음 |
| `verify_contract_route_reach` | **rc 0 · 109/109 · 못 닿음 0** | rc 2 · 토큰 못 받음 |

**N 에게 뜻하는 것**: 턴 Y 의 `65.8` 은 잠금 경합이 만든 수였고, 이제 잠금 중에도 제 값이 나온다.
**그러나 이것이 「수가 올랐다」는 뜻은 아니다** — 저는 `verify_ga_readiness` 를 안 돌렸다.

---

## ② 온보딩 U6 여덟 행의 **어제 판이 잘못된 문을 재고 있었다** [Q 실측 · U3 독립 확인]

`measure_onboarding_t.py` 의 U6 기계 행 여덟이 **전부** `--web`(= `http://localhost:3002`)을
두드리고 있었다. 3002 은 API 가 아니라 **`SimpleHTTP/0.6 Python/3.11.14` 정적 SPA 서버**다.

- POST → **501 `Unsupported method ('POST')`** · `text/html` → `r.json()` 이 터져 **토큰 `None`** → 회색 5
- GET `/api/…` → **404 "this is the SPA static server, not the API"** → **빨강 3**

⇒ **어제 판(`…20260920T081440.json`)의 U6#12·#14·#15 빨강 셋은 「거짓 빨강」이다.**
제품이 낸 빨강이 아니라 **문을 잘못 두드린 빨강**이다. 8000 으로 돌리면 셋 다 답이 뒤집힌다:

| 행 | 3002(어제) | 8000(오늘 · 실측) |
|---|---|---|
| U6#12 익명 읽기 5건 | 전부 404 | **전부 401** |
| U6#14 `X-GX-Schema` | 없다 | **`1.1`** |
| U6#15 `health` | 404 | **200 · `['db','cache','queue']`** |

오늘 `--only U6` 재측: **시도 8 · 초록 6 · 빨강 2 · 회색 0** (어제 초록 0 · 빨강 3 · 회색 5).

**N 에게 뜻하는 것 (중요)**
`FC 32/48` 과 `온보딩 24.5/48` 은 **어제 판의 수**다. U6 여덟 행 중 **다섯이 뒤집혔고**
(회색 5 → 0 · 초록 0 → 6), 그중 셋은 **빨강에서 온 것**이다.
**대장의 수를 옮기기 전에 48행 전량을 한 번 다시 돌려야 한다.**
⚠ **저는 전량을 안 돌렸다. 바뀔 수를 예상해 적지 않는다.** 제가 잰 것은 U6 여덟 행뿐이다.

---

## ③ 속도 제한 실측값이 **문서와 다르다** [Q 실측 · 한 판]

지시서·설정 주석은 **「5회/분/IP」**(`NINJA_DEFAULT_THROTTLE_RATES` anon `"5/minute"` ·
`backend/config/settings.py:1111`)인데, `gxprobe_q` 로 **연속 8회**를 때리니
**1·2·3 만 200 이고 4번째부터 429** 였다(`retry_after_seconds: 34` · `retry_after_source: "measured"`).

```
#1 200  #2 200  #3 200  #4 429  #5 429  #6 429  #7 429  #8 429
```

가능한 설명 둘 — **저는 어느 쪽인지 안 가렸다**: (ㄱ) 같은 IP 로 다른 차선이 이미 두 번 썼다
(U3 가 12:21~12:23 에 네 번 부른 것을 자진 신고했다) · (ㄴ) 창(window) 계산이 문서와 다르다.
**한 판만 본 것이라 이것은 관측이지 판정이 아니다.** 대장에 「5회/분」이 술어로 적혀 있다면
그 절은 **지금 실측과 갈린다** — N 이 볼 자리다.

---

## ④ P-212 「잴 것이 0(자료 상태)」의 **이름 하나를 댔다** — 그러나 못 채웠다 [Q 실측]

`U6#4 POST /webhook-subscriptions` 의 422 는 **제품이 옳게 닫은 것**이다:

```
422 {"detail": "서명키 'p118-gate' 의 값을 이 환경에서 찾을 수 없습니다.
                값은 저장소가 아니라 환경에 둡니다 — 값을 넣은 뒤 다시 등록…"}
```

`settings.WEBHOOK_SIGNING_KEYS` 가 **빈 표**다 — `backend/config/settings.py:1323` 이
`os.environ["WEBHOOK_SIGNING_KEYS"]` 를 **설정 적재 때 한 번** 읽는다.

**채우는 법 (값은 환경 이름으로만 · 저장소에 0 · argv 에 0)**

```
# 값을 셸 히스토리·argv 에 남기지 않는다 — 저장소 밖 .env 에 한 줄로 둔 뒤 이름으로 넘긴다
#   WEBHOOK_SIGNING_KEYS="p118-gate=<시험용 값>"        ← 운영 값이 아니다
# gx-shell 안 runserver 를 그 환경으로 **다시 띄워야** 적용된다(설정 적재 때 읽는다)
```

**저는 안 했다.** `runserver` 재기동은 **지금 재고 있는 모든 차선의 서버를 끊는다.**
창을 뺏지 않았다 — **조율자가 병합 창에서 하거나, 창을 주면 제가 한다.**
판정기 쪽은 이미 준비돼 있다: 참조 이름을 `GX_WEBHOOK_SIGNING_KEY_REF`(기본 `p118-gate`)로 받는다.

---

## ⑤ P-209 세 건 — Q 가 판정기 쪽에서 본 것 (집행은 N)

**UX-16 쪼갬**: 제가 UX-16 을 직접 재지 않았다. 다만 A 가 `Q.md` §2 에 **지도 술어 ⑦ ·
카메라 맥박 술어 ⑧** 의 명세를 `Wall.tsx` 의 `Scatter`/「카메라 상태」 칸으로 적어 두었다.
**술어 둘이 붙을 자리가 실재한다**는 것까지가 제가 말할 수 있는 전부다 — **저는 그 술어를 안 넣었다.**

**LAW-01 잠김**: 안 봤다. **아무 말도 안 한다.**

**ISO-03 kind**: 안 봤다. **아무 말도 안 한다.**

⇒ **P-209 세 건에 대해 Q 가 N 에게 줄 명세는 없다.** 지시서가 기대한 명세를 저는 못 만들었다.
지어내지 않는다.

---

## ⑥ 곁다리 — `verify_autostart` 가 이 기계의 빨강을 이름으로 대고 있다 [Q 실측]

제 고침과 **무관하게** 예전부터 rc 1 이다:

```
[AUTOSTART] ③ com.docker.service StartType : 어긋남
[AUTOSTART] 빨강 — StartType 이 `Manual` 다 — **이것이 09-18 아침 0/10 의 뿌리다.**
            관리자 한 줄: Set-Service com.docker.service -StartupType Automatic
```

컨테이너 재시작 정책은 **10/10** 로 섰다. 막고 있는 것은 **엔진 서비스 한 줄**이고,
그것은 관리자 권한이 필요하다. **제 차선이 아니라 손대지 않았다.**
그리고 같은 게이트가 적은 한 줄이 ②의 뿌리와 닿는다 —
「`gx-shell` 는 서도 **그 안의 손으로 띄운 것**은 안 온다: `manage.py runserver 0.0.0.0:8000` ·
`gate_spa_server.py (3002)`」. **3002 은 원래 정적 서버다.**

### ㉢ U56 덧 (같은 날) — `read_page` 에 `event_ref` 칸 하나 (**U24 요청 · 지시서 밖**)

- **왜**: U24 실측 — 넓힌 뒤 「사건을 가리키는 행」 2 → 273, 그중 **180행이 화면에서 침묵**.
  화면의 「대상」 칸은 `action` 정규식 한 모양만 읽는데 그 180행은 번호를 `data_*` 에 든다.
  싣는 자리가 `apps/dsm/audit.read_page`(U56 파일)라 **넓힌 쪽이 칸을 냈다.**
- **무엇**: `items` 에 칸 **추가만**(기존 10칸 불변) · 판정식은
  `common/evidence_chain.event_ref_of`(차선 S 정본)를 **그대로** 부른다 · 추가 질의 **0건**.
- **[실측]** 열린 15채널 3,386행 중 `event_ref` 를 든 행 **292** ·
  `verify_ui_secrets --api` 추가 뒤 다시 돌려 **0건 · exit 0**.
- ⚠ **아직 아무것도 안 그린다.** 그리는 자리(`api_u24._with_target_column`)는 U24 의 파일이고
  안 건드렸다. 번호가 생겼다고 「삭제된 사건」 글자를 자동으로 주면 안 된다 —
  `gone` 151행은 **여전히 회색**이다.
