# U1 — 턴 Z 체크포인트

갱신 2026-09-21 · 소유: `scripts/probe_events.py` · `backend/kernels/k2_notify/services.py` · 훈련 구역 자료

---

## ① ★ 대표 결정 ⑥ — OPS-15 「훈련 구역으로 울려라」

**[차선 보고 · 실측]** 운영 표에 썼다(시험 DB 가 아니다). 증거:
`docs/agent/evidence/OPS-15/drill_zone_20260921.json`

| 무엇 | 수 | 어떻게 확인했나 |
|---|---|---|
| 훈련 구역 | **1** | `Zone #338 「[훈련] 안양천 시험구역」 · kind=camera_group · is_active=true · group 4` — ORM 재조회로 확인 |
| drill 카메라 | **3** | `#5356·5357·5358 「[훈련] 안양천 시험카메라 1~3」` · 구역에 M2M 로 묶임 |
| **경보** | **1회** | ★ **운영 스케줄러가 울렸다** — 손으로 부르지 않았다 |
| 웹푸시 도달 | **0 (회색)** | 자격 부재 · 아래 ④ |

### 어떻게 울렸나 — **손이 아니라 beat 가 울렸다**

세 대의 맥박을 **같은 순간**(`record_frame(at=지금-6분)`)으로 끊었다. 문턱 5분 밖 ·
동시 창 5분 안(간격 0). 그리고 **기다렸다.** 1분마다 도는 운영 태스크가 잡았다:

```
[2026-09-21 10:30:30,559: WARNING/ForkPoolWorker-30750] [OPS][PULSE] 군집 두절 1건 — 구역 1 · 카메라 3
Task stream_monitors.camera_pulse_scan_beat[716cdc1c-…] succeeded: {'zones': 1, 'cameras': 3, 'created': 1}
```

★ **앞뒤 회차가 이 한 줄의 뜻을 정한다**:
직전 회차들 `{'zones': 0, 'cameras': 0, 'created': 0}` — **구역이 0개였다**(그것이 이 결정의 이유다) ·
직후 회차 `{'zones': 1, 'cameras': 3, 'created': 0}` — 규칙이 제 5분 재발 억제로 **한 번만** 울렸다.
즉 **1회**는 내가 세서 1이 아니라 **규칙이 스스로 1을 냈다.**

사건: `#326411 · camera_cluster_down · warning · 03:30:30Z · 카메라 5356(가장 먼저 끊긴 대표)`.

### 표식 — `data_source=drill` 하나만

`track_id = data_source=drill;run=20260921T102945`(`common.probe_marker.drill_mark()`).

- `is_drill_track` **참** · `is_probe_track` **거짓** — 두 표식은 다르다(P-201)
- **제품은 센다**: `exclude_probe` 뒤 남는 수 **1** ← 의도대로다
- **청구는 안 한다**: `exclude_unbillable` 뒤 남는 수 **0**
- **화면은 「훈련」이라 말한다**: `apps.dsm.services.event_data_source` → `"drill"` ·
  목록 갈래(`event_data_sources`) → `{326411: "drill"}`

⚠ **표식은 규칙이 안 적는다 — 심은 쪽이 적었다.** `camera_pulse.scan_clusters` 는
`record_detection` 에 `track_id` 를 안 넘긴다(그 파일은 내 것이 아니라 고치지 않았다).
그래서 beat 가 만든 행에 **내가 곧바로** 표식을 얹었다(같은 실행 · 초 단위).
표식은 「만든 쪽이 적는다」(`common/probe_marker.py` 머리말)는 규약 그대로지만,
**그 몇 초 동안 그 행은 실사건으로 보였다** — 적어 둔다.

### 뒤처리 — 구역은 남기고, 카메라는 껐다

구역 행 · 카메라 3행 **그대로 남겼다**(지우지 않았다). 다만 **세 대를 `is_active=False`** 로 뒀다.

> 이유: beat 가 **1분마다** 돈다. 끊긴 채 활성으로 두면 5분마다 **표식 없는** 군집 두절이
> 영원히 쌓인다 — 하루 288건 · 청구에 들고 관제 큐를 덮는다. 그 288건에는 아무도 표식을
> 안 얹는다(나는 그때 없다).
> 지금 판정: `구역 카메라 0대 — 군집 규칙은 3대부터 본다` · `fires=false`.
> **다시 울리려면**: 세 대를 활성으로 되돌리고 맥박을 끊으면 된다(같은 절차 · 2분).

⚠ **대신 잃은 것도 적는다**: 이로써 「활성 카메라 3대 이상인 구역」은 **다시 0개**다.
오늘 증명된 것은 **경보 경로가 산다**이지 **현장이 감시된다**가 아니다 — 그것이 절 상태를
`rule_only` 로 두는 이유이기도 하다.

### 절 상태 — `rule_only` **유지** (대장은 N 이 쓴다)

`docs/agent/checkpoints/turn-z/N.md` 에 쪽지를 남겼다. **초록으로 올리지 않는다.**

### ★ 오늘 발견한 구멍 하나 — **훈련 카메라는 청구에 든다**

`apps/dsm/metering.py::_cameras` 는 **살아 있는 카메라를 전부** 센다. 표식은 사건·발송의
`track_id` 에만 있고 **카메라에는 표식 칸이 없다.** 그러므로 오늘 만든 훈련 카메라 3대는
그 달 「카메라 대수」에 **든다** — 「drill 은 청구에서 빠진다」가 **사건에만** 참이다.
(지금은 비활성이지만 `_cameras` 는 `is_active` 가 아니라 `deleted` 만 본다.)
N·U56 에게 알린다 — 이 차선의 파일이 아니다.

---

## ② F-04 억제 키 확정 — **키는 한 곳 · 시험 넷은 한 계약을 가리킨다**

**세종 계약**: 억제 키는 `event_id` 가 **아니라** 「같은 스트림 + 같은 유형 + 직전 발송 시각」.

### 고친 것

- `backend/kernels/k2_notify/services.py`
  - **`_suppression_key(event)` 신설** — 키가 사는 **한 곳**. `suppress` 의 질의는
    `**_suppression_key(event)` 로 펼친다(키를 손으로 다시 적지 않는다).
  - 턴 Y 가 남긴 유보(「지금 구현은 상위집합 · 좁히는 판단은 네 시험의 주인들과 함께」)를
    **계약 문장으로 교체**했다. 이유도 함께 적었다: *억제는 사람에게 「같은 일이 또 왔다」를
    막는 것이지 사건 번호를 막는 것이 아니다.*
  - 문안은 **GX-COPY 사전 그대로** 인용했다(짧은 말 「5분 안에 같은 사건 재발송 억제」 ·
    화면의 말 「새로 보낸 알림이 없습니다. …」). 사전의 **「같은 사건」의 뜻**이
    「같은 사건 번호」가 아니라 「같은 카메라에서 같은 종류의 일」임을 못박았다.
  - ⚠ **동작은 안 바뀌었다** — 턴 Y 의 질의가 이미 이 키였다. 바뀐 것은
    **어디에 적혀 있는가**와 **누가 그것을 지키는가**다.

### 모은 시험 넷 → 계약 정본 하나

새 파일 **`backend/tests/test_f04_suppression_key.py` (10 시험 · 전부 초록)** 이 키의 정본이다.
흩어져 있던 넷은 **그대로 두고**(각자 제 갈래를 계속 잰다) 각 파일에 **정본을 가리키는 줄**을 넣었다:

| 흩어져 있던 시험 | 들고 있던 조각 |
|---|---|
| `test_k2_notify_kernel::SuppressionTest` | 옆 사건 접힘 · 자기 사건 접힘 · 5분 밖 안 접힘 · 실패는 근거 아님 |
| `test_u56_notify_repeat_count` | `total=0`(행이 안 는다) · **다른 종류는 안 접힌다** |
| `test_s_webhook_outbox` | 접힌 알림은 **웹훅도 안 나간다** |
| `test_d_mobile_field` | **재알림은 이 판정을 안 본다** |

정본이 재는 것 — 위 네 갈래 + **이 저장소에 없던 대조 둘**:
- ③ **스트림이 다르면 안 접힌다** — 키의 첫 칸을 아무도 안 재고 있었다
- ⑦ **키가 한 곳인가** — `suppress` 본문이 키를 손으로 다시 적으면 빨강(소스를 읽어 잰다)
- 그리고 ⑥ **훈련(`drill:`) 발송은 억제 근거가 아니다** (턴 T · U3 의 계약)

### ★ 탐지기 대조 (이 시험이 눈이 멀지 않았는가)

`_suppression_key` 에 `"event_id": event.pk` 를 **일부러 넣고** 돌렸다 →
**2 failed / 8 passed**(`…neighbour_event_is_also_folded` · `…never_carries_the_event_number`).
되돌리고 다시 → **10 passed**. 즉 「번호로 억제」는 이제 **시험이 잡는다.**

관련 시험 한 묶음 재실행: **149 passed**
(`test_f04_suppression_key` · `test_k2_notify_kernel` · `test_s_webhook_outbox` ·
`test_u56_notify_repeat_count` · `test_d_mobile_field` · `test_u1_probe_not_counted` ·
`test_u1_drill_counted` · `test_k1_event_kernel`).

---

## ③ `probe_events --census` — **표식으로만 센다** (P-201 마무리)

**rc = 0 · probe 사건 0건.**

```
[PROBE] 자기시험 통과 — **출생 표본 10**(양성 3 · 음성 7)
[PROBE] [입력] **표식 하나로 센다** — `track_id` 가 'data_source=probe' 로 시작하는 행
[PROBE] probe 사건 **0건**
[PROBE] id: []
[PROBE] 미처리 — `response_state=occurred` **0** · `status=new` **0**
[PROBE] 0건 — ㉠지움(2026-09-21 · 대표 결정 · 4건) 뒤의 옳은 0이다. 게이트가 다시 심으면 이 수가 다시 오른다
```

### ★ 그 빨강은 「하나를 놓쳤다」가 아니었다 — **둘째 갈래가 다른 것을 세고 있었다**

착수 전 실측(운영 DB):

| 세는 법 | 수 | 무엇이었나 |
|---|---|---|
| 표식 `data_source=probe` | **0** | 대표 결정으로 씨앗 4건을 지웠다 |
| 카메라 이름 `gxprobe` | **4** | 카메라 `#3947 gxprobe-D384-screen 캡처용 카메라` 에 매달린 **훈련(`drill`) 사건 4건** |

즉 카메라 이름 갈래는 **탐침이 아니라 훈련을 세고 있었다.** P-201 이 두 표식을 가른 뒤로
그 갈래는 「probe 를 세는 법」이 아니라 「**그 카메라에 매달린 모든 것을 세는 법**」이 됐고,
이름이 `gxprobe` 라서 그 차이가 조용했다. **이름은 표식이 아니다.**

고친 것: `census()` 에서 카메라 갈래 폐지 · 상수 `PROBE_CAMERA_HINT` **삭제**(이름을 남겨
두면 다음 사람이 그것을 빌려다 또 이름으로 센다) · 0 을 낼 때 **왜 0 인지**를 판정기가 말한다.
두 판정기의 짝맞춤(`tests/test_u1_probe_not_counted.py`)은 **그대로 초록**이다.

⚠ **빌려 쓰던 곳 하나** — `scripts/measure_onboarding_t.py`(차선 Q)가 그 상수를 빌려
HTTP 응답 행을 걸렀다. 지금은 `ImportError` 갈래로 떨어져 **그 자리의 거르기가 통째로 꺼진다**
(그 파일이 그렇게 설계돼 있다 — 「정본을 못 부르면 거르지 않는다」). probe 가 0건이라
지금은 수가 안 바뀌지만 **꺼진 것은 사실**이다. `docs/agent/checkpoints/turn-z/Q.md` 에
두 줄짜리 고칠 자리를 적어 두었다.

---

## ④ 못 한 것 · 재지 못한 것

- **웹푸시 도달 1 — 재지 못했다(회색 · 0 이 아니다).**
  제품 경로(`notify_prefs.send_test_push` → 커널 `send_webpush` · **훈련 채널**)를 실제로
  때렸고 **`PushSendNotConfigured`** 가 났다: 비어 있는 환경변수 **`GX_VAPID_PUBLIC_KEY` ·
  `GX_VAPID_PRIVATE_KEY` · `GX_VAPID_SUBJECT`**(이름만 적는다 · 값 0).
  구독 기기는 **살아 있다(1건)** · 발송 행은 **0** (225 → 225) · **운영 채널 발송 0**.
  턴 T(P-160)에 자격을 만들어 금고에 두고 도달 1을 쟀는데, 지금 `gx-shell`·`gunicorn`·`celery`
  어디에도 그 이름 셋이 없다 — **재생성 창에서 사라진 것으로 보인다.**
  ★ 자격을 다시 실으면 **이 경로 그대로** 1이 난다(코드는 손댈 것이 없다). 자격 재발급은
  조율자 몫이다(「우리가 만드는 자격은 우리가 만든다」).
  ⚠ 새 VAPID 쌍을 만들면 **기존 구독 1건은 죽는다**(구독이 옛 공개키에 묶여 있다) —
  그때는 기기에서 「알림 받기」를 다시 켜야 한다.
- **훈련 카메라 3대가 청구 「카메라 대수」에 드는 구멍**(위 ①) — 고치지 않았다(남의 파일).
- **잠금화면 도달**은 애초에 이 턴 밖이다(데스크톱 Chrome · 휴대폰은 스테이징 뒤).
- ~~전 시험 한 바퀴는 못 봤다~~ → **끝났다. `1903 passed · 10 skipped · 0 failed`**
  (`pytest tests --ignore=tests/e2e` · `DB_TEST_NAME=test_gx_u1` · **16분 10초** ·
  `FULLRC=0` · 2026-09-21). 보고를 한 번 접을 때는 56% 지점이라 **회색으로 적었고**,
  끝난 뒤 이 줄로 갚는다 — 안 본 수를 미리 적지 않는다.
  ⚠ 이 바퀴는 **다른 차선이 같은 작업트리·같은 postgres 를 쓰는 동안** 돌았다.
  그래서 이 초록은 「이 순간의 트리가 통과한다」이고, 병합 시점의 트리는 다시 재야 한다.
  ⚠ 읽을 때 주의: 이 바퀴는 **다른 차선이 같은 작업트리를 고치는 동안** 돌았다
  (`git status` 에 `apps/dsm/services.py` · `billing_marks.py` · `audit.py` · `metering.py` 등
  남의 수정 있음). 거기서 나는 빨강이 **내 것이라고 단정하면 안 된다.**
  내 자리는 위 149 초록이 정본이다.

## 잰 것 목록 (이 차선이 실제로 실행한 것)

| 무엇 | 결과 |
|---|---|
| `pytest tests/test_f04_suppression_key.py` | **10 passed** |
| 탐지기 대조(키에 `event_id` 를 넣고) | **2 failed / 8 passed** → 되돌리고 10 passed |
| 관련 8파일 한 묶음 | **149 passed** |
| `scripts/probe_events.py`(자기시험만) | **rc 0** · 출생 표본 10 |
| `scripts/probe_events.py --census` | **rc 0 · 0건** |
| `scripts/verify_camera_pulse.py`(OPS-15 게이트 · gx-shell) | **rc 0 통과** — 내가 만든 자료가 게이트를 안 깼다 |
| `scripts/verify_layers.py` | **rc 0** · 계층 위반 0 |
| `scripts/verify_dormant.py` | rc 1 — 빨강 하나는 `evidence_chain.dangling_event_refs`(턴 Y 와 같은 것 · **내 것이 아니다**). 새 `_suppression_key` 는 사적 이름이라 모수 밖 |
| 운영 DB 전수 확인 | `camera_cluster_down` **전 DB 통틀어 1건**(그 1건이 오늘 것 · 표식 drill) · probe 사건 **0** · 발송 행 225 → 225 |

---

## ⑤ 받은 쪽지 — **사건 1쪽에 「훈련」이 한 글자도 없다** (U24 → N.md ④)

U24 가 오늘 실측했다: 훈련 사건(`#305027`)의 「사건 1쪽」 완성 HTML 에 `훈련`·`drill` 이
**0건**이고, `incident_report.py`·`docx_export.py` 에도 그 낱말이 없다.
**청구는 훈련을 빼는데 종이는 훈련이라고 말하지 않는다** — 결재에 올라가면 거짓이다.

★ **오늘 그 구멍에 사례가 하나 늘었다**: 이 차선이 만든 `#326411` 이 그 종이에 나가면
「카메라 군집 두절」이 **실사건처럼** 적힌다.

**고치지 않았다 — 고칠 자리가 `backend/apps/dsm/incident_report.py::build_html` 인데
그 파일은 이 차선의 소유표에 없고, U24 가 방금 그 파일로 검수용 DOCX 를 냈다.**
지금 배너 한 줄을 넣으면 그들이 낸 종이와 다른 종이가 된다. **조율자가 한 차선에 붙여
주시길 청한다** — 판정 함수는 이미 있다(`apps.dsm.services.event_data_source(view=…)` →
`"drill"`, 오늘 이 차선이 실물로 확인). 필요한 것은 그 답을 배너 한 줄로 그리는 일뿐이다.
