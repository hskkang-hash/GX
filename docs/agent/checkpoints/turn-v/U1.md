# U1 — 턴 V 체크포인트 (P-176)

마지막 갱신: 2026-09-18 · 차선 U1(관제요원 축 · 온보딩 U1#1·2·3·8·9·11·19)

## 닫음

**① 단위시험 (내 자리)** — 82 passed / 0 failed
`docker exec -e DJANGO_SETTINGS_MODULE=config.settings -e DB_TEST_NAME=test_gx_u1 gx-shell
python -m pytest tests/test_u1_handover_and_note.py tests/test_u1_queue_field_signals.py
tests/test_dsm_app.py tests/test_incident_report.py -q --nomigrations -p no:randomly`

**② HTTP 회귀** — U1 읽기 면 **11/11 200** · 익명 대조 401
대상 `http://localhost:8500`(nginx · **8000·8010·3002 은 안 떠 있다**) · `gxseed_u1_operator`
(자격 이름 `GX_SEED_ROLE_PASSWORD` · sha256[:12]=de08e1f17eb3 · 로그인 1회만)

| 자리 | 결과 |
|---|---|
| `GET /api/dsm/dashboard/frame` | 200 683B · `link` 있음 · `panel_total` 2 (U1#2 자리 지켜짐) |
| `GET /api/dsm/events?limit=5` · `/events/queue` · `/events/summary` | 200 · queue 카드 15 · `focus` 온전 |
| `GET /api/dsm/cameras/pulse` | 200 · `counts={alive:0,total:6,never_seen:6}` (U1#3) |
| `GET /api/dsm/handover/draft` · `/handover/latest` | 200 · 초안 본문 **145자**(0자 아님 · U1#19) |
| `GET /api/dsm/events/4802` · `/timeline` · `/field-replies` | 200 |
| 익명 `GET /api/dsm/events/queue` | **401** — 토큰이 실제로 뭔가를 바꿨다 |

**③ 편리성 계측 다듬기** — `scripts/merge_convenience_u1.py`
`--dump -`(표준입력) · `--status`(읽기 전용) · 붙인 뒤 **지표별 잰 수 표**(분모는 대장의
`metrics` 길이에서 센다 — 손으로 적은 수 없음) · 덤프 진단(`in_flight` · `storage_ok=false` ·
대장에 없는 지표)을 **붙이지 않고 말한다** · 걷기 전 `typeof window.__gxMetrics` 안내.

**④ [조율자 요청 · U3 발] 스키마를 대장에서 읽는다 — 도구 두 벌을 하나로**
- `SCHEMA` 는 이제 **기본값**이고, 받는 모양은 `ledger_schema(ledger)` 가 **대장의 `schema` 칸**에서 읽는다.
  칸이 없거나 빈 문자열·공백이면 지금까지대로 기본값(`gx.convenience.u1.v1`)을 쓴다.
- `self_test(ledger_path)` — 실물 표본을 **지금 다루는 대장**으로 잰다(남의 파일로 잰 초록을 없앴다).
  실물 표본의 단언을 「U1 지표 세 이름」에서 **「붙이기가 지표 목록을 바꾸지 않는다」**로 바꿨다 —
  손으로 적은 이름 목록은 표가 자라는 날과 다른 축을 다루는 날 둘 다 거짓 빨강을 낸다.
- 자기시험 **양성 7 · 음성 8 · 실물 표본 1** (전 4/5/1 에서 갈래 둘 더함):
  ⑦ 대장이 제 모양을 들고 온다(양성=그 모양 받음 · 음성=기본값 모양 거절) ·
  ⑧ `schema` 칸 없는 옛 대장은 기본값으로 그대로 돈다(양성 · 음성 · 빈 문자열 음성).
- **눌러 본 것**(실물 대장은 안 건드리고 사본으로):
  - `--ledger …/U3-V/convenience_u3.json --self-test` → 통과(표본=convenience_u3.json)
  - U3 사본 + U3 덤프 → `0줄 → 1줄 · 잰 지표 1/1` rc=0
  - U1 사본 + U1 덤프 → `0줄 → 3줄 · 잰 지표 2/3 · 경고 3줄` rc=0
  - **축 섞기 음성**: U3 대장에 U1 덤프 → `멈춘다 — 덤프의 모양이 다르다` **rc=1**
  - 실물 대장 둘 다 여전히 `runs: 0`
- **`scripts/merge_convenience_u3.py` 는 지웠다.** 지우기 전 사본을 임시 자리에 떠 뒀다
  (추적 안 되던 파일이라 git 이 되살리지 못한다).
  `docs/agent/evidence/U3-V/convenience_u3.json`(대장)은 **그대로 뒀다** — 값이 갈 자리다.

**⑤ U2#4 뒷걸음질의 원인 — 수로 잡았다**
- `GET /api/dsm/events/4802/snapshot` → **200 image/jpeg 37,594B**
- `GET /api/dsm/events/268496/snapshot` → **404**
- 268496 은 `evidence/P-157/runs/20260918T053537/seed.json` 의 `first_event_id` 이고
  `scripts/measure_onboarding_t.py:792` 가 그것을 `--snap-event` 기본으로 쓴다.
- DB: `GX-SEED-DSM`(`seed_dsm_events`) 22건 중 스냅샷 **20건** ·
  `gxprobe-D384-screen-CAM`(`capture_screens.seed_events`) **10건 전부 빈 문자열**.
- 곧 **앱은 멀쩡하고 씨앗이 그림을 안 싣는다.** V 의 우회: `--snap-event 4802`.

## 손 위
없음 — 위 다섯이 닫혔다.

## 안 한 것 (넘긴다)
- **U3 에 요청 한 줄**: `evidence/U3-V/convenience_u3.json` 의 `rules[0]` 과 `measured_by.handoff` 가
  아직 지워진 `merge_convenience_u3.py` 를 가리킨다 — `scripts/merge_convenience_u1.py --ledger <이 파일>` 로
  고쳐 달라. 그 파일은 U3 자리라 내가 안 고쳤다.
- 씨앗 스냅샷 고치기 자체 — Q 자리(`capture_screens.py` · `seed.json` · `measure_onboarding_t.py`). 요청 한 줄 셋으로 넘겼다.
- 씨앗 정리 순서 — **조율자에게 묻는다.** `gxprobe-D384-screen-CAM` 사건이 **10건 쌓여 있고**
  (회차마다 2건 · 지운 적 없음) 268494·268495 가 미처리로 남아 `handover/draft` 의 「미처리 2건」이 그것들이다.
  내가 혼자 지우지 않았다.
- 번들에 `__gxMetrics` 가 실렸는지 — **못 쟀다(회색)**. `frontend/dist` 없음 · 8500 의 `/` 404. 빌드 뒤 V 가 콘솔 한 줄로 가른다.
- 브라우저 걷기·캡처·온보딩 측정 — V 의 일.
- **빨강 5는 내 것이 아니다**(HEAD 에는 없다 · 다른 차선 작업본에서 난다):
  `backend/apps/dsm/onboarding.py:258` 이 커널 비공개 모듈 `kernels.k1_event.field_reply` 를
  직접 가져온다(계층 위반 1 · K1 소비 App 2 · 미등재 소비자 1) ·
  진입면 107→108(`POST /api/dsm/system/requests/{int:request_id}/handled` 가 대장에 없다).
