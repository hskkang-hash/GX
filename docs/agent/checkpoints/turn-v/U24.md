# U24 체크포인트 (턴 V) — 2026-09-18 07:2x

## 닫음 (눌러서 본 것만 · 전부 gx-shell `runserver:8000` · 계정 `gxseed_u4_official` id 109)
- **감사 필터 3** — 없음 822 · `since=05:00Z` 89 · `since..until` 47 · **미래 0**(음성) ·
  `actor_id=109` 478 · **`actor_id=999999` 0**(음성) · `action=write:` 165 · **`action=zzz` 0**(음성)
- **해시 체인 칸 + 요약** — 200 · `prev_hash`·`hash`·`chain` 셋 다 옴 · `chain_states` 옴
- **`audit/export.csv`** — 200 · 6,100B / 22줄 · 머리줄에 `prev_hash,hash,chain` ·
  같은 필터가 파일에도 걸린다(`action=write:` 6,251B) · 미래 조건 139B(머리줄만)
- **체인 검증(커널)** — `evidence_chain.verify_chain()` · 전체 1,994 · 체인 행 1,705 ·
  **끊김 3 · ok False** · GENESIS 에서 시작 True · 오늘 앵커 한 줄 찍힘
  → 같은 창(05:44~05:46)을 화면 문으로 물으니 **`chain_states.broken` 도 정확히 3** ·
    끊긴 id 셋(250280 · 250290 · 250291)이 커널과 **한 건도 안 어긋난다**
- **자동본** — `run_id=1` `trigger=auto` · DOCX 200 **38,470B** · PDF 200 **51,716B**
- **403 읽기/쓰기 갈래** — `node scripts/check_denial_kind.mjs <번들>` → **통과 22 · 실패 0**
- **타입** — `tsc -p tsconfig.app.json --noEmit` 전체 3,234줄(인수 자산 빚) · **내 파일 오류 0**

## 손 위
- `docs/design/GX-COPY_v1.md` — 새 문장 넷 등재 중. 다음 한 줄: 등재 뒤 `verify_ui_copy` 를 돌린다.

## 안 한 것
- 뒷단 단위시험 재확인(`tests/test_u24_*`)
- 프런트 번들 짓기(조율자에게 넘길지 물어야 한다)

## 넘길 빨강 둘 (고치지 않았다 — 남의 파일 · 권한 결정)
1. **감사 체인 끊김 3 — 동시 쓰기 경합** [실측]
   `common/evidence_chain.py::append_evidence_hash` 는 `transaction.atomic()` 안에서
   `_last_hash()` 를 **잠금 없이 SELECT** 한다. 같은 순간 들어온 감사 두 행이 같은
   앞 해시를 읽어 체인이 갈라진다. 실물: 250279·250280 이 **둘 다** prev=6560f0dac5 ·
   250289·250290·250291 이 **셋 다** prev=1467890b16. 전부 같은 초(05:45:00 · 05:45:04)에
   설정 화면이 문 셋을 동시에 부른 자국이다. → 차선 S(LAW-08).
2. **LAW-07′ 메뉴는 섰는데 문이 U4 에게 닫혀 있다** [실측]
   메뉴 자리는 전부 있다(`product_menus` U4 표 2줄 · DB `menu.Menu` #139 `/dsm/privacy-requests` ·
   `roleNav.NAV_ALLOW.U4` · `Home.tsx` 카드 · `App.tsx` 라우트).
   그런데 `GET /api/dsm/law/privacy-requests` 를 U4 로 부르면 **403** 「이 화면은 관리자만
   볼 수 있습니다.」 — `law_api._admin` 이 `is_global_admin or is_tenant_admin` 만 통과시킨다.
   잰 값: U4 tenant_admin **False** · U2 **False** · U5 **True**.
   → 권한을 넓히는 것은 대표·세종 결정이라 **손대지 않았다.** 지금은 「눌러도 403 나는 줄」이다.

## 기계 메모
- **`gx-gunicorn-e`(nginx 8500)가 낡은 코드로 돈다** — 8500 의 감사 응답에는 체인 칸이 **없고**
  gx-shell `runserver:8000`(04:47 기동)에는 있다. 재기동하지 않았다(다른 차선이 재고 있다).
- 호스트에서 `localhost:8000`·`3002` 는 안 열린다(gx-shell 이 게시 안 함). `docker exec` 로 쟀다.
- **계정 경합**: `gxseed_u4_official` 이 U4 계정의 유일한 시드다. 이 턴에 세션이 두 번
  `session_evicted` 로 끊겼다(다른 차선이 같은 이름으로 로그인). 다시 로그인해서 이었다.
