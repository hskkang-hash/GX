# 조율자 기록 — 턴 V (2026-09-18)

## 착수
- `turn-v 시작 · 기계 시각 2026-09-18 18:03 KST · 살아 있는 차선 0/6(착수 전)`
- 컨테이너 10/10 Up · V_LOCK 비잠금 · HEAD `dd77b48`(turn-q · **push 대기**)
- 차선 6 착수 **18:10** (F · U24 · U56 · U3 · U1 · Q — 한꺼번에)

## 살아 있는 차선
| 시각 | 살아 있는 차선 | 마지막 체크포인트 |
|---|---|---|
| 18:10 | 6/6 (착수) | — |
| 18:5x | **5/6** — U1 닫음 | U1 체크포인트 있음 |
| 19:1x | **4/6** — U3 닫음 (U1 은 U3 요청으로 재개) | U3 체크포인트 있음 |

## 조율자가 20분에 한 것
| 항목 | 상태 | 증거 |
|---|---|---|
| P-176 체크포인트 규약 | ○ | `_규약.md` · **위임 이의 #1**(브랜치 → 파일) |
| P-174 Docker AutoStart | △ | gx-shell `restart=unless-stopped`(10/10) · Desktop `AutoStart` False→True · **`com.docker.service` Manual 은 관리자 한 줄 필요** (D-495) |
| P-177 저장 상한 | △ | **위임 이의 #2** — 200 은 이 기계 가용의 3.7배. 값 안 바꿈. `evidence/P-177/` |
| P-182 번들 게이트 | ○ | `verify_bundle_api_base.py` · `ALL_GATES` 16→**17** · 초록/빨강/회색 눌러서 확인 (D-494) |
| P-182 deploy.sh | ○ | `ensure_spa()` 의 **옛 SPA 본문 heredoc** 제거 → 저장소본 복사 (거짓 초록의 씨가 살아 있었다) |
| 쓰기 면 선등록 | ○ | `write_surfaces_v11.yaml` 턴 V 블록 — WS-24·25·26 + 표 밖 ㉠㉡㉢ |
| 역할 계정 4/4 | ○ | u1·u2·u4·u5 전부 200 [실측 18:2x] |
| 턴 U 재측 전제 | ○ | live 번들에 `http://localhost:8000` 박힘 확인 — V 의 판은 깨끗한 대상에서 시작한다 |

## 넘길 것
- `gxprobe_v` 가 턴 U 의 세션을 아직 쥐고 있다 — V 착수 전에 정리.
- 아침 절차: 컨테이너는 스스로 서도 **gx-shell 안 게이트 서버 둘**(8000 · 3002)은 안 돌아온다.

## 차선이 올린 것 (조율자가 받은 자리)
- **U1 → Q** 요청 3 (씨앗이 그림을 안 싣는다): `capture_screens.seed_events` 가 `upload_snapshot` 을
  타게 · `seed.json` 에 `snapshot_path` 칸 · `measure_onboarding_t:792` 가 **그림 있는 첫 씨앗**을
  고르게. 전달 완료.
- **U1 → F** 빨강 5 (작업본에서 난다 · HEAD 에는 없다): `onboarding.py:258` 이 커널 비공개
  `kernels.k1_event.field_reply` 를 직접 가져옴(계층 위반 1 · 소비 App 2 · 미등재 1) +
  진입면 107→**108** 미등재(`POST /api/dsm/system/requests/{id}/handled` = WS-24). 전달 완료.
- **U1 → 조율자 · 대표 결정 자리**: 개발 DB 에 `gxprobe-D384-screen-CAM` 씨앗 사건이 **10건**
  쌓여 있고(캡처 회차마다 2건 · 지운 적 없음) 그중 268494·268495 가 미처리로 남아
  **U1 인계 초안의 「미처리 2건」이 바로 그 찌꺼기**다. 지우면 MinIO 객체도 함께 간다 →
  **삭제는 대표 결정**. 이번 턴은 안 지우고 잰다(V 에게 그대로 알린다).

## 조율자 오판 (자진)
- 차선 지시에 **U1 을 「시민 제보자」로 적었다.** 저장소의 U1 은 **관제요원**이다
  (`onboarding_48.md:151` · 계정 `gxseed_u1_operator`). U1 이 저장소를 정본으로 보고
  바로잡아 일했다 — 옳다. 페르소나 이름을 지시서에서 베끼지 말고 **정본에서 읽었어야** 했다.

- **U3 → U1** 요청: `merge_convenience_u1.py` 가 `SCHEMA` 를 **대장의 `schema` 칸**에서 읽게 →
  `merge_convenience_u3.py`(도구)는 지운다. 축마다 도구를 두지 않는다(D-369). 전달 완료.
- **U3 → 조율자 · 넘김 3**
  · `frontend/src/features/dsm/pages/EventList.tsx:475` 근처에 **같은 조용한 실패**가 남아 있다
    (판정 실패 = 토스트 + 모달 유지). EventDetail 과 같은 모양으로 옮길 것 — 다음 턴 §9.
  · `backend/stream_monitors/services/capture_service.py::_capture_frame` 의
    `subprocess.run(ffmpeg …)` 에 **타임아웃이 없다**(`-stimeout` 도 없다). 닿지 않는 카메라에
    붙으면 **스스로 끝나지 않는다** — W0-17 이 잡는 모양. 조율자가 병합 뒤에 본다.
  · 실카메라 스냅샷은 **망이 열려야** 초록이 된다 → 대표 결정 후보.
- **U3 실측 · 실카메라 0/4 갈래**: `112.170.174.81:38554` timeout(컨테이너·호스트 둘 다) ·
  `vpn-vn.gaion.dev` 이름 못 풂 · `192.169.1.100:554` · `10.0.0.21:554` timeout.
  제품의 길은 서 있다(ffmpeg·ffprobe·cv2 4.11.0 있음) — **없는 것은 원천이다.** 회색.
