# 차선 U3 — 턴 V 체크포인트 (P-176)

마지막 갱신: 2026-09-18 (착수 +110분 · 네 항목 손 뗌 · 보고만 남음)

## 닫음

1. **오탐 판정 실패 → 상태 칸** — `frontend/src/features/dsm/pages/EventDetail.tsx`.
   토스트(`message.error`)를 없애고 `FailureNotice`(상태 칸 · 「다시 시도」가 그 판정을
   그대로 다시 낸다)로 옮겼다. 실패하면 확인 창을 **닫는다**(열어 두면 그 칸을 가린다).
   성공 토스트도 없앴다 — 성공은 「현재 판정」 배지가 말한다(UX-25).
   잰 것: `tsc --noEmit` 에서 `features/dsm`·`features/mobile` 오류 **0**(저장소 전체 잔여
   1,729 는 전부 남의 옛 파일) · `verify_ui_copy` **통과 · 새 위반 0**.
   사전 등재: `docs/design/GX-COPY_v1.md` 맨 끝 절(턴 V · 차선 U3).
2. **U3#2 씨앗 사건 주소** — `backend/kernels/k1_event/services.py::record_detection`.
   부르는 쪽이 주소를 안 주면 **그 카메라의 `install_address` 를 물려받는다**(D-330 이
   이미 판정해 둔 것을 배선만 안 해 둔 자리). 출처가 `unset` 인 행은 물려받지 않는다 —
   알림 쪽 `_location_line` 이 세워 둔 문턱과 같은 값이다.
   씨앗 파일(`scripts/capture_screens.py`)과 `seed.json` 은 **안 건드렸다**(Q 소유).
   잰 것: 새 시험 `backend/tests/test_d330_event_address_from_camera.py` **7 passed** ·
   `test_d330 + test_f10_camera_address + test_fx5_address` **37 passed**.
3. **편리성 #5 배선 정정 + 대장** — 시계가 「상세를 연 때」에 멈추고 있었다(PRD §6 #5 는
   「문자 수신 → **접수 회신**」이다 · 열기에서 멈춘 시계는 언제나 더 작은 수를 낸다).
   `frontend/src/features/mobile/metrics.ts` 에 `markOpened` 를 넣어 열기는 **탭 한 번**으로
   세고, 시계는 `MobileEventDetail` 의 접수가 **서버에 기록된 뒤** 멈춘다.
   대장 `docs/agent/evidence/U3-V/convenience_u3.json`(measured=null · 사유 적음) +
   붙이는 도구 `scripts/merge_convenience_u3.py`(규칙은 U1 도구에서 가져다 쓴다).
   잰 것: `--self-test` **통과(양성 4 · 음성 2 · 실물 표본 1)** · `--status` 「줄 0 · 잰 지표 0/1」 ·
   U1 도구 자기시험도 **통과**(영향 없음).
4. **실카메라 스냅샷 1 — 없다 · 회색.** 아래 「안 한 것」 참조(실측을 적었다).

회귀: 관련 시험 15파일 **195 passed** · `test_k1_event_kernel` **21 passed**(계층 시험 포함 22) ·
`verify_layers` **통과 0건**.

## 손 위

- 없다 — 보고만 남았다. (실카메라 증거는 `docs/agent/evidence/U3-V/real_camera_snapshot.md` 에 적었다.)

## 안 한 것

- **실카메라 스냅샷 — 못 찍었다(회색).** 닿는 카메라가 없다 [실측 2026-09-18]:
  `112.170.174.81:38554` TimeoutError(컨테이너·호스트 둘 다) · `vpn-vn.gaion.dev` 이름 못 풂 ·
  `10.0.0.21:554`·`192.169.1.100:554` TimeoutError. 제품의 길(`CaptureService._capture_frame`
  ffmpeg → MinIO)은 서 있으나 **원천이 없다.** 가짜 그림으로 초록을 만들지 않았다.
- **편리성 #5 의 수** — 못 쟀다. 브라우저 측정은 V 의 일이다(차선은 브라우저를 재지 않는다).
  배선과 대장과 붙이는 도구까지만 세웠다 — 회색이지 초록이 아니다.
- **`EventList.tsx` 의 같은 조용한 실패** — 같은 모양이 그 파일에도 있다(475줄 근처
  `message.error` + 모달 유지). 한 파일은 한 차선이라 **안 고쳤다** — 보고에 요청 한 줄.
