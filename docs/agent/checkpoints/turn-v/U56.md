# U56 체크포인트 (턴 V) — 1·2·4 닫음

## 닫음
- **1 키 범위 403 을 HTTP 로** [실측 · 두 출처 대조] `GET /api/dsm/events` —
  범위 안 **200** · 범위 밖(`stats:read`) **403** · 미설정(옛 키) **403**.
  nginx 8500 · gunicorn 8000 둘 다 같은 수. `scripts/probe_key_scope_http.py` ·
  시험 7 (`TurnVKeyScopeOverHttpTest`).
  원인 **둘**: ⓐ `assert_path_scope` 를 부르는 HTTP 자리 0곳(세웠다) ⓑ `stats`·`pulse`
  미선언(요청 한 줄로 넘긴다).
  ⚠ 함정: `gx-gunicorn-e` 는 `/app/gunicorn.conf.py` 의 `preload_app = True` 를 읽는다 →
  **HUP 로는 앱 코드가 안 바뀐다.** `docker restart` 여야 한다.
- **2 `/roles` 표 0 마지막 갈래** — 실제 계정으로 갈랐다:
  `_check_path_permission(gxseed_u5_sysop, "/roles", "read")` **True** · 역할 16행 ⇒
  U5 가 본 0행은 **화면(㉡ · rj-core §0.4)**. 남은 진짜 갈래는 **권한 없는 계정**이었고,
  그 거절이 **200 봉투 안에**(`/api/roles/` 는 승격 목록 밖) 와서 8초 뒤
  「세지 못했습니다」로 적히던 것을 「볼 권한이 없습니다」로 즉시 말하게 했다
  (`ListRealityNote.tsx` · tsc 내 파일 오류 0).
- **4 OpenAPI** — U56 문 여섯에 응답 스키마를 붙였다(`components.schemas` 3 → 10).
  시험 194/0.

## 손 위
- 3 API-03·04 — 명세 문서(§1·§3 한계 절) 고치고 `authn_paths` U6 행 확정.
  다음 한 줄: `docs/design/GX-API_연계명세_v0.1.md` §3 의 「오늘의 한계」를 실측으로 교체.

## 안 한 것
- 5 P-178 dry-run(SQL · compose diff · OPS-23/24) · 6 P-177 화면 상한
