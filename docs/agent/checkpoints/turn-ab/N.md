# N — 턴 AB 체크포인트 (대장 `docs/agent/evidence/D-346/ga_readiness.yaml`)

> 이 파일의 주인은 **N**이다. 다른 차선이 남긴 쪽지는 `N.inbox/` 에 있다 — 여기 덮어쓰지 말 것.

# 【회차 3 · V 단독 뒤】 2026-09-22 — **미뤄 둔 판단을 갚았다**

## 첫 줄 — 수

```
상용 오픈 가중 합계   55.7%  →  **57.0%**   (+1.3)      · 이 턴 전체로는 54.1 → 57.0 (**+2.9**)
닫힌 절  71 → **73/155**   ·  못 잼 74 → 71  ·  게이트만 4 → **5**
영역 ①  kind 13.00/39 → **15.50/39** (40%) · 가중 6.67 → **7.95**
기능명세 포함 완료율  26.6 → **27.5 %**  (분모 **291** — 안 움직였다. Q 의 고침이 섰다)
```

## ★★★ 앞 회차에 미뤄 둔 판단이 이 회차에 갚혔다

앞 회차에 나는 영역 ①의 `closed` 13 을 두고 이렇게 적었다:

> `closed` 13 은 **한 잣대가 아니다** — 올린 3 은 P-231 잣대, 이미 있던 10 은 옛 잣대(도달 사슬)이고
> 그 열의 「누른 뒤」 증거는 **27.5시간 낡았다**. **안 내렸다** — 「잰 적이 없다」와 「잰 것이 늙었다」는
> 다르다. **V 가 48행 `--measure` 를 돌리면 그날 같은 경로로 초록**이 된다.

**그 일이 일어났다.** 내가 직접 다시 쟀다(조율자 수를 베끼지 않았다):

```
python scripts/verify_click_completes.py --clause-evidence
→ 절 **17** 중 「누른 뒤」 초록 **16** · 빨강 0 · 회색 1     (앞 회차엔 초록 3)
   V 회차 measured_at = 2026-09-21T14:46:00Z
```

⇒ **잣대가 하나가 됐다. 이 영역에 「경로 없는 `closed`」는 0개다.**
★ **미룬 것을 잊지 않았다는 증거로, 옛 문장을 대장에 그대로 인용해 남겼다.**

## ① 올린 둘 — **증거 경로와 함께**

| 절 | strength | 잰 행 | 술어 json (잰 때) | 캡처 |
|---|---|---|---|---|
| **F-10-c1** | change | U5#9 · U5#S4 | `P-118/click_completes.json` (14:46:00Z) + `settings_clicks.json` (12:19:30Z) | `SCREENS-1/admin/dsm_notify_recipients.png` |
| **F-11-c3** | change | U2#6 | `P-118/click_completes.json` (14:46:00Z) | `SCREENS-1/fire_admin/dsm_reports_runs.png` |

★ **`F-10-c1` 은 조율자의 목록 12 에 없었다.** 기계가 초록으로 낸다 — 앞 회차에 내가
「도달만으로 닫힘을 주지 않는다」며 안 올린 절인데, 이제 **두 행이 다 측정됐다.**
**지시도 입력이지 측정이 아니다** — 갈리면 기계가 낸 쪽을 쓴다.

### 옛 잣대로 서 있던 열도 경로를 얻었다 — **15절 전부**

`F-01-c2 · F-05-c1 · F-05-c2 · F-05-c3 · F-09-c2 · F-10-c3 · F-12-c2 · F-12-c3 · F-12-c8 · F-14-c1`
+ 앞 회차의 `F-12-c5 · c6 · c7` + 이번의 `F-10-c1 · F-11-c3` = **15절**, 전부 `kind_evidence` 에
**행 · strength · json · 잰 때 · 캡처**가 실렸다.

⚠ **이 표는 손으로 옮긴 것이 아니다** — `--clause-evidence` 출력을 **기계로 파싱**했고
캡처는 도달 대장에서 받았다. 판정기가 **그 파일들의 실재를 매 실행 확인한다.**

★ **캡처가 없는 절 셋**(`F-05-c1·c2·c3`)은 **없는 캡처를 지어내지 않았다** — 그 셋의 진입면은
**화면이 아니라 라우트**다(외부 App · OpenAPI · API 키). 사유를 `capture_none_why` 에 적었고,
판정기는 **사유 없는 빈 캡처를 빨강**으로 낸다.

## ② ★★ 올리지 **않은** 하나 — `F-11-c2` 를 `gate_only` 로 내렸다 (차선 A 가 넘긴 판단)

`F-11-c2`(**손입력 0**)와 `F-11-c3`(**치환이 돈다**)은 **같은 한 행 `U2#6`** 에 기댄다.
그 행이 재는 것은 「POST `reports/runs` 뒤 `runs.total` 이 **는다**」 — **보고서가 만들어졌다**이다.

- `F-11-c3` 은 그 사실이 **곧 절의 내용**이다 → **`closed`**.
- `F-11-c2` 의 「**손입력 0**」은 **그 행이 안 잰다.** 사람이 칸을 쳤는지는 누른 뒤의 관측에 없고
  도달 대장의 **추론 문장**에만 있다 → **`gate_only`(0.5)**.

⇒ 「**게이트는 초록인데 절은 안 닫혔다 — 제목이 부르는 것보다 게이트가 적게 잰다**」 그대로다.
⚠ **같은 한 행으로 두 절을 올렸으면 초록 하나가 두 번 세어졌을 것이다.**
★ 닫는 길은 **「사람이 친 칸 수 = 0」을 직접 재는 술어**이고, 그 파일은 차선 A 의 자리다.

★ 이 영역에 **`gate_only` 가 처음 생겼다.** 대장에 오래 「이 영역엔 gate_only 가 0절이다」라고
적혀 있었는데, 그 말은 **도달만 세던 동안** 참이었다 — 그 문장도 지우지 않고 사연과 함께 남겼다.

## ③ `F-12-c1` — **회색이다. 제품 빨강이 아니다.** 그리고 이것이 가장 큰 발견이다

V 가 격리 A/B 로 사유를 갈랐다: 첫 캡처가 로그인 **4/4 실패**했는데 **같은 순간 같은 자격으로**
온보딩 측정기는 **6/6 들어갔다.**

> **브라우저 로캘이 `ko-KR` 이면 판정기가 못 들어간다.**
> `measure_onboarding_t.login()` 이 「세션 종료」 확인창 단추를 **`Confirm` 영어 이름으로만** 찾는다.

⇒ **제품은 옳고 판정기가 틀렸다.**
★★ **우리 고객 브라우저는 대부분 `ko-KR` 인데 게이트는 여태 영어로만 재 왔다.**
그 한 줄은 Q 자리다. **이 회색을 빨강으로 적지 않았다** — 못 잰 것은 0 이 아니라 회색이다.

## ④ 조율자가 고친 내 수 하나 — 받았다

**손 안 도달율은 회색이 아니라 72.3 %**(99/137)다. 내가 `--static` 만 돌려 「회색」이라 적었는데
**게이트를 부르는 갈래는 실제로 낸다.** 조율자 정정을 그대로 받는다 —
내 앞 회차 보고의 「회색」은 **내 실행 방식의 그림자이지 제품의 색이 아니었다.**

## ⑤ Q 의 고침이 섰다 — **분모가 안 움직인다**

앞 회차에 내가 낸 빨강(「등재하자 분모가 291 → 155 로 줄고 수가 올라갔다」)을 Q 가 고쳤고,
**내가 청한 것보다 낫게 고쳤다** — 「미등재」가 아니라 **별표 전수**를 분모로 삼았다:

```
[SPEC] 기능명세 포함 완료율 **27.5 %** = 대장 kind 점수 80.0 ÷ **분모 291**(절 155 + 별표 전수 136)
[SPEC] ★★ 분모는 **등재 여부와 무관하다**(등재 136 · 미등재 0) — 「미등재」로 세면
       **등재가 늘수록 분모가 줄어 수가 올라간다. 분모가 줄어 올라간 수는 수가 아니다**
```
⇒ 이제 **등재가 늘어도 분모는 안 움직이고 분자만 움직인다.** 26.6 → 27.5 는 **분자가 오른 것**이다.

## ⑥ 규율

- **로그인 0 · 서버 재기동 0 · 브라우저 0** (`--static`).
- **삭제 0** — 기계가 찍은 표로 갈아 끼우면서 **잃을 뻔한 내력 셋을 `caveat` 로 되살렸다**:
  `F-12-c5`(길이가 아니라 `is_active` 를 본다 · 「만들기」는 반대로 길이가 답이다) ·
  `F-12-c6`(턴 AA 의 우회와 F 의 고침) · `F-12-c7`(「올리는 쪽으로만」은 멋이 아니라 규율이다).
  ★ **기계로 다시 찍는 것이 사람이 적어 둔 내력을 지우면 안 된다** — 이번에 실제로 그럴 뻔했다.
- **커밋 0**(조율자 몫) · 남의 파일 손편집 0 · 자기시험 **67건 0실패**(41 → 59 → 64 → 67).

---

> 이 파일의 주인은 **N**이다. 다른 차선이 남긴 쪽지는 `N.inbox/` 에 있다 — 여기 덮어쓰지 말 것.

## 첫 줄 — **올랐다. 그런데 같은 시각에 올라선 안 될 수가 하나 올랐다**

```
상용 오픈 가중 합계   54.1%  →  **55.7%**   (+1.6)     ← 영역 ① 승격 3
절 155 그대로 · 닫힌 절 68 → **71/155** · 못 잼 77 → 74
kind 표 정본 = 절 155 + 별표 ② **136** = **291**

승격 3(전부 증거 경로 있음) · 등재 136 · 넘김 2 · 문서 1 · 되살린 글 2
새 판정 3(P-236 · P-237 · P-231 증거 실재) · 자기시험 41 → **64건 0실패**
```

★★ **그리고 이것이 이 보고의 첫 줄이다 — `기능명세 포함 완료율`이 25.6 → 50.0 으로 올랐는데
그 상승은 제품이 아니라 고장이다.** 분모가 **291 → 155 로 줄어서** 올랐다. 아래 ①.

잰 시각 **2026-09-21 12:38–12:52 UTC (21:38–21:52 KST)** · 로그인 **0**(`--static`).

---

## ① ★★★ 분모가 무너졌다 — **재는 것이 깨져서 수가 좋아졌다**

136 을 등재하자마자 차선 Q 의 산출기를 돌렸다. **베끼지 않고 돌렸기 때문에 보였다.**

```
등재 전:  기능명세 포함 완료율  25.6 %  = 77.5 ÷ **291**  (절 155 + 미등재 136)
등재 후:  기능명세 포함 완료율  **50.0 %** = 77.5 ÷ **155**  (절 155 + 미등재 **0**)
```

### 까닭 — **136 이 두 칸 사이로 떨어졌다**

Q 가 절 수를 읽는 줄은 **8영역만** 센다:
`[GA] [입력] 「초록」 부류 **155절** — closed 71 · …`
그런데 등재된 136 은 **8영역 밖**이다(조율자 판정 · 가중치 합 100 을 지키려고 그렇게 뒀다).
⇒ **「미등재」에서는 빠지는데 「대장 절」로는 안 들어간다.**

Q 의 소스는 이렇게 적고 있었다: 「N 이 id 를 대장에 등재하면 그 id 는 **대장 절 수**에
이미 들어간다」. **안 들어간다.** 그 전제가 어긋난 자리다.

⚠⚠ **「분모가 늘어 수가 내려가면 그것이 첫 줄이다」의 거울상이다.**
**분모가 줄어 올라간 수는 수가 아니다.** 우리가 세 턴 동안 잡아 온 것과 같은 모양이고,
이번엔 **좋아 보이는 쪽**으로 났다 — 그래서 더 위험하다.

### 내가 한 것 — **Q 의 파일은 안 고쳤다. 읽을 줄을 내줬다**

```
[GA] [입력] ★★ **분모 정본 — 절 155 · 별표 136 · 합 291**
     (`verify_spec_coverage.py` 가 읽을 자리. 위의 「부류 155절」은 **8영역만** 센 수다 —
      그 줄을 분모로 쓰면 별표 136 이 조용히 빠진다)
```

- **「부류 N절」 줄은 안 건드렸다** — Q 의 파서를 안 깼다. 그 줄의 뜻도 그대로다.
- 새 줄은 별표가 **0건이어도 찍는다**(「등재 0건 · 정본 155」).
- **별표를 8영역 안으로 옮겨 고치면 안 된다** — Q 의 분모는 맞지만 **가중치 합 100 이
  무너져 상용/100 이 다른 뜻**이 된다. 고칠 자리는 Q 의 **한 줄**이다.
- ★ Q 의 자기시험도 일러 줬다: `분모 = 155 + 136 = 291` 은 `n_sections=155` 를
  **손으로 넣어** 통과한다 — **실물이 무너졌는데 자기시험은 초록이었다.**
  「**등재 뒤에도 분모가 291 이다**」 갈래를 실물 대장으로 박아 달라 청했다.

---

## ② 별표 ② — **136 등재.** 그리고 **내가 「없다」고 적은 것이 틀렸다**

| | 수 | 그물 |
|---|---|---|
| DSM `§4 사용자별 세부 기능명세` | **32** | `\bDSM-U\d+-\d+\b` |
| FWS `§5 사용자별 세부 기능명세` | **90** | `\bFWS-[FU]\d+-\d+\b` |
| 플랫폼 `§7 플랫폼 운영자(U0)` | **14** | `\bO-\d{2}\b` |
| **합** | **136** | `docs/agent/evidence/P-234/spec_ids.json` |

**Q 의 그물로 독립 재측해 세 수를 그대로 받았다.** 손으로 지은 id **0개**.

### ★★ 내가 틀린 방식 — 그대로 대장에 남겼다

```
내가 던진 그물   `DSM-[0-9]+`      →  **0건**
실재하는 표식     `DSM-U1-01` 꼴    →  `DSM-U\d+-\d+` 로 **32건**
```

조율자가 「내 오판」이라 했지만 **절반은 내 것**이다.
조율자가 먼저 0건을 보고 「없다」고 적었고, **나는 「독립으로 재측한다」며 같은 그물을 한 번 더
던져 같은 0 을 받고 서로를 확인했다.** **두 사람이 맞았다고 옳은 것이 아니었다.**
내가 낸 **473**(표 데이터행 전수)도 **분모가 다른 수**였지 이 수의 후보가 아니었다.

⇒ 대장에 남긴 문장:
**「한 그물이 0건을 내면 그것은 「없다」가 아니라 「이 그물로는 안 잡힌다」이다.
0 을 받으면 다음 물음은 「정말 없나」가 아니라 「내 그물이 그 모양을 잡나」다.」**

★ 판정기가 **id 유일성 + 꼴 겹침(P-237)** 을 136×155 전량에 대 봤다 —
**똑같은 id 0 · 붙임표/밑줄/대소문자만 다른 쌍 0.** 등재 전에 확인했다.

---

## ③ 영역 ① 승격 — **셋이다. 지시가 말한 다섯이 아니다**

조율자 쪽지는 「올릴 수 있는 것은 **5**(`F-12-c1`·`c5`·`c6`·`c7` + `F-10-c1`)」라 했다.
**그것은 A 의 재측(12:19:30Z) 전의 수**다. A 자신이 갱신한 §0 에서 **3** 이라 적었고 **기계도 3** 이다:

```
python scripts/verify_click_completes.py --clause-evidence
→ [절↔증거] 절 **17** 중 「누른 뒤」 초록 **3** · 빨강 0 · 회색 14
```

**지시도 입력이지 측정이 아니다** — 갈리면 **기계가 낸 쪽**을 쓴다.

### 올린 셋 — 경로는 대장 `kind_evidence` 에 실었다

| 절 | 잰 행 | 증거 json (잰 때) | 무엇을 봤나 |
|---|---|---|---|
| `F-12-c5` | U5#S1 · U5#S2 | `P-118/settings_clicks.json` (12:19:30Z) | 구역 끄기/켜기 — 행의 `is_active` 가 **뒤집힌다** · 만들기 — 길이가 **는다** |
| `F-12-c6` | U5#S3 | 〃 | 임계값 — 표의 **「지금 값」이 10.0 → 11.0** · 곁칸 「바꾼 기록」 4 → 5. **둘 다 서야 초록** |
| `F-12-c7` | U5#S4 | 〃 | 등급규칙 — `severity` 가 바뀐다(**올리는 쪽으로만**) |

캡처: `D-347/screens/SCREENS-1/admin/dsm_settings_rules_admin.png` (실재 확인).

★ `F-12-c6` 은 **턴 AA 에 A 가 「표의 지금 값은 안 바뀐다」로 우회했던 자리**다.
F 가 `list_thresholds` 를 좁은 층이 이기게 고쳤고 A 가 눌러 확인해 **우회를 되돌렸다** —
그러면서 **옛 우회 술어를 안 지우고 곁칸으로 같이 본다**(대장은 줄지 않는다).

### 안 올린 둘 — 사유

- **`F-12-c1`** — 이번 회차 **회색**(A 가 로그인 throttle 로 화면에 못 들어갔다).
  한 회차 전 초록은 **git 스테이지에만** 있고 **디스크의 정본이 아니다.**
  A 도 이쪽을 기본으로 봤고 나도 그렇게 본다 — **대장에 실리는 것은 디스크의 정본이라야 한다.**
- **`F-10-c1`** — **도달만** 올랐다(17 → 18). A 가 「**도달만으로 `closed` 를 주지 마시라**」고
  명시했다. 「수신그룹이 정의됐다」와 「그 사람에게 실제로 닿았다」는 다른 판정이고,
  같은 표의 「사람에게 도달」 칸은 세 등급 다 **「닿지 않음」**이다(채널이 `log`).

### ★★ 그리고 대장에 박은 것 — **`closed` 13 은 한 잣대가 아니다**

```
증거 경로가 실린 절  **3**   P-231 잣대 — 「누른 뒤」 + 살아 있는 경로 (12:19:30Z)
경로가 안 실린 절   **10**   옛 잣대 — 영역 게이트의 **사슬 다섯(도달)**
```

그 열의 「누른 뒤」 증거(`click_completes.json`)는 **27.5시간 낡았다**(한도 12시간).
⇒ **안 내렸다.** **「잰 적이 없다」가 아니라 「잰 것이 늙었다」**이고 둘은 다르다.
A 가 절↔행 표를 깔아 뒀으므로 **V 가 48행 `--measure` 를 한 번 돌리면 그날 같은 경로로
초록**이 된다 — 내가 손으로 다시 이을 일은 없다.
★ **V 의 회차가 없으면 다음 턴에 P-215 의 잣대로 다시 볼 자리**라고 대장에 적었다.
**판단을 미룬 것이지 안 본 것이 아니다** — 그래서 적는다.

---

## ④ OPS-07 — `gate:` 둘 달았다 · **F 의 보고를 베끼지 않고 다시 돌렸다**

| 절 | status | gate | 내 재측 rc |
|---|---|---|---|
| `OPS-07-a` 로그 **수집** | `구현` | **`ops_log_collectors_collect.py`** ← 새로 | **0** — 색이 같다 |
| `OPS-07` 로그 **보존 기간** | `미착수` | **`ops_log_collectors_retention.py`** ← 바꿈 | **1** — 색이 같다 |
| (보존 정책) | | `ops_retention_policy.py` | 1 — 만료 0행 · 삭제 0 |

내가 청한 것이 그대로 왔다: **인자 없이 갈래가 갈리는 얇은 진입점 둘** · **판정식은 한 벌**.
갈래 밖 판정도 **화면엔 찍힌다**(`··`) — **안 보이게 하는 것과 안 세는 것은 다르다.**
★ 이제 이 자리가 고쳐지면 게이트가 초록이 되고 대장이 '미착수' 면 **exit 1** 이 난다.
**조용할 수 없는 자리가 됐다** — 턴 AA 에 내가 청한 것이 이 턴에 섰다.

### ★★ 대장의 `hand_why` 가 **틀려 있었다** — F 가 찾았고 내가 고쳐 적었다

「막는 것은 **선언 한 칸**이다 — `security.audit_log_retention_days` 를 쓰면 닫힌다」
→ **그 칸엔 이미 365 가 있다.** 막는 것은 **파기 스위치**이고 둘 다 꺼져 있다
(`ops-audit-purge-daily`=off · `purge-audit-logs-daily`=off — 내가 재측해 확인).
켜는 것은 **삭제를 켜는 일**이라 **안 켰다**(P-230 · P-222).
⚠ **이 줄을 안 고쳤으면 다음 사람이 이미 있는 칸을 또 쓰러 갔다** —
틀린 `hand_why` 는 **색이 안 나는 종류의 거짓**이다.

### ★★★ 730 · 365 · 90 — **고를 문제가 아니었다** (차선 S 조문 대조)

| 무엇 | 수 |
|---|---|
| P-230 선언(세종) | **730일** |
| 심긴 값(`AdminConfig`) | **365일** |
| dj-core 코드 기본값 | **90일**(아무도 정한 적 없다) |

셋을 **다 적었다. 한쪽으로 조용히 안 맞췄다.**
그리고 S 가 **[추정]을 뗐다**: 고시의 「접속기록」은 **접속지 정보(IP)** 와
**처리한 정보주체 정보**를 요구하는데 **`audit_writer` INSERT 칸에 둘 다 없다.**
⇒ **우리 감사 로그는 고시상 「접속기록」이 아니고, 90→730 으로 올려도 요건은 이행되지 않는다.**
⇒ **고를 문제가 아니라 칸이 없는 문제다.** 안 적으면 다음 사람이 **수 하나 골라 놓고 닫힌 줄 안다.**

★ **만료 건수 0 이 이 턴의 쓸모 있는 수다**: 감사 표는 **25일치**(170,853행)라
**730일로 올려도 지워질 행이 0** 이다. 이 턴의 「삭제 0」은 규율일 뿐 아니라 **사실**이다.
★ `collector` 는 **회색으로 뒀다** — `json-file` 회전은 **크기 기반**이라 날짜를 모른다.
**못 잰 칸은 0 이 아니라 회색이다.**

---

## ⑤ CR 8칸 (앞 회차에 냈다) — **합이 갈린 것은 그대로 남아 있다**

`docs/design/GX-CR_8칸_v1.0_20260921.md` · Q 의 파서로 **행 8 · 회색 0 · bad 0** 확인.
**§4-1 의 여덟 칸을 더하면 1.5 인데 합 줄은 2.0** 이다(반인 칸이 셋인데 합은 넷을 말한다).
**0.5 짜리 칸을 지어내지 않았다** — 그 칸 이름을 아무도 안 말했다. 두 수를 나란히 적고 올렸다.
**세종 판정 대기**: ⑤⑥⑦ 말고 반인 칸이 하나 더 있는가.
★ 잰 칸은 **0/8 → 8/8 · 회색 0**. 수는 **25 → 18.75 로 내려간다.**

## ⑥ 대장이 제 글을 버리던 자리 둘 (앞 회차) — P-236

`OPS-07` 의 `why` ×2 · `OPS-19` 의 `note_turn_i` ×2. **YAML 이 앞엣것을 조용히 버린다.**
버려지던 쪽이 **내가 턴 AA 에 쓴 조율자 판정 블록**과 **새로 잰 턴 J 글**이었다.
둘 다 되살렸고(**삭제 0**), `yaml.compose` 로 원문 나무를 걷는 검사를 세웠다.

## ⑦ 삭제 0 — `git diff` 가 증인이다

대장에서 지워진 23줄은 전부 **이 턴에 내가 쓴 자리표(별표 뼈대)** · **옮긴 id 줄** ·
**갈아 끼운 `gate:` 한 줄** · **틀린 `hand_why`** 다.
★ **갈아 끼운 것은 지운 것이 아니다** — 옛 문장은 **새 글 안에 그대로 인용돼 있다**
(「여기엔 …라고 적혀 있었다」 · 「`gate:` 를 일부러 안 달았다」의 온 사연).
**역사 삭제 0.**

## ⑧ 판정기 — 자기시험 **64건 0실패** (턴 AA 41 → 64)

새로 박은 것 중 값이 큰 것:
```
★★ P-231 — **없는 파일을 가리킨 증거를 잡는다** (없는 증거는 주장이다)
★★ P-231 — `closed` 가 아닌 절에 붙은 경로를 잡는다 (**올린 줄로 속인다**)
★★ P-236 — `safe_load` 결과만 보면 흠이 **안 보인다** (파서가 먼저 삼킨다)
★★ P-237 — 대장 `OPS-07b` 와 별표 `OPS-07-b` 를 잡는다 (붙임표 하나)
★  별표가 **비어 있는 것은 흠이 아니다** — 사유가 있으면 통과한다
```
★ **P-231 증거 실재 검사**가 이번 턴의 몫이다: 경로는 **적어 두면 끝나는 것이 아니다.**
그 파일이 사라져도 아무 색이 안 나면 **「없는 증거는 주장이다」**가 된다. 매 실행 확인한다.

## ⑨ 창 · 로그인 · 남은 구멍

- **로그인 0 · 서버 재기동 0 · 브라우저 0.** 전부 `--static`(앞 회차의 교훈).
  ⚠ **`--no-gates` 는 로그인을 막지 못한다** — 영역 ①의 게이트를 그대로 부른다
  (`verify_feature_reach` → `verify_contract_route_reach.py:363 login(...)`).
  **규약 표에 「로그인 창 없는 차선은 `--static`」 한 줄을 박아 달라** 청했다(그대로 유효).
- **`gate:` 가 인자를 못 싣는다** — `SEC-01`·`SEC-05`·`SEC-17`·`OPS-13a`·`PERF-04`·`LAW-08`
  여섯이 **언제나 회색**이다. 턴 AA 의 구멍이 그대로다.
  ★ 다만 **OPS-07 은 이 구멍을 우회해 닫혔다** — F 가 **얇은 진입점**으로 갈랐다.
    같은 손이 나머지 여섯에도 쓰일 수 있다(다음 턴 후보).
- **`OPS-07b` 의 「선언하고」가 시드 위에서만 초록**인 자리는 이 턴에도 안 건드렸다.
- **영역 ① 의 옛 잣대 10** — 위 ③. V 의 회차가 열쇠다.

## ⑩ 내가 안 한 것 — 남의 파일

`verify_spec_coverage.py`·`verify_readiness_scores.py`(Q) **읽기만** ·
`ops_log_collectors*.py`·`ops_retention_policy.py`(F) **읽고 돌리기만** ·
`verify_click_completes.py`·`row_map.json`(A) **읽고 돌리기만** ·
`spec_ids.json`(Q 의 파생물) **손으로 한 자도 안 고쳤다** —
⚠ 다만 **Q 의 산출기를 돌렸기 때문에 그 파생물이 스스로 갱신됐다**(`등재 0 → 136` ·
`미등재 136 → 0`). 그것이 이 파일의 사는 방식이고(「손으로 고치지 않는다」),
동시에 **내 등재가 기계에 실제로 보였다는 증거**다. 숨기지 않고 적는다 ·
§0.4 금지구역 **안 들어갔다** · `--no-verify` 0 · **삭제 0** · **커밋 0**(조율자 몫).
