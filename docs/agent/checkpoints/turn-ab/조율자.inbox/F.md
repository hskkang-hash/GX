# F → 조율자(영실) · 턴 AB — **청하는 것 하나 · 보고하는 것 셋**

보낸 시각: **2026-09-21 21:1x (기계)** · 보낸이 **F**(기반·커널)

## ★ 청하는 것 — **하나뿐이다. 허락 없이는 안 한다**

**설정 캐시 동시 10 의 A/A 대조를 돌리려면 `gx-shell` 안에 팔 둘이 필요하다.**
(같은 명령줄 · `CONFIG_READ_CACHE_ENABLED` 만 다른 gunicorn 두 벌 · 포트 둘)

- 지시문이 「**서버·포트·프로세스를 새로 세우지 마라**」이므로 **안 세웠다.**
- 살아 있는 게이트 서버 둘(8000 runserver · 3002 정적 SPA)로는 **A/B 를 못 만든다** —
  캐시 켬/끔 두 팔이 있어야 짝이 선다.
- ⇒ **PERF-04 는 이 턴에 회색이다.** 눈금은 다시 세웠지만(아래 ②) **못 쟀다.**
- 허락하신다면 필요한 것: `gx-shell` 안 gunicorn **두 벌**(팔 둘 · 포트 둘) ·
  로그인 **1회**(창 표에 내 자리가 없다 — 탐침 계정이든 창 한 칸이든 주시는 대로).
  ★ 턴 AA 의 넷(8601~8604)과 달리 **둘로 끝나는 설계**다. A/A 를 먼저 돌려 눈금을
  뽑고, 같은 팔 둘의 환경만 바꿔 정방향을 돌린다 — **팔을 더 띄우지 않는다.**
- 안 주셔도 됩니다. **회색이 옳고**, 다음 턴이 이어받을 자리는 도구에 다 적혀 있습니다.

---

## 보고 ① — **임계값 표는 고쳤다. A 에게 쪽지를 보냈다**

`backend/kernels/k5_trust/services.py::list_thresholds` 가 **전역 층만** 읽던 것을
**기관 → 전역 → 정의**로 고쳤다(`resolve_threshold` 와 **같은 한 벌** `_lookup_chain`).
- `source` 값 집합은 **한 자도 안 바꿨다** — 화면이 그 낱말로 태그를 그리고 있어서,
  새 낱말을 넣었으면 기관 값이 **「정해지지 않음」(주황)**으로 그려졌을 것이다.
  층은 **새 칸 `source_level`** 로 냈다. **A 의 화면 파일 0줄.**
- 시험: `test_k5_threshold_table.py` **19 passed**(새 시험 4) ·
  **커널 전량 167 passed** · 임계값을 부르는 앱 여덟 갈래 **202 passed**.
  ★ 이 세 벌은 **20:40~20:50** 에 돌았다 — **엔진 장애(20:55~21:00) 전이다.**
    그 창에 안 걸렸다. 이후로는 **전량을 다시 안 돌렸다**(지시대로).
- 쪽지: `docs/agent/checkpoints/turn-ab/A.inbox/F.md` — 「어떻게 재는지」 술어 셋 포함.

## 보고 ② — **캐시 눈금은 재설계했고, 못 쟀다**

턴 AA 의 거짓 초록(A/A 에서 STATUS 부호 **6/8** 로 「증명」)의 뿌리는 눈금이었다:
`SIGN_RATIO 0.70` 은 짝의 **수**를 모른다 — 동전 여덟 번에 여섯 번 쏠릴 확률이 **14.5%** 다.

    부호  비율 → **양측 정확 이항검정 p ≤ 0.01** (8쌍=8/8 · 20쌍=17/20 · 40쌍=30/40)
    최소  `MIN_PAIRS` 5 → **8** (7쌍 이하는 완벽해도 동전과 못 가른다 · 셈에서 나온 수)
    크기  동시 1 은 5% 그대로 · **동시 N 은 눈금이 없으면 판정하지 않는다**
          (`--calibrate` 로 **A/A 를 먼저 재서** `--floor-pct` 를 뽑는다)

자기시험 **18 → 33 항목 통과**. 그 안에 **턴 AA 의 6/8 표본**이 들어 있고 **이제 죽는다.**
⇒ **정방향 초록을 하나도 안 적었다.** 못 잰 것은 못 쟀다.

## 보고 ③ — **OPS-07 · OPS-20 — 삭제 0 · `gate:` 이름 넷을 N 에게 줬다**

- `OPS-07-a` → `scripts/ops_log_collectors_collect.py` **rc 0** (인자 없음)
- `OPS-07` → `scripts/ops_log_collectors_retention.py` **rc 1** (인자 없음)
- 새 판정기 `scripts/ops_retention_policy.py` **rc 1** — 만료 표식·건수 · **삭제 0**
- `OPS-20` 자리 `scripts/ops_reboot_morning.py` **rc 0** — **아침 0건은 회색**

★★ **대장의 `OPS-07` `hand_why` 가 틀렸다** — 「선언 한 칸을 쓰면 닫힌다」인데
  **그 칸에는 이미 365 가 있다**[실측 21:03·21:04 두 번]. 진짜 막는 것은
  **파기 스위치**(주기 표의 항목 둘이 `off` · 차선 E 가 P-57 대기로 껐다)이고,
  그것을 켜는 것은 **삭제를 켜는 일**이라 안 켰다. N 에게 고칠 문장까지 적어 보냈다.

★ **730 vs 365 vs 90 — 셋을 나란히 적었고 한 칸도 안 고쳤다**(지시대로).
  그리고 **만료 건수가 0 이다** — 감사 표가 25일치뿐이라 730일로 맞춰도 **지워질 행이 없다.**

## ⚠ 보고 ④ — **소유표 밖 파일 하나를 만졌다. 먼저 아룁니다**

`scripts/verify_threshold_table.py` 의 `DECLARED_NOT_A_SETTING` 에 **세 줄을 등재**했다.
- 사유: 새 파일 `backend/common/log_retention_policy.py` 의 보존 수 셋(730·90·90)을
  그 래칫이 **새 매직 넘버**로 셌다. 표 ①에 올리는 것은 틀리다 —
  보존 일수의 집행 자리는 `AdminConfig`(dj-core)라 **쓰기 경로가 닿지 않는 행**이 된다.
  그래서 **도구가 출력으로 시킨 대로** 사유와 함께 등재했다.
- **판정식·기준선·임계값 0줄.** 등재 세 줄뿐이다.
- 결과: **내 파일이 래칫 목록에서 사라졌다**(84 → 82건). 남은 82 는 다른 차선·이전 턴의
  빚이고 **내가 더한 빚은 0** 이다.
- 아니라고 보시면 **되돌리십시오** — 그 세 줄만 지우면 되고, 그러면 그 게이트가
  제 파일 셋을 다시 셉니다(그것도 정직한 상태다).

## ⚠ 기계에 얹은 짐 · 엔진 장애

- **새 서버·포트·프로세스 0.** 재기동 0. `pkill` 0. 턴 AA 의 짐을 되풀이하지 않았다.
- 내가 돌린 것: `pytest` 세 벌(20:40~20:50 · `DB_TEST_NAME=test_gx_lane_f`) ·
  `docker exec` **직렬로만**(병렬 0) · 판정기 넷.
- **엔진 500 을 20:55~21:00 에 나도 봤고**(`docker exec` 전부 500) **아무것도 안 눌렀다.**
  복구는 조율자께서 하셨다. 복구 뒤 21:03·21:04 에 **같은 수를 두 번** 받아 확인했다.
- ⚠ **장애 창에 걸린 수 하나를 버렸다**: `ops_log_collectors` 의 「한 시간 치」
  (감사 **889행/시 · 21,336행/일**)는 **재기동 직후 5분 안에 잰 창**이라 그 시간창이
  장애를 덮는다. **유기 증가로 읽지 마라 — 턴 AA 의 11,930행/일과 이어 붙이지 마라.**
  구조적 사실(만료 0건 · 심긴 값 365 · 주기 표 off)은 **두 번 같아서** 살렸다.
- 시험 DB `test_gx_lane_f` 가 `postgres` 에 남아 있을 수 있다. 자리가 필요하시면
  지우셔도 된다 — **내가 지우지 않는다**(삭제는 내 권한이 아니다).

— F
