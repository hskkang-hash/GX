# A → 조율자(영실) · 턴 AB

차선 **A** · 2026-09-21

---

## ⚠⚠ 1. **Docker 데몬이 죽었습니다 — 전 차선이 지금 거짓 빨강을 잽니다** [실측]

```
[실측 2026-09-21 20:4x KST · 호스트]
docker ps        → 500 Internal Server Error (…/dockerDesktopLinuxEngine/v1.54/containers/json)
docker exec …    → 같은 500 · 6회 연속 재시도 실패 (10초 간격)
docker version   → 클라이언트는 29.3.1 로 답합니다 (죽은 것은 **데몬**입니다)
```

**턴 AA §5-1 과 같은 사고입니다.** 그때는 데몬이 돌아오면서 `gx-shell` 안의
**runserver 8000 · SPA 3002 가 전부 사라졌습니다**(주 프로세스가 `sleep infinity` 라
그 안에서 돌던 것들이 함께 걷혔습니다). **이번에도 같은 일이 날 것으로 봅니다.**

### 이것이 지금 수를 어떻게 망치고 있나 — **바이트로 댑니다**

```
[영역 ① · 같은 row_map · 같은 증거 · 데몬만 다름]
데몬 살아 있을 때   초록 17 · 빨강 0 · 회색 15   → 가중 8.72
데몬 죽은 뒤        초록 15 · **빨강 3** · 회색 14 → 가중 7.69
빨강 셋 = F-05-c1 · F-05-c2 · F-05-c3
사유   = 「게이트 «scripts/verify_contract_route_reach.py» 가 **빨강(exit 1)** 이다」
진짜 사유 = 그 게이트가 **gx-shell 안으로 위임**해 HTTP 를 때리는데(`GX_ROUTE_CONTAINER`)
            `docker exec` 가 500 이라 한 건도 못 때렸습니다
```

★ **그 빨강은 제품이 아니라 데몬입니다.** 규약대로면 **못 잰 것은 회색**이어야 하는 자리이고
(「못 잰 칸은 0 이 아니라 회색」), 지금은 **못 잰 것이 빨강으로 내려앉습니다.**
판정기 쪽 자리라 **제가 안 고쳤습니다**(P-203 · 판정기는 조율자가 안 고친다).

⚠ **`docs/agent/evidence/P-106/feature_reach.json` 에 그 판(15/3)이 이미 적혔습니다.**
게이트가 돌 때마다 스스로 씁니다 — 제가 손으로 쓴 것이 아닙니다.
「경합 중에 잰 수는 수가 아니다」에 걸리는 판이니, **데몬이 돌아온 뒤 다시 재서 덮어 주십시오.**

### ✅ 해소됐습니다 — 다시 쟀습니다 [영실님 복구 뒤]

```
[실측 21:0x · 복구 뒤]
gx-shell 안  8000 OPEN(pid 60) · 3002 OPEN(pid 163) — **스스로 돌아왔습니다**
제 3012(턴 AA 격리본)은 안 돌아왔고 **다시 안 띄웠습니다** (치워 달라 청했던 것입니다)
[REACH] 색: 초록 **18** · 빨강 0 · 회색 14 · 잠김 7 → 가중 **9.23**
```
**저는 서버를 하나도 새로 안 세웠습니다.**

---

## ⚠⚠ 1-b. **데몬이 돌아온 뒤에도 빨강 셋이 남았습니다 — 둘째 사유는 차선 B 입니다**

데몬 탓으로 덮고 넘어갈 뻔한 자리라 갈라서 적습니다.
⚠ 제가 한 번 헛읽었습니다 — `게이트 | grep …; echo $?` 는 **grep 의 코드**를 찍습니다.
파이프를 걷어내고 **진짜 종료 코드**를 받으니 게이트가 살아 있는데도 **exit 1** 이었습니다.

```
[실측] verify_contract_route_reach.py  REAL EXIT=1 · DSM 라우트 **20개가 -> 500**
[실측] gx-shell:/tmp/gx_gates/api.log
       psycopg2.errors.UndefinedColumn:
           column stream_monitors_streammonitor.data_source does not exist
       ← backend/stream_monitors/migrations/0031_p224_camera_data_source.py
         (**P-224 · 차선 B 의 이번 턴 마이그레이션** · 창 10:30~10:45)
[실측] 잠시 뒤  showmigrations stream_monitors → 0031 **[X]**
[실측] 다시 재니 라우트 **109/109 도달 · exit 0** → 영역 ① **초록 18 · 빨강 0**
```

★ **모델에 칸이 생겼는데 DB 에 아직 안 앉은 창**이 있었고, 그 사이
**DSM 읽기 라우트 20개가 통째로 500** 이었습니다. 그 창에 잰 차선은 누구든
**제 화면이 멀쩡한데 빨강**을 받았을 것입니다(N 이 본 71.5 → 69.3 흔들림도 이 창일 수 있습니다).

⇒ **제가 고친 것은 없습니다 — 기다렸다가 다시 쟀습니다.** 제 차선 밖입니다(B 의 마이그레이션).
⇒ 병합 뒤 다시 재실 때 **`showmigrations` 가 전부 `[X]` 인지 먼저 보시고** 재시면
   같은 거짓 빨강을 한 번 덜 보실 것입니다.

### 남은 부탁 하나

**영역 ① 정본은 초록 18 · 가중 9.23 입니다**(`P-106/feature_reach.json` 도 18 로 덮였습니다 — 확인했습니다).
중간 판(15/3 · 7.69)을 보신 자리가 있으면 **버려 주십시오.**

---

## ✅ 1-c. F 쪽지 처리 완료 — **재측했고 초록입니다** · 그런데 창 하나를 태웠습니다

```
[실측 2026-09-21T12:19:30Z]
U5#S3  OOOO  F-12-c6  임계값 저장
  `thresholds` '10.0,…' → '**11.0**,…'   ← 「지금 값」이 바뀐다 (F 의 고침이 실물에서 섭니다)
  곁칸 `history` 4 → 5                    ← 「바꾼 기록」도 안 지우고 같이 봅니다
```
★ 쪽지를 **믿지 않고 시각으로 갈랐습니다** — 커널 파일 mtime 11:35:59 UTC 대
runserver 시작 12:01:04 · gunicorn 12:00:43 (**둘 다 고침보다 늦게 떴으니 옛 코드일 수 없습니다**).
마침 영실님의 21:01 복구가 둘을 새로 띄워 준 덕입니다.
새 곁칸(`also`)의 음성 대조도 **6/6 → 8/8** 로 늘렸습니다 —
「값은 바뀌었는데 기록이 안 남았다」는 **빨강**, 「기록 칸을 못 읽었다」는 **회색**입니다.

### ⚠ 청 하나 — **`u4_official` 창을 한 번만 더 주십시오**

```
U4#S5  X???  F-12-c1  그 사람으로 **화면에 들어가지 못했다** (/login 에 머문다)
       화면이 한 말: 「지금 다시 시도할 수 있습니다.」  ← **로그인 throttle**
```
**제 탓입니다.** 커널 고침을 확인하려고 손으로 `u5_sysop` 로그인을 **세 번** 했고
(응답 모양을 몰라 두 번 헛짚었습니다) 그 직후 회차라 **IP 당 5회/분**에 걸렸습니다.
그래서 설정 다섯이 **5/5 → 4/5** 가 됐습니다(**빨강 0** · 내려간 하나는 **못 잰 것**입니다).

★ **다시 안 눌렀습니다.** 창 표상 60분 뒤 `u4` 는 **U3** 의 것이고, 이번에 영실님이
**`u5_sysop` 만** 비었다고 하셨습니다. 로그인이 `end_previous_session` 이라 제가 누르면
**U3 의 세션을 끊습니다** — 턴 AA 에 셋이 끊긴 그 모양이라 **안 했습니다.**

⇒ `u4` 를 한 번만 주시면 **한 회차(2분)로 `F-12-c1` 을 되돌려 놓겠습니다.**
못 주시면 그대로 두십시오 — 한 회차 전(`10:13:39Z`) 판은 **git 스테이지에 살아 있고**
(`git show :docs/agent/evidence/P-118/settings_clicks.json`) N 께 선택지 둘을 적어 보냈습니다.
**저는 「디스크의 정본이 회색이니 안 올리는 쪽」을 기본으로 봅니다.**

---

## 2. `.git/index.lock` — **제가 안 지웠습니다** (✅ 영실님이 걷어내셨고 지금은 staging 됩니다)

```
[실측] .git/index.lock  0바이트 · mtime 20:15 · 확인 시각 20:44
       실행 중인 프로세스: gitleaks.exe (pid 2568)  ← `git` 본체가 아니라 **훅**입니다
```
gitleaks 가 돌고 있다는 것은 **어느 차선인가 지금 커밋 중**이라는 뜻이라
**살아 있는 잠금**으로 읽었습니다. 제 기억의 규칙(「0바이트·오래됨·**git 프로세스 0** 일 때만 지운다」)이
셋째 조건에서 안 맞습니다. 그래서 **안 지웠고**, 제 파일들을 `git add` 하지 못했습니다.

⚠ 턴 AA 에 **제 `row_map.json` 쓰기가 통째로 HEAD 로 되돌려진 사고**가 있었고(그 턴 §1),
그때 처방이 「쓰자마자 `git add`」였습니다. 이번엔 그 처방을 **못 썼습니다.**
대신 제 두 파일을 저장소 밖에 복사해 두었습니다. 병합 전에 아래 둘이 **제 판인지** 확인해 주십시오:

```
scripts/verify_click_completes.py        (AREA1_CLAUSE_MAP · --clause-evidence 가 있으면 제 판입니다)
docs/agent/evidence/P-142/row_map.json   (rows 25 · linked 8 이면 제 판입니다)
```

---

## 3. 「지금은 …」 전제 둘을 대 봤습니다 — **하나는 맞고 하나는 반쪽입니다**

| 지시서가 적은 전제 | 실측 | 판정 |
|---|---|---|
| 「게이트 서버는 지금 **둘 다 살아 있다**」 | 착수 시각(20:3x)에 **맞았습니다** — 8000 runserver(pid 1236 · 09:50부터) · 3002 SPA(pid 1322) | **참** |
| 〃 (제 첫 확인) | ⚠ 제 첫 확인은 **거짓 음성**이었습니다 — `curl` 이 `gx-shell` 에 **없어서** 빈 값이 왔습니다. `python urllib` 로 다시 재서 **둘 다 200** 을 받았습니다 | 제 실수 · 적어 둡니다 |
| 「3002 = 턴 AA 합본 번들」 | `/app/_fe_dist/assets/SettingsRules-CXr6lIOs.js` · `scope_ref` **1** · `/api/dsm/me` **1** · 「지금:」 **1** — 턴 AA 의 제 제품 고침 둘이 **들어 있습니다** | **참**(턴 AA 보 ④ 는 해소됐습니다) |
| 「도달 **17**/39 · `kind closed` **10**/39」 | 도달 17 실측 확인 · `ga_readiness.yaml` 의 `kind_derived.closed` 도 **10** 실측 확인 | **참** |
| 「목표 **도달 ≥ 24**」 | ⚠ **이 전제는 제 실측과 안 맞습니다** — 제 보고 §1 을 보십시오 | **반증** |

---

## 4. ⚠ 「도달 ≥ 24」는 이번 턴에 **정직하게는 못 닿습니다** — 사유를 댑니다

남은 회색 15절을 **한 절씩** 코드로 대 봤습니다. 캡처만 있으면 서는 절은 **둘뿐**입니다.

| 절 | 막는 것 | 캡처로 풀리나 |
|---|---|---|
| **F-10-c1** | 옛 사유가 낡았다 | **이었습니다 · 초록** (+1) |
| **F-14-c2** | 화면은 **있는데 찍은 적이 없다** | **예** — 아래 §5 |
| F-01-c1 · F-02-c2 · F-04-c1 · F-04-c2 · F-10-c2 | **시간·내부 변환** — 화면 한 장이 답할 수 없다 | 아니오 |
| F-02-c1 · F-03-c1 · F-03-c2 | 그 신호/결합/상향을 **보여 주는 화면이 없다** | 아니오 |
| F-03-c3 | 폴리곤 편집기가 없다(있는 폴리곤은 **§0.4 금지구역** 쪽입니다) | 아니오 |
| F-09-c1 | 5상태 중 **넷을 찍으려면 화면에 시험 장치**가 필요합니다 — **P-220 에 걸립니다** | 아니오 |
| F-11-c1 | 캡처가 **종이에 안 실린다**(`incident_report.py` — **U24 의 파일**) | 아니오(U24 가 고치면 예) |
| F-12-c4 | **위젯 관리 문이 서버에 0건**입니다(grep 0) | 아니오 |
| F-13-c1 | 부작위(비행 명령 미전송) · 재는 게이트가 없습니다 | 아니오 |

⇒ 정직한 천장은 **17 + 2 = 19** 입니다(F-14-c2 는 캡처가 서야 하고, 그것은 Docker 가 필요합니다).
**24 로 가려면 절을 억지로 잇는 수밖에 없고, 그 초록은 초록이 아닙니다.**
「분모를 손으로 적지 않는다」와 같은 자리라 **수를 만들지 않고 사유를 적었습니다.**

---

## 5. 다음 회차가 찍을 자리 — **이름으로 못박아 둡니다** (`F-14-c2`)

★ 턴 AA 의 제 판단이 **한 자리를 안 봤습니다 — 제가 틀렸습니다.**
저는 **월간 자동본**(`build_monthly_html`)만 보고 「오탐률을 내는 자리가 없다」고 적었습니다.
자동본에 대해서는 지금도 참입니다. 그런데 **전용 화면이 따로 있습니다**:

```
route   /dsm/stats/false-positive        (routes.u24.ts:28 · <CameraTuning mode="false-positive"/>)
문      GET /api/dsm/stats/false-positive/by-camera
그리는 줄  CameraTuning.tsx:89  rateLine → `N% (false_positive/reviewed)`
           ★ **비율과 분자·분모를 같은 줄에** 적습니다. `reviewed` 가 곧 **사람의 판정**이라
             이 절의 「분모·분자로 돌아온다」가 그 한 줄입니다.
           ★ 분모 0 이면 `measurable:false` 로 **비율을 안 만듭니다**
             (절의 note 「분모 0 은 0% 가 아니라 None 이다」와 같은 규율입니다)
월간      DAY_OPTIONS = [7, 30]  (CameraTuning.tsx:86) · 서버도 30일을 받습니다(_MAX_WINDOW 366일)
캡처      INDEX 42장에 **한 장도 없습니다**
```

⇒ 막는 것은 「화면이 없다」가 아니라 **「찍은 적이 없다」**입니다 —
턴 AA 에 넉 장을 찍어 일곱 절을 연 것과 **똑같은 모양**입니다.

**찍을 자리**: route `/dsm/stats/false-positive` · 역할 U2 `fire_admin`(또는 `admin`) ·
슬러그 `dsm_stats_false_positive` ·
⚠ **「30일」 칸을 누른 뒤에** 찍어야 합니다 — `days` 는 주소에 안 실리고 `useState(7)` 로 시작합니다.
**그냥 열어 찍으면 7일 창이라 「월간」이 아닙니다.**

⚠ 저는 이 캡처를 **못 찍었습니다**(Docker 500). 그래서 `row_map` 에 **잇지 않고**
사유를 「찍은 적이 없다」로 갈아 끼워 두었습니다 — 지금 이으면 캡처가 없어 **빨강**이 날 자리입니다.

---

## 6. 요청 — 공용 파일 한 줄 (제 차선이 아닙니다)

`frontend/src/features/dsm/pages/CameraTuning.tsx` 는 **제 파일**이라 제가 고칠 수 있으나,
고치면 **번들을 다시 빌드해 심어야** 하고 그것은 조율자의 줄입니다. 제안만 적습니다:

> `days` 를 주소에 싣는다(`?days=7|30`). 지금은 `useState(7)` 이라
> **「월간 오탐률 자리」를 링크로 주고받을 수 없고**, 캡처도 7일 창만 찍힙니다.
> 턴 AA 에 `SettingsRules` 의 탭을 주소에 실은 것과 **같은 사유**입니다.

**이번 턴에 저는 이 고침을 하지 않았습니다** — 빌드가 필요한데 지금 Docker 가 죽어 있고,
`gx-fe-build:/app/src` 를 덮으면 **동시에 빌드하는 다른 차선의 산출물이 제 소스로 설 수** 있습니다
(턴 AA 에 제가 겪은 자리입니다). **조율자 판단에 맡깁니다.**
