# U3 → 조율자 (영실) · 턴 AB

마지막 갱신: 2026-09-21 21:1x KST · 기계 시각 · 브랜치 `turn-q`

---

## ① 창 — **`gxseed_u4_official` 을 안 씁니다. `gxseed_u5_newop` 하나만 씁니다**

들어가기 전 한 줄을 적으라 하셔서 적습니다. **안 들어갑니다.**

사유는 취향이 아니라 **실측**입니다 [U3 · ORM 직독 · 2026-09-21]:

```
gxseed_u1_operator (U3 정본 계정 · U1 이 쥠)   roles=[(id 12, …)]
gxseed_u5_newop    (제 창)                      roles=[(id 12, …)]   ← **같은 역할**
gxseed_u4_official                              roles=[(id  9, …)]   ← 다른 역할 (**읽기 전용**)

⚠ 정정: `role.group_id`(6·7·9)는 **역할이 속한 묶음이지 자료의 기관이 아니다.**
   [U3 실측 21:3x] `DeliveryRecord` 230행 · `DetectionEvent` 41행이 **전부 group 4** —
   **기관은 하나**다. 그러니 「기관이 다르다」는 제 첫 글이 **틀렸다.**
   갈리는 것은 기관이 아니라 **역할**이고, 그 역할이 무엇을 못 하는지는 아래가 잰다.
```

- U3 의 술어 둘(#1 `/m/inbox` 카드 · #7 `POST …/response`)은 **쓰기**이고,
  `u4_official` 은 **그 쓰기를 못 합니다** — [U3 실측 21:3x] 없는 id 로 두드려도
  **403 `read_only_role` 「읽기 전용 계정입니다 — 이 작업은 수행할 수 없습니다」** 입니다.
  그것은 **화면의 사실이 아니라 권한의 사실**입니다.
- `u5_newop` 은 정본 계정과 **역할이 같습니다.**
- ⇒ **A·U24 와 `u4_official` 을 다툴 일이 없습니다.** 제 창의 절반을 돌려 드립니다.

## ② VAPID — **대표 손이 필요한 자리 하나**(P-228 · 값 0)

**금고 쌍 재적재에 성공했습니다.** 새 쌍을 **안 만들었고**, 따라서 **구독 재등록 안내
카드도 안 띄웠습니다**(P-228 둘째 갈래 미발동 — 기존 구독 1건이 **살아 있습니다**).

| 이름 | 길이 | sha256 앞 12자 | 어디 |
|---|---|---|---|
| `GX_VAPID_PUBLIC_KEY` | 87 | `879067f2f9a1` | 금고 `C:\GuardianX-vault\webpush\vapid_20260917.env` |
| `GX_VAPID_PRIVATE_KEY` | 43 | `909ed33a72ca` | 〃 |
| `GX_VAPID_SUBJECT` | 23 | `ebd85a0437d7` | 〃 |

- 저장소·`.env*`·보고·argv·쿼리 문자열에 **값 0글자**. 컨테이너로는
  `docker exec -e <이름>` 으로 **이름만** 넘겼습니다(`=값` 을 안 썼습니다).
- **`.env*` 를 한 자도 안 고쳤습니다**(대표 자리).

### ⚠ 대표 손이 필요한 자리 — **한 줄**

지금 자격은 **제가 부른 한 번의 프로세스에만** 있었습니다. **돌고 있는
`gx-gunicorn-e`·`gx-celery-e` 의 환경에는 아직 없습니다**(실측: 세 이름 전부 `len=0`).
⇒ **고객이 `/m/settings` 의 「내 기기로 지금 한 통」을 누르면 아직 503** 입니다.

싣는 일은 **컨테이너 환경 변경 = 재기동**이라 **제가 안 했습니다**(서버 재기동 금지).
대표/조율자 손이 필요한 한 줄은 이것뿐입니다:

> 금고의 `vapid_20260917.env` 세 이름을 `gx-gunicorn-e`·`gx-celery-e` 환경에 실어
> **재기동 한 번**. (값은 금고에만 있습니다 — 채팅·보고에 옮겨 적지 마십시오.)

## ③ 남의 파일에 있는 「사람에게 안 감」 — **손 안 댔습니다. 쪽지로 넘깁니다**

정본을 `features/dsm/deliveryOutcome.tsx` **하나**로 모았습니다(§3 ③). 제 소유가
아닌 자리 둘은 **한 줄도 안 고쳤습니다**:

| 파일 | 줄 | 차선 | 넘긴 곳 |
|---|---|---|---|
| `pages/NotifySettings.tsx` | 153 · 465 · 520 | **U56** | `U56.inbox/U3.md` |
| `pages/DrillMode.tsx` | 34 · 59 · 131 · 151 | **소유표에 없음** | ← **이 쪽지** |

★ `DrillMode.tsx` 는 §3 소유표 어느 줄에도 없습니다. 그 화면의 「(사람이 아니라
로그에 도달합니다)」는 제 정본과 **같은 사실을 다른 말로** 합니다. **제가 안 고쳤고**
(소유 밖), **누구 것인지도 제가 정할 수 없습니다** — 판정을 청합니다.
⚠ 다만 `DrillMode` 의 말은 **사건의 성질**(이 테넌트가 훈련 중)이라 제 것과
**다른 사실일 수도** 있습니다. 같은 낱말로 묶기 전에 그것부터 갈라야 합니다.

## ④ 공용 파일 — **청하지 않습니다**

`App.tsx` · `routes.ts` · `services/API.ts` **0줄**. 새 라우트 0 · 새 쓰기 문 0.

---

## ⑤ ★★ 막혔습니다 — **`gxseed_u5_newop` 의 비밀번호가 `.env.gates` 에 없습니다**

**2/5 쓰고 멈췄습니다. 더 안 찍습니다** — 5 회면 계정이 잠기고, 잠긴 계정은
제 차선이 아니라 **V 의 48행과 K 의 카드 캡처까지** 끊습니다.

```
[U3 실측 · gx-shell 안 · 2026-09-21 21:0x KST]
POST /api/v1/auth/login  {username: gxseed_u5_newop, end_previous_session: true}
  자격 GX_SEED_ROLE_PASSWORD  len=24  sha256앞12 = de08e1f17eb3   ← 턴 AA 가 적은 값과 같다
  →  400  "비밀번호가 올바르지 않습니다. 시도 2/5."
```

**자격이 틀린 것이지 문이 닫힌 것이 아닙니다.** `.env.gates` 35행이 스스로 적고 있습니다:

```
# 시드 역할 계정(U2·U4) 공용 비밀번호 — seed_role_users (E3 · 2026-09-04)
```

⇒ 그 낱말은 **`u2_manager` 와 `u4_official` 것**입니다. `u5_newop` 은 09-08 에
`POST /api/v1/user/create-user` 로 따로 만들어졌고(`evidence/CR-USER/U5U6/evidence_index.json`),
**그때 쓴 비밀번호가 저장소 어디에도 안 적혀 있습니다**(grep 0건 — 당연히 옳은 일입니다).

### 청하는 것 — 셋 중 **하나**면 됩니다 (값은 적지 마십시오 · 이름만)

1. `u5_newop` 의 비밀번호를 담은 **환경 변수 이름** 하나 (예: `.env.gates` 에 한 줄 추가) — 가장 깨끗합니다.
2. 또는 **`gxseed_u1_operator` 로 U3#7 을 U1 이 눌러** 주고 그 `audit_id` 를 쪽지로 (U3 정본 계정이 그쪽입니다).
3. 또는 제 창(60~80)에 **`u4_official` 만** 쓰라는 한 줄 — 다만 그 계정은
   `view_only_-_anyang`(⚠ 「기관 7」 은 **틀린 말** — ⑩ 참조 · 읽기 전용 역할)이라
   **`POST …/response` 가 403 일 것**이고,   ← **실제로 403 이었습니다**(⑨)
   그러면 U3#7 은 **화면의 사실이 아니라 권한의 사실**로 회색이 됩니다.

**그때까지 U3#7 은 회색입니다 — 색을 제가 칠하지 않습니다.**

## ⑥ 지금까지 바뀐 서버 상태 — **둘뿐** (둘 다 훈련 표식)

| 무엇 | 언제 | 되돌릴 수 있나 |
|---|---|---|
| `DeliveryRecord` +1행 (`channel=webpush` · `drill:webpush:dc716379fa14` · id **694**) | 21:02 KST | 훈련 표식 · `suppress` 가 안 셈 |
| `gxseed_u5_newop` 로그인 실패 **2회**(시도 2/5) | 21:0x KST | 성공 1회면 0 으로 돌아갑니다 |

**제품 쓰기 0 · 새 라우트 0 · 서버 재기동 0 · 삭제 0 · 커밋 0.**

---

## ⑦ 덧 — **제품의 장부가 ⑤를 뒷받침합니다** (추론이 아닙니다)

이 턴에 **차선 B 가 쓴 마이그레이션**이 이미 적어 두고 있었습니다
(`backend/common/migrations/0003_p224_seed_account_marks.py`):

```
(110, gxseed_u1_operator)  … seed_role_users 가 만든 역할 씨앗 (employee_id=GX-SEED-ROLE-U1)
(108, gxseed_u2_manager)   … 〃 (GX-SEED-ROLE-U2)
(109, gxseed_u4_official)  … 〃 (GX-SEED-ROLE-U4)
(115, gxseed_u5_sysop)     … 〃 (GX-SEED-ROLE-U5)
(117, gxseed_u5_newop)     … 온보딩 첫 근무일 계측용 예비 계정 — **employee_id 표식 없음**
```

⇒ `u5_newop` 은 **`seed_role_users` 가 만든 계정이 아닙니다.** 그러니 그 명령의
공용 낱말이 그 계정에 맞을 이유가 없습니다. **제가 자격을 잘못 고른 것이 아니라,
그 계정의 자격이 어디에도 안 적혀 있는 것**입니다.

---

## ⑧ ★ ①을 **고칩니다** — `u4_official` 에 **21:29 에 한 번** 들어갑니다 (읽기만)

①에 「안 들어갑니다」라고 적었는데, ⑤(자격 없음) 때문에 **그 말을 무릅니다.**
말을 바꾼 사실을 지우지 않고 그대로 둡니다.

- **언제**: 창 표의 `u4_official` **60~80분 = 21:29~21:49 KST** (A·U24 의 0~60 이 끝난 뒤).
- **무엇**: `GET /api/dsm/deliveries`(mine 두 갈래 × limit 두 갈래) · `GET /api/dsm/me/handled-events`.
- **쓰기**: **실재하는 id 로 안 두드립니다.** `POST …/events/987654321/response` ·
  `…/field-reply` 로 **닿음만** 잽니다(404 = 관문 지나 핸들러까지 · 403 = 관문에서 막힘 ·
  P-83 눈금). **상태 0 변경.**
- **로그인 1회.** 끝나면 나옵니다. 80분 뒤 U56 창을 안 넘습니다.

⚠ 그래도 **U3#7 은 이것으로 안 닫힙니다** — 「누른 뒤 서버 기록이 는다」는 **실재 사건에
실제 전이**가 있어야 하고, 그건 `u4_official`(읽기 전용 · ⚠ 「기관 7」 은 ⑩에서 무릅니다)이
할 일이 아닙니다.
**⑤의 셋 중 하나**가 여전히 필요합니다.

---

## ⑨ 창을 쓰고 나왔습니다 — **로그인 1회 · 21:29** (요약)

```
POST /api/v1/auth/login  gxseed_u4_official  →  200 · access_token 길이 300
  자격 GX_SEED_ROLE_PASSWORD  sha256앞12 = de08e1f17eb3   ← u5_newop 에서는 같은 낱말이 400
  ⇒ 로그인 문은 살아 있습니다. 못 들어간 것은 그 계정의 자격이 없어서입니다.

deliveries?mine=true   limit 50 / 500  →  total 3 / 3    카드 2장  (log 3 · 전부 「기록만」)
deliveries?mine=false  limit 50 / 500  →  total 50 / 230  사건 17  (log 226 · email 2 · webpush 2 · 실패 1)
me/handled-events                       →  200 · total 0
POST events/987654321/response|field-reply → **403 read_only_role** 「읽기 전용 계정입니다」
```

- **실재 id 로 쓰기를 안 두드렸습니다. 상태 0 변경.**
- `mine=true` 는 `limit` 을 500 으로 열어도 **3** — **서버가 좁힌 3** 입니다.
- `mine=false` 의 **230 = ORM 전량 230** — 두 갈래가 같은 수입니다.
- 80분(21:49) 전에 나왔습니다. **U56 창을 안 넘었습니다.**

## ⑩ ⚠ 제 ① 쪽지의 **정정** — 「기관 6 / 기관 7」은 틀렸습니다

```
[U3 실측 · ORM · 21:3x]  DeliveryRecord {group 4: 230} · DetectionEvent {group 4: 41}
                          세 계정의 user.groups(m2m) 는 전부 빈 목록
```
⇒ **기관은 하나(4)** 입니다. `role.group_id`(6·7·9)는 **역할이 속한 묶음**이지 자료의
기관이 아닙니다. 갈리는 것은 **역할**이고, 그 역할이 못 하는 것은 위 **403** 이 말합니다.
**틀린 글을 지우지 않고 정정으로 남겼습니다** — 안 그러면 다음 사람이 있지도 않은
「테넌트 격리 문제」를 팝니다.
