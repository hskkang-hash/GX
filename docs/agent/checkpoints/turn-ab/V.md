# 턴 AB — V 단독 검증자

브랜치 `turn-q` · HEAD `f799fb1` · 잠금 `docs/agent/evidence/V_LOCK` 세션 `turn-ab-v`(호스트에 잠갔다 · **아직 안 풀었다 — 조율자가 `--unlock`**)
기계 시각 시작 2026-09-21T23:42 KST · 끝 **2026-09-22T00:29 KST** (컨테이너 UTC 15:29Z) · 소요 47분

---

## 0. 첫 줄 — **조율자의 수와 갈린 것 다섯**

| 수 | **V 실측 [정본]** | 조율자 입력 | 갈림 |
|---|---|---|---|
| **48행 「누른 뒤」** | **31/48** (초록 31 · 빨강 2 · 회색 15) | 0/48 회색 | ★ **잤다 — 회색이 걷혔다** |
| **온보딩 48행** | **34.0/48** (70.8 % · 잰 48 · 회색 **0**) | 정본 24.5(잰 43) · 마지막 재측 29.0 | ★ **+5.0** |
| **FC** | **62.5** [잰 칸 **2/3**] | 60.4 [1/3] | ★ 칸이 하나 섰다(내 ④가 세웠다) |
| **PR** | **50.0** [잰 칸 1/4] | 48.1 [1/4] | +1.9 — 대장 kind 77.5/155 = 50.0 |
| **손 안 도달율** | **72.3 %** (99/137) | 「게이트 회차 필요 — 회색」 | ★ 쟀다 |

같은 수 넷: **상용 55.7** · **영역 ① 도달 18 · 가중 9.23 · closed 13** · **CR 18.8[8/8]** · **기능명세 포함 26.6 %(분모 291)**.

★ **분모부터 봤다**(D-513): 기능명세 분모 **291 = 절 155 + 별표 136** — 산출기가 대장의 분모 정본 줄에서 읽었고 등재 136·미등재 0 인데도 **291 그대로**다. 수가 26.6 **위로 안 올라갔다** — 사고 없음.

---

## 1. 첫 표 — 역할별 7줄

### ① 온보딩 48행 — **34.0/48 = 70.8 %** · 잰 행 **48/48** · 회색 **0**
`measure_onboarding_t.py`(gx-shell 안) · 실측 `2026-09-21T15:08:50Z` · 증거 `docs/agent/evidence/P-159/onboarding_measure_20260921T145138.json`

| 역할 | 점수 | 초록 | 반(◐) | 빨강 | 회색 |
|---|---|---|---|---|---|
| **U1** | **5.5/8** | 5 | 1 | 2 | 0 |
| **U2** | **3.5/8** | 3 | 1 | 4 | 0 |
| **U3** | **6.0/8** | 6 | 0 | 2 | 0 |
| **U4** | **5.0/8** | 4 | 2 | 2 | 0 |
| **U5** | **7.5/8** | 7 | 1 | 0 | 0 |
| **U6** | **6.5/8** | 6 | 1 | 1 | 0 |
| **합** | **34.0/48** | 31 | 6 | 11 | **0** |

★ **§9 불변 ③ 충족** — 여섯 역할 모두 **8/8 을 쟀다**. 0 인 역할 없음. 분모 48 은 `onboarding_48.md` 의 행 수이고 손으로 안 적었다(`--check` O: 문서 48 ↔ 도구 48).
⚠ 목표 **≥ 36 미달** (34.0).

**빨강 11** — U1#8(처리 단계 값 0칸) · U1#11(큐에 「확인·접수」 안 보임 — 초점 사건이 미처리가 아니면 그 단추가 없다 · 데이터 상태) · U2#2 · U2#3(등급 재판정 칸 0개) · U2#6(`/report-template` templates 200=False) · U2#16 · U3#2 · U3#14 · U4#1 · U4#15(「보고 표시」·「보고함」 둘 다 화면에 없다) · U6#4(웹훅 구독 422).

### ② 48행 「누른 뒤」 — **31/48** (초록 31 · 빨강 2 · 회색 15)
`verify_click_completes.py --measure`(호스트에서 — 소스에 `docker exec` 가 있다) · 실측 `2026-09-21T14:46:00Z` · 증거 `docs/agent/evidence/P-118/click_completes.json`

| U1 | U2 | U3 | U4 | U5 | U6 |
|---|---|---|---|---|---|
| **7/8** | **5/8** | **5/8** | **4/8** | **4/8** | **6/8** |

- **빨강 2 — 「요청은 나갔는데 값이 안 바뀐다」**: `U3#2`(서버가 `address` 에 아무것도 안 냈다) · `U6#4`(다시 읽었는데 `total` 그대로 1).
- 회색 15 는 전부 **이름을 적은 회색**이다(「이 도구가 누를 자리를 선언하지 않았다」 13 · 화면에서 못 찾음 2). 0 이 아니다.
- **설정 5칸은 48 밖의 표다** — 4/5(회색 1: `U4#S5` 무권한 계정이 **화면에 못 들어갔다**). 이 칸만 A 의 `12:19:30Z` 관측이고 나머지는 내 회차다. **대장은 안 줄였다.**
- ⚠ 목표 **≥ 36 미달** (31).

### ③ 영역 ① — 도달 **18** · 가중 **9.23** · closed **13**
`verify_feature_reach.py` — 초록 18 · **빨강 0** · 회색 14 · 잠김 7 (절 39) · 46.2 % → 가중 **9.23**
`verify_ga_readiness.py` — 절 18/39 = 46 % · **kind 13.00/39 = 33 %** → 영역 가중 6.67
★ Q 의 D-512 가 실제로 섰다: 이번 회차는 **환경이 살아 있어 빨강 0 · 회색 14** 로 갈렸고, 「돌지 못했다」는 한 건도 없었다.

★ **④가 12절을 그대로 올렸다** — `--clause-evidence` 가 절 **17 중 초록 16 · 회색 1**. 그중 **증거가 이번 회차(`14:46:00Z`)로 새로 선 절 12**:
`F-01-c2 · F-05-c1 · F-05-c2 · F-05-c3 · F-09-c2 · F-10-c3 · F-11-c2 · F-11-c3 · F-12-c2 · F-12-c3 · F-12-c8 · F-14-c1`
(`F-10-c1` 은 두 파일에 걸치고, `F-12-c5/c6/c7` 은 A 의 설정 회차 그대로.)
회색 1 = **`F-12-c1`** — `U4#S5` 가 `gxseed_u4_official` 로 **화면에 못 들어갔다**(/login 에 머문다). ★ 아래 3-② 가 그 사유다.

### ④ 세 수 + 기능명세 — `--aa` · `verify_spec_coverage.py` (둘 다 호스트)

| 수 | 값 | 잰 칸 | 못 잰 칸(회색) |
|---|---|---|---|
| **FC** | **62.5** (하한 41.7 · 상한 75.0) | **2/3** | ③ 계약 39절 도달 — `--static` 갈래라 게이트를 안 불렀다. ★ 같은 순간 내가 따로 잰 값은 **18/39 = 46.2 %** |
| **PR** | **50.0** (하한 35.0 · 상한 65.0) | **1/4** | 재부팅 아침 N/10(술어 없음) · 스테이징 6/6(`evidence/OPS-21` 없음 · 스테이징 0) · 창 2a·2b(`P-220/window_2ab.md` 없음 · 미집행) |
| **CR** | **18.8** | **8/8** | 없음 (하한=상한) |
| **기능명세 포함** | **26.6 %** (상한 73.4) | 분모 **291** = 절 155 + 별표 136 | — |

**★ CR 8칸 — 산출기가 낸 빨강 두 줄을 그대로 옮긴다**
```
X ★★ 표의 합이 제 행과 갈렸다 — WO-04 §4-1 이 적은 합 2.0/8, 여덟 행을 더하면 1.5/8 (-0.5).
   행이 정본이다 — 합은 행에서 나와야 하고, 안 나오는 합은 다시 셀 수 없는 수다(P-93).
   CR 은 25.0% 가 아니라 18.8% 다
X ★  격자 밖 — 문서가 적은 CR 합 2.20 는 이 셈법이 낼 수 있는 수가 아니다.
   8칸이 {0, 0.5, 1} 이면 합은 0.5의 배수뿐이다
```
칸값: 가격 숫자 4 **0** · 계량 씨앗 0 **0** · 실카메라 ≥1 **0** · 공개 주소+TLS **0** · 웹푸시 도달 **0.5** · SLA v1.0 **0.5** · 채널 계약서 **0.5** · 계약 11조 **0** = **1.5/8**.
⇒ **세종께: 반인 칸이 하나 더 있습니까. 없으면 정본은 18.8 %.** (N 전사본 `GX-CR_8칸_v1.0_20260921.md` 와 WO-04 §4-1 두 벌을 산출기가 다 읽고 대조했고 **둘은 같다** — 갈린 것은 문서의 **합 줄**이다.)

★ 그리고 **빨강 한 줄이 더 있다**: `CR 셈법 ①` — 문서 0.60 ≠ 영역 ⑧ 비율(상태 0.500 · 부류 0.400). **문서와 코드가 갈렸다.** 고치는 자리는 문서다(P-203 · 세종·조율자 자리).

### ⑤ 첫 10분 화면 12 (6 역할) — **잰 12/12 · 시험 장치가 보이는 화면 3/12**
내 회차 · gx-shell 안 · 로그인 4회(계정 전환마다 61초) · 증거 `docs/agent/evidence/P-220/turn-ab-v/*.png` + `result.json`(실측 `2026-09-21T15:23:59`)

| # | 역할 | 화면 | 첫 진입(KICK) | 시험 장치 |
|---|---|---|---|---|
| 01 | U1 | `/start` | **문구 O · `onboarding-kick` 1** | 0 |
| 02 | U1 | `/dsm/queue` | — | ★ **보인다** |
| 03 | U3 | `/start?role=MOBILE` (390px) | **O · 1** | 0 |
| 04 | U3 | `/m/inbox` (390px) | — | ★ **보인다** |
| 05 | U2 | `/start?role=MANAGER` | **O · 1** | 0 |
| 06 | U2 | `/dsm/dashboard` | — | ★ **보인다** |
| 07 | U4 | `/start?role=BOSS` | **O · 1** | 0 |
| 08 | U4 | `/dsm/events` | — | 0 |
| 09 | U5 | `/start?role=SYSOP` | **O · 1** | 0 |
| 10 | U5 | `/dsm/metering` | — | 0 |
| 11 | U6 | `/start` → 「외부 연계 개발자」 탭 | **O · 1** | 0 |
| 12 | U6 | `/dsm/integrations` | — | 0 |

**② 차선 K 가 넘긴 캡처 — 6 역할 첫 진입 6/6 섰다.** 여섯 장 전부에 `처음이세요 — 오늘 먼저 세 가지`(`KICK_HEADLINE`)와 `data-testid="onboarding-kick"` **1개**가 실재한다.
번들을 **다시 굽지 않았다** — 3002 이 내주는 `/app/_fe_dist/assets/Onboarding-DzhZ65Tk.js` 안에 그 문구가 있는 것을 **내가 직접 grep 으로 확인**했다(조율자 실측과 같다).

**시험 장치 셈 — 화면 3장 · 줄 단위로 센다**(글자 38은 낱말 겹침이 있어 정본이 아니다):
```
02 U1 「지금 처리할 것」   초점 카드 1 = [훈련] 안양천 시험카메라 1(훈련 배지 O)
                        대기 카드 11장 안에 시드 카메라 (검수용) · gxprobe-D384-screen 캡처용 카메라
06 U2 「관제 현황」 최근 이벤트 10줄 =
        [훈련] 안양천 시험카메라 1        2줄  ← 훈련 배지 O (제품이 세는 것이 옳다 · P-201)
        gxprobe-D384-screen 캡처용 카메라 4줄  ← 훈련 배지 O · 그러나 **이름이 게이트 탐침이다**
        시드 카메라 (검수용)              4줄  ← ★ **배지 0** · 실사건과 똑같이 보인다
04 U3 「내게 온 이벤트」 9건 =
        [훈련] 안양천 시험카메라 1        2건  ← ★ **배지 0**(모바일엔 배지 자리가 없다)
        시드 카메라 (검수용)              7건  ← ★ **배지 0**
```
⇒ **고객 첫 화면의 시험 장치 = 목표 0 에 대해 화면 3 · 줄 8~11.** 빨강.

---

## 2. ★ U1 이 남긴 빨강 — **눈으로 대 봤다. 참이다. 그리고 한 겹 더 있다**

### ㉠ 「시드(검수용)」 배지는 **영원히 안 뜬다** — 확인
- 사전: `copy.ts:234-240` `seed`·`seeded`·`demo` → 「시드(검수용)」 · `drill`·`training` → 「훈련」
- 서버: `apps/dsm/services.py::event_data_source` 는 `DATA_SOURCE`(=`drill`) 아니면 **`"live"`** 만 낸다. **셋째 낱말이 없다.**
- 화면(`06_U2_dashboard.png`): **시드 카메라 넉 줄에 배지가 한 개도 없다.** 실사건과 **구별되는 것은 카메라 표시 이름 「(검수용)」 넉 자뿐**이고, 그것은 제품이 하는 말이 아니라 **우리가 붙인 이름**이다.
- ⇒ **U1 의 빨강 그대로 둔다.** 「거르지 못하면 말하게 한다」가 **시드 자리에서는 아직 말하지 않는다**.

### ㉡ ★ 새로 잰 것 — **모바일에는 「훈련」이라고 말하는 자리가 아예 없다**
`dataSourceBadge` 를 부르는 화면은 **다섯**이다 — `ControlDashboard` · `DrillMode` · `EventDetail` · `EventList` · `FocusQueue`.
**`MobileInbox.tsx` 는 없다.** 그래서 `04_U3_inbox.png` 의 아홉 건에 배지가 **0** 이다.
지금은 손해가 안 보인다 — 그 두 건은 카메라 **이름**에 `[훈련]` 이 박혀 있어서 사람이 읽을 수 있다. 그러나 **이름에 그 글자가 없는 훈련 사건이 모바일로 가는 순간, 현장 사람은 훈련을 실재난으로 읽는다.**
⇒ **턴 AC 자리**(U3 축 · `MobileInbox.tsx` 한 줄). 제품을 안 고쳤다 — 적어서 넘긴다.

---

## 3. ★★ 이 회차에 새로 잰 것 — **우리는 고객의 로캘로 한 번도 안 쟀다**

### ① 무슨 일이 있었나
첫 캡처 회차에서 **로그인 4/4 가 전부 실패**했다(`ok=False`). 같은 순간 같은 자격(`sha256[:12] de08e1f17eb3` · 길이 24)으로 온보딩 측정기는 **6/6 들어갔다.**

### ② 격리 A/B 로 가렸다 — **가설을 기각하고 적는다**
로그인 코드를 **온보딩 측정기의 것으로 바꾸고**(`from measure_onboarding_t import login`) 로캘만 남겨 한 번 더 쟀다:
```
A  new_context(..., locale="ko-KR")   ok=False  posts=[200]      profile=False  → /login 에 머문다
B  new_context(...)            (기본) ok=True   posts=[200,200]  profile=True   → 들어간다
```
그 다음 **짐작을 관측으로 바꿨다** — ko-KR 로 누른 뒤 화면에 실제로 뜬 단추를 적었다:
```
[AB2] 로그인 화면 단추: ['', '로그인']
[AB2] 누른 뒤 단추:    ['', '로그인', '', '확인', '취소']
[AB2] 본문: … 세션 종료 / 현재 이 계정은 다른 기기에서 사용 중입니다. … / 확인 / 취소
```

### ③ 판정 — **제품은 옳고, 판정기가 틀렸다**
`measure_onboarding_t.login()`(그리고 그것을 베낀 자리들)은 「다른 기기」 확인창을 **`Confirm` 이라는 영어 이름으로만** 찾는다. 브라우저 로캘이 **ko-KR 이면 그 단추 이름은 「확인」**이라 **영원히 못 누르고**, 로그인이 안 끝나 그 회차가 **통째로 회색**이 된다.
★ **우리 고객의 브라우저는 대부분 ko-KR 이다. 그런데 우리 게이트는 지금까지 영어 로캘로만 쟀다.**
★ 이것이 `F-12-c1`(`U4#S5` · 「그 사람으로 화면에 들어가지 못했다」)과 **같은 모양**일 수 있다 — 다만 그 회차는 A 의 것이고 내가 로캘을 못 봤으므로 **잇지 않는다. 회색이다.**
⇒ 고칠 자리 한 줄: `scripts/measure_onboarding_t.py::login` 의 `get_by_role("button", name="Confirm")` 에 **「확인」을 같이 받게** 한다(차선 Q 소유 · P-203 으로 조율자·V 는 안 고친다).

---

## 4. 3종 분류 · 나머지 수

```
[GA] 분류 57건 — 설계 잠금 1(분모에서 뺀다) · 손 밖 17 · 손 안 39
[GA] 손 안 도달율 72.3% = 구현 99 / (155 − 잠금 1 − 손 밖 17 = 137)
[GA] 「초록」 부류 155절 — closed 71 · ratchet 3 · rule_only 3 · unmeasurable 74 · gate_only 4
[GA] 상용 오픈 가중 합계 55.7%   (옛 식 「구현/전체」로 세면 70.2% — 거리 14.5)
[GA] 별표 ② 기능명세 136 등재 · kind 표 정본 291
[GA] 대장↔게이트 27절 · 게이트 26벌 — 색이 같다 23 · **못 쟀다 4**  → rc 2 (회색)
        SEC-17(`--api` 안 줌) · OPS-13a · PERF-04 · LAW-08(`--db` 안 줌)
```
8영역: ①46%(kind 33) ②80 ③100 ④67 ⑤71 ⑥92 ⑦62 ⑧50.

⚠ `verify_readiness_scores.py --report` 는 `verify_ga_readiness.py` 를 **`--static` 으로** 부른다 — 그 갈래의 「영역 ① 0/39 · 손 안 59.1 %」는 **0 이 아니라 못 잰 것**이다. **정본은 게이트를 부른 쪽**(도달 18 · 72.3 %)이다.

**MEASURED N/80 은 내가 안 쟀다 — 회색**(조율자 69/80 은 입력으로 남긴다).

---

## 5. 상태를 바꾼 자리 — **되돌린 것과 못 되돌린 것**

| 자리 | 무엇이 남았나 | 되돌렸나 |
|---|---|---|
| **U5#1** 사용자 생성 | `gxprobe_onb_20260921T145138` **id=226 · `is_active=True`** · 사용자 수 118 → **119** | ★ **못 되돌렸다.** 측정기가 비활성화를 시도했고 실패했다. ⚠ **지난 회차 넷(118·119·224·225)은 전부 `is_active=False`** 로 되돌아가 있다 — 이번만 실패했다 |
| **U5#4** 카메라 일괄 등록 | 카메라 수 15 → **16**(테넌트 면) · 전체 52 | 되돌리는 문 없음 — 측정기가 안 지운다 |
| **U5#5** 주소 채우기 | 미입력 6 → **5** | 같음 |
| **U5#14** 재시작 요청 | 요청 행 4 → **5** | **없다 — 기록을 지우는 문이 제품에 없고 그것이 옳다** |
| U5#9 알림 규칙 · U3#16 차단 시간대 · U6#1 API 키 | — | **되돌렸다**(켜기 8 복구 · 원래 값 복구 · `DELETE api-keys/70 200`) |
| `verify_click_completes` 되돌림 | 선언 1자리 | 되돌림 1 · 못 되돌림 0 |

★ **나는 손으로 안 지웠다**(삭제 0 · 규약). 그리고 이것이 **보고 §6 ⚠ 「발급 순간 배선 다섯 자리」의 실물**이다 —
**이 회차가 만든 계정 1 · 카메라 1 이 지금 청구 면에 들어 있다.** 표식이 안 달려 있으면 다음 청구 회차가 그것을 센다.
⇒ 조율자·차선 B 자리.

---

## 6. 못 잰 것 (회색 · 0 이 아니다)

- **MEASURED N/80** — 안 불렀다.
- **게이트 4벌**(SEC-17 · OPS-13a · PERF-04 · LAW-08) — GA 가 부를 때 인자를 안 줘서 회색. 나도 따로 안 줬다.
- **`F-12-c1`** — A 의 회차에서 u4 가 화면에 못 들어갔다. 로캘 때문인지 **내가 안 갈랐다**.
- **`/start` 의 「외부 연계 개발자」 탭은 질의로 못 연다** — `ROLE_TO_CHAPTER` 에 `partner` 열쇠가 없다(`Onboarding.tsx:259`). 나는 **탭을 눌러** 찍었다. 결함이라기보다 **딥링크가 여섯 중 다섯만 된다**는 사실이고, 적어서 넘긴다.
- 편리성 8지표 — **안 쟀다**(시간이 창을 넘었다). 회색.

---

## 7. 규율

로그인 **16회** — 온보딩 측정기 6(U1·U2·U3·U4·U5·U6 탐침 + 관리자 토큰) · 캡처 1차 4(전부 실패) · 캡처 2차 4 · A/B 2.
(`verify_feature_reach`·`verify_ga_readiness` 가 게이트 안에서 부른 로그인은 내가 세지 않았다 — 회색.)
계정 전환마다 **61초** 지켰다. **서버를 세우거나 재기동한 일 0** · 도커 엔진 **안 죽었다**(10/10 그대로) · 번들 **안 구웠다** ·
**삭제 0** · **커밋 0** · **제품 파일 0줄** · §0.4 금지구역 **읽기만** · 비밀 출력 **0**(이름 · 길이 24 · `sha256[:12] de08e1f17eb3` 까지).

**내가 쓴 파일** — 전부 증거다:
```
M  docs/agent/evidence/P-106/feature_reach.json          (판정기가 썼다)
M  docs/agent/evidence/P-118/click_completes.json        (판정기가 썼다)
?? docs/agent/evidence/P-159/onboarding_measure_20260921T145138.json
?? docs/agent/evidence/P-220/turn-ab-v/                  (PNG 12 + result.json)
```
그 밖에 스크래치패드의 임시 스크립트 셋(`v_first10.py`·`ab_locale.py`·`ab_locale2.py`)은 **저장소 밖**이고 컨테이너 `/tmp` 에 있다.

---

## 8. 조율자에게 — 순서대로

1. ★ **`python scripts/v_lock.py --unlock`** — 내가 안 풀었다(규약: 조율자가 지운다). **안 풀면 다음 게이트가 전부 회색**이 된다.
2. ★★ **판정기 한 줄** — `measure_onboarding_t.login()` 이 「확인」을 못 누른다(§3). **고객 로캘로 재 본 적이 없다.**
3. ★ **`MobileInbox.tsx` 에 훈련 배지가 없다**(§2-㉡) — U3 축.
4. **남은 상태 셋**(계정 226 · 카메라 +1 · 재시작 요청 행) — 청구 표식 자리(§5).
5. **CR 합 줄**(2.0 ↔ 1.5)과 **CR 셈법 ① 영역 ⑧ 비율**(0.60 ↔ 0.500/0.400) — 문서를 고칠 자리.
