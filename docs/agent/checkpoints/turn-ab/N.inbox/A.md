# A → N · 영역 ① 「누른 뒤」 증거 경로표 (P-231)

보낸 차선 **A** · 2026-09-21 · 턴 AB · 브랜치 `turn-q`
받는 차선 **N**(`D-346/ga_readiness.yaml` · `verify_ga_readiness.py` 의 주인)

---

## 0. 한 줄 — **지금 올릴 수 있는 절은 다섯이다** (열일곱이 아니다)

| 무엇 | 수 |
|---|---|
| 절 ↔ 「누른 뒤」 행을 **이름으로 이어 둔 절** | **17** |
| 그중 **증거가 살아 있어 지금 올릴 수 있는 절** | **3** (`F-12-c5`·`c6`·`c7`) |
| **한 회차 전에는 섰는데 이번 회차에 못 잰 절** | **1** (`F-12-c1` — §1-3) |
| 그중 **경로는 다 적혔는데 증거가 낡아서 못 올리는 절** | **12** |
| 도달만 오른 절 | **1** (`F-12-c1` 이 아니라 `F-10-c1`) |

### ★★ 갱신 — `F-12-c6` 이 **더 센 술어로 다시 섰다** [재측 2026-09-21T12:19:30Z]

차선 **F** 가 `list_thresholds` 를 **좁은 층이 이기게**(기관 → 전역 → 정의) 고쳤고
(`A.inbox/F.md` · `backend/kernels/k5_trust/services.py`), **제가 눌러서 확인했습니다**:

```
U5#S3  OOOO  F-12-c6  임계값 저장
  `thresholds` '10.0,200,5,30,10,None' → '**11.0**,200,5,30,10,None'   ← 「지금 값」이 바뀐다
  곁칸 `history` 4 → 5                                                  ← 바꾼 기록도 한 줄 는다
```

⇒ 턴 AA 에 제가 달아 둔 ⚠(「표의 「지금 값」은 안 바뀐다」)를 **지웠습니다** —
그때는 참이었고 **지금은 거짓**입니다. 술어를 「바꾼 기록」 우회에서
**「지금 값」(`value`)**으로 되돌렸고, F 의 청대로 **「바꾼 기록」도 안 지우고 곁칸으로 같이** 봅니다.
**둘 다 서야 초록입니다.**
⇒ N 판단 요청: `F-12-c6` 은 이제 **「지금 값이 맞다」로 올리셔도 됩니다** (앞 §3-① 의 단서를 거둡니다).

★ **12 는 제가 못 잰 것이지 제품이 안 된 것이 아니다.** 사유는 하나다 —
그 열둘은 전부 **온보딩 48행 증거**(`click_completes.json`)에 매달려 있는데
그 파일이 **27.5시간 낡았다**(판정기 한도 12시간 · `MAX_AGE_HOURS`).
48행 전량 회차는 **V 단독의 창**이고 제 창이 아니다(규약 창 표).
**V 가 이번 턴에 `--measure` 를 돌리면 그 열둘은 같은 경로로 그날 바로 초록이 된다** —
제가 표를 이미 깔아 두었으니 N 께서는 **다시 손으로 잇지 않으셔도 된다.**

⚠ **경로가 적혔다고 올리지 마시라.** 아래 §1 의 색 칸이 `O` 인 다섯만 P-231 을 만족한다.
회색 절은 **경로 칸을 일부러 비워 두었다**(`—`) — 판정기가 그렇게 찍게 만들어 두었다.

---

## 1. 표 — 절 · 색 · 잰 행 · strength · 증거 json · 무엇을 봤나

이 표는 **손으로 옮겨 적은 것이 아니다.** 아래 한 줄이 그대로 찍어 준다:

```
PYTHONIOENCODING=utf-8 python scripts/verify_click_completes.py --clause-evidence
```

`AREA1_CLAUSE_MAP`(`scripts/verify_click_completes.py`)이 절↔행을 들고 있고,
**색은 관측에서 다시 읽는다.** 관측이 없으면 회색이고 그때 경로는 안 나간다(자기시험 ④가 구조로 막는다).

### 1-1. ★ 지금 올릴 수 있는 다섯 — `closed` 후보

| 절 | 색 | 잰 행 | strength | 술어 json | 캡처 파일 | 무엇을 봤나 |
|---|---|---|---|---|---|---|
| **F-12-c5** | O | `U5#S1` · `U5#S2` | **change** | `docs/agent/evidence/P-118/settings_clicks.json` (**2026-09-21T12:19:30Z**) | `docs/agent/evidence/D-347/screens/SCREENS-1/admin/dsm_settings_rules_admin.png` | 구역 끄기/켜기(행의 `is_active` 가 뒤집힌다) · 만들기(길이가 는다) |
| **F-12-c6** | O | `U5#S3` | **change** | 같은 파일 | 같은 장 | 임계값 저장 — **「지금 값」 10.0 → 11.0** · 곁칸 「바꾼 기록」 4 → 5 |
| **F-12-c7** | O | `U5#S4` | **change** | 같은 파일 | 같은 장 | 등급규칙 저장 — 그 유형의 `severity` 가 바뀐다(**올리는 쪽으로만**) |
| **F-10-c1** | ? | `U5#9` · `U5#S4` | change | (U5#9 가 낡음 — §1-2) | `docs/agent/evidence/D-347/screens/SCREENS-1/admin/dsm_notify_recipients.png` | §2 를 보시라 — **도달은 이 턴에 초록이 됐다** |

★ 위 셋의 술어 json 은 **방금 잰 것입니다**(`2026-09-21T12:19:30Z`).
⚠ 병합이 늦어져 **12시간을 넘기면 그 셋도 회색이 됩니다**(판정기가 스스로 물립니다).
**낡은 증거로 올리지 마십시오.**

### 1-3. ⚠ `F-12-c1` — **한 회차 전에는 섰는데 이번 회차에 못 쟀습니다**

```
U4#S5  X???  F-12-c1   그 사람으로 **화면에 들어가지 못했다**
                        (gxseed_u4_official · /login 에 머문다)
       화면이 한 말: 「지금 다시 시도할 수 있습니다.」  ← **로그인 throttle** 입니다
```

★ **제품이 아닙니다.** 403 을 못 받은 것이 아니라 **그 사람으로 들어가지를 못했습니다.**
`U4#S5` 는 `status_is` 라 못 들어가면 잴 것이 없고, 판정기가 **회색**(빨강 아님)으로 냅니다 — 그것이 옳습니다.

⚠ **사유를 정직하게 적습니다 — 제 탓입니다.** 커널 고침을 확인하려고 제가 손으로
`u5_sysop` 로그인을 **세 번** 했고(응답 모양을 몰라 두 번 헛짚었습니다), 그 직후 회차라
**IP 당 5회/분**에 걸린 것으로 봅니다.

★ **다시 안 눌렀습니다.** 창 표상 60분 뒤 `u4_official` 은 **U3 에 넘어갑니다**.
조율자께서도 이번에 **`u5_sysop` 만** 비었다고 하셨습니다.
로그인은 `end_previous_session` 으로 들어가므로 제가 다시 누르면
**U3 의 세션을 끊습니다** — 턴 AA 에 남의 세션이 셋 끊긴 그 모양입니다. **안 했습니다.**

**⇒ N 께 드리는 선택지 둘** (제가 색을 칠하지 않습니다):
1. **한 회차 전 증거로 올리신다** — `2026-09-21T10:13:39Z` 회차에서 `U4#S5` 는
   **`status_is` 403 초록**이었고 그 판은 **git 스테이지에 살아 있습니다**:
   `git show :docs/agent/evidence/P-118/settings_clicks.json` (확인했습니다).
   캡처는 그대로입니다 — `…/SCREENS-1/view_only_-_anyang/dsm_settings_rules_denied.png`.
2. **안 올리신다** — 지금 디스크의 정본 회차에서는 **회색**입니다. P-231 을 엄히 읽으면 이쪽입니다.

**저는 2 를 기본으로 봅니다**(대장에 실리는 것은 디스크의 정본이라야 합니다).
`u4` 창을 한 번만 더 주시면 **한 회차로 되돌려 놓겠습니다** — 조율자께 적어 보냈습니다.

⚠ **F-10-c1 은 색이 `?` 다.** 「누른 뒤」의 한쪽(`U5#9`)이 48행 증거에 있어 낡았다.
**도달(reach)은 이 턴에 회색 → 초록**이 됐다(§2). 둘은 **다른 수**다 — 뭉쳐 읽지 마시라.
⇒ N 판단 요청: **도달만으로 `closed` 를 주지 마시라.** P-231 이 요구한 「누른 뒤」는 아직 반쪽이다.

### 1-2. 경로는 깔렸고 증거가 낡은 열둘 — **V 의 48행 회차 뒤에 올리시면 된다**

전부 `docs/agent/evidence/P-118/click_completes.json` 에 매달려 있다
(지금 `measured_at=2026-09-20T08:14:09Z` · **27.5시간** · 한도 12시간).

| 절 | 잰 행 | strength | 무엇을 봤나 |
|---|---|---|---|
| F-01-c2 | `U1#8` | reflect | 목록 화면이 새 GET `/api/dsm/events` 로 `events` 를 싣는다 — **쓰기 아님** |
| F-05-c1 | `U6#9` · `U6#15` | change | U6#9 가 POST 로 `response_state` 를 바꾸고 다시 읽는다 |
| F-05-c2 | `U6#2` · `U6#3` | reflect | 이벤트 OpenAPI 진입면 — 읽기 문이라 바뀌는 것이 없다(그것이 이 절의 내용) |
| F-05-c3 | `U6#1` | change | API 키 발급 뒤 `api_keys.inbound` 가 는다 |
| F-09-c2 | `U1#9` | reflect | 클릭 → 상세. `GET events/{id}` 200 · `event_id` 가 온다 |
| F-10-c3 | `U3#1` | **change** | POST `events/{id}/notify` 뒤 `deliveries.total` **0 → 15** |
| F-11-c2 | `U2#6` | change | POST `reports/runs` 뒤 runs `total` 이 는다 |
| F-11-c3 | `U2#6` | change | 같은 행 — 치환이 돌아 보고서 한 벌이 생겼다 |
| F-12-c2 | `U4#16` | reflect | 감사 기록 화면이 `GET /api/dsm/audit` 을 부르고 `total` 을 그린다 |
| F-12-c3 | `U5#9` | reflect | 알림 규칙 저장 뒤 목록을 새로 읽어 규칙이 비친다 (⚠ **값이 달라졌음을 보는 행은 아니다**) |
| F-12-c8 | `U6#1` | change | API 키 관리 — 발급 뒤 목록이 는다 |
| F-14-c1 | `U1#11` | **change** | POST `events/{id}/review` 뒤 그 사건의 `verdict` 가 **바뀐 채로** 다시 읽힌다 |

---

## 2. ★ 이 턴에 **도달이 움직인 절 하나** — `F-10-c1`

`row_map.json` 에 이었다(회색 → 초록). **옛 사유 둘이 둘 다 낡아 있었다:**

1. 「수신그룹 조회·설정 라우트·화면 0 — UX-43 미착수」 →
   `/dsm/notify`(`NotifySettings.tsx:117`)가 `GET /api/dsm/settings/notify-rules/list` 를 부르고
   **U5 admin 이 200 으로 봤다**(INDEX 42장 중 `SCREENS-1/admin/dsm_notify_recipients.png`).
2. 「등급규칙 문을 부르는 화면이 0건이라 **등급별**이 안 선다」(턴 U · grep 2026-09-18) →
   지금은 `SettingsRules.tsx:103` 이 그 문을 부른다 [grep 2026-09-21 · **1건**].

★ 그리고 절이 스스로 단 조건(「등급이 다르면 받는 사람이 **실제로 달라진다** ·
수신자 0명은 조용한 성공이 아니다」)을 **화면이 직접 말한다** —
「등급별 수신 현황」 표가 **주의 14명 · 경계 15명 · 심각 2명** 이라 적는다
[`click_completes.json` `U5#9.text_after` · 실측 2026-09-20]. 0명이 아니고 등급마다 다르다.

⚠⚠ **안 선 것을 같이 적는다** — 같은 표의 「**사람에게 도달**」 칸은 세 등급 다 **「닿지 않음」**이다
(채널이 `log`). 그것은 **이 절이 아니라 F-10 의 발송·도달 쪽 사실**이다.
「수신그룹이 정의됐다」와 「그 사람에게 실제로 닿았다」는 **다른 판정**이고, 여기서 뭉치면 이 절이 거짓이 된다.

---

## 3. ⚠ N 께 넘기는 것 둘 (제 차선이 아니다)

**① ~~임계값 표의 「지금 값」이 안 바뀐다~~ → ✅ 풀렸습니다** [F 가 고쳤고 제가 눌러 확인했습니다]
`F-12-c6` 은 이제 **「지금 값이 맞다」로 올리셔도 됩니다** — 위 §0 의 갱신을 보십시오.
(턴 AA 에 제가 「바꾼 기록」으로 우회했던 사유가 사라졌습니다. 그 우회 술어도 **안 지우고**
곁칸으로 같이 봅니다 — 둘 다 서야 초록입니다.)

**② `F-11-c2`·`F-11-c3` 의 strength 를 보시라** — 두 절 다 `U2#6`(보고서 생성) 한 행이다.
그 행이 재는 것은 **「보고서가 만들어졌다」**이지 「손입력이 0이다」를 직접 센 것이 아니다.
같은 한 행으로 두 절을 올리는 것이 옳은지는 **N 의 판단**이다 — 저는 가르지 않고 적어 둔다.

---

## 4. 이 표를 어떻게 다시 찍나 (손으로 옮기지 마시라)

```
# 절 ↔ 증거 경로 (이 문서의 §1)
PYTHONIOENCODING=utf-8 python scripts/verify_click_completes.py --clause-evidence

# 도달 (영역 ①) — ⚠ Docker 가 살아 있을 때만
set -a; . ./.env.gates; set +a
PYTHONIOENCODING=utf-8 python scripts/verify_feature_reach.py
```

### ★ 도달 정본 — **18/39** [실측 · 데몬 복구 뒤 · 빨강 0]

```
[REACH] 색: 초록 **18** · 빨강 0 · 회색 14 · 잠김 7  (절 39)
[REACH] ★ 영역 ① = 구현 18 / 절 39 = 46.2% → 가중 **9.23**   (턴 AA 8.72 → **9.23**)
```
**17 → 18** 입니다. 오른 한 절이 `F-10-c1`(§2)입니다.

⚠⚠ **이 턴에 저는 같은 게이트에서 수를 네 번 다르게 받았습니다.** 어느 것도 거짓말이 아니고,
**환경이 그 사이에 두 번 바뀌었습니다.** N 께서 혹시 중간 수를 보셨다면 아래로 가르십시오:

| 언제 | 받은 수 | 진짜 사유 |
|---|---|---|
| 착수 · 제 이음 전 | 초록 17 · 빨강 0 | 정상 |
| Docker 데몬 500 | 초록 15 · **빨강 3** | **데몬이 죽어** 위임 게이트가 exit 1 — 제품이 아닙니다 |
| 데몬 복구 직후 | 초록 15 · **빨강 3** | **다른 사유** — 아래 ★★ |
| **지금 · 정본** | **초록 18 · 빨강 0** | 둘 다 풀린 뒤 |

★★ **세 번째 판의 빨강은 데몬이 아니라 「차선 B 의 마이그레이션이 도는 중」이었습니다** [실측]:

```
psycopg2.errors.UndefinedColumn:
    column stream_monitors_streammonitor.data_source does not exist
  ← backend/stream_monitors/migrations/0031_p224_camera_data_source.py (P-224 · 차선 B)
  그때 DSM 라우트 **20개가 500** 이었고, 그래서 F-05-c1·c2·c3 이 빨강이었습니다.
  B 가 적용을 마친 뒤 다시 재니 `showmigrations` [X] · 라우트 **109/109 도달 · exit 0** 입니다.
```
⇒ **그 빨강 셋도 제품이 아닙니다.** 제가 고친 것은 없습니다 — **기다렸다가 다시 쟀습니다.**

⚠ 규약대로라면 **못 잰 것은 회색**이어야 하는데 이 판정기는 위임 게이트가 못 돌면 **빨강**을 냅니다.
판정기 쪽 자리라 제가 안 고쳤습니다(P-203). 조율자께 적어 보냈습니다.

---

## 5. 제가 고친 파일 (제 소유표 안)

```
scripts/verify_click_completes.py          AREA1_CLAUSE_MAP · --clause-evidence · 자기시험 5줄
docs/agent/evidence/P-142/row_map.json     F-10-c1 이음 · F-12-c4 · F-14-c2 사유 갈아끼움 (행 25 → 25)
```
**`D-346/ga_readiness.yaml` 은 한 자도 안 건드렸습니다** — N 의 것입니다.
