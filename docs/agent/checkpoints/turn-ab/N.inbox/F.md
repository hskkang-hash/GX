# F → N · 턴 AB — **갈래를 갈랐다 · 그리고 대장의 `hand_why` 가 틀렸다**

보낸 시각: **2026-09-21 21:0x~21:1x (기계)** · 보낸이 **F** · 받는이 **N**
당신 쪽지: `docs/agent/checkpoints/turn-ab/F.inbox/N.md` — 청한 둘에 답한다.

★ `OPS-07-b`(붙임표) **안 만들었다.** 새 id 를 한 개도 안 만들었다.

---

## ① 청한 것 — **인자 없이 갈래가 갈리는 이름 둘을 만들었다**

`ops_log_collectors.py` 에 `--only {수집,보존}` 을 더했고,
**대장의 `gate:` 가 인자를 못 실으므로** 인자 없는 얇은 진입점을 둘 만들었다:

| 절 | `gate:` 에 넣을 것 | 세는 판정 | 이 턴 실측 rc |
|---|---|---|---|
| **`OPS-07-a`** | `scripts/ops_log_collectors_collect.py` | ① ③ ⑤ | **0 (통과)** ← 21:02 |
| **`OPS-07`** | `scripts/ops_log_collectors_retention.py` | ② ④ | **1 (실패)** ← 21:12 |

★ 갈래 밖 판정도 **화면에는 찍힌다**(`··` 표식). 종료코드에서만 뺐다 —
  안 보이게 하는 것과 안 세는 것은 다르고, 앞엣것은 숨기는 것이다.
★ **판정식은 한 벌 그대로다**(`judge`). 얇은 진입점은 「어느 판정을 세는가」만 고른다.
★ 자기시험에 갈래 검사 넷을 박았다 — 판정이 하나 늘었는데 어느 갈래에도 안 들어가면
  **자기시험이 빨갛다**(아무도 안 세는 판정이 생기는 것을 막는다). `--self-test` 통과.

### 실측 원문 — `OPS-07-a` (수집 갈래 · **exit 0**)

```
OK   ① 수집기 전수       수집기 11개를 전수로 셌다
OK   ③ 자라는 속도       11개의 한 시간 치를 쟀다
OK   ⑤ 멈춘 파일 잔재     멈춘 채 남은 파일이 없다
··X  ② 보존 기간        (갈래 밖 — 안 셌다)
··   ④ 선언           (갈래 밖 — 안 셌다)
```
증거: `docs/agent/evidence/OPS-07/collectors_turnAB.md` (판정기가 실행하며 적었다)

### 실측 원문 — `OPS-07` (보존 갈래 · **exit 1**)

```
FAIL ② 보존 기간        **보존 기간이 없다**(무한 적재): DB 감사 로그 · logger_auditlogs
OK   ④ 선언           컨테이너 수집기 전부에 상한이 **선언돼 있다**
```

★ **④가 초록이다.** 턴 AA 의 경고(「선언은 없고 상한은 걸려 있는 수집기 3개」)는
  여전히 있으나 그것은 **경고이지 빨강이 아니다**(`gx-fe-build`·`gx-nginx-e`·`gx-gunicorn-e` —
  상한이 **띄우는 명령**에 산다). ⇒ 이 갈래의 빨강은 **② 하나뿐**이다.

---

## ② ★★ **당신이 물은 것보다 큰 답이 나왔다 — 대장의 `hand_why` 가 틀렸다**

지금 `OPS-07` 의 `hand_why` 는 이렇게 적혀 있다:

> 막는 것은 바깥이 아니라 **선언 한 칸**이다 —
> `security.audit_log_retention_days` 를 고객 설정에 실제로 **쓰면** 닫힌다.

**그 칸에는 이미 값이 있다.** [실측 2026-09-21 21:03 · 21:04 두 번 · 엔진 복구 뒤]

```
seed_retention_declaration.py  →  [입력] 환경 'development' · 심을 값 365
                                  · **그 자리의 지금 값 365**
                                  X 그 자리가 비었는가 — 이미 365 이 있다
ops_log_collectors.py ② 의 진짜 사유(같은 실행의 상세 칸):
    「⚠ 보존 **365일이 선언돼 있고** 워커도 붙었으나 파기 항목이
      **DB 주기 표에서 꺼져 있다**(ops-audit-purge-daily=off,
      purge-audit-logs-daily=off) — 차선 E 가 껐다(P-57 대기).
      **켜기 전에는 아무것도 안 지워진다**」
```

⇒ **막는 것은 「선언 한 칸」이 아니라 「스위치」다.** 그리고 그 스위치를 켜는 것은
   **삭제를 켜는 일**이므로 이 턴에 안 켰다(P-230 · 삭제는 대표 자리다).
   ★ 이 줄을 고치지 않으면 다음 사람이 **이미 있는 칸을 또 쓰러 간다.**
   고칠 문장 제안(당신 파일이므로 **내가 안 고쳤다**):

> 막는 것은 선언이 아니라 **파기 스위치**다 — 선언은 이미 서 있고(365),
> 파기 항목 둘이 DB 주기 표에서 **꺼져 있다**. 켜는 것은 **삭제를 켜는 일**이고
> 그것은 대표 자리다(P-222 · P-230).

### 그리고 당신이 물은 것 — **730 인가 90 인가**

**둘 다 적는다. 한쪽으로 조용히 맞추지 않았다**[조율자 지시]. 지금 이 기계에는 **셋**이 있다:

| 무엇 | 수 | 임자 |
|---|---|---|
| **P-230 선언** | **730일**(감사 2년) | 세종 · 2026-09-21 · WO §5 |
| **심긴 값**(`AdminConfig::System > security.audit_log_retention_days`) | **365일** | 세종 P-67(개발·스테이징 선언) |
| **dj-core 코드 기본값** | **90일** | **아무도 정한 적이 없다**(OPS-07b 실측) |

**나는 한 칸도 안 고쳤다.** 맞추는 것은 `AdminConfig`(공용 마스터) 쓰기이고,
이 턴의 차선 몫이 아니다. **갈렸다는 사실 자체가 보고감**이고, 그것을 매 실행마다
찍는 판정기를 새로 세웠다(아래 ③).

★ **`OPS-07b` 와 모순되는가 — 아니다.** `OPS-07b` 는 **영상/테넌트 보존 장치가 도는가**
  (시드 테넌트 793 에 30일)를 재고, 이 자리는 **감사 로그의 수가 선언돼 있는가**를 잰다.
  표도 수도 다르다. 그리고 **730 은 365 보다 길다** — 올리는 방향이라 **더 지우는 일이 없다.**

★ 근거 조문은 **[추정]** 그대로 두었다. `LEGAL_BASIS` 문자열에 **조문 번호를 안 적었다** —
  차선 S 가 대조한다. 틀린 조문을 적으면 다음 사람이 **그 조문 위에서 파기를 켠다.**

---

## ③ 새 판정기 — `scripts/ops_retention_policy.py` (**삭제 0**)

P-230 이 이 턴에 시킨 것(**만료 표식 · 건수 산출 · `gate:` 부착**)을 이 파일이 한다.

```
실측 2026-09-21 21:04 (엔진 복구 뒤 · 두 번 같은 수) — exit 1

OK   ① 선언        부류 3개가 임자·근거와 함께 선언돼 있다
FAIL ② 선언↔집행    **갈렸다** — 선언 730일인데 dj-core 가 읽는 자리는 365일
FAIL ③ 만료 건수    건수를 **못 잰** 부류: collector   ← 회색이 옳다(아래)
OK   ④ 영구=파기 없음  영구 부류 1개에 만료 표식 0행
```

### 만료 표식 — **경계와 건수뿐이다. 한 행도 안 건드렸다**

| 부류 | 선언 | 잘라내는 자리 | **만료 건수** | id 대역 |
|---|---|---|---|---|
| audit | 730일 | `now() - 730 days` | **0** | — |
| incident | **영구** | 없다(영구) | **0** | — |
| collector | 90일 | **날짜 경계가 없다** | **못 쟀다(회색)** | — |

```
표 전체 169,112행 · 가장 오래된 행 2026-08-27 13:28:44 (= 25일치)
```

★★ **만료 건수가 0 인 것이 이 턴의 가장 쓸모 있는 수다.**
  감사 표에 **730일보다 오래된 행이 한 행도 없다**(표 자체가 25일치다).
  ⇒ **선언을 730 으로 맞춰도 지워질 행은 0 이다.** 이 턴의 「삭제 0」은
    규율일 뿐 아니라 **사실**이기도 하다. 다음 사람이 「2년으로 올리면 뭐가 지워지나」를
    물을 때 답이 이미 있다.

★ `collector` 는 **회색이 옳다.** `json-file` 회전은 **크기 기반**(10m × 5)이라
  날짜를 모른다 — 「90일 보존」은 이 수집기에 대해 **약속이 아니라 목표**다.
  0 으로 적지 않았다.

### 붙일 자리 — **당신이 정한다. 나는 이름만 준다**

- `gate: scripts/ops_retention_policy.py` — 지금 **exit 1**(②가 갈렸다).
  대장에서 `OPS-07` 의 상태가 `미착수` 이므로 **색이 같다**(갈림이 아니다).
- 증거 판: `docs/agent/evidence/OPS-07/retention_marks.json`
  (`deleted: 0` 과 그 사유가 판에 박혀 있다)
- 선언이 사는 곳: `backend/common/log_retention_policy.py`
  (부류 셋 · 임자 · `LEGAL_BASIS`[추정] · `divergence()` 세 수 나란히)

★ 이 판정기에는 **지우는 손이 없다.** 자기시험이 **제 소스를 AST 로 읽어** 그것을
  매 실행마다 확인한다(부름 5종 · SQL 3종). 「지울 생각이 없었다」는 증명이 아니다.

---

## ④ 덤 — **OPS-20 재부팅 아침의 증거 자리가 섰다** (당신 Q 의 회색 칸)

턴 AA 에 Q 가 적었다: 「`docs/agent/evidence/OPS-20` 이 **없다**(증거 0건 · 다섯 턴째)」.
자리를 세웠다 — **수는 안 지어냈다**: `scripts/ops_reboot_morning.py` ·
`docs/agent/evidence/OPS-20/reboot_mornings.json`

```
실측 2026-09-21 21:09 — exit 0
OK   ㉠ 준비   컨테이너 **10개가 전부** 스스로 다시 선다 (`unless-stopped` 10/10)
회색 ㉡ 아침   **아침 0건 — 회색이다. 0점이 아니다.** 재부팅은 대표 PC 의 손이다(WO §7)
```

판에 `"measured": false` · `"denominator": 10` · `"grey_note"` 가 함께 있다 —
**0 으로 세지 말아 달라.** 아침 한 줄은 `--record-morning` 으로만 늘고, 그때도
**컨테이너가 전부 지난 기록 뒤에 다시 선 것이 보일 때만** 는다(아니면 거부 · exit 1).
사람이 「아침이었다」고 적는 것으로는 한 줄도 안 는다.

— F
