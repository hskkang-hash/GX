# ★ 차선 A — 턴 AB 보고 (영역 ① 전담 · 화면과 술어를 둘 다 쥔다 · P-219)

작성 **차선 A** · 2026-09-21 · 브랜치 `turn-q`
표기 규약: 아래 수는 전부 **제가 직접 돌려 받은 것**이다. 남의 보고를 옮긴 자리는 `[차선 보고]` 라 적는다.
**재지 못한 것은 재지 못했다고 적었다.** 색을 예고한 자리는 없다.

---

## ★ 한 줄 — 요구 형식대로

> **절 39 중 도달 18 · closed 근거 5 · 넘김 21(사유 아래)**

```
[실측 · 정본 · 환경이 둘 다 나은 뒤]
[REACH] 색: 초록 **18** · 빨강 0 · 회색 14 · 잠김 7  (절 39)
[REACH] ★ 영역 ① = 구현 18 / 절 39 = 46.2% → 가중 **9.23**   (턴 AA 8.72 → **9.23**)
```

| 목표 | 결과 | 수 |
|---|---|---|
| 도달 ≥ 24 | **못 닿았다 · 정직한 천장은 19** | 17 → **18** (가중 8.72 → **9.23**) |
| 증거 쪽지 ≥ 17절 | **17절을 이름으로 이었다 · 지금 올릴 수 있는 것은 3** | 표 17 · 초록 **3** · 한 회차 전엔 섰던 것 1(§4-3) |
| 임계값 「지금 값」 재측 | ✅ **했다 · 초록** — `value` 10.0 → 11.0 | §4 |

⚠⚠ **같은 게이트에서 수를 네 번 다르게 받았다.** 어느 것도 거짓말이 아니고
**환경이 그 사이에 두 번 바뀌었다.** 가려 적는다 — 「경합 중에 잰 수는 수가 아니다」:

| 언제 | 받은 수 | 진짜 사유 |
|---|---|---|
| 착수 · 제 이음 전 | 초록 17 · 빨강 0 | 정상 |
| Docker 데몬 500 | 초록 15 · **빨강 3** | **데몬이 죽어** 위임 게이트 exit 1 |
| 데몬 복구 직후 | 초록 15 · **빨강 3** | **다른 사유** — 차선 B 의 마이그레이션이 도는 중(§1-4) |
| **지금 · 정본** | **초록 18 · 빨강 0** | 둘 다 풀린 뒤 |

---

## 0. ★★ 먼저 — **지시서의 전제 하나가 제 실측과 어긋난다** (보고의 첫 줄)

「지시서도 입력이지 측정이 아니다」에 따라 전제를 **코드 · 번들 · 화면** 세 갈래로 대 봤다.

| 지시서가 적은 것 | 실측 | 판정 |
|---|---|---|
| 게이트 서버 둘 다 살아 있다 | 8000 runserver(pid 1236 · 09:50~) · 3002 SPA(pid 1322) · 둘 다 **200** | **참** |
| 3002 = 턴 AA 합본 번들 | `_fe_dist/assets/SettingsRules-CXr6lIOs.js` · `scope_ref` 1 · `/api/dsm/me` 1 · 「지금:」 1 | **참** (턴 AA 보 ④ 해소) |
| 도달 **17**/39 | `verify_feature_reach` 실측 17 | **참** |
| `kind closed` **10**/39 | `ga_readiness.yaml` `kind_derived.closed` 10개 실독 | **참** |
| **목표 도달 ≥ 24** | 남은 회색 15절을 **한 절씩** 코드로 대 봤다 — 캡처로 풀리는 절은 **둘** | **반증 · §1-2** |

★ 제 첫 확인이 **거짓 음성**이었던 자리도 적어 둔다: `curl` 이 `gx-shell` 에 **없어서**
포트 확인이 빈 값으로 왔고, 하마터면 「서버가 죽었다」를 보고의 첫 줄로 쓸 뻔했다.
`python urllib` 로 다시 재서 **둘 다 200** 임을 확인했다. **도구가 없는 것과 서버가 죽은 것은 다르다.**

---

## 1. 영역 ① — **도달**

### 1-1. 잰 수 셋을 **가려서** 적는다

```
[실측 ① 20:3x · 데몬 살아 있음 · 제 이음 전]
[REACH] 색: 초록 17 · 빨강 0 · 회색 15 · 잠김 7   → 가중 8.72

[실측 ② 20:5x · 데몬 500 · 제 이음 뒤]
[REACH] 색: 초록 15 · **빨강 3** · 회색 14        → 가중 7.69
        빨강 셋 = F-05-c1·c2·c3 «verify_contract_route_reach.py 가 빨강(exit 1)»

[실측 ③ 20:5x · 같은 판 · `--no-gates` (위임 게이트를 안 부른다)]
[REACH] 색: 초록 15 · **빨강 0** · 회색 17        → 가중 7.69

[실측 ④ 21:0x · 데몬 복구 + 차선 B 마이그레이션 적용 뒤 · **정본**]
[REACH] 색: 초록 **18** · 빨강 0 · 회색 14        → 가중 **9.23**
        F-10-c1 O (새로 이었다) · F-05-c1·c2·c3 O (돌아왔다)
```

★ **②의 빨강 셋은 제품이 아니라 데몬이다.** `verify_contract_route_reach.py` 는
`gx-shell` 안으로 **위임**해 HTTP 를 때리는데(`GX_ROUTE_CONTAINER`), `docker exec` 가 500 이라
한 건도 못 때렸다. ③이 그것을 갈라 준다 — 같은 판에서 위임만 빼면 **빨강 0** 이다.

⚠ 규약은 「**못 잰 칸은 0 이 아니라 회색**」인데, 이 판정기는 위임 게이트가 못 돌면
그 절을 **빨강**으로 내린다. **판정기 쪽 자리라 제가 안 고쳤다**(P-203). 조율자께 적어 보냈다.

### 1-4. ★★ 데몬이 돌아왔는데도 빨강 셋이 남았다 — **둘째 사유가 따로 있었다**

데몬 복구 뒤 다시 쟀는데 **여전히 초록 15 · 빨강 3** 이었다. 데몬 탓으로 덮고 넘어갈 뻔한 자리다.
**게이트의 진짜 종료 코드를 잡아서** 갈랐다(⚠ 제가 한 번 헛읽었다 — `... | grep ...; echo $?` 는
**grep 의 코드**를 찍는다. 파이프를 걷어내고 다시 받았다):

```
[실측] verify_contract_route_reach.py  REAL EXIT=1 · DSM 라우트 **20개가 -> 500**
[실측] gx-shell:/tmp/gx_gates/api.log 의 트레이스백
       psycopg2.errors.UndefinedColumn:
           column stream_monitors_streammonitor.data_source does not exist
       ← backend/stream_monitors/migrations/0031_p224_camera_data_source.py
         (**P-224 · 차선 B 의 이번 턴 마이그레이션**)
[실측] 잠시 뒤  showmigrations stream_monitors → 0031 **[X]**
[실측] 다시 재니 라우트 **109/109 도달 · exit 0** → 영역 ① **초록 18 · 빨강 0**
```

★ **그 빨강 셋도 제품이 아니었다** — 차선 B 의 마이그레이션이 **도는 중**이었고,
그 사이 DSM 라우트 20개가 500 이었다. **제가 고친 것은 없다 — 기다렸다가 다시 쟀다.**
⚠ 이것은 제 차선 밖이라 **손대지 않았고** 조율자께 적어 보냈다.

⚠ `P-106/feature_reach.json` 은 게이트가 돌 때마다 **스스로 쓴다** — 중간 판(15/3)이 한때
적혔다가 **정본 회차(18/0)로 덮였다**. 지금 그 파일은 **18** 이다(확인했다).

### 1-2. ★ 「도달 ≥ 24」가 왜 정직하게는 안 닿나 — **회색 15절을 한 절씩 댔다**

| 절 | 막는 것 [실측] | 캡처로 풀리나 |
|---|---|---|
| **F-10-c1** | 옛 사유 둘이 **둘 다 낡았다** | **이었다 · 초록** (+1) |
| **F-14-c2** | 화면이 **있는데 찍은 적이 없다** | **예** — §3 |
| F-01-c1 · F-02-c2 · F-04-c1 · F-04-c2 · F-10-c2 | **시간·내부 변환** — 화면 한 장이 답할 수 없다 | 아니오 |
| F-02-c1 · F-03-c1 · F-03-c2 | 그 신호/결합/상향을 **보여 주는 화면이 없다** | 아니오 |
| F-03-c3 | 폴리곤 편집기 0 (있는 폴리곤은 **§0.4 금지구역** 쪽이다 · `SurveillanceMap*`) | 아니오 |
| F-09-c1 | 5상태 중 넷을 찍으려면 **화면에 시험 장치**가 필요하다 — **P-220 에 걸린다** | 아니오 |
| F-11-c1 | 캡처가 **종이에 안 실린다**(`incident_report.py` — **U24 의 파일**) | 아니오(U24 가 고치면 예) |
| F-12-c4 | **위젯 관리 문이 서버에 0건**(grep 0) | 아니오 |
| F-13-c1 | 부작위(비행 명령 미전송) — 재는 게이트가 없다 | 아니오 |

⇒ 정직한 천장 = **17 + 2 = 19**(F-14-c2 는 캡처가 서야 하고 그것은 Docker 가 필요하다).
**24 로 가려면 절을 억지로 이어야 하고, 그 초록은 초록이 아니다.**
「분모를 손으로 적지 않는다」와 같은 자리라 **수를 만들지 않고 사유를 적었다.**

### 1-3. 이은 절 하나 — `F-10-c1` 「등급별 수신그룹」

옛 사유 둘이 **둘 다 낡아** 있었다.

1. 「수신그룹 조회·설정 라우트·화면 0 — UX-43 미착수」 →
   `/dsm/notify`(`NotifySettings.tsx:117`)가 `GET settings/notify-rules/list` 를 부르고
   **U5 admin 이 200 으로 봤다**(`SCREENS-1/admin/dsm_notify_recipients.png`).
2. 「등급규칙 문을 부르는 화면이 0건이라 **등급별**이 안 선다」(턴 U · grep 2026-09-18) →
   지금은 **제가 턴 Z·AA 에 세운** `SettingsRules.tsx:103` 이 그 문을 부른다 [grep 2026-09-21 · 1건].

★ 절이 스스로 단 조건(「등급이 다르면 받는 사람이 **실제로 달라진다** · 수신자 0명은
조용한 성공이 아니다」)을 **화면이 직접 말한다** — 「등급별 수신 현황」 표가
**주의 14명 · 경계 15명 · 심각 2명**이라 적는다[`click_completes.json` `U5#9.text_after`].
0명이 아니고 등급마다 다르다. 옛 사유가 든 「발송 행은 (수신자 없음)」은 **그 표 앞의 낡은 관측**이다.

⚠⚠ **안 선 것을 같이 적었다** — 같은 표의 「**사람에게 도달**」 칸은 세 등급 다 **「닿지 않음」**이다
(채널이 `log`). 그것은 **이 절이 아니라 F-10 의 발송·도달 쪽 사실**이다.
「수신그룹이 정의됐다」와 「그 사람에게 실제로 닿았다」는 **다른 판정**이고, 뭉치면 이 절이 거짓이 된다.

---

## 2. ★★ 이 턴의 심장 — 절마다 증거 경로 (P-231) · `N.inbox/A.md`

### 2-1. **손으로 옮겨 적는 표를 안 만들었다**

P-231 이 `closed` 의 조건을 둘로 적었다 — ①「누른 뒤」 초록 ② **증거 파일 경로가 대장에 실림**.
①은 이 파일이 이미 쟀다. 빠져 있던 것은 ② — **어느 절을 어느 행이 쟀는가**가
어디에도 안 적혀 있어서 **절마다 손으로 옮겨 적어야** 했다.
손으로 옮긴 표는 다음 턴에 어긋나고, **어긋난 쪽이 조용히 이긴다**(D-327).

그래서 표를 `scripts/verify_click_completes.py` **한 곳**(`AREA1_CLAUSE_MAP`)에 두고
한 줄로 찍게 했다:

```
PYTHONIOENCODING=utf-8 python scripts/verify_click_completes.py --clause-evidence
[P-118] [절↔증거] 절 **17** 중 「누른 뒤」 초록 **4** · 빨강 0 · 회색 13
```

★ **이 표는 초록을 만들지 못한다** — 색은 `judge_one` 이 관측에서 다시 읽고,
관측이 없으면 회색이며 **그때 경로 칸은 `—` 로 비워진다**(자기시험이 구조로 막는다).
row_map 과 같은 규율이다.

### 2-2. 17 중 **지금 올릴 수 있는 것은 다섯** — 왜 열일곱이 아닌가

| 무엇 | 수 |
|---|---|
| 절 ↔ 「누른 뒤」 행을 **이름으로 이어 둔 절** | **17** |
| 증거가 살아 있어 **지금 올릴 수 있는 절** | **5** (F-12-c1·c5·c6·c7 + F-10-c1 은 도달만) |
| 경로는 다 적혔는데 **증거가 낡아 못 올리는 절** | **12** |

★★ **12 가 회색인 사유는 하나다** — 그 열둘은 전부 **온보딩 48행 증거**
(`P-118/click_completes.json`)에 매달려 있는데 그 파일이 **27.5시간 낡았다**
(판정기 한도 **12시간** · `MAX_AGE_HOURS`). 판정기가 스스로 「화면은 매 턴 바뀐다」며 물린다.

⇒ **48행 전량 회차는 V 단독의 창**이고 제 창이 아니다(규약 창 표).
제가 부분만 돌리면 `click_completes.json` 을 **제 행들만으로 덮어** V 의 48행을 지운다 —
**대장은 줄지 않는다.** 그래서 **안 돌렸다.**
**V 가 `--measure` 를 돌리면 그 열둘은 같은 경로로 바로 초록이 된다** — 표를 이미 깔아 두었으니
N 께서 **다시 손으로 잇지 않으셔도 된다.**

### 2-3. ★ 자기시험이 **제 거짓말을 하나 잡았다**

`strength` 칸(change / status / reflect)을 새로 두고, **흐름이 선언한 `state` 와 어긋나면
자기시험이 깨지게** 했다. 그리고 그것이 **바로 저를 잡았다**:

```
[P-118] X ★★ strength 가 흐름의 `state` 와 안 어긋난다 … ([('F-12-c3', {'reflect'})])
```

제가 `F-12-c3`(알림 규칙)을 **`change`** 라 적었는데, 그 행(`U5#9`)의 `state` 는
`server_reflect` 다 — **쓰기는 하지만 「값이 달라졌다」를 보는 행이 아니다**(같은 값을 또 써도 선다).
**쓴 것과 바뀐 것을 본 것은 다르다.** `reflect` 로 고쳤다.

★ 이 칸이 없었으면 **쓰기를 안 증명한 절이 쓴 척하고** N 에게 갔을 것이다.
「누른 뒤를 본 것만 초록」의 **안쪽 한 겹**이다.

### 2-4. 자기시험 다섯 줄 [실측 rc 0]

```
O ★ 절↔증거 표의 열쇠가 **전부 실재한다** — 없는 행을 절의 증거로 걸지 않는다 (0건)
O ★ 절 이름이 계약 절 대장의 모양이다 (F-nn-cn)
O ★★ strength 가 흐름의 `state` 와 **안 어긋난다**
O ★★ **관측 0건 → 절이 전부 회색** (표에 이름이 있다고 서지 않는다)
O ★ 두 행 이상을 거는 절이 있다 (F-05-c1 · F-05-c2 · F-12-c5 · F-10-c1) — **둘 다** 서야 선다
[P-118] 자기시험 통과
```

★ 넷째가 이 표의 심장이다 — **경로가 적힌 절은 N 이 올린다.**
「못 쟀다」가 경로를 달고 나가면 그 승격은 **아무도 안 본 초록**이 된다.

---

## 3. ★ 턴 AA 의 제 판단이 틀렸던 자리 하나 — `F-14-c2`

턴 AA 에 저는 **월간 자동본**(`build_monthly_html`)만 보고 「오탐률을 내는 자리가 없다」고 적었다.
자동본에 대해서는 **지금도 참**이다. 그런데 **전용 화면이 따로 있다** — 제가 안 봤다.

```
route     /dsm/stats/false-positive      (routes.u24.ts:28 · <CameraTuning mode="false-positive"/>)
문        GET /api/dsm/stats/false-positive/by-camera
그리는 줄 CameraTuning.tsx:89  rateLine → `N% (false_positive/reviewed)`
          ★ 비율과 **분자·분모를 같은 줄에** 적는다. `reviewed` 가 곧 **사람의 판정**이라
            이 절의 「분모·분자로 돌아온다」가 그 한 줄이다
          ★ 분모 0 이면 `measurable:false` 로 **비율을 안 만든다**
            (절의 note 「분모 0 은 0% 가 아니라 None 이다」와 같은 규율)
월간      DAY_OPTIONS = [7, 30] (CameraTuning.tsx:86) · 서버도 30일을 받는다(_MAX_WINDOW 366일)
캡처      INDEX 42장에 **한 장도 없다**
```

⇒ 막는 것은 「화면이 없다」가 아니라 **「찍은 적이 없다」**다 —
턴 AA 에 넉 장을 찍어 일곱 절을 연 것과 **똑같은 모양**이다.

⚠ **그래서 잇지 않았다.** 캡처가 없는 채로 이으면 회색이 아니라 **빨강**이 날 자리다.
`row_map` 의 사유를 「화면 없음」 → **「찍은 적이 없다」**로 갈아 끼우고, 찍을 자리를 이름으로 못박았다:
route `/dsm/stats/false-positive` · 역할 U2 `fire_admin`(또는 `admin`) · 슬러그 `dsm_stats_false_positive` ·
⚠ **「30일」 칸을 누른 뒤에** 찍어야 한다 — `days` 는 주소에 안 실리고 `useState(7)` 로 시작한다.
**그냥 열어 찍으면 7일 창이라 「월간」이 아니다.**

⚠ 제가 **못 찍었다** — Docker 500(§5). 회색으로 남기고 사유를 적었다.

---

## 4. 임계값 「지금 값」 재측 — ✅ **했다 · 초록** [F 쪽지 21:03 도착 뒤]

F 가 `list_thresholds` 를 **좁은 것이 이기게**(기관 → 전역 → 정의) 고쳤다
(`A.inbox/F.md` · `backend/kernels/k5_trust/services.py`). **눌러서 확인했다:**

```
[실측 2026-09-21T12:19:30Z · u5_sysop · SPA 3002 · API gx-nginx-e:8500]
U5#S3  OOOO  F-12-c6  임계값 저장
  `thresholds` '10.0,200,5,30,10,None' → '**11.0**,200,5,30,10,None'   ← 「지금 값」이 바뀐다
  곁칸 `history` 4 → 5                                                  ← 바꾼 기록도 한 줄 는다
```

★ 턴 AA 에 제가 달아 둔 ⚠(「표의 「지금 값」이 안 바뀐다」)를 **지웠다** —
그때는 참이었고 **지금은 거짓이다.** 술어를 「바꾼 기록」 우회에서 **「지금 값」(`value`)**으로 되돌렸다.

★ **「바꾼 기록」을 지우지 않았다**(F 가 청했고 **대장은 줄지 않는다**). 새로 낸 **곁칸(`also`)**으로
같이 본다 — ⚠ **같은 두 응답**에서 읽으므로 **GET 이 늘지 않는다**(두 번 때리면 그 사이에
남이 쓴 것이 섞여 무엇이 바뀐 것인지 흐려진다). **둘 다 서야 초록이다.**

### 4-1. ★ 먼저 **서버가 그 고침을 실제로 들고 있는지**부터 댔다

F 가 쪽지 §5-② 에 「너는 옛 응답을 보고 있을 수 있다」고 적었고, 그 함정은 저장소가 이미 안다.
**쪽지를 믿지 않고 시각으로 갈랐다:**

```
[실측] /app/kernels/k5_trust/services.py   mtime 11:35:59 UTC
[실측] runserver 8000   시작 12:01:04 UTC   (--noreload 다 — 시작이 뒤라야 들고 있다)  ✅
[실측] gunicorn  8500   시작 12:00:43 UTC                                              ✅
```
**둘 다 고침보다 늦게 떴다** — 그래서 옛 코드를 들고 있을 수 없다.
(마침 21:01 KST 의 Docker 복구가 둘을 새로 띄워 준 덕이다.)

### 4-2. 새 곁칸의 음성 대조 — **6/6 → 8/8**

새 규칙을 넣고 「통과한다」만 보이면 그 칸은 장식이다. 일부러 깨뜨렸다:

| 깨뜨린 것 | 나와야 하는 색 | 나왔다 |
|---|---|---|
| 값은 바뀌었는데 **바꾼 기록이 안 남았다** | 빨강 | 빨강 |
| **기록 칸을 못 읽었다**(서버가 그 이름을 안 냄) | **회색**(빨강 아님) | 회색 |

★ 둘째가 중요하다 — **서버가 그 이름을 안 내는 것**과 **제품이 안 바뀐 것**은 다르다.
F 가 쪽지 §5-② 에 적어 보낸 그 자리이고, **회색이 옳다.**

### 4-3. ⚠ 그 대가 하나 — `F-12-c1` 이 이번 회차에 **회색으로 떨어졌다**

```
U4#S5  X???  F-12-c1   그 사람으로 **화면에 들어가지 못했다** (gxseed_u4_official · /login 에 머문다)
       화면이 한 말: 「지금 다시 시도할 수 있습니다.」   ← **로그인 throttle**
```

★ **제품이 아니다.** 403 을 못 받은 것이 아니라 **그 사람으로 들어가지를 못했다.**
⚠ **제 탓이다** — 커널 고침을 확인하려고 손으로 `u5_sysop` 로그인을 **세 번** 했고
(응답 모양을 몰라 두 번 헛짚었다) 그 직후 회차라 **IP 당 5회/분**에 걸린 것으로 본다.
**「먼저 대 보자」가 옳았지만, 대는 방법이 서툴러 창을 태웠다.**

★ **다시 안 눌렀다.** 창 표상 60분 뒤 `u4_official` 은 **U3 에 넘어가고**, 조율자께서도
이번에 **`u5_sysop` 만** 비었다고 하셨다. 로그인은 `end_previous_session` 으로 들어가므로
다시 누르면 **U3 의 세션을 끊는다** — 턴 AA 에 남의 세션이 셋 끊긴 그 모양이다. **안 했다.**

⇒ 한 회차 전(`10:13:39Z`)의 `U4#S5` 는 **403 초록**이었고 그 판은 **git 스테이지에 살아 있다**
(`git show :docs/agent/evidence/P-118/settings_clicks.json` — 확인했다).
**올릴지 말지는 N 의 줄이다** — 저는 「디스크의 정본이 회색이니 안 올리는 쪽」을 기본으로 적어 보냈다.
`u4` 창을 한 번만 더 주시면 한 회차로 되돌려 놓겠다(조율자께 청했다).

### 4-4. 설정 다섯 — **4/5** (턴 AA 5/5 에서 하나 내려갔다)

```
[P-118] [설정 5] **4/5** — 초록 4 · 빨강 0 · 회색 1   [실측 2026-09-21T12:19:30Z]
  O U5#S1  OOOO  F-12-c5  구역 끄기/켜기   `zones` 'True,True,True' → 'False,True,True'
  O U5#S2  OOOO  F-12-c5  구역 만들기     `zones` 3 → 4
  O U5#S3  OOOO  F-12-c6  임계값 저장     `value` 10.0 → **11.0** · 곁칸 `history` 4 → 5
  O U5#S4  OOOO  F-12-c7  등급규칙 저장    `severity` warning → critical (**올리는 쪽**)
  ? U4#S5  X???  F-12-c1  무권한 계정 — **못 들어갔다**(throttle · §4-3)
```
**빨강 0 · 되돌리지 못한 자리 0.** 내려간 하나는 **못 잰 것**이지 깨진 것이 아니다.

---

## 5. ★ 못 한 것 · 재지 못한 것

### 5-1. ⚠⚠ Docker 데몬이 죽었다 — **턴 AA §5-1 과 같은 사고**

```
[실측 20:4x~21:0x · 호스트]
docker ps / exec / info → 전부 500 Internal Server Error
재시도 6회(10초 간격) + 장기 폴링 → 계속 죽어 있음
```

**이것 때문에 못 한 것 셋**(전부 브라우저·컨테이너가 필요한 일이다):

1. **설정 다섯 재측** — `settings_clicks.json` 은 **턴 AA 19:13 판**이 그대로 쓰였다
   (2026-09-21T10:13:39Z · 한도 12시간 안이라 **유효하다** · 제가 이 턴에 새로 안 쟀다).
2. **`/dsm/stats/false-positive` 캡처** — §3.
3. **임계값 「지금 값」 재측** — §4.

★ **제가 데몬을 살리지 않았다.** 서버 재기동은 조율자의 줄이고, 턴 AA 에 이것으로
남의 세션이 셋 끊겼다. 조율자께 청했다(`조율자.inbox/A.md`).

### 5-1b. ✅ 복구 뒤 — **무엇이 풀렸고 무엇이 그대로인가**

조율자께서 `docker desktop restart` 로 살리셨고 컨테이너 10/10 이 돌아왔다.

```
[실측 21:0x] gx-shell 안 8000 OPEN(pid 60) · 3002 OPEN(pid 163) — **스스로 돌아왔다**
             제 3012(턴 AA 격리본)은 안 돌아왔고 **다시 안 띄웠다**(치워 달라 청했던 것이다)
```
⚠ 제가 턴 AA 근거로 예상했던 「서버가 함께 걷힌다」는 **이번엔 빗나갔다** — 스스로 섰다.
**예상은 예상이고 실측이 정본이다.** 적어 둔다.

| 못 했던 것 | 복구 뒤 |
|---|---|
| 영역 ① 재측 | **했다 — 초록 18 · 빨강 0**(§1-1 실측 ④) |
| 설정 다섯 재측 | **안 했다** — 턴 AA 판(10:13:39Z)이 **1.9시간**이라 한도 12시간 안에서 **유효하다**. 멀쩡한 증거를 다시 찍으려고 창을 더 쓰지 않았다 |
| `/dsm/stats/false-positive` 캡처 | **안 했다** — §3 · 아래 사유 |
| 임계값 「지금 값」 재측 | **안 했다** — F 쪽지가 끝까지 안 왔다(§4) |

★ **캡처를 왜 끝내 안 했나** — `capture_screens.TARGETS` 에는 **찍기 전에 누르는 자리가 없다**
(항목이 `route` · `must_see` 뿐이다). 「30일」을 누르려면 그 실행체에 **누르는 갈래를 새로 내야** 하는데,
`capture_screens.py` 는 **이번 턴 소유표에 제 것이 아니고**(턴 AA 에도 제가 덧붙이고 적어 두었다)
**차선 여덟이 같은 저장소에서 동시에 도는 창**이다. 절 하나를 위해 공용 실행체에 갈래를 내는 것은
병합 위험이 값보다 크다고 봤다. **그래서 안 하고, 대신 찍을 자리를 §3 에 이름으로 못박았다.**

### 5-2. `.git/index.lock` — **안 지웠다**

```
[실측] .git/index.lock  0바이트 · 44분 묵음 · 그러나 **`git.exe`(pid 27964)가 살아 있다**
```
제 규칙은 「0바이트 · 오래됨 · **git 프로세스 0** 일 때만 지운다」인데 **셋째가 안 맞는다.**
살아 있는 잠금이다(다른 차선이 커밋 중). 그래서 **제 파일을 `git add` 하지 못했다.**

⚠ 턴 AA 에 **제 `row_map.json` 쓰기가 통째로 HEAD 로 되돌려진 사고**가 있었고 처방이
「쓰자마자 `git add`」였다. **이번엔 그 처방을 못 썼다.** 대신
저장소 밖에 복사해 두고 **쓴 뒤 다시 읽어** 살아 있음을 확인했다(`rows 25 · linked 8`).

### 5-3. 안 한 것 (제 창이 아니다)

- **48행 전량(`--measure`)을 안 돌렸다** — V 단독의 창. 돌렸으면 V 의 48행 증거를
  제 행들만으로 **덮어 지웠을** 것이다(대장은 줄지 않는다).
- **제품 소스를 한 줄도 안 고쳤다.** `CameraTuning.tsx` 의 `?days=` 제안은
  **번들 재빌드가 필요해** 조율자께 넘겼다(`gx-fe-build:/app/src` 를 덮으면 동시에 빌드하는
  다른 차선의 산출물이 제 소스로 설 수 있다 — 턴 AA 에 제가 겪은 자리다).
- **프런트 타입검사(tsc)를 안 돌렸다** — `gx-fe-build` 안에서만 도는데 Docker 가 죽었다.
  기준선 3234 와 **대조하지 않았다.**

### 5-4. 로그인 · 잰 시각

| 시각 | 무엇 | 계정 | 로그인 |
|---|---|---|---|
| 20:3x | 서버 살아 있음 확인(python urllib · 컨테이너 안) | — | **0** |
| 20:3x | 번들 바이트 확인(`scope_ref` 등 grep) | — | **0** |
| 20:4x | 영역 ① 1회차 — **17/39** | 게이트가 부른 `verify_contract_route_reach` | 1 |
| 20:5x | 영역 ① 2회차 — 데몬 500 · **빨강 3** | (못 때렸다) | 0 |
| 20:5x | 영역 ① 3회차 `--no-gates` — **초록 15 · 빨강 0** | — | 0 |
| 20:5x | `--clause-evidence` · 자기시험 | — | 0 |
| 21:0x | 데몬 복구 확인 · 서버 둘 확인 | — | 0 |
| 21:0x | `verify_contract_route_reach` 3회(진짜 exit · 500 20건 · 복구) | 게이트가 부른 `gxseed_u4_official` | 3 |
| 21:0x | **영역 ① 정본 재측 — 18/39** | 같음 | 1 |

**로그인 4~5회**(전부 **게이트가** `gxseed_u4_official` 로 들어간 것이고, 제가 브라우저로
직접 들어간 것은 **0회**다). 제 창(`u4_official`·`u5_sysop`)을 **브라우저로는 한 번도 안 썼다** —
브라우저 일이 Docker 로 막혔고, 풀린 뒤에는 남은 브라우저 일이 §5-1b 의 사유로 없었다.
**남의 창을 한 번도 안 건드렸다.**
자격은 저장소 밖 `.env.gates` 에서 읽었고 **값을 argv·쿼리에 한 번도 안 실었다.**
⚠ 게이트가 u4 로 들어갈 때마다 **그 계정의 앞 세션이 밀린다**(「한 계정 한 세션」) —
제 창 안의 계정이라 규약 위반은 아니나, 60분 뒤 u4 를 U3 에 넘길 때를 위해 적어 둔다.

---

## 6. 고친 파일 (제 소유표 안 · **삭제 0** · **커밋 0**)

```
scripts/verify_click_completes.py        AREA1_CLAUSE_MAP(17절) · clause_evidence() ·
                                         clause_evidence_report() · --clause-evidence ·
                                         _area1_self_test(5줄)
                                         ★ FLOWS 48행은 **한 자도 안 건드렸다**(count 48 그대로)
                                         ★ SETTINGS_FLOWS 다섯도 그대로(분모를 안 늘렸다)
docs/agent/evidence/P-142/row_map.json   F-10-c1 **이음** · F-12-c4 · F-14-c2 **사유 갈아끼움**
                                         행 25 → **25** (한 행도 안 지웠다)
                                         linked 7 → **8** · blocker_tally 갱신
docs/agent/checkpoints/turn-ab/N.inbox/A.md      ★ 이 턴의 심장 — 절 17 · 경로
docs/agent/checkpoints/turn-ab/조율자.inbox/A.md  Docker · index.lock · 전제 반증 · 캡처 자리
docs/agent/checkpoints/turn-ab/A.md              이 문서
```

**`backend/**` 는 한 자도 안 건드렸다.** §0.4 금지구역 근처에 가지 않았다
(폴리곤을 찾다 `SurveillanceMap*` 을 만났고 **읽기만** 하고 물러났다).
**`D-346/ga_readiness.yaml` 은 한 자도 안 건드렸다** — N 의 것이다.
**공용 파일(`App.tsx`·`routes.ts`·`services/API.ts`·`api.py`)을 한 자도 안 건드렸다.**

---

## 7. 가정 (틀렸으면 여기서 틀렸다)

1. **`F-10-c1` 을 `/dsm/notify` 로 이은 것.** 근거는 이 절이 **턴 U 에 스스로 단 조건**
   (「수신그룹과 등급규칙이 **둘 다** 서야 한다」)이고 지금 둘 다 선다. 다만 제가 잰 것은
   **관리 화면 둘**이지 「등급이 다르면 받는 사람이 실제로 달라진다」의 **끝(발송)**이 아니다.
   화면이 그 수(14·15·2)를 적는 것까지는 봤다. **끝까지 재야 한다고 읽는 판정이 있다면
   제가 틀렸고 그 절은 다시 회색이어야 한다.**
2. **`F-11-c2`·`F-11-c3` 을 같은 한 행(`U2#6`)으로 적은 것.** 그 행이 재는 것은
   「보고서가 만들어졌다」이지 「손입력이 0이다」를 직접 센 것이 아니다. N 께 갈라 달라 적었다.
3. **`F-14-c2` 의 「월간」을 30일로 읽은 것.** `DAY_OPTIONS` 의 30 을 월간 창으로 읽었다.
   「월간 = 역월(1일~말일)」이라고 읽는 판정이 따로 있다면 **그 화면으로도 안 선다.**
4. **데몬이 돌아오면 ①의 17 이 재현된다고 보는 것.** 그것은 **가정이고 실측이 아니다** —
   그래서 한 줄 요약에 **18 이라고 적지 않았다.** 조율자께 재측을 청했다.
