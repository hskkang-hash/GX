# 차선 U56 — 기관 관리자 · 연계 (턴 AB · 2026-09-21 20:3x~21:1x KST)

WO-GX-20260921-04 파 1 턴 2 「돈 내는 고객이 URL 로 들어와 제 일을 끝낸다」 · 브랜치 `turn-q`

## ★ 첫 줄 — **U5 8 중 닫힘 4 · U6 8 중 닫힘 0 · 넘김 4**

**색을 스스로 칠하지 않는다.** 아래는 **이번 턴에 내가 잰 것**만이고, 색은 V·조율자가 읽는다.

| | 수 | 무엇 |
|---|---|---|
| **U5 8행 · 이번 턴 내가 술어를 세워 본 것** | **4** | #9 · #10 · #14(전 턴 그대로) · #15(상한 선언 확인) |
| **U5 8행 · 고쳤으나 화면을 못 본 것** | **1** | #2 — **뿌리를 찾아 고쳤다. dist 가 안 서서 누른 뒤를 못 봤다** |
| **U6 8행 · 이번 턴 닫은 것** | **0** | #3 · #4 둘 다 **제품이 아니라 판정기가 낸 빨강**이었다(아래 ③④) |
| **넘김** | **4** | ① U5#2 빌드 창(조율자) ② U5#15 `Metering.tsx`(B) ③ U6#3 씨앗(V) ④ U6#4 판정기 한 줄(V·Q) |
| **로그인** | **4회** (`gxseed_u5_sysop` 만 · `gxseed_u4_official` **0회**) | 브라우저 **0회** — 전부 gx-shell 안 HTTP·ORM |
| **서버 재기동** | **0회** · 청하지도 않았다 | 앞판 빌드도 **안 했다**(이유는 ①) |
| **뒷단 변경** | **0줄** | 이번 턴 `backend/**` 를 한 줄도 안 고쳤다 |

---

## ① ★★ U5#2 `/roles` 0행 — **인수 자산의 결함이 아니다. 우리가 턴 O 에 낸 퇴행이다**

### ★ 지시서대로 **재현 시험을 먼저 세웠다**

```
scripts/repro_u5_2_roles_zero_rows.mjs        (새 파일 · 의존성 0 · node 24 가 .ts 를 그대로 읽는다)
  node scripts/repro_u5_2_roles_zero_rows.mjs      → rc 0 · 18/18 PASS
  ★ 음성 대조 — 고침(`withCurrentPath`)을 **되돌린 사본**으로 같은 시험을 돌렸다:
       rc 1 · **7건 어긋남**   ⇒ 이 시험은 실제로 내 줄을 본다. 「눈 감은 초록」이 아니다.
       되돌림은 **스크래치패드 사본**에만 했다 — 저장소 파일 잔여 0(`grep` 확인).
```

### 뿌리 — 번들에서 사슬 **넷을 다 밟았다** [실측 · `rj-core/dist/rj-core.es.js`]

```
㉠ RoleManagement 의 목록 호출은 조건 안에 있다
      useEffect(() => { E && !zn(z) && y(); }, [B, E, z])      E = pageSize
㉡ E 는 표가 **제 설정(grid config)** 을 받은 뒤에야 채워진다
      useEffect(() => { u0?.pagination_size && f && W(Number(u0.pagination_size)); })
㉢ 그 설정은 이 조건 아래에서만 불린다
      x0 && !A1 && (activeSubItemSession || activeItemSession) && userInfo?.id && k1?.id
㉣ k1 = getMenuByMainIdAndSubId(...)  →  **useMenuData() 의 목록에서 그 id 를 찾는다.**
      못 찾으면 null.

⇒ 목록에서 뗀 줄은 k1=null → 설정 없음 → pageSize 없음 → **목록 호출 0회** → 표 **0행**.
  화면은 「없다」와 **똑같은 그림**이 된다.
```

### 시각이 그대로 말한다 [정본 `docs/agent/onboarding_48.md`]

```
2026-09-07   admin 으로 /roles → **15개 역할 · Add New Role 보임**        ●
2026-09-10   턴 O — `features/nav/roleNav.ts` 가 서고 U5 표에 /roles 가 없다
2026-09-17   같은 계정 같은 주소 → **표 행 0 · Add New Role 없음**        ○
```

### 서버는 멀쩡하다 — **창에서 직접 두드렸다**

```
[실측 2026-09-21 21:0x · gx-shell 안 8000 · gxseed_u5_sysop 실제 로그인 JWT]
  GET /api/roles/?page_size=1000&current_page=1  -> **200**
    최상위 칸 ['count','current_page','message','page_size','roles','status','success','total_pages']
    count = **16** · total_pages = 1 · roles 길이 = **16** · success = True
```

⇒ **서버가 16행을 준다.** 인수 코드가 읽는 칸 이름(`.roles` · `.count` · `.total_pages`)과도
맞는다. 즉 부르기만 하면 그려진다 — **안 부르는 것이 전부였다.**

### 내 머리말이 세 턴 동안 틀렸다 — 지우지 않고 바로잡았다

`roleNav.ts` 머리말은 「**가림이지 자물쇠가 아니다 — 주소를 치면 들어간다**」라고 적어 왔다.
**들어가기는** 한다. 그러나 **그 화면이 제 표를 못 그린다.** 권한은 안 막았는데 **화면을
막고 있었다.** 머리말을 안 지우고 ⚠⚠ 한 절을 붙여 어디가 반만 참이었는지 적었다.

### 고친 것 — **뗀 것을 되돌리지 않는다. 서 있는 자리만 남긴다**

```
roleNav.ts   ④ menuForPath(menus, pathname)      나무 전수 · 정확 일치 먼저 · 없으면 가장 긴 앞자리
             ④ withCurrentPath(filtered, original, pathname)
                  잘라 낸 목록에 **지금 서 있는 주소의 줄 하나**를 뿌리로 되돌린다
             navSignature 를 **나무 끝까지** 세도록 고쳤다(종전은 뿌리+자식 수뿐 —
                  그 깊이에서는 `hideAcquired` 가 한 번도 안 걸렸다)
RoleNavFilter.tsx  두 갈래 모두 `withCurrentPath` 를 지나고, 두 갈래 모두 **모양**으로
             끝났는지 본다(종전 `pruned.changed` 판정이면 되돌린 줄 때문에 **끝나지 않는
             갱신**이 난다 — 시험 5절이 그것을 잰다) · `pathname` 을 의존성에 넣었다
```

⚠ **값을 숨기지 않는다**: 그 화면에 **서 있는 동안** 사이드바 줄이 하나 는다(U5 는 7 → 8).
  떠나면 다시 7 이다. 역할 홈에서 세는 **「메뉴 ≤ 7」은 그대로**다(시험 3절).
⚠ **권한을 열지 않았다.** 줄을 남기는 것은 화면 결정이고 판정은 서버가 한다
  (`core/role/permission.py::path_permission` · 권한 없는 계정은 `ListRealityNote` 가
  「볼 권한이 없습니다」로 말한다 — 턴 V 에 그 갈래를 닫아 뒀다).

### ★★ 같은 뿌리에 **다른 차선의 빨강 둘**이 걸려 있다 (쪽지 보냄)

| 빨강 | 소유 | 왜 0행인가 | 종전 판독 |
|---|---|---|---|
| **U4#11** `/device` | U24 | `NAV_ALLOW.U4` 는 둘뿐이라 뗀다 | 「데이터 상태(권한)」 — **틀렸다** |
| **U2#6** `/report-template` | U24 | `NAV_ACQUIRED_HIDDEN` 넷 | 「화면이 그 문을 안 부른다」 — 맞지만 **원인이 반대** |

내 고침이 세 자리를 한꺼번에 덮는다. **U24 의 행 색은 내가 안 칠한다** — 쪽지만 보냈다.
★ 반대 증거: `/users`(menuId 3)는 U5 표에 **있어** 남았고, 같은 인수 표인데 **29명 +
  Add New User 가 그려진다**(09-07 · 턴 AA 재확인). 갈린 것은 **표에 있느냐뿐**이다.

### ⚠ 못 한 것 — **화면에서 누른 뒤를 못 봤다**

3002 은 **정적 dist** 다. 앞판 소스를 고쳐도 **빌드 전에는 화면이 안 바뀐다.**
빌드는 힙 6GB 를 쓰고, **엔진이 방금 동시 exec 폭주로 죽었다.** dist 를 갈아 끼우면
지금 재는 다른 차선의 화면이 중간에 바뀐다. **그래서 안 했고, 조율자에게 창을 청했다.**
⇒ **U5#2 는 내 손에서 초록이 아니다.** 고침과 재현 시험까지다.

---

## ② U5#10 채널 「사람에게 닿는 채널 ≥ 1」 — **초록 조건을 세우고, 실제로 세워 봤다(되돌림)**

### 서버 쪽은 턴 AA 에 이미 갈라 놓았다 — 이번 턴에 **화면의 머리글이 아직 거짓이던 것**을 찾았다

```
[실측 2026-09-21 20:5x · ORM · notify_rule_overview(gxseed_u5_sysop)]
  critical_recipient_count = 4      ← **사람은 있다**
  critical_human_channels  = []     ← 채널만 훈련용
  critical_blocked         = True
  사유(서버)               = 「…사람은 4명 있지만, 그 규칙의 채널이 사람에게 닿지 않는…」
```

그런데 화면의 **머리글**은 `CRITICAL_BLOCKED_TITLE` 하나였다 —
**「지금 심각 경보를 받는 사람이 없습니다.」** 사람은 **4명 있다.** 본문(사유)은 턴 AA 에
갈랐는데 **사람이 먼저 읽는 줄이 여전히 틀린 말을 하고 있었다.** 그 줄만 보고 간 운영자는
역할에 사람을 더 넣으러 가고, 넣어도 빨강이 안 풀린다.

### 고친 것 — 머리글 · 대비책 · 초록 조건

```
CRITICAL_NO_HUMAN_CHANNEL_TITLE  = 「심각 경보가 사람에게 닿지 않습니다 — 채널이 훈련·검수용뿐입니다.」
blockedByNoPeople(data)          머리글만 고른다. **막혔는지는 서버가 정한다**(두 벌 금지 · D-212)
fallbackBlockReason(data)        서버 문장이 없을 때도 **둘을 안 뭉친다**(종전 대비책은 뭉쳤다)
빨강 안에 한 줄                   「초록이 되는 조건: 역할에 사람 1명 이상 · 채널에 사람에게 닿는 것 1개 이상」
```
⚠ 별표(마크다운)를 안 썼다 — 이 상자는 마크다운을 안 그린다(서버 쪽이 같은 이유로 같은 금지를
  적어 두었다). 굵게는 `<Text strong>` 으로 했다.

### ★ 초록 조건이 **실제로 서는지** 창에서 눌러 보고 되돌렸다

```
[실측 2026-09-21 21:0x · 8000 · gxseed_u5_sysop]
  전:  blocked=True  · human=[]        · 사람=4
       심각 규칙 [(9,fire_user,['log'],켜짐) (10,operator,['log'],꺼짐)
                  (11,fire_admin,['log'],켜짐) (12,view_only_-_anyang,['log'],켜짐)]
  누름: POST /settings/notify-rules/save  rule 9 · channels=email,log   -> **200**
       응답 channels ["email","log"] · audit_id 320698
  후:  blocked=**False** · human=**['email']** · 사람=**6** · 사유 **빈 문자열(초록)**
  [되돌림] 같은 문으로 channels=log -> 200
  되돌린 뒤: blocked=True · human=[] · 규칙 9 채널 ['log'] · **원래와 같은가 = True**
```

★ **사람 수가 4 → 6 으로 움직였다.** `resolve_recipients` 가 채널에 따라 달라지기 때문이다 —
  즉 「N명」은 채널과 **독립된 수가 아니다.** 이 사실은 화면이 초록에 채널 이름을 함께
  적어야 하는 또 하나의 이유다(턴 AA 에 붙여 둔 그 줄).
★ 채널 레지스트리 실측: `email`(붙음·사람) `webpush`(붙음·사람) `log`(붙음·**사람 아님**)
  `push`·`sms`·`webhook`(자리만 · 아직 안 나감). ⇒ **초록으로 가는 길이 실재한다.**

⚠ 저장 문(`save_rule`)의 **409 는 이번에도 안 넓혔다** — 여전히 「심각 수신자 0명」만 거절한다.
  즉 **배지가 저장 문보다 엄격하다**(안전한 방향). 넓히면 시험 100건의 계약이 바뀐다 — 다음 턴.
⚠ 화면의 **제목 1**(U5#10 정본 술어의 둘째 칸)은 **못 봤다** — 빌드 전이다.

---

## ③ U5#15 사용량 % 상한 — **상한은 이미 선언돼 있다. 정본이 낡았다**

```
[실측 2026-09-21 20:5x · ORM · common.ops_tasks.storage_declaration()]
  declared=True · capacity_gb=50.0 · used_gb=0.0012 · used_pct=0.0 · verdict=OK
  env_name=GX_STORAGE_CAPACITY_GB · capacity_source=「환경 선언 — …컨테이너 환경에 적혀 있습니다」
```

- **내 화면 `/dsm/system`(SystemSettings.tsx)은 이미 넷을 그린다**: 상한 · 쓰는 중 · **사용률** ·
  어디서 선언하나. 그리고 **미선언이면 수를 안 그리고 회색**(「판정 불가」 + 서버 사유)이다 —
  P-223 이 요구한 모양 그대로다. **이번 턴에 이 파일은 고칠 것이 없었다.**
- 그런데 **U5#15 의 정본 경로는 `/dsm/metering`** 이고, 그 화면에는 `%` 도 상한도 없다
  (`Metering.tsx` grep 0건). **그 파일은 이번 턴 B 의 일**(「이번 달 사용량」에 훈련·시험 제외
  표기)과 같은 자리라 **한 줄도 안 건드렸다.** 붙일 문과 지킬 규율 셋을 `B.inbox/U56.md` 에 적었다.
- ⚠ 온보딩 정본의 U5#15 근거 「**상한 미선언**」은 **틀렸다** — 선언돼 있다. 남은 것은
  「그 수를 `/dsm/metering` 이 안 그린다」뿐이다. **넘김(B).**

---

## ④ 역할 표시명 사전 §4-2 정정판 — **등재했다. 등재 전에 다시 쟀다**

```
[실측 2026-09-21 20:4x · ORM · role.Role 전수 **16종** · id / role_name / code]
   1 SuperUser              superuser        12 fire_user              fire_user
   3 User                   user             13 fire_admin             fire_admin
   4 admin                  admin            17 Surveillance Order     surveillance_order
   5 operator_etri          operator         18 Surveillance Operation surveillance_operation
   6 order_etri             order            22 delivery admin         delivery_admin
   7 delivery operation     delivery_operation  28 tenant_admin_4      tenant_admin_4
   8 delivery order         delivery_order
   9 View Only - Anyang     view_only_-_anyang
  10 drone_robot_user       drone_robot_user  11 drone_robot_admin     drone_robot_admin
```

**⇒ §4-2 의 코드 여덟은 `code` 칸과 한 자도 안 틀린다. 조율자 실측이 맞다. 등재했다.**
세종의 `view_only`·`sysop` 오기는 정정판으로 사라졌다(P-233).

★ **그런데 `role_name` 은 `code` 가 아니다** — `operator_etri` ≠ `operator` ·
  `View Only - Anyang` ≠ `view_only_-_anyang` · `delivery operation`(공백) 등 **여섯이 다르다.**
  이 사전의 열쇠는 **`code`** 다. `role_name` 을 대면 전부 「표시명 없음」이 된다.
  부르는 쪽이 무엇을 넘기는지가 이 사전의 정확도를 정한다 — 파일 머리말에 박았다.

★ **접두 일치·유사도 금지 규칙은 그대로 뒀다.** 그 규칙이 없었다면 턴 AA 에
  `view_only_-_anyang` 이 `view_only` 의 표시명을 달고 나갔을 것이고, **세종의 오기는
  화면에서 안 보였을 것**이다. 규칙이 오기를 드러냈다 — 그 사실을 머리말에 적었다.

★ **비운 여덟도 이름으로 적었다** — `ROLE_NAMES_BLANK_BY_DECISION`(사유가 값이다).
  「깜빡했다」와 「안 붙이기로 했다」를 가른다. `verify_ui_copy` 가 `*_BY_DECISION` 을
  **선언으로 인정**한다(이번 회차 「선언으로 건너뛴 조각 8개」).

★ **16 − 8 = 8.** §4-2 는 이 묶음을 「인수 **9종**」이라 적었으나 **세면 여덟**이다.
  수를 옮기지 않고 센 수를 적는다.

### ⚠ §4-2 와 저장소가 **갈린 자리 하나** — 조용히 맞추지 않았다

§4-2 는 `surveillance_order`·`surveillance_operation` 을 「표시명 없음 · GX 테넌트 배정 금지」에
넣었는데, 이 저장소는 그 둘을 **이미 사람에 잇고 있다**:
`roleNav.NAV_ROLE_CODES`(→U1·U2 · 사이드바 5줄·7줄 · 영어 0) · `k3_roles`(OPERATORS·MANAGERS).
또 닫는 조건의 「사람이 정해진 **7역할**」과 §4-2 의 **등재 8**은 같은 묶음이 아니다.
**§4-2 를 따르되 갈린 사실을 코드와 보고에 남겼다.** 지금 이 갈림의 손해는 **0** 이다 —
그 둘에 배정된 계정이 **전 테넌트 0명**이기 때문이다(아래 ⑤). 정본 판정을 청한다.

---

## ⑤ ★ P-229 표 밖 역할 배정 실측 — **0 이 아니다. 여덟이다**

```
[실측 2026-09-21 20:5x · SQL · is_active · deleted 없음 · user_profile_link 로 테넌트 조인]

★ GX 테넌트(ETRI-Group)에서 표 밖 8역할을 가진 살아 있는 계정 = **8명** — 전부 `order`
     order_chi · order_chungnam · order_user · order_user_1 · order_user_2 ·
     order_user_3 · order_user_4 · order_user_5

다른 테넌트(참고)
     delivery_admin      32  (Anyang 11 · Gaion 11 · Thailand 8 · Gongju 1 · 소속 없음 1)
     delivery_operation   3  · delivery_order 5 · drone_robot_admin 6 · drone_robot_user 3
     order                9  (ETRI-Group 8 + GeumSan-ETRI 1)
★ surveillance_order · surveillance_operation = **전 테넌트 0명**

역할별 살아 있는 계정(전 테넌트 · 참고): admin 9 · fire_user 8 · fire_admin 4 ·
     view_only_-_anyang 3 · operator 10 · user 8 · superuser 13 · tenant_admin_4 1
```

**⇒ 「0 이면 영어 줄은 손대지 않는다」의 전제가 안 선다.** 고객 테넌트에 여덟 명이 있고,
그 계정으로 들어오면 **영어 사이드바가 열 줄 넘게 뜬다**(인수 넷만 뗀 나머지).

**제안(집행 0 · 기관 관리자 몫)**: 여덟은 전부 `order`(배송 주문)이고 `k3_roles` 가 이미
「이 제품의 역할이 아니다」로 판정한 코드다. ① 실제 GuardianX 사용자면 그 사람의 일에 맞는
DSM 역할로 **재배정**, ② 배송 제품의 잔재면 **GX 테넌트 밖으로**.
**나는 한 행도 안 바꿨다**(배정 0 · 삭제 0 · 비활성화 0).

---

## ⑥ U6#3 · U6#4 — **둘 다 제품이 아니라 판정기가 낸 빨강이었다**

### ㉠ U6#3 「JWT 404」 — 제품은 **200** 이다

```
[실측 2026-09-21 21:0x · 8000 · gxseed_u5_sysop JWT]
  GET /api/dsm/events                 -> 200 · total 28 · id [342448, 326411, 305027]
  GET /api/dsm/events/342448          -> **200**
  GET /api/dsm/events/326411          -> **200**
  GET /api/dsm/events/{id} X-API-Key  -> 401     ← D-371 의 **의도된 절반** 그대로
  GET /api/dsm/events/987654321       -> 404     ← 판정기가 **씨앗 없이** 두드리던 자리
```

`measure_onboarding_t.py:1551` 이 `ev = seed_b or U6_ABSENT_EVENT(987654321)` 다.
**턴 Z 에 Q 가 「가설」로 남긴 것이 이제 실측으로 확인됐다.**
⇒ U6#3 의 ○ 는 **도구의 자료 상태**다. 씨앗을 실으면 **◐(상한)** 이다.
⚠ 마지막 전량 회차(`…034646.json`)도 JWT=404 였다 — 그 회차에 `seed_b` 가 없었거나
  ETRI-Group 밖이었다는 뜻이다. **넘김(V — 씨앗 확인).**

### ㉡ U6#4 「웹훅 서명키 시드(422 닫기)」 — **시드할 것이 없다.** 한 걸음 문이 이미 있다

```
[실측 2026-09-21 21:0x · 같은 자격]
  ① POST /api/dsm/webhook-subscriptions?…&signing_key_ref=p118-gate        -> **422**
       「서명키 'p118-gate' 의 값을 이 환경에서 찾을 수 없습니다…」   ← 옳은 거절이다
  ② POST /api/dsm/settings/webhook-subscriptions/issue?endpoint_url=…      -> **200**
       구독 id 4 · 서명키 이름 wh_g4_ca94ffc851
       비밀: **길이 64 · sha256 앞12 = 950950542f4b**   (값은 어디에도 안 적는다)
       응답 칸 [audit_id, endpoint_url, event_types, filters, is_active, min_severity,
                payload_format, signing_key_name, signing_key_secret, subscription_id]
  [되돌림] DELETE /api/dsm/webhook-subscriptions/4 -> 200
       확인: gx_webhook_subscription #4 `is_active=False` (행은 남는다 — 제품의 규율)
```

⇒ 422 는 **제품이 옳게 낸 거절**이다(값 없는 이름으로 등록하면 「등록은 됐는데 못 보내는
구독」이 생긴다). 닫는 길은 **환경에 비밀을 넣고 서버를 다시 세우는 것이 아니라**,
판정기가 **제품의 문**을 부르는 것이다 — 그 문이 서명키를 그 자리에서 만들어 구독에 문다
(P-145 · `apps/dsm/webhook_key_service.py`). §4-4 의 U6 카드 ②도 그 문이 정본이다.
**판정기 파일은 내 소유가 아니라 한 줄도 안 고쳤다** — 고칠 한 줄을 쪽지에 적었다. **넘김(V·Q).**

⚠ 함께 볼 것: 해지는 **행을 안 지운다**. `_count_rows` 가 목록 길이를 세면 `n1 > n0` 이
  회차마다 참이 되고 되돌린 뒤에도 수가 안 준다.
⚠ **내가 남긴 것 하나**: 비활성 구독 행 **#4** 한 줄(그 표의 유일한 행). 제품의 해지 문을
  그대로 썼다 — **삭제 0** 이므로 지우지 않았다. 숨기지 않고 적는다.

---

## ⑦ 잰 것 · 안 잰 것 · 도구

### 타입 총량 — **기준선이 이미 3234 가 아니다. 내 몫은 0 이다**

```
[실측 2026-09-21 21:1x · gx-fe-build:/tmp/u56ab 격리 복사 · 원본 /app 무손상]
  ① 작업본 그대로                               TSC_EXIT=2 · 총 오류 줄 **3238**
  ② 내 앞판 4파일만 HEAD(0b7222f)로 되돌린 판    TSC_EXIT=2 · 총 오류 줄 **3238**
  ③ 두 판을 정렬해 comm 양방향                  **차이 0줄**
  ⇒ 내 넷이 더한 오류 **0**. 3234 → 3238 의 **+4 는 같은 작업본의 다른 차선 것**이다.
  오류가 몰린 자리: `features/delivery/**`(인수 자산) 상위 일곱 파일이 400줄 넘는다.
  잔여 0 — `/tmp/u56ab` 삭제 확인.
```

### 게이트

```
PYTHONIOENCODING=utf-8 python scripts/verify_ui_copy.py     → **rc 0** · 새 위반 0
  본 비율 100%(한글 18,852자 전수) · 잔여 82(기준선 84 · **갚은 것 2**)
  표시명 사전 3/3 — `ROLE_DISPLAY_NAMES`(U56) 가 선 것으로 인정됐다
  선언으로 건너뛴 조각 8 — 새로 만든 `ROLE_NAMES_BLANK_BY_DECISION` 이 여기 든다
⚠ 20:4x 에 같은 게이트가 `NameError: skipped_by_declaration` 로 죽었다(내 것이 아니다 ·
  남의 작업 중으로 보인다). 21:1x 에 다시 도니 rc 0 이었다. **그 사이 수는 수가 아니다.**
node scripts/repro_u5_2_roles_zero_rows.mjs                 → **rc 0** (음성 대조 rc 1 · 7건)
```

**뒷단 시험은 안 돌렸다** — 이번 턴 `backend/**` 변경이 **0줄**이고, 조율자가 「전량 금지 ·
좁혀서」라고 했으며, 엔진이 방금 죽었기 때문이다. 돌릴 새 계약이 없다.

### 시각 · 로그인

| 시각(KST) | 무엇 | 어디서 |
|---|---|---|
| 20:3x | 규약 · WO §3·§4-2·§4-4·§5 · 턴 AA 기록 · 온보딩 U5·U6 읽기 | 호스트 |
| 20:3x~20:4x | `/roles` 뿌리 추적 — `App.tsx` · `ListRealityNote` · **rj-core 번들 사슬 넷** | 호스트 · gx-fe-build |
| 20:4x | `role.Role` 전수 16 · `code` 칸 실측 · dj-core `core/role/api.py` 읽기 | gx-shell |
| 20:4x~20:5x | ★ **도커 엔진 500 (전 API)** — 내가 안 건드렸다. 재기동 안 했다 | — |
| 20:5x | 조율자 복구 뒤 재측: 역할별 계정 · **P-229 여덟** · 저장 선언 · notify 도달 | gx-shell |
| 21:0x | **창** — `gxseed_u5_sysop` 로그인 4회 · `/api/roles/` 16 · U5#10 A/B(되돌림) · U6#3·U6#4 | gx-shell 안 8000 |
| 21:1x | 격리 타입검사 A/B · `verify_ui_copy` · 재현 시험 · 보고·쪽지 셋 | 호스트 · gx-fe-build |

- **로그인 4회 · 전부 `gxseed_u5_sysop`.** `gxseed_u4_official` 창은 **안 썼다**(0회) —
  이번 턴 내 일이 U5·U6 면이었고, 남는 창을 붙잡고 있지 않았다.
- **브라우저 0회.** 전부 HTTP·ORM·순수 함수다. ⇒ 「화면에 그렇게 뜬다」는 **안 쟀다.**
- **서버 재기동 0 · 앞판 빌드 0 · 커밋 0 · 삭제 0 · `--no-verify` 0.**
- 비밀 출력 0 — 서명키는 **길이 64 · sha256 앞 12자**로만 적었다.

---

## ⑧ 못 한 것 · 재지 못한 것 — 숨기지 않는다

1. **앞판 넷 중 어느 것도 화면에서 못 봤다.** 3002 은 정적 dist 이고 빌드를 안 했다(이유 ①).
   U5#2 · U5#10 의 화면 칸은 **내 손에서 회색**이다.
2. **U5#15 를 정본 경로(`/dsm/metering`)에서 못 닫았다** — 그 화면은 B 의 자리다. 쪽지만.
3. **U6#3 · U6#4 를 내가 닫을 수 없다** — 둘 다 판정기(V) 파일의 한 줄이다. 실측과 고칠 줄만 적었다.
4. **표 밖 여덟 계정을 재배정하지 않았다** — P-229 대로 **제안만**. 집행은 기관 관리자.
5. **`surveillance_*` 의 정본(§4-2 냐 roleNav·k3_roles 냐)을 못 정했다.** 판정을 청했다.
6. **저장 문 409 를 안 넓혔다** — 배지가 저장 문보다 여전히 엄격하다(안전한 방향).
7. **타입 총량 +4 의 출처를 못 짚었다** — HEAD 전체를 다시 치는 한 판이 더 필요하고,
   엔진이 방금 죽었다. **내 몫이 0 이라는 것만 A/B 로 못 박았다.**
8. **비활성 구독 행 #4 하나를 남겼다**(제품의 해지 문 · 삭제 0).
9. **앞판 시험 달리개가 없어** 내 재현 시험이 `node` 단독이다 — CI·훅에 안 걸려 있다.
   `.pre-commit-config.yaml` 은 내 소유가 아니라 배선을 안 했다. 다음 턴 Q 후보.

## 건드린 파일

```
고친 것 (전부 내 소유)
  frontend/src/features/nav/roleNav.ts
  frontend/src/features/nav/RoleNavFilter.tsx
  frontend/src/features/dsm/roleNames.ts
  frontend/src/features/dsm/pages/NotifySettings.tsx
새로 만든 것
  scripts/repro_u5_2_roles_zero_rows.mjs
  docs/agent/checkpoints/turn-ab/U56.md
  docs/agent/checkpoints/turn-ab/조율자.inbox/U56.md · U24.inbox/U56.md · B.inbox/U56.md
```

## 금지구역

`backend/delivery/` · `orders/` · `terminals/` · `frontend/src/features/{Delivery,Orders,Terminal}` ·
`rj-core` · `dj-core` · `MapForRoute*` · `FormRoute.tsx` — **한 줄도 안 고쳤다.**
`rj-core` 는 **번들을 읽기만** 했고(`dist/rj-core.es.js`), 읽은 것이 이번 턴 ①의 전부다.
`backend/common/billing_marks.py` · `backend/apps/dsm/metering.py` — **읽기만**(B 소유).
