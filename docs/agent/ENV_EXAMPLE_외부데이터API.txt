# ============================================================================
# GuardianX — 외부 데이터 API 환경변수 템플릿  (.env.example)
# ----------------------------------------------------------------------------
# ★ 이 파일에는 실제 키를 절대 넣지 않는다 (D-204·D-319). 변수 이름과 설명만 둔다.
# ★ 실제 값은 C:\GuardianX-vault\ → 로컬 .env (gitignore 대상) → 환경변수.
# ★ verify_secrets(gitleaks) 게이트가 실값 커밋을 exit 1 로 막는다.
#
# ★ D-316 — 외부 자원은 「문서에 있다」가 아니라 「계정에서 보았다」로만 등재한다.
#   각 항목에 source 를 단다.  account_verified = 계정 화면에서 확인함
#                              doc_only         = 문서에만 있음 → 어댑터 금지
#   verify_external_sources.py 가 doc_only 항목을 참조하는 어댑터를 exit 1 로 막는다.
#
# 실측 근거: data.go.kr 활용신청 현황 **18건 전수** (2026-09-04 · 전건 승인)
# 갱신: 2026-09-04
# ============================================================================

# ── P0 · 재난 판정에 직접 쓰이는 것 ─────────────────────────────────────────

# 공공데이터포털 (data.go.kr) 일반 인증키 — 아래 포털 API 전부 공통 1개
# 인코딩본(%2B·%3D%3D 포함)과 디코딩본 둘 다 저장. 라이브러리에 따라 쓰는 쪽이 다르다.
# source: account_verified (2026-09-04)
DATA_GO_KR_KEY_ENCODED=
DATA_GO_KR_KEY_DECODED=

# 기상청_기상특보 조회서비스   ← FX-1 건조특보+연기 / FX-2 호우특보+수위   ★핵심
# source: account_verified (2026-09-04 · 승인)
# ※ 엔드포인트는 포털 "활용신청 상세"에서 실측해 채운다. 추정 금지(D-280).
KMA_WARNING_URL=

# 한국천문연구원_특일 정보     ← FX-6 휴일 인지 알림 분기
# source: account_verified (2026-09-04 · 승인)
HOLIDAY_API_URL=

# 전국초중등학교위치표준데이터 ← 대피소·우선보호시설 컨텍스트
# source: account_verified (2026-09-04 · 승인)
SCHOOL_LOCATION_URL=https://api.data.go.kr/openapi/tn_pubr_public_elesch_mskul_lc_api

# ── P0 · 주소 (별도 계정) ───────────────────────────────────────────────────

# 주소정보 연계서비스 (juso.go.kr) — 포털 키와 별개 계정. 승인키 2건 발급됨.
# source: account_verified (2026-09-04 · 승인키 2건)
# ★ D-318 — 승인키가 둘이다. 값으로 구분하지 말고 **신청서의 API 명으로** 이름표를 붙인다.
# ★ D-318 — FX-5 가 요구하는 것은 「좌표 → 도로명주소(역지오코딩)」다.
#   juso 주력은 「검색어 → 주소」·「주소 → 좌표」이므로 **역방향 지원 여부를 실호출 1회로 판정**한다.
#   판정 전에는 어댑터를 만들지 않는다(D-298).
JUSO_API_KEY_1=            # 신청서 A — 실측 후 이름표를 이 줄 주석에 적는다
JUSO_API_KEY_2=            # 신청서 B — 동上
JUSO_REVERSE_SUPPORTED=unknown    # unknown | yes | no  ← unknown 인데 어댑터 있으면 exit 1

# ── P1 · 보조 (절 표가 나온 뒤 판단) ────────────────────────────────────────

# source: account_verified (2026-09-04 · 승인)
KMA_SHORT_FCST_URL=       # 기상청_단기예보 조회서비스
KMA_MID_FCST_URL=         # 기상청_중기예보 조회서비스
SMALL_BIZ_AREA_URL=       # 소상공인시장진흥공단_상가(상권)정보 — 취약시설 컨텍스트 후보

# ── 시설물 App 슬롯 예약 (계약 범위 밖 — 지금 만들지 않는다) ────────────────

# source: account_verified (2026-09-04 · 승인)
BLDG_HUB_URL=             # 국토교통부_건축HUB_건축물대장정보 서비스
LAND_USE_REG_URL=         # 국토교통부_토지이용규제정보서비스

# ── 신청되지 않음 / 불필요 — 어댑터 금지 (D-316·D-321) ──────────────────────

# 11. CCTV·재난안전정보
# source: doc_only · status: not_required
# ★ D-321 — 포털의 재난안전 CCTV 정보는 "어디에 CCTV 가 있는지"의 **메타데이터**이지 영상이 아니다.
#   우리는 CCTV 영상을 직접 받는다(L1 현장 연계). 설치 위치 목록은 우리 기능 어디에도 입력이 아니다.
#   이 항목이 잠금으로 남아 있던 이유는 하나 — **이름이 우리 도메인과 닮아서**다.
DISASTER_CCTV_URL=

# 5. 국립공원 안전정보 / 6. 아동센터·돌봄센터 안전정보
# source: doc_only · status: not_applied
# 활용신청 현황 18건에 없다. 컨텍스트 보강용이며 계약 AC 어디에도 걸리지 않는다.
NLPRK_SAFETY_URL=
CHILDCARE_SAFETY_URL=

# 기상청 API허브 (apihub.kma.go.kr) — 포털과 별개 계정
# source: doc_only · status: not_applied
# 포털 기상특보(위)로 FX-1·FX-2 가 충족되면 불필요. 절 표가 나온 뒤 판단한다.
KMA_APIHUB_KEY=

# ── 공통 동작 설정 (키 아님 — 커밋 가능) ────────────────────────────────────

EXTERNAL_API_TIMEOUT_SEC=5        # 외부 호출 타임아웃 (C-3.3 — 무제한 금지)
EXTERNAL_API_RETRY=1              # 재시도 1회. 실패 시 영상 단독 판정으로 저하 운전
EXTERNAL_API_CACHE_TTL_SEC=300    # K7 컨텍스트 캐시. 특보는 5분이면 충분
EXTERNAL_API_ENABLED=false        # ★ 기본 false. 스키마 실측·어댑터 완성 후 개별 활성화

# ============================================================================
# 제품 코드에 넣지 않는 것 (저장소 등재 금지 — D-317)
#   조달청 3종(계약·낙찰·공공데이터개방표준) · 관세청 수출입 · 금융위 4종 ·
#   국세청 사업자등록 · 대구교육청 방과후학교
#   → 조달청 3종은 지자체 관제 입찰 모니터링에 유용하나 **제품 기능과 무관**하므로
#     대표 참고 자료로만 쓰고 저장소에 넣지 않는다.
#
# 증거 남길 때 (D-319)
#   실호출 요청 URL 의 키 파라미터를 **반드시 마스킹**한다. evidence/ 는 커밋된다.
# ============================================================================
