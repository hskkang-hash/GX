# ============================================================================
# GuardianX — 외부 데이터 API 환경변수 템플릿  (.env.example)
# ----------------------------------------------------------------------------
# ★ 이 파일에는 실제 키를 절대 넣지 않는다 (D-204). 변수 이름과 설명만 둔다.
# ★ 실제 값은 C:\GuardianX-vault\ → 로컬 .env (gitignore 대상) 또는 비밀저장소로.
# ★ verify_secrets 게이트가 실값 커밋을 exit 1 로 막는다.
#
# 선별 근거: D-298 (공공 Data API 29종 중 제품 기능 관련만 등재)
# 갱신: 2026-08-30
# ============================================================================

# ── P0 · 재난 판정에 직접 쓰이는 것 ─────────────────────────────────────────

# 기상청 API허브 (apihub.kma.go.kr) — 별도 계정·별도 키
# 용도: FX-1 건조특보+연기=심각 상향 / FX-2 호우특보+수위 선제 경계
KMA_APIHUB_KEY=

# 공공데이터포털 (data.go.kr) 일반 인증키 — 아래 포털 API 전부 공통 1개
# 인코딩본(%2B·%3D%3D 포함)과 디코딩본 둘 다 저장. 라이브러리에 따라 쓰는 쪽이 다르다.
DATA_GO_KR_KEY_ENCODED=
DATA_GO_KR_KEY_DECODED=

# 기상청 포털 API 3종 — 엔드포인트는 포털 "활용신청 상세"에서 실측해 채운다.
# ★ 추정 금지(D-280). 확인 전에는 빈 값으로 두고 어댑터를 만들지 않는다.
KMA_WARNING_URL=          # 23. 기상특보 조회서비스   ← FX-1 핵심
KMA_SHORT_FCST_URL=       # 27. 단기예보 조회서비스
KMA_MID_FCST_URL=         # 25. 중기예보 조회서비스

# 11. CCTV·재난안전정보 — 문서에 항목만 있고 상세가 비어 있다.
# 이름이 우리 도메인과 직결되므로 대표 확인 후 최우선 등재. 확인 전 어댑터 금지.
DISASTER_CCTV_URL=
DISASTER_CCTV_SERVICE_NAME=   # 포털상의 정확한 서비스명 (동명 서비스 다수)

# ── P1 · 컨텍스트·시설·표기 ─────────────────────────────────────────────────

# 9. 주소정보 연계서비스 (juso.go.kr) — 좌표 → 도로명 주소
# 용도: FX-5 이벤트 위치를 사람이 읽는 주소로 (F-10 알림·F-11 보고서에 직결)
# ※ 포털 키와 별개 계정. 승인키를 따로 받는다.
JUSO_API_KEY=
JUSO_API_URL=https://business.juso.go.kr/jst/jstRoadNmAddrApiSearch

# 24. 한국천문연구원 특일 정보 — 공휴일·대체공휴일
# 용도: FX-6 휴일 인지 알림 (평일 주간 담당자 ↔ 휴일 당직 그룹 분기)
HOLIDAY_API_URL=          # 포털 활용신청 상세에서 실측

# 3./29. 전국초중등학교위치표준데이터 — 대피소·우선보호시설
SCHOOL_LOCATION_URL=https://api.data.go.kr/openapi/tn_pubr_public_elesch_mskul_lc_api

# 5. 국립공원 안전정보 — 산악 화재·조난 컨텍스트
NLPRK_SAFETY_URL=
NLPRK_SAFETY_SERVICE_NAME=

# 6. 아동센터·돌봄센터 안전정보 — 취약계층 우선 대피
# ★ 대표 문서에 5번과 같은 엔드포인트(/getNlprkSafetyInfoSearch_4)가 적혀 있다.
#   최소 하나는 다른 API다. 포털 마이페이지에서 확인 전까지 비워 둔다.
CHILDCARE_SAFETY_URL=
CHILDCARE_SAFETY_SERVICE_NAME=

# ── 공통 동작 설정 (키 아님 — 커밋 가능) ────────────────────────────────────

EXTERNAL_API_TIMEOUT_SEC=5        # 외부 호출 타임아웃 (C-3.3 — 무제한 금지)
EXTERNAL_API_RETRY=1              # 재시도 1회. 실패 시 영상 단독 판정으로 저하 운전
EXTERNAL_API_CACHE_TTL_SEC=300    # K7 컨텍스트 캐시. 특보는 5분이면 충분
EXTERNAL_API_ENABLED=false        # ★ 기본 false. 어댑터 완성·실측 후 개별 활성화

# ============================================================================
# 제품 기능에 쓰지 않는 것 (등재하지 않음 — D-298)
#   NEIS · 대구교육청 방과후학교 · Open DART · 국세청 사업자등록 · 금융위 4종 ·
#   관세청 수출입 · 가맹사업 · 소상공인 상가 · 범죄정보(개인정보 부담)
#   건축HUB · 토지이용규제 → 시설물 App(향후 슬롯) 예약. 계약 범위 밖
#
# 사업개발용 (제품 코드 아님, 대표 참고)
#   조달청 나라장터 계약·낙찰·공공데이터개방표준 3종
#   → 지자체 관제 입찰 모니터링에 유용하나 제품 기능과 무관하므로 저장소에 넣지 않는다
# ============================================================================
