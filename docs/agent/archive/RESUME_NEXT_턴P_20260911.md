# RESUME_NEXT.md — GuardianX 초효율 작업지시서 · 턴 P (2시간 · 호출은 나갔는데 상태가 안 변한다 — 여섯 곳의 뿌리 하나 · 커밋은 이번 턴 첫 30분에 끝난다 · 로그인 뒤 첫 화면은 역할 홈이다)

- 발행: **2026-09-11 10:00(기계) · 턴 P** · 발행자: 세종(CPO) · 수신: 영실(CPM · 조율자)
- 직전 보고(턴 O · 09-10 18:33 · **커밋 없음 · 네 턴째** · HEAD `6cf2c19` · 작업본 **239파일**) 읽음: 상용 **67.8**(+0.0 — 고친 것이 이미 「구현」인 절 안) · **`click_completes` 48흐름 첫 실측 11/48**(초록 11 · 빨강 13 · 회색 24 · U1 3/8 · U2 **0/8** · U3 2/8 · U4 0/8 · U5 2/8 · U6 4/8) · 걷기 U1 빨강→**통과** · **확인창 층 살아남**(import + 재빌드 · 「실제로 판정」 요청 0 → `POST …/review` · verdict None→confirmed · 11곳) · **P0 셋 닫음**(익명 OTP 401 · 읽기 전용 쓰기 0→367/377 차단 · 회귀 0 · **74 회전 검증 · 옛 단어 0**) · **택배 운송장 끊음**(`GET …/report.pdf` 1쪽 · 택배 필드 0 · md5 사건마다 다름) · 사진 0px→640×360(3화면) · 종결 사건 도달 0/9→9/9 · 「그날 그 시각」→사건 1건 12.7초 · 단위 1,283/0/3(+56) · `deploy.sh` exit 0 ×2 · **정정 자진 둘**(`unstableSetRender` 검사 불성립 → `_reactRoot` · 「74 회전」 추정 안 함) · **TOP 5**: ① 25절 회색 · 「서버는 주는데 화면이 안 그린다」 넷(상황판 preset · 실시간 counts · 역할 부여 preset · 웹훅 total) · 자격 형식 6/7(MinIO 5·5자 · DB 5자 · 서명키 `local-bringup-only-not-a-real-key` · 운영 프로필 서버 0) · 익명 읽기 5 여전히 200 · 공개 URL 0 · 남은 턴 9 · **TIME_ZONE 사건 시각 2시간 이름**(대표 ④로 올림) · **재시작 정책은 창 없이 한 줄**(`docker update --restart=unless-stopped`) — 영실 [실측].
- 규칙(대표): ① 삭제·되돌리기 외 선택은 세종 판정으로 집행 ② 멀티 에이전트 ③ 2시간 권고·짧은 턴은 이어서 ④ 사업 문서는 개발을 막지 않는다.

---

## §0 상용 기준 완료율 — 첫 표 8줄 (이번 보고에 없어 세종이 채운다 · P-117 세 번째 미이행)

| 수 | 지금 | 출처 | RC-1 |
|---|---|---|---|
| 상용 /100 | **67.8** [실측] | 대장 139절 · ① 영역 게이트 | 88 |
| 손 안 도달율 | **70.2** [실측 · 턴 M] | — | 100 |
| 온보딩 48행 | **60** [실측 · 턴 L] · 재측 없음 | — | ≥ 80 |
| **FC 기능 완결(v0.2)** | **하한 22.9 · 상한 72.9** [게이트 실측 11/48 · 회색 24 = 못 잼] · 손 셈 33.3(턴 O 이전) | `verify_click_completes` | ≥ 70 |
| PR 상용 준비 | **20.0** [실측 · 09-08] · 이번 보고 없음 | GX-FCPR §2 | ≥ 80 |
| CR 돈 내는 8조건 | **12.5** [실측 · 09-08] · 이번 보고 없음 · 세종 판정 ②⑧이 1로 올라갈 근거(74 회전 · OTP · 읽기 전용 · 「그날 그 시각」 12.7초) → **37.5 [판정]** — 실측이 덮는다 | REVIEW §1 | ≥ 75 |
| 8영역 | 미보고 | — | — |
| 커밋 | **HEAD `6cf2c19` · 239파일 · 네 턴째 보류 · 사유 없음** · 보험 패치 `turnO_…patch` 7.0M | — | — |

**G1~G5**: G1 ◐(공개 URL 0 · 운영 프로필 0) · G2 **◐→●**(OTP · 읽기 전용 · 74 회전 · 익명 읽기 5만 남음) · G3 ◐(확인창 살아남 · 걷기 통과 · **U2가 화면에 못 들어감**) · G4 ◐(67.8 · ① 25절 회색) · G5 ◐(회수증 수동 1 · 재시작 정책 없음 · TIME_ZONE).

---

## §1 인정 · 오판 셈

1. **확인창 층이 살아났다** — 요청 0 → `POST …/review` · verdict None→confirmed · 11곳. 그리고 근거를 **정정**했다(`unstableSetRender`는 minify가 이름을 바꿔 성립하지 않는 검사 → `_reactRoot` · 두 차선 A/B). 결론은 맞았고 증거 한 줄이 틀렸다고 스스로 적은 것 — 옳다.
2. **P0 셋을 한 턴에 닫았다** — OTP 401(존재 신탁 404도 제거) · 읽기 전용 0→367/377 · 회귀 0 · 74 회전 검증. 「74개 회전됐다」를 S 보고 전에 추정하지 않고 기다린 것 — 옳다.
3. **택배 운송장을 끊었다 · 「그날 그 시각」 12.7초** — U4의 첫 질문에 답이 생겼다. 종결 사건 9/9 도달. 인정.
4. **`click_completes`가 태어났다** — 술어 넷(실재 · 나감 · 다시 읽어 변함 · 화면이 말함) · 면제 칸 없음 · 출생 표본 3/3(「그려졌다」로 0.5 받던 단추 셋을 0으로). 첫 실측 11/48 — 그리고 **회색 24를 「못 쟀다 · 초록이 아니다」로 이름을 적었다.** 인정. 이것이 이 저장소가 가장 경계하는 모양을 지킨 게이트다.
5. **「서버는 주는데 화면이 안 그린다」를 단일 병증으로 이름 붙였다** — 사진 · 판정 · 상황판 · 실시간 · 역할 부여 · 웹훅 여섯 곳 같은 모양. 옳다 — 그래서 이번 턴은 여섯을 따로 고치지 않고 **뿌리 하나를 먼저 잰다**(P-129).
6. **TIME_ZONE 2시간 · 재시작 정책 한 줄** — 둘을 대표 결정으로 올린 것은 신중했다. 그러나 둘은 **삭제·되돌리기가 아니고 되돌릴 수 있다** → 세종 위임 안이다. 세종이 판정으로 내린다(P-127 · P-128). 대표 결정 목록에서 뺀다.
7. **커밋 없음 · 네 턴째 · 사유 없음** — 「사유 없는 커밋 보류는 없다」는 턴 N에 불변으로 올랐다. 두 턴 연속 위반이다. **영실 오판 +1(형식 아닌 불변 위반).** 이번 턴 첫 30분에 끝낸다(P-126). 보험 패치가 있어 손실은 없다 — 그러나 239파일이 세상에 한 벌인 상태를 네 턴 끌었다.
8. **첫 표 8줄(P-117) 세 번째 미이행** — 이번엔 세종이 §0에 대신 채웠다. 다음 보고부터 첫 표 없는 보고는 V가 회색으로 세고, 세종은 그 보고의 수를 인용하지 않는다.
9. **U2 팀장 8행이 전부 회색 — 「그 사람으로 화면에 들어가지 못했다 · /login에 머문다」**. 74 회전 뒤 게이트 계정(`gxseed_u2_manager`)의 `.env.gates`가 갱신되지 않았을 가능성이 첫째다 — 턴 O 함정 ②(「회전하다 게이트 계정을 끊지 않는다 — `AS=` 목록 먼저」)가 실현됐는지 **첫 20분에 잰다**(P-130). 실현됐으면 영실 오판 +1(이번 보고에 「U2 0/8」의 원인이 없다).

**세종 오류 0 · 누계 23 · 영실 오판 1(+U2 원인에 따라 1) · 위임 이의 0 · 세종 미판정 0.**

---

## §2 판정 — 세종이 정해 집행 (삭제·되돌리기·운영계 외부 행위 없음 · 있으면 대표)

### P-126 커밋 — 이번 턴 첫 30분 · 셋으로 갈라 · 사유 없는 보류는 없다 [판정 · 조율자]
- 순서: 보험 패치 갱신(239) → 게이트 14벌 → **막는 게이트가 있으면 이름·빨강 사유를 첫 줄에** → 없으면 **커밋 셋**: ① `feat(P0): OTP 401 · 읽기 전용 403 · 74 회전 · 확인창 import` ② `feat(화면): 전체·기간 · 사진 · 메뉴 허용 목록 · 보고서 1쪽 · 문장 4종` ③ `test/docs: click_completes · 증거 · 지시서`. 셋이 각각 게이트를 통과해야 한다(예외 칸 없음 · `--no-verify` 없음). 재기동 → `live_freshness` 초록.
- 막는 게이트가 있는데 사유가 권한 밖이면 P-108 순서(격리 전/후 · 본 서버 회색 표기). **막는 게이트도 없고 커밋도 없으면 그것은 오판이다.**

### P-127 TIME_ZONE = Asia/Seoul — 세종 집행 · 대표 결정에서 내린다 [판정 · E]
- 실측 [세종이 저장소에서 읽음]: `settings.py:799 TIME_ZONE = env("TIME_ZONE", default="Asia/Ho_Chi_Minh")` · `:801 USE_TZ = True`. **UTC+7 기본값이 이전 고객의 흔적**이고, `USE_TZ=True`라 DB는 aware(UTC)로 저장한다 → 바꾸는 것은 **표시·naive 해석만**, 저장 데이터 재해석 없음. 삭제·되돌리기 아님 · 되돌릴 수 있음(env 한 줄) → **세종 위임 안**.
- 집행: `.env`에 `TIME_ZONE=Asia/Seoul`(비밀 아님 · `.env.example`에도 이름·기본값) → **반경 실측 먼저**: naive datetime을 받는 자리(보고서 기간 필터 · `since/until` · 크론 표현식 · 회수증 시각) 목록 → 격리(`gx_e`) 전/후: 사건 상세 시각 · 목록 「N분 전」 · 보고서 PDF 시각 · 대응 시계 4시각 · 걷기 → 본 서버 재기동. 닫는 조건: **화면 시각 = 기계 시각(KST) · 「표기 시간대: Asia/Seoul」 문구가 참이 된다.** DB 재해석이 필요한 자리가 하나라도 나오면 그 자리만 대표(dry-run 첨부 · D-209).
- 원칙 등재: **「기본값은 이전 고객의 것이다 — 이름을 보면 묻는다.」** `prod_settings` +1: `TIME_ZONE`·`LANGUAGE_CODE`·통화·국기 기본값이 KR인가.

### P-128 재시작 정책 — 세종 집행 · 창 없이 한 줄 [판정 · E]
- `docker update --restart=unless-stopped` — 정책 없는 여섯(`postgres` · `redis` · `gx-shell` · `gx-gunicorn-e` · `gx-nginx-e` · `gx-fe-build`). 무중단 · 되돌리기 한 줄(`--restart=no`) · 재생성 아님 → 세종 위임 안. **compose에도 같은 줄**(재생성 시 유지 — `restart: unless-stopped`가 이미 있는 셋과 같은 모양). 세 아침 연속 전체 다운의 해법. 전/후: `docker inspect` 정책 6/6 · 다음 아침 `live_freshness`.
- DB를 든 컨테이너가 정책 없이 떠 있던 것은 대장 OPS 절 하나로(회색 → 초록).

### P-129 「호출은 나갔는데 상태가 안 변한다」 — 여섯의 뿌리 하나를 먼저 잰다 [판정 · C+Q · 첫 일]
- 여섯(사진 · 판정 · 상황판 `preset` · 실시간 `counts` · 역할 부여 `preset` · 웹훅 `total`)을 따로 고치지 않는다. **가설 [추정 · 세종]**: 봉투 승격(D-349 · `API_CONTRACT_PROMOTE_PATHS` 접두 5)이 응답 모양을 바꿨고, 프런트의 응답 해석(`res.data` ↔ `res.data.data` · 성공 판정 필드)이 승격 전 모양을 읽는다 — 여섯이 승격 접두 안에 있는지 **대조표 한 장**이 첫 실측. 맞으면 고침은 **응답 어댑터 한 곳**이고, 아니면 여섯 각각의 이름·원인.
- `click_completes` 칸 ③「다시 읽어 변했는가」와 ④「화면이 말하는가」의 갈림(OOX? · OOOX)을 **제품 전역 술어**로 올린다 — 영실 제안 그대로. 닫는 조건: 넷 → 0 · 사진·판정 회귀 0.

### P-130 U2가 화면에 못 들어간다 — 첫 20분 [판정 · 조율자]
- `gxseed_u2_manager`가 `/login`에 머문다 → 원인 셋 중 어느 것인가 [실측]: ⓐ 74 회전이 게이트 계정을 함께 바꿨고 `.env.gates` 미갱신(함정 ②) ⓑ 세션 충돌(다른 하네스가 물고 있음) ⓒ 로그인 율제한 5/m. **ⓐ면 `.env.gates` 이름만 갱신(값은 저장소 밖) + 영실 오판 +1 · ⓑ면 하네스 세션 종료 순서 · ⓒ면 게이트 계정은 율제한 예외가 아니라 간격.** 고친 뒤 U2 8행 재측 — 팀장 8행은 FC 분모의 1/6이다.

### P-131 로그인 뒤 첫 화면은 역할 홈이다 [판정 · 제품 결정 · UX-32 · C]
- U1#1 빨강의 뜻: 로그인 뒤 `/profile`(dj-core `Personal Information`)에 떨어진다 — 첫인상이 다른 제품. **역할 홈**: U1 「지금 처리할 것」 · U2 「무슨 일 있었나」(밤사이 요약) · U4 「무슨 일 있었나」(7일) · U5 「관제 대시보드」 · 역할 0 「역할이 아직 없습니다」. `/profile`은 「내 정보」로 갈음(UX-29). 닫는 조건: 역할 계정 4 로그인 → 첫 URL이 역할 홈 · `click_completes` U1#1 초록. PRD v2.7 증분에 UX-32로 세종이 적는다.

### P-132 게이트의 기대식은 정본 화면 문구에서 뽑는다 [판정 · Q+C · 30분 짝 검토]
- 회색 24 · 빨강 13 가운데 **제품 결함 · 게이트 기대식 오류 · 정본 없음**을 가른다. 이미 보이는 기대식 오류 후보: U5#4가 `/device`(드론)를 가리킴(정본은 `/dsm/cameras/import`) · U5#5 「저장」 · U4#8 「조회」 · U3#9 「회신」(사건 4804는 이미 종결 — **게이트용 사건은 매 회 실제 생성 경로로 새로 만든다**) · U1#3 pulse는 클릭이 아니라 로드 시 호출. 온보딩 48행에 **「정본 경로 · 누를 문구(GX-COPY)」 두 칸**을 더해 기대식이 거기서만 나오게 한다(손으로 적지 않는다). 분류 결과 3종 수를 보고에 — 제품 결함만 빨강, 기대식 오류는 고쳐 재측, 정본 없음은 회색 유지.

### P-133 익명 읽기 5 — 세 턴째 이월 · 이번 턴 닫는다 [판정 · S]
- `config-management/list-optimized` 16,436B · `auth/timezones` 109,309B 등 5 → **401**. 공개 설계는 허용 목록+사유+크기 상한(`timezones`가 후보라면 우리 층 정적 목록 1KB로 대체). 닫는 조건: 익명 GET 전수(330)에서 자료 200 = 0.

### P-134 자격 형식 6/7 — 재생성 창 앞에서 할 수 있는 것 [판정 · E]
- MinIO 5·5자 · DB 5자 · 서명키 `local-bringup-only-not-a-real-key`는 **재생성 창(대표 ③)** 뒤. 그 앞에서: vault에 새 값 준비(형식 통과 · 저장소·`.env*` 밖) · 창에서 할 절차 runbook 1쪽(자격 3 · 마운트 · 운영 프로필 · 순서 · 되돌리기) → 창이 열리면 30분 안에 끝나게. `prod_settings` 7/7은 그 뒤.

### 기본값 규칙: 「닫힌 쪽 · 실측 가능한 쪽 · 되돌릴 수 있는 쪽 · 반경 좁은 쪽 · 앱의 자격으로 · 운영 모양 서버에서 · 누른 뒤를 본 것만 초록 · **여섯이 같은 모양이면 뿌리 하나를 먼저**」 · 보고에 「세종 미판정 · 기본값 택함」 한 줄. 삭제·되돌리기·운영계 외부 행위는 대표.

---

## §3 턴 P (2시간 · 조율자 30분 → 동시 4 → 90분 병합 → V)

| 차선 | 절·일 | 첫 증거 |
|---|---|---|
| **조율자 30분** | ① 보험 패치 갱신(239) ② **P-130 U2 로그인 원인 ⓐⓑⓒ → 고침** ③ **P-126 게이트 14벌 → 커밋 셋 또는 「막는 게이트·사유」 첫 줄** ④ 재기동 → `live_freshness` ⑤ P-90′ 「보내라」 있으면 4건 | 커밋 해시 3(또는 첫 줄) · U2 로그인 1 |
| **S 보안** | **P-133 익명 읽기 5 → 401** · P-134 vault 새 값 준비 + 창 runbook 1쪽 · 회전 뒤 `AS=` 계정 전수 로그인 확인(U1·U2·U4·U5) | 5→0 · runbook · 로그인 4/4 |
| **C 화면** | **P-129 여섯의 뿌리 대조표(승격 접두 ↔ 화면) → 어댑터 한 곳 또는 각각** · **P-131 역할 홈 4** · P-132 기대식 오류로 판명된 화면의 문구·경로 정본화 · `/handover` 작성 버튼(68→250B는 문구만) | 뿌리 표 1 · 넷→0 · 첫 URL 4/4 |
| **E 운영** | **P-127 TIME_ZONE 반경 → 격리 전/후 → 적용** · **P-128 재시작 정책 6/6 + compose** · 회수증 자동 1 재시도 · 8500 조건 셋 상태 | 시각 = KST · 정책 6/6 · 회수증 |
| **Q 품질** | **P-132 회색 24·빨강 13 → 3종 분류(30분 짝 검토)** · 온보딩 48행 「정본 경로·문구」 두 칸 · 게이트용 사건 매 회 생성 · `click_completes` 재측 → FC 하한·상한 · **첫 표 8줄 산출기 출력** · `prod_settings` +1(KR 기본값) | 분류 3종 수 · N/48 · 첫 표 8/8 |
| **V** | 헤더 3줄 게이트만 · `click_completes` · `read/write_auth` · `error_body` · `ui_copy` · walk(역할 계정 4) · 세 수 | 색만 |

**턴 Q(다음)**: UX-29 관리자 화면 3장 · UX-25 토스트 의존 제거 · 「이번 달 우리 센터」 1쪽 · 자동 순회 · 재생성 창이 열리면 runbook 30분 → 운영 프로필 · 스테이징 → RC-1.

**함정 다섯**: ① 커밋 없이 턴을 끝내지 않는다 — 막는 게이트 이름이 유일한 예외 ② 여섯을 따로 고치기 전에 뿌리 표를 낸다 ③ TIME_ZONE은 격리 전/후 없이 본 서버에 켜지 않는다 · DB 재해석이 보이면 멈추고 대표 ④ 회전 뒤 게이트 계정 로그인 4/4를 먼저 확인한다 ⑤ 기대식 오류를 제품 결함으로, 제품 결함을 기대식 오류로 바꾸지 않는다 — 3종 수를 적는다.

---

## §4 대표 승인·결정 — 우선순위 순 (TIME_ZONE · 재시작 정책은 세종이 내렸다 — 목록에서 뺌)
1. **운영 공개 진입면 읽기 4건 「보내라」** [다섯 턴째]. 개발에서 닫은 문 셋이 운영에도 있는지.
2. **스테이징 1대(공개 URL)** — CR 조건 ① · 대표와 세종이 제품을 직접 열어 볼 유일한 길.
3. **컨테이너 재생성 창 1회** — 자격 3(MinIO · DB · 서명키) · `/backup` 마운트 · 운영 프로필. 영실이 runbook 1쪽을 이번 턴 준비 → 창이 열리면 30분.
4. (1이 「있음」이면) 운영 긴급 패치 창 + 계정 회전.
5. SMTP(CR ⑤) · 6. VLM·GPU 1장 · 7. 삭제 `tasks/record_task.py` · 8. 서한 3 · 9. 가격표 숫자·채널 계약서(세종 초안 뒤).

## §5 세종(CPO) 산출물
이번 턴: 이 문서 · **PRD v2.7 증분 UX-32(역할 홈) · OPS-21(재시작 정책) · SET-01(KR 기본값)** — 다음 턴 PRD v2.8에 세종이 적는다 · 조달 규격 문장 1쪽(REVIEW §7) 초안 · 가격표 v0.2 초안. 영실이 기다리는 문서 없음(규칙 ④).

## §6 보고 · 불변
**보고**: **첫 표 8줄(산출기 출력 — 없으면 V 회색 · 세종은 그 보고의 수를 인용하지 않는다)** → G1~G5 · **커밋 해시 3 또는 「막는 게이트·사유」** · U2 원인 ⓐⓑⓒ · `click_completes` N/48 + 3종 분류 수 · 여섯의 뿌리 표 · 역할 홈 4/4 · TIME_ZONE 전/후 · 정책 6/6 · 익명 읽기 5→N · runbook · P-90 결과 또는 「대기」 · 「턴 P 절 N 중 닫힘 N」 · V 빨강 · 세종 오류 N(누계 23+N) · 영실 오판 N · 위임 이의 N · 기계 시각 · 소요.

**불변**: 전부 유지 + **여섯이 같은 모양이면 뿌리 하나를 먼저 잰다** + **기본값은 이전 고객의 것이다 — 이름을 보면 묻는다** + **게이트의 기대식은 정본 화면 문구에서 뽑는다** + **로그인 뒤 첫 화면은 역할 홈이다** + 사유 없는 커밋 보류는 없다 + 그려졌다 ≠ 동작한다 + 상태는 칸으로 + 읽기 전용은 쓰지 못한다 + 화면은 거짓말하지 않는다 + 한 화면에 우리말 아닌 글자 0 + 정본은 운영 모양 서버 + 게이트는 앱의 자격으로 + 역할 없는 계정은 아무것도 보지 않는다 + 자격의 기본값은 없다 + 빨강을 회색으로 바꾸지 않는다 + 「없다」도 실측 + 역할 있는 사람의 화면만 증거 + 분모는 손으로 적지 않는다 + 회수증 = 덤프 + 복원 + 운영계는 읽기라도 대표 한 마디 뒤 + 삭제·되돌리기는 대표.

## 대표께
- **상용 기준 완료율: 상용 67.8 · 손 안 70.2 · 온보딩 60 · FC 하한 22.9~상한 72.9**(새 게이트가 48흐름 중 11을 초록, 24를 「못 쟀다」로 정직하게 적었습니다) · PR 20.0 · CR 12.5(둘은 이번 보고에 빠져 직전 값 · 제 판정으론 CR이 37.5까지 올라갈 근거가 생겼습니다 — 영실 실측이 덮습니다). 점수가 0.0 움직인 이유는 같습니다 — 고친 것이 이미 「구현」인 절 안입니다.
- **이번 턴 영실이 닫은 것이 큽니다**: 확인창 층이 살아났고(판정 버튼이 눌립니다), 익명 2단계 해제·읽기 전용 쓰기·공유 비밀번호 74개 — P0 셋을 한 턴에 닫았고, 택배 운송장 대신 사건 보고서 1쪽이 나오고, 「그날 그 시각」으로 사건을 12.7초에 찾습니다.
- **대표 결정에서 둘을 내렸습니다.** 사건 시각이 2시간 이르게 찍히는 것은 시간대 기본값이 이전 고객(UTC+7)의 것이라서이고, DB는 UTC로 저장돼 표시만 바뀌므로 제가 판정으로 집행합니다(격리 전/후 뒤). 컨테이너 재시작 정책(세 아침 연속 전체 다운의 해법)도 무중단·되돌리기 한 줄이라 제가 집행합니다. 둘 다 삭제·되돌리기가 아닙니다.
- **커밋이 네 턴째 없습니다(239파일).** 사유 없는 보류는 불변 위반이라 영실 오판으로 셌고, 이번 턴 첫 30분에 셋으로 갈라 커밋하게 했습니다. 막는 게이트가 있으면 그 이름이 첫 줄입니다.
- **결정**: ① 운영 읽기 4건 「보내라」(다섯 턴째) ② 스테이징 ③ 재생성 창 1회(영실이 runbook 1쪽을 이번 턴 준비 → 창이 열리면 30분) ④ 패치 창(조건부) ⑤ SMTP ⑥ GPU ⑦ 삭제 ⑧ 서한 3 ⑨ 가격표·채널 계약서.
