# RESUME_NEXT.md — GuardianX 초효율 작업지시서 · 턴 U

WO-GX-20260915-01 **파 3 턴 1 「끝내기」** · 2시간 · 조율자 20분 → 동시 6(Q 포함) → 병합 → V 단독 · U4 재난안전과 + U5·U6가 하루를 끝내는 화면을 짓는다 — 온보딩 두 번째 수 · 웹푸시 비밀은 본문으로 · 재부팅 아침 실측이 첫 일

- 발행: **2026-09-17 20:45(기계) · 턴 U** · 착수 **09-18 09:30(재부팅 실측 뒤)** · 발행자: 세종(CPO) · 수신: 영실(CPM · 조율자) · 차선 5 + Q · V
- 직전 보고(턴 T · 09-17 18:5x · 소요 7시간 20분 = 대표 대기 186분 + 막힌 78분 + 일 · 커밋 셋 `6c67c20`·`839f33c`·`0c8b957` · push 됨 · 태그 `pre-turn-t`) 읽음: 상용 **66.3**(+1.0 = SEC-18a 잠김→구현 · 회색 2 = PERF-04 · **OPS-13a**(창이 앞단 로그에 남긴 「뒷단 부재 234건」)) · 손 안 **66.4**(89/134) · **FC 28/48**(단독 · 목표 32에 4 부족 · 빨강 2 = 데이터/환경) · 영역 ① **7/39**(회색 25 = row_map 사유 낡음 ≥ 4) · **온보딩 48행 첫 수 16.0/48**(두 칸 채운 22행 중 ● 14 · ◐ 4 · ○ 4 · 정본 없음 26) · PR 50.0(어긋난 행 2 → 0) · CR 34.4~45.6 · 단위 **1,537/0/5** · **창 ⓪~⑤ 되돌리기 없음**(대표 한 줄 16:5x → 자격 3 · `/backup` · `prod_settings` **7/7** · 컨테이너 **10/10** · 함정 넷은 runbook 「집행 기록」에) · **웹푸시가 닿았다**(M4 → 구독 → `test-send` → `deliveries #481 channel=webpush succeeded=true` → Chrome 서비스워커 「[훈련] GuardianX 알림 시험」 · 데스크톱) · 누른 뒤 본 결함 1: **구독 비밀 셋이 쿼리 문자열로 접근 로그에**(D-486) · P-161 `resolver` + upstream `resolve` 격리 ⓐⓑⓒ → 본 서버 10/10 · **영실 오판 2**(V 단독 중 조율자가 GA를 돌려 세션 빼앗음 D-487 · 씨앗 id 배선 · 자진) · 커밋 자리 옳은 빨강 둘(403→429가 게이트 로그인 기다림을 끊음 · 대장 합쳐 쓰이자 옛 캡처 시각 벌어짐) — 영실 [실측].
- PRD v1.1 §10 상태: ③ 창 **끝남** · ① 이메일+웹푸시 · ⑤ DOCX 정본 · ② 스테이징 기한 **09-19(내일)** · ④ SMTP 값 5 대기 · ⑥⑦ 10월. WO §6 파 3 = U4 + U5·U6(09-22~23 예정 → **하루 앞당겨 09-18 착수** — 파 2가 이틀에 끝났다).
- 검토대 접수 0건.

## §0 상용 기준 완료율 — 첫 표 8줄

| 수 | 지금 [실측 · 턴 T] | 직전 | 뜻 |
|---|---|---|---|
| 상용 /100 | **66.3**(139) | 65.3 | +1.0 SEC-18a · 회색 2 — OPS-13a는 **창이 만든 회색**(P-171로 가른다) · PERF-04는 결정 ⑤ 뒤 재판정(P-172) |
| 손 안 도달율 | **66.4**(89/134) | 66.2 | 분모 +1 = SEC-18a 손 밖→손 안(이름 있음 · 옳다) |
| FC 하한 | **58.3**(28/48 · 단독) | 54.2 | +2 · 목표 32 미달 → **이번 턴 목표 34** |
| 영역 ① | **7/39** | 7/39 | 회색 25 중 사유 낡음 ≥ 4 → Q 재판정(P-169) |
| PR · CR | **50.0** · **34.4~45.6** | 50.0 · 34.4~45.6 | 어긋난 행 0 · CR ①②는 스테이징·SMTP |
| **온보딩 48행** | **33.3(16.0/48) — 첫 수** | 0(회색) | 다섯 턴 만에 수가 됐다 · 두 칸 채운 22행 · **정본 없음 26은 이번 턴 절반(13) 채운다** |
| 커밋 | master 3 · push 됨 · `pre-turn-t` | push 됨 | 원격 10 |
| 단위 시험 | **1,537/0/5** | 1,471 | +66 · 창 뒤 전수 20분 |

**G1~G5**: G1 ○(P-90 주소 다섯 턴째 · 스테이징 내일 기한) · G2 ● · G3 ◐(28/48 · 온보딩 33.3) · G4 ◐(66.3) · G5 **●→◐**: 창 끝남 · `prod_settings` 7/7 · 10/10 · 앞단 순서 고침 — **재부팅 아침 실측이 남았다**(첫 일).

## §1 인정 · 오판 셈

1. **창 ⓪~⑤ 되돌리기 없음** — 대표 한 줄이 16:5x에 왔고 그때 열었다(정지 13:48 → 186분 대기를 「막힘」이 아니라 「창의 전제」로 따로 센 것 옳다). 함정 넷(compose 치환 더미 · MSYS 경로 · gx-shell 꾸러미·캐시 소실 → 보존/복원 · 분류기가 긴 명령을 막아 한 컨테이너씩)을 **절차는 안 바꾸고 「집행 기록」에 적은 것** — 옳다. 운영 프로필 7/7이 처음 섰다.
2. **웹푸시가 닿았다 — 제품 경로 · 훈련 채널 · 대표 승인 뒤.** 그리고 **누른 뒤에 결함을 봤다**: 구독 비밀 셋이 쿼리 문자열로 가서 접근 로그에 남는다. 「닿았다」에서 멈추지 않고 로그를 본 것 — 이 턴의 값. U3 첫 일(P-166).
3. **온보딩 48행 첫 수 16.0/48** — 다섯 턴 만에 회색이 수가 됐다. 22행만 잰 것을 「22행 중」이라 적고 정본 없음 26을 숨기지 않았다. 옳다.
4. **오판 둘 자진**(D-487) — ① V 단독 중 조율자가 `verify_ga_readiness`를 돌려 그 안의 로그인이 V 세션을 빼앗고 429 → V가 잡아 나쁜 수를 버리고 재측. 「경합 중 수는 수가 아니다」의 반대편 — **재는 동안 조율자도 로그인하지 않는다**(P-170 규약). ② Q의 씨앗 id 배선을 그대로 옮겼는데 capture가 씨앗을 스스로 지워 넘길 id가 없었다 — 도구 규약(P-170). **영실 오판 2.**
5. **커밋 자리에서 잡힌 옳은 빨강 둘** — 이번 턴이 만든 규약 변화(403→429 · 대장 합침)가 기존 판정기의 눈을 멀게 했고 커밋 훅이 잡았다. 원칙 등재: **규약을 바꾼 차선은 그 규약을 읽는 판정기 목록을 같은 커밋에 갱신한다**(D-479 `used_by`의 짝 · P-171).
6. **OPS-13a 회색이 「창이 만든 회색 · 고장 아님」** — 산출기가 스스로 「이번 판은 부하 한 벌이 아니다」라고 회색을 낸 것은 옳다. 그러나 234건을 「알려진 창에 묻지 않는다」(P-92)와 「계획 창은 SLA 밖」은 다르다 — 갈라 준다(P-171).
7. **소요 7시간 20분 중 대표 대기 186분** — 창이 14:00에 열렸으면 4시간대였다. 다음 창은 **집행자 세션과 대표 한 줄을 같은 시각에** 묶는다(§4).
8. **막힌 78분 중 분류기 차단 6회 → 명령 나눔 8** — 도구 환경 제약을 절차로 흡수했다. 옳다 · runbook에 적혀 다음 창은 짧다.

**세종 오류 0(누계 28) · 영실 오판 2(자진) · 위임 이의 0 · 세종 미판정 0.**

## §2 판정 — 세종이 정해 집행 (삭제·되돌리기·운영계 외부 행위·`.env` 실제 값은 대표)

### P-166 웹푸시 구독 비밀 — 본문으로 · 회전으로 닫는다 [판정 · U3 첫 일 · D-486]
- `endpoint`·`p256dh`·`auth`는 **POST 본문(JSON)** 으로만 · GET/쿼리 금지 · 앞단 접근 로그 형식에서 쿼리 마스킹(`?…` 절단) 1줄. 이미 로그에 남은 구독 셋은 **회전**(D-004): 구독 해지 → 서비스워커 재구독(브라우저가 새 키 발급) → 옛 구독 무효 — 로그 줄은 **지우지 않는다**(죽은 값). 시험 1: 구독 요청이 쿼리에 비밀을 싣으면 400. 게이트 `ui_secrets`에 「접근 로그 쿼리 비밀 0」 1줄.
- 닫는 조건: 재구독 → `test-send` → `channel=webpush succeeded=true` 1 · 접근 로그 최근 100줄에 `p256dh` 0.

### P-167 VAPID 실값 · 운영 env-file — 다음 창 항목 1 [판정 · E · §4]
- 운영 프로필 스택은 `docker run`이라 env 변경 = 재생성 = **창**. 이번 턴은 안 한다. 대표 승인 창 2(30분 · 항목: VAPID 3 · 잔존 시험 DB 처분 결과 · SMTP 값 오면 5)에 묶어 §4로. 그때까지 훈련 채널 발송은 dev VAPID(gx-shell) · `prod_settings` 형식 검사 +1은 **같은 커밋**(영실 제안 그대로).
- 「우리가 만드는 자격은 우리가 만든다」 — 실값 생성·금고 보관은 이미 세종 위임 안에서 끝났다 · 싣는 시각만 대표.

### P-168 잔존 시험 DB 4 — 삭제는 대표 · 접근 차단은 세종 [판정 · E]
- 네 DB의 **이름 · 크기 · 마지막 접근 · 격리 이름 규칙(`DB_TEST_NAME` 접두) 안/밖** 표 1 → §4. 삭제 전 세종 집행: 앱 계정의 **CONNECT 권한 회수**(되돌릴 수 있음) → 30일 뒤 삭제 후보. 「격리 이름의 기본값은 없다」 — 규칙 밖 이름이 하나라도 있으면 그 자체가 결함으로 대장 OPS 절 1.

### P-169 FC 34 · 영역 ① 10 · 온보딩 두 번째 수 [판정 · Q+V]
- Q: row_map 회색 25 중 **사유 낡음 ≥ 4 재판정**(라우트 대장 749 기준) · 온보딩 「정본 없음」 26행 중 **13행 두 칸 채움**(파 3 화면이 서는 U4·U5 행 우선) · probe 규약 유지 · 씨앗 id 파이프(P-170 ②).
- V 단독: 로그인 4/4 → S4(U4)·S5(U5) 걷기 + S1 회귀 → `click_completes` 48 → **온보딩 35행 재측** → 캡처 append → `feature_reach`. 목표 FC ≥ 34/48 · 영역 ① ≥ 10/39 · 온보딩 ≥ 35행 잰 수. 못 미치면 그대로.

### P-170 V 단독 세션 규약 둘 [판정 · 조율자 · Q]
- ① **재는 동안 아무도 로그인하지 않는다** — V 단독 시작 시 `evidence/V_LOCK`(시작 시각 · V 세션 id) 파일 · 모든 게이트 러너·`verify_ga_readiness`는 LOCK이 있으면 **로그인을 건너뛰고 회색**(「V 단독 중 · 재지 않음」) · 끝나면 조율자가 지운다. 조율자는 그 40분에 브라우저·게이트 로그인을 하지 않는다.
- ② 씨앗 id 파이프 — `capture_screens`는 씨앗을 **지우지 않는다**(`--keep-event` 기본) · 씨앗 생성 → id 파일(`runs/<시각>/seed.json`) → `click_completes`·`feature_reach`가 그 파일을 읽는다. 「대체」 규칙 도구화(턴 S 이월)와 한 커밋.

### P-171 계획 창은 SLA 밖 — 그러나 묻지 않는다 [판정 · E+세종]
- OPS-13a 회색 234건은 **대표 승인·시작/종료 시각 기록이 있는 계획 창**(16:54~17:05)의 것 → SLA 5xx 정본에서 **창 구간을 따로 센다**: 두 줄(창 안 N · 창 밖 N) — 「알려진 창에 묻지 않는다」(P-92)는 유지: 창 구간도 수를 적되 SLA 분모에서만 뺀다. 판정기 `front_line_502`가 `RUNBOOK_재생성창 집행 기록`의 시각 구간을 읽는다(손으로 적지 않는다). **세종 산출물**: LAW-05 v0.3 「계획 점검 창」 조항 1줄(사전 통지 · 30분 상한 · 월 1회) — 이번 턴 세종.
- 닫는 조건: OPS-13a 회색 → 초록/빨강(창 밖 5xx로) · 정본 두 줄 보고.

### P-172 PERF-04 — 결정 ⑤ 「안 산다」 뒤 재판정 [판정 · Q]
- VLM 보류(v1.2 재결정)이므로 PERF-04(VLM 지연 예산)는 **v1.1 범위 밖** → `hand: out` · 사유 「결정 ⑤ · v1.2」 · 회색 유지(지우지 않는다 · 사유를 바꾼다). 상용 /100 분모는 그대로(대장 항목은 지우지 않는다).

### P-173 파 3 턴 1 본 일 — WO §6 파 3 턴 5 그대로 + 이월 [판정 · 차선 5]
- **F**: 온보딩 카드 40 자동 완료 훅 **전수 배선**(서버 기록이 닫는다 · 사람이 체크하지 않는다) · `verify_onboarding_walk` 6/6 첫 실측 · 「다시 시도」 없는 자리 잔여 · 토스트 8자리 결론.
- **U24(U4 몫)**: `Reports.tsx`(서식 3 — 사건 1쪽 · 「이번 달 우리 센터」 자동본 · 상급 제출용 · **DOCX 정본**(결정 ⑤) · PDF 병행) · `monthly_report.py` 배치(매월 1일 · 이번 턴은 수동 1회로 자동본 1) · `docx_export.py`(HWP가 여는 서식 · `render(report_run) -> bytes`) · `AuditLog.tsx` 완성(필터 3 · 해시 체인 표시 · CSV) · LAW-07′ 메뉴 배치. 닫는 조건: U4 8행 중 ≥ 5 초록 · 자동본 1 · 파일 2(DOCX·PDF) · 택배 필드 0.
- **U56(U5·U6 몫)**: `CameraAddress` **한 대 고치기**(주소 미입력 39 → 이번 턴 1) · 재시작 요청 라우트(`system/restart-request` · 실행은 대표·창) · **백업 회수증 표시**(마지막 회수증 시각 · 다음 예정) · **저장 상한 선언**(`storage_used_pct` 0.0 → 상한 선언 → 수) · API-03·04(키 범위 · pulse · stats) · 연계 명세 1장 · OpenAPI `components.schemas` ≥ 1 · 401/403/404/422 문서. 닫는 조건: U5 8행 ≥ 5 · U6 8행 ≥ 6.
- **U3**: P-166 첫 일 → 실카메라 스냅샷 1(시드 아님 · `data_source=live` · 원본 무반출) · 편리성 #5 계측.
- **U1**: 회귀 감시 · 편리성 #1~#3 두 번째 값.
- **E(조율자 겸)**: **아침 재부팅 실측**(09-18 09:00 · 세션 밖 → 첫 일 · 10/10 · 앞단 200 · `live_freshness --all`) · P-168 표 · P-171 판정기 · 창 2 항목 표(§4).
- **Q**: P-169 · P-170 ② · P-172 · 온보딩 13행.

### 기본값 규칙
닫힌 쪽 · 실측 가능한 쪽 · 되돌릴 수 있는 쪽 · 반경 좁은 쪽 · 앱의 자격으로 · 운영 모양 서버에서 · 누른 뒤를 본 것만 초록 · 값은 출력하지 않는다 · 대장은 줄지 않는다 · 경합 중 수는 수가 아니다 · 분모 0인 초록은 초록이 아니다 · **재는 동안 아무도 로그인하지 않는다** · **규약을 바꾼 차선이 판정기 목록을 갱신한다**.

## §3 턴 U — 시간표 (09-18 · 기계 KST)

| 시각 | 누가 | 무엇 |
|---|---|---|
| 09:00~09:30 | E(조율자) | **재부팅 실측**(Docker 데몬 · 컨테이너 10/10 · 앞단 200 · `live_freshness --all` FRESH) → G5 ●/◐ 확정 · 결과가 9/10이면 P-161 재판정 첫 줄 |
| 09:30~09:50 | 조율자 | `pre-turn-u` 태그 · 역할 계정 4/4 · **Q 먼저** · 쓰기 면 선등록(reports · report_run · audit read · restart-request · address · webhook filters · api keys) · 보험 패치 · P-90 주소 오면 P-144 |
| 09:50~11:20 | 동시 6 | F · U24(U4) · U56(U5·U6) · U3(P-166 → 실카메라) · U1(훅) · Q(P-169 · P-170 ② · P-172) |
| 11:20~11:35 | 조율자 | 병합 F→U1→U3→U24→U56→Q · 커밋 셋(막는 게이트 있으면 이름 첫 줄) · P-171 판정기 · P-168 표 |
| 11:35~12:15 | **V 단독** | `V_LOCK` → 로그인 4/4 → S4·S5 + S1 회귀 → `click_completes` 48 → **온보딩 35행** → 캡처 append → `feature_reach` → 3종 · `V_LOCK` 해제 |
| 12:15 | 조율자 | 보고(§5) · 막힌 시간 따로 · 창 2 항목 표 §4 |

**함정 다섯**: ① V 단독 중 조율자는 로그인하지 않는다 — `V_LOCK` ② 구독 비밀은 본문으로만 — 쿼리에 있으면 400 ③ 창 2는 이번 턴 열지 않는다 — 항목 표만 ④ 시험 DB 삭제는 대표 — 이번 턴은 표와 CONNECT 회수만 ⑤ 규약을 바꾸면 판정기 목록을 같은 커밋에.

## §4 대표 — 지금 셋 + 창 2

| # | 무엇 | 어떻게 |
|---|---|---|
| 1 | **스테이징 1대 — 기한 09-19(내일)** | 공개 URL·TLS · 잠금화면 웹푸시 · `walk_scenarios` 스테이징 · CR ① · 파 4(09-24~25) 앞에 있어야 RC-1 |
| 2 | **P-90 운영 주소** | `https://…` 한 줄 — 다섯 턴째 |
| 3 | **창 2 일시**(30분) | 항목: VAPID 실값 3(P-167) · 잔존 시험 DB 처분(P-168 표 뒤 「지워라」) · SMTP 값 5(오면). **집행자 세션 확인 뒤 한 줄** — 창 1의 186분 대기를 되풀이하지 않기 위해 「일시 + 「열어라」」를 한 문장으로 |
| 기계 | Docker Desktop AutoStart 켬 | 재부팅 아침 실측(09-18 09:00) 전에 켜져 있으면 10/10을 자동으로 본다 |

그 뒤: 가격표 숫자 · 채널 계약서(CR ⑦).

## §5 보고 · 불변
**보고**: 첫 표 8줄(산출기) → G1~G5(**재부팅 실측 N/10 첫 줄**) → **온보딩 N/48(잰 행 M · 정본 없음 N)** · FC N/48 · 영역 ① N/39 → U4 8행 · U5 8행 · U6 8행 → 자동본 1 · 파일 2 → **웹푸시 재구독 1 · 접근 로그 `p256dh` 0** → 실카메라 스냅샷 1 → OPS-13a 두 줄(창 안/밖) · PERF-04 hand:out → `V_LOCK` 사용 1 · 조율자 로그인 0 → 시험 DB 4 표 · CONNECT 회수 4 → 창 2 항목 표 → 대장 행 수 → 차선별 닫힘·넘김 → 막힌 시간 → V 빨강 · 3종 → 가정 → 세종 오류(누계 28+N) · 영실 오판 · 위임 이의 → 기계 시각 · 소요.

**불변**: 전부 유지 + **재는 동안 아무도 로그인하지 않는다** + **규약을 바꾼 차선이 판정기 목록을 같은 커밋에 갱신한다** + **계획 창은 SLA 밖이되 수는 적는다** + **비밀은 본문으로 — 쿼리 문자열에 비밀 0** + 분모 0인 초록은 초록이 아니다 + 우리가 만드는 자격은 우리가 만든다 + 창과 다른 변경을 섞지 않는다 + 대장은 줄지 않는다 + 경합 중 수는 수가 아니다 + 한 파일은 한 차선 + 온보딩 카드의 완료는 서버 기록이 닫는다 + 누른 뒤를 본 것만 초록 + 삭제·되돌리기·운영계·`.env` 실제 값은 대표.

## 대표께
- **완료율**(턴 T 실측): 상용 **66.3**(+1.0) · 손 안 66.4 · **FC 58.3**(28/48 · +2) · PR 50.0 · CR 34.4~45.6 · **온보딩 48행 첫 수 33.3**(16/48 · 다섯 턴 만에 회색이 수가 됐습니다 · 아직 22행만 잰 수). 창이 열려 운영 설정 7/7 · 컨테이너 10/10이 처음 섰고, 웹푸시가 제품 경로로 실제 알림에 닿았습니다(데스크톱 · 훈련 채널). 커밋 셋이 원격에 올라갔습니다.
- **누른 뒤에 본 결함 하나** — 웹푸시 구독 비밀이 주소 뒤 쿼리로 가서 접근 로그에 남습니다. 본문으로 옮기고 구독을 회전해 닫습니다(로그는 지우지 않습니다 — 죽은 값이 됩니다). 이것이 다음 턴 U3의 첫 일입니다.
- **파 3을 하루 앞당겨 내일 09-18 시작합니다** — 재난안전과의 보고서 3종(DOCX 정본 · 자동본) · 감사 조회 · 관리자의 카메라 주소 · 백업 회수증 표시 · 연계 명세. 첫 일은 아침 재부팅 실측(앞단 기동 순서를 고친 뒤 처음 맞는 아침)입니다.
- **결정**: ① **스테이징 — 기한이 내일(09-19)입니다** · ② 운영 주소 한 줄(다섯 턴째) · ③ 창 2 일시 — VAPID 실값·시험 DB 처분·SMTP를 한 창에 묶어 30분, 「일시 + 열어라」를 한 문장으로 주시면 186분 대기가 되풀이되지 않습니다 · 기계: Docker 자동 시작.
