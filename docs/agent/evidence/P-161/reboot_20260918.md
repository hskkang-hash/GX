# 재부팅 실측 — 2026-09-18 아침 (턴 U 첫 일 · 조율자) [실측]

지시서 §3 09:00~09:30 「재부팅 실측(Docker 데몬 · 컨테이너 10/10 · 앞단 200 · `live_freshness --all` FRESH)
→ G5 ●/◐ 확정 · 결과가 9/10이면 P-161 재판정 첫 줄」. **P-161(앞단 기동 순서 `resolver` + upstream
`resolve`)을 고친 뒤 처음 맞는 아침**이다.

## 수 — **0/10** (측정 시각 12:23:49 · 부팅 +3시간 18분)

| 무엇 | 실측 |
|---|---|
| 호스트 마지막 부팅 | **2026-09-18 09:05:28** (`Win32_OperatingSystem.LastBootUpTime`) |
| 잰 시각 | 2026-09-18 12:23:49 — 부팅 **+198분** |
| Docker 데몬 | **안 떴다** — `npipe:////./pipe/dockerDesktopLinuxEngine` 없음 (docker CLI exit 7) |
| `Docker Desktop.exe` 프로세스 | **없음** |
| `com.docker.service` | `Stopped` · StartType **Manual** |
| HKCU `…\Run\Docker Desktop` | 등록돼 있다(값: `C:\Program Files\Docker\Docker\Docker Desktop.exe`) |
| `%APPDATA%\Docker\settings-store.json` `AutoStart` | **False** |
| 컨테이너 | **0/10** |
| 앞단 8500 | 닿지 않음(`000`) |

**G5 = ○ (●도 ◐도 아니다).** 재부팅 뒤 이 기계는 **사람이 손대기 전까지 제품이 서지 않는다.**
「9/10 이면 P-161 재판정」이라고 적었는데 9도 10도 아니었다 — **앞단 기동 순서의 문제가 아니라
그 아래층(데몬)이 안 떴다.** P-161 은 이 판으로 재판정되지 않는다(데몬이 없으면 앞단도 없다 —
아직 못 쟀다 · 회색). 데몬이 뜬 뒤의 기동 순서는 아래 ②가 처음으로 보여 준다.

⚠ HKCU `Run` 에 항목이 있는데도 안 떴다 — Docker Desktop 자신의 `AutoStart=False` 가 이긴다(둘은
다른 스위치다). §4 「기계: Docker Desktop AutoStart 켬」은 **대표 칸**이라 조율자가 켜지 않았다.

## ② 손으로 데몬을 띄운 뒤 — 10/10 · 앞단 200 · 5/5 FRESH [실측 12:24~12:25]

`Start-Process "Docker Desktop.exe"` 한 번(조율자 · 되돌릴 수 있는 국소 행위 · 창이 아니다).

| 무엇 | 실측 |
|---|---|
| 데몬 기동 뒤 자동 복귀 | **9/10** — `restart=unless-stopped` 인 아홉이 18초 안에 전부 `Up` |
| 안 돌아온 하나 | **`gx-shell`** — `restart=no` · `Exited (255)`. `docker start gx-shell` 로 세움 |
| 컨테이너 | **10/10** |
| 앞단 | `GET /admin/login/` **200** · `GET /api/dsm/health` **200**(창 뒤 P-161 그대로 · reload 없음) |
| gx-shell 안 꾸러미 | playwright · pywebpush **살아 있다**(`pip_ok`) · `ms-playwright` 캐시 · `/tmp/gx_spa_server.py` · `/app/_fe_dist` 그대로 — **재생성이 아니라 기동이라 안 지워졌다**(턴 T 창 함정 ③ 과 다른 자리) |
| 게이트 서버 둘 | runserver 8000 **200**(VAPID env-file 그대로) · SPA 3002 **200** — 프로세스는 컨테이너 정지와 함께 죽으므로 **매 기동마다 다시 띄운다** |
| `live_freshness --all` | **5/5 FRESH** (제품 넷 + runserver) |

## 이 측정이 말하는 것 셋

1. **제품 컨테이너의 재시작 정책은 옳다** — 데몬만 뜨면 아홉이 스스로 돌아온다(사람 0). 앞단이
   뒷단보다 먼저 떠도 200 을 냈다(P-161 이 고친 그 자리 · 창 뒤 첫 실제 기동).
2. **그 위층이 비어 있다** — 데몬이 자동으로 뜨지 않으면 정책은 한 줄도 쓰이지 않는다. 지금 이
   기계의 복구 시간은 「대표가 Docker 를 켜는 시각」이고, 그것은 수가 아니다.
3. **`gx-shell` 은 제품이 아니다**(게이트·시험 자리) — `restart=no` 는 의도로 볼 수 있으나,
   아침마다 게이트가 회색이 되는 원인이므로 **이름을 붙여 둔다**: 다음 창에서 정책을 줄지,
   아침 절차(`docker start gx-shell` + 게이트 서버 둘)로 둘지는 §4 대표 칸.

## 다음 사람이 그대로 쓸 아침 절차 (창 아님 · 4줄)

    Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"   # 데몬 (AutoStart 켜면 불필요)
    docker start gx-shell
    docker exec -d -w /app -e DJANGO_SETTINGS_MODULE=config.settings --env-file C:/GuardianX-vault/webpush/vapid_20260917.env gx-shell python manage.py runserver 0.0.0.0:8000 --noreload
    docker exec -d gx-shell python /tmp/gx_spa_server.py
    python scripts/verify_live_freshness.py --all        # 5/5 FRESH 여야 잰 것이다
