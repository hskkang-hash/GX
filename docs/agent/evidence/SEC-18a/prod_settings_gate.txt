# SEC-18a 증거 — scripts/verify_prod_settings.py
# 실행: 2026-09-20 KST · 차선 N (P-207 셋째 묶음) · 브랜치 turn-q · HEAD 0acf8d1
# 명령: PYTHONIOENCODING=utf-8 python scripts/verify_prod_settings.py  (gx-shell 로 자동 위임)
# ⚠ 이 판정기는 Django 를 여러 판 띄운다 — 앱 서버 재기동은 하지 않는다(별도 프로세스)
# ---
[P-107] TARGET=gx-shell 컨테이너에서 파이썬을 12번 **실제로 띄운다** (config.settings_prod / config.settings)
[P-107] AS=(계정 없음) — 자식 프로세스의 환경에 선언 이름만 준다: DJANGO_SECRET_KEY · DJANGO_ALLOWED_HOSTS · DJANGO_CSRF_TRUSTED_ORIGINS (값은 이 판정 안에서만 산다) · 앱이 든 MINIO_ACCESS_KEY/MINIO_SECRET_KEY 는 **길이와 같음 여부만** 본다
[P-107] SOURCE=뜬 판이 스스로 낸 django.conf.settings — 소스에 적힌 글자가 아니다
[P-107] MEASURED=(선언 없음 — 이 게이트는 **무엇을 · 분모 몇으로** 쟀는지 말하지 않는다. 그 `exit 0` 은 「이 호출이 통과」일 뿐이다 · P-204)
[P-107]   ✗ 머리글: MEASURED= 가 없다 — **`exit 0` 은 「이 호출이 통과」다.** 무엇을 · 분모 몇으로 쟀는지 말하지 않은 0 은 초록이 아니다 (P-204)  ← verify_prod_settings.py
[SEC-18] O 닫힌 관측 7/7 초록 (양성 대조)
[SEC-18] O 변이 7/7 를 잡는다 (음성 대조 · 규칙)
[SEC-18] O 출생 표본 7/7 빨강 — 2026-09-06 의 settings.py 와 턴 L 의 5자 자리표 자격을 그대로 잡는다
[SEC-18] O 관측 0건은 7 수 다 빨강 (회색이 초록이 되지 않는다)
[SEC-18] [입력] C:\GuardianX\guardianx-source\backend · 판 12개를 **실제로 띄운다**
[SEC-18] 이 자리에서는 못 띄웠다 — 판 「full」이 우리 거부가 아닌 이유로 죽었다(rc=1):
[SEC-18] → gx-shell 에 위임한다 (호스트에는 dj-core·celery 가 없다)
[P-107] TARGET=gx-shell 컨테이너에서 파이썬을 12번 **실제로 띄운다** (config.settings_prod / config.settings)
[P-107] AS=(계정 없음) — 자식 프로세스의 환경에 선언 이름만 준다: DJANGO_SECRET_KEY · DJANGO_ALLOWED_HOSTS · DJANGO_CSRF_TRUSTED_ORIGINS (값은 이 판정 안에서만 산다) · 앱이 든 MINIO_ACCESS_KEY/MINIO_SECRET_KEY 는 **길이와 같음 여부만** 본다
[P-107] SOURCE=뜬 판이 스스로 낸 django.conf.settings — 소스에 적힌 글자가 아니다
[P-107] MEASURED=(선언 없음 — 이 게이트는 **무엇을 · 분모 몇으로** 쟀는지 말하지 않는다. 그 `exit 0` 은 「이 호출이 통과」일 뿐이다 · P-204)
[P-107]   ✗ 머리글: MEASURED= 가 없다 — **`exit 0` 은 「이 호출이 통과」다.** 무엇을 · 분모 몇으로 쟀는지 말하지 않은 0 은 초록이 아니다 (P-204)  ← verify_prod_settings.py
[SEC-18] O 닫힌 관측 7/7 초록 (양성 대조)
[SEC-18] O 변이 7/7 를 잡는다 (음성 대조 · 규칙)
[SEC-18] O 출생 표본 7/7 빨강 — 2026-09-06 의 settings.py 와 턴 L 의 5자 자리표 자격을 그대로 잡는다
[SEC-18] O 관측 0건은 7 수 다 빨강 (회색이 초록이 되지 않는다)
[SEC-18] [입력] /app · 판 12개를 **실제로 띄운다**
    full               떴다
    no_secret          거부 · DJANGO_SECRET_KEY 가 선언되지 않았다 — 운영 프로필은 서명 키 없이 뜨지 않는다. 만드는 법: python -c "import secrets;print(secrets.token_urlsafe(64))"
    placeholder_secret 거부 · DJANGO_SECRET_KEY 가 자리표다(your-secret-key-here, secret-key) — **저장소를 읽은 사람이 아는 값**으로는 세션을 서명할 수 없다
    debug_on           떴다
    no_hosts           거부 · DJANGO_ALLOWED_HOSTS 가 선언되지 않았다 — 어떤 Host 머리글자를 받을지 정하지 않은 채로 공개 URL 을 열지 않는다 (예: guardianx.example.kr,stg.guardianx.example.kr)
    star_hosts         거부 · DJANGO_ALLOWED_HOSTS 에 `*` 가 있다(['*']) — 어떤 Host 든 받는 것은 선언이 아니라 선언의 부재다
    no_origins         거부 · DJANGO_CSRF_TRUSTED_ORIGINS 가 선언되지 않았다 — 개발 프로필의 기본값은 `http://localhost:8000` 두 개다. 그 값으로 공개 URL 을 열면 폼 제출이 막힌다 (예: https://guardianx.example.kr)
    origins_no_scheme  거부 · DJANGO_CSRF_TRUSTED_ORIGINS 에 스킴이 없다(['guardianx.example.kr']) — Django 4+ 는 스킴을 요구한다
    placeholder_minio  거부 · MINIO_ACCESS_KEY 가 9자다 — 하한은 20자다. **짧은 자격으로 뜬 앱이 곧 사고다**
    short_minio        거부 · MINIO_ACCESS_KEY 가 5자다 — 하한은 20자다. **짧은 자격으로 뜬 앱이 곧 사고다**
    same_minio         거부 · MINIO_ACCESS_KEY 와 MINIO_SECRET_KEY 가 **같은 문자열이다**(둘 다 29자) — 접근키는 로그·URL 에 남는 쪽이고 비밀키는 안 남는 쪽이다. 둘이 같으면 접근키가 남는 모든 자리에 비밀키가 같이 남는다
    dev_profile        떴다
[SEC-18] 수 7
  O  SECRET_KEY     선언 없음→거부 O · 자리표→거부 O · 길이 103
  O  DEBUG          기본 False · DJANGO_DEBUG=true 를 줘도 False
  O  ALLOWED_HOSTS  선언대로 ['stg.guardianx.example.kr', 'guardianx.example.kr'] · 선언 없음→거부 O · `*`→거부 O
  O  COOKIES        여섯 다 닫혔다 · 개발 프로필 대조: 3/6 이 열려 있다 ['SESSION_COOKIE_SECURE', 'CSRF_COOKIE_SECURE', 'CSRF_COOKIE_HTTPONLY']
  O  ORIGINS_TLS    출처 2개 · 선언 없음→거부 O · 스킴 없음→거부 O · HSTS 31536000s · 프록시 ['HTTP_X_FORWARDED_PROTO', 'https']
  O  ERROR_BODY     스위치 True · 그물 실림 True
  O  CREDENTIAL_FORMAT MinIO 접근/비밀 20·40자 · DB 비밀 32자 · 서명 키 103자 · 자리표 없음 | 자리표→거부 O · 5자→거부 O · 둘이 같음→거부 O
[SEC-18] 통과 7/7 — 운영 프로필로 뜨면 닫혀 있고, 선언이 빠지면 **뜨지 않는다**
# EXIT=0
