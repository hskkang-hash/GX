# SEC-17 증거 ① 정적 면 — scripts/verify_ui_secrets.py (인자 없음)
# 실행: 2026-09-20 KST · 차선 N (P-207 셋째 묶음) · HEAD 0acf8d1
# 명령: PYTHONIOENCODING=utf-8 python scripts/verify_ui_secrets.py
# ⚠ 이것은 **정적 면만** 잰 것이다. 이 절은 API 응답 본문도 주장한다 → ② 파일을 함께 볼 것
# ---
[P-107] TARGET=frontend/src (정적) + http://localhost:8000
[P-107] AS=GX_ROUTE_USER 가 선언되지 않았다 — **누구로 잴지 모르는 채 잰 것이다**
[P-107] SOURCE=프런트 소스 트리 + 살아 있는 서버 응답 (HTTP)
[P-107] MEASURED=(선언 없음 — 이 게이트는 **무엇을 · 분모 몇으로** 쟀는지 말하지 않는다. 그 `exit 0` 은 「이 호출이 통과」일 뿐이다 · P-204)
[P-107]   ✗ 머리글: MEASURED= 가 없다 — **`exit 0` 은 「이 호출이 통과」다.** 무엇을 · 분모 몇으로 쟀는지 말하지 않은 0 은 초록이 아니다 (P-204)  ← verify_ui_secrets.py
[SECRETS] [입력] 832개 프런트 파일 (주석 걷어낸 뒤) · 패턴 6종
[SECRETS] 렌더 문자열 0건
[SECRETS] 통과 — 화면과 응답이 우리 언어로 말하지 않는다
# EXIT=0
