# OPS-07b — **파기를 되돌렸다** (실측 1건 · 턴 H · 차선 E)

> 이 문서는 `scripts/ops_audit_purge_undo.py --drill` 이 **기계로 적었다.**

- 잰 시각(UTC): `2026-09-06T10:58:57+00:00`
- 환경: `development` · 선언된 보존 일수: **365일**
- 저널 자리: `/backup/audit_purge_journal`

## 수

| | |
|---|---|
| 표 (시작) | 22,475행 |
| 이미 만료돼 있던 남의 행 | 0건 |
| 심은 표본 | 3행 (`create_datetime` 2025-08-30) |
| 파기 판정 | `OK` · 3건 |
| 저널 파일 | `/backup/audit_purge_journal/audit_purge_20260906T105857Z.json` |
| 저널 sha256 | `4b319ba7415480b8fd86cb3c3ca8384ee41eaa89ef29dae0efe10ae84d5f7886` (sha256 값) |
| 파기 뒤 남은 표본 | 0행 |
| 되돌린 뒤 돌아온 표본 | 3행 |
| 직렬화 지문 (전) | `4b319ba7415480b8fd86cb3c3ca8384ee41eaa89ef29dae0efe10ae84d5f7886` (sha256 값) |
| 직렬화 지문 (후) | `4b319ba7415480b8fd86cb3c3ca8384ee41eaa89ef29dae0efe10ae84d5f7886` (sha256 값) |
| 표 (끝) | 22,477행 (+2 — 이 사이 **다른 차선이 쓴 줄**이다. 판정이 아니라 관찰이다) |

## 판정

- **OK** ① 파기가 돌았는가 — 판정 'OK' · 
- **OK** ② 저널이 떴는가 — 저널 /backup/audit_purge_journal/audit_purge_20260906T105857Z.json
- **OK** ③ 행이 정말 없어졌는가 — 심은 3행 중 남은 0행
- **OK** ④ 행이 돌아왔는가 — 3/3행
- **OK** ⑤ **같은 행인가** (직렬화 지문 sha256) — 지문이 같다
- **OK** ⑥ 심은 표본이 표에서 사라졌는가 — 남은 표본 0행 · 표 총량 22,475 → 22,477 (+2 — 이 사이 다른 차선이 쓴 줄이다. 판정이 아니라 관찰이다)

## 이 실측이 **말하지 않는 것**

1. **저널이 있어야 되돌아온다.** 저널을 뜨기 전에 지워진 행은 이 도구로도
   못 되돌린다 — 그래서 `ops_audit_purge_beat` 은 저널을 못 뜨면
   dj-core 를 **부르지 않는다**(`SKIPPED_UNREVERSIBLE` · 호출 0).
2. **저널이 사는 볼륨이 죽으면 되돌림도 죽는다.** 저널은 백업과 같은
   볼륨(`/backup`)에 있고, 그 볼륨은 **같은 기계 안**이다(OPS-12a 미완).
3. 저널은 **평문 JSON** 이다 — 감사 로그의 사용자명·IP 가 그대로 들어 있다.
   그 자리는 백업과 같은 취급을 받아야 한다(D-204).
4. 이것은 **감사 로그 표 하나**의 되돌림이다. 다른 표의 파기(영상·스냅샷)는
   여기서 재지 않았다.

