# UX-15 증거 — scripts/verify_wall_keys.py
# 실행: 2026-09-20 KST · 차선 N (P-207 여섯째 묶음) · HEAD 0acf8d1
# 명령: PYTHONIOENCODING=utf-8 python scripts/verify_wall_keys.py
#
# ★★ 이 판정기는 **UX-15 와 UX-16 두 절을 한 번에 잰다.** 조율자가 경계하라 한 자리다.
# 가르는가 — **반은 가르고 반은 못 가른다:**
#   ✔ 가른다: 판정기가 검사마다 **묶음표(group)를 달아 둔다** (소스 466~472줄)
#       keys  ① 키보드·입력창 갈래   ⑦ 한글 IME 상태 재현   → **UX-15**
#       alarm ② 소리 — 심각만·묶음 1회                  → **UX-15**
#       wall  ③ 글자 24 이상 ④ 자동갱신·마지막갱신 ⑤ 실패를 말한다 → **UX-16**
#       queue ⑥ 큐 화면 배선                          → **UX-16**
#     그리고 출력이 **검사마다 PASS 를 따로 찍는다** — 그래서 절별 귀속이 가능하다.
#   ✘ 못 가른다: **종료코드가 하나다.** ③이 무너지면 UX-15 도 함께 빨개진다.
#     (다만 그 고장 모양은 **거짓 빨강**이지 거짓 초록이 아니다 — 덜 위험하다.)
#
# → 그래서 **UX-15 만 닫았다.** UX-16 을 왜 안 닫았는지는 대장 UX-16 의 note 에 있다.
#   UX-15 의 분모: **술어 3종**(① ② ⑦) · 파일 5개. 셋 다 PASS.
# ---
[P-107] TARGET=저장소 C:/GuardianX/guardianx-source @ 0acf8d1 · 작업본 80파일 어긋남 (살아 있는 것을 재지 않는 게이트다)
[P-107] AS=(자격증명 없음 — 소스·문서 파일을 읽는다)
[P-107] SOURCE=저장소 작업본 트리 (0acf8d1 · 지금 읽는다)
[P-107] MEASURED=(선언 없음 — 이 게이트는 **무엇을 · 분모 몇으로** 쟀는지 말하지 않는다. 그 `exit 0` 은 「이 호출이 통과」일 뿐이다 · P-204)
[P-107]   ✗ 머리글: MEASURED= 가 없다 — **`exit 0` 은 「이 호출이 통과」다.** 무엇을 · 분모 몇으로 쟀는지 말하지 않은 0 은 초록이 아니다 (P-204)  ← verify_wall_keys.py
[WALL] [입력] 파일 5개 (주석 걷어낸 뒤) · 술어 7종
[WALL] PASS ① 키보드 · 입력창 갈래
[WALL] PASS ② 소리 — 심각만 · 묶음 1회
[WALL] PASS ③ 월 모드 글자 24 이상
[WALL] PASS ④ 월 모드 자동 갱신 · 마지막 갱신
[WALL] PASS ⑤ 월 모드 실패를 말한다
[WALL] PASS ⑥ 큐 화면 배선
[WALL] PASS ⑦ 한글 IME 상태 재현 (P-58)
[WALL] 통과 — 술어 7/7종
# EXIT=0  (← 이 0 은 **일곱 검사 전부**의 0 이다. UX-15 몫은 ①②⑦ 셋)
