[P-107] TARGET=호스트 + 컨테이너 «gx-celery-e» 의 금고 «/backup» · 증거 파일 D-373/backup_last.json
[P-107] AS=(계정 없음) — `docker exec` 로 금고를 **읽기만** 한다(`verify_backup_recovery.list_vault()` 를 통해서). 이 파일 안에 그 호출이 있으므로 **호스트에서** 부른다
[P-107] SOURCE=기계가 쓴 판정문의 `invoked_by` 칸 + 그 판정문이 가리키는 파일명을 금고의 실제 최신 덤프와 대조 — 판정문만 믿지 않는다
[P-107] MEASURED=㉤ — 마지막 **beat** 가 부른 덤프의 나이 ≤ 26h — **분모 44**(금고 덤프 40 · 회수증 후보 3 · 판정문 1). OPS-04(`verify_backup_recovery.py`)의 ㉡(바이트 신선도, 48h)과는 **다른 물음**이다 — 이 게이트는 그것을 rc 에 안 넣는다
[OPS-19] [입력] 판정문 1 · 회수증 후보 3
[OPS-19] ㉠ 판정문 초록 — 기계가 OK 를 적었다
[OPS-19] ㉤ beat 덤프 초록 — beat 가 부른 덤프가 10.1시간 전에 있다
[OPS-19] ㉢ 회수증 초록 — 복구 회수증이 1.7일 전에 있다 (restore_drill_last.json)
[OPS-19] (참고 · 이 게이트의 rc 에는 안 들어간다) OPS-04 금고 신선도: 초록 — 바이트가 있는 덤프가 10.1시간 전에 있다
[OPS-19] 초록 — beat 가 부른 덤프가 최근에 있다.
EXIT=0
