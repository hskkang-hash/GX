# P-149 · 판정기가 row_map 을 읽는다 — 그리고 그래도 수는 7 이다 (턴 R · 차선 Q)

**잰 때** 2026-09-16 (기계) · **차선 Q** · HEAD `5a403ef`(작업본 미커밋) · **커밋하지 않았다**

## 한 줄

    feature_reach  7/39  →  **7/39**   (초록 7 · 빨강 0 · 회색 25 · 잠김 7)

판정기는 이제 `row_map` 을 **입력으로 읽는다**(P-149 가 요구한 것). 그런데 수는 안 움직였다.
**목표 12 는 오지 않았고, 기준을 무르게 해서 12 를 만들지 않았다.**

## ① 무엇을 고쳤나 — `scripts/verify_feature_reach.py`

| 자리 | 전 | 후 |
|---|---|---|
| 사슬 선언 | 코드 안 `REACH_MAP` 뿐 | **`row_map` 이 먼저**, 코드가 그 다음 |
| 회색 사유 | 코드 안 `GREY_WHY` 뿐 | **`row_map` 이 적은 사유가 먼저** (절을 실제로 두드린 쪽의 말) |
| 다리 | 캡처 · 라우트 **둘** | 캡처 · 라우트 · **시험 셋** (`proof_ok` — 대장 `proof` 파일이 실재해야 한다) |
| 읽는 입력 | CONTRACT · INDEX | **+ `P-142/row_map.json`(절 단위) · `P-106/row_map.json`(페인 18행)** |

읽는 벌이 둘인 이유: **P-106 은 페인포인트 18행**이고 계약 절 39개에 절 id 가 없다.
절 단위로 두드린 벌은 **P-142** 다. 지시서가 가리킨 P-106 도 함께 읽되, 그 벌이 실제로
무엇을 들고 있는지는 아래 ③ 에 적는다.

**세 짝 규칙**: 셋 중 하나라도 없으면 **회색**이다. 초록이 아니고, 빨강도 아니다.

자기시험 **11 → 17건**. 새로 선 갈래 여섯 중 둘이 이 판정의 심장이다:

- ★★ `row_map` 이 `linked: true` 라 적어도 **캡처가 없으면 회색** — 파일이 초록을 못 만든다
- ★ 캡처·라우트가 이어져도 **시험이 없으면 초록이 아니다**
- (양성 대조) `row_map` 이 선언하고 **증거가 서면 초록** — 입력이 실제로 판정에 닿는다

## ② 왜 7 인가 — **판정기가 안 읽어서가 아니었다**

판정기의 새 입력 줄이 그대로 말한다:

    [입력] row_map 2벌 — 절 단위 25행(선언 0 · 회색 사유 25) · 페인 18행 · **INDEX 밖의 새 장 0**
      ↳ `linked:true` 인 행이 **0** 이다 — row_map 은 읽혔고, 그 파일이
        「이을 절이 없다」고 적고 있다. 판정기가 안 읽어서가 아니다

`P-142/row_map.json` 은 턴 Q 에 회색 25 를 **절마다 두드린** 벌이고 스스로 이렇게 적었다:

> `newly_linked_count: 0` · 「이미 있는 역할 계정 캡처 32장으로는 회색 25 중 한 절도
> 세 짝이 서지 않는다. 억지로 잇지 않았다」

그리고 **이번 턴에 계획된 네 화면이 서도 0절 움직인다**고 미리 적어 두었다 —
역할 홈·큐가 덮는 절(F-01-c2 · F-09-c2 · F-14-c1)은 **이미 초록**이고,
카메라 격자와 M2 사진은 **계약 F-01~F-14 의 절이 아니다**.

## ③ P-106/row_map.json 은 새 증거를 안 들고 있다 [실측]

| 행 | 든 장 | 그중 INDEX 에 **이미 있는** 장 |
|---|---|---|
| 12행(장을 든 행 전부) | 32 | **32** |
| 나머지 6행(5·9·10·11·12·17) | 0 | — |

**INDEX 밖의 새 장 0.** 그래서 이 벌을 입력으로 넣어도 더해지는 화면 증거가 없다.
「판정기가 row_map 을 안 읽는다」는 턴 Q 의 진단은 **참이었지만 그것이 유일한 벽은 아니었다** —
읽게 만든 지금, 벽은 **캡처 자체가 없다**는 쪽이다.

## ④ 회색 25 를 움직이는 것 (P-142 가 세어 둔 그대로)

| 필요한 것 | 절 수 |
|---|---|
| 설정 화면 벌(S-10·S-16·S-17·S-21) — 역할 계정 GET 한 장씩 | 9 |
| 화면이 답할 수 없는 절 — 시간·부작위·내부 판정 → **게이트로**(D-210 · F-05 선례) | 9 |
| 보고서 화면(UX-40·S-11) | 4 |
| `/dsm/dashboard` 네 상태 캡처 + 술어 변경(지금 술어는 200 만 본다 — 오류 상태는 200 이 아니다) | 1 |
| 감사 조회 화면(UX-41·S-12) | 1 |
| 관리자 설정 무권한 403 — 또는 게이트로 | 1 |

**시험 다리는 36/39 절에 이미 서 있다**(나머지 3 은 `proof` 가 빈 잠김 절). 즉 지금 막는 것은
시험이 아니라 **캡처**다.

## ⑤ 온보딩 48행 — 재측했고, **깨끗한 수가 안 나왔다**

`verify_click_completes --measure` 를 gx-shell 안에서 실제로 돌렸다
(2026-09-16T01:16:51Z · 역할 계정 · 브라우저).

    **11/48** — 초록 11 · 빨강 2 · 회색 35

**이 수를 온보딩 값으로 쓰면 안 된다.** 회색 35 중 **24 가 측정대의 사실**이다:

| 막힌 자리 | 행 | 판정기가 적은 말 |
|---|---|---|
| U3 8행 · U5 8행 | 16 | 「그 사람으로 **화면에 들어가지 못했다**」(`/login` 에 머문다) |
| U6 8행 | 8 | 「그 사람의 걷기가 끊겼다: **403**」 |

실제로 잰 것은 **24행**이고 그 안에서 초록 11 · 빨강 2 · 회색 11 이다.
U6 403 은 P-135 자격 회전 뒤 `gxprobe_q` 자격/권한과 P-146(발급 권한 역할 매핑)에 걸려 있고,
U3·U5 로그인 실패는 **계정당 동시 세션 1개**를 병렬 차선이 쥐고 있을 때 나는 모양이다
(P-106 README 가 ⑦ 영역에서 같은 자국을 적어 두었다).

**문서에서 다시 센 수**(「있는가」 술어 · 손 아닌 파일에서):

    본문 표 = U1 6.5 · U2 5.5 · U3 4.0 · U4 3.0 · U5 4.0 · U6 4.0 = **27.0 / 48** (행 48 확인)
    문서가 마지막으로 적은 수 = **29.0 / 48 = 60%** (P-98 · 턴 M 이 「안 움직였다」고 확인)

지시서 §0 의 「마지막 58~63」은 **낡았다** — 58 은 턴 F·H, 60 이 그 뒤(P-98)다.

⚠ **이 재측이 다른 차선의 자리를 뺏었을 수 있다.** 같은 턴에 F·U1·U3·U24·U56 이 돌고 있고
계정당 세션이 하나다. 다음부터 이 게이트는 **시각을 맞춰서** 불러야 한다.

## ⑥ 빨강 5 → 3종 (2026-09-15T12:24 벌 · 그 벌을 이 폴더에 보존했다)

정본 벌은 `click_completes_20260915_1224_previous.json`
(내 재측이 `P-118/click_completes.json` 을 덮었으므로 **지우지 않고 여기 남긴다**).

| 흐름 | 종 | 근거 [실측] | 몫 |
|---|---|---|---|
| **U3#3** 상황 사진 | **① 제품 결함** | 서버는 `snapshot_path` 를 내는데 **화면에 없다**(`on_screen:false`). 휴대전화 상세가 바이트를 안 그린다 | U3 (P-148) |
| **U6#1** API 키 발급 | **① 제품 결함** | `POST /api/dsm/settings/api-keys` → **403** · 본문 「설정 변경 권한이 없는 계정」. 역할에 발급 권한이 없다 | U56 (P-146) |
| **U1#19** 인계 메모 | **② 기대식 오류** | 술어 `_cell_state` 가 `after in (None,"",[],{},0)` — **참값 0 을 「아무것도 안 냈다」로 읽는다.** 화면에는 「미처리: 0건」이 실제로 적혀 있다 | 판정기 (Q) |
| **U2#1** 밤사이 요약 | **② 기대식 오류** | 위와 같은 자리(`summary?hours=12` · `unhandled=0`). 덧붙여 시드 사건이 11~12일 전이라 12시간 창이 비어 있다(③ 도 겹친다) | 판정기 (Q) |
| **U6#4** 웹훅 수신 | **③ 데이터 없음** | `POST /api/dsm/webhook-subscriptions` → **422** · 본문 「서명키 … 의 값을 이 환경에서 찾을 수 없습니다. 값은 저장소가 아니라 환경에 둡니다」. 제품이 **옳게** 거절했고 값이 없다 | U56 (P-145) |

**① 2 · ② 2 · ③ 1.** ② 둘은 **판정기 쪽 결함**이고 `verify_click_completes.py` 는 내 소유가
아니라 **고치지 않았다** — 색만 낸다. 고치면 U1#19·U2#1 은 다시 재야 한다(빨강이 아닐 수 있다).

⚠ **빨강을 회색으로 바꾸지 않았다.** 위 셋 중 U3#3·U6#1·U6#4 는 2026-09-16 재측에서
「못 쟀다(회색)」로 나왔으나, 그것은 **자리를 못 잡아서**이지 빨강이 풀린 것이 아니다.

## ⑦ PR · CR 산출기 첫 판 — `scripts/verify_readiness_scores.py`

없던 것은 셈법이 아니라 **그 표를 읽는 코드**였다. 정본은 둘 다 이미 있었다.

    ★ **PR(15관문) 50.0%** (7.5/15)  ← `GX-FCPR §2` 눈금 ↔ `§3′-2` 값 · 어긋남 **0**
    ★ **CR(8조건)  12.5%** (1.0/8)   ← `GX-REVIEW §1` · ①0 ②0.5 ③0 ④0 ⑤0 ⑥0 ⑦0 ⑧0.5
      · §12 가 적어 둔 12.5% 와 **같다** — 문서와 코드가 안 갈렸다

자기시험 **45 → 55건**. 새 갈래 열에 음성 대조 다섯이 들어 있다(모르는 낱말은 회색 ·
여덟이 아니면 빨강 · 관문이 갈리면 빨강 · §2 를 못 읽으면 회색 · §3′-2 는 §2 눈금으로 안 집힌다).

**「끝나는가」 술어의 PR 은 회색으로 남긴다** — 15칸을 **행별로** 적은 정본이 저장소에 없다
(§12 는 합계 한 줄뿐). 없는 표를 지어내지 않는다.

⚠ **산출기는 여전히 exit 1 이다.** 내가 더한 자리 때문이 아니라 **선행 빨강** 하나 때문이다:

> PRD v2.6 §3 이 **자기 자신과 갈렸다** — 표의 판정 열 {해결 7 · 반 3 · 못 함 8}
> ≠ 요약 문장 {해결 7 · 반 5 · 못 함 6}

산출기가 맞출 자리가 아니다. **PRD 를 고쳐야 한다**(세종 몫).

⚠ **RC-1 문턱이 두 문서에서 갈린다**: 지시서 §0 은 PR ≥ 85 · CR ≥ 87.5 인데
`GX-FCPR §3` 은 **PR ≥ 80**, `GX-REVIEW §12` 는 **CR ≥ 75** 다. 어느 쪽이 정본인지 정해야 한다.

## ⑧ 못 잰 것

- **온보딩 48행의 깨끗한 수** — 위 ⑤. 계정 자리를 못 잡았다.
- **새 캡처(역할 홈 4 · 큐 · M2)** — 이 턴 F·U1·U3 차선의 산출물이고, 잴 때
  `INDEX.yaml` 은 **32장 그대로**(최신 png 2026-09-07T18:56)였다. 새 장이 들어오면
  판정기는 이제 **고칠 것 없이** 그 장을 먹는다 — 그것이 이 판정의 값이다.
- **커밋하지 않았다.**
