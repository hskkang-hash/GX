# P-63 재기동 창 — **집행됨** (2026-09-05 22:20~22:50 · 턴 F 조율자 첫 일)

턴 E 에는 「할 것」으로만 남아 있었다(다섯 차선이 `gx-shell` 안에서 돌고 있었다).
턴 F 는 **차선 착수 전에** 창을 열었다 — 절차 정본은 `RUNBOOK_로컬기동.md` STEP 2D.

## ⓪ 창을 열기 전 — 확인 셋 [실측 22:20]

| 확인 | 결과 |
|---|---|
| ㉠ 시험 DB 에 붙은 연결 | **0** |
| ㉡ 이름 붙은 볼륨 | `gx_pgdata` OK · redis 볼륨 `57b682cd…593b97` |
| ㉢ 스냅샷 | `/c/GuardianX-vault/gx_20260905_2220.dump` **7,214,081 바이트** |
| (추가) celery 큐 잔량 | `default\x06\x169` = **0** · `default` = 0 — 워커가 사흘치를 이미 비웠다 |

## ① 형상 뜨기 [22:20]
`/c/GuardianX-vault/recreate/` (저장소 밖 · D-204) — `gx-shell.env`(41 vars) · `postgres.env`(3) ·
`redis.env`(0) · `gx-fe-build.env`(0) + 각 `*.json`(되돌리기 원본).

## ② ~ ⑦ 집행 [22:38~22:48]

| # | 컨테이너 | 한 일 | 결과 |
|---|---|---|---|
| ② | `gx-shell` | 재생성 + **`frontend/` 읽기전용 마운트 추가**(P-63) | ● |
| ③ | `redis` | 재생성(볼륨 그대로) | ● `PONG` · DBSIZE 96 |
| ④ | `postgres` | 재생성(`gx_pgdata` 그대로) | ● public 스키마 표 **223개** 그대로 · DB 6개 그대로 |
| ⑤ | `gx-fe-build` | 재생성 | ● (⚠ `/app` 사본이 처음으로 돌아갔다 — STEP 2C 재투입 필요) |
| ⑥ | `minio` | `compose up -d --force-recreate` + `gx-main-network` 별칭 재연결 | ● health 200 |
| ⑦ | `gx-nginx-e` | **reload 만**(재기동 아님) | ● `nginx -t` ok |

## ★ 창을 열지 않았으면 못 봤을 것 — **게이트가 유령 파일을 훑고 있었다**

`gx-shell` 의 `/repo/frontend` 는 **마운트가 아니라 사본**이었다(`docker cp` 흔적 ·
`Mounts` 에 없다). 그 사본에는 파일이 **1,956개** 있었고 호스트 저장소의 실제 프런트는
**1,003개**다. 즉 **953개가 저장소에 없는 유령**이었고, 컨테이너 안에서 도는 게이트
(`verify_ui_secrets` · `verify_ui_copy` · `verify_wall_keys` · `verify_post_arg_style` ·
`scan_frontend_success_contract` · `verify_bundle_hash`)가 그것을 훑고 있었다.

**「빌드했다」와 「서비스된다」 사이에 게이트가 선다**(턴 E 원칙)의 게이트판이다 —
**「저장소에 있다」와 「게이트가 보는 것」 사이에도 게이트가 서야 한다.**
마운트로 바꾼 뒤 **1,003개로 일치**한다 [실측 22:39].

## ⑧ 확인 — 선언이 아니라 **적용**을 본다 [실측 22:48]

### ㉠ 로그 상한 — 도커에게 물었다. **9/9 전부**

```
CONTAINER                    DRIVER     MAXSIZE  MAXFILE
guardianx-source-minio-1     json-file  10m      5
gx-beat-e                    json-file  10m      5
gx-celery-e                  json-file  10m      5
gx-fe-build                  json-file  10m      5
gx-gunicorn-e                json-file  10m      5
gx-nginx-e                   json-file  10m      5
gx-shell                     json-file  10m      5
postgres                     json-file  10m      5
redis                        json-file  10m      5
```
창을 열기 전 상한이 **없던 다섯**(`gx-shell`·`postgres`·`redis`·`minio`·`gx-fe-build`)이 전부 걸렸다.

### ㉡ 앞단 접근로그가 파일을 떠났는가 — **떠났다**
`/var/log/nginx/gx-front.access.log` = **6,612,367 바이트에서 멈춤**(mtime 13:22 · reload 이전).
reload 뒤 두드린 요청은 `docker logs gx-nginx-e` 에 나온다 —
`127.0.0.1 [05/Sep/2026:13:50:22 +0000] "GET / HTTP/1.1" 404 …`.
설정 실측: `gx-front.conf:66  access_log /dev/stdout gx;`
⚠ **죽은 파일 6.6MB 가 아직 디스크에 남아 있다** — 차선 E 가 처리한다.

### ㉢ 살아 있는가 — 전부 산다
- `postgres`: `SELECT 1` ● · `database_guardianx` public 표 223개
- `redis`: `PONG`
- `gx-shell`: `manage.py check` → **System check identified no issues (0 silenced)**
- `gx-celery-e`: `inspect ping` → **1 node online**
- `minio`: `/minio/health/live` **200**
- `gx-nginx-e`: 8500 응답(`/` 는 404 — SPA 미기동 상태의 정상값)

### ㉣ 판정기 — 아직 **빨강 하나**가 남았다
`python scripts/ops_log_collectors.py` → ① 수집기 전수 OK · ② **보존 기간 FAIL** ·
③ 자라는 속도 OK · ④ 선언 OK.
② 의 이유 둘: 죽은 nginx 접근로그 파일 · DB 감사 로그(`logger_auditlogs` 4,528행 · 10.7MB ·
보존 90일 선언 + beat 실재인데도 빨강). **차선 E 로 넘긴다 — 로그 상한을 다 걸었다고
이 줄이 초록이 되지 않는 것이 옳다**(RUNBOOK STEP 2D 말미).

## 되돌리기
`/c/GuardianX-vault/recreate/*.json` 이 재생성 **전** 형상 그대로다. 되돌린 것은 **없다**.
