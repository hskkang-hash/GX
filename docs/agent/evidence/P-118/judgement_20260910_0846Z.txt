[P-118] O 양성 대조 38/38 초록 (네 칸이 다 선 관측)
[P-118] O 기대 호출 식 38개가 **자기 URL 을 스스로 받는다**
[P-118] O 변이 9/9 를 잡는다 (음성 대조 · 규칙)
[P-118] O 출생 표본 3/3 빨강 — 「그려졌다」로 0.5 를 받던 단추 셋을 **0 으로** 읽는다 (실제로 판정 · 되돌리기 · 현장 회신)
[P-118] O 관측 0건 → 0/48 · 회색 48 (**못 잰 것이 통과가 되지 않는다**)
[P-118] O 면제 칸 없음 — 판정기는 **관측만** 받는다 (D-327)
[P-118] O 분모 48 = 여섯 사람 × 여덟 ({'U1': 8, 'U2': 8, 'U3': 8, 'U4': 8, 'U5': 8, 'U6': 8})
[P-118] 자기시험 통과

[P-118] [입력] 48건 — 온보딩 48행(여섯 사람 × 여덟, docs/agent/onboarding_48.md)
[P-118]        술어: ① 누를 것이 **실재하는가** · ② 누른 뒤 그 요청이 **나갔는가** · ③ **새 GET 으로 다시 읽어** 값이 변했는가 · ④ 그 뒤 화면이 **그 말을 하는가** — 넷이 다 서야 초록. 하나라도 못 재면 회색, 재서 안 되면 빨강
[P-118] **11/48** — 초록 11 · 빨강 13 · 회색 24   [실측 2026-09-10T08:46:01Z · TARGET=http://localhost:3002]
[P-118] 사람별: U1 3/8 · U2 0/8 · U3 2/8 · U4 0/8 · U5 2/8 · U6 4/8

  X U1#1    OOOX 교대 시작 — 로그인                값은 바뀌었는데 화면이 말하지 않는다 — 기대한 말(관제 · 대시보드 · 이벤트 · GuardianX) 중 아무것도 화면에 없다
  X U1#2    OOX? 전체 상황판 한눈에                 요청은 나갔는데 서버는 `preset`='OPERATOR' 를 내는데 **화면에 없다**
  X U1#3    OX?? 죽은 카메라 확인                  눌렀는데 `GET /api/dsm/cameras/pulse` 가 나가지 않았다 (그 사이 나간 호출 0건)
  X U1#4    OOX? 실시간 스트림 열기                 요청은 나갔는데 서버는 `counts`=3 를 내는데 **화면에 없다**
  O U1#8    OOOO 이벤트 목록 확인                  `events`=20 가 화면에 있다 · 「이벤트」 · 눌렀고 나갔다
  O U1#9    OOOO 이벤트 상세 열기                  `event_id`=4804 가 화면에 있다 · 「이벤트」 · 눌렀고 나갔다
  O U1#11   OOOO 진위 판단 — 진짜인가 오탐인가          `verdict` None → 'confirmed' · 「판정」 · 눌렀고 나갔다
  ? U1#19   OO?? 교대 인계 메모                   상태를 다시 읽지 못했다 — 다시 읽지 못했다: 다시 읽기 HTTP 502
  ? U2#1    X??? 밤사이 요약 보기                  그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  ? U2#2    X??? 미처리 이벤트 확인                 그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  ? U2#3    X??? 이벤트 등급 재판정                 그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  ? U2#4    X??? 심각 이벤트 상황 판단               그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  ? U2#6    X??? 상황보고서 생성                   그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  ? U2#9    X??? 요원별 처리 현황                  그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  ? U2#16   X??? 알림 규칙 확인                   그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  ? U2#19   X??? 장애 판단 — 시스템인가 현장인가         그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
  X U3#1    OX?? 알림 수신                      눌렀는데 `POST /api/dsm/events/\d+/notify` 가 나가지 않았다 (그 사이 나간 호출 0건)
  O U3#2    OOOO 위치 확인 — 어디로 가나             `address`='경기도 안양시 만안구 안양천서로 100 (시드 카메라)' 가 화면에 있다 · 「어디로」 · 눌렀고 나갔다
  O U3#3    OOOO 상황 사진 1장 보기                `snapshot_path`='guardianx-dev/detections/119/20260903/125631_580118.jpg' 가 화면에 있다 · 「사진」 · 눌렀고 나갔다
  X U3#7    OX?? 현장 도착 보고                   눌렀는데 `POST /api/dsm/events/\d+/response` 가 나가지 않았다 (그 사이 나간 호출 0건)
  ? U3#9    X??? 현장 상황 한 줄 보고               「회신」를 화면에서 못 찾았다 (/m/events/4804)
  ? U3#14   X??? 해당 카메라 모바일 실시간             정본이 「없음」이라 적은 자리 — 정본: 없음 — 구간 티켓은 계약 11조 잠김
  ? U3#16   X??? 근무 외 알림 차단                 정본이 「없음」이라 적은 자리 — 정본: 없음 — 알림 채널 결정 대기
  X U3#19   OX?? 내가 처리한 이벤트 목록              눌렀는데 `GET /api/dsm/deliveries` 가 나가지 않았다 (그 사이 나간 호출 0건)
  ? U4#1    X??? 주간 상황 요약                   정본이 「없음」이라 적은 자리 — 정본: 라우트는 hours=168 을 받지만 **누를 자리가 없다**
  ? U4#5    X??? 월간 보고서 자동 생성               「생성」를 화면에서 못 찾았다 (/report-template)
  ? U4#7    X??? 보고서 다운로드                   「다운로드」를 화면에서 못 찾았다 (/report-template)
  ? U4#8    X??? 특정 사건 이력 조회                「조회」를 화면에서 못 찾았다 (/dsm/events)
  X U4#9    OX?? 증빙 영상 확인                   눌렀는데 `GET /api/dsm/events/\d+/clip` 가 나가지 않았다 (그 사이 나간 호출 0건)
  X U4#11   OX?? 카메라 설치 현황                  눌렀는데 `GET /api/(dsm/cameras|stream|device)` 가 나가지 않았다 (그 사이 나간 호출 25건)
  ? U4#15   X??? 상급기관 제출 자료                 정본이 「없음」이라 적은 자리 — 정본: 없음 — 상급기관 서식 T4
  ? U4#16   X??? 감사 대응 이력                   정본이 「없음」이라 적은 자리 — 정본: 감사는 쌓이는데 **볼 자리가 없다**
  ? U5#1    X??? 사용자 계정 생성                  「추가」를 화면에서 못 찾았다 (/users)
  X U5#2    OOX? 역할 부여·변경                   요청은 나갔는데 서버는 `preset`='MANAGER' 를 내는데 **화면에 없다**
  ? U5#4    X??? 카메라 등록                     「추가」를 화면에서 못 찾았다 (/device)
  ? U5#5    X??? 카메라 설치 주소 입력               「저장」를 화면에서 못 찾았다 (/dsm/cameras/address)
  ? U5#9    X??? 알림 규칙 설정                   정본이 「없음」이라 적은 자리 — 정본: 설정 화면·라우트 없다
  ? U5#10   X??? 알림 채널 설정                   정본이 「없음」이라 적은 자리 — 정본: 없음 — 알림 채널 결정 대기(대표)
  O U5#14   OOOO 시스템 상태 확인                  `status`='waiting' 가 화면에 있다 · 「상태」 · 눌렀고 나갔다
  O U5#15   OOOO 저장 용량 확인                   `status`='waiting' 가 화면에 있다 · 「용량」 · 눌렀고 나갔다
  X U6#1    OO?? API 키로 인증                  관문이 거절했다 — 403 (권한)
  O U6#2    OOOO 이벤트 목록 조회                  `events`=5 가 화면에 있다 · 「events」 · 눌렀고 나갔다
  O U6#3    OOOO 이벤트 상세 조회                  `event_id`=4804 가 화면에 있다 · 「event_id」 · 눌렀고 나갔다
  X U6#4    OOX? 이벤트 발생 웹훅 수신               요청은 나갔는데 다시 읽었는데 `total` 가 그대로다 (0)
  X U6#9    OOOX 이벤트 상태 갱신                  값은 바뀌었는데 화면이 말하지 않는다 — 기대한 말(response_state) 중 아무것도 화면에 없다
  O U6#12   OOOO 인증 실패 처리                   상태코드 401 · 「unauthor」 · 눌렀고 나갔다
  ? U6#14   X??? 스키마 버전 확인                  정본이 「없음」이라 적은 자리 — 정본: 없음 — 스키마 버전
  O U6#15   OOOO 연계 헬스체크                    `status`='waiting' 가 화면에 있다 · 「status」 · 눌렀고 나갔다

[P-118] ★ **눌렀는데 아무것도 나가지 않는다** 6건 — 2026-09-08 의 그 모양:
      U1#3    눌렀는데 `GET /api/dsm/cameras/pulse` 가 나가지 않았다 (그 사이 나간 호출 0건)
      U3#1    눌렀는데 `POST /api/dsm/events/\d+/notify` 가 나가지 않았다 (그 사이 나간 호출 0건)
      U3#7    눌렀는데 `POST /api/dsm/events/\d+/response` 가 나가지 않았다 (그 사이 나간 호출 0건)
      U3#19   눌렀는데 `GET /api/dsm/deliveries` 가 나가지 않았다 (그 사이 나간 호출 0건)
      U4#9    눌렀는데 `GET /api/dsm/events/\d+/clip` 가 나가지 않았다 (그 사이 나간 호출 0건)
      U4#11   눌렀는데 `GET /api/(dsm/cameras|stream|device)` 가 나가지 않았다 (그 사이 나간 호출 25건)
[P-118] ★★ **요청은 나갔는데 값이 안 바뀐다** 4건 — 이 결함의 가족:
      U1#2    요청은 나갔는데 서버는 `preset`='OPERATOR' 를 내는데 **화면에 없다**
      U1#4    요청은 나갔는데 서버는 `counts`=3 를 내는데 **화면에 없다**
      U5#2    요청은 나갔는데 서버는 `preset`='MANAGER' 를 내는데 **화면에 없다**
      U6#4    요청은 나갔는데 다시 읽었는데 `total` 가 그대로다 (0)
[P-118] 회색 24건 — **못 쟀다. 초록이 아니다.** 이름을 적는다:
      U1#19   상태를 다시 읽지 못했다 — 다시 읽지 못했다: 다시 읽기 HTTP 502
      U2#1    그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U2#2    그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U2#3    그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U2#4    그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U2#6    그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U2#9    그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U2#16   그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U2#19   그 사람으로 **화면에 들어가지 못했다** (gxseed_u2_manager · /login 에 머문다)
      U3#9    「회신」를 화면에서 못 찾았다 (/m/events/4804)
      U3#14   정본이 「없음」이라 적은 자리 — 정본: 없음 — 구간 티켓은 계약 11조 잠김
      U3#16   정본이 「없음」이라 적은 자리 — 정본: 없음 — 알림 채널 결정 대기
      U4#1    정본이 「없음」이라 적은 자리 — 정본: 라우트는 hours=168 을 받지만 **누를 자리가 없다**
      U4#5    「생성」를 화면에서 못 찾았다 (/report-template)
      U4#7    「다운로드」를 화면에서 못 찾았다 (/report-template)
      U4#8    「조회」를 화면에서 못 찾았다 (/dsm/events)
      U4#15   정본이 「없음」이라 적은 자리 — 정본: 없음 — 상급기관 서식 T4
      U4#16   정본이 「없음」이라 적은 자리 — 정본: 감사는 쌓이는데 **볼 자리가 없다**
      U5#1    「추가」를 화면에서 못 찾았다 (/users)
      U5#4    「추가」를 화면에서 못 찾았다 (/device)
      U5#5    「저장」를 화면에서 못 찾았다 (/dsm/cameras/address)
      U5#9    정본이 「없음」이라 적은 자리 — 정본: 설정 화면·라우트 없다
      U5#10   정본이 「없음」이라 적은 자리 — 정본: 없음 — 알림 채널 결정 대기(대표)
      U6#14   정본이 「없음」이라 적은 자리 — 정본: 없음 — 스키마 버전

[P-118] 빨강 13 — **누른 뒤가 안 끝난다.** 그려진 것으로 점수를 주지 않는다
