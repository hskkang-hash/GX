# WP-2 EXIT 부록 — 무엇을 검증했고 무엇을 아직 모르는가 (D-262 ③④)

**생성** `python scripts/build_leak_targets.py --appendix` · **결정** D-262 · D-263 · D-261
**입력** W0-13 인구조사 · 실경로 매핑(AST) · 격리 시험 AST · 분류 등록부

> D-262 는 EXIT 문서가 **스스로** 무엇을 검증했고 무엇을 모르는지 말하게 하라고 했다.
> **모르는 것을 모른다고 적은 문서가, 다 안다고 적은 거짓 문서보다 납품에 강하다.**
> 에스비 검수의 "보안 점검 통과"(계획서 7장)도 이 문서로 답한다.

| 구분 | 종 | EXIT 조건 |
|---|---:|---|
| 검증됨 (격리 시험 `MODELS`) | 9 | ① 시나리오 5/5 |
| **P0 — 전수 시험 필요** | **15** | ② **EXIT 필수** |
| P1 — 계획 등재 | 50 | ③ EXIT 필수 아님 |
| UNREVIEWED — 사유 첨부 | 57 | ④ **자동 면제 금지** |
| 합계 (테넌트성 모델) | 131 | |

---

## 1. P1 50종 — 시험 계획 등재 (D-262 ③)

**EXIT 필수가 아니다.** 착수는 WP-2 EXIT **후**, P0 가 닫힌 다음이다.
여기 적는 이유는 착수가 아니라 **모수를 밝히기 위해서**다 — 이 표가 없으면
"P0 만 봤다"와 "P0 밖에 없다"가 구별되지 않는다.

| 모델 | 행 | group NULL | 단건 | 목록 | 본문지목쓰기 | 사유 |
|---|---:|---:|---:|---:|---:|---|
| `delivery.DeliveryOperationApprovalChecklist` | 3,013 | 100.0% | 0 | 0 | 1 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 1) — 행 3013건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `delivery.TerminalSequence` | 875 | 0.0% | 0 | 0 | 3 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 3) — 행 875건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `orders.OrderAssignment` | 261 | 100.0% | 0 | 0 | 3 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 3) — 행 261건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `delivery.DeliveryOperationApproval` | 256 | 0.0% | 0 | 0 | 1 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 1) — 행 256건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.DimensionsAndWeight` | 44 | 0.0% | 4 | 2 | 3 | 라우트가 닿는다(단건 4 · 목록 2 · 본문지목쓰기 3) — 행 44건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.PropulsionSystem` | 43 | 0.0% | 0 | 0 | 1 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 1) — 행 43건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `surveillance.SurveyMission` | 39 | 0.0% | 10 | 3 | 2 | 라우트가 닿는다(단건 10 · 목록 3 · 본문지목쓰기 2) — 행 39건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `orders.ExternalOrderStatus` | 38 | 0.0% | 5 | 6 | 1 | 라우트가 닿는다(단건 5 · 목록 6 · 본문지목쓰기 1) — 행 38건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `operation_settings.OperationSettings` | 35 | 14.3% | 8 | 6 | 1 | 라우트가 닿는다(단건 8 · 목록 6 · 본문지목쓰기 1) — 행 35건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `terminals.Routes` | 30 | 0.0% | 17 | 4 | 6 | 라우트가 닿는다(단건 17 · 목록 4 · 본문지목쓰기 6) — 행 30건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `dashboard.DashboardPanel` | 26 | 69.2% | 0 | 2 | 2 | 라우트가 닿는다(단건 0 · 목록 2 · 본문지목쓰기 2) — 행 26건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `orders.OrderItemType` | 23 | 65.2% | 0 | 2 | 1 | 라우트가 닿는다(단건 0 · 목록 2 · 본문지목쓰기 1) — 행 23건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `operational_data.OperationalDataUploadStatus` | 21 | 0.0% | 8 | 1 | 0 | 라우트가 닿는다(단건 8 · 목록 1 · 본문지목쓰기 0) — 행 21건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `dashboard.WeatherSetting` | 20 | 0.0% | 0 | 1 | 1 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 1) — 행 20건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.Protocol` | 17 | 100.0% | 7 | 1 | 3 | 라우트가 닿는다(단건 7 · 목록 1 · 본문지목쓰기 3) — 행 17건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.PackagingSpecification` | 15 | 0.0% | 7 | 2 | 1 | 라우트가 닿는다(단건 7 · 목록 2 · 본문지목쓰기 1) — 행 15건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `delivery.DeliveryStatus` | 14 | 100.0% | 17 | 0 | 16 | 라우트가 닿는다(단건 17 · 목록 0 · 본문지목쓰기 16) — 행 14건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.Library` | 13 | 0.0% | 5 | 1 | 1 | 라우트가 닿는다(단건 5 · 목록 1 · 본문지목쓰기 1) — 행 13건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.CameraType` | 12 | 0.0% | 11 | 1 | 3 | 라우트가 닿는다(단건 11 · 목록 1 · 본문지목쓰기 3) — 행 12건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `handover.HandoverContent` | 11 | 0.0% | 0 | 1 | 4 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 4) — 행 11건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `handover.HandoverNotice` | 11 | 0.0% | 0 | 5 | 7 | 라우트가 닿는다(단건 0 · 목록 5 · 본문지목쓰기 7) — 행 11건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `terminals.TerminalType` | 9 | 100.0% | 8 | 7 | 6 | 라우트가 닿는다(단건 8 · 목록 7 · 본문지목쓰기 6) — 행 9건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `terminals.Function` | 9 | 100.0% | 6 | 8 | 4 | 라우트가 닿는다(단건 6 · 목록 8 · 본문지목쓰기 4) — 행 9건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `orders.OrderStatus` | 7 | 100.0% | 8 | 0 | 8 | 라우트가 닿는다(단건 8 · 목록 0 · 본문지목쓰기 8) — 행 7건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `surveillance.SurveillanceStatus` | 7 | 100.0% | 7 | 1 | 0 | 라우트가 닿는다(단건 7 · 목록 1 · 본문지목쓰기 0) — 행 7건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `terminals.DayOfWeek` | 7 | 100.0% | 0 | 1 | 0 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 7건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.BatteryType` | 6 | 100.0% | 3 | 1 | 1 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.GNSSSystem` | 6 | 100.0% | 3 | 1 | 1 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.ImageStabilization` | 6 | 100.0% | 3 | 1 | 1 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.MotorType` | 6 | 100.0% | 3 | 1 | 1 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `devices.DeviceStatus` | 5 | 100.0% | 10 | 2 | 7 | 라우트가 닿는다(단건 10 · 목록 2 · 본문지목쓰기 7) — 행 5건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `terminals.DeactivateReason` | 4 | 100.0% | 4 | 0 | 0 | 라우트가 닿는다(단건 4 · 목록 0 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `print_format.PrintFormat` | 4 | 25.0% | 4 | 0 | 1 | 라우트가 닿는다(단건 4 · 목록 0 · 본문지목쓰기 1) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `surveillance.SurveillanceProfileRepeatType` | 4 | 100.0% | 1 | 1 | 0 | 라우트가 닿는다(단건 1 · 목록 1 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `orders.PaymentType` | 4 | 100.0% | 0 | 1 | 0 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `stream_monitors.AIModel` | 4 | 100.0% | 0 | 1 | 1 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 1) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `terminals.LocationType` | 4 | 100.0% | 0 | 1 | 0 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `partner.Partner` | 3 | 0.0% | 9 | 1 | 1 | 라우트가 닿는다(단건 9 · 목록 1 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `operational_data.OperationalData` | 3 | 100.0% | 5 | 3 | 1 | 라우트가 닿는다(단건 5 · 목록 3 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `operational_data.OperationalNotice` | 3 | 0.0% | 4 | 1 | 1 | 라우트가 닿는다(단건 4 · 목록 1 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `checklist_setting.ChecklistSettingCategory` | 3 | 100.0% | 2 | 1 | 1 | 라우트가 닿는다(단건 2 · 목록 1 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `surveillance.SurveyMissionStatus` | 3 | 100.0% | 2 | 0 | 2 | 라우트가 닿는다(단건 2 · 목록 0 · 본문지목쓰기 2) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `surveillance.MissionPurpose` | 3 | 100.0% | 1 | 4 | 1 | 라우트가 닿는다(단건 1 · 목록 4 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `delivery.DeliveryCancellation` | 3 | 0.0% | 1 | 9 | 2 | 라우트가 닿는다(단건 1 · 목록 9 · 본문지목쓰기 2) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `surveillance.SurveillanceProfileRepeatUntilType` | 3 | 100.0% | 0 | 1 | 0 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `terminals.TerminalException` | 2 | 100.0% | 7 | 0 | 0 | 라우트가 닿는다(단건 7 · 목록 0 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `flight_log.DroneAnomalyPrediction` | 2 | 100.0% | 2 | 0 | 0 | 라우트가 닿는다(단건 2 · 목록 0 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `delivery.DeliveryCancellationType` | 2 | 100.0% | 0 | 0 | 2 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 2) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `handover.HandoverShift` | 2 | 100.0% | 0 | 4 | 0 | 라우트가 닿는다(단건 0 · 목록 4 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |
| `orders.DeliveryOption` | 2 | 100.0% | 0 | 1 | 0 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을 건드릴 수 있으나 IDOR 배선이 달라 P1 로 둔다 |

> **본문지목쓰기**(pk 가 아니라 요청 본문으로 대상을 고르는 쓰기)가 있는 항목은
> IDOR 배선이 달라 P1 로 두었을 뿐 **위험이 없다는 뜻이 아니다.**
> 남의 테넌트 행을 건드릴 수 있고, 시험 방식만 다르다.

---

## 2. UNREVIEWED 57종 — 분류 사유 전건 (D-262 ④ · D-263)

> ★ **면제가 아니다.** 기계 사유는 전건 같다 —
> "핸들러 AST 추적에서 이 모델을 만지는 라우트를 **찾지 못했다**".
> **'라우트 없음'이 아니라 '못 찾았다'** 로 읽어야 한다. 동적 디스패치·시그널·
> Celery 태스크 경로는 이 추적이 보지 못한다.

아래 **선언** 열이 사람 판단이다. 선언된 것은 `backend/tests/tenant_classification.py` 에
근거와 함께 등재돼 있고, **격리 시험이 그 선언을 단언으로 지킨다**(D-261 b·c).
빈 칸은 **아직 아무도 판단하지 않았다**는 뜻이다 — 그것도 사실대로 적는다.

| 모델 | 행 | group NULL | 선언 | 근거 |
|---|---:|---:|---|---|
| `advanced_table.GridSettingUser` | 3,230 | 28.0% | deferred (별도 판단 필요) | 사용자별 표 설정(3,230행). 마스터인지 사용자 데이터인지 갈린다 — 사람 판단 필요 |
| `orders.DeliveryEvent` | 3,033 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `delivery.Address` | 1,353 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `file_management.UserMediaFile` | 1,077 | 20.8% | deferred (별도 판단 필요) | 사용자 업로드 파일(1,077행). 업무 데이터일 가능성이 높다 — 공용으로 선언하면 안 된다 |
| `advanced_table.GridSetting` | 928 | 82.2% | **shared** (전 테넌트 조회 가능) | 표 컬럼 기본 정의(928행). 화면 구성 마스터이며 업무 데이터가 아니다 |
| `menu.RoleMenu` | 923 | 4.4% | deferred (별도 판단 필요) | 역할↔메뉴 매핑(923행). 일부만 공용일 수 있다 (§3-3) |
| `delivery.DeliveryOperationHistory` | 856 | 32.7% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.PackagingOptionSpecification` | 693 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.Timezone` | 598 | 100.0% | **shared** (전 테넌트 조회 가능) | IANA 시간대 목록(598행). 테넌트마다 다른 시간대 집합을 갖지 않는다 |
| `menu.RoleTab` | 375 | 48.0% | deferred (별도 판단 필요) | 역할↔탭 매핑(375행). 같은 이유 |
| `devices.PackagingOption` | 359 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `orders.ProductItem` | 247 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.Country` | 238 | 100.0% | **shared** (전 테넌트 조회 가능) | 국가 코드 목록(238행). 국제 표준이라 테넌트별로 갈리지 않는다 |
| `menu.Menu` | 107 | 52.3% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.CoreUser` | 95 | 100.0% | deferred (별도 판단 필요) | 소유 테넌트는 UserProfileLink.group 이 이미 말한다. created_by 문제와 별개다 — W0-16 역할 분리와 함께 본다 (backfill_dryrun.md §3-3) |
| `user.UserSettings` | 93 | 3.2% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.Profile` | 92 | 1.1% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.UserProfileLink` | 92 | 1.1% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `advanced_table.UserGridManagement` | 77 | 83.1% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `menu.UserMenu` | 60 | 100.0% | **shared** (전 테넌트 조회 가능) | 메뉴 트리 정의(60행). 무엇이 보이는가는 역할이 정하지 메뉴 행이 정하지 않는다 |
| `devices.PackagingSpecificationDevice` | 45 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.CargoCompartments` | 44 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.EnvironmentalSpecification` | 44 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.NavigationControl` | 44 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.SafetyFeature` | 44 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.SensorSuite` | 44 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.Telemetry` | 44 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.FlightPerformance` | 41 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.InsuranceInformation` | 40 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.ManufacturerInformation` | 40 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.RadioCommunication` | 40 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.CurrencyFormat` | 31 | 100.0% | **shared** (전 테넌트 조회 가능) | 통화 표기 규칙(31행). 통화는 테넌트 소유가 아니다 |
| `menu.Tab` | 27 | 100.0% | **shared** (전 테넌트 조회 가능) | 탭 정의(27행). 같은 이유 |
| `devices.FrameType` | 24 | 100.0% | **shared** (전 테넌트 조회 가능) | 기체 프레임 종류(24행). 장비 분류 마스터 |
| `role.Role` | 15 | 33.3% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.FrameClass` | 14 | 100.0% | **shared** (전 테넌트 조회 가능) | 기체 등급(14행). 장비 분류 마스터 |
| `configuration.AdminConfig` | 12 | 91.7% | **shared** (전 테넌트 조회 가능) | 전역 관리 설정(11행). 정의상 전역이다 |
| `user.UserGroup` | 10 | 10.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.DateFormat` | 8 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.DeviceCamera` | 8 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `file_management.UserMediaFileItem` | 8 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.PackageType` | 7 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.WeekDay` | 7 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.NotificationChannel` | 5 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.NumberFormat` | 5 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `devices.DeviceType` | 4 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.PrivacyLevel` | 4 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.TimeFormat` | 4 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `terminals.PurposeType` | 3 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `terminals.TerminalPurpose` | 3 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.Language` | 3 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `advanced_table.GridSettingCategory` | 2 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `terminals.RouteService` | 2 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.Gender` | 2 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.Nationality` | 2 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `user.Theme` | 2 | 100.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |
| `advanced_table.SearchConditionsUser` | 2 | 0.0% | — (미판단) | 라우트를 못 찾았다는 것 외에 아직 근거가 없다 |

**선언 14종 · 미판단 43종** (합계 15,210행).

> 미판단이 남아 있는 채로 EXIT 하는 것은 **허용된다** — D-262 ④ 가 요구한 것은
> "전부 판단하라"가 아니라 "각각의 분류 사유를 문서로 첨부하라"이기 때문이다.
> 다만 **미판단을 판단으로 위장하지 않는다.** 이 표의 빈 칸이 그 정직함의 자리다.

---

## 3. 이 부록이 답하는 질문

| 질문 | 답 |
|---|---|
| 테넌트 격리를 몇 종에서 시험했나 | 지금 9종 → EXIT 시 24종 |
| 시험하지 않은 것은 몇 종인가 | 107종 — 그리고 **각각의 사유가 위에 있다** |
| 시험하지 않은 것이 안전하다고 말하나 | **아니다.** 모른다고 말한다 |
| 그 상태로 납품이 되나 | 모수를 밝힌 문서가 판정 가능한 문서다. "100%" 라 적고 대상이 9종인 것보다 강하다 |

## 4. 재생성

```bash
python scripts/map_routes_to_models.py --json   # 라우트↔모델 (AST)
python scripts/build_leak_targets.py --json     # 분류
python scripts/build_leak_targets.py --appendix > docs/agent/evidence/W0-14/exit_coverage_appendix.md
```

※ 손으로 고치지 않는다. 손으로 고치면 정본과 갈리고, 그때 어느 쪽이 진실인지 알 수 없게 된다.
