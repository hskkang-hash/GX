# WP-2 EXIT 부속 — P1 계획 등재 · P2 분류 사유 · GAP 편입 (D-262 ③④)

**작성** 2026-08-28 · **결정** D-262 ③④ · D-263(자동 면제 금지) · D-271 ② · D-272
**생성 근거** `backend/tests/tenant_census.py` (142종 전건 · `scripts/gen_tenant_census.py` 생성)

> 이 문서는 **손으로 쓰지 않았다.** 인구조사 표에서 뽑았다 —
> 손으로 쓰면 표와 문서가 갈리고, 그때 어느 쪽이 진실인지 알 수 없게 된다.

---

## 0. 모수와 분류 — 분모를 먼저 적는다

```
격리 대상 모수 142종 (술어 = groups M2M ∪ group FK · 저장소 관할 · dj-core 23종 제외)
  covered  9   이미 격리 시험 레지스트리에 있던 것
  P0      15   EXIT 필수 — 전수 등재 · 전수 green
  P1      50   계획 등재 (D-262 ③) — §1
  P2      39   미검토 · 사유 첨부 (D-262 ④) — §2
  GAP     29   인구조사가 못 보던 것 — 이번에 편입 (§3)
```

### ★ 수치 정정 — 지시서 §3⑤의 "P1 17종 · P2 99종"은 1차 값이다

| 시점 | P1 | P2/UNREVIEWED | 근거 |
|---|---:|---:|---|
| 1차 (이름 매칭) | 17 | 99 | D-260 |
| 3차 (실경로 · 단건 정의 시정) | 50 | 57 | `leak_targets_coverage.md` §7-2 |
| **이번 (저장소 관할 · dj-core 제외)** | **50** | **39** | 57 중 18종이 dj-core `user.*` |

P0 가 6→15 로 정정된 것과 같은 계열이다. **수가 조용히 바뀌지 않게** 여기 적어 둔다.

---

## 1. P1 50종 — 시험 계획 등재 (EXIT 필수 아님 · D-262 ③)

도달 가능성 분포: {'via_parent': 35, 'direct_pk': 15}

### 1-1. 착수 순서와 시기

| 묶음 | 대상 | 시기 | 근거 |
|---|---|---|---|
| **P1-a** | 행 250+ 4종 (`DeliveryOperationApprovalChecklist` 3,013 · `TerminalSequence` 875 · `OrderAssignment` 261 · `DeliveryOperationApproval` 256) | WP-2 EXIT **직후** | 행이 많고 본문지목쓰기 경로가 닿는다 — 새면 크게 샌다 |
| **P1-b** | `direct_pk` 15종 | WP-DA2a 착수 전 | pk 로 지목되므로 IDOR 배선이 P0 와 같다 — 등재 비용이 낮다 |
| **P1-c** | 나머지 (코드·마스터성 소량 테이블) | WP-DA2a 와 병행 | 행이 적고 대개 부모를 통해서만 닿는다 |

> **P1 은 "안전하다"가 아니라 "EXIT 를 막지 않는다"이다.** 그 차이를 EXIT 보고에 그대로 적는다.

| 모델 | 도달 | 행 | 분류 사유(요약) |
|---|---|---:|---|
| `delivery.DeliveryOperationApprovalChecklist` | via_parent | 3013 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 1) — 행 3013건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의… |
| `delivery.TerminalSequence` | via_parent | 875 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 3) — 행 875건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 … |
| `orders.OrderAssignment` | via_parent | 261 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 3) — 행 261건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 … |
| `delivery.DeliveryOperationApproval` | via_parent | 256 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 1) — 행 256건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 … |
| `devices.DimensionsAndWeight` | via_parent | 44 | 라우트가 닿는다(단건 4 · 목록 2 · 본문지목쓰기 3) — 행 44건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `devices.PropulsionSystem` | via_parent | 43 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 1) — 행 43건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `surveillance.SurveyMission` | direct_pk | 39 | 라우트가 닿는다(단건 10 · 목록 3 · 본문지목쓰기 2) — 행 39건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 … |
| `orders.ExternalOrderStatus` | via_parent | 38 | 라우트가 닿는다(단건 5 · 목록 6 · 본문지목쓰기 1) — 행 38건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `operation_settings.OperationSettings` | direct_pk | 35 | 라우트가 닿는다(단건 8 · 목록 6 · 본문지목쓰기 1) — 행 35건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `terminals.Routes` | direct_pk | 30 | 라우트가 닿는다(단건 17 · 목록 4 · 본문지목쓰기 6) — 행 30건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 … |
| `dashboard.DashboardPanel` | via_parent | 26 | 라우트가 닿는다(단건 0 · 목록 2 · 본문지목쓰기 2) — 행 26건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `orders.OrderItemType` | via_parent | 23 | 라우트가 닿는다(단건 0 · 목록 2 · 본문지목쓰기 1) — 행 23건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `operational_data.OperationalDataUploadStatus` | via_parent | 21 | 라우트가 닿는다(단건 8 · 목록 1 · 본문지목쓰기 0) — 행 21건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `dashboard.WeatherSetting` | via_parent | 20 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 1) — 행 20건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `devices.Protocol` | direct_pk | 17 | 라우트가 닿는다(단건 7 · 목록 1 · 본문지목쓰기 3) — 행 17건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `devices.PackagingSpecification` | direct_pk | 15 | 라우트가 닿는다(단건 7 · 목록 2 · 본문지목쓰기 1) — 행 15건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `delivery.DeliveryStatus` | via_parent | 14 | 라우트가 닿는다(단건 17 · 목록 0 · 본문지목쓰기 16) — 행 14건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의… |
| `devices.Library` | via_parent | 13 | 라우트가 닿는다(단건 5 · 목록 1 · 본문지목쓰기 1) — 행 13건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `devices.CameraType` | via_parent | 12 | 라우트가 닿는다(단건 11 · 목록 1 · 본문지목쓰기 3) — 행 12건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 … |
| `handover.HandoverContent` | via_parent | 11 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 4) — 행 11건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `handover.HandoverNotice` | via_parent | 11 | 라우트가 닿는다(단건 0 · 목록 5 · 본문지목쓰기 7) — 행 11건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `terminals.Function` | direct_pk | 9 | 라우트가 닿는다(단건 6 · 목록 8 · 본문지목쓰기 4) — 행 9건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `terminals.TerminalType` | direct_pk | 9 | 라우트가 닿는다(단건 8 · 목록 7 · 본문지목쓰기 6) — 행 9건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `orders.OrderStatus` | via_parent | 7 | 라우트가 닿는다(단건 8 · 목록 0 · 본문지목쓰기 8) — 행 7건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `surveillance.SurveillanceStatus` | via_parent | 7 | 라우트가 닿는다(단건 7 · 목록 1 · 본문지목쓰기 0) — 행 7건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `terminals.DayOfWeek` | via_parent | 7 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 7건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `devices.BatteryType` | direct_pk | 6 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `devices.GNSSSystem` | direct_pk | 6 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `devices.ImageStabilization` | direct_pk | 6 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `devices.MotorType` | direct_pk | 6 | 라우트가 닿는다(단건 3 · 목록 1 · 본문지목쓰기 1) — 행 6건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `devices.DeviceStatus` | via_parent | 5 | 라우트가 닿는다(단건 10 · 목록 2 · 본문지목쓰기 7) — 행 5건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행… |
| `orders.PaymentType` | via_parent | 4 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `print_format.PrintFormat` | direct_pk | 4 | 라우트가 닿는다(단건 4 · 목록 0 · 본문지목쓰기 1) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `stream_monitors.AIModel` | via_parent | 4 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 1) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `surveillance.SurveillanceProfileRepeatType` | via_parent | 4 | 라우트가 닿는다(단건 1 · 목록 1 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `terminals.DeactivateReason` | via_parent | 4 | 라우트가 닿는다(단건 4 · 목록 0 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `terminals.LocationType` | via_parent | 4 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 4건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `checklist_setting.ChecklistSettingCategory` | via_parent | 3 | 라우트가 닿는다(단건 2 · 목록 1 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `delivery.DeliveryCancellation` | via_parent | 3 | 라우트가 닿는다(단건 1 · 목록 9 · 본문지목쓰기 2) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `operational_data.OperationalData` | direct_pk | 3 | 라우트가 닿는다(단건 5 · 목록 3 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `operational_data.OperationalNotice` | direct_pk | 3 | 라우트가 닿는다(단건 4 · 목록 1 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `partner.Partner` | direct_pk | 3 | 라우트가 닿는다(단건 9 · 목록 1 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `surveillance.MissionPurpose` | via_parent | 3 | 라우트가 닿는다(단건 1 · 목록 4 · 본문지목쓰기 1) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `surveillance.SurveillanceProfileRepeatUntilType` | via_parent | 3 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `surveillance.SurveyMissionStatus` | via_parent | 3 | 라우트가 닿는다(단건 2 · 목록 0 · 본문지목쓰기 2) — 행 3건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `delivery.DeliveryCancellationType` | via_parent | 2 | 라우트가 닿는다(단건 0 · 목록 0 · 본문지목쓰기 2) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `flight_log.DroneAnomalyPrediction` | via_parent | 2 | 라우트가 닿는다(단건 2 · 목록 0 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `handover.HandoverShift` | via_parent | 2 | 라우트가 닿는다(단건 0 · 목록 4 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `orders.DeliveryOption` | via_parent | 2 | 라우트가 닿는다(단건 0 · 목록 1 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |
| `terminals.TerminalException` | via_parent | 2 | 라우트가 닿는다(단건 7 · 목록 0 · 본문지목쓰기 0) — 행 2건으로 P0 문턱(50) 미만이거나 **pk 를 지목하는 경로가 없다**. 본문지목쓰기는 남의 행을… |

---

## 2. P2 39종 — 미검토 · **각각의 사유** (D-262 ④ · 자동 면제 금지)

도달 가능성 분포: {'no_route': 36, 'direct_pk': 3}

> **P2 는 면제가 아니다.** D-263 이 금지한 것이 "라우트가 안 보이니 안전하다"이고,
> 1차 측정에서 93종이 같은 앱에 단건 경로를 가지고 있었다.
> 여기 적힌 사유는 **"지금 아는 것"의 기록**이지 결론이 아니다.

| 모델 | 도달 | 행 | 분류 사유(요약) |
|---|---|---:|---|
| `advanced_table.GridSettingUser` | no_route | 3230 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `orders.DeliveryEvent` | no_route | 3033 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `delivery.Address` | no_route | 1353 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `file_management.UserMediaFile` | no_route | 1077 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `advanced_table.GridSetting` | no_route | 928 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `menu.RoleMenu` | no_route | 923 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `delivery.DeliveryOperationHistory` | no_route | 856 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.PackagingOptionSpecification` | no_route | 693 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `menu.RoleTab` | no_route | 375 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.PackagingOption` | no_route | 359 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `orders.ProductItem` | no_route | 247 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `menu.Menu` | direct_pk | 107 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `advanced_table.UserGridManagement` | no_route | 77 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `menu.UserMenu` | no_route | 60 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.PackagingSpecificationDevice` | no_route | 45 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.CargoCompartments` | no_route | 44 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.EnvironmentalSpecification` | no_route | 44 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.NavigationControl` | no_route | 44 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.SafetyFeature` | no_route | 44 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.SensorSuite` | no_route | 44 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.Telemetry` | no_route | 44 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.FlightPerformance` | no_route | 41 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.InsuranceInformation` | no_route | 40 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.ManufacturerInformation` | no_route | 40 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.RadioCommunication` | no_route | 40 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `menu.Tab` | direct_pk | 27 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.FrameType` | no_route | 24 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `role.Role` | direct_pk | 15 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.FrameClass` | no_route | 14 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `configuration.AdminConfig` | no_route | 12 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.DeviceCamera` | no_route | 8 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `file_management.UserMediaFileItem` | no_route | 8 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.PackageType` | no_route | 7 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `devices.DeviceType` | no_route | 4 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `terminals.PurposeType` | no_route | 3 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `terminals.TerminalPurpose` | no_route | 3 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `advanced_table.GridSettingCategory` | no_route | 2 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `advanced_table.SearchConditionsUser` | no_route | 2 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |
| `terminals.RouteService` | no_route | 2 | 핸들러 AST 추적에서 이 모델을 만지는 라우트를 찾지 못했다. **'라우트 없음'이 아니라 '못 찾았다'로 읽어야 한다** — 동적 디스패치·시그널·태스크 경로는 이… |

---

## 3. GAP 29종 — 인구조사가 못 보던 것 (이번에 편입)

도달 가능성 분포: {'via_parent': 10, 'direct_pk': 4, 'no_route': 15}

`leak_targets.json`(131종)은 백필 dry-run 의 **덤프 집계**에서 나왔고, 덤프 집계는
**행이 있는 표만** 센다. 그래서 이 29종이 목록 밖에 있었다.

> **행이 0인 것은 "격리가 필요 없다"가 아니라 "아직 안 썼다"이다.**
> 내일 첫 행이 들어오는 순간 그 모델은 무방비로 시작하고, 목록에 없으니 아무도 보지 않는다.
> **행이 있는데도 빠져 있던 것이 3종**이었다 — 그쪽은 0행 아티팩트가 아니라 집계 누락이다.

이제 `test_census_matches_code` 가 표와 코드를 대조하므로, 새 모델이 생기면 시험이 실패한다.

| 모델 | 도달 | 행 | 분류 사유(요약) |
|---|---|---:|---|
| `stream_monitors.StreamMonitorAIModel` | via_parent | 14 | 인구조사에 없었는데 **14행이 있다** — 0행 아티팩트가 아니라 집계 누락이다 |
| `handover.HandoverNoticeComment` | via_parent | 3 | 인구조사에 없었는데 **3행이 있다** — 0행 아티팩트가 아니라 집계 누락이다 |
| `stream_monitors.DrawingSession` | via_parent | 1 | 인구조사에 없었는데 **1행이 있다** — 0행 아티팩트가 아니라 집계 누락이다 |
| `article.Article` | direct_pk | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `delivery.DeliveryReturn` | via_parent | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `devices.DeviceGNSS` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `devices.DeviceProtocol` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `devices.IMU` | direct_pk | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `devices.Unit` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `discuss.Comment` | via_parent | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `discuss.Following` | direct_pk | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `discuss.Rating` | via_parent | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `discuss.Topic` | direct_pk | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `guardian.GroupObjectPermission` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `logger.AuditLogs` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.Bank` | via_parent | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.DeliveryProof` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.GuessUser` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.Invoice` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.OrderAssignmentProcess` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.OrderComment` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.PaymentCallback` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.Recipient` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.RefundOrder` | via_parent | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.ReturnOrder` | via_parent | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `orders.Sender` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `stream_monitors.DetectionEvent` | no_route | None | 인구조사에 없었고 셀 수도 없다 — 저장소 모델 ↔ 운영 스키마 불일치 (P-LOCAL-4) |
| `tag.Tag` | via_parent | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |
| `tag.TaggedItem` | no_route | 0 | 인구조사(덤프 집계)에 없었다 — 행이 0이라 안 보였다. **아직 안 쓴 것이지 안전한 것이 아니다** |

---

## 4. 이 문서를 다시 만드는 법

```bash
docker exec gx-shell sh -c 'cd /app && PYTHONPATH=/app python /gxscripts/gen_tenant_census.py'
```

수가 달라지면 **그 차이가 조사 대상**이다. 맞을 때까지 인자를 고치지 않는다.
