# 창 2b 사전 점검 — 대표 문장이 가리키는 것과 지금 실물 [실측 2026-09-24 21:1x · 조율자 · 읽기만]

대표 문장(09-24 대화 · 그대로):
> 「09-26 14:00 창 2b 열어라 · VAPID 3 · SMTP 5 · WEBHOOK_SIGNING_KEYS 이름 · 시험 DB 넷 지워라(표 그대로)」

창은 09-26 14:00 이다. 그 전에 **재생성 없이 알 수 있는 것**을 먼저 봤다. 이번에는 아무것도 바꾸지 않았다.
지운 것 0 · 바꾼 환경 0 · 값 출력 0(이름 · 길이 · sha256 앞 12자만).

## 한눈에

| 항목 | 문장 | 지금 실물 | 창에서 할 일 |
|---|---|---|---|
| VAPID 3 | 싣는다 | **이미 실려 있다** — 턴 AE 창 2b(`709a5aa` · 09-23) · 두 통 == `.env.vapid` | **다시 만들지 않는다**(새 쌍 = 이미 구독한 것이 또 끊긴다) · 도달 1 실측만 |
| SMTP 5 | 싣는다 | **금고에 파일이 없다** — `C:\GuardianX-vault\smtp.env` 없음 | 13:50 까지 금고에 없으면 **SMTP 만 빠진다**(없는 값을 지어내지 않는다) |
| `WEBHOOK_SIGNING_KEYS` | 이름 | 두 통 모두 **이름이 없다** · 금고에도 없다 | 창 안에서 새로 만들어 금고 → `--env-file` (VAPID 와 같은 길) |
| 시험 DB 넷 | 지운다(표 그대로) | **표의 넷 중 둘만 남아 있다** · 표 밖 **14개** | 아래 §3 — 대표 한 마디가 더 필요하다 |

## §1 VAPID — 이미 실려 있다

| 자리 | `GX_VAPID_PUBLIC_KEY` | `GX_VAPID_PRIVATE_KEY` |
|---|---|---|
| `gx-gunicorn-e` | 87 · `f688c72a46b9` | 43 · `331969183dd2` |
| `gx-celery-e` | 87 · `f688c72a46b9` | 43 · `331969183dd2` |
| `.env.vapid`(저장소 뿌리 · `.gitignore:69`) | 87 · `f688c72a46b9` | 43 · `331969183dd2` |
| 금고 `webpush/vapid_20260917.env` (옛 쌍) | 87 · `879067f2f9a1` | 43 · `909ed33a72ca` |

→ 두 통은 **09-23 새 쌍**을 들고 있다. 09-24 05:39 에 `-old` 셋을 치우고 로그 상한을 붙여 다시 만들 때도 새 쌍이 따라왔다.
세종 WO-12 §3 은 창 2b 를 「미집행」으로 적었는데, **VAPID 몫은 턴 AE 에 이미 집행됐다.** 남은 것은
**도달 1(실기기)** 이다. 그때 못 잰 까닭은 제품이 아니라 도구였다. Playwright 크로미움에는 FCM 에 등록할 구글 API 키가 없다.
그래서 **사람의 브라우저 한 대**가 있어야 닿는다(대표 PC 크롬 · `[훈련]` 제목).

## §2 SMTP 5 · 서명키

| 이름 | 두 통 | 금고 |
|---|---|---|
| `SMTP_SERVER` · `SMTP_PORT` · `SMTP_USERNAME` · `SMTP_PASSWORD` | **있다** — 컨테이너를 만들 때 들어간 옛 값(`SMTP_SERVER` 12자 · `SMTP_PASSWORD` 5자) | `smtp.env` **없음** |
| `SMTP_TLS`(다섯째) | **없다** | 없음 |
| `WEBHOOK_SIGNING_KEYS` | **없다** — `settings.py:1335` 가 빈 표로 읽는다 | 없음 |

- 비밀번호 찾기 메일은 `SMTPEmailBackend` 가 `os.getenv("SMTP_*")` 로 읽는다(dj-core · 턴 AH 실측). 금고의 다섯 값이 그 이름 그대로여야 한다.
- 서명키 값의 모양은 `sdn=<값>,agency=<값>`(`settings.py:1322`)이다. 창 안에서 `secrets.token_urlsafe` 로 만들고 곧장 금고에 쓴다. 화면에는 이름·길이만 낸다.

## §3 시험 DB — 표(09-18)와 지금

`docs/agent/evidence/P-168/test_db_table_20260918.md` 의 넷:

| # | 이름 | 09-18 표 | 지금 |
|---|---|---|---|
| 1 | `test_database_guardianx` | 31 MB | **없다** |
| 2 | `test_database_guardianx_e2e` | 32 MB | **없다** |
| 3 | `test_gx_lane_u24` | 32 MB · 마지막 쓰기 09-17 | **있다** · 32 MB · 마지막 쓰기 **09-21 09:46**(뒤 차선이 다시 썼다) |
| 4 | `test_gx_u3b` | 32 MB · 09-17 | **있다** · 32 MB · 09-17 00:51 그대로 |

**1·2 가 없어진 까닭:** 전량 시험은 `--create-db` 로 이 이름을 만들고 **끝나면 스스로 지운다**(pytest-django 기본).
표를 찍은 날 남아 있던 것은 끝까지 못 간 실행의 찌꺼기였다. 그 뒤 정상 실행이 같은 이름을 만들었다가 치웠다.
**누가 몰래 지운 것이 아니다.**

**표 밖 14개** — 전부 연결 0 · 앱 DB 아님:

| 이름 | 크기 | 마지막 쓰기 | 어디서 |
|---|---|---|---|
| `test_database_guardianx_p11836` | 38 MB | 09-23 08:46 | `backend/conftest.py:202` 다툼 가드 — 두 실행이 같은 이름을 쥐면 `_p<pid>` 로 가른다. 끝까지 못 간 실행이 남긴다 |
| `test_database_guardianx_p12539` | 33 MB | 09-23 08:46 | 〃 |
| `test_database_guardianx_p13147` | 38 MB | 09-23 08:46 | 〃 |
| `test_database_guardianx_p41547` | 34 MB | 09-23 08:46 | 〃 |
| `test_database_guardianx_p6586` | 22 MB | **09-24 10:33** | 〃 — 오늘 세션이 끊겨 조율자가 죽인 전량 실행(㉞)의 것으로 보인다 [추정] |
| `test_gx_lane_u24b` · `test_gx_lane_u3` · `test_gx_lane_u56` · `test_gx_fix1` · `test_gx_u24c` · `test_gx_u1b` | 32~36 MB | 09-21 09:46 | 차선 `DB_TEST_NAME` (규칙 안) |
| `test_gx_u56` · `test_gx_u24` | 32~33 MB | 09-21 12:00 | 〃 |
| `test_gx_u24ab` | 32 MB | 09-22 05:24 | 〃 |

- 합계: 표의 남은 둘 64 MB · 표 밖 14개 462 MB · 앱 DB `database_guardianx` 588 MB(연결 14 · **후보 아님**).
- 차선 이름(`test_gx_*`)은 턴 AI 의 차선이 **다시 만든다**(`--create-db`). 지워도 다음 실행에 해가 없다. 되돌릴 수 없는 것은 **그 안의 자료**뿐이고, 시험 자료다.

### 대표께 여쭐 한 가지

「표 그대로」는 **넷 중 남은 둘**(`test_gx_lane_u24` · `test_gx_u3b`)만 가리킨다. 표 밖 14개는 문장이 덮지 않으니 조율자가 지우지 않는다.
- ㉠ **「둘만」** — 문장 그대로. 14개는 이 표로 다음 결정에.
- ㉡ **「열여섯 전부(이 표 그대로)」** — 이 문서 §3 이 새 표가 된다. 창 안에서 연결 0 을 다시 확인하고 지운다.

## §4 창 2b 를 **헤드리스는 못 연다** — 대표가 대화형 세션에 있어야 한다

`LOOP_PROTOCOL.md §3`: 「창(재생성)·시험 DB 처분 같은 대표 결정 집행은 헤드리스로 하지 않는다 — 대표가 있는 대화형
세션에서만」. 헤드리스 허용 목록도 `docker run` · `docker stop` · `DROP` 을 거부한다. 09-26 13:50 에 루프의 영실은
`waiting_ceo` 로 넘긴다. **14:00 에 대표 PC 에서 대화형 Claude Code 한 창**이 있어야 창이 열린다.
그 창에서 실기기 웹푸시 도달 1 도 대표 크롬으로 잰다(§1).
