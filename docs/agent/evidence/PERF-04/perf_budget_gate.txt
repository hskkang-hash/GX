# PERF-04 증거 — scripts/verify_perf_budget.py
# 실행: 2026-09-20 KST · 차선 N (P-207 다섯째 묶음) · HEAD 0acf8d1
# 명령: PYTHONIOENCODING=utf-8 python scripts/verify_perf_budget.py
# ⚠ 이 판정기는 차선 F 의 증거를 읽는다. F 의 벽시계 A/B 창은 **16:25~16:55 예약**이고
#   지금은 안 열렸다 — 그 전에 잰다(겹치지 않는다). F 가 새로 재면 이 수는 바뀔 수 있다.
# ★ 조율자가 「지난 턴 색」을 말했다가 지웠다 — 예상 없이 그냥 쟀다.
# ---
[P-107] TARGET=http://localhost:8000 (gx-shell 안 · 호스트에 포트가 없다)
[P-107] AS=GX_ROUTE_USER 가 선언되지 않았다 — **누구로 잴지 모르는 채 잰 것이다**
[P-107] SOURCE=살아 있는 서버 응답 (HTTP) — 사진도 손 목록도 아니다
[P-107] MEASURED=(선언 없음 — 이 게이트는 **무엇을 · 분모 몇으로** 쟀는지 말하지 않는다. 그 `exit 0` 은 「이 호출이 통과」일 뿐이다 · P-204)
[P-107]   ✗ 머리글: MEASURED= 가 없다 — **`exit 0` 은 「이 호출이 통과」다.** 무엇을 · 분모 몇으로 쟀는지 말하지 않은 0 은 초록이 아니다 (P-204)  ← verify_perf_budget.py
[PERF-BUDGET] 자기시험 통과 — 초록 1 · 예산초과 2(모드별) · **회귀 1** · **P-53 응답시간/오류율 가르기 10**(출생 표본 턴 D 502 8건 포함) · 출생 표본 1(캐시 적중) · 회색 3 · 동시성 대조 2 · 잡음 대조 3 · 걸침 대조 3 · **회귀 걸침 3** · **모드 7** · 종료코드 7 · 구조 3 · 표 대조 2
[PERF-BUDGET]   F05 캐시 금지 (P-19)                                     이 면은 캐시 금지 면이 아니다 — 검사하지 않는다
[PERF-BUDGET]   SCREEN 캐시 금지 (P-19)                                  이 면은 캐시 금지 면이 아니다 — 검사하지 않는다
[PERF-BUDGET]   MEDIA 캐시 금지 (P-19)                                   이 면은 캐시 금지 면이 아니다 — 검사하지 않는다
[PERF-BUDGET]   STATUS 캐시 금지 (P-19)                                  1자리 전부 `BYPASS_PATTERNS` 에 걸린다
[PERF-BUDGET]   F05 계약 F-05 진입면 (NFR-05-1) · 예산 500ms                p95(가운데) 213ms ≤ 500ms (n=780 · 오류 1 · 3벌 166–225ms) · ⚠ 표본에 오류 1건(0.13%)이 섞였다 — 이 칸은 **응답시간만** 판정한다. 오류율은 아래 「[전체] 가용성」 칸이 낸다 (P-53)
[PERF-BUDGET] ? F05 계약 F-05 진입면 (NFR-05-1) · 회귀 +20%                 **판정 불가** — 측정 잡음 28% 가 문턱 20% 보다 크다 (p95 165.6–225.1ms · 3벌). `runserver` 는 한 프로세스라 꼬리가 스케줄러를 탄다 — 회귀 감시는 gunicorn+nginx 뒤에 뜻이 선다 (OPS-13)
[PERF-BUDGET]   SCREEN 화면 API · 예산 800ms                             p95(가운데) 238ms ≤ 800ms (n=780 · 오류 1 · 3벌 230–290ms) · ⚠ 표본에 오류 1건(0.13%)이 섞였다 — 이 칸은 **응답시간만** 판정한다. 오류율은 아래 「[전체] 가용성」 칸이 낸다 (P-53)
[PERF-BUDGET] ? SCREEN 화면 API · 회귀 +20%                              **판정 불가** — 측정 잡음 25% 가 문턱 20% 보다 크다 (p95 229.9–290.0ms · 3벌). `runserver` 는 한 프로세스라 꼬리가 스케줄러를 탄다 — 회귀 감시는 gunicorn+nginx 뒤에 뜻이 선다 (OPS-13)
[PERF-BUDGET]   MEDIA 스냅샷·미디어 · 예산 1000ms                            p95(가운데) 187ms ≤ 1000ms (n=780 · 오류 0 · 3벌 178–228ms)
[PERF-BUDGET] ? MEDIA 스냅샷·미디어 · 회귀 +20%                              **판정 불가** — 측정 잡음 27% 가 문턱 20% 보다 크다 (p95 177.6–228.4ms · 3벌). `runserver` 는 한 프로세스라 꼬리가 스케줄러를 탄다 — 회귀 감시는 gunicorn+nginx 뒤에 뜻이 선다 (OPS-13)
[PERF-BUDGET]   STATUS 상태·가용성 (캐시 금지 · P-19) · 예산 300ms              p95(가운데) 181ms ≤ 300ms (n=780 · 오류 0 · 3벌 164–218ms)
[PERF-BUDGET] ? STATUS 상태·가용성 (캐시 금지 · P-19) · 회귀 +20%               **판정 불가** — 측정 잡음 30% 가 문턱 20% 보다 크다 (p95 164.1–217.6ms · 3벌). `runserver` 는 한 프로세스라 꼬리가 스케줄러를 탄다 — 회귀 감시는 gunicorn+nginx 뒤에 뜻이 선다 (OPS-13)
[PERF-BUDGET]   [전체] 가용성 · 5xx ≤ 0.1%                                5xx 2/3,120 = **0.064%** ≤ 0.1% (SLA 가용성 99.9% 월 환산 · 세종 P-53) · 면별 F05 1/780=0.13% · MEDIA 0/780=0.00% · SCREEN 1/780=0.13% · STATUS 0/780=0.00% · 주입 제외 **0건**(뺀 것이 없다 — 이 계량은 주입하지 않는다) · 5xx 코드별 502×2
[PERF-BUDGET]   [전체] 계약 · 4xx 0건                                     4xx 0/3,120
[PERF-BUDGET] ★ 이 수는 **합격선 모드**로 잰 것이다 — 대상이 runserver 가 아니다(Server: 'nginx'). 절대 예산도 빨강이고, 위 단서(기준선)는 이 수에 붙지 않는다
[PERF-BUDGET] ★★ **한 벌로 판정하지 마라** — 최근 4벌의 5xx 비율이 0.194% · 0.083% · 0.000% · 0.064% 로 예산 0.1% 를 **걸친다.** 어느 벌을 쓰느냐로 색이 갈린다 — 이 게이트의 색은 지금 코드가 아니라 **그 순간의 기계 상태**를 함께 담고 있다 loadavg 2.72 1.26 0.71 · loadavg 5.01 2.03 0.79 3/581 196781 · loadavg 2.92 2.41 1.54 2/529 4364 · loadavg 3.67 2.96 2.05 1/532 4811
[PERF-BUDGET] 잰 때 2026-09-15T05:06:32+00:00 · 기준선 2026-09-05T02:50:35+00:00
[PERF-BUDGET] **회색(exit 2)** — 못 쟀다. 위의 ? 가 그 자리다. 회색은 초록이 아니다 (D-301)
# EXIT=2
