============================================================
 GuardianX 개발 → 스테이징 형상 미러 — DRY-RUN
 시각 2026-09-05T14:26:08Z
============================================================

 ⚠ 계획이다. **원격 서버에 아무것도 보내지 않았다.**

── 1. **옮기지 않는 것** (이 목록이 이 스크립트의 절반이다) ────────────────────────────────────────────
  ✗ 테넌트 데이터 — 사용자·카메라·이벤트·배송·영상
  ✗ 감사 로그(logger_auditlogs) — 정본은 하나여야 한다
  ✗ .env 의 **값** (열쇠 이름만 맞춘다)
  ✗ MinIO 객체(영상·이미지)
  ★ 스테이징에 필요한 데이터는 **씨앗으로 새로 만든다.** 개발 DB 를 덤프해서
    붓는 순간 스테이징이 개인정보 처리 시스템이 되고, 그 시스템에는 접근 통제도
    파기 주기도 서 있지 않다.

── 2. ref — 태그 하나로 못박는다 ────────────────────────────────────────────
  [실측] HEAD cce7f7b
  [실측] 가장 최근 태그: rc-sdn-excl-0.1
  [어긋남] 작업 트리가 **깨끗하지 않다** — 커밋 안 된 것이 스테이징에 안 간다
  [계획] git tag -a stg-YYYYMMDD-N -m '스테이징 배포' && git push origin stg-…

── 3. 설정 열쇠 — 개발과 스테이징의 **이름 집합**을 견준다 (값은 안 본다) ────────────────────────────────────────────
  [실측] backend/.env.stg — 열쇠 25개 (본보기 backend/.env.example 는 75개)
  [어긋남] backend/.env.stg 에 **없는** 열쇠: AI_ANALYSIS_URL AI_GRPC_URL AI_RTSP_PATH ANYANG_SERVICE_KEY BACKEND_URL CHILDCARE_SAFETY_SERVICE_NAME CHILDCARE_SAFETY_URL DATA_GO_KR_KEY_DECODED DATA_GO_KR_KEY_ENCODED DB_TEST_NAME DISASTER_CCTV_SERVICE_NAME DISASTER_CCTV_URL DJANGO_CSRF_TRUSTED_ORIGINS EXTERNAL_API_CACHE_TTL_SEC EXTERNAL_API_ENABLED EXTERNAL_API_RETRY EXTERNAL_API_TIMEOUT_SEC GCS_APIKEY GUARDIANX_ENVIRONMENT HOLIDAY_API_URL IS_USE_WEBRTC JUSO_API_KEY JUSO_API_URL JUSO_POPUP_KEY_1 JUSO_POPUP_KEY_2 K2_ALERT_CHANNEL K2_ALERT_EMAIL_TO K2_OPS_ALERT_ROLE_CODES K2_SEND_ALLOWED_DOMAINS KAKAO_API_KEY KAKAO_GEOCODING_URL KAKAO_JS_API_KEY KMA_APIHUB_KEY KMA_MID_FCST_URL KMA_SHORT_FCST_URL KMA_WARNING_URL MINIO_USE_HTTPS NLPRK_SAFETY_SERVICE_NAME NLPRK_SAFETY_URL OPENSEARCH_HOST OPENSEARCH_INDEX OPENSEARCH_PASSWORD OPENSEARCH_USERNAME RTSP_URL SCHOOL_LOCATION_URL SENDER_EMAIL SMTP_PASSWORD SMTP_PORT SMTP_SERVER SMTP_USERNAME STREAM_URL TENANT_ADMIN_ROLE_PREFIX TENANT_GLOBAL_ADMIN_ROLE_CODES TENANT_SCOPE_ENFORCE TENANT_TRUST_LEGACY_SUPERUSER
  [주의] backend/.env.stg 에만 있는 열쇠: JWT_ACCESS_TOKEN_LIFETIME JWT_REFRESH_TOKEN_LIFETIME JWT_SECRET_KEY MINIO_SECURE SIGNALR_HUB_URL  (스테이징 전용이면 정상)
  [실측] frontend/.env.stg — 열쇠 14개 (본보기 frontend/.env.example 는 16개)
  [어긋남] frontend/.env.stg 에 **없는** 열쇠: VITE_CGS_APIKEY VITE_ENABLE_DEMO VITE_GCS_API_URL VITE_GCS_TELEMETRY_API_URL VITE_USE_POLLING
  [주의] frontend/.env.stg 에만 있는 열쇠: VITE_MAP_COUNTRY VITE_MAP_LANGUAGE VITE_MAP_PROVIDER  (스테이징 전용이면 정상)
  ★ **값은 여기서 옮기지 않는다.** 열쇠 하나가 빠지면 그 기능이 조용히 기본값으로
    떨어지고, 기본값으로 도는 것은 아무도 안 본다 — 그래서 이름만이라도 견준다.

── 3b. 빠진 열쇠가 **무엇으로 떨어지는가** — 안전한 기본값과 위험한 기본값을 가른다 ────────────────────────────────────────────
  [어긋남] TENANT_SCOPE_ENFORCE 가 .env.stg 에 **없다** → 코드 기본값 False 로 떨어진다 [실측]
  [어긋남] TENANT_TRUST_LEGACY_SUPERUSER 가 .env.stg 에 **없다** → 코드 기본값 True 로 떨어진다 [실측]
  [어긋남] GUARDIANX_ENVIRONMENT 가 .env.stg 에 **없다** → 코드 기본값 "development" 로 떨어진다 [실측]
  [어긋남] K2_SEND_ALLOWED_DOMAINS 가 .env.stg 에 **없다** → 코드 기본값 [] 로 떨어진다 [실측]
  [어긋남] K2_OPS_ALERT_ROLE_CODES 가 .env.stg 에 **없다** → 코드 기본값 [] 로 떨어진다 [실측]
  [어긋남] DJANGO_CSRF_TRUSTED_ORIGINS 가 .env.stg 에 **없다** → 코드 기본값 (settings.py 에서 못 찾았다) 로 떨어진다 [실측]
  ★★ 이 여섯을 고른 이유: 앞의 셋은 **없으면 위험한 쪽**으로 떨어지고
     (테넌트 격리 강제 꺼짐 · 옛 superuser 신뢰 켜짐 · 환경 이름이 development),
     뒤의 셋은 **없으면 안전한 쪽**으로 떨어진다(허용 목록이 비면 실발송 0 · P-41).
     둘을 한 목록으로 두면 「열쇠 55개 빠짐」이라는 한 줄이 되고, 그 한 줄은
     아무도 안 읽는다.

── 4. 스키마 — 덤프가 아니라 마이그레이션으로 옮긴다 ────────────────────────────────────────────
  [실측] 이 저장소가 들고 있는 마이그레이션 파일 289 개
  [계획] docker compose -f docker-compose.stg.yml exec -T backend-stg python manage.py migrate --plan
  [계획]   ↑ **먼저 --plan 으로 무엇이 도는지 본다.** 스테이징에서 처음 도는 마이그레이션이
  [계획]     운영에서도 처음 돈다 — 스테이징은 그것을 미리 겪는 자리다.
  ✗ pg_dump/pg_restore 로 개발 DB 를 붓지 않는다 (위 1의 이유)

── 5. 이미지 — 스테이징에서 **다시 빌드**한다 ────────────────────────────────────────────
  [실측] 이 기계의 이미지: 2 개(guardianx-*)
  [안함] docker compose -f docker-compose.stg.yml build --pull
  ★ 개발 기계의 이미지를 save/load 로 옮기지 않는다 — 그 이미지에는 개발 기계의
    빌드 캐시·마운트 흔적이 섞여 있고, 무엇이 들어갔는지 되짚을 수 없다.

── 6. 앞단 — 번들은 **해시로** 확인한다 ────────────────────────────────────────────
  [계획] python scripts/verify_bundle_hash.py     # 뜬 번들이 이 ref 의 것인가
  ★ 「배포했다」와 「그 배포가 떠 있다」는 다른 사실이다(D-301).

── 7. 옮긴 뒤 — 사람이 아니라 판정기가 답한다 ────────────────────────────────────────────
  [계획] ops_monitor.py --check              # 감시 14신호(감사 큐 지연 포함 · P-65)
  [계획] ops_log_collectors.py               # 로그 상한(OPS-07)
  [계획] verify_send_allowlist.py            # 실발송 허용 도메인(P-41)
  [계획] verify_purge.py                     # 파기가 그대로 지우는가(P-57)

============================================================
 어긋난 자리 **9 개** — 위 [어긋남] 줄을 보라.
 모드 DRY-RUN — **아무것도 옮기지 않았다**
============================================================
