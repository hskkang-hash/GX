# OPS-13a — 502 와 워커 재활용의 동시성 (2026-09-07T10:07:25+00:00)

**이 파일은 `scripts/verify_front_line_502.py` 가 실행하며 적었다.**

## 502 는 언제 나는가 — **대조와 함께** [실측]

읽은 것: 앞단 접근로그 3722줄(시각 창 09:49:31–09:53:37) · 뒷단 자국 둘 — 예고(`Autorestarting`) 17건 · **종료(`Worker exiting`) 16건**
표본: 502 **8건** · 정상 200 3714건(대조로 400건 솎음) · 재시도 27건 · 되살림 19건

| 창 | 502 | 정상 200 (대조) |
|---|---|---|
| **±0초 (같은 초)** | 2/8 = 25% | 66/400 = 16% |
| ±1초 | 8/8 = 100% | 168/400 = 42% |
| ±5초 | 8/8 = 100% | 338/400 = 84% |
| **요청 구간 × `Worker exiting`** | 6/8 = 75% | 69/400 = 17% |

★ 마지막 줄이 판정에 쓰는 자다. 위 세 줄은 **종전 자**이고 지우지 않는다 —
  지우면 「무엇을 바꿔서 설명이 되었는가」가 사라진다(P-92 · 턴 J).

★ **대조가 없으면 이 표를 못 읽는다.** 재활용이 잦으면 창을 넓히는 것만으로
  무엇이든 재활용 옆에 놓인다 — 위 표의 아래 줄들이 그 사실을 스스로 말한다.

## 판정

  OK   ① 표본         502 8건 · 대조 200 400건 · 재활용 17건
  FAIL ② 설명         **요청 구간 안의 `Worker exiting` 으로도 설명되지 않는 502 가 2건** — 알려진 창에 묻지 마라. 이것은 다른 결함이다
  OK   ③ 대조 분리      요청 구간 · `Worker exiting` — 502 75% 대 정상 17% (차 58%p ≥ 30%p)
  OK   ④ 삼킴         앞단이 재시도 27건 중 **19건을 되살렸다**(손님은 200 을 봤다) · 새어 나간 502 8건

판정 **실패** (exit 1).
