# OPS-13a — 502 와 워커 재활용의 동시성 (2026-09-07T10:07:22+00:00)

**이 파일은 `scripts/verify_front_line_502.py` 가 실행하며 적었다.**

## 502 는 언제 나는가 — **대조와 함께** [실측]

읽은 것: 앞단 접근로그 3722줄(시각 창 09:44:14–09:49:30) · 뒷단 자국 둘 — 예고(`Autorestarting`) 20건 · **종료(`Worker exiting`) 16건**
표본: 502 **0건** · 정상 200 3722건(대조로 400건 솎음) · 재시도 30건 · 되살림 30건

| 창 | 502 | 정상 200 (대조) |
|---|---|---|
| **±0초 (같은 초)** | 0/0 = 0% | 59/400 = 15% |
| ±1초 | 0/0 = 0% | 148/400 = 37% |
| ±5초 | 0/0 = 0% | 348/400 = 87% |
| **요청 구간 × `Worker exiting`** | 0/0 = 0% | 75/400 = 19% |

★ 마지막 줄이 판정에 쓰는 자다. 위 세 줄은 **종전 자**이고 지우지 않는다 —
  지우면 「무엇을 바꿔서 설명이 되었는가」가 사라진다(P-92 · 턴 J).

★ **대조가 없으면 이 표를 못 읽는다.** 재활용이 잦으면 창을 넓히는 것만으로
  무엇이든 재활용 옆에 놓인다 — 위 표의 아래 줄들이 그 사실을 스스로 말한다.

## 판정

  OK   ① 표본         이번 부하에서 502 **0건** — 맞댈 양성이 없다. 0 은 좋은 소식이되 **이 부하 이 순간의** 0 이다
  GRAY ② 설명         502 가 없어 설명할 대상이 없다
  GRAY ③ 대조 분리      502 가 없어 갈릴 것이 없다
  OK   ④ 삼킴         앞단이 재시도 30건 중 **30건을 되살렸다**(손님은 200 을 봤다) · 새어 나간 502 0건

판정 **못 쟀다** (exit 2).
