# OPS-13a — 502 와 워커 재활용의 동시성 (2026-09-07T10:07:20+00:00)

**이 파일은 `scripts/verify_front_line_502.py` 가 실행하며 적었다.**

## 502 는 언제 나는가 — **대조와 함께** [실측]

읽은 것: 앞단 접근로그 3722줄(시각 창 09:39:50–09:44:13) · 뒷단 자국 둘 — 예고(`Autorestarting`) 21건 · **종료(`Worker exiting`) 16건**
표본: 502 **3건** · 정상 200 3718건(대조로 400건 솎음) · 재시도 37건 · 되살림 34건

| 창 | 502 | 정상 200 (대조) |
|---|---|---|
| **±0초 (같은 초)** | 3/3 = 100% | 71/400 = 18% |
| ±1초 | 3/3 = 100% | 161/400 = 40% |
| ±5초 | 3/3 = 100% | 325/400 = 81% |
| **요청 구간 × `Worker exiting`** | 3/3 = 100% | 80/400 = 20% |

★ 마지막 줄이 판정에 쓰는 자다. 위 세 줄은 **종전 자**이고 지우지 않는다 —
  지우면 「무엇을 바꿔서 설명이 되었는가」가 사라진다(P-92 · 턴 J).

★ **대조가 없으면 이 표를 못 읽는다.** 재활용이 잦으면 창을 넓히는 것만으로
  무엇이든 재활용 옆에 놓인다 — 위 표의 아래 줄들이 그 사실을 스스로 말한다.

## 판정

  OK   ① 표본         502 3건 · 대조 200 400건 · 재활용 21건
  OK   ② 설명         502 3건 **전부**가 `Worker exiting` 을 요청 구간 안에 갖는다 (종전 자 ±0초·`Autorestarting` 로는 미설명 0건이었다 — 그 0건은 다른 결함이 아니라 **판정기의 눈**이었다)
  OK   ③ 대조 분리      요청 구간 · `Worker exiting` — 502 100% 대 정상 20% (차 80%p ≥ 30%p)
  OK   ④ 삼킴         앞단이 재시도 37건 중 **34건을 되살렸다**(손님은 200 을 봤다) · 새어 나간 502 3건

판정 **통과** (exit 0).
