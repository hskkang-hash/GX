# W0-1b 증거 — 운영 실키 53개가 평문으로 발견됨 (2026-08-22)

**발견 경위** RUNBOOK_로컬기동 STEP 1 인벤토리 중 · **조치** 금고 격리 완료 · **대표 승인** 2026-08-22

> **이 티켓의 긴급도가 바뀐 이유는 하나다.** 지금까지 W0-1b 는 "`.env.example` 에 커밋됐던
> 값을 예방적으로 재발급한다"는 위생 작업이었다. 이제는 **살아 있는 운영 키 53개가
> 통째로 복사되기 쉬운 11GB 폴더 안에 평문으로 있었다**는 사고 대응이다.

---

## 1. 무엇이 어디에 있었나

```
발견 위치:  C:\GuardianX\GuardianX build\gx-build-20260210\gx-build\config\
              gx_be.env   (1,682 bytes)
              gx_fe.env   (  861 bytes)
```

| 파일 | 실값이 든 항목 | 비고 |
|---|---|---|
| `gx_be.env` | **38 / 39** | 값이 빈 것은 1개뿐 |
| `gx_fe.env` | **15 / 15** | 전부 실값 |

**키 이름만 세었다. 값은 읽지도 옮겨 적지도 않았다** (D-204 — 검증은 형태로 한다).

### 1-1. W0-1b 재발급 대상 15개와의 대조

티켓 `spec` 이 지목한 15개 항목이 **전부 여기에 실값으로 있다.**

| 티켓의 대상 | `gx_be.env` | `gx_fe.env` |
|---|---|---|
| `DB_PASSWORD` | ✅ | — |
| `MINIO_ACCESS_KEY` · `MINIO_SECRET_KEY` | ✅ ✅ | — |
| `SMTP_USERNAME` · `SMTP_PASSWORD` | ✅ ✅ | — |
| `GCS_APIKEY` | ✅ | — |
| `OPENSEARCH_USERNAME` · `OPENSEARCH_PASSWORD` | ✅ ✅ | — |
| `KAKAO_API_KEY` | ✅ | — |
| `ANYANG_SERVICE_KEY` | ✅ | — |
| `VITE_KAKAO_API_KEY` | — | ✅ |
| `VITE_GOOGLE_MAPS_API_KEY` | — | ✅ |
| `VITE_TURN_USERNAME` · `VITE_TURN_PASSWORD` | — | ✅ ✅ |
| `VITE_CGS_APIKEY` | — | ✅ |

**추가로 티켓이 몰랐던 항목**: `DJANGO_SECRET_KEY`.
이것은 세션 서명·CSRF·비밀번호 재설정 토큰의 뿌리다. **재발급 목록에 넣어야 한다** —
이 값이 새면 나머지 14개를 다 바꿔도 세션을 위조할 수 있다.

---

## 2. 조치 — 삭제가 아니라 격리

```
옮긴 곳:  C:\GuardianX-vault\secrets-20260210\
남긴 것:  <원위치>\config\README_MOVED.md  (경로 · 복원 절차 · 폐기 시점)
```

**삭제하지 않은 이유**: `docker-compose.yml` 이 `./config/gx_be.env`(L128·L156)와
`./config/gx_fe.env`(L183)를 `env_file` 로 참조한다. 지우면 기동이 깨진다.
기동 직전에 복사해 오고 끝나면 지우는 절차를 `README_MOVED.md` 에 적었다.

**저장소에는 넣지 않았다** (D-002 · 금지 #1). 이 문서에도 값은 없다.

---

## 3. 위험 판정 — 무엇이 실제로 위험한가

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | `VITE_*` **5개** | **이미 공개된 것으로 간주** | 브라우저 번들에 실려 배포됐다. 폐기가 유일한 대응이며 금고 격리와 무관하다 |
| 2 | `DJANGO_SECRET_KEY` | **최우선** | 세션·CSRF·토큰 서명의 뿌리. 다른 키를 다 바꿔도 이것이 남으면 세션 위조가 된다 |
| 3 | `DB_PASSWORD` · `MINIO_*` · `OPENSEARCH_*` | 높음 | 운영 데이터 직접 접근 |
| 4 | `SMTP_*` | 높음 | 발신 도용 — 피싱에 쓰이면 조직 신뢰가 상한다 |
| 5 | `KAKAO_API_KEY` · `ANYANG_SERVICE_KEY` | 중간 | 과금·쿼터. `ANYANG_SERVICE_KEY` 는 발급기관 문의 필요 |
| 6 | `GCS_APIKEY` | 중간 | **D-003**: `VITE_CGS_APIKEY`(클라이언트)와 **다른 값**으로 재발급할 것 |

> **노출 범위는 아직 모른다.** 이 11GB 폴더가 언제부터 어디에 복사됐는지가 실제 위험을 정한다.
> 담당 개발자 퇴사 시점의 반출 여부를 포함해 **사람이 확인할 사항**이다 (§5).

---

## 4. DoD 에 추가할 것

티켓의 dod 는 "15개 항목 전부 재발급 완료. 구 키로 API 호출 시 401/403 확인"이다.
발견 사실에 따라 **두 줄이 더 필요하다** (정본 수정은 대표 승인 사항):

1. `DJANGO_SECRET_KEY` 를 재발급 대상에 포함하고, 교체 후 **전 세션 무효화**를 확인한다.
2. 재발급이 끝나면 `C:\GuardianX-vault\secrets-20260210\` 의 사본도 **폐기**한다.
   무효가 된 키를 보관할 이유가 없고, 보관하면 "어느 쪽이 진짜인가"를 다시 판단해야 한다.

---

## 5. 사람이 해야 할 것 (에이전트가 대신할 수 없다)

- [ ] **노출 범위 판정** — 이 폴더가 외부로 복사·전달된 적이 있는가. 퇴사자 반출 포함
- [ ] `DJANGO_SECRET_KEY` 포함 **16개 재발급** (각 콘솔 접근 필요 — D-010)
- [ ] 구 키로 호출해 **401/403 확인** (재발급했다는 말이 아니라 이것이 증거다)
- [ ] `ANYANG_SERVICE_KEY` 발급기관 문의
- [ ] 재발급 후 금고 사본 폐기

> **키를 바꾸기 전에 노출 범위부터 본다.** 이미 쓰이고 있었다면 재발급은 대응의 시작이지
> 끝이 아니다 — 무엇이 그 키로 무엇을 했는지 로그를 봐야 한다.
