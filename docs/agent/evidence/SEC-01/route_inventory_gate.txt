# SEC-01 증거 — scripts/verify_route_inventory.py
# 실행: 2026-09-20 KST · 차선 N (P-207) · 브랜치 turn-q · HEAD 0acf8d1 · 호스트 파이썬
# 명령: PYTHONIOENCODING=utf-8 python scripts/verify_route_inventory.py
# ---
[P-107] TARGET=gx-shell 컨테이너 · DJANGO_SETTINGS_MODULE=config.settings (앱과 같은 설정) · 호스트에서 부르면 docker exec 로 위임한다
[P-107] AS=(HTTP 계정 없음) — gx-shell 안 Django ORM 으로 읽는다 · DB 자격은 앱이 들고 있는 것 그대로(이름: DATABASE_URL / POSTGRES_*)
[P-107] SOURCE=살아 있는 레지스트리를 gx-shell 에서 다시 뽑아 파일 docs/agent/evidence/D-343/route_inventory.json (기록 2026-09-19T18:18 · 자기기록 measured_at=2026-09-19T16:18:43+07:00 · 514434바이트)
[ROUTE-LEDGER] [입력] 라우트 763건 · 대장 763건 (인벤토리 측정일 2026-09-19T16:18:43+07:00 · 21.5시간 전)
[ROUTE-LEDGER] 들어오는 키: 선언 2 · 지금 닿는 자리 549 · 관문 없음 73
[ROUTE-LEDGER] PASS 대장과 코드가 갈리지 않았다 (입력 신선 — 21.5시간 전 측정)
# EXIT=0

# ── 추가 [실측 2026-09-20 15:55 · 차선 N] 판정 도중에 **기준이 바뀌었다** ──────
# 위 캡처 뒤, `scripts/_gate_header.py` 가 15:50 에 P-204 검사(`MEASURED=`)를 얻었다.
# 같은 판정기를 다시 부르니 머리글에 줄 둘이 **새로 붙는다**(exit 는 그대로 0):
#   [P-107] MEASURED=(선언 없음 — ... 그 `exit 0` 은 「이 호출이 통과」일 뿐이다 · P-204)
#   [P-107]   ✗ 머리글: MEASURED= 가 없다 ... 분모 몇으로 쟀는지 말하지 않은 0 은 초록이 아니다
# 본문 판정은 안 바뀌었다: [입력] 라우트 763건 · 대장 763건 · PASS · EXIT=0 (21.6시간 전 측정).
# ★ 이것은 이 절의 문제가 아니라 **저장소 전체의 문제**다 — `scripts/verify_*.py` **79개 중
#   `measured=` 를 넘기는 것이 0개**다. 차선 N 은 판정기를 안 고쳤다(P-203 · Q 소유).
