# 잠금 대장 — **무엇이 막고 있고, 누가 푸는가** (D-307)
#
# ★ 지금까지 잠김은 전부 "막혔다" 한 단어였다. 셋은 성격이 전혀 다르다:
#     DEV       개발로 풀린다        → 일정 문제
#     CONTRACT  상대방 이행이 필요   → 책임 문제
#     ADMIN     우리 쪽 사무 1회     → **하루면 풀리는데 안 한 것**
#     OPS       현장이 아는 값       → **우리가 만들 수 없는 것** (D-338)
#   섞어 두면 하루짜리가 계약 리스크 뒤에 숨는다. 그래서 유형을 칸으로 만든다.
#
# ⚠ `unlock_type` · `unlock_owner` 가 비면 `scripts/verify_blockers.py` 가 exit 1 한다.
#   등재는 면제가 아니라 **선언**이다 — 여기 이름을 적는 일이 곧 "이것이 막혀 있음을
#   알고 있고, 누가 푸는지도 안다"는 진술이다 (D-264 · D-281 계열).
#
# ═══════════════════════════════════════════════════════════════════════════
# ★ 판정 술어 (D-323) — 「끝났는가」가 아니라 **「지금 막혀 있는가」**
# ═══════════════════════════════════════════════════════════════════════════
#
#     blocked_now = 「그것 때문에 **지금 이 환경에서** 막혀 있는가」
#
#   · **발급 완료 ≠ 전달 완료 ≠ 환경 존재.** 세 사실을 한 칸에 두지 않는다 (D-290 계열).
#   · 「유형을 사실과 다르게 적으면 그 순간 이 대장이 하는 일이 없어진다.」
#   · 잠김 총수와 **지금 막고 있는 수**는 다른 수다. 게이트가 둘을 따로 출력한다.
#
#   verified_at / verified_by — **"완료했다"는 진술이 아니라 확인 행위가 잠금을 내린다.**
#   두 칸이 비면 exit 1. 무엇으로 확인했는지(파일·호출·화면)를 적는다.
#
#   ★ 없앤 것과 미룬 것은 다르다 — `blocked_now: false` 인 항목을 **지우지 않는다.**
#     지우면 나중에 왜 안 했는지 아무도 모른다.
#
# ★ D-333 — CONTRACT·ADMIN(= 우리 밖에 기대는 잠금)에는 두 칸이 더 필요하다:
#     internal_alternative_considered : 무엇을 검토했는가 (없으면 "없음"이라고 적는다)
#     why_not                          : 왜 안 되는가
#   외부 의존 하나를 **안 만드는 것**이 외부 의존 하나를 잘 만드는 것보다 언제나 싸다.

meta:
  decision: D-307
  updated_at: "2026-09-02"
  enum:
    unlock_type: [DEV, CONTRACT, ADMIN, OPS]
  # ★ D-320 — CONTRACT 항목에는 칸 셋이 더 필요하다. **잠김이 조용히 늙지 않게** 한다.
  #   sent_at : 발송일 · due_by : 회신 기한(발송 + 15일 · 계약 3조2항 준용)
  #   경과 일수는 게이트가 **매 실행마다 계산해 출력**한다 —
  #   시간이 지나는 것이 화면에 보여야 협상 카드가 된다.

blockers:

  - id: SDN_API_SPEC
    title: 에스비정보기술 SDN 컨트롤러 API 명세 미수령
    unlock_type: CONTRACT
    unlock_owner: 에스비정보기술 (독촉·기한 연장 통지는 대표)
    blocks: [F-06, F-07, F-08, F-13, E2E-3]
    since: "2026-08-25"          # 계약 3조2항이 정한 제공 기한
    sent_at: "2026-09-04"        # 발송문 발송 완료 (대표 회신)
    due_by: "2026-09-19"         # 발송 + 15일 (계약 3조2항 준용)
    blocked_now: true
    verified_at: "2026-09-06"
    verified_by: |
      backend/adapters/sdn/__init__.py 의 KERNEL_READY=False 를 파일에서 확인 ·
      명세 문서가 docs/design/DA-02 에 수령분으로 들어온 것이 없음을 저장소에서 확인.
    internal_alternative_considered: |
      추측 필드로 Mock 어댑터를 먼저 세우는 안. 그리고 **포트·수령요건만 세우고 멈추는 안.**
    why_not: |
      뒤의 것을 **채택했고 이미 서 있다**(포트 34건 · A차단 24). 앞의 것은 안 한다 —
      추측 필드 위의 어댑터는 재작업이 확정이고 D-280 위반이다. 상대방의 명세는
      우리가 아는 것으로 대체할 수 없다 — I-1~I-5 의 필드 이름은 **그쪽 사실**이다.
    why: |
      Mock 조차 I-1~I-5 의 필드 이름과 타입이 있어야 만들 수 있다(DA-02 §0).
      추측 필드 위의 어댑터는 재작업이 확정되고 D-280 위반이다.
      ★ 2026-09-04 **잠금의 성격이 바뀌었다**(D-320): 「미발송」 → 「발송·회신 대기」.
        이로써 계약 6조3항(협조 지연 시 이행기한 자동 연장)의 **기산점이 섰다.**
        이건 개발 진척이 아니라 **계약상 지위의 변화**다 — 이제 지연의 책임 소재가
        문서로 남고, 게이트가 경과 일수를 매 실행마다 출력한다.
    unlock_step: |
      명세 수령. 포트·수령요건 34건(A차단 24)은 이미 서 있으므로,
      오면 `SdnPort` 구현체 하나를 채우고 KERNEL_READY 를 올리면 끝난다.
    state_in_code: backend/adapters/sdn/__init__.py — KERNEL_READY=False + NOT_READY_REASON

  - id: JUSO_API_KEY
    title: 도로명주소 팝업 API 승인키 2건 — 발급됨 · **이 환경에 없음** · 그러나 막고 있지 않다
    unlock_type: ADMIN
    unlock_owner: 대표 (키 전달 1회) — 다만 **지금 요청드리지 않는다**
    blocks: [카메라 주소 입력 팝업 UI (D-331)]
    since: "2026-08-31"          # D-298 로 FX-5 착수가 승인된 날
    blocked_now: false
    verified_at: "2026-09-06"
    verified_by: |
      대표 승인 화면 2건에서 **API 유형**을 실측(「도로명주소 팝업 API」) ·
      backend/adapters/juso/__init__.py 의 JUSO_REVERSE_SUPPORTED='no' 를 파일에서 확인 ·
      K5 표 ②(backend/kernels/k5_trust/credentials.py)에 JUSO_POPUP_KEY_1/2 로 등재.
      ★ 이 두 칸은 이제 손으로 적는 칸이 아니라 **조회 결과**다 —
        `kernels.k5_trust.refresh_credential(name=..., verified_by=...)` 이 채운다(D-325).
    internal_alternative_considered: |
      **카메라 설치 주소**(D-330). 카메라는 고정 설치물이므로 설치할 때 주소를 안다.
    why_not: |
      ★ 반대다 — **내부가 이겼다.** 이벤트 위치는 그 카메라의 주소이고, 외부 호출 0건 ·
      타임아웃 없음 · 비용 0 이다. 결과까지 더 좋다: 「정문 (서울시 …로 12)」.
      남은 팝업 키의 용처는 **주소를 입력할 때 오타를 막는 것** 하나이고, 그것은
      지금 막고 있는 일이 아니다.
    why: |
      ★ 2026-09-06 **사유가 통째로 바뀌었다** — 두 가지가 동시에 일어났다(D-332):
        ① 키가 왔다(파일로). 다만 **아직 개발 환경에 넣지 않았다** — 발급 ≠ 전달 ≠
           환경 존재(D-323). 그래서 이 항목은 남는다.
        ② 그 키로 FX-5 를 할 수 없음이 확정됐다(D-329). API 유형이 **도로명주소 팝업 API**
           였다 — 브라우저에 주소검색 창을 띄우는 UI 위젯이고 서버 조회 API 조차 아니다.
           **막고 있던 이유 자체가 사라졌다.**
      그래서 유형은 ADMIN 그대로이되 `blocked_now: false` 다.
      ★ 지우지 않는 이유: **없앤 것과 미룬 것은 다르다.** 지우면 나중에 왜 안 했는지
        아무도 모르고, 다음 사람이 같은 키를 다시 신청한다.
      직전(2026-09-04) 사유는 「승인키 미전달 + 방향 미판정」이었다 — 그 기록도 남긴다.
    unlock_step: |
      표 ①②가 선 뒤에, 팝업 키를 프론트엔드 설정값으로 넣고 카메라 등록 화면에
      주소검색 팝업을 붙인다(D-331). **값은 저장소·문서 어디에도 적지 않는다**(D-319).
      상태는 K5 표 ②에서 `refresh_credential` 로 올린다 — 손으로 적지 않는다.
    state_in_code: backend/kernels/k5_trust/credentials.py — JUSO_POPUP_KEY_1/2 (typed)

  # ★ CCTV_SERVICE_NAME 은 **여기서 내려갔다** (D-321, 2026-09-04).
  #   근거: 활용신청 현황 18건 전수에 없고, **신청할 필요도 없다.**
  #   포털의 재난안전 CCTV 정보는 "어디에 CCTV 가 있는지" 의 **메타데이터**이지 영상이
  #   아니다. 우리는 CCTV 영상을 **직접 받는 회사**다(L1 현장 연계) — 설치 위치 목록은
  #   우리 기능 어디에도 입력으로 들어가지 않는다.
  #
  #   ★ 그동안 이것이 잠금으로 남아 있던 이유는 하나다 — **이름이 우리 도메인과 닮아서.**
  #     "CCTV·재난안전정보" 는 우리가 하는 일처럼 들린다. 그러나 하는 일이 아니다.
  #     **이름의 유사성을 기능의 필요성으로 읽은 것** — 이것도 착시의 한 종류다.
  #   국립공원(5)·아동센터(6)도 같다: 컨텍스트 보강용이고 계약 AC 어디에도 걸리지 않는다.
  #   셋의 상태는 .env.example 에 `source: doc_only` + `status: not_required` 로 남는다.

  - id: F03_POLYGON_DESIGN
    title: F-03 위험구역 폴리곤 — 좌표 표현·좌표계 미확정 · **해소됨**
    unlock_type: DEV
    unlock_owner: Code (설계 확정 후)
    blocks: []
    since: "2026-09-01"
    resolved_at: "2026-09-10"
    blocked_now: false
    verified_at: "2026-09-10"
    verified_by: |
      [2026-09-10 · 해소 후 실측]
        ZONE_POLYGON_READY=True · `_point_in_polygon` 실재 ·
        backend/tests/test_zone_judgment.py 31 passed (경계 사례 넷 포함) ·
        scripts/verify_zone_polygon.py exit 0 — 「구현 없이 상수만 올린 것」 갈래도 초록.

      ★ 잠금 규약이 **값을 냈다**: 상수를 올리자 게이트가 test_f03_polygon_contract_ac 를
        요구했고, 구현만 올리고 시험을 안 낼 길이 없었다(D-299).
    why: |
      ★ **해소 [판정 D-365 · 2026-09-10]** — 미룬 이유가 사라져서가 아니라
        **미루는 값보다 갚을 절의 값이 커져서** 골랐다. 고른 것을 고른 자리에 적는다:

          표현    GeoJSON Polygon 부분집합 (RFC 7946) · 바깥 고리 하나 · 구멍 없음
          좌표계  WGS84 · [경도, 위도] 순서 · 평면 판정
          경계    경계 위의 점은 **안** (BOUNDARY_IS_INSIDE=True) —
                  경계에 선 사람을 밖으로 세면 놓친다. 헛경보는 사람이 지우고
                  놓친 것은 아무도 모른다
          거절    자기교차 폴리곤은 **판정하지 않는다** — '안' 이 하나로 정해지지 않고,
                  어느 규칙을 골라도 화면이 그린 모양과 갈린다

        [해소 전] GeoJSON 인지 좌표쌍 배열인지, WGS84 인지 TM 인지가 정해지지 않았고,
        지금 한쪽을 고르면 그 선택이 곧 F-03 의 계약이 됐다(D-280).
        그동안 kind='camera_group' 이 같은 질문에 답했다(D-299).
    unlock_step: |
      (해소 완료) F-03 설계 확정 → ZONE_POLYGON_READY=True →
      test_f03_polygon_contract_ac 가 의무가 됨. 세 단계 모두 2026-09-10 에 끝났다.
    state_in_code: backend/stream_monitors/services/zones.py — ZONE_POLYGON_READY=True

  - id: CLIP_EXTRACTION
    title: F-09 영상 구간 추출·트랜스코딩 미구현
    unlock_type: DEV
    unlock_owner: Code
    blocks: [F-09 영상 재생(바이트)]
    since: "2026-09-02"
    blocked_now: true
    verified_at: "2026-09-06"
    verified_by: |
      backend/stream_monitors/services/clips.py 의 CLIP_EXTRACTION_READY=False 를 파일에서 확인.
    why: |
      바이트 구간 읽기 · 키프레임 경계 맞춤 재인코딩 · 재저장 셋이 필요하고,
      특히 키프레임 판단은 코덱·컨테이너별로 달라 실측 없이 고르면 그 선택이 곧
      F-09 의 계약이 된다(D-280). 그동안 재생 라우트는 **구간에 묶인 만료 티켓**만 내고
      바이트를 흘리지 않는다 — 계약 11조를 어기지 않는 유일한 상태다.
    unlock_step: |
      F-09 영상 재생 설계(코덱·전송 방식·보존 기간) 확정 → CLIP_EXTRACTION_READY=True.
      올리는 순간 test_extraction_integrity_contract_ac 가 의무가 된다.
    state_in_code: backend/stream_monitors/services/clips.py — CLIP_EXTRACTION_READY=False

  # ★ D-397 — **화면 16장을 여는 중에 나왔다.** 그리고 둘을 가른다:
  #   ① 이 환경에서 500 인 것 — 원인은 MinIO 부재다. 환경이다
  #   ② 저장소가 죽었을 때 화면이 「자료 없음」으로 보이는 것 — **환경이 아니라 설계다**
  #   ②는 운영에서도 난다. MinIO 가 잠깐만 죽어도 당직자는 「영상이 없다」고 읽는다.
  - id: MEDIA_LIST_SHOWS_EMPTY_WHEN_STORAGE_DOWN
    title: 저장소 미도달 시 미디어 목록이 오류 대신 **빈 목록**으로 보인다
    unlock_type: DEV
    unlock_owner: Code (화면 쪽은 rj-core 인수 화면이라 그쪽 판정이 필요하다)
    blocks: [UX-09]
    since: "2026-09-14"
    blocked_now: true
    verified_at: "2026-09-18"
    verified_by: |
      ★ 갱신 [실측 2026-09-18 · P-5]: **MinIO 를 세웠고 500 은 사라졌다** —
        두 자리 모두 `200 · success:true`. `route-alive` 도 초록이 되었다.
        그러나 **이 등재는 내려가지 않는다.** 여기 적힌 결함은 「저장소가 죽었을 때」의
        일이고, 저장소를 실제로 내려 다시 재어 보니 **그때가 더 나빠졌다:**
            저장소 살아 있을 때   200 · success:true · 1건
            저장소 **내렸을 때**   200 · success:true · 1건   ← 아무 일도 안 일어난다
        `minio_client.available` 이 **기동 시 한 번** 정해지고 다시 보지 않기 때문이다
        (`stream_monitors/utils/minio_client.py:21,41` — `__init__` 에서만 True 가 된다).
        예전에는 저장소가 없으면 최소한 **500 으로 소리쳤다.** 지금은 조용하다.
        운영에서 새벽 두 시에 저장소가 죽으면 화면은 계속 「자료가 있습니다」라고 말하고,
        당직자는 파일을 눌러 본 뒤에야 안다. 자세한 것은 MINIO_AVAILABILITY_IS_BOOT_TIME_ONLY.
      [실측 2026-09-14] `scripts/capture_screens.py` 16장 실행 중 `/media-data` 화면이 부른
      `GET /api/media-data/?page_size=25&current_page=1` 이 **500**.
      토큰으로 직접 재현했고 응답 본문을 그대로 받았다. 화면은 그 500 을 받고도
      「0 of 0」 을 그려 **자료가 없는 것처럼** 보였다 (본문에 오류 문구 0자).
      `scripts/verify_route_alive.py` 가 같은 자리를 **죽은 라우트 2건**(경로 끝
      빗금 있는 것과 없는 것)으로 잡고 exit 1 이다 — 게이트는 빨간 채로 둔다(D-327).
    why: |
      ★ 이번 턴에 **응답이 말하던 두 거짓말은 고쳤다** [실측]:
        · `success: true` · `status: 500`  →  `success: false`
          (dj-core `BaseResponse(success=True, …)` 가 기본값이다. 안 넘기면 조용히 참이 된다)
        · 「Failed to retrieve **operational notice** list」 → 「Failed to retrieve media list」
          (`MESSAGE_ENUM.GET_LIST_FAILED` 하나를 여러 앱이 나눠 쓰는데 영문이 한 앱 것이었다)
      **그러나 화면은 그대로다.** 화면이 `success` 를 보지 않기 때문이고, 그 화면은
      인수 자산이다. 즉 **서버는 이제 진실을 말하는데 화면이 안 듣는다.**
      ★ 500 자체는 이 환경에 MinIO 가 없어서다(`minio.invalid`). 그것은 환경이다.
        여기 등재하는 것은 **「저장소가 죽었을 때 사용자가 그것을 알 수 있는가」**이고,
        지금 답은 **아니오**다 (D-378 — 결함 등급은 보이는 것으로 정한다).
    internal_alternative_considered: |
      같은 저장소에 이미 **저하 운전** 처리가 있다 —
      `[DEGRADED] 외부 의존 실패 … 저하 운전으로 계속한다` (streaming.invalid 자리).
      미디어 목록을 그 방식으로 바꾸면 200 + 빈 목록이 되는데, **그것은 지금 화면이
      보이는 것과 똑같다.** 사용자에게는 아무것도 나아지지 않는다 — 그래서 안 했다.
    why_not: |
      고칠 자리는 서버가 아니라 **화면**이다. 그 화면(rj-core 계열 인수 자산)은
      우리가 고치지 않는다. `/users` 빈 화면(RJCORE_BLANK_ON_NO_PERMISSION)과
      **같은 갈래**다 — 서버는 옳게 말하는데 화면이 그 말을 버린다.
    unlock_step: |
      ① 화면이 `success:false` 를 받으면 안내를 띄우도록 인수 화면 쪽 판정을 받는다
      ② 그때까지 게이트 `route-alive` 는 이 자리에서 **빨간 채로 둔다** —
         초록으로 만드는 예외를 넣지 않는다. 넣는 순간 이 대장이 하는 일이 없어진다
    state_in_code: backend/media_data/views/media_data_view.py — list_media 가 success=False 를 내리도록 고쳤다 (화면은 그대로다)

  - id: MIGRATE_ONE_LINER
    title: 빈 DB 에서 `manage.py migrate` 한 줄로 세울 수 없다
    unlock_type: DEV
    unlock_owner: dj-core 담당 (이 저장소는 §0.4 로 못 고친다)
    blocks: [설치 절차]
    since: "2026-08-30"
    blocked_now: true
    verified_at: "2026-09-06"
    verified_by: |
      docs/agent/evidence/e2e/migration_from_scratch.md 의 실행 기록을 파일에서 확인 — dj-core logger/0009 의 의존 미선언은 §0.4 금지구역이라 이 저장소가 못 고친다.
    why: |
      dj-core 의 logger/0009 가 multilanguage 의존을 선언하지 않는다. §0.4 금지구역이라
      이 저장소가 고칠 수 없다(P-ENV-1). 순서 제약을 넣어 봤더니 **이미 도는 환경 전부가
      InconsistentMigrationHistory 로 죽었다** — 되돌렸다.
    unlock_step: |
      dj-core 의 마이그레이션 의존 선언 수정. 그동안 설치 매뉴얼 §2-3 의 **세 줄**을 쓴다.
    state_in_code: docs/agent/evidence/e2e/migration_from_scratch.md

  # ★ D-338 ③ — 개발 잠금이 아니다. **우리가 만들 수 없는 값**이다.
  #   쓰기 지점은 이번 턴에 섰다(set_camera_address). 남은 것은 현장이 아는 값뿐이다.
  - id: CAMERA_INSTALL_ADDRESS
    title: 카메라 39대의 설치 도로명주소가 비어 있다
    unlock_type: OPS
    unlock_owner: 고객사 (설치 위치를 아는 쪽)
    blocks: [F-10 알림 위치줄의 주소 표기]
    since: "2026-09-06"
    blocked_now: false
    verified_at: "2026-09-07"
    verified_by: |
      scripts/verify_camera_address.py 를 이 환경에서 실행해 미입력 건수를 확인했고,
      backend/tests/test_camera_address_write.py 7건이 쓰기 경로가 실재함을 증명한다.
      E2E-1 이 주소가 채워진 카메라로 돌아 **알림 본문에 주소가 실제로 나가는 것**을 본다.
    why: |
      카메라는 고정 설치물이고 설치 위치는 **현장을 아는 쪽만 안다**(D-330).
      우리가 지어내면 그 주소가 알림에 그대로 나가 사람을 엉뚱한 곳으로 보낸다 —
      **틀린 주소는 없는 주소보다 나쁘다.**
      ★ blocked_now 가 false 인 이유: 주소가 없어도 알림은 좌표 1줄로 그대로 나간다.
        발송을 지연시키지 않는다(D-308 · D-330). 막고 있는 것이 아니라 **덜 좋은 것**이다.
    unlock_step: |
      고객사에서 「카메라 이름 / 도로명주소 / 현장 표현」 세 칸 목록을 받아
      `python manage.py set_camera_address --csv cameras.csv` 한 줄.
      개발·시연용은 표본으로 채워 증명한다 — 납품 시 실값이 필요하다.
    internal_alternative_considered: |
      좌표→주소 역지오코딩(FX-5). 외부 API·키·저하 운전이 필요했다.
    why_not: |
      필요 없었다. 카메라가 고정물이라 설치 시점에 주소를 안다(D-330 · D-333).
      그리고 결과가 더 좋다 — 역지오코딩은 도로명주소만 주지만 현장 표현(「정문」)은 못 준다.
    state_in_code: |
      backend/stream_monitors/models.py — address_source='unset' 이 기본값 ·
      backend/stream_monitors/management/commands/set_camera_address.py — 쓰기 지점

  - id: DJCORE_200_ENVELOPE_403
    title: dj-core 의 권한 거부가 **HTTP 200 봉투 안에 403** 으로 나간다
    unlock_type: DEV
    unlock_owner: dj-core 소유팀 (우리는 §0.4 로 손댈 수 없다 · D-207)
    blocks: [상용 오픈 ②보안-04, 외부 App 연동 규격]
    since: "2026-09-08"
    blocked_now: false
    verified_at: "2026-09-08"
    verified_by: |
      익명 GET 51자리를 캐시 우회로 호출한 실측(docs/agent/evidence/D-343/authn_gap_calls.json)
      에서 **20건이 HTTP 200 + 본문 status_code 403** 이었다.
      코드 자리도 확인했다: site-packages/core/role/permission.py:508 —
      `return {"success": False, ..., "status_code": 403}` 을 그대로 돌려주고
      django-ninja 가 그것을 200 으로 직렬화한다.
    why: |
      **막고 있으므로 유출은 아니다.** 권한은 실제로 거부됐다.
      그러나 호출자가 HTTP 상태를 믿으면 **거부를 성공으로 읽는다** — D-284(거짓 성공)의 HTTP 판이다.
      ★ blocked_now 가 false 인 이유: 지금 어떤 기능도 이것 때문에 멈춰 있지 않다.
        막고 있는 것이 아니라 **틀린 신호를 내보내는 것**이다. 그러나 외부 App 이 붙는 순간
        이것은 연동 규격의 결함이 된다 — F-05 진입면에 파트너가 붙기 전에 풀려야 한다.
    unlock_step: |
      ① dj-core 소유팀에 시정 요청 (permission.py 가 403 상태로 응답하게)
      ② 그때까지 **우리 층 대응**: 우리가 만드는 진입면(F-05 · apps/dsm)은
         `common/api_contract.py` 의 상태 승격을 지나므로 이 모양이 나오지 않는다.
         새 라우트는 우리 층에서 만든다 — 금지구역 안으로 손을 넣지 않는다(D-207).
    internal_alternative_considered: |
      미들웨어에서 본문의 status_code 를 읽어 HTTP 상태로 승격시키는 것.
    why_not: |
      전역 승격은 **응답 모양을 바꾸는 일**이라 프런트 계약 전체를 건드린다.
      지금 그것을 하면 무엇이 깨졌는지 모르는 채로 초록이 된다(D-343 의 경고와 같은 이유).
      그래서 지금은 **재고 기록**으로 두고, 범위 판정을 받는다.
    state_in_code: |
      scripts/probe_authn_gap_calls.py — effective_status() 가 봉투가 아니라 내용을 읽는다.
      dj-core 쪽 자리는 site-packages/core/role/permission.py:508 이고
      **저장소 밖(§0.4 금지구역)이라 여기에 경로로 적지 않는다** — 읽기만 했다.

  - id: BROWSER_E2E_HARNESS
    title: E2E 가 브라우저를 열지 않아 **실행 중 화면을 찍을 수 없다** — **[2026-09-12 풀림]**
    unlock_type: DEV
    unlock_owner: 우리 (개발)
    blocks: [상용 오픈 ⑦사용성-UX-04, 피드백 콘솔의 스크린샷 자리]
    since: "2026-09-08"
    blocked_now: false
    unblocked_at: "2026-09-12"
    unblocked_by: |
      [실측 2026-09-12 · D-384 ②] **3장이 찍혔다.** `scripts/capture_screens.py` 가
      playwright chromium 으로 로그인 → /dsm/dashboard → /dsm/events → /dsm/events/{id}
      를 지나가며, 화면마다 「그 화면에만 있는 글자」를 확인한 뒤에만 찍는다.
      `scripts/verify_screens.py` 통과 (3장 · run_log 와 ±5분 대조).
      ★ 앞선 잠금 `FRONTEND_DEPS_DRIFT` 는 **완전히 풀리지 않았다** —
        타입오류 3229건은 그대로이고, 풀린 것은 **빌드 1회 성공**뿐이다 (D-381).
        그 하나로 이 잠금이 열렸다는 사실이 D-381 의 문턱 낮추기가 옳았다는 증거다.
    verified_at: "2026-09-08"
    verified_by: |
      backend/tests/e2e/ 의 시나리오 두 벌이 전부 django.test.TestCase 다 —
      Client 로 HTTP 를 때리고 DB·메일을 단언한다. 브라우저를 띄우는 구동체가 저장소에 없다
      (playwright·selenium 어느 것도 requirements 에 없음을 파일에서 확인).
    why: |
      D-347 은 **「E2E 실행 중에 찍는다 · 별도로 화면을 만들어 찍지 마라 · 그림·모형·합성 금지」**다.
      지금 E2E 가 지나가는 자리에는 **화면이 없다.** 그래서 찍을 것이 없다.
      ★ 화면을 따로 띄워 찍으면 그것이 바로 D-347 이 금지한 합성이고,
        「고객이 보는 자리에 실제가 아닌 화면」이 된다 — 착시가 아니라 거짓말이다(D-284).
      그래서 **한 장도 넣지 않고 0장으로 시작한다.** 틀과 게이트는 세웠다 —
      docs/agent/evidence/D-347/screens/INDEX.yaml · scripts/verify_screens.py
    unlock_step: |
      ★ [2026-09-11 · D-371 갱신] **①의 절반이 풀렸다.** 지우지 않고 덧붙인다:
        화면 셋이 코드에 섰다 — frontend/src/features/dsm/pages/{ControlDashboard,
        EventList,EventDetail}.tsx. 「띄울 화면이 없다」는 더 이상 사유가 아니다.
        남은 것은 **구동체**와 **설치본** 둘이다:
          · playwright(또는 selenium)가 여전히 requirements 에 없다
          · 이 환경에 frontend/node_modules 가 없다. 컨테이너 `gx-fe-build` 의
            rj-core 판이 저장소와 어긋나 저장소 전체 타입오류 3229건이 난다 [실측]
            (새 화면 셋의 오류는 **0건** — 어긋난 것은 기존 화면과 rj-core 다)
        ⚠ 그 판 어긋남은 **이 잠금과 별개의 사실**이고, 화면 캡처보다 먼저 풀려야 한다.
      ① 브라우저 구동 E2E 한 벌 (프런트를 띄우고 로그인→대시보드→이벤트 상세까지)
      ② 그 실행 중에 캡처하고 메타 4칸(route·user_role·scenario·captured_at)을 남긴다
      ③ verify_screens.py 가 E2E 로그의 단계·시각과 ±5분 안에서 맞는지 대조한다
      ★ ①이 서기 전에는 INDEX.yaml 이 **비어 있는 것이 정직한 상태다.**
    internal_alternative_considered: |
      기존 Django E2E 가 렌더한 HTML 을 헤드리스로 그려 캡처하는 것.
    why_not: |
      우리 E2E 는 JSON API 를 때린다 — 그릴 HTML 자체가 없다.
      프런트를 따로 띄워 같은 화면을 흉내 내면 그것은 **시험이 지나간 그 화면이 아니다**(D-347 ①).
    state_in_code: |
      backend/tests/e2e/e2e_contract.py — 단계 등재부. **화면 단계가 없다.**
      (0장으로 시작하는 틀은 D-347 의 화면 인덱스에 있다 — 그쪽은 이 잠금의 결과이지
       상태의 자리가 아니다.)

  - id: DELIVERY_ZONE_ANON_LEAK
    title: 금지구역(backend/delivery) 의 2자리가 익명에게 데이터를 돌려주던 것 — **해소됨**
    unlock_type: DEV
    unlock_owner: backend/delivery 소유팀 (§0.4) — 다만 **해소는 우리 층에서 했다**
    blocks: []
    since: "2026-09-08"
    resolved_at: "2026-09-09"
    blocked_now: false
    verified_at: "2026-09-09"
    verified_by: |
      [2026-09-09 · 해소 후 재호출 · 익명 · 캐시 우회]
        GET /api/delivery/drone-monitoring/drone-status   **401** (63 B) ← 그날 17,416 B
        GET /api/delivery/etri-mock/test-scenarios        **401** (63 B) ← 그날  1,976 B
      backend/tests/test_access_gate.py 가 이 둘을 회귀로 못박는다.
      forbidden-zone 게이트는 초록이다 — 그 파일은 한 줄도 안 바뀌었다.

      [2026-09-08 · 해소 전 실측] 같은 두 자리가 200 으로 데이터를 돌려주고 있었다.
      음성 대조 3자리는 같은 실행에서 401 — 환경이 인증을 안 거는 것이 아니었다.
    why: |
      ★ **해소 [판정 D-348 · 2026-09-09]** — 막는 자리를 바꿔서 풀었다.
        D-207 이 금지한 것은 **그 파일을 수정하는 것**이고, 그 경로로 가는 요청을
        **우리 층에서 막는 것**은 금지된 적이 없다. `common/access_gate.py` 한 겹이다.
        「어느 규칙을 어길까」를 고르지 않고 **절차에 예외 경로를 만들었다** —
        규칙도 안 어기고 구멍도 안 남겼다.

      ── 아래는 해소 전의 기록이다. 지우지 않는다(없앤 것과 미룬 것은 다르다) ──
      ★ **우리가 고칠 수 있었고, 실제로 한 번 고쳤다가 되돌렸다.**
        `backend/delivery/views/api.py` 의 4자리에 `JwtOrInboundKey()` 를 붙였더니
        `verify_gates.sh` 의 forbidden-zone 게이트가 **STOP(blocked)** 로 잡았다.
        `backend/delivery/` 는 §0.4 금지구역이다(D-207 · 불변 제약).
      **게이트가 내 손을 잡았다** — 그리고 그것이 맞다(D-327: 게이트 > 지시).
      사고를 알면서 두는 것은 나쁘지만, **금지구역에 손을 넣는 것은 더 나쁘다.**
      막는 방법을 아는 채로 등재해 둔다 — 이 항목은 「모른다」가 아니라 「못 한다」이다.
    unlock_step: |
      셋 중 하나. 어느 것이든 **우리 판정이 아니다.**
      ① 금지구역 소유팀이 그 4자리에 인증을 붙인다 (붙일 코드는 아래 그대로다)
      ② 대표·기획이 이 파일을 티켓의 `reuse_targets` 로 **명시 지목**한다
         (forbidden-zone 게이트가 지목된 파일은 허용한다 — 이미 그 문이 있다)
      ③ 앞단(nginx)에서 그 두 경로를 막는다 — 우리 층 밖이라 금지구역이 아니다.
         다만 **인증이 아니라 차단**이라 정식 해소가 아니다
    internal_alternative_considered: |
      우리 층 미들웨어에서 그 경로만 익명 차단.
    why_not: |
      경로 문자열로 남의 라우트를 막는 것은 **그 파일이 바뀌면 조용히 풀린다** —
      게이트가 못 보는 자리에 방어선을 두는 것이고, 그것이 D-334 가 만든 상태다.
      막을 것은 라우트 선언 자리에서 막는다.
    state_in_code: |
      backend/tests/test_authn_gap_closed.py — 닫은 24자리의 회귀. **이 2자리는 그 목록에 없다** ·
      docs/agent/evidence/D-343/INCIDENT.md §4 — 왜 안 닫혔는지.

  - id: AI_CALLBACK_NEEDS_CREDENTIAL
    title: AI 서비스의 검출 콜백 2자리가 **인증 없이 쓰고 있었다** — 막았고, 그 클라이언트를 고쳐야 한다
    unlock_type: DEV
    unlock_owner: AI 서비스 팀 (호출하는 쪽)
    blocks: [미디어 분석 결과 콜백(VideoAnalysis 갱신)]
    since: "2026-09-11"
    blocked_now: true
    verified_at: "2026-09-11"
    verified_by: |
      [실측 2026-09-11 · scripts/probe_write_surface.py · 익명 호출]
        POST /api/media-data/detect-callback    익명이 핸들러 자리에 **도달** · 본문은 update_or_create/add
        POST /api/media-data/upload-detection   익명이 핸들러 자리에 **도달** · 본문은 create/save
      (대체물을 끼우고 때렸다 — 본체는 한 줄도 돌지 않았다. D-334 의 방법)
    why: |
      D-368 이 정한 것: **「깨지는 클라이언트가 있으면 그것이 곧 발견이다.
      쓰기 면이 막혀서 깨지는 것은 인증 없이 쓰고 있던 클라이언트다.
      그건 보호할 것이 아니라 고칠 것이다.」**

      이 두 자리는 익명이 `VideoAnalysis` 행을 만들고 고칠 수 있는 자리였다.
      쓰기 오염은 조용하다 — 심어진 분석 결과는 진짜와 섞여서 **나중에 골라낼 수 없다.**
      그래서 같은 파일의 형제 라우트들과 **같은 관문**(`CustomJWTAuth`)을 붙였다.

      ★ 이것은 「우리가 못 하는 일」이 아니라 **「우리가 했고, 상대가 맞춰야 하는 일」**이다.
        그래서 `blocked_now: true` 이지만 주인은 우리가 아니다 — 대장에 두는 이유가 그것이다.

      ⚠ 배포 전에 AI 서비스 쪽 자격증명을 먼저 준비해야 한다. 준비 없이 배포하면
        **미디어 분석 콜백이 401 로 떨어진다.** 이 항목이 그 경고다.
        (재난 검출 본선은 이 경로가 아니다 — 그쪽은 gRPC 이고
         `stream_monitors/services/grpc_dual_stream_service.py` → K1 로 간다.
         즉 **F-02·F-10 계약 경로는 이 변경에 닿지 않는다** [실측].)
    unlock_step: |
      ① AI 서비스에 자격증명 하나를 발급한다 —
         `POST /api/dsm/settings/api-keys` (K5 발급면 · D-367 트리의 F-05 발급)
         또는 서비스 계정의 접근 토큰
      ② AI 서비스가 콜백에 `Authorization` 헤더를 붙이도록 고친다
      ③ 붙인 뒤 `scripts/probe_write_surface.py` 를 다시 돌려
         그 두 자리가 `rejected_elsewhere` 로 내려갔는지 확인한다
      ★ 되돌리는 것은 해소가 아니다 — 관문을 떼면 이 항목이 아니라 사고가 돌아온다.
    internal_alternative_considered: |
      두 자리만 인증 예외로 남기고 IP 허용목록으로 막는 것.
    why_not: |
      IP 는 인증이 아니다. 같은 망 안의 무엇이든 그 자리에 쓸 수 있고, **누가 썼는지가
      기록에 안 남는다.** 오염된 행을 나중에 골라내려면 「누가」가 있어야 한다.
    state_in_code: |
      backend/media_data/views/media_data_view.py — 두 라우트에 auth=CustomJWTAuth() ·
      scripts/verify_write_auth.py — `writes` 0건 래칫(이 자리가 다시 열리면 exit 1) ·
      docs/agent/evidence/D-368/write_surface.json — 전수와 갈래

  - id: FRONTEND_DEPS_DRIFT
    title: 프런트 의존 설치본이 저장소와 어긋난다 — **타입오류 3229건** (빌드는 [2026-09-12] 선다)
    unlock_type: DEV
    unlock_owner: rj-core 소유팀 + 우리 (판 맞추기)
    blocks: [상용 오픈 ⑦사용성-UX-04, 타입 검사]
    since: "2026-09-11"
    blocked_now: true
    verified_at: "2026-09-12"
    partial_unblock: |
      ★ [실측 2026-09-12 · D-381] **「빌드가 되는가」와 「타입이 깨끗한가」를 나눠 답한다.**

          빌드   `npx vite build` → **exit 0 · 66초** · 번들 산출됨.  **선다.**
          타입   `tsc --noEmit` → **3229건.**  그대로다.

      즉 이 잠금은 **캡처를 막지 않는다** — 그래서 `blocks` 에서 BROWSER_E2E_HARNESS 를
      뺐다. 타입검사와 번들링은 다른 일이고, 스크린샷에 필요한 것은 후자였다.

      ⚠ [실측] 처음 돌린 빌드는 **JS 힙 부족으로 죽었다**(기본 2GB).
        `NODE_OPTIONS=--max-old-space-size=6144` 로 넘겼다. 이것은 판 어긋남이 아니라
        **번들 크기**의 문제다 — 단일 청크가 16MB 다. 둘을 섞어 읽지 말 것.

      ⚠ [실측] `frontend/` 에 **lock 파일이 없다.** D-381 ④ 가 「lock 이 없으면 그것이
        진짜 결함」이라 한 그 상태다. 그리고 `rj-core` 는 커밋이 아니라 **브랜치**를
        가리킨다(`#rj-core-v2`) — 같은 명세가 날마다 다른 것을 설치할 수 있다.
        **판의 진실이 저장소에 없다.** 이것이 3229건의 뿌리이고, 아래 unlock_step ①이다.
    progress_20260913: |
      ★ [실측 2026-09-13 · D-387] **lock 이 생겼고 판이 고정됐다.** 절반이 풀렸다.

          설치본의 커밋을 실측했다 (컨테이너 `gx-fe-build` 의 package-lock.json):
              rj-core        commit_sha 51b01e56da253e1b601fb97efd65d10dafe6b7a0
              @gaion/gcs-fe  commit_sha a7846a02bbf790738f894a115d54667128d9b2c9
          그 해시로 `frontend/package.json` 을 **커밋 핀**으로 바꾸고,
          lock 파일을 저장소에 올렸다 (2,384 패키지가 판·해시로 고정된다).
          **브랜치 참조 잔여 0건** [실측] — `scripts/verify_deploy_ready.py` 가 센다.

          재현: 같은 입력으로 **두 번 빌드**해 산출물 197개의 이름·해시가 전부 일치.
                **깨끗한 디렉터리**(`/app/_clean`)에서 한 번 더 빌드 — 같은 197개 일치.
                빌드 exit 0 · 각 50초 (`NODE_OPTIONS=--max-old-space-size=6144`).

      ⚠ **아직 확인하지 못한 것 — 핀이 실제로 그 커밋을 가져오는지는 못 봤다.**
        이 환경에서 사내 git(192.168.0.22:22)은 **연결이 거부된다** [실측:
        `ssh: connect to host 192.168.0.22 port 22: Connection refused`].
        그래서 `npm ci` 로 처음부터 설치해 보는 갈래는 **돌리지 못했다.**
        지금 증거가 말하는 것은 **번들의 재현성**이고, **설치의 재현성이 아니다** —
        둘을 섞어 읽지 말 것 (D-301: 검사 못한 것은 0건 검사가 아니다).

      ⚠ 타입오류 **3229건은 그대로다.** 고정은 그 수를 줄이지 않는다 —
        **늘지 않게 잠그는 일**이다 (D-387: 래칫으로 두고 새 코드 100%).

    verified_by: |
      [실측 2026-09-11]
        · 호스트에 `frontend/node_modules` 가 **없다** (`npx tsc` 가 tsc 를 못 찾는다)
        · 있는 설치본은 컨테이너 `gx-fe-build` 뿐이다 (node_modules 778개)
        · 그 안에서 `npx tsc --noEmit -p tsconfig.app.json` → 저장소 전체 **3229건**
          첫 오류들이 `Module 'rj-core' has no exported member 'AddUser'` 류다 —
          컨테이너의 rj-core 판이 저장소 코드가 기대하는 판이 아니다
        · ★ **새로 얹은 `frontend/src/features/dsm/` 의 오류는 0건**이다.
          어긋난 것은 기존 화면과 rj-core 이지 이번 턴의 코드가 아니다
    why: |
      화면 셋(D-373)이 코드에 섰지만 **빌드가 서지 않으면 브라우저 E2E 도 서지 않고**,
      그러면 D-347 의 스크린샷 자리가 계속 0장이다. 즉 이 잠금이
      `BROWSER_E2E_HARNESS` 보다 **앞에** 있다.

      ★ 고치지 않았다. `rj-core` 는 §0.4 이관 자산(git+ssh 사내 저장소)이고,
        **어느 판이 정본인지는 우리가 정할 수 없다.** 판을 맞추는 일은 버전 판정이지
        코드 수정이 아니다 — **남의 뿌리는 대장에** (D-358 원칙 그대로).

      ⚠ 3229 를 「우리 코드의 빚」으로 읽지 말 것. 분모가 다르다.
    unlock_step: |
      ⓪ ★ **lock 을 만든다.** 지금 `frontend/` 에는 lock 파일이 없고, `rj-core` 는
         브랜치(`#rj-core-v2`)를 가리킨다 — 그래서 「저장소가 정본」이라는 D-381 ③ 을
         적용할 대상 자체가 없다. **lock 고정이 첫 일이다** (D-381 ④ 그대로).
      ① `frontend/package.json` 의 `rj-core#rj-core-v2` 가 가리키는 커밋과
         컨테이너 설치본의 커밋을 **대조한다** (둘 중 무엇이 정본인지가 첫 질문이다)
      ② 정본 판으로 `npm ci` 를 다시 하고 `npx tsc --noEmit` 건수를 다시 센다
      ③ 0 이 아니어도 좋다 — **줄어드는 것이 보이면** 그다음이 브라우저 E2E 다
      ★ 새 화면(features/dsm)의 오류가 0건인 상태를 **기준선으로** 잡아 두면,
        판을 맞추다 새 화면이 깨지는 것을 그 자리에서 안다
    internal_alternative_considered: |
      새 화면만 따로 빌드해 캡처하는 것 (별도 vite 진입점).
    why_not: |
      그것은 **시험이 지나간 그 화면이 아니다** — D-347 ①이 금지한 합성이다.
      로그인·사이드바·권한 배선을 지나오지 않은 화면은 고객이 보는 화면이 아니고,
      그런 캡처를 검수 콘솔에 넣으면 착시가 아니라 거짓말이 된다 (D-284).
    state_in_code: |
      frontend/src/features/dsm/ — 새 화면 셋 (타입오류 0건) ·
      docs/agent/evidence/D-347/screens/INDEX.yaml — 0장 사유에 이 사실이 함께 적혀 있다

  # ═════════════════════════════════════════════════════════════════════════
  - id: RJCORE_BLANK_ON_NO_PERMISSION
    title: 권한 없는 화면이 안내 대신 **빈 화면**을 낸다 — `/users` 본문 0자 [실측 2026-09-13]
    unlock_type: DEV
    unlock_owner: rj-core 소유팀 (§0.4 이관 자산 — 우리가 고칠 수 없다)
    blocks: [상용 오픈 ⑦사용성-UX-06, DA-03 §3-4 화면 규약]
    since: "2026-09-13"
    blocked_now: true
    verified_at: "2026-09-13"
    verified_by: |
      [실측 2026-09-13] 브라우저(playwright chromium)로 로그인 뒤 `/users` 로 갔다:
          본문 글자 수 **0** · JS 오류 **0건** · 그 화면이 부른 API **6건 전부 200**
      즉 **서버도 살아 있고 스크립트도 안 죽었는데 화면에 아무것도 없다.**
      원인 [실측]: 이 계정의 `role` 이 **None** 이다 — 읽기 권한 판정이 거짓이 되어
      `rj-core` 의 `UserManagement` 가 **아무것도 그리지 않는다**.
    why: |
      DA-03 §3-4 는 「"권한없음"은 미렌더가 기본이나, **화면 전체가 권한없음이면 빈 화면
      대신 '이 화면에 접근할 권한이 없습니다 · 관리자 문의' 한 장을 낸다**」로 정해 두었다.
      지금 동작은 그 규약 위반이고, **당직자에게는 「화면이 고장 났다」로 보인다**
      (D-378: 결함 등급은 내부 원인이 아니라 보이는 것으로 정한다).

      ★ 이 결함은 **화면을 띄우기 전에는 보이지 않았다.** 단위 538건과 E2E 23건이
        전부 초록인 채로 이 화면은 비어 있었다 — D-386 이 말한 그 층이다.
    unlock_step: |
      ① rj-core 소유팀에 전달: 권한 0인 화면은 안내 한 장을 렌더한다 (DA-03 §3-4)
      ② 우리가 할 수 있는 것 — **우리 화면(features/dsm)에는 같은 구멍을 만들지 않는다.**
         새 화면은 권한 없음일 때 안내를 낸다. 남의 화면은 대장에 둔다 (D-358)
      ③ 검수 콘솔 UX-05(역할↔화면 매핑)와 **같은 자리에서 묻는다** — 「이 역할에 이 화면이
         보이면 안 된다」의 답이 오면, 그때 「보이면 안 될 때 무엇을 보이나」도 정해진다
    internal_alternative_considered: |
      우리 App.tsx 에서 그 라우트를 감싸 안내 화면을 덧대는 것.
    why_not: |
      덧대면 **우리 코드가 남의 화면의 권한 판정을 흉내내게 된다** — 판정식이 두 벌이 되고
      (D-212 가 금지한 모양), rj-core 가 규약을 고치는 날 두 안내가 겹친다.
      지금은 **사실을 대장에 두고**, 우리 화면에만 규약을 지킨다.
    state_in_code: |
      scripts/capture_screens.py — `KNOWN_BLANK` 「열어 봤더니 빈 화면」 목록 ·
      docs/agent/evidence/D-386/screen_routes.json 의 `blank_screens`

  # ═════════════════════════════════════════════════════════════════════════
  - id: K3_ROLE_MAPPING_UNCONFIRMED
    title: 역할↔프리셋 매핑 — **7종은 우리 판정이고, 8종은 미배선이다.** 정답은 고객이 안다
    unlock_type: CONTRACT
    unlock_owner: 안양 관제팀 (고객)
    blocks: [상용 오픈 ⑦사용성-UX-05, 역할별 화면의 정확성]
    since: "2026-09-12"
    # ★ 게이트가 요구해서 채우는 칸이 아니다 — **묻지 않은 질문은 지연이 시작되지도
    #   않는다.** CONTRACT 로 올린 순간 「언제 물었고 언제까지 답을 받나」가 없으면
    #   그 잠금은 영원히 「고객 대기」로 늙는다 (D-320 · 계약 6조3항 기산).
    #   그래서 이 턴의 회신에 이 질문을 실어 보내는 것을 발송으로 잡는다.
    sent_at: "2026-09-12"        # 이 턴 회신 §7 「역할↔화면 매핑 확인」으로 발송
    due_by: "2026-09-27"         # 발송 + 15일 (계약 3조2항 준용)
    blocked_now: true
    verified_at: "2026-09-12"
    verified_by: |
      [실측 2026-09-11 · role.Role 전수] 이 저장소의 역할 코드는 **15종**이다.
      DA-03 §3-4 는 역할을 **「다섯 종류의 사람」**으로 적었다. 그 둘을 잇는 표가
      문서 어디에도 없다 — 그래서 `backend/config/k3_roles.py` 에서 **우리가 이었다.**

      이은 것 7종 (근거는 파일의 주석마다 있다):
        OPERATOR  : fire_user · surveillance_operation · operator
        MANAGER   : fire_admin · surveillance_order · admin
        EXECUTIVE : view_only_-_anyang
      비운 것 8종 (`K3_UNMAPPED_BY_DECISION` · **추측으로 채우지 않았다**):
        order · delivery_admin · delivery_order · delivery_operation ·
        drone_robot_admin · drone_robot_user · user · superuser
    why: |
      ★ **이것은 인용이 아니라 우리 판정이다** (D-383). 그 표기를 여기 남기는 이유는
      다음 사람이 이 표를 「DA-03 에서 옮겨 적은 것」으로 읽지 않게 하기 위해서다 —
      옮겨 적은 표는 원본을 찾아 대조할 수 있지만, **판정한 표는 판정한 사람에게
      물어야 한다.** 어느 쪽인지 모르면 아무도 못 고친다 (D-322 의 계열).

      ★ 그리고 이 매핑의 정답은 우리에게 **없다.** 「재난관제 관리자가 임계값을 편집할
        수 있는가」는 코드가 답할 수 있는 질문이 아니라 **운영 규정**이다.
        틀리면 두 방향으로 틀린다: 넓으면 볼 것이 아닌 사람이 보고, 좁으면 일이 안 된다.

      ⚠ 지금 상태는 **안전한 쪽으로 틀려 있다** — 매핑에 없는 역할은 `matched=False` 로
        가장 좁은 프리셋(OPERATOR)에 떨어지고, 편집(E)은 설정 없이는 **절대 참이 되지
        않는다**. 그래서 이 잠금은 「위험」이 아니라 「미확정」이다.
    unlock_step: |
      ★ **화면이 없어도 할 수 있는 검수다** — 이것이 검수 콘솔의 첫 실전 용도다(D-383).
      ① 검수 콘솔의 `UX-05` 항목으로 관제팀에 셋을 묻는다:
         ㉠ 이은 7종이 **맞는가**
         ㉡ **빠진 것**이 있는가 (미배선 8종 중 화면을 받아야 하는 역할)
         ㉢ **이건 보이면 안 된다** 가 있는가
      ② 받은 답만 `K3_ROLE_PRESET_MAP` 과 `K3_WIDGET_MATRIX` 에 넣는다.
         **답이 없는 칸은 비운 채로 둔다** — 비어 있는 것이 틀린 것보다 낫다(D-280).
      ③ 답을 받은 날짜와 답한 사람을 여기 `verified_by` 에 적는다 — 진술이 아니라 출처다.
    internal_alternative_considered: |
      역할 이름의 유사도로 나머지 8종도 자동 매핑하는 것.
    why_not: |
      이름이 비슷한 것과 **권한이 같은 것**은 다르다. `drone_robot_admin` 에 MANAGER 를
      주면 배송 관리자가 재난 임계값 편집 화면을 얻는다 — 그것은 추측이 만든 권한이고,
      추측으로 만든 권한은 **아무도 자기가 가진 줄 모른다.** (D-280 추정 금지)
    state_in_code: |
      backend/config/k3_roles.py — 이은 7종 · 비운 8종(사유 포함) ·
      backend/kernels/k3_dashboard/presets.py — 설정이 비면 가장 좁은 쪽으로 떨어진다 ·
      docs/agent/evidence/D-347/screens/SCREENS-1/OPERATOR/dsm_dashboard.png —
        ★ **그 상태가 화면에 찍혀 있다**: 「역할 매핑을 찾지 못해 가장 좁은 화면으로
        떨어졌습니다」. 캡처가 이 잠금의 증거를 겸한다

  # ═════════════════════════════════════════════════════════════════════════
  # P-12 회색 다섯 — **판정 불가는 초록도 빨강도 아니다** [실측 2026-09-17]
  #
  #   2026-09-16 집계: 잼고 통과 4 · 잼고 실패 0 · **못 잼 5**
  #   2026-09-17 집계: 잼고 통과 6 · 잼고 실패 1 · 못 잼 0 (**대기 2**)
  #
  #   다섯을 하나씩 열어 보니 **성격이 셋으로 갈렸다:**
  #     ① 진짜 빚 2 — 자격증명·도구가 없어 못 쟀다 (route-alive · secrets). 갚았다.
  #     ② 잘못 칠한 색 1 — 쟀고 알고 있는 빚을 SKIP 으로 냈다 (bypass). 래칫으로 옮겼다.
  #     ③ 빚이 아닌 것 2 — 볼 대상이 0건이었다 (model-inheritance · ui-library). **대기**다.
  #   ★ 「회색 5」라는 수는 **다섯 자리가 안 지켜진다**고 읽힌다. 실제로는 셋이었다.
  # ═════════════════════════════════════════════════════════════════════════

  - id: GATE_ROUTE_ALIVE_CREDENTIAL
    title: route-alive 가 자격증명이 없어 아무것도 못 재고 있었다 — **해제됨**
    unlock_type: DEV
    unlock_owner: Code
    blocks: [P-12①]
    since: "2026-09-14"
    blocked_now: false
    verified_at: "2026-09-17"
    verified_by: |
      [실측] 같은 게이트를 두 번 돌려 갈라지는 것을 확인했다:
        자격증명 있이  → exit 1 (죽은 라우트 2건을 잡는다)
        자격증명 없이  → exit 2 (판정 불가)
      증거: docs/agent/evidence/D-386/route_alive.json — 26건 · 200 24 · 500 2.
    why: |
      값이 저장소에 있으면 안 되고(D-204), 없으면 게이트가 못 잰다. 그 사이에서
      이 자리는 **영원히 회색**이었다.
      ★ 그리고 풀고 나서 더 나쁜 것이 나왔다: 자격증명을 넣자 게이트가 **exit 0** 을 냈는데,
        26건 중 24건이 401 이었다. `/api/token/pair` 가 내주는 JWT 를 dj-core 가
        `user.token` 이 없다고 거절했고(core/auth.py:38 "Token expired"),
        판정 규칙이 401 을 「문지기가 섰다(살아 있다)」로 읽었다.
        **토큰을 받았다는 사실이 토큰이 먹힌다는 증거가 아니었다.**
        그 401 밑에 `/api/media-data(/)` **500 두 자리**가 덮여 있었다.
    unlock_step: |
      ① 값은 로컬 `.env.gates`(gitignored)에 산다. 저장소엔 이름만 —
         docs/agent/ENV_EXAMPLE_게이트자격증명.txt (D-204)
      ② 서버가 호스트에서 안 보이면(gx-shell 이 8000 을 게시하지 않는다)
         `GX_ROUTE_CONTAINER` 로 컨테이너 안에서 같은 코드를 돌린다 — 두 벌을 만들지 않는다
      ③ 로그인 뒤 **익명으로 한 번 더 때려** 토큰이 무엇을 바꿨는지 센다.
         0건이면 판정 불가다 — 못 들어간 채로 본 401 을 초록으로 적지 않는다
    state_in_code: |
      scripts/verify_route_alive.py — LOGIN_PATHS · _extract_token(조용한 성공 거절) ·
      token_changed_anything(익명 대조) · load_local_env · delegate_to_container ·
      BIRTH_SAMPLE_TOKEN_DEAF (출생 표본 ② 26건 중 24건 401)

  - id: GATE_SECRET_SCANNER_ABSENT
    title: 시크릿 스캐너가 설치되어 있지 않아 못 재고 있었다 — **해제됨**
    unlock_type: DEV
    unlock_owner: Code
    blocks: [P-12②]
    since: "2026-08-13"
    blocked_now: false
    verified_at: "2026-09-17"
    verified_by: |
      [실측] gitleaks 8.21.2 를 판·해시 고정으로 설치하고(sha256 대조 일치),
      게이트가 SKIP → PASS 로 갈렸다. 자기시험 양성 1(심은 키 32자를 잡는다) ·
      음성 2(플레이스홀더를 안 잡는다 · 큰 잠금 파일의 integrity 해시를 안 잡는다).
      분류기 양성 대조: 저장소가 나르는 자리에 심으니 exit 1, 치우니 exit 0.
    why: |
      ★ 깔고 처음 돌린 날 나온 수가 스캔 면에 따라 **19건 대 1건**이었다.
        파일 면(--no-git)은 빌드 산출물과 로컬 `.env.stg` 까지 세고,
        이력 면은 커밋된 것만 센다. 물어야 할 것은 **「저장소가 무엇을 나르는가」**다.
      실제 발견 1건: backend/stream_monitors/example_frontend.html:813 에
        Django CSRF 토큰 32자가 하드코딩. 게다가 그 값이 차 있어서 바로 아래
        `if (!csrfToken)` 대체 경로 세 갈래가 **한 번도 안 돌고 있었다** — 유출이면서
        동시에 기능 결함이었다. 값을 비워 두 가지를 함께 고쳤다.
      ★ 측정기 결함 하나도 함께 나왔다: `regexTarget="line"` 허용목록이
        **~100KB 넘는 파일에서 조용히 꺼진다**(gitleaks 청크 경계). 같은 한 줄이
        118바이트에서는 허용되고 202KB 에서는 오탐이었다. 자리(paths)로 한 번 더 잠갔고
        그 잠금이 살아 있는지를 **4000줄 표본**으로 매번 확인한다.
    unlock_step: |
      python scripts/install_secret_scanner.py — 판·해시는 scripts/secret_scanner.lock.json
      이 고정한다(D-387). 해시가 다르면 **설치를 거절한다.** 받은 물건은 tools/(gitignored)에
      두고 저장소는 lock 만 든다.
    state_in_code: |
      scripts/install_secret_scanner.py — 판·해시 고정 설치 ·
      scripts/secret_scanner.lock.json(판 8.21.2 + sha256) ·
      scripts/verify_secret_scan.py(스캔 면 판정 + 자기시험) ·
      .gitleaks.toml(자리 허용목록) · .gitleaksignore(래칫 D-311)

  - id: BYPASS_FRAMEWORK_MODELS
    title: 권한 우회 목록에 프레임워크 모델 7건이 남아 있다 — 회색이 아니라 **빚**이다
    unlock_type: DEV
    unlock_owner: Code (W0-2 작업2 — 뷰 레벨 필터)
    blocks: [W0-2]
    since: "2026-08-13"
    blocked_now: true
    verified_at: "2026-09-17"
    verified_by: |
      [실측] `scripts/../docs/agent/verify_gates.sh --gate bypass` 가 목록 7건을 읽고
      기준선 7건과 대조한다: coreuser · group · multilanguagecontent · role ·
      usergroup · userprofile · userprofilelink. 새로 는 것 0건.
      래칫 대조 [실측]: 양성 1(하나 늘면 잡는다) · 음성 3(그대로 · 줄어듦 · 대소문자만 다름).
    why: |
      ★ 이 자리는 **못 잰 자리가 아니었다.** 게이트는 목록을 읽었고 몇 개가 남았는지
        알고 있었는데 SKIP 을 냈고, SKIP 은 판정 불가로 집계된다(D-400).
        **아는 것을 모른다고 적은 것**이고, 그래서 회색 다섯 중 하나로 셈해졌다.
      아는 빚에는 회색이 아니라 래칫이 맞다(D-311). 「언젠가 없앤다」가
      **「오늘 안 늘었다」**로 바뀐다 — 뒤엣것만 매일 확인할 수 있다.
    unlock_step: |
      ① 뷰 레벨 필터로 프레임워크 모델의 테넌트 경계를 세운다 (W0-2 작업2)
      ② 하나씩 목록에서 빼고 기준선을 함께 내린다. 기준선이 0 이 되면 이 등재를 내린다
      ③ ⚠ 기준선을 **올리는** 수정은 곧 결재 요청이다 — 조용히 늘리지 않는다
    state_in_code: |
      backend/common/base_model.py — performance_bypass_models ·
      docs/agent/verify_gates.sh — gate_bypass 의 FW_BASELINE (래칫 기준선)

  - id: GATE_TARGETS_ZERO_WAITING
    title: model-inheritance · ui-library 는 **볼 대상이 0건**이었다 — 빚이 아니라 대기
    unlock_type: DEV
    unlock_owner: Code
    blocks: []
    since: "2026-09-16"
    blocked_now: false
    verified_at: "2026-09-17"
    verified_by: |
      [실측] 두 게이트의 [입력] 줄이 0건과 그 사유를 말한다 —
      「베이스라인(HEAD) 대비 변경된 models.py 가 없다」 ·
      「변경된 frontend/src 파일이 없다」. 게이트는 멀쩡히 서 있고 지나갈 것이 없었다.
      심어 보면 즉시 판정한다(각 게이트의 양성 대조는 종전대로 살아 있다).
    why: |
      「못 잼」 한 칸에 성격이 다른 둘이 들어 있었다: **못 본 것**과 **볼 것이 없던 것.**
      앞의 것은 갚아야 할 빚이고 뒤의 것은 아니다. 섞어 두면 「회색 5」가 되고,
      그 수를 보는 사람은 다섯 자리가 안 지켜진다고 읽는다 — 실제로는 셋이었다.
    unlock_step: |
      해제할 것이 없다. 이 둘은 **models.py 나 frontend/src 를 건드리는 커밋에서
      자동으로 잼으로 바뀐다.** 없애지는 않는다(P-12 ④) — 대상 0과 게이트 부재는 다르다.
    state_in_code: |
      docs/agent/verify_gates.sh — waiting() 헬퍼 · tally() 집계 줄
      (「잼고 통과 N · 잼고 실패 N · 못 잼 N (대기 N)」)

  # ═════════════════════════════════════════════════════════════════════════
  # P-5 저장소 — 세웠고, 세우고 나서 **더 조용한 결함**이 드러났다 [실측 2026-09-18]
  # ═════════════════════════════════════════════════════════════════════════

  - id: MINIO_ABSENT_IN_DEV
    title: 개발 환경에 객체 저장소가 아예 없었다 (`minio.invalid`) — **해제됨**
    unlock_type: DEV
    unlock_owner: Code
    blocks: [P-5, UX-09]
    since: "2026-09-13"
    blocked_now: false
    verified_at: "2026-09-18"
    verified_by: |
      [실측] `docker compose up -d minio` 로 세웠다(판을 digest 로 고정).
      health 200 · 버킷 `guardianx-dev` · 탐침 객체 1건 ·
      `/api/media-data/` 와 `/api/media-data` **둘 다 500 → 200 · success:true** ·
      게이트 `route-alive` **잼고 실패 → 잼고 통과**(죽은 라우트 0).
      판정기가 양쪽으로 무는 것도 확인했다: 저장소를 내리면 `verify_minio.py` exit 1,
      되돌리면 exit 0.
    why: |
      `MINIO_ENDPOINT=minio.invalid` 는 **일부러 안 풀리는 이름**이었다 — 없다는 사실이
      조용해지지 않게. 그 덕에 부재가 500 으로 보였고, 그 500 이 게이트의 유일한 빨강이었다.
      ★ 기존 자격증명은 5자짜리 **더미**였다(MinIO 루트 요건은 8자 이상) — 즉 한 번도
        진짜로 서 본 적이 없었다. 새로 만들어 뿌리 `.env`(gitignored)에 두었다.
    unlock_step: |
      해제 완료. 재현: `cp .env.example .env` → 값 채움 → `docker compose up -d minio`.
      ⚠ 이 기계의 개발 환경은 손으로 만든 `gx-main-network` 위에 있어서, compose 가 세운
      컨테이너를 그 망에 **한 번 더 이어 준다**(`docker network connect --alias minio`).
      새 환경을 compose 만으로 세우면 그 줄은 필요 없다.
    state_in_code: |
      docker-compose.yml — minio 서비스(digest 고정) · minio-data 볼륨 ·
      .env.example(이름만) · scripts/verify_minio.py(강제 도구)

  - id: MINIO_AVAILABILITY_IS_BOOT_TIME_ONLY
    title: 저장소가 **돌다가 죽으면** 제품이 알아채지 못한다 — 500 대신 조용한 200
    unlock_type: DEV
    unlock_owner: Code (다만 처분은 설계 판정이 먼저다 — 세종)
    blocks: [UX-09, 평상 운영 ①]
    since: "2026-09-18"
    blocked_now: true
    verified_at: "2026-09-18"
    verified_by: |
      [실측] MinIO 를 실제로 내리고 같은 라우트를 때렸다:
          살아 있을 때   200 · success:true · 1건
          **내렸을 때**   200 · success:true · 1건
          되돌린 뒤      200 · success:true · 1건
      코드 근거: `backend/stream_monitors/utils/minio_client.py` — `self.available` 은
      `__init__` 안에서만 True 가 되고(21·41행) 이후 다시 확인하지 않는다.
      즉 **가용성은 기동 시점의 사진 한 장**이다.
    why: |
      ★ 고치기 전보다 **더 조용해졌다.** 저장소가 없던 동안에는 최소한 500 으로 소리쳤다.
        이제는 기동 때 살아 있기만 하면, 그 뒤 저장소가 죽어도 목록이 계속 나온다.
        새벽에 저장소가 죽으면 당직자는 파일을 눌러 본 뒤에야 안다 — D-378 이 말한
        「보이는 것으로 등급을 정한다」의 반대쪽 얼굴이다.
      ★ 함께 드러난 것: **감시도 눈이 멀어 있었다.** `ops_monitor.py` 의
        `object_store_alive` 는 `default_storage.exists(...)` 를 불러 **예외가 없으면
        닿았다**로 읽었는데, 저장소가 죽어도 그 호출은 예외 없이 False 를 돌려준다.
        「없다」와 「못 물어봤다」를 한 값으로 읽은 것이다(D-301). **그쪽은 이번에 고쳤다** —
        이제 저장소에게 버킷 존재를 직접 묻고, 못 닿으면 ALARM 이다(OK→ALARM→OK 실측).
    unlock_step: |
      ① 감시 축은 **끝났다** — `object_store_alive` 가 저장소 죽음을 잡는다 [실측].
      ② 남은 것은 **읽기 면**이다. `minio_client.available` 을 매 요청 확인하면 요청마다
         왕복이 붙는다(F-05 p95 를 먹는다). 그래서 처분은 셋 중 하나이고 **판정이 먼저다**:
           ㉮ 짧은 TTL 캐시(예: 5초)로 다시 보기   ㉯ 실패 시에만 재확인 후 500 으로 승격
           ㉰ 그대로 두고 감시에만 맡긴다(화면은 조용한 채로)
      ③ ⚠ 어느 쪽이든 화면이 `success:false` 를 안 듣는 문제는 그대로다
         (MEDIA_LIST_SHOWS_EMPTY_WHEN_STORAGE_DOWN · rj-core 인수 화면).
    state_in_code: backend/stream_monitors/utils/minio_client.py — self.available 은 __init__ 에서만 세워진다
