# SEC-06 증거 — scripts/verify_credential_store.py
# 실행: 2026-09-20 KST · 차선 N (P-207 둘째 묶음) · 브랜치 turn-q · HEAD 0acf8d1 · 호스트 파이썬
# 명령: PYTHONIOENCODING=utf-8 python scripts/verify_credential_store.py
# ---
[P-107] TARGET=저장소 C:/GuardianX/guardianx-source @ 0acf8d1 · 작업본 26파일 어긋남 (살아 있는 것을 재지 않는 게이트다)
[P-107] AS=(자격증명 없음 — 소스·문서 파일을 읽는다)
[P-107] SOURCE=파일 backend/kernels/k5_trust/credentials.py (기록 2026-08-30T16:26 · 12178바이트)
[P-107] MEASURED=(선언 없음 — 이 게이트는 **무엇을 · 분모 몇으로** 쟀는지 말하지 않는다. 그 `exit 0` 은 「이 호출이 통과」일 뿐이다 · P-204)
[P-107]   ✗ 머리글: MEASURED= 가 없다 — **`exit 0` 은 「이 호출이 통과」다.** 무엇을 · 분모 몇으로 쟀는지 말하지 않은 0 은 초록이 아니다 (P-204)  ← verify_credential_store.py
  OK   ★ 출생표본 capability 빈 키를 기능 코드가 읽으면 잡는다
  OK   상태가 present 여도 capability 만으로는 못 지나간다
  OK   typed + capability 면 안 잡는다
  OK   독스트링 안의 언급은 참조가 아니다
  OK   주석 안의 언급도 참조가 아니다
  OK   표 자신은 예외다
  OK   probe 는 예외다 — 그것이 typed 로 올리는 일을 한다
  OK   ★ 문자열 안이어도 **읽는 행위**면 잡는다
  OK   이름이 부분 문자열로 겹치는 것은 안 잡는다
  OK   표 ②를 읽는다 (4건)
[CRED] 자기시험 10건 통과 (양성 3 · 음성 6 · 출생 표본 포함)
[CRED] 표 ② **4건** · 기능 코드가 쓸 수 없는 이름 **2건** (모수=795파일 · 술어=capability 기재 + 상태 typed 이상)
[CRED] 통과 — capability 없는 키를 만지는 기능 코드가 없다
# EXIT=0
