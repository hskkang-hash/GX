# P-106 · 페인포인트 18 ↔ 역할 계정 화면 32장 — **행별 대조**

**잰 때** 2026-09-07 20:0x~20:4x (KST) · **차선 P** · HEAD `6cf2c19`
**정본** `docs/design/GX-FCPR_기능완결성_상용준비_셈법_v0.1.md` §3′-0b · §3′-1
**기계가 읽는 꼴** `docs/agent/evidence/P-106/row_map.json` (QA `verify_feature_reach` 가 그대로 먹을 수 있다)

## 한 줄

    FC 하한 38.9% → **41.7%**   ·   상한 44.4% → **52.8%**
    (확인된 합 7.0 → 7.5 · 손 밖 1행 · **회색 1행이 새로 생겼다**)

**58.3 은 오지 않았다.** 그 수는 가능성이었지 목표가 아니었다. 행별로 두드려 보니
증거가 실제로 닿은 자리는 **두 행**(1 · 4)이었고, 한 행(8)은 오히려 **회색으로 내려갔다.**

## 대조를 지배한 사실 하나 — **그 32장은 전부 「읽기」다**

    비-GET 호출 = 2건 / 전 32장
      POST /api/v1/auth/login 400   (로그인 실패 갈래 · L1)
      POST /api/proxy/notam   200   (OEM 화면)
    → DSM 제품 라우트에 대한 **쓰기 호출 0건**

이 한 줄이 열여덟 행의 절반을 결정했다. **페인이 「쓰기」로 풀리는 행은 이 벌로는
1 이 될 수 없다** — 오탐 판정(2) · 접수(7) · 카메라 일괄 등록(13) · 훈련 켜기(14) ·
개인정보 접수(11) · 웹훅 등록(17).

그리고 그 여섯 중 둘(13 · 14)은 **화면이 완성돼 있다.** 카메라 일괄 등록 화면은
「주소 없는 카메라 4/5대」를 세어 보여 주고 CSV 칸과 dry-run 단추를 갖췄는데
**CSV 칸이 빈 채로 찍혔고 단추는 꺼져 있다.** 훈련 모드 화면은 사유 필수 스위치와
보고서 자리까지 갖췄는데 화면이 스스로 「이 테넌트는 훈련 모드를 한 번도 켠 적이
없습니다」라 적는다. **제품의 흠이 아니라 캡처 실행체의 범위다.**

> **다음 벌이 할 일은 화면을 더 찍는 것이 아니라 쓰기를 한 번씩 누르는 것이다.**
> 그 한 번이 여섯 행을 움직인다 — 하한 41.7 → 최대 **58.3**. 즉 §3′-0 이 적어 둔
> 58.3 은 틀리지 않았고, **다만 그 길이 「화면 대조」가 아니라 「누르기」였다.**

## 올린 둘 · 내린 하나

| 행 | 페인 | 값 | 왜 움직였나 |
|---|---|---|---|
| **1** | U1 처리한 것과 안 한 것이 섞인다 | 0.5 → **1** | §1 이 요구한 셋이 다 있다 — 스크린샷(`fire_user/dsm_events_preset_unhandled.png` · U1 `gxseed_u1_operator` · 1440px · 시드) + 그 화면이 부른 라우트(`events?response_state=occurred 200` · `events/queue?limit=200 200`) + 시험(`test_c_w1_presets.py` 44 passed). 픽셀에 「미처리 7건」·「처리 단계」 축·단일 초점 큐가 실제로 있다 |
| **4** | U2 밤사이 무슨 일이 | 0.5 → **1** | 같은 셋. 요약 한 줄(미처리 2 · 오탐 0 · 미판정 2)과 **두 끝 닫힌 12시간 창**이 픽셀에 있다. ⚠ 캡처 계정은 U1(fire_user) — U2(fire_admin) 로 S2 를 완주한 화면은 아직 없다 |
| **8** | U3 사진 한 장이 문장 열 줄보다 낫다 | 0.5 → **회색** | 큐 카드가 「사진을 불러오지 못했습니다」를 그렸고, 오늘 재측에서 `GET /api/dsm/events/4805/snapshot` 은 **503 「저장소 연결 안 됨 — MinIO」**. 앱의 MinIO 자격이 자리표시자라는 **환경의 사실**이지 제품의 사실이 아니다 → 0 으로도 0.5 로도 못 센다(하한 0 · 상한 1) |

**그래서 하한은 +1.0 이 아니라 +0.5 만 올랐다.** 두 행을 올리고 한 행을 내린 것이
정직한 셈이다.

## 화면이 닿지 않는 행 여섯

5(카메라 소음 순위) · 9(정숙시간 · 손 밖) · 10(hwpx) · 11(LAW-07) · 12(감사 읽기) ·
17(웹훅). 라우트가 아예 없거나(5 · 9 · 10 · 12), 화면이 답이 아니거나(17 — U6 은
개발자다), 32장 중 그 라우트를 부른 화면이 0장(11)이다. **이 여섯은 화면 대조로는
움직이지 않는다.** 값을 그대로 두었다.

## 화면이 오히려 **못 함을 증명한** 행 둘

- **16 관리 화면이 남의 제품 (0)** — 이제 사진이 있다. 32장 중 **11장이 남의 제품**이고
  (device · roles · menu · configuration-management · profile · operation-settings ·
  surveillance-dashboard · media-data · flight-log-analysis · notam · multi-stream-monitor),
  왼쪽 메뉴가 영문이다(Dashboard · Surveillance · GCS · Asset · Delivery Settings · Admin).
  `verify_sidebar` 실측이 그 크기를 잰다 — **admin 71줄(제품 4) · view_only 26줄(제품 2).**
- **18 화면이 무슨 말인지 (0.5)** — 값은 그대로지만 **사유가 바뀌었다.** 「역할 계정 화면
  미확인」이 아니라 「확인했고 절반이다」다. DSM 화면 넷의 우리말은 픽셀로 훌륭하다
  (「미처리 — 아직 아무도 손대지 않은 것」 · 「여기 없는 것은 아직 없는 것입니다」).
  그러나 **같은 계정이 보는 관리·OEM 화면 11장은 영문 그대로**다. 「전원」의 절반이다.

## 대조 중에 나온 어긋남 셋 (제품이 아니라 **증거**의 어긋남)

1. **INDEX 의 상태 코드가 픽셀과 어긋난다.** `dsm_queue_focus.png` 의 `calls` 는
   `GET /api/dsm/events/4805/snapshot **200**` 이라 적었는데, 그 사진 안에서 카드는
   「사진을 불러오지 못했습니다」를 그리고 있고 오늘 같은 경로는 **503** 이다.
   *한 장의 메타가 그 장의 픽셀과 다르면 인덱스가 거짓말을 싣는다(D-284).* C 차선에.
2. **요약 한 줄과 목록이 다른 창을 센다.** 「미처리 보기」 화면 위에 붙은 요약은
   12시간 창(미처리 **2건**)인데 그 아래 목록은 창이 없다(**7건**). 두 수가 한 화면에
   나란히 있으면 읽는 사람은 둘 중 하나를 틀린 수로 읽는다.
3. **`/handover` 는 본문 0자다**(D-386 `blank_screens`). U1 의 「아무 일 없는 8시간」이
   기대는 인계 화면이 백지다. `/users` 도 같은 모양이고, `/monitoring-dashboard` ·
   `/intergrated-dashboard` 는 `/profile` 로 튕긴다.

---

# 8영역 — **어느 판정기가 · 어느 산출물을 · 언제 쓴 것으로 · 실측인가 회색인가**

P-104 의 대조를 이 턴에 다시 돌렸다(20:1x). 지난 턴과 갈린 자리 둘을 ★ 로 적는다.

| 영역 | 가중 | 수 | 판정기 | 읽는 산출물 · 쓴 때 | 판정 |
|---|---|---|---|---|---|
| ① 기능 완결성 | 20% | 82% | `verify_ga_readiness` **자체 정규식** — **게이트 0벌** | `D-309/contract_ac_ledger.yaml` · **09-03 10:51** (나흘 전) | **회색** — 대장에 `gate:` 가 **한 칸도 없어** 대장↔게이트 대조가 이 영역을 통째로 건너뛴다 |
| ② 보안 | 20% | 80% | 게이트 10벌 (오늘 전부 초록) | `D-343/route_inventory.json` · **09-07 16:42** | **실측** |
| ③ 격리·다중테넌시 | 10% | 100% | `verify_tenant_scope` · `verify_route_scope_declared` + 시험 3벌 | **살아 있는 라우터** / `D-343` · 09-07 16:42 | **실측** (다만 런타임 눈은 pre-commit 에 없다 — 커밋 때는 정적 518건만 본다) |
| ④ 운영 | 15% | 77% | 게이트 4벌 | ★ **오늘 `verify_front_line_502` 가 빨강** — 절 OPS-13a 는 「구현」인데 게이트는 실패 | ★ **회색** (지난 턴 실측+회색 → 이제 **모순**: 미설명 502 2건) |
| ⑤ 성능·확장 | 10% | 71% | `verify_perf_budget` | `PERF-01/load.json` 09-05 10:38 · `PERF-04/budget.json` 09-05 20:29 | **회색** — 게이트가 **스스로 exit 2** 를 냈다(회귀 잡음 27~80% > 문턱 20% · STATUS 300ms 를 p95 196–338ms 가 걸친다). 제품이 아니라 **runserver 라는 측정대**의 회색 · OPS-13 뒤 gunicorn 에서 다시 잰다 |
| ⑥ 품질 | 10% | 92% | `verify_decision_tools` · `verify_alarm_budget` (오늘 초록) | 코드·대장 직접 | **실측** |
| ⑦ 사용성 | 10% | 89% | `verify_sidebar` · `verify_ui_copy` · `verify_wall_keys` | `UX-20/copy_baseline.txt` 09-07 09:23 | ★ **회색** — `verify_sidebar` 가 `verify_ga_readiness` 안에서는 **빨강**(「U5 는 시드 계정이 없다」)인데 20분 뒤 단독 실행은 **exit 0**(admin 71줄까지 잼). **역할 계정 세션을 잡느냐에 따라 뒤집힌다** — 지금 C 차선이 브라우저로 같은 계정을 몰고 있다 |
| ⑧ 법·인증 | 5% | 60% | `verify_clip_extraction` · `verify_retention_declared` · `verify_evidence_chain` (오늘 초록) | `D-373/backup_last.json` · **09-07 20:12** | **실측** |

    가중 합계 **82.2%**  ·  `verify_ga_readiness` **exit 1(빨강)**
      대장↔게이트 26절 · 게이트 25벌 — 색이 같다 **23** · 못 쟀다 **1**(PERF-04) · **빨강 2**(OPS-13a · UX-25)

**그 82.2 중 실측은 가중의 45%뿐이다.**

    실측 ② 20 + ③ 10 + ⑥ 10 + ⑧ 5 = **45%**
    회색 ① 20 + ④ 15 + ⑤ 10 + ⑦ 10 = **55%**

**가장 무거운 칸(① 20%)이 가장 회색이다.** 게이트가 한 벌도 없어서 「빨강이 날 수
없는」 영역이고, 빨강이 날 수 없는 수는 초록이 아니라 **아직 색이 없는 수**다.
QA 가 `verify_feature_reach` 로 ① 에 게이트를 붙이는 중이고, **붙으면 헤드라인은
내려갈 것이다.** 그것이 후퇴가 아니라 반납이라는 것은 셈법 §0 이 이미 적어 두었다.

> ⚠ **⑦ 을 잴 때 남의 세션을 빼앗는다.** `verify_sidebar` 는 역할 계정 넷으로 실제
> 로그인한다. 계정마다 동시 세션이 하나라, C 차선이 브라우저로 그 계정을 몰고 있는
> 동안 이 게이트를 부르면 **둘 중 하나가 튕긴다.** 이 턴의 ⑦ 빨강이 그 자국일
> 가능성이 높다. 병렬 차선에서 이 게이트는 **시각을 맞춰서** 불러야 한다.

---

## 곁가지 — PR 도 한 칸 내려갔다 (§3′-2 항목 15)

`verify_front_line_502` 가 이 턴에 **빨강**이다: 502 29건 중 **요청 구간 안의
`Worker exiting` 으로도 설명되지 않는 것이 2건**. §2 의 술어는 「넘지만 **전부**
설명됨 = 0.5 · **미설명 1+** = 0」이므로 항목 15 는 **0.5 → 0**, PR 은 **53.3 → 50.0%**.

**항목 3(`live_freshness`)과 항목 15(5xx)는 둘 다 재측마다 뒤집히는 칸이다.**
그러므로 PR 을 적을 때는 언제나 **잰 시각**을 함께 적는다 — 시각 없는 PR 은
다음 재기동에 거짓이 된다.
