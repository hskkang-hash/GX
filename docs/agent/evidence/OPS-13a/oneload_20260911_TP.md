# OPS-13a — 502 와 워커 재활용의 동시성 (2026-09-11T01:57:33+00:00)

**이 파일은 `scripts/verify_front_line_502.py` 가 실행하며 적었다.**

## 502 는 언제 나는가 — **대조와 함께** [실측]

읽은 것: 앞단 접근로그 3603줄(시각 창 08:15:35–08:16:48) · 뒷단 자국 둘 — 예고(`Autorestarting`) 14건 · **종료(`Worker exiting`) 14건**
뒷단 생사: 로그가 보이는 처음 2026-09-06T14:37:41+00:00 · **재기동 7회**(문이 닫혀 있던 창)
표본: 502 **1건**(전체 1건 중 · 뒷단 부재 0건 · 뒷단 로그 밖 0건을 뺐다) · 정상 200 3602건(전체 3602건 중 같은 자로 0건을 뺐다 · 대조로 400건 솎음) · 재시도 56건 · 되살림 56건
★ **뺀 수를 지우지 않는다**(P-92): 종전 판은 502 1건 전부를 ②에 넣었고, 그래서 미설명 0건을 「다른 결함」이라 적었다. 그중 0건은 결함이 아니라 **뒷단이 그 순간 없었던 것**이고 0건은 **맞댈 자국이 없는 구간**이었다.

| 창 | 502 | 정상 200 (대조) |
|---|---|---|
| **±0초 (같은 초)** | 1/1 = 100% | 72/400 = 18% |
| ±1초 | 1/1 = 100% | 181/400 = 45% |
| ±5초 | 1/1 = 100% | 362/400 = 90% |
| **요청 구간 × `Worker exiting`** | 1/1 = 100% | 75/400 = 19% |

★ 마지막 줄이 판정에 쓰는 자다. 위 세 줄은 **종전 자**이고 지우지 않는다 —
  지우면 「무엇을 바꿔서 설명이 되었는가」가 사라진다(P-92 · 턴 J).

★ **대조가 없으면 이 표를 못 읽는다.** 재활용이 잦으면 창을 넓히는 것만으로
  무엇이든 재활용 옆에 놓인다 — 위 표의 아래 줄들이 그 사실을 스스로 말한다.

## 판정

  OK   ① 표본         502 1건 · 대조 200 400건 · 재활용 14건
  OK   ② 설명         502 1건 **전부**가 `Worker exiting` 을 요청 구간 안에 갖는다 (종전 자 ±0초·`Autorestarting` 로는 미설명 0건이었다 — 그 0건은 다른 결함이 아니라 **판정기의 눈**이었다)
  OK   ③ 대조 분리      요청 구간 · `Worker exiting` — 502 100% 대 정상 19% (차 81%p ≥ 30%p)
  OK   ④ 삼킴         앞단이 재시도 56건 중 **56건을 되살렸다**(손님은 200 을 봤다) · 새어 나간 502 1건
  OK   ⑤ 뒷단 부재      이 판의 502 는 **전부 살아 있는 뒷단이 낸 것**이다 — 뺀 것이 없다

판정 **통과** (exit 0).
