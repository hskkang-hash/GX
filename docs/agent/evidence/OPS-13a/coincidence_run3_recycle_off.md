# OPS-13a — 502 와 워커 재활용의 동시성 (2026-09-05T06:36:16+00:00)

**이 파일은 `scripts/verify_front_line_502.py` 가 실행하며 적었다.**

## 502 는 언제 나는가 — **대조와 함께** [실측]

읽은 것: 앞단 접근로그 3722줄(38961번째 줄 다음부터) · 뒷단 재활용 0건
표본: 502 **0건** · 정상 200 3722건(대조로 400건 솎음) · 재시도 0건 · 되살림 0건

| 창 | 502 | 정상 200 (대조) |
|---|---|---|
| **±0초 (같은 초)** | 0/0 = 0% | 0/400 = 0% |
| ±1초 | 0/0 = 0% | 0/400 = 0% |
| ±5초 | 0/0 = 0% | 0/400 = 0% |

★ **대조가 없으면 이 표를 못 읽는다.** 재활용이 잦으면 창을 넓히는 것만으로
  무엇이든 재활용 옆에 놓인다 — 위 표의 아래 줄들이 그 사실을 스스로 말한다.

## 판정

  GRAY ① 표본         재활용이 **0건**이다 — 맞댈 자국이 없다. `max_requests` 가 꺼졌거나 부하가 워커 하나당 상한에 못 미쳤다
  GRAY ② 설명         502 가 없어 설명할 대상이 없다
  GRAY ③ 대조 분리      502 가 없어 갈릴 것이 없다
  GRAY ④ 삼킴         삼킬 것이 **없었다** — 재시도 0건 · 502 0건. 뒷단이 이 부하 동안 연결을 한 번도 끊지 않았다는 뜻이고, 앞단의 재시도 설정이 옳은지는 **이 판으로는 못 잰다**

판정 **못 쟀다** (exit 2).
