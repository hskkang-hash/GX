# OPS-13a — 502 와 워커 재활용의 동시성 (2026-09-11T01:57:25+00:00)

**이 파일은 `scripts/verify_front_line_502.py` 가 실행하며 적었다.**

## 502 는 언제 나는가 — **대조와 함께** [실측]

읽은 것: 앞단 접근로그 45889줄(0번째 줄 다음부터 · ⚠ 회전에 미끄러질 수 있는 자) · 뒷단 자국 둘 — 예고(`Autorestarting`) 173건 · **종료(`Worker exiting`) 170건**
뒷단 생사: 로그가 보이는 처음 2026-09-06T14:37:41+00:00 · **재기동 7회**(문이 닫혀 있던 창)
표본: 502 **22건**(전체 226건 중 · 뒷단 부재 194건 · 뒷단 로그 밖 10건을 뺐다) · 정상 200 33844건(전체 42725건 중 같은 자로 8881건을 뺐다 · 대조로 400건 솎음) · 재시도 397건 · 되살림 371건
★ **뺀 수를 지우지 않는다**(P-92): 종전 판은 502 226건 전부를 ②에 넣었고, 그래서 미설명 198건을 「다른 결함」이라 적었다. 그중 194건은 결함이 아니라 **뒷단이 그 순간 없었던 것**이고 10건은 **맞댈 자국이 없는 구간**이었다.

| 창 | 502 | 정상 200 (대조) |
|---|---|---|
| **±0초 (같은 초)** | 14/22 = 64% | 65/400 = 16% |
| ±1초 | 22/22 = 100% | 164/400 = 41% |
| ±5초 | 22/22 = 100% | 324/400 = 81% |
| **요청 구간 × `Worker exiting`** | 19/22 = 86% | 71/400 = 18% |

★ 마지막 줄이 판정에 쓰는 자다. 위 세 줄은 **종전 자**이고 지우지 않는다 —
  지우면 「무엇을 바꿔서 설명이 되었는가」가 사라진다(P-92 · 턴 J).

★ **대조가 없으면 이 표를 못 읽는다.** 재활용이 잦으면 창을 넓히는 것만으로
  무엇이든 재활용 옆에 놓인다 — 위 표의 아래 줄들이 그 사실을 스스로 말한다.

## 판정

  OK   ① 표본         502 22건 · 대조 200 400건 · 재활용 173건
  FAIL ② 설명         **요청 구간 안의 `Worker exiting` 으로도 설명되지 않는 502 가 3건** — 알려진 창에 묻지 마라. 이것은 다른 결함이다
  OK   ③ 대조 분리      요청 구간 · `Worker exiting` — 502 86% 대 정상 18% (차 69%p ≥ 30%p)
  OK   ④ 삼킴         앞단이 재시도 397건 중 **371건을 되살렸다**(손님은 200 을 봤다) · 새어 나간 502 22건
  GRAY ⑤ 뒷단 부재      **204건을 창 밖으로 뺐다** — 뒷단 부재 194건(재기동 7회 · 앞단은 `connect() 111 refused` 를 적었다 · 재활용이 아니다) · 뒷단 로그 밖 10건(맞댈 자국이 아예 없는 구간). **이 판은 부하 한 벌이 아니다** — `verify_perf_budget --measure` 로 부하를 걸고 그 시각 창(`--since-time`/`--until-time`)으로 다시 불러라 (P-101)

판정 **실패** (exit 1).
