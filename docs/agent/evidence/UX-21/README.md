# UX-21 — 관제 역할에서 메뉴 연결을 끊었다 (2026-09-05 TC · 세종 P-40)

**다음 사람에게 세 줄.** 「왜 우리 관제요원에게 드론 화면이 안 보이나」의 답이 여기 있다.

1. **이 끊기는 소속(테넌트)을 가로지른다.** `core.menu.RoleMenu` 는 공용 마스터가 아니라
   소속을 갖는 표인데(969행 중 926행이 소속을 가짐 [실측 2026-09-05]), 이번 집행은
   `--all-tenants` 로 돌렸다 — 그 역할을 쓰는 **모든 소속**에서 끊겼다.
2. **되돌리는 명령은 이것이다.** `python manage.py relink_control_role_menus`
   (한 소속만 되살리려면 `--group <소속id>`). 메뉴 행도 라우트도 지우지 않았으므로
   되돌리면 그대로 돌아온다.
3. **장부는 옆에 있다.** `menu_unlink_ledger.json` — 끊긴 연결마다 소속·역할·메뉴와
   **끊기 전의 네 칸**이 적혀 있다. 되돌리기는 표가 아니라 이 장부를 되돌려 쓴다.

---

## 무엇을 왜 끊었나

세종 P-40: 드론 Device · NOTAM · 비행로그 · 조사 프로파일 · 영문 CRUD 4장을
**U1(관제요원) · U2(관제팀장) · U4(재난안전과)** 역할에서 끊는다. **U5(시스템 관리자)는 유지**한다 —
장비를 등록할 자리가 실제로 그 화면이기 때문이고, 대신 한국어 헤더 한 줄로 감쌌다.

끊은 것은 **역할↔메뉴 연결의 네 칸**(read·create·update·delete)뿐이다.
**메뉴 행도, 라우트도, 연결 행 자체도 한 줄도 지우지 않았다** — 삭제는 대표 승인 사항이고,
지우면 되돌릴 것이 없어진다.

## 이 표의 소유가 아직 선언되지 않았다는 사실

분류 등록부(`backend/tests/tenant_classification.py`)에서 `menu.RoleMenu` 는
**`DEFERRED`** 다 — 「역할↔메뉴 매핑(923행). 일부만 공용일 수 있다」.
즉 **공용 마스터로도, 주인 없음으로도 선언되지 않았다.** 그래서 실물을 쟀고,
소속을 갖는 표라는 것을 확인한 뒤 `--group` / `--all-tenants` 를 **반드시 받도록** 만들었다.
두 명령은 실행할 때마다 등록부에 분류를 다시 물어 출력하고,
분류가 `DEFERRED` 에서 바뀌면 **멈춘다** — 전제가 깨진 채로 조용히 도는 것이 가장 나쁘다.

`menu.Menu` 는 등록부에 **선언이 아예 없다.** 이 명령들은 그 표를 읽기만 하므로
지금 문제가 되지 않으나, 선언이 없다는 사실은 그대로 남는다.

---

# P-50 — 레거시 화면 셋을 **U5 에서도** 끊었다 (2026-09-05 턴 E · 세종 P-50)

**다음 사람에게 세 줄.** 「관리자인데 감시 대시보드가 안 보인다」의 답이 여기 있다.

1. **끊은 것은 넷이다** — `/surveillance-dashboard`(스켈레톤 고착) ·
   `/media-data`(빈 버킷 브라우저) · `/multi-stream-monitor`(검은 타일 · UX-23
   카메라 격자가 대체) · **`Media Viewer` 묶음 마디**. 마디를 함께 끊은 이유는
   아래 「값나가는 것」에 적었다.
2. **되돌리는 명령은 판정별로 된다.**
   `python manage.py relink_control_role_menus --bundle P-50`
   — UX-21 8자리는 끊긴 채로 두고 이 셋만 되살린다.
3. **끊긴 역할은 일곱이다** — U1·U2·U4 **그리고 U5**(admin). `superuser` 는
   **일부러 안 넣었다**: 전역 관리자 판정은 `common/tenant_roles.is_global_admin`
   한 곳이 한다(D-212). 그 역할에는 셋이 그대로 보인다.

## 실측 [2026-09-05 · 개발 DB]

| | 끊기 전 | 끊은 뒤 |
|---|---|---|
| U1 `fire_user` | 화면 9 · 마디 5 | **화면 8 · 마디 4** |
| U2 `fire_admin` | 화면 12 · 마디 5 | **화면 10 · 마디 4** |
| U4 `view_only_-_anyang` | 화면 20 · 마디 7 | **화면 18 · 마디 6** |
| U5 `admin` | 화면 61 · 마디 14 | **화면 58 · 마디 13** |
| `Menu` 행 | 131 | **131** |
| `RoleMenu` 행 | 969 | **969** |

끊은 연결 **12개** · 삭제 **0건**. 사라진 줄은 위 넷뿐이고 다른 줄은 하나도 안 움직였다
(`role_menus_before_P50.txt` ↔ `role_menus_after_P50.txt` diff).

**되돌리기 왕복 실측** (`p50_roundtrip.log`):
끊기 → 되붙이기(**끊기 전과 차이 0줄**) → 다시 끊기(**첫 끊기와 차이 0줄**).
왕복 내내 `Menu` 131 · `RoleMenu` 969 그대로.

## 값나가는 것 — **자식 잃은 묶음 마디는 스스로 남는다**

dj-core `core/menu/api.py::list_menus` 는 보이는 메뉴를
`permit_read=True 인 것 ∪ 그 조상들` 로 만든다. 그러므로 묶음 마디는
**자식이 남아서만 보이는 것이 아니라 제 행의 `permit_read` 로도 보인다.**

`Media Viewer`(Menu 76)의 자식은 `/multi-stream-monitor` **하나뿐**이다
[실측]. 자식만 끊으면 사이드바에 「Media Viewer」 한 줄이 남고, 그 마디의
`path` 는 라우트가 아니라 문자열 `Media Viewer` 라 **눌러도 아무 데도 안 간다**.
rj-core 사이드바는 `sub_menus.length > 0` 이 아니면 그 항목의 `path` 로
이동시킨다 — 빈 묶음을 숨기지 않는다 [실측 · `rj-core.es.js`].
그래서 P-50 은 마디를 함께 끊는다.

**다음에 같은 함정에 빠질 자리**: 자식이 하나뿐인 묶음 마디를 끊을 때마다 같다.
`Media Viewer` 말고도 `Asset` · `GCS` · `Delivery Settings` 같은 마디가 있고,
그들의 자식을 마저 끊는 날에는 마디도 같이 봐야 한다.
