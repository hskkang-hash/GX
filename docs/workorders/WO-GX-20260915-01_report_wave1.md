# WO-GX-20260915-01 보고 — 파 1 · 턴 1 (턴 Q)

- 기계 시각 2026-09-15 21:27 KST · 소요 약 2시간 25분(19:10 착수 · 조율자 20분 → 차선 9 동시 → 병합 → V) · 조율자 영실(Code · Opus) · 차선 9(F-FE · F-DB · U1 · U3 · U24 · U56 · E · Q · S) + 대장 보조 1
- 커밋(`turn-q`): `99f273b` feat(v1.1-백엔드) · `86d08b2` feat(화면) · `bc1deb0` test/docs · 이 보고가 든 커밋 증거 · master 앞감기(`pre-turn-q` 태그 뒤 · 이 커밋까지)
- **막는 게이트: 없음.** 커밋 도중 막힌 7건은 전부 원인을 없앤 뒤 통과했다(아래 「막혔던 것」 · `--no-verify` 0).

## 첫 표 8줄 (산출기 출력 · 없으면 회색)

| 수 | 지금 [실측] | 직전 | 출처 |
| --- | --- | --- | --- |
| 상용 /100 | **65.3** (−2.4 = 영역 7 분모 27→37 · v1.1 절 10 사실대로 등재 · 산출기 출력 · exit 2 = PERF-04 회색 1) | 67.7 | `verify_ga_readiness` |
| 손 안 도달율 | **66.2** (88/133 · 분모 +10) | 71.5 | `verify_ga_readiness` |
| 온보딩 48행 | **회색** — 이번 턴 재측 안 함 | 58~63(턴 F) | `onboarding_48.md` |
| FC 하한 | 하한 **50.0** (24/48 · 12:24Z 브라우저 실측) | 45.8 (22/48) | `verify_click_completes --measure` |
| 영역 ① 기능 완결성 | **7/39** · 회색 25 · 빨강 0 (가중 3.59) | 7/39 | `verify_feature_reach` |
| PR 상용 준비 | **회색** — 산출기 없음 | — | — |
| CR 돈 내는 8조건 | **회색** — 산출기 없음 | — | — |
| 커밋 | turn-q 4 · master 앞감기(`pre-turn-q` 태그 뒤 · 이 커밋까지) | turn-p 6 · master 미병합 | git |

**G1~G5**: G1 ○(공개 URL 0 · 자격 형식 1 실패 — 새 값 넷은 금고에 준비) · G2 ●(익명 반출 0 · **로그인 뒤 첫 화면 역할 홈 4/4**) · G3 ◐(FC 하한 50.0 · 빨강 5 · 첫 화면 역할 홈 4/4) · G4 ◐(65.3 (−2.4 = 영역 7 분모 27→37 · v1.1 절 10 사실대로 등재 · 산출기 출력 · exit 2 = PERF-04 회색 1)) · G5 ◐(재시작 10/10 · Docker 자동 시작 켬 · 저장 상한 50GB 선언 · 502 격리 29·33→10·11, 0 미달).

## P-135 비밀번호 노출 — 닫힘

- 회전: 같은 규칙의 값 **전부** — 세 값 · **10계정**(탐침 `gxprobe_e2e` 1 · 시드 4 · 차선 탐침 5). 지시서의 「5」보다 넓다 — `GX_PROBE_PASSWORD` 를 `gxprobe_v·e·s·q·c` 다섯이 함께 쓰고 있었다.
- 옛 값 grep: 세션 기록 5개에만 있었다 → 스크럽(JSON 줄 깨짐 0) · docs 0 · git 이력 0.
- 신설 게이트 `verify_no_secret_echo` 초록(자기시험 양성 5 · 출생 표본 = P-135 진단 stderr) · pre-commit `gx-no-secret-echo`.
- 로그인 **10/10 · 역할 계정 4/4** — gx-shell 안에서(호스트는 8000 에 닿지 않는다).

## P-136 병합 · bundle

- `pre-turn-p`=`6cf2c19` · master 앞감기 `dd3dd7b`(작업본 무변경) · `evidence/bundles/turn-p.bundle` 84.8MB(무시 목록 · gitleaks 6커밋 0).
- 이번 턴: `pre-turn-q` 태그 → master 앞감기 앞감기(`pre-turn-q` 태그 뒤 · 이 커밋까지).

## 첫 URL — 4/4

U1 `/dsm/queue`(「가장 급한 하나」 보임) · U2 `/dsm/events` · U4 `/dsm/events?period=d7` · U5 `/dsm/dashboard` · 휴대전화 U1 `/m/inbox` — 전부 `Personal Information` 아님 · 캡처 5장(`evidence/P-141/first_url/`).
⚠ U4 첫 화면은 기간 7일에 「미처리 보기」가 기본으로 켜져 0건이다(시드 사건은 11일 전 — 데이터 사실). 조회 전용 공무원의 첫 화면에 「미처리」 거름이 맞는지는 판정이 필요하다.

## 어댑터 대조표 (P-129) — 셋 중 같은 뿌리 **0**

봉투 승격은 상태줄만 바꾸고 본문은 그대로다 — 여섯 자리 어디서도 응답 모양이 뿌리가 아니었다. 대신 다음 라우트에서 터질 해석 결함 셋을 `unwrap()` 한 곳에서 닫았다.

## U6 403 · 서명키 · 발송 수

- U6 403 — **반쪽만 닫힘.** 서버 쪽: `gxprobe_q` 에 `tenant_admin_4` 시드 부여 → 그 계정의 키 발급 200(시험 · 권한 검사 불변). 그러나 여정 행 U6#1 은 **여전히 403** — 판정기가 그 행을 관리자 `gxseed_u5_sysop`(역할 `admin`)으로 두드리고, `admin` 은 `tenant_admin` 으로 이어지지 않는다. 남은 결함 = **U5 관리자가 API 키를 발급하지 못한다**(다음 턴 · SEC-22 배선).
- 웹훅 서명키 — **막힘**: `WEBHOOK_SIGNING_KEYS` 환경값이 비어 있다 = `.env` 실제 값(대표).
- 발송 수 — 5분 중복 억제(F-04 · 설계대로). 화면이 그 0 을 버리던 것을 「새로 보낸 알림이 없습니다」로 말하게 했다.

## click_completes · 3종 · 매핑 · row_map

- `click_completes --measure`(12:24Z · 브라우저 실측) **24/48** — 초록 24 · 빨강 5 · 회색 19 (턴 P 22/48 · +2). 사람별 U1 6/8 · U2 3/8 · U3 5/8 · U4 2/8 · U5 3/8 · U6 5/8.
  - 새 초록 둘: **U1#1**(`/login` → `/dsm/queue` · 「가장 급한 하나」 — 역할 홈) · **U3#1**(발송 이력 0 → 15 — 기대식 정정).
  - 빨강 5: U1#19 · U2#1 데이터 없음(창 안 사건 0) · U3#3 술어 없음 · U6#4 서명키 환경값 없음(대표) · **U6#1 403 — 새로 드러난 제품 결함**: 판정기는 이 행을 관리자 `gxseed_u5_sysop`(역할 `admin`)으로 두드린다(`API_ACTOR`). U56 이 역할을 준 `gxprobe_q` 는 이 행에 닿지 않는다 — **U5 관리자가 키를 발급하지 못한다**(`admin` → `tenant_admin` 배선 없음 · SEC-22 다음 몫).
- 빨강 7 3종(턴 P 관측 기준): 제품 결함 1 · 기대식 오류 3(→ 판정기 정정 · U3#3 은 술어 없어 그대로) · 데이터 없음 3.
- P-140 매핑 **18/18** · 새 절 0 · 대장 영역 7 에 v1.1 절 10 등재.
- P-142 **7→7(0)** — 기존 역할 계정 캡처로 이을 절 0 · 판정기는 row_map 을 읽지 않는다(REACH_MAP 은 코드 안).

## 운영

- OPS-13 격리: 502/3,600 — 지금 모양 29·33 → peer 세 줄 10·11 · 재시도 없이 나가는 502 0/21 · **N→0 미달**.
- 저장 상한 50GB(`backend/.env` · `.env.example`) → `storage_used_pct` UNKNOWN → 0.0.
- Docker Desktop 자동 시작 켬(대표 허락) · **재부팅 전/후 실측은 넘김**(세션이 끊긴다).
- 자격 3: 새 값 넷 금고 준비 · 형식 통과 · `RUNBOOK_재생성창.md`. 창 30분만으로는 `prod_settings` 7/7 이 안 선다(가드 코드가 먼저).

## 턴 Q 절 — 닫힘 / 넘김

닫힘: P-135 회전·스크럽·게이트·로그인 4/4 · P-136 태그·병합·bundle · P-141 역할 홈 첫 URL 4/4(+휴대전화 1) · P-129 대조표 + unwrap 한 곳 · U6 403 서버 쪽(gxprobe_q 역할) · 발송 수 조용한 0 제거 · 기대식 오류 셋 정정 · P-140 18/18 · api 분할(D-464) · 쓰기 면 선등록 · v1.1 표 일곱(D-463) · U1 판정+접수 한 트랜잭션 + 큐 키 1 · U24 통계 셋 · U3 현장 사진 라우트 + 지도 링크 · P-138 새 값 넷 + runbook · P-139 상한 50GB · Docker 자동 시작 · 결정 대장 D-440~466
넘김: P-137 N→0(10·11 에서 멈춤 · 운영 모양 재측 다음 턴) · P-142 7→15(전제 불성립 · 설정 화면 벌이 서야 움직인다) · SEC-16 서명키(`.env` 실제 값 = 대표) · U6#1 U5 관리자 키 발급(admin→tenant_admin 배선 · 다음 턴) · U3#3 판정 술어(사진 적재) · 재부팅 전/후 실측 · people.py(잠든 코드 · 설계 검토) · P-90 운영 탐침(대표 확인) · 온보딩 48행 재측

## V 빨강

click_completes 빨강 5(위) · feature_reach 회색 25 · PERF-04 회색(gunicorn 기준선 없음) · live_freshness 초록 · no_secret_echo 초록 · ui_copy·ui_secrets 초록 · contract_route_reach 59/59 초록

## 막혔던 것 (전부 원인 제거)

1. pre-commit 설정 미스테이징 → 설정 파일을 ①에
2. `gx-tenant-scope` — 인구조사 생성기가 낡은 경로 지도로 사람 판정 두 줄을 덮음 → HEAD + 새 6줄
3. `gx-tool-selftest` — 새 게이트 출생 표본 없음 → P-135 사고 모양 fixture
4. `gx-cache-bypass` — U3 시험 둘 표기 없음 → `X-No-Cache` 우회 + 선언
5. `end-of-file-fixer` — 증거 JSON 4 → 다시 스테이징
6. `gx-ga-readiness` — F-05 세 절 빨강 ← snapshot 503 ← **조율자가 runserver 를 MinIO 환경 없이 띄움** → 기록대로 재기동 → 200
7. `verify_dead_fields` — v1.1 쓰기 0곳 14칸 → D-463 배선 차선·턴 선언

## 가정 목록 (세종 미판정 · 기본값 택함)

1. `api.py` 는 옮기지 않고 차선 컨트롤러를 뒤에 붙였다 · `api_base.py` 없음(D-464)
2. v1.1 표는 `stream_monitors` 에(`apps.dsm` 은 앱이 아니다) · 추상 `TenantModel` 신설(D-463)
3. 쓰기 면 선등록 17줄(지시서의 「13」과 다르다 — 표를 한 줄씩 옮긴 수)
4. 통계 경로 `/stats/*` 이름공간 · 배치 표 대신 60초 캐시
5. 역할 홈 사양 §5 미정 셋은 사양 표대로(U5 = 대시보드)
6. 저장 상한은 작은 쪽(호스트 C: 가용 × 0.8 = 50)
7. `people.py` 는 커밋하지 않았다 — 잠든 코드 게이트 + 운영 코드의 시험 클라이언트 모양(설계 검토)
8. `evidence/P-90/probe_spec.md` 는 커밋·집행하지 않았다 — 「대표 승인」이 파일에만 있고 이 세션에 대표 지시가 없다

## 세종 오류 · 영실 오판 · 위임 이의

- 세종 오류 후보 2: PRD 에 없는 ID 「UX-32-U4」 · P-142 「7→15」의 전제(판정기가 row_map 을 안 읽는다)
- **영실 오판 2**: ① 차선 시험 DB 분리를 `TOX_PARALLEL_ENV` 로 지시 — conftest 가 덮어 9차선이 공용 DB 에서 돌았다(올바른 이름 `DB_TEST_NAME`) ② runserver 재기동 때 MinIO 환경 누락 — 자기 메모에 적혀 있던 것을 어겼다
- 위임 이의 1: P-90 운영 탐침 — 파일의 승인 문구로는 운영계를 두드리지 않는다(대표 확인 요청)
