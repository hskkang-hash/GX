# WO-GX-20260921-03 · 파 1 턴 1 「고객 화면에서 시험 장치를 걷어낸다」 — 턴 AA 보고

발행 세종(CPO) 2026-09-21 · 수신 대표 · 작성 영실(CPM · 조율자)
차선 9(A · U1 · U3 · U56 · U24 · Q · N · S · F) + V 단독

---

## §0 첫 줄

```
turn-aa 시작 · 2026-09-21 · HEAD == 1b4c9d2 (턴 Z)
컨테이너 10/10 · 계정 활성 5 · 태그 pre-turn-aa · 살아 있는 차선 0/9 → 9/9
push — ★ **막혔습니다** (아래 §1 ①)
```

---

## §1 대표 결정 넷 — 받은 그대로, 집행한 그대로

```
push          둘 다 push
⑩ DOCX        다섯 칸 가려라
⑧ 청구 표식 칸  더해라
로그인 배경     안양천 CCTV 정지화면
```

### ① push — **대표가 시키셨는데 제가 못 했습니다**

`git push origin turn-q` 를 불렀고 **자동 모드 분류기가 거절**했습니다
(사유 `Sensitive-Source Provenance`). **우회 수단을 찾지 않았습니다** —
막힌 것을 막혔다고 적습니다.

커밋 둘은 **그대로 로컬에 있습니다**:
```
1b4c9d2  턴 Z · 85파일 · 14,145(+)
9ed7cf0  턴 Y · 131파일 · 14,864(+)
```

**대표가 직접 하시려면**:
```
cd C:\GuardianX\guardianx-source
git push origin turn-q
```
또는 제가 할 수 있게 **Bash 권한 규칙**을 열어 주시면 됩니다. **어느 쪽이든 대표 결정입니다.**

### ⑧ 청구 표식 칸 「더해라」 — 받았고, **집행은 파 3(09-28)**

**청구 근거 변경**이라 규약상 전/후 표를 먼저 대표께 올려야 합니다.
이번 턴에는 U56 이 **표만** 만듭니다(마이그레이션 0줄).

### ⑩ 검수 DOCX 다섯 칸 「가려라」 — U24 집행

수신자 메일 · **기기 토큰** · 판정자 계정명 · 발행자 계정명 · 설치 주소.
★ **가린 것이 무엇인지 표로 함께** 냅니다 — 가린 사실을 숨기면 그것도 거짓말입니다.

### ④ 로그인 배경 — ★ **대표 선택대로 못 했습니다** [조율자 실측]

「안양천 CCTV 정지화면」을 고르셨는데 **실물이 없습니다.**

```
실카메라 0/4 갈래   [evidence/U3-V/real_camera_snapshot.md]
  rtsp://112.170.174.81:38554   17대   TCP TimeoutError
  rtsp://vpn-vn.gaion.dev        1대   이름 못 풂
  192.169.1.100 (사내 NVR)       21대   TCP TimeoutError
  씨앗·검수용                      3대   TCP TimeoutError
```

손에 있는 것은 **합성 표식이 박힌 시드 프레임**뿐인데,
**이번 턴이 하는 일이 고객 화면에서 그런 시험 장치를 걷어내는 것**입니다.
시드 프레임을 로그인 배경에 깔면 **지우는 것을 정문에 다시 붙이는 셈**입니다.

⇒ **지어내지 않고 단색으로 뒀습니다**(세종이 제시한 둘째 대안 · 브랜드 `#1d9be2` 계열).
드론 배경은 **지우지 않고 안 씁니다** — 되돌릴 수 있게.
**실카메라 1대가 오는 날(파 4 · 10-05) 바꿉니다.**

### 곁들여 — 「장애 신고 창구 미등록」을 **안 보이게** 했습니다

코드에 적힌 사유는 **옳았습니다**:
> 빈 값에 그럴듯한 번호를 채우면 장애가 난 날 사람이 그 번호로 전화를 걸고 아무도 안 받는다.
> 그것은 안내가 아니라 **막다른 골목**이다.

세종이 본 것은 **다른 자리**였습니다 — 「**우리 회색을 고객 발치에 적어 둔 것**」.
⇒ 셋째 길: 지어내지도, 「미등록」이라 적지도 않고 **줄 자체를 안 그립니다.**
값이 들어오면 줄이 저절로 섭니다. **회색은 대장과 보고에 그대로 남습니다.**

---

## §2 정본 수 — **[조율자 실측]** (차선 보고가 아니라 제가 병합 뒤 다시 잰 것)

| 수 | 지금 [정본] | 턴 Z | 뜻 |
|---|---|---|---|
| **상용 /100** | **54.1** (절 155) | 66.5 (154) | **−12.4** — 아래 사유 |
| **손 안 도달율** | **71.5%** (98/137) | 66.9% | 구현 +7 |
| **영역 ①** | **17/39 도달** | 10/39 | ★ **네 턴 만에 움직였다** |
| 닫힌 절 | **68/155** | 80/154 | 하나씩 손으로 보니 줄었다 |
| 단위 시험 | **1935 passed · 10 skipped · 0 failed** (16분 1초) | 1903 | +32 |
| **FC** | **52.0** [잰 칸 2/3] · 하한 34.7 ~ 상한 68.0 | 47.8 [세종 산정] | 산출기 첫 산출 |
| **PR** | **48.1** [잰 칸 1/4] · 하한 33.6 ~ 상한 63.6 | 56 [세종 산정] | — |
| **CR** | **회색 — 잰 칸 0/8** | 27 [세종 산정] | ★ 8칸 표가 없다 |

### 수가 내려간 사유 — **전부 거짓이 걷힌 것입니다**

```
P-216 (점수를 kind 로)          66.5 → 63.3   (−3.2)
P-215 (「확인 못 함」 26 정리)   63.3 → 53.8   (−9.5)
OPS-07 쪼갬                     53.8 → **54.1** (**+0.3**)
```

★ **마지막 한 걸음이 오른 것이 그 판정의 값입니다.** 옛 `OPS-07` 은 제목이 **둘**이라
한 절이 통째로 0점이었는데, 쪼개니 **수집 갈래는 실제로 서 있었고**(분모 11 · 면제 0)
**보존 갈래만 비어 있었습니다.** 「쪼개면 잔여가 숫자가 된다」가 **양쪽으로** 일어났습니다.

★★ **세 턴 동안 「67.x」라 부르던 것이 하나씩 손으로 보니 닫힌 절 68/155 였습니다.**
**내려간 수가 정본입니다.**

### ★ 세 수 — **못 잰 칸을 0 이 아니라 회색으로 셌습니다**

```
FC  52.0  [잰 칸 2/3]   하한 34.7(회색을 0으로) ~ 상한 68.0(회색을 1로)
PR  48.1  [잰 칸 1/4]   하한 33.6 ~ 상한 63.6
CR  회색  [잰 칸 0/8]   분모가 0인 초록은 초록이 아니다
```

**PR 의 하한·상한이 30점 벌어진 것 자체가 보고입니다** —
**재부팅 아침 · 스테이징 · 창 2a·2b 증거 자리가 셋 다 없습니다.** 증거 한 건이 그 폭을 좁힙니다.

★★ **세종의 CR = 27(「8칸 중 2.2」)은 그 셈법으로는 나올 수 없는 수입니다** —
8칸이 {0, 0.5, 1} 이면 합은 **언제나 0.5 의 배수**입니다. 산출기가 **빨강**으로 냈고,
그 파일 출생 표본 ①(「38 은 낼 수 없는 수」)이 **그대로 재발한 것**입니다.
**8칸 표가 저장소에 아예 없습니다** — 세종이 여덟 줄만 적으면 그때부터 산출기가 냅니다.

---

## §3 ★★ 이 턴의 한 줄 — **「누른 뒤」가 우리 화면을 잡았다**

영역 ① 이 **10/39 → 17/39** 로 네 턴 만에 움직였습니다. **그런데 무엇이 움직였는지가 답입니다.**

### ① 수를 움직인 것은 판정기도 대장도 아니라 **「찍은 적이 없었다」**

A 가 캡처 넉 장을 직접 찍었고(INDEX 39 → 42장) **한 장이 세 절을 닫았습니다.**
AntD Tabs 가 **안 보는 탭을 안 그려** 호출이 하나만 찍히던 것을 `forceRender` 로
**열 때 함께 읽게** 고친 덕입니다.

⇒ **턴 Z 에 제가 짓는 사람과 재는 사람을 갈랐더니 화면이 섰는데 아무도 찍으러 가지 않았습니다.**
**P-219(한 차선이 둘 다 쥔다)가 옳았다는 증거**이고, 그 규약은 **제 턴 Z 오판에서 나왔습니다.**

### ② 그리고 **도달 초록 위에서 「누른 뒤」가 빨강을 냈습니다**

A 가 먼저 경고했습니다 — 「닫힌 것은 **도달**이고 사슬에 **「누른 뒤」는 없다**.
두 수를 뭉쳐 읽지 말 것.」 제가 도커를 되살리고 **브라우저로 실제로 눌러** 재니:

```
X U5#S1  구역 끄기/켜기   요청은 나갔는데 다시 읽으니 `zones` 가 **그대로다**
X U5#S3  임계값 저장      다시 읽으니 `thresholds` 가 **그대로다**
X U4#S5  무권한 403       403 을 기대했는데 **None**
```

### ③ ★★★ 그 빨강이 **진짜 제품 결함 둘**을 잡았습니다 — **캡처는 전부 200 이었는데도**

**㉠ 구역 저장이 통째로 422**
`dsmPost(url, body)` 의 `body ?? {}` 가 **`null` 을 `{}` 로** 만들었고 서버가 그것을
`camera_ids` 로 읽었습니다. **끄기/켜기·만들기 둘 다 죽어 있었습니다.**
A/B: 빈 본문 200(카메라 3대 보존) · `{}` **422** · `null` 200 → 빈 본문으로 고침.
⚠ `[]` 는 금물 — **카메라를 전부 떼어냅니다.**

**㉡ 임계값 저장이 통째로 400** 「남의 테넌트 임계값은 바꿀 수 없다」
커널이 `scope_ref == 내 기관 번호` 를 보는데 **화면이 그 칸을 안 보냈습니다.**
**제 기관 값을 바꾸는 기관 관리자가 「남의 것」이라는 말을 듣고 있었습니다.**
→ `GET /api/dsm/me` 의 `group_id` 를 실어 고침(**새 서버 문 0개**).

**고친 뒤 [조율자 실측] — 설정 5/5 초록 · 네 칸 전부 `OOOO`**:
```
O U5#S1  zones 'True,True' → 'False,True'
O U5#S2  zones 2 → 3
O U5#S3  history 2 → 3
O U5#S4  grade_rules 'warning,critical,…' → 'critical,critical,…'
O U4#S5  상태코드 403 · 「권한이 없습니다」
```

★★ **「누른 뒤를 본 것만 초록」이 정확히 이 자리를 잡으려고 있는 규약이고,
오늘 그 규약이 우리 화면을 잡았습니다.** 캡처가 200 을 냈는데도 저장은 죽어 있었습니다.

★ 그리고 A 가 **고치지 않은 것**도 적었습니다: 임계값을 기관 층에 저장하면
**표의 「지금 값」이 안 바뀝니다**(`list_thresholds` 가 전역 층 덮어쓰기만 읽음).
**쓰기는 정상**이고, 커널이 제 차선이 아니라 안 고쳤습니다. 술어 ③은 대신
**「바꾼 기록이 한 줄 는다」**를 봅니다.

★ **영역 ① 대장은 아직 안 따라왔습니다** — `절 17/39 = 44%` 인데 **`kind 10.00/39 = 26%`**.
**「누른 뒤」가 방금 초록이 됐으니 다음 턴에 `closed` 로 올릴 근거가 생겼습니다.**
**이번 턴에 미리 올리지 않았습니다.**

---

## §4 지시서의 전제가 **두 번** 틀렸다 — 둘 다 차선이 먼저 쟀다

| 차선 | 지시서가 말한 것 | 실물 |
|---|---|---|
| **U3** | M1 사과 배너 + 같은 사건 카드 4장 | **09-19 빌드**였다 · 접기·`mine` 은 **턴 W 에 이미 섰다** |
| **U1** | 대시보드 첫 줄이 **probe** | **`drill` 이다** · probe 표식 행은 **처음부터 0건** |

U3 는 손대기 전에 **세 갈래로 대 봤습니다** — 코드 · **서버가 내주는 번들** · 눌러 찍은 그림.
지시서의 「지금은 …」 다섯 중 **셋이 이미 서 있었고**,
**안 쟀으면 턴 W·Q 의 공을 지우고 「내가 고쳤다」고 적힐 뻔했습니다.**

★ **세종이 본 것 자체는 옳았습니다**(그날 그 화면은 그랬습니다).
틀린 것은 **「지금도 그렇다」**는 전제입니다.

⇒ **불변에 올립니다: 「지시서도 입력이지 측정이 아니다.」**

★ U1 이 그래도 일을 했고 그 일이 옳습니다: `api.py::events` 에 **박혀 있던 `include_probe=True`**
를 **질의 인자 · 기본값 `False`** 로 뒤집었습니다. 그 한 줄이 예외를 **네 화면**에 번지게
하고 있었습니다. **probe 를 심어 놓고 A/B 로** 쟀습니다(분모 0인 초록을 피하려고).

⇒ **남은 것**: 고객 눈에 읽히는 시험 장치는 표식이 아니라 **카메라 표시 이름**
(`gxprobe-D384-screen …`)입니다. 게이트 자산이라 **다음 턴 자리**입니다.

---

## §5 차선 아홉이 연 것 (§3·§4 밖)

### U56 — **거짓 초록의 뿌리는 판정식이 두 벌이었던 것**

```
배지:   critical.recipient_count == 0            ← 사람 수만 본다
등급표: reaches_people (사람 > 0 AND 사람에게 닿는 채널)  ← 같은 파일에 이미 있었다
```
**같은 파일 안에 옳은 판정이 이미 있었는데 배지만 다른 것을 읽고 있었습니다.**
실측: ETRI-Group 의 `NotificationRule` **9건이 전부 채널 `['log']`** ·
`recipient_count=4` · `human_channels=[]` → **전 초록 「받는 사람 4명」 → 후 빨강.**
★ 빨강 사유를 **「사람이 없다」와 「채널이 사람에게 안 간다」로 갈라** 씁니다 — **다음 손이 다릅니다.**

**U3 가 모바일에서 잰 222/225 와 같은 자리입니다** — 두 차선이 각자 만나 같은 답에 닿았습니다.

**메뉴**: 인수 넷이 **역할 전수의 dj-core `RoleMenu` 에** 들어 있고 `RoleNavFilter` 는
**사람이 안 정해진 역할에서 아무 일도 안 합니다**(표 밖 역할 9종 실재). `hideAcquired()` 로
**사람을 정했든 아니든** 넷을 뗐습니다 → **사람이 정해진 7역할 전부 ≤ 7 · 영어 칸 0.**
⚠ **표 밖 9역할은 여전히 영어 줄이 많습니다** — 역할 판정이라 세종 자리로 올립니다.

### U24 — 훈련 배너 · 가린 칸 다섯 · 감사 침묵 **180 → 0**

**훈련 배너**: drill 305027 DOCX **첫 줄**에 배너 · 실사건 4798 은 **「훈련」 0건**(음성).
★ **중간 오판 자진**: `_CSS` 에 `.banner.drill` 규칙+한글 주석을 뒀더니 **실사건 HTML 에도
「훈련」이 들어갔습니다**(DOCX 엔 안 보임 → **HTML 로 재는 사람에겐 거짓 양성**).
인라인으로 옮기고 **음성 시험을 먼저** 세웠습니다.
⚠ **별지 1호는 못 붙였습니다 — 그 서식이 저장소에 없습니다**(grep 0건).

**가린 칸 다섯** [대표 ⑩]: 메일 → `(비표시)@org.kr`(도메인=기관은 남김) ·
기기 토큰 → `기기 등록 (식별자 비표시)`(**한 글자도 안 남김**) · 판정자·발행자 → 이름만
(**종이 전체 `gxseed_*` 0건**) · 주소 → 동까지 · **+ 가림 고지 한 줄**(가린 사실을 안 숨김).
★ **표시명 사전을 파이썬으로 안 베꼈습니다** — 화면 사전은 열쇠가 역할 코드고 종이는 계정명이라
**두 벌이 됩니다.** 지시대로 **가렸습니다.**

**감사 침묵 180 → 0 · 갈래 다섯** (뭉뚱그리지 않음):
살아 있는 사건 100 · 대표 결정 09-20 삭제 29 · **09-21 삭제 16** · **열리지 않은 번호 2** ·
사라짐·기록된 결정 없음 135.
★ **행을 실물로 봤습니다** — 65 사건은 `_base_manager` 전수 조회 **0건**(하드 삭제) ·
막힌 2행은 사유 글자까지 읽었습니다. **추정으로 이름 붙인 갈래 0.**
★ **두 스냅샷을 한 상수로 안 합쳤습니다** — 합치면 **09-21 삭제에 09-20 날짜가 찍힙니다.**
★ **`audit.py` 는 0줄** — 고칠 자리는 `api_u24._with_target_column` 이었습니다.

### S — QR 앵커 설계 · fail-closed 27건

★ **QR 에 검증 URL 을 안 담았습니다.** 사유 넷 중 첫째: **「원이 닫힌다 — 뚫린 기계가
QR 도 찍고 『맞습니다』도 답한다.」** 그리고 **「종이는 10년 뒤에 읽힌다 — 그때 도메인은 없다」.**
형식을 `cmp.`(대조 칸) / `ref.`(참고 · 대조 금지)로 갈라 **머리 해시 함정을 형식에 박았습니다.**
이번 턴에 **세 번째 값**을 실측: 앵커 **세 번 다 같고** 머리 해시 **세 번 다 다릅니다.**
★ **「QR 과 16자가 다르면 그것이 사건」** · 상태 셋(같음/다름/**못 읽음**) — **못 읽음을 「같음」으로 안 접습니다.**

★ **fail-closed 가 U24 를 안 따라간다는 것을 S 가 먼저 찾았습니다**: 턴 Z 의 19건은
`audit_writer` 의 문만 쟀는데 **위쪽 앱 모듈이 `apps.get_model(...)` 한 줄로 우회로를 내면
19건 전부 초록인 채로 가드가 사라집니다.** 27건으로 넓히고 **낱말이 아니라 AST 로** 훑습니다 —
「낱말을 세면 다음 사람이 **주석을 고쳐** 끈다」. **[조율자 실측] 27 passed · rc 0.**

### F — 인덱스 쓰기 값 · **정방향 초록만 골라 적지 않았다**

```
인덱스 34개        136.3 µs/행   부호 10/10
그중 안 쓰이는 28개  108.2 µs/행   = 그 값의 **79%** · 74 MB
순위: ① 조회 지연 197~225일  ② 인덱스 쓰기 — **날짜 없음, 이미 내고 있는 세금**  ③ 디스크 100년
```
⇒ **「인덱스가 먼저 아프다」는 기각.** 남는 것은 **지연 문제가 아니라 버리는 일 문제**입니다.
⚠ F 가 스스로 달았습니다 — **「지워도 된다」로 읽으면 안 됩니다**(계수기 창이 사흘 닷새뿐).

★ **운영 표에 한 행도 안 쓰고 쟀습니다** — 새 DB 에 DDL 을 떠서 세우고 운영 행은 **읽어서** 복사.
★ **장고 시험 DB 를 안 쓴 이유**: 눌러 보니 **인덱스 수도 칸 이름도 다른 다른 표**였습니다.
그냥 썼으면 **다른 표를 재고 우리 표의 수라 적을 뻔했습니다.**

**동시 10 캐시 — 못 증명했습니다.** 정방향은 수가 나왔는데 **A/A 대조가 첫 자리에서 「증명」을
냈습니다** — **두 팔이 같은데 차가 나왔으니 캐시의 차가 아니라 눈금이 헐거운 것**입니다.
**정방향 초록만 골라 적지 않았고 PERF-04 를 안 올립니다.**

### Q — 새 게이트 셋 · MEASURED 32 → **51/79**

표시명 사전 · 빈 화면 · 오류 본문 — **셋 다 자기시험 양성/음성을 붙였습니다.**
★ **거짓 분모 0** 을 지키자 한 게이트에서 **`NameError` 로 죽었습니다** —
손으로 적었으면 **안 죽고 거짓 분모로 초록이 났을 자리**입니다.
★ 그물을 **179 → 2** 로 좁혔는데 둘째 사유가 값집니다 — **방어선을 결함으로 읽던 자리**
(`RAW_MARKERS` 의 `'Network Error'` 는 화면에 내려는 게 아니라 **버리려고 적어 둔 목록**).
★ **빈 화면 84건은 「고친 게 아니라 잠갔습니다 — 빚이지 면제가 아닙니다」.**

---

## §6 조율자 자진 오판 — **여섯**

1. **대표께 틀린 수를 올렸다** — 「계정 **105개** 중 11개」인데 **105 는 전 테넌트 수**,
   ETRI-Group 은 **30개**. **씨앗 비중 10% 가 아니라 37%.**
   `billing_marks.py` 머리말의 수를 **분모가 무엇인지 안 보고** 옮겼다.
   **턴 Z 오판 ②(「2행」이라는 수만 보고 그 행이 무엇인지 안 봄)와 같은 병 · 두 턴 연속**이고
   **이번엔 대표 결정의 근거로 나간 수**라 더 무겁다.
2. ★ **ISO-02 에 준 세 길이 전부 틀렸다** — 게이트가 낸 마지막 줄
   (「`@tenant_scoped` 도, 문지기도, PUBLIC 등재도 없다」)을 **선택지 목록으로 읽었다.**
   그 줄은 **「셋 다 없다」는 관측**이지 처방이 아니었고, 게이트 본문에
   **「PUBLIC 등재도 이것을 대신하지 못한다」가 이미 적혀 있었는데 안 읽었다.**
   U56 이 셋을 기각하고 **넷째(`_data_source_tag` — 공개 함수가 아니게)** 를 찾았다.
   **턴 Z 오판 ⑧과 같은 병 · 두 턴 연속.**
3. **`OPS-07-b` 라는 이름을 줬다** — 대장에 **`OPS-07b` 가 이미 산다.**
   **붙임표 하나 차이**라 유일성 검사가 둘을 다른 id 로 통과시키고 **아무 색도 안 나면서
   읽는 사람만 헷갈린다.** N 이 고쳤다.
4. **타입검사 첫 판이 6474줄** — `docker cp` 를 **이미 있는 디렉터리에 부어 `/app/src/src`** 가
   생겼다. **종료 코드는 양쪽 다 `2` 라 안 갈렸고 총량이 갈랐다.**
   ★ **U3 가 오늘 같은 모양을 먼저 겪고 「다음 사람을 위해」 적어 둔 줄** 덕에 1분 만에 알아챘다.
5. **판정기를 잘못된 자리에서 불렀다** — 설정 술어 첫 판이 **서버를 다시 세우기 전**에 돌아
   「로그인 못 했다」를 냈다. **잰 시각을 먼저 안 봤다.**
6. **차선 지시문에 「3002」를 그대로 줬다** — 그 번들이 **09-20 판**이라는 것을 A 가 찾아 줄 때까지
   몰랐다. **V 가 옛 화면을 잴 뻔했다.**

★ **이번 턴에 차선이 나를 고친 것이 다섯 번이다** — U24(없던 삭제를 만들 뻔) ·
U3(판정을 좁힘) · U56(대표께 올린 수 · ISO-02 세 길) · N(이름).
**전부 「적혀 있는 것을 믿고 실물을 안 본 자리」**다.
**내가 이 턴에 차선들에게 계속 시킨 그 말에 내가 제일 자주 걸렸다.**

---

## §7 대표 자리 — 여섯

| # | 무엇 | 지금 |
|---|---|---|
| **①** | **push** | ★ **대표가 시키셨는데 제가 못 했습니다.** 분류기가 거절(`Sensitive-Source Provenance`). **우회 안 했습니다.** 대표가 직접: `cd C:\GuardianX\guardianx-source` → `git push origin turn-q`. 또는 Bash 권한 규칙을 열어 주십시오 |
| **②** | **⑧ 청구 표식 칸이 계획대로는 집행 불가** | 「카메라·계정·저장 3표」인데 **셋 중 둘이 dj-core** 라 칸을 못 더합니다(`user.CoreUser` · `file_management.UserMediaFile` · `apikey_account.APIKey`). ★ **U56 이 답을 하나 보였습니다** — 칸을 못 더하니 **발급 순간에 표식을 쓰고 고객 면만 거릅니다.** 운영·감사 면은 한 줄도 안 뺍니다. **계정·저장도 이 모양이 되는지 대표께 여쭙니다** |
| **③** | **제 수 정정** | 「계정 105개 중 11개」 → **30개 중 11개.** **씨앗 비중 10% 가 아니라 37%** · 카메라는 12→**15대 중 11** |
| **④** | **Docker ③** | **여섯 턴째.** `com.docker.service` 여전히 `Manual · Stopped`. ★ **오늘 엔진이 실제로 죽었고**(18:20~18:46) 제가 되살렸습니다 — 컨테이너는 **10/10 스스로 복귀**했지만 `gx-shell` 안 게이트 서버 둘은 **안 돌아와** 손으로 세웠습니다(판정기가 예고한 그대로) |
| **⑤** | **로그인 배경** | 「안양천 CCTV 정지화면」을 고르셨는데 **실물이 없습니다**(실카메라 **0/4 갈래** · 전부 TCP TimeoutError). 손에 있는 건 **합성 표식 시드 프레임**뿐이고 **이번 턴이 걷어내는 바로 그것**이라 **지어내지 않고 단색으로** 뒀습니다. 드론 배경은 **지우지 않고 안 씁니다.** 실카메라 오는 날(10-05) 바꿉니다 |
| **⑥** | **감사 로그에 보존 기간이 없습니다** | 무한 적재. 턴 X 에 F 가 「인덱스는 읽기만 빠르게 할 뿐 **표가 자라는 건 안 막는다**」고 적어 둔 자리가 **세 턴 뒤 빨강**으로 돌아왔고, 그 사이 대장은 **`구현`·`closed`** 로 적고 있었습니다. **`gate:` 가 없어 한 번도 대조되지 않았습니다** |

**09-23 12:00 창 2a** — C0 에서 「계속/여기까지」 한 마디. 절차서는 섰습니다.

---

## §8 세종 판정 청함 — 넷

1. **CR 8칸 표가 없습니다.** 그리고 **「8칸 중 2.2」는 그 셈법으로 나올 수 없는 수**입니다.
   `| CR1 | 가격 숫자 4 | 0 | 근거 |` **여덟 줄**만 적으시면 그때부터 산출기가 냅니다.
2. **역할 코드 다섯** — 지시서가 준 사전 넷 중 **`view_only` · `sysop` 이 이 저장소에 없습니다**
   (`role.Role` 16종 실측). U56 이 **추측으로 안 이었고** 지금 규칙 표 2줄이 「표시명 없음」입니다.
3. **표 밖 9역할의 영어 줄** — `RoleMenu` 에 사람이 안 정해진 역할 9종이 실재하고
   여전히 영어 줄이 많습니다. 역할 판정입니다.
4. **영역 ① 의 `kind`** — 도달 17 인데 `kind` 는 10 입니다. **「누른 뒤」가 방금 초록이 됐으니**
   다음 턴에 `closed` 로 올릴 근거가 생겼습니다. **이번 턴에 미리 안 올렸습니다.**

---

## §9 다음 턴으로 넘기는 것

**한 차선이 둘 다 쥐어야 닫히는 것**
- **임계값 「지금 값」이 안 바뀐다** — `list_thresholds` 가 **전역 층 덮어쓰기만** 읽습니다
  (`k5_trust/services.py:56`). **쓰기는 정상.** 화면이 **제 사용자에게 유효하지 않은 값**을 보여 줍니다
- **「사람에게 안 감」 계열 — 세 파일 · 일곱 자리.** 정본은 `deliveryOutcome.tsx:74` 하나
- **탐침 카메라 #3947 표시 이름** — 고객 눈에 읽히는 마지막 시험 장치. 게이트 자산이라 조심
- **별지 1호 훈련 배너** — 그 서식이 **저장소에 없습니다**

**재야 할 것**
- **48행 「누른 뒤」 전량** — 오늘 게이트가 `0/48 · 회색 48` 을 냈습니다. **V 의 창**
- **CR 8칸** · **재부팅 아침 N/10** · **스테이징** — PR 의 30점 폭이 그것입니다
- **설정 캐시 동시 10** — 눈금을 창 짝 20개에 맞춰 다시

**고칠 것**
- `OPS-07`(보존 기간) — 이제 **`미착수` + `gate:` 부착**이라 **고쳐지면 초록이 되고
  그때 미착수로 남아 있으면 exit 1** 입니다. **조용할 수 없는 자리**가 됐습니다
- `MEASURED` 남은 회색 **28** — **턴 AC 부터 빨강**(P-217). ★ 그 기한을 **문서가 아니라
  `decisions.yaml` + `enforced_by`** 로 세워야 합니다(「결정문은 배선이다」)
- **안 쓰이는 인덱스 28개 · 74 MB** — 계수기 창을 늘려 다시 본 뒤

---

## §10 불변 — 이 턴에 새로 배운 것

기존 전부 유지 + 이 턴이 실제로 겪어서 아는 것:

- ★ **「지시서도 입력이지 측정이 아니다」** — 한 턴에 두 번 값을 했다(U3 · U1)
- ★ **한 차선이 화면과 술어를 둘 다 쥔다** — 갈라 놓으면 **화면이 서도 아무도 찍으러 안 간다**
- ★ **도달과 「누른 뒤」는 다른 수다** — 뭉쳐 읽지 않는다. 캡처가 200 을 내도 저장은 죽어 있을 수 있다
- ★ **타입검사는 종료 코드가 아니라 총량으로 가른다** — 심링크 누락 15374 · 중첩 6474,
  **종료 코드는 셋 다 2**
- ★ **`docker cp <디렉터리> <컨테이너>:<있는 디렉터리>` 는 안에 넣는다** — 덮으려면 먼저 지운다
- ★ **게이트의 마지막 줄은 관측이지 처방이 아니다** — 「셋 다 없다」를 「셋 중 하나를 하라」로 읽지 않는다
- ★ **모듈의 실재는 분모가 아니다**(N) · **`closed` 는 「면제 없음」이다**(N)
- ★ **낱말이 아니라 AST 로 훑는다** — 낱말을 세면 다음 사람이 **주석을 고쳐** 끈다(S)
- ★ **못 잰 칸은 0 이 아니라 회색이다** — 하한과 상한을 함께 낸다(Q)
- ★ **빚은 잠그되 면제가 아니라고 적는다**(Q) · **거짓 분모를 안 적으면 도구가 죽어서 알려 준다**(Q)
- ★ **쪽지는 제 파일을 갖는다** — `<받는차선>.inbox/<보낸차선>.md`. **덮을 파일이 구조적으로 없다**
- ★ **창은 조율자가 시각으로 나눈다 · 서버 재기동은 조율자 허락 없이 금지**
  ⇒ [실측] U3 로그인 **턴 Z 7회 → 1회** · F 가 **감사 표를 읽고 남을 피해 계정을 옮겨 탔다** ·
  **턴 Z 에 세 번 끊겼던 자리가 이번엔 0회**
