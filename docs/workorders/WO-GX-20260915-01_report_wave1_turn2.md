# WO-GX-20260915-01 보고 — 파 1 · 턴 2 (턴 R)

- 기계 시각 **2026-09-16 13:33 KST** · 소요 약 **3시간 40분** · 조율자 영실(Code · Opus) · 차선 7(F · U1 · U3 · U24 · U56 · E · Q)
- 커밋(`turn-q`): `2492b96` feat(백엔드 · 15파일 +1,570) · `3e71a4b` feat(화면 · 17파일 +1,579) · `701a990` test/docs(48파일) · 태그 `pre-turn-r`=`5a403ef` · **master 앞감기 `5a403ef` → `701a990`**(`merge-base --is-ancestor` 로 순수 앞감기임을 먼저 확인 · 되감기 아님)
- 단위 시험 **1,387 passed · 0 failed · 4 skipped**(전수 · 14분 22초 · 병합 뒤 깨끗한 판). 앞서 보인 1 failed 는 **내가 커널 시그니처를 고치는 도중에 돌아 코드가 섞인 실행**이었고, 그 한 건은 따로 3 passed 로 확인했다.
- **막는 게이트: 없음** — GA 의 FAIL 0(직전 판에서 빨갛던 SEC-05 · QA-03 둘 다 닫혔다 · 대장↔게이트 색이 같다 **25**, 직전 23). 병합에서 막힌 것 **열하나**는 아래 「막혔던 것」에 전부 적었다(`--no-verify` 0).
- ⚠ 그러나 **`verify_ga_readiness` 자체는 회색(exit 2)** 이다 — PERF-04 한 절을 못 쟀다(성능 예산 기준선 없음). **회색은 초록이 아니다**(D-301). 「막는 게이트 없음」과 「전부 쟀다」는 다른 말이고, 이 턴은 앞엣것만 참이다.

## 첫 표 8줄 (산출기 출력 · 없으면 회색)

| 수 | 지금 [실측] | 직전 | 출처 |
| --- | --- | --- | --- |
| 상용 /100 | **65.3** (변화 0) | 65.3 | `verify_ga_readiness` |
| 손 안 도달율 | **66.2%** (88/133 · 변화 0) | 66.2 (88/133) | `verify_ga_readiness` |
| 온보딩 48행 | **회색** — 깨끗한 수를 못 냈다(아래 Q 차선) | 회색 | `onboarding_48.md` |
| FC 하한 | **47.9** (23/48 · 단독 세션 03:36Z) | 50.0 (24/48 · 차선 경합 중) | `verify_click_completes --measure` |
| 영역 ① 기능 완결성 | **7/39** · 회색 25 · 빨강 0 (변화 0) | 7/39 · 회색 25 | `verify_feature_reach` — **한때 3/39 로 보였다**(아래 §영역 ①) |
| PR(15관문) | **50.0%** (7.5/15) | 회색(산출기 없음) | `verify_readiness_scores` |
| CR(8조건) | **12.5%** (1.0/8) · 문서 §12 와 일치 | 회색(산출기 없음) | `verify_readiness_scores` |
| 커밋 | turn-q +3 · master `701a990` · **origin 0 — 못 밀었다** | turn-q 4 · 원격 0 | git |

**G1~G5**
- **G1 ○** 공개 URL 0 · 새 자격 넷 금고에 준비 · **가드 코드가 섰다**(격리 `gx_e` 에서 `verify_prod_settings` **7/7** · 나쁜 값 4종 전부 **기동 실패** — 경고만 찍고 뜨는 자리 0)
- **G2 ●** 익명 반출 0 · 역할 홈 유지 · `no_secret_echo` 초록 · **증거가 나르던 자격 16건도 가렸다**(SEC-05)
- **G3 ◐** 여정 **23/48**(빨강 7 · 회색 18) — 빨강 7 중 **제품 결함 0**(3종 분류는 아래)
- **G4 ◐** 상용 **65.3**(변화 0) · 손 안 **66.2%**(88/133)
- **G5 ◐** 저장 상한 50GB · Docker 자동 시작 켬 · 502 격리 **8·8**(0 미달 · 운영 모양 아님 → OPS-13b **안 켬**) · **재부팅 후 미실측**

## P-90 — 승인은 왔고, 주소는 오지 않았다 (D-467)

- 대표가 **집행자 세션에 직접** 적었다(§4 ③ 문구와 글자까지 일치). P-144 의 집행 조건 ①은 **성립했다.**
- 그럼에도 **보내지 않았다 — 운영 주소가 없다.** 실측: `.env.gates` 의 `GX_API` 는 `http://localhost` · `decisions.yaml` 에 P-90 주소 0건 · 저장소의 유일한 실재 외부 `.kr` 은 `backend/third_api/common/utils.py:54-66` 의 **배송(ETRI) 제3자 연계** 주소다.
- 근거는 우리가 이미 세운 원칙이다(`evidence/P-90/commands.md` §4): **승인에는 주소가 붙어야 한다. 대상 없는 승인을 받은 쪽이 대상을 추정하면, 그 순간 그것은 승인받지 않은 행위다.**
- 정본 충돌 하나: `evidence/P-90` 에 사양이 둘이다(`probe_spec.md` 익명 GET 4 ↔ `commands.md` HEAD/OPTIONS 4). 대표 문구가 「익명 GET 4」이므로 **`probe_spec.md` 가 정본**이다.
- **필요한 것은 한 줄**: `https://…` (같이 주시면 좋은 것: 허용 IP · 보내도 되는 시간대)

## P-153 push — 전제는 다 섰고, 집행만 못 했다

- gitleaks **129커밋 전수 · 유출 0**(`evidence/P-153/gitleaks_history_20260916.md`). P-135 회전 뒤에 쟀으므로 옛 값이 이력에 있었다면 여기서 나왔어야 한다.
- 원격 등록(`origin`) · `ls-remote` **exit 0 · 0줄 · 표준오류 없음** → 실재하고 닿으며 **브랜치 0(빈 저장소)**. 덮어쓸 것이 없다.
- **`git push` 가 승인 분류기에 세 번 막혔다**(원격 등록·도달 시험까지 넣으면 네 번). 우회하지 않았다.
- ⚠ 커밋 셋은 `master`(`701a990`)에 **이미 들어가 있다 — 로컬에서 잃은 것은 없다.** push 는 그것을 바깥에 복제하는 일이고, 늦어져도 잃는 것은 없다.
- 대표께: `git push` Bash 권한을 열어 주시거나, 직접 `git push origin master turn-p turn-q && git push origin --tags` 를 실행해 주시면 닫힌다.

## 조율자 20분

- 낡은 차선 시험 DB 8개 정리 · 역할 계정 **로그인 10/10 · 역할 4/4**(속도 제한 403 한 번은 제품이 옳게 막은 것 — 61초 뒤 통과)
- 쓰기 면 17 중 **넷이 태어났다**: WS-01 `onboarding/progress` · WS-03 `handover` · WS-04 `events/{id}/note` · WS-13 `people`
- ⚠ **표에 없던 쓰기 면이 하나 태어났다** — `POST /settings/webhook-subscriptions/issue`(P-145 판정이 이번 턴에 섰다). **선등록이 실패한 자리도 기록이다.** 다음 파의 선등록은 「세종 판정이 그 턴에 새로 서는 면」 자리를 함께 잡는다.
- WS-14 `notify-rules` 는 태어나지 않았다(화면 골격만 · 서버 문 없음).

## 차선 7 — 닫힘 / 넘김

| 차선 | 닫은 것 | 넘긴 것 |
| --- | --- | --- |
| F | `/dsm/home` 띠 3장 · **띠 수 = 재조회 수**(분모 22 · equal True ×3) · UX-46 진행률(익명 401 · 시험 11 passed) | 브라우저 실측 회색 · P-140 은 **고칠 줄이 없었다**(이미 UX-32) |
| U1 | 인계 초안 **4줄 자동**(사건 #4819 반영) · 메모 왕복(감사 기록 버그 발견·수정) · 9/9 · **U1#3 초록** | U1#19 는 판정기 결함(아래) |
| U3 | **U3#3 초록** — img 1 · naturalWidth 640 · 200 image/jpeg · M3 390 캡처 | 웹푸시 골격까지(발송 문 없음) |
| U24 | 검색 **11.69초**(12.7 유지) · 재판정 **영속 확인** · TeamStatus 골격 | U2#9 는 판정기에 정본이 없어 회색 |
| U56 | 서명키 1회 노출·해시 저장 · `admin` 역할 매핑(K3) · 발급 200 · 폐기 200 · `people.py` 배선 | `rotate_signing_key` 는 **부르는 자리가 0곳**(문 없는 함수 · 다음 턴) |
| E | `verify_prod_settings` **격리 7/7** · 나쁜 값 4종 전부 **기동 실패** | 502 는 10·11 이 아니라 **8·8** 이고 **운영 모양이 아니다** → OPS-13b **켜지 않음** · 재부팅 후 미실측 |
| Q | `feature_reach` 가 row_map 을 **읽게 됨** · PR·CR 첫 수 · 빨강 3종 | **12 안 나왔다 — 기준을 무르게 하지 않았다** · 온보딩 깨끗한 수 못 냄 |

## 여정 — 빨강 7의 3종 (D-470 · D-471)

**23/48** — 초록 23 · 빨강 7 · 회색 18 [실측 2026-09-16T03:36Z · 단독 세션].
사람별 U1 5/8 · U2 3/8 · U3 5/8 · U4 2/8 · U5 3/8 · U6 5/8.

**내려간 것을 내려간 대로 적는다** — 턴 Q 50.0(24/48) → **47.9**(23/48). 다만 두 수는 같은
조건이 아니다: 턴 Q 의 24 는 **차선 일곱이 같은 역할 계정으로 동시에 누르는 중**에 잰 값이고
(그 경합이 값을 20↔22 로 흔드는 것을 Q 차선이 실측했다), 이번 23 은 병합 뒤 **단독 세션**이다.
총합은 하나 줄었지만 안은 둘이 바뀌었다:

- **U6#1 → 초록**(U6 4/8 → 5/8). 기대식 정정이 먹혔다. 제품은 **이미 고쳐져 있었다**
  (`POST /settings/api-keys` → 200 · `is_tenant_admin(gxseed_u5_sysop)` → True) —
  판정기가 그 사실을 못 보고 있었을 뿐이다.
- **U1#11 → 빨강**(U1 6/8 → 5/8). 「다시 읽었는데 `verdict` 가 그대로다(`confirmed`)」.
  제품 결함이 아니다 — **측정이 자기가 재는 데이터를 바꿔 놓는다.**

### 3종 분류

| 종 | 행 | 근거 |
| --- | --- | --- |
| **기대식 오류 3** | U1#19 · U2#1 · U2#2 | `verify_click_completes.py:473` 이 **참값 0·빈 목록**과 「칸이 없다」를 한 묶음으로 읽는다. 차선 둘(U1 · Q)이 서로 모르게 같은 자리를 짚었고 U1 은 curl 로 `200 {"unhandled": 0}` 까지 봤다 |
| **데이터 상태 3** | U1#11 · U3#1 · U6#9 | 셋 다 **제품이 옳게 동작한 결과**다 — 앞선 실행이 이미 판정했다 / 5분 중복 억제(F-04 설계대로) / U24 차선이 재판정하며 종결로 바꿔 두었다 |
| **데이터 없음 1** | U6#4 | 서명키 값이 그 환경에 없어 제품이 **옳게 422** |
| **제품 결함** | **0** | |

- **고쳤다 — U6#1(D-470).** 판정기가 **그 응답에 한 번도 없었던 칸**(`total`)을 재조회하고
  있었다. `setting_overview(domain="api_keys")` 가 내는 것은 outbound · inbound ·
  inbound_api_type · inbound_capability · api_keys 다섯이다. 없는 칸은 앞뒤가 늘 None 이라
  「그대로다」가 되고, 그래서 **제품이 고쳐진 뒤에도 빨강**이었다. **제품 쪽을 먼저 실측하고**
  고쳤다 — 순서를 바꾸면 빨강을 거짓 초록으로 바꾸게 된다.
- **고치지 않았다 — U1#19 · U2#1 · U2#2.** 원인을 알면서 안 고쳤다. 둘을 가르려면 관측기가
  「그 칸이 있었는가」를 기록해야 하고 지금 증거에 그 자리가 없다. **관측 없이 판정만 고치면
  빨강이 거짓 초록이 된다**(턴 Q 가 U3#3 에서 거부한 그 일). 다음 턴에 `had_field` 를 더한다.
- ★ **D-471 — 빨강 셋의 뿌리가 하나였다:** 여정 판정기는 **「한 번만 참인 상태」**를 재고 있어,
  제품이 옳을수록 **연속 실행에서 스스로 빨개진다.** 가르는 법: 증거의 `calls` 에서 **요청의
  상태코드**를 먼저 본다 — 200 인데 `before == after` 면 제품이 아니라 데이터 상태다.
  고치는 길(다음 턴): 쓰기 흐름은 매 실행마다 **새 사건을 시드**한다. ⚠ 되돌림은 감사
  기록이라 함부로 하지 않는다 — U24 가 4798 의 판정을 되돌리지 않은 것도 같은 이유였고,
  그 판단은 옳았다.

## 영역 ① 은 내려가지 않았다 — **내 보고가 두 번 틀렸다**

처음에 나는 이렇게 적었다: 「Q 차선이 `feature_reach` 를 조여서 내려갔다 — 엄해진 자가 낸 수다.」
다음엔 이렇게 적었다: 「기준 탓인지 입력 탓인지 못 가린다.」 **둘 다 틀렸다.**

같은 턴에 `capture_screens.py` 가 **그 게이트의 입력 대장을 잘라 놓았다**:
`D-386/screen_routes.json` 이 screens 27 → 2 · viewers 32 → 2 로 덮였고(D-473),
그 파일은 `verify_feature_reach.py:73` 이 읽는 바로 그 자리다(`verify_envelope` ·
`verify_route_alive` 도 같은 파일을 읽는다). 되살린 뒤 다시 쟀다:

| | 잘린 대장으로 잰 값 | 대장을 되살린 뒤 [참값] |
| --- | --- | --- |
| 영역 ① | 3/39 · 회색 29 | **7/39 · 회색 25** |
| 상용 /100 | 63.2 | **65.3** |
| 손 안 도달율 | 63.2% (84/133) | **66.2% (88/133)** |

셋 다 턴 Q 와 **같은 수**로 돌아왔다. 즉 내가 보고하려던 「−2.1 하락」은 **존재하지 않는
하락**이었고, 덮인 대장이 만든 그림자였다.

★ 여기서 값진 것은 수가 아니라 **순서**다. 대장 복원을 하지 않고 커밋했다면 이 보고서는
「상용 63.2 · 손 안 84절 · 영역 ① 3/39」로 나갔을 것이고, 그 수는 **그럴듯한 설명까지 달고**
나갔을 것이다. 게이트가 멈춰 세운 것은 커밋이지만, 실제로 막은 것은 **거짓 보고**다.

다만 이것은 그대로다 — Q 차선은 목표 12 를 **못 냈고, 기준을 무르게 해서 맞추지 않았다.**
그 판단은 여전히 이 턴에서 가장 값지다. 그리고 이제 분명해졌다: `row_map` 을 입력으로
넣어도 수는 7/39 그대로다 — **벽은 판정기가 아니라 캡처였다**(Q 차선의 진단대로).

## 막혔던 것 — 열하나 (전부 원인 제거 · `--no-verify` 0)

1. **진입면 대장 +4** — 차선 셋이 각자 「내 것은 등재했고 남은 빨강은 내 결함이 아니다」로
   보고했고 **셋 다 옳았다.** 두 차선에 걸친 자리는 조율자가 병합에서 맞춘다(그 파일이 이미
   적어 둔 규약). → 12 passed
2. **`dead_fields` 등재 6줄 만료** — D-463 이 「U1·F 차선이 파 1 턴 2 에 쓴다」로 적은
   이름표의 만료일이 왔다. 게이트가 먼저 알아냈다
3. **첫 커밋이 그 수리를 안고도 막혔다** — pre-commit 은 **스테이징 안 된 파일을 치워 두고**
   돈다. 등재를 지운 판정기를 ③ 몫으로 남겨 뒀더니 **옛 판정기로 새 코드**를 쟀다.
   **선언과 배선은 같은 커밋에 있어야 한다**
4. **`gx-tool-selftest`** — F 차선의 새 판정기에 출생 표본이 없었다(미추적 파일도 게이트는
   본다). 이 도구를 태어나게 한 두 유혹(분모를 줄인다 · 사람이 눌러 닫는 문을 낸다)을
   fixture 로 박았다
5. **`verify_tenant_scope` 위반 6**(D-469) — U56 의 새 커널 함수 셋에 `*, scope: TenantScope`
   가 없었다. 형제 파일 관례를 그대로 따랐다(1차는 시그니처가 막고 2차는 `_group_of` 가
   막는다). ★ `signing_key_name_for` 는 **숫자(group_id)를 받지 않는다** — 숫자를 받으면
   「어느 테넌트의 이름인가」를 커널이 아니라 호출자가 정하게 된다
6. **SEC-05 ①** — E 차선이 「접근키 == 비밀키」를 재려고 박은 29자 가짜 값을 스캐너가 잡았다.
   **스캐너는 뜻이 아니라 모양을 본다.** 허용 목록으로 무르게 하는 대신 엔트로피를 뺐다
7. **QA-03** — **내가 쓴 D-467** 의 `enforced_by` 에 실행 가능한 것이 없었다. 맞는 지적이다
8. **`cache_bypass`** — F 차선 시험이 익명 401 을 묻느라 스택을 타는데 선언이 없었다
9. **SEC-05 ② — 증거가 자격을 날랐다**(D-472). 내가 U6#1 을 고쳐 다시 재게 만든
   `evidence/P-118/click_completes.json` 을 스캐너가 잡았다. U6#1 은 **API 키를 발급하는
   흐름**이라 내 정정이 만든 유출인지부터 확인했다 — **아니었다**(지도 SDK 의 공개
   클라이언트 키 16건 · 우리 제품 키 0건). 그래도 넘기지 않았다: 이 파일은 우리가 **커밋하는**
   파일이라, 저장소가 그 값을 나르면 **다음에 진짜로 새는 값과 구별되지 않는다.**
   ⚠ **가리는 자리가 판단의 전부였다** — 요청을 잡는 `on_req` 에서 가리면 `on_resp` 가
   `c["url"] == r.url` 로 짝을 못 찾아 **모든 상태코드가 None** 이 되고 여정 48행이 무너진다.
   **저장소에 적는 순간** 한 곳에서만 가렸고, 가린 뒤 집계 **23/48 불변**으로 확인했다
10. **캡처 대장이 덮였다 — 32항목이 2항목**(D-473). `D-347/screens/INDEX.yaml` 과 짝인
   `run_log.json`(1,513B → 232B). 디스크의 PNG 30장은 남아 전부 「출처 없는 캡처」가 됐다.
   ★ **게이트가 먼저 알아냈다** — 아무도 지웠다고 말하지 않았고, **지워진 것은 지워진 것이
   보이지 않는다.** 처방은 **HEAD + 이번 턴 갱신분**. 다시 찍힌 둘만 이번 턴 판을 얹었다 —
   PNG 가 09-16 것인데 09-07 시각을 달면 그것이야말로 그 파일이 막으려는 거짓말이다(D-284)
11. **네 번째 대장도 덮여 있었다 — 그리고 그건 게이트 셋의 입력이었다**(D-473).
   D-473 에 「이 파일을 건드리는 실행 뒤에는 **항목 수를 센다**」고 적어 놓고 적은 대로
   증거 JSON 열셋을 HEAD 와 대조했더니 하나가 걸렸다 — `D-386/screen_routes.json`
   **52,260B → 5,242B(10%)**. 복원 뒤 `verify_envelope` 재측: **통과 · 고유 호출 46건 대조 ·
   봉투 갈림 0**(잘려 있을 때의 초록은 사실상 **아무것도 안 잰 초록**이었다).
   ★★ **복원하다 내가 같은 잘못을 작게 저질렀다** — `viewers` 를 라우트로 묶어 32 → 28 이
   됐다(같은 화면을 **다른 역할 계정이 본 기록** 4건 · P-98 이 33장을 걸러낸 그 칸이다).
   생성기를 나무라던 내가 같은 모양으로 지웠고, **세어 보고 되찾았다**(기록 전체를 열쇠로
   재병합 → screens 28 · viewers 33 · 「HEAD 보다 줄면 멈춘다」 단언을 코드에 걸었다)

★ 커밋 ③은 네 번 막혔다. 마지막 한 번은 게이트가 아니라 **살아 있는 감시 기록**이었다 —
`ops_tasks.py` 가 5분마다 다시 쓰는 `D-373/monitor_last.json` 이 훅이 도는 중에 바뀌자
pre-commit 이 그것을 「훅이 고쳤다」로 오인했다. 그 파일을 커밋에서 빼고(그 순간의 우연을
턴 증거로 박지 않는다) beat·celery 를 잠시 멈춘 뒤 통과시켰다(둘 다 곧바로 다시 켰다).

★★ 이 턴에 **대장 덮어쓰기가 네 번**이다 — 턴 Q 인구조사 · INDEX · run_log · screen_routes.
그리고 `--freeze` 가 사람의 문단을 지우는 자리(D-337)까지 같은 계열이다.
**생성기는 자기가 만든 것만 안다.** 사람이 쌓은 것과 기계가 낸 것이 한 파일에 있으면,
**기계에게 그 파일을 통째로 주지 않는다.**

## 가정 목록

1. 차선 시험 DB 이름은 지시서의 `gx_test_<lane>` 이 아니라 **실측으로 검증된
   `test_gx_<lane>`** 을 썼다(conftest 가 그 이름을 읽는다)
2. 커밋 셋의 가름: 백엔드 / 화면 / test·docs. **등재를 지운 판정기 한 벌은 배선과 같은
   커밋**에 넣었다(위 3)
3. `rotate_signing_key` 를 **지우지 않았다** — 삭제는 대표 결정이다. 문 없는 함수로 남기고
   보고에 적는다
4. 사건 4798 의 판정(실제→오탐)을 **되돌리지 않았다** — 판정은 감사 기록이다
5. U6#1 의 기대식만 고치고 나머지 셋은 안 고쳤다(관측이 없다)
6. `D-373/monitor_last.json` 은 커밋에서 뺐다(5분마다 바뀌는 살아 있는 값)

## 세종께 — 셋

1. **PRD v2.6 §3 이 자기 자신과 갈렸다** — 표 {해결 7 · 반 3 · 못함 8} ≠ 요약 {해결 7 · 반 5 ·
   못함 6}. 세종 합 9.5 가 어느 쪽으로 센 수인지 정하고 **PRD 를 고쳐야 한다**(산출기가 맞출
   자리가 아니다). 이것 때문에 `verify_readiness_scores` 는 수를 내고도 exit 1 이다
2. **RC-1 문턱이 두 문서에서 갈린다** — 지시서 §0 PR≥85 · CR≥87.5 ↔ `GX-FCPR §3` PR≥80 ·
   `GX-REVIEW §12` CR≥75
3. P-140 「UX-32-U4」는 이미 매핑에 `UX-32` 로 적혀 있었다 — **고칠 줄이 없었다**

## 영실 오판 3

1. **차선에 이름을 가를 때 역할 계정을 안 갈랐다.** dj-core 는 동시 접속 1개라 병렬 차선이
   서로의 브라우저 측정을 튕겨 냈고, **증상이 빨강이 아니라 회색**이라 조용했다. Q 의 온보딩
   재측은 회색 35 중 24가 이 경합이었다 — **쓰면 안 되는 수**다. 턴 Q 의 시험 DB 오판과 같은
   계열. 규약: 누르는 측정은 차선에 맡기지 않고 병합 뒤 **단독 세션**으로 한 번만
2. **영역 ① 의 하락을 두 번 잘못 설명했다** — 「기준이 엄해져서」 → 「못 가린다」 → 실제로는
   **내려간 적이 없었다.** 수가 움직이면 **판정기보다 그 입력을 먼저 의심해야 한다**
3. **grep 필터가 답을 네 번 삼켰다.** `^\s+` 로 거르다 `is_tenant_admin` 판정 줄을, `tail` 로
   자르다 집계 머리줄과 실패한 훅 이름을, 파이프 뒤 `$?` 를 읽다 git 의 종료 코드를 놓쳤다.
   네 번 다 **내가 만든 눈가림**이었다

## 넘긴 것

P-90(**주소 대기**) · push(**집행 권한 대기** — 전제는 다 섰다) · `verify_route_alive`(복원한
대장으로 재측 · 서버·자격 필요) · 판정기 `had_field` 관측 → U1#19·U2#1·U2#2 재측 ·
쓰기 흐름 **매 실행 새 사건 시드**(D-471) · `capture_screens.py` 누적 고치기(D-473) ·
`rotate_signing_key` 의 문 · OPS-13b(운영 모양 재측 뒤) · 재부팅 전/후 실측 ·
온보딩 48행 깨끗한 수 · U2#9 정본 · UX-43 알림 규칙 서버 문 · 영역 ① 캡처 · PERF-04 기준선
