# WO-GX-20260924-13 보고 — 턴 AI · 「길목은 섰고 화면은 아직 옛것이다」

| 항목 | 내용 |
|---|---|
| 보고 | 영실(CPM · 조율자) → 세종 · 참조 대표 |
| 착수 · 끝 | 2026-09-24 22:06 → 23:5x KST(기계) · 약 1시간 50분 · **대표 대화형 지시 「RESUME_NEXT 읽고 시작」** — 감시기(헤드리스)가 아니라 대화형 영실이 열었다(LOOP_STATE `code_running` · updated_by yeongsil) |
| 차선 | **넷**(S · Q · F · U온 · sonnet) — 한도 전 5 에서 하나 뺐다(턴 AC 교훈). E 의 P-321·P-315 는 착수 전 `0115ad4` 로 섰고 창 2b 준비 표는 조율자. U24 · U56 · A 는 8차선판이라 **이 턴에 없다** |
| 커밋 | 8(`32e6ca5` … `0a2ab7d`) · 태그 `pre-turn-ai` = `0115ad4` · **push 대기 7**(`32e6ca5` 는 대표 지시로 push 됨) |

## §0 첫 표

| 수 | 지금 | 직전 | 뜻 |
|---|---|---|---|
| **상용 /100** | **58.1** [GA 판정기 · 23:4x] | 58.1 | 닫힌 절 76/157 · 이 턴은 절을 닫는 턴이 아니었다 · GA 회색 3절(SEC-17 `--api` 없음 · PERF-04 · LAW-08 `--db` 없음 — P-304 인자 실행은 **이 턴에 못 했다**) · ★ OPS-13a 는 **재서 빨강**(`verify_front_line_502` exit 1)인데 대장엔 아직 unmeasurable |
| **기준선 빚** | 제품 언어 **0** · 게이트 자기시험 짝 **82**(새 칸 · 늘 출력) | 0 · — | P-322 ⓐ · 숨긴 빚은 거짓 초록 |
| **온보딩 48(정본 8500)** | **30.5/48 = 63.5 % · 장부 기재 1** | 30.5(장부 미기재) | P-332 — RC-1 S1 이 이제 30.5 를 읽는다(빨강 · 옳다) · **재측 0**(아래 §5) |
| **「누른 뒤」** | **못 잼**(판정기 회색 48/48) | 32(참고) | V 화면 측정은 배포 뒤로 미뤘다(§5) |
| **카드 걷기(P-311)** | **도구 1 · 첫 수 후보 5/6** — U1 3/3 · U2 1/3 · U3 3/3 · U4 1/1 · U5 3/6 · U6 회색 | 회색 | 분모 = 서버 기록이 닫는 카드(38 중 20) · U6 계정 로그인 400 |
| **비번 찾기** | 길목 **3/3** · `frontendUrl` **8500** · 게이트 PASS · 발송 0통(SMTP 는 `smtp.invalid`) | 6/6 · 거짓 「보냈습니다」 | 길목이 켜지니 화면은 **무반응**(§3) |
| **살아 있는 서버** | `restart` 도구 · `restarts.jsonl` 2줄(초록 1 · **빨강 1 = 재는 서버 옛 코드 → 다시 띄움**) · 스모크 셋 200 | 없음 | P-321 · P-315 · P-338 |
| **전량** | 1회차 **2,244 통과 · 4 실패** → 고침 → 2회차 ****2,248 통과 · 0 실패**(18분 45초)** | 2,212/0 | 4 실패 = 새 길목이 앞 턴 걷기 시험을 막음(§4) |
| **S1~S7(RC-1 판정기)** | ● 0 · ◐ 3(S2·S3·S4 회색) · ○ 4(S1 63.5 · S5 0 · S6 0.5 · S7 0.5) | 같음 | `evidence/RC1/turn_ai.json` · FC 63.5[1/3] · PR 52.9[1/4] · CR 31.2[8/8] |
| **아침** | 2/10 · 손 재부팅 유지(대표 09-24) | 2/10 | 이 턴 재부팅 0 |
| **바깥 사용자** | 0 %(스테이징 안 옴 · 열세 턴째) | 0 | |

## §1 미커밋 0 · push 대기 표
미커밋 **0**(보고서 커밋 뒤). push 대기 **7 + 이 보고서 1**:
`0115ad4` P-321·P-315 · `3a54a40` 착수 묶음 · `e28097e` S · `5b2429a` Q · `2f6621d` F · `14cc540` U온 · `0a2ab7d` 창 ③ · (보고서).
대표 손: `cd C:\GuardianX\guardianx-source` → `git log --oneline origin/turn-q..turn-q` → `git push origin turn-q`.

## §2 restart · verify_live_code · 스모크 (P-321 · P-315 · P-338)
- `scripts/restart_live.py --reason …` — 두 통만(이름 박힘) → 건강·celery ready → `verify_live_code` → `smoke_live` → `restarts.jsonl`.
- 21:36 첫 실행 초록. **23:11 병합 뒤 빨강** — gunicorn 은 새 코드(마스터 23:11)인데 **재는 서버**(gx-shell `--noreload` runserver · 20:19 기동)가 옛 코드. 도구가 턴 AH ㉛ 을 그 자리에서 잡았다. 같은 명령으로 다시 띄움(프로세스 환경 == 컨테이너 환경) → 두 서버 신선 · 8000 200.
- 스모크는 **커밋 훅이 아니라 재시작 뒤**(P-338 인정). 계정은 `GX_SMOKE_USER` 짝이 있으면 그것, 없으면 `GX_ROUTE_USER`(역할 계정 U4)로 **물러선다** — P-339 `gx-smoke` 는 `.env.gates` 값이라 대표.
- ⚠ 도구의 빨강 문구 「재시작했는데도 옛 코드」는 재는 서버가 옛것일 때도 같다 — 어느 서버인지는 위 `[LIVE]` 줄이 말한다. 다음 턴에 문구를 가른다.

## §3 비밀번호 찾기 — 길목 3/3 · frontendUrl · 그리고 화면
- **P-325** `backend/common/reset_mail_gate.py` — 경로만 보고 본문(주소)은 안 읽는다 → SMTP TCP 1회(2초 · 60초 캐시) → 죽었으면 모두에게 같은 503. 먼저 실패해 봤다: 순서를 뒤집으면 2/3 빨강.
- ★ 조율자 병합에서 문구를 고쳤다: 「관리자에게 **알렸습니다**」 → 「**문의하십시오**」 — K2 에 인프라 장애를 알릴 자리가 없어(장치 이벤트 전제) 로그만 남긴다. 거짓말 하나를 고치며 다른 거짓말을 만들지 않는다.
- **P-326** `frontendUrl` 칸 없음(→ 3001) → `http://localhost:8500` · 게이트 `verify_password_reset` PASS(링크 host == 칸) · 자기시험 16/16 · 짝 시험 3(조율자).
- ★★ **새 빈틈 — 화면이 실패를 안 보인다.** 데스크톱 `/forgot-password` 는 rj-core `ForgotPasswordPage`(§0.4), 모바일 `ForgotPasswordMobile` 도 실패를 `console.log` 만 한다. 지금 SMTP 가 `smtp.invalid` 라 길목이 늘 닫혀 있고, 사람은 **거짓 「보냈습니다」 대신 무반응**을 본다. 09-26 창 2b 에 Mailpit 이 붙으면 길목은 통과로 바뀐다. 다음 턴: `App.tsx` 의 `ForgotPassword` 감싸개(우리 코드)가 받을 자리 — 세종 판정 청구.
- 덤: 탐침 계정 `gxprobe_p326_link_host@test.invalid` 가 개발 DB 에 남았다(실발송 0).

## §4 전량 4 실패 — 새 코드가 옛 시험을 막았다
1회차 2,244/4. 넷 다 `test_s_password_reset_walk`(턴 AH P-290) — 발송 부품만 가짜로 바꿔 「메일이 되는 날」을 재던 시험이 새 길목의 실제 TCP 에 막혔다(503). 시험 준비에 `common.reset_mail_gate.smtp_reachable` → True 를 더했다(발송 부품을 바꾼 것과 같은 뜻 · 죽은 갈래는 test_p325 가 쥔다). 해당 + 이웃 35/0 · 2회차 전량 **2,248 통과 · 0 실패**(18분 45초).
★ 차선 S 는 제 시험만, 조율자의 병합 전 묶음(60/0)도 이 파일을 안 넣었다 — **앞 턴 시험이 새 전제에 걸리는 것은 전량만 잡는다**(㉚ 의 거울).

## §5 V · 온보딩 · 「누른 뒤」 — 화면 배포가 막혀 미뤘다
- 프런트(U온 첫 화면 · F 폰트)를 커밋된 소스로 빌드했다(`gx-fe-build:/app/_bld_u1ab/dist_ai` · 같은 출처 · 새 문구 1 · `localhost:8000` 1건은 옛 웹소켓 하드코딩 — 지금 배포본과 같다).
- **8500(`C:\GuardianX\gx-spa`)에 올리는 것이 권한 분류기에 「운영 배포」로 막혔다.** 우회하지 않았다 — `gx-spa` 무변경(파일 238 · `index-CUuQMWok.js`) 확인.
- 그래서 V 의 화면 측정(온보딩 48 재측 · `click_completes` 48 첫 실측)은 **안 했다** — 옛 화면을 재면 「재는 서버가 옛 코드면 잰 수가 거짓이다」에 그대로 걸린다. 카드 걷기만 API 라 V_LOCK 안에서 쟀다(§0).
- **대표 결정 청구**: 「8500 에 dist_ai 올려라」(되돌리기 = 스크래치 백업 복사 한 줄) → 그 뒤 V 40분.

## §6 차선별 닫힘 · 넘김
| 차선 | 닫힘 | 넘김 |
|---|---|---|
| S | P-325 길목 3/3 · P-326 8500 · 게이트 1 | 화면 무반응(§3) · K2 인프라 경보 자리 · P-287 잔여·`verify_entry_surface` |
| Q | P-332 장부 · S1 읽는 자리(참고 절 제외) · P-329 finalizer(가른 이름만 · kill 된 실행은 못 치운다) | **P-318 배선 반쪽** — 판정 함수·시험 13 은 섰는데 계측기 `result()` 59곳 중 화면 글자를 넘기는 곳 **0** → 수는 아직 안 움직인다 · U5#2 |
| F | 게이트 짝 요구(기준선 빚 82 · 새 게이트 2/2 짝) · 걷기 도구 · 폰트 404(rj-core 가 부르는 자리에 같은 폰트 · sha 0be2399ea925) | 검사기 빨강 셋(HEADER_LIVE `verify_release_candidate` · PIPE_EXIT 옛 파일 4 · MEASURED_LINE D-511 기한) — **HEAD 에선 검사기 자기시험이 날짜 폭탄으로 빨강**이라 판정도 못 했다(F 가 고침) · 커밋 훅 아님 |
| U온 | P-316 「카메라 N대 중 응답 0 — 카메라 연결 확인으로」 + 「카메라 주소 채우기」 → `/dsm/cameras/address` | U1#4 스트림 자리는 `MultiStreamMonitor`(dsm 밖) · 배포 대기 |
| E(조율자) | 창 2b 준비 표 ⓪~⑫ · env-file 바꿈 표 | 집행은 09-26 14:00 대표 창 |

## §7 창 2b 준비 — 실측 셋 (`docs/agent/runbook/창2b_준비표_20260926.md`)
① dj-core SMTP 이름은 **여섯**(`SMTP_TLS` 기본 STARTTLS) — Mailpit EHLO 에 `starttls` 없음 → `SMTP_TLS=0` 을 새로 넣어야 닿는다 ② K2 `EMAIL_*` 세 줄은 `backend/.env` 에만 넣으면 **두 통에 안 닿는다**(금고 env-file 로 섰다 · `EMAIL_HOST` 없음) — env-file 에도 ③ Mailpit 은 빈 자격 login 도 235 — P-328 「빈 값」 그대로.
⚠ 조율자가 ③ 에서 **두 번 추정했고 둘 다 틀렸다**(「username 없으면 건너뛴다」 → 「Mailpit 은 AUTH 가 없어 죽는다」) — 준비 표엔 잰 것만 남겼다.

## §8 막힌 시간 · 오판 · 세종 오류
- 막힘: 배포 권한 1(§5) · 커밋 훅 끝 줄바꿈 2회(재스테이징 · `--no-verify` 0) · `docker cp` MSYS 경로 1(빌드 폴더 src 를 비운 뒤 실패 — 즉시 다시 채움 · 1,019 파일 대조).
- **영실 오판 이 보고 2(자진)**: ㊱ 창 2b SMTP 자격 두 번 추정(§7) ㊲ 병합 전 새 시험 묶음에 앞 턴 걷기 시험을 안 넣어 4 실패를 전량에서야 봤다(§4).
- 세종 오류 이 보고 0(누계 48). P-315 「커밋 훅」은 P-338 로 이미 정정.

## §9 대표께 — 결정 · 손
| # | 무엇 | 한 줄 |
|---|---|---|
| ㉠ | **8500 에 새 화면 올리기**(권한 분류기가 막은 자리) | 「dist_ai 올려라」 → 조율자가 `gx-spa` 백업 → 교체 → V 40분 |
| ㉡ | push 대기 8 | `git push origin turn-q` |
| ㉢ | 스모크 전용 계정 `gx-smoke`(P-339) | `.env.gates` 두 이름 — 대표 값 |
| ㉣ | CEO_INBOX 결정 ①~⑤(시험 DB · portfwd · pk=6610 · 창 2b 내용 · K2 세 줄) | 09-26 13:50 전 |
| ㉤ | 09-26 14:00 대화형 창 · SMTP 금고(있으면) | 준비 표대로 |

기계 시각 2026-09-24 23:56 KST · 소요 약 1시간 50분.
