# WO-GX-20260915-01 · 파 3 턴 1 「끝내기」 — 턴 U 보고

- 수신: 세종(CPO) · 대표 · 작성: 영실(CPM · 조율자) · 기계 시각 **2026-09-18 15:4x**
- 착수 12:23(세션 재개) · 소요 약 3시간 20분 — ⚠ 지시서의 시간표(09:30 착수 · 12:15 보고)와 다르다:
  **어젯밤 세션이 한도로 끊겨** 차선 여섯이 도중에 죽었고, 오늘 12:23 에 이어받아 다시 띄웠다.
  그 사이 아침 09:05 에 기계가 재부팅됐고 **아무도 서버를 켜지 않았다** — 그것이 §1 의 첫 수다.
- 커밋: `1924525`(병합 · 83파일 · +7,955/−412) · 측정 커밋 1(V 산출물·도구 고침)
- 직전(턴 T): 상용 66.3 · FC 28/48 · 영역 ① 7/39 · 온보딩 16.0/48 · 단위 1,537/0/5

## §0 첫 표 — 여덟 줄 [전부 실측]

| 수 | 지금 | 직전 | 뜻 |
|---|---|---|---|
| 상용 /100 | **67.8** | 66.3 | **+1.5** — 영역 ①(가중 20%)이 7 → 10 으로 오른 것 하나가 냈다 |
| 손 안 도달율 | **69.2%**(92/133) | 66.4(89/134) | 구현 +3 · 분모 −1(PERF-04 가 손 밖으로 · P-172) |
| FC 하한 | **28/48** | 28/48 | 목표 34 **미달**. 빨강은 2뿐이고 **회색 18** 이 막았다 — 그 18 중 16 이 「정본이 없다고 적은 자리」이고 **넷은 정본이 낡았다**(§4) |
| 영역 ① | **10/39** ✅ | 7/39 | 목표 ≥10 **달성** · **빨강 0** · 회색 22 · 잠김 7 |
| 온보딩 48행 | **14.5/48**(잰 행 22) | 16.0/48(22) | **내려갔다** — 데이터 탓 둘(§3). ⚠ 문서의 두 칸은 **35행**이 됐는데 **도구의 행 목록은 턴 T 의 22행**이다 |
| 재부팅 실측 | **0/10** | (없음) | 부팅 +198분에 Docker 데몬이 없었다 — G5 ○ |
| 커밋 | 2(병합 · 측정) | 3 | 라우트 대장 749 → **761** · 진입면 95 → **107** |
| 단위 시험 | **1,593 / 1 / 5** → 고친 뒤 전건 초록 | 1,537/0/5 | 빨강 1 = 마이그레이션 미적용(적용 뒤 22 passed) |

**G1~G5**: G1 ○(P-90 여섯 턴째 · 스테이징 기한 지남) · G2 ● · G3 ◐(FC 28/48 · 온보딩 14.5) · G4 ◐(67.8)
· G5 **○** — 재부팅 뒤 사람이 켜기 전까지 제품이 서지 않았다.

## §1 재부팅 아침 실측 — **0/10**. 앞단 순서가 아니라 그 아래층이었다 (D-491)

| 무엇 | 실측 |
|---|---|
| 호스트 부팅 | 2026-09-18 **09:05:28** |
| 잰 시각 | **12:23:49** — 부팅 +198분 |
| Docker 데몬 | **없음**(CLI exit 7) · `Docker Desktop.exe` 프로세스 없음 · `com.docker.service` Stopped/Manual · AutoStart **False** |
| 컨테이너 | **0/10** · 앞단 8500 닿지 않음 |

P-161(앞단 기동 순서)은 이 판으로 재판정되지 않는다 — **데몬이 없으면 앞단도 없다**(회색).
손으로 데몬을 띄우자 **9/10 이 18초 만에 스스로** 돌아왔다(`restart=unless-stopped`). 안 온 하나는
`gx-shell`(`restart=no` · 게이트 자리)이고 `docker start` 로 세웠다 → **10/10 · 앞단 200 · 5/5 FRESH**.
재생성이 아니라 기동이라 컨테이너 안 꾸러미(playwright·pywebpush)와 `/tmp` 는 **살아 있었다**.
아침 절차 4줄을 `docs/agent/evidence/P-161/reboot_20260918.md` 에 적었다.

## §2 차선 여섯 — 닫힘 / 넘김

| 차선 | 절 | 닫힘 | 넘김 | 한 줄 |
|---|---|---|---|---|
| F | 5 | 3 | 2 | 온보딩 술어 둘(ThresholdChange · 감사 시험 발송) 배선 · 시험 20/0 · HEADLINE 6 사전 등재 · **지시서 수치 셋을 실측으로 정정**(카드 「40」 → 코드 27·PRD 38 · walk 「6/6」 → 판정 7종·자기시험 11 · 「6자리·8자리」 원본 목록이 저장소에 없다 — 실제 자리는 더 많다) |
| U24 | 6 | 6 | 0 | 서식 3 · `docx_export.render()` DOCX 정본 + PDF · `monthly_report` 배치 · 감사 해시 체인·CSV · `Reports.tsx` · **개발 DB 에 자동본 1**(id 1 · `trigger=auto` · DOCX 38,423B · PDF 50,689B · **택배 낱말 0**) · 시험 141/0 |
| U56 | 7 | 7 | 0 | 카메라 주소 **전용 문**(일괄 등록 문을 타던 쓰기) · 재시작 **요청만** 기록 · 백업 회수증 · 저장 상한 선언 · API 키 범위 · 연계 명세 1장 · `/roles` 가름 · 시험 82/0 |
| U3 | 5 | 5 | 0 | **P-166**: 구독 비밀 본문 전용 · 쿼리에 오면 **400**(시험 6 + 실물 HTTP 2) · `push.ts` 본문 전송 · 커널 `WEBPUSH` 상수 통합 · 구역 필터 배선 · 편리성 #5 계측 · 시험 114/0 |
| U1 | 4 | 4 | 0 | 상급 보고 토글(상태 칸 + FailureNotice) · **U1#11 은 기대식 오류로 가름**(정렬 무변경) · 편리성 배선 확인 · 시험 53/0 |
| Q | 6 | 6 | 0 | row_map 사유 7 교체 · 온보딩 9행 채움 · 씨앗 id 파이프(`seed.json`) · **상태 바꾸는 클릭 되돌림 규약** · PERF-04 `hand:out` · `seed_events(address=)` |

조율자: V_LOCK(P-170 ①) · 계획 창 판정(P-171) · 진입면 +12 · 라우트 대장 761 · beat 월 1일 03:00 ·
사건 보고 조립 하나로 · App.tsx/routes.ts 배선 · 0030 적용 · P-168 표 · 문서 병합(GX-COPY · 온보딩 · 쓰기 면).

## §3 V 단독 — 잠금 안에서 49분, 조율자 로그인 **0**

로그인 4/4 · 걷기 **3/3**(S1 회귀·S2·S3) · 캡처 **33/36** · FC **28/48** · 영역 ① **10/39** · 온보딩 **14.5/48**.

- **`V_LOCK` 이 실제로 쓰였다** — V 가 잠그고 세션 id 를 자기 도구에 줬고, 조율자는 그 49분 동안
  브라우저도 게이트도 열지 않았다(D-487 이 만든 규약의 첫 집행).
- **[되돌림] 줄**: 선언 1자리 · 되돌림 1 · **못 되돌림 0** — 턴 T 가 규칙을 끈 채 남긴 그 자리가 이번엔 돌아왔다.
- **캡처 새 자리 석 장**(감사 · 수신자 · API 키)이 전부 찍혔고 제 API 를 200 으로 불렀다 — 영역 ① 셋이 그것으로 이어졌다.
- 못 찍은 석 장은 **인수 화면**(`/device` · `/roles` · `/survey-profile`)이고 턴 T 에도 같은 자리가 빨강이었다(회귀 아님).
- **걷기 S4·S5 는 못 걸었다 — 이름이 이미 임자가 있다**: `perf_load.SCENARIOS["S4"]` 는 부하용 「섞기」이고
  `walk_scenarios.NOT_WALKED` 가 걷지 않기로 못박은 이름이며 S5 는 어디에도 없다. **이름을 지어내 걷지 않았다.**
- **온보딩이 내려간 이유 둘**(둘 다 데이터·씨앗): U2#4 씨앗 사건에 스냅샷 참조가 없다 · U3#2 씨앗 주소가
  **카메라**에만 실리는데 술어·화면은 **사건**의 주소를 읽는다. 올라간 하나는 U1#2(빨강 → 반).

## §4 이 턴의 값 — **정본이 낡은 것을 실측이 잡았다** (V · FC 회색 18 중 넷)

`scripts/verify_click_completes.py` 의 정본이 「없다」고 적은 자리가 지금은 **있다**:

| 행 | 정본이 적은 말 | 지금 사실 |
|---|---|---|
| U4#5 | 「`POST /api/dsm/reports` 없음 · 그리는 화면도 없다」 | `POST /api/dsm/reports/runs` 있고 화면은 `/dsm/reports` |
| U4#7 | 「서버 문은 `report.pdf` 하나 · 프런트 참조 0」 | `.docx`·`.pdf` 있고 `Reports.tsx` 가 부른다 |
| U2#6 | 「그 문을 부르는 화면이 없다」 | 같은 `/dsm/reports` 의 「사건 보고서」 카드 |
| U4#16 | 「감사는 쌓이는데 **볼 자리가 없다**」 | `/dsm/audit` 있고 이번 캡처가 200 을 기록 |

⚠ **V 는 이 정본을 고치지 않았다** — 고치면 그 자리에서 회색이 초록이 되고, 그것이 「기대식을 고쳐
초록을 만드는 것」이다. **정정 문안만** 냈다(다음 턴 Q 가 넣는다). 그러므로 **FC 28 은 낮게 나온 수이고,
낮은 채로 보고한다.**

그리고 V 는 U4 로 **직접 눌러 봤다**(점수는 안 바꿨다 — 정본이 없어 회색인 행이다):
「만들기」 → **403** 이고 화면이 사람 말로 그것을 말한다(읽기 전용 계정 안내 + FailureNotice) ·
자동본 **DOCX 200 · 38,467바이트** 내려받음. 흠 하나: 403 이 모달과 상태 칸에 겹쳐 뜨고 모달 문구가
「열 수 없습니다」(읽기)라 **만들기**(쓰기)와 어긋난다 — 다음 턴 U24.

## §5 커밋 자리 — 게이트가 낸 **옳은 빨강 여섯**, 하나도 넘기지 않았다 (D-492)

| 게이트 | 무엇을 잡았나 | 어떻게 닫았나 |
|---|---|---|
| 사전(`verify_ui_copy`) | 새 문장이 `**켜져 있는**` — 화면엔 **별 두 개가 그대로** 보인다 + 미등재 | 별 제거 · GX-COPY 등재 |
| 라우트 대장(D-343) | 인벤토리 24.6시간 낡음(회색) → 새로 뜨니 미등재 **12건** | `--refresh` → `--freeze` · 749 → **761** |
| 타임아웃(W0-17) | **새 결함**: 저장 용량 집계가 MinIO 를 **상한 없이** 부른다(화면+크론 공용) | `connect 3 · read 10` |
| 결정 도구(QA-03) | 내가 쓴 D-488 이 파일 이름만 · D-491 은 실행 가능한 것이 없다 | 경로·게이트 채움 |
| 커널 스코프(C-3.1) | 새 커널 모듈의 공개 함수 여섯이 스코프도 문지기도 없다 | 순수 셋은 **비공개**로 · DB 만지는 둘은 **문지기 실호출** · `check_request_key_scope` 는 커널에서 **없애고** `assert_path_scope` 로(커널은 HTTP 를 모른다) |
| 죽은 필드(D-304) | 새 필드 둘이 쓰기 0곳 | 하나는 쓰기가 보이게 · 하나는 사유+결정번호로 등재 |

여섯째로 **동음이의 래칫(161 → 174)은 올렸다** — 는 것이 전부 실재 라우트 경로(`/api/dsm/settings/api-keys`)를
더 많이 적은 것이어서다. 다만 감사 행위 이름 둘은 진짜로 고쳤다(`read:api-key:…` → `read:inbound-api-key:…`).
**래칫을 올린 것은 자백이다** — D-492 에 그대로 적었다.

일곱째(판정기의 눈): `no_route` 대조가 **조각 이름만 같으면** 잡는 술어라 우리 카메라 주소 문이 §0.4 의
`delivery.Address` 와 부딪혔다. 술어를 느슨하게 하지 않고 **(모델, 경로) 한 쌍 + 사유** 등재로 갈랐고,
**등재를 비우면 다시 빨강**인 것을 음성 대조로 확인했다(래칫 살아 있음).

## §6 조율자 오판 — **둘** (자진)

1. **번들을 API 주소 없이 지었다**(D-493). 병합 뒤 프런트를 다시 지으며 `/app/.env` 를 안 가져가
   `VITE_API_URL` 이 안 박혔고, 로그인이 제 원점으로 나가 **501**. **V 의 측정 전부가 그 한 줄에 막혔다**(40분).
   V 는 대상을 고치지 않고 같은 원점 프록시로 운영 모양 그대로 쟀다 — 옳다.
   그 자리에서 **거짓 초록의 씨**를 찾아 닫았다: 게이트 SPA 서버가 `GET /api/…` 에 **200+index.html** 을
   주던 것 → **404**(눌러서 확인) · 그 서버를 저장소로 옮겼다(`scripts/gate_spa_server.py` — 종전에는
   컨테이너 `/tmp` 에만 살아 창마다 사라졌다). ⚠ **이번 수는 다시 재지 않았다** — 측정 뒤에 대상을 고치고
   다시 잰 수는 다른 수다. 다음 턴 첫 실측으로 넘긴다.
2. **문서만 채우고 도구를 안 늘렸다.** 온보딩 두 칸이 31 → **35행**이 됐는데 `measure_onboarding_t.py` 의
   행 목록은 **턴 T 의 22행** 그대로다. 그래서 「35행 재측」은 물리적으로 불가능했고 V 는 22행을 쟀다.
   이것은 「규약을 바꾼 쪽이 그 규약을 읽는 도구를 같은 커밋에 갱신한다」(P-171)를 **조율자가 어긴 것**이다.

**세종 오류 0(누계 28) · 영실 오판 2(자진) · 위임 이의 0.**

## §7 P-166~P-173 집행표

| 판정 | 결과 |
|---|---|
| P-166 구독 비밀 | **닫힘** — 본문 전용 · 쿼리 400 · 앞단 로그 마스킹까지(눌러서 확인: 표식 적중 0) · 회전 절차는 명령 순서로 적었다(실행은 기기가 있는 자리) |
| P-167 VAPID 실값 | 이번 턴 안 함(창 2 · 대표) |
| P-168 시험 DB 4 | 표 1 냈다 · **CONNECT 회수 0/4 — 못 했다**: 이 서버의 로그인 역할이 둘뿐이고 **앱이 superuser 로 붙는다**(회수해도 우회). 더 무거운 사실로 OPS 절 제안 2 |
| P-169 FC 34·영역 ① 10·온보딩 35 | 영역 ① **달성** · FC·온보딩 **미달**(§3·§4) |
| P-170 V 단독 규약 둘 | **둘 다 닫힘** — V_LOCK 첫 집행 · 씨앗 id 파이프(`seed.json`) |
| P-171 계획 창 | 판정기 세웠다(창 안/창 밖 두 줄 · 30분 상한 · 자기시험 +7). **전제는 틀렸다**: 창 안 502 **0건**이고 234건은 09-06~09-15 것 — 창이 502 를 만든 게 아니라 **창이 설명할 자국을 지웠다**. 게다가 `--max-requests` 가 없어 **재활용 자체가 없다** → OPS-13a 는 다시 재서 닫히지 않는다(닫는 길 둘을 세종께) |
| P-172 PERF-04 | `hand: out` · 회색 유지 · 분모 그대로 |
| P-173 파 3 본 일 | §2 표 |

## §8 대표 결정 (여섯)

| # | 무엇 | 왜 지금 |
|---|---|---|
| 1 | **스테이징 1대** | 기한(09-19)이 지났다. 잠금화면 웹푸시·CR ①·RC-1 이 전부 여기 걸려 있다 |
| 2 | **P-90 운영 주소** | 여섯 턴째 |
| 3 | **창 2 일시**(30분) | 항목이 늘었다: VAPID 실값 3 · 시험 DB 4 삭제(127MB) · SMTP 5 · **저장 상한 `GX_STORAGE_CAPACITY_GB`**(지금 화면이 「분모 없이 답하지 않는다」) · **앱 DB 역할 분리**(앱이 superuser 다) |
| 4 | **Docker AutoStart 켬** | 오늘 아침 **0/10** 의 직접 원인 · 켜면 다음 아침은 사람 손 없이 10/10 |
| 5 | **`NotificationRule` pk 9 되돌리기** | 턴 T V 의 클릭이 끈 채 남아 `verify_seed_roles` 가 빨강이다. 명령 한 줄을 들고 있다(도구 규약이 생겨 **다시는 안 생긴다**) |
| 6 | **OPS-13a 를 어떻게 닫을까** | ㉠ 재활용 부재를 결정으로 선언하고 `hand: out` ㉡ `--max-requests` 를 켜고 재측(창) — 조율자는 ㉠ 이 지금 사실에 맞다고 보나 판정은 세종·대표 |

## §9 다음 턴으로 넘기는 것

- **Q**: FC 정본 넷 정정(§4 문안 그대로) · `measure_onboarding_t` 행 목록을 35행으로 · 걷기 S5·S6 이름 세우기 · U3#2 씨앗이 **사건에도** 주소를 싣게
- **U24**: 403 모달/상태 칸 겹침과 문구(읽기↔쓰기) · 감사 필터 3 의 화면 쪽 마무리
- **U56**: 키 범위 403 을 **HTTP 로** 보이게(`stats`·`pulse` 가 `inbound_key=True` 를 선언해야 한다 — 그 두 파일은 U24·U3 소유) · `/roles` 표 0 의 마지막 갈래
- **U3**: 오탐 판정 실패 자리(토스트 「불가」로 판정한 그 하나)를 상태 칸으로
- **F**: `DsmSystemRequest.handled_note` 를 닫을 문 · U3·U6 온보딩 버킷
- **조율자**: 번들을 `.env` 와 함께 짓는 절차를 runbook 에 · 온보딩 도구·문서를 같은 커밋에 · 씨앗 정리 순서
