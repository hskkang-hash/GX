# WO-GX-20260921-04 보고 — 턴 AB 파 1 턴 2 「돈 내는 고객이 URL 로 들어와 제 일을 끝낸다」

| 항목 | 내용 |
|---|---|
| 지시서 | `docs/workorders/WO-GX-20260921-04_턴AB_상용100_온보딩100_멀티에이전트13_3시간_20260921.md` |
| 형태 | 조율자 20분 → **동시 차선 11** → 병합 → V 단독 |
| 보고 | 영실(CPM · 조율자) → 세종(CPO) · 참조 대표 |
| 기계 시각 | 2026-09-21 (KST) |

---

## §0 첫 줄 — **수가 올랐다. 그런데 이 턴이 실제로 한 일은 거짓 초록 다섯을 벗긴 것이다**

```
상용 /100        54.1 → **57.0**  (+2.9 · 영역 ① 승격 5 · 절 155 그대로)
닫힌 절          68 → **73**/155
kind 표 정본     155 → 291     (절 155 + 별표 ② 기능명세 136)
기능명세 포함     ≈29 [세종 손] → **27.5 %** [산출기]
CR               회색[0/8] → 잰 칸 8/8 · 회색 0 · 25 → 18.75 %
단위 시험        1,935 → **2,110** passed
```

★ **올라간 1.6 보다 무거운 것은 이 턴에 벗겨진 거짓 초록 다섯이다.** 전부 「우리가 옳다고 적어 두고
한 번도 안 댄 자리」였다:

| 벗긴 것 | 무엇이었나 | 찾은 이 |
|---|---|---|
| **타입검사** | `tsc --noEmit` 을 그냥 부르면 **`"files": []` 솔루션 파일**이라 **rc 0 · 0줄**. 기준선이 3234 인데 **0 이 나오는 것이 유일한 신호**였다 | K |
| **SEC-04 「0건」** | 분모가 **0** 이었다 — `inspect.getfile` 이 **감싼 쪽 파일**을 줘 우리 라우트 **764건이 전부 「남의 것」**. 「모른다」가 아니라 「남의 것」이라 **안전망도 같이 초록** | S |
| **기능명세 id 「없다」** | ★ **내 실측이 틀렸다.** 한 그물이 0건을 냈을 뿐이고 **136개가 실재**한다 | Q |
| **U5#2 `/roles` 0행** | 인수 자산의 결함이 아니라 **우리가 턴 O 에 낸 퇴행** · **U24 의 빨강 둘이 같은 뿌리** | U56 |
| **`--no-gates` 가 로그인 0** | 절의 게이트만 끄고 **영역 ①의 게이트는 그대로 불러** 로그인한다. 막는 것은 `--static` 뿐 | N |

---

## §1 첫 표 10줄 — **[전부 조율자 실측 · 병합 뒤]**

| 수 | 지금 [정본] | 턴 AA | 목표 | 판정 |
|---|---|---|---|---|
| **상용 /100** | **57.0** (절 155) | 54.1 | ≥ 58 | **미달 (−1.0)** · V 뒤 승격으로 55.7 → 57.0 |
| 손 안 도달율 | **72.3 %** (99/137) | 71.5 % | — | ★ **내가 회색이라 적은 것이 틀렸다** — 아래 §15 |
| **온보딩 48행** | **34.0/48** (70.8 % · **잰 48 · 회색 0**) | 24.5(잰 43) | 잰 48 · ≥ 36 | **잰 48 달성 · 36 미달** |
| **48행 「누른 뒤」** | **31/48** (초록 31 · 빨강 2 · 회색 15) | 0/48 회색 | 잰 48 · ≥ 36 | **0 → 31** |
| **영역 ①** | **도달 18 · closed 15** (승격 5) · kind **15.50/39** | 17 · 10 | 도달 ≥ 24 | ★ **반증됨 — §3** |
| FC / PR / **CR** | **62.5[2/3]** · **50.0[1/4]** · **18.75[8/8]** | 52.0 · 48.1 · 회색 | 3/3·4/4·8/8 | CR **닫힘** · FC 칸 하나 더 섬 |
| **기능명세 포함** | **27.5 %** (분모 **291**) | ≈29 [손] | 산출기 첫 산출 | **닫힘** · 분모 안 움직임 |
| 첫 10분 화면 12 | **잰 12/12 · 시험 장치 3/12** | 미확인 | **0**/12 | ★ **빨강** |
| MEASURED | **69/80** | 51/79 | ≥ 65 | **넘김** |
| 커밋 | 턴 AB 커밋 1 · **push 0/4** | 0/3 | — | ★ 대표 한 줄 |

**병합 게이트** [조율자 실측]
```
타입검사        3234 = 기준선 정확히 · 우리 줄 0 · A/B 3234→3235→3234 (검사기가 우리 줄을 본다)
                ★ -p tsconfig.app.json 으로만 잰다
금지구역        PASS rc 0 — 변경 141건 전수 중 §0.4 0건
전량 시험       **2,110 passed · 0 failed · 6 skipped** (16분 53초) — 첫 회차의 빨강 2 는 진입면 계약이었고 등재 뒤 사라졌다
fail_closed     27 passed
ui-secrets --api rc 0 · 렌더 문자열 0 · API 응답 0
evidence_chain --db  ★ rc 1 — **새 어긋남 6건** (알려진 결함 · §5)
번들            새로 구워 3002 에 심고 **바이트로** 확인 (Onboarding-DzhZ65Tk.js 안에 K 의 문구 실재)
```

★ **진입면 계약이 조용히 늘 뻔한 문 하나를 멈춰 세웠다.** 전량 시험이 `109 != 110` 으로 빨개졌고,
U24 가 낸 **`GET /api/dsm/events/{int:event_id}/situation-report.docx`**(별지 1호 DOCX)였다.
인증(`auth=JwtOrInboundKey()`)과 판정식(`_report_reader_denial` — 보고서와 **같다**)을 확인하고
**뜻을 두고 등재**했다. **손으로 그 줄을 더하는 일이 곧 「진입면을 넓힌다」는 선언**이다.

---

## §2 ★★ 지시서의 전제가 **여섯 번** 틀렸다 — 전부 차선이 먼저 쟀다

**「지시서도 입력이지 측정이 아니다」가 한 턴에 여섯 번 값을 했다.**

| # | 지시서가 말한 것 | 실물 | 잰 이 |
|---|---|---|---|
| ① | 영역 ① **도달 ≥ 24** | **정직한 천장은 19** — 남은 회색 15절을 한 절씩 코드로 대 보니 캡처로 풀리는 것은 **둘뿐** | A |
| ② | **P-229** 「표 밖 9역할에 배정된 실사용자 **0** 이면 손대지 않는다」 | **0 이 아니라 8명** — 그 계정으로 들어오면 **영어 사이드바가 열 줄 넘게** 뜬다. (그리고 「9종」은 세면 **여덟**) | U56 |
| ③ | **P-227** 「탐침 카메라를 `probe` 표식으로 거른다」 | **절반만 참** — 그 카메라 위 사건 16건 = probe 12 + **drill 4**. drill 은 **빼서도 안 된다**(제품은 훈련을 센다) | U1 |
| ④ | **U6#3 JWT 404** · **U6#4 웹훅 422** | **제품이 옳다.** 404 는 판정기가 **없는 id** 로 두드린 것이고, 422 는 제품이 옳게 낸 거절이며 **한 걸음 문이 이미 있다** | U56 |
| ⑤ | 「`report-template` 에 별지 1호를 배선하라」 | **금지구역 화면**이다(`rj-core` import · **택배 운송장 서식**). 저장소가 두 곳에 「우리 보고서는 `/dsm/reports`」라 적어 뒀다 | U24 |
| ⑥ | 정본 경로 `features/dsm/**components**/deliveryOutcome.tsx` | **없는 경로** — 안 재고 만들었으면 **정본이 두 벌**이 될 뻔했다 | U3 |

★ 그리고 **U2#4 는 씨앗 문제가 아니라 술어 문제**였다(U24): 데스크톱·모바일이 **같은 부품**을 쓰고,
술어가 `src.includes('snapshot')` 인데 `src` 는 **언제나 `blob:`** 이라 **어떤 씨앗으로도 그 술어는 0** 이다.

---

## §3 ★★ 「도달 ≥ 24」를 못 채웠다 — **수를 만들지 않고 사유를 적었다**

차선 A 가 남은 회색 **15절을 한 절씩** 코드로 댔다:

| 막는 것 | 절 | 캡처로 풀리나 |
|---|---|---|
| 사유가 낡았다 / 찍은 적이 없다 | `F-10-c1` · `F-14-c2` | **예** (+2) |
| **시간·내부 변환** — 화면 한 장이 답할 수 없다 | `F-01-c1` `F-02-c2` `F-04-c1` `F-04-c2` `F-10-c2` | 아니오 |
| 그 신호/결합/상향을 **보여 주는 화면이 없다** | `F-02-c1` `F-03-c1` `F-03-c2` | 아니오 |
| 폴리곤 편집기가 **§0.4 금지구역** 쪽 | `F-03-c3` | 아니오 |
| ★ **P-220 을 어기지 않고는 못 찍는다**(5상태 중 넷) | `F-09-c1` | 아니오 |
| 캡처가 **종이에 안 실린다**(U24 파일) | `F-11-c1` | U24 가 고치면 |
| 위젯 관리 문이 **서버에 0건** | `F-12-c4` | 아니오 |
| 부작위 — 재는 게이트가 없다 | `F-13-c1` | 아니오 |

⇒ **정직한 천장 17 + 2 = 19.** 실측 도달은 **18**(`F-14-c2` 는 캡처가 서야 하고 그것은 V 창이다).
★ **「24 로 가려면 절을 억지로 이어야 하고, 그 초록은 초록이 아니다.」**

★ **승격은 3 이다**(`F-12-c5`·`c6`·`c7`) — **내가 N 에게 「5」라고 지시했는데 그것은 A 의 재측 이전 수**였고,
**N 이 기계가 낸 쪽을 썼다.** 안 올린 둘의 사유도 갈렸다: `F-12-c1`(이번 회차 회색 · 한 회차 전 초록은
**git 스테이지에만** 있고 디스크 정본이 아니다) · `F-10-c1`(**도달만** — A 가 「도달만으로 closed 주지 마시라」).

★ **N 이 대장에 박았다: `closed` 13 은 한 잣대가 아니다.** 올린 3 은 P-231 잣대, **이미 있던 10 은 옛 잣대**이고
그 열의 「누른 뒤」 증거는 **27.5시간 낡았다**(한도 12h). **안 내렸다 — 「잰 적이 없다」와 「잰 것이 늙었다」는 다르다.**

---

## §4 제품 결함 — 이 턴에 잡은 것

| # | 무엇 | 고객에게 무엇이었나 | 차선 |
|---|---|---|---|
| ① | **U5#2 `/roles` 0행** | 기관 관리자가 **역할 화면을 못 쓴다.** 서버는 멀쩡(`200 · count 16`)한데 **사이드바에서 뗀 줄이라 그 화면이 제 표를 못 그렸다.** `09-07` 15행 → `09-10` 턴 O 퇴행 → `09-17` 0행 | U56 |
| ② | ★ **상급기관 제출본이 훈련 둘을 실제 재난으로 올리고 있었다** | 종이에 `data_source` 칸이 없어 **알 길이 없었다.** 지금은 첫 줄이 「**확인하지 못했습니다** — 훈련 사건이 섞여 있을 수 있습니다」 | U24 |
| ③ | **임계값 「지금 값」이 안 바뀐다** | 제 기관 값을 바꾼 관리자가 **화면에서 제 값을 못 봤다.** `list_thresholds` 가 전역 층만 읽었다 → 기관 → 전역 → 정의 | F · A |
| ④ | **「사람에게 안 감」 판정식이 두 벌** | 배지는 사람 수만, 등급표는 채널까지 — **같은 파일에 옳은 판정이 이미 있었다.** 그리고 화면 머리글은 **사람이 4명 있는데 「없습니다」**라고 말하고 있었다 | U56 · U3 |
| ⑤ | **U3#7 이 서버의 답을 버렸다** | 화면이 **제가 보낸 값**으로 「옮겼습니다」를 적고 있었다 → 이제 **서버가 답한 값**으로 적고, `audit_id` 가 없으면 그 조각을 안 적는다 | U3 |
| ⑥ | **「시드(검수용)」 배지가 영원히 안 뜬다** | 사전엔 있는데 서버가 `live`/`drill` **두 낱말만** 보낸다. **캡처에서 시드 카메라 넉 줄이 실사건과 똑같이 보인다** | U1 |
| ⑦ | **DOCX 가 표 첫 줄로 열 수를 잡는다** | 좁은 줄이 먼저 오면 **DOCX 가 아예 안 나온다**(`IndexError`). ★ **HTML 만 보면 안 보이는 고장** | U24 |
| ⑧ | **U4 사이드바에 보고서·감사 기록이 없다** | ★ **화면도 있고 서버 권한도 열려 있는데 가는 길이 없다** — 주소를 직접 치지 않으면 못 간다 | U24 |

---

## §5 ★★ 새 빨강 둘 — 숨기지 않는다

### ㉠ LAW-08 증거 체인 — **어긋남이 이 턴에 0 → 6 으로 늘었다**

```
[조율자 실측] verify_evidence_chain.py --db  rc 1
  감사 행 4,146 · 체인 3,857 · 등재된 끊김 23
  ★ 새로 난 어긋남 6건 — 전부 hash_mismatch (옛 23 은 전부 prev_mismatch · 다른 종류)
  턴 AB 시작 0  →  S 가 잴 때 4  →  병합 때 6
```
**원인은 공용 파일 한 줄**(S 가 DB 없이 왕복 재현 · 내가 코드로 확인):
```python
# backend/common/evidence_chain.py:774
base = dict(payload) if isinstance(payload, dict) else {}   # dict 아니면 통째로 버린다
# 그런데 해시는 **버리기 전 값**을 덮고, strip_chain 은 dict 가 아니면 **그대로 돌려준다** — 비대칭
```
**손해 둘 · 둘째가 더 크다**
1. **영구 빨강** — 그 행은 영원히 검증을 통과 못 하고 **쓸 때마다 한 건씩 는다**(4→6 이 그 증거).
   **매일 나는 빨강 옆에서는 진짜 빨강도 안 보인다.**
2. ★★ **증거가 사라졌다** — 임계값을 **무엇으로** 바꿨는지가 감사 행에서 없어졌다.
   `data_before` 는 살아 있는데 `data_after` 가 빈칸이다. **체인은 고치면 되지만 안 남은 값은 안 남은 것이다.**

**안 고친 이유**: S 도 나도 안 고쳤다. **「창과 다른 변경을 섞지 않는다」** — 증거가 해시되는 방식을
바꾸는 일은 **병합 몇 분**이 아니라 제 창과 제 음성 대조를 가져야 한다. **고치다 틀리면 빨강보다 나쁘다** —
체인이 초록인데 거짓인 상태가 된다. 이미 난 6건도 손대지 않았다(P-191).
⇒ **턴 AC 의 첫 자리**. S 가 고칠 한 줄까지 적어 뒀다.

### ㉡ 감사 로그는 고시상 **「접속기록」이 아니다** — P-230 의 [추정] 을 S 가 뗐다

「개인정보의 안전성 확보조치 기준」 **제8조제1항**(고시 제2026-9호 · 시행 2026.7.1): **1년 이상**,
단 ①5만명↑ ②**고유식별정보·민감정보** ③기간통신사업자는 **2년 이상**.

★★ **범주가 어긋난다** — 고시의 「접속기록」은 **접속지 정보(IP)** 와 **처리한 정보주체 정보**를 요구하는데
**[S 실측] `audit_writer` INSERT 칸에 둘 다 없다.**
⇒ **90일을 730일로 올려도 접속기록 요건은 이행되지 않는다. 고를 문제가 아니라 칸이 없는 문제다.**
(F 가 낸 **730 · 365 · 90** 세 수를 한쪽으로 조용히 안 맞췄고, 대장에 셋 다 적혔다.
★ F 가 찾은 것: 대장 `hand_why` 가 「선언 한 칸을 쓰면 닫힌다」였는데 **그 칸엔 이미 365 가 있었다.**
막는 것은 **파기 스위치**이고, 켜는 것은 **삭제를 켜는 일**이라 안 켰다.)
⚠ 제8조**제2항** 시행일 **[미확인 1건]** — **지어내지 않고 [미확인]으로 남겼다.**

---

## §6 청구 전/후 표 — 대표 ⑧ 집행 (P-224)

| 재는 것 | 전 (11:37) | 후 (12:05) | 차이 |
|---|---|---|---|
| ETRI-Group **청구 카메라** | **15** | **4** | **−11** |
| ETRI-Group **청구 계정** | **30** | **19** | **−11** |
| ETRI-Group 화면 카메라(운영 면) | 15 | **15** | **0** ★ |
| ETRI-Group 화면 계정(운영 면) | 30 | **30** | **0** ★ |
| 다른 테넌트 아홉 | — | **한 칸도 안 변함** | **0** |
| 미디어(저장) | 115건 · 1.39 GiB | 같음 | **못 쟀다**(0 아님) |

★ **청구만 줄고 운영 면은 안 움직였다 — 감춘 것이 아니라 뺀 것이다.**
★ **이름으로 안 걸렀다**(D-280): 세는 코드에 `gxseed`·`gxprobe` 낱말 **0** · 소급은 **pk 로 얼린 22줄**.
★ dj-core 셋(`CoreUser`·`UserMediaFile`·`APIKey`)에 **칸을 안 더했다** — 곁표 `common.BillingMark` + `mark_unbillable()`.
★ 가격표 `price_table.yaml` 은 **값 5칸을 비웠다** → 초안이 「단가 미확정」 **회색** · **훈련 줄만 0원 초록**.
⚠ **넘김**: 소급은 한 번이다. **발급 순간 배선 다섯 자리**(게이트 탐침 카메라 · 온보딩 계측기 · 훈련 창 ·
씨앗 계정 · 탐침 계정)가 안 달리면 **새 씨앗이 다시 청구에 든다.**

---

## §7 kind 표 — **분모가 291 이 됐다**

```
절 155 (8영역 · 가중치 합 100 → 상용/100 을 만든다)
별표 ② 기능명세 136  (DSM 32 · FWS 90 · 플랫폼 14 — 전부 실재 표식에서 기계가 뽑음)
합 291
```
★ **별표는 8영역 밖에 둔다** [조율자 판정] — 안에 끼우면 **가중치 합 100 이 무너져 상용/100 이 다른 뜻**이 된다.
⇒ **상용/100 은 절 155 그대로 · kind 표만 「절 + 별표」.**

★★ **등재하자 수가 올라갔다 — 그리고 그것이 이 턴의 두 번째로 무거운 발견이다.**
```
등재 전:  기능명세 포함  25.6 % = 77.5 ÷ 291   (절 155 + 미등재 136)
등재 후:  기능명세 포함  50.0 % = 77.5 ÷ 155   (절 155 + 미등재 0)     ← 136 이 두 칸 사이로 떨어졌다
고친 뒤:  기능명세 포함  26.6 % = 77.5 ÷ 291   (등재 여부와 **무관**)
```
★ **「분모가 늘어 수가 내려가면 첫 줄이다」의 거울상이다 — 분모가 줄어 올라간 수는 수가 아니다.**
**재는 것이 깨져서 수가 좋아진 것**이라 내려간 수보다 위험하다 — **아무도 안 본다.**
★ **Q 의 자기시험이 이것을 못 잡았다**: `분모 291` 검사가 `n_sections=155` 를 **손으로 받아** 통과했다.
**분모를 손으로 넣는 자기시험은 분모를 안 재는 자기시험이다.** 이제 전부 대장이 찍은 글자에서 읽고,
**옛 식이 등재 뒤 291→155 로 줄던 것**을 **출생 표본**으로 박았다. 결정문 **D-513** · `enforced_by` 배선.

---

## §8 대표 — 손이 물리적으로 필요한 것 (결정은 P-222 로 위임됨)

| 언제 | 한 줄 | 왜 대표 손인가 |
|---|---|---|
| **지금** | `cd C:\GuardianX\guardianx-source && git push origin turn-q` — **네 커밋**(`9ed7cf0` 턴 Y · `1b4c9d2` 턴 Z · `0b7222f` 턴 AA · 턴 AB) | ★ **두 턴째 분류기가 거절**(`Sensitive-Source Provenance`). **우회 0.** 보험으로 `_patches/turnAB_*.bundle`(5.4 MB · `git bundle verify` 통과)을 떠 뒀고 **저장소엔 안 올린다** |
| 매일 아침 | Docker `com.docker.service` 자동 시작 + 재부팅 1회 | ★ **일곱 턴째.** 이 턴에도 **엔진이 죽었다**(§10) |
| 09-23 12:00 | 창 2a C0 · 금고 값 | ⚠ **VAPID 는 U3 가 되살렸다**(훈련 도달 1 · `delivery_id=694`). 다만 **돌고 있는 `gx-gunicorn-e`·`gx-celery-e` 환경엔 아직 없어**(세 이름 `len=0`) **고객이 「내 기기로 한 통」을 누르면 여전히 503.** 싣는 일은 **컨테이너 재생성**이라 안 했다 — 제 창이 필요하다 |
| 09-28 | 가격 숫자 4 확인 | 코드는 **빈 설정 + 회색**으로 이미 간다 |

**⚠ 이 턴에 대표 결정이 필요한 삭제 — 없다.** 삭제 0건으로 닫았다.

---

## §9 세종 판정 청함 — 넷

1. ★★ **CR 8칸 — §4-1 의 표 안에서 합이 갈린다.** 여덟 칸을 더하면 **1.5**, 합 줄은 **2.0**.
   **반인 칸이 셋인데 합은 넷을 말한다.** 둘 다 0.5 배수 격자 안이라 **형식으로는 못 가린다.**
   N 도 Q 도 나도 **0.5 칸을 지어내 2.0 을 맞추지 않았다.**
   ⇒ **⑤⑥⑦ 말고 반인 칸이 하나 더 있습니까.** 칸 이름 하나면 된다. 없으면 **정본은 18.75 %**.
2. **`surveillance_*` 둘의 정본.** §4-2 는 「표시명 없음 · 배정 금지」인데 `roleNav.NAV_ROLE_CODES`·`k3_roles` 는
   **이미 U1·U2 로 잇는다.** ★ **지금 손해 0**(전 테넌트 배정 0명)이라 **조용히 안 맞췄다** —
   맞추면 어느 쪽이 정본인지 영영 모르게 된다.
3. **P-229 의 전제가 틀렸다** — 표 밖 역할에 실사용자 **8명**이 산다. U56 은 **제안만** 했다(집행 0).
   그 계정으로 들어오면 **영어 사이드바가 열 줄 넘게** 뜬다. **재배정을 할 것인가.**
4. **별지 1호 문의 자리.** U24 가 `monthly_report.KINDS` 가 잠겨 있어 **사건 하나를 지금 그려 내려주는 문**으로 냈다.
   실행 기록에 넷째 종류가 서는 날 옮겨 붙인다 — **그 턴을 언제로 잡을 것인가.**

---

## §10 ★ 이 턴의 사고 — 도커 엔진이 또 죽었다 (두 턴 연속)

```
증상   v1.54/info · /containers/json · /containers/gx-shell/json  전부 500
       API 판을 1.51·1.48·1.44·1.41 로 낮춰도 같은 500 → 판 차이가 아니다
복구   조율자가 `docker desktop restart` → 컨테이너 10/10 스스로 복귀
       게이트 서버 둘은 안 돌아와 손으로 세움 (판정기가 예고한 그대로 · 턴 AA 와 같다)
```
★ **차선 K 가 먼저 신고하고 재기동하지 않고 허락을 기다렸다** — 턴 AA 에 F 가 같은 판단을 했다. **규약이 두 번 값을 했다.**
★ **원인의 모양이 턴 AA 와 다르다**: 턴 AA 는 **한 차선의 프로세스 스무 개**, 이 턴은
**차선 열하나의 동시 `docker exec`** + **U24 가 자진한 pytest 두 벌 겹침**(컨테이너 안 `Cannot allocate memory`).
⇒ ★ **「동시 12 차선」은 사람의 수가 아니라 엔진의 수에 걸린다.** 다음 지시서에 올릴 값이다.
⇒ **전량 시험과 프런트 빌드를 겹쳐 돌리지 않는다**(이 턴 병합에서 지켰다).

★ **내가 차선 둘의 추론을 바로잡았다** — S 는 「엔진이 **스스로** 살아났다」, A 는 「게이트 서버가 **스스로**
돌아왔다」고 적었는데 **둘 다 내가 한 일**이고 차선은 알 수 없었다. **기록이 한 턴만 지나면 「원인 없는 사고」로 굳는다.**
★ 차선 **F** 가 독립으로 받쳤다 — 「컨테이너 `StartedAt` 이 전부 21:00:43 KST 라 조율자의 복구 시각이
제 OPS-20 실측으로 확인된다.」 **같은 사건을 세 차선이 각자 잰 수가 맞았다.**

★ **낡은 test DB 하나가 전량 시험을 1,680 errors 로 만들었다** (`relation "django_content_type" already exists`).
엔진이 죽는 동안 끊긴 실행이 남긴 것이고 — **S 와 U24 가 둘 다 미리 경고한 자리**다. 걷어내고 재실행했다.

---

## §11 조율자 자진 오판 — **둘**

1. ★★ **「기능명세 id 가 저장소에 없다」를 실측으로 적고 대표께 올렸다. 틀렸다.**
   내가 던진 그물 `(DSM|FWS|…)-[0-9]{1,3}` 이 **`DSM-U1-01` 의 `U1` 마디를 못 넘었다.**
   실재 표식은 **DSM 32 · FWS 90 · 플랫폼 14 = 136** 이고, **세종의 32·90 은 한 자도 안 틀렸다**(운영자 12 만 실물 14).
   내 「표 데이터행 473」은 **다른 것을 센 수**였다.
   ★ 더 무거운 것: **그 틀린 결론을 N 과 Q 두 차선의 지시문에 전제로 박아 보냈다.**
   ★ N 이 책임을 반으로 갈랐다 — 「나는 **『독립 재측』이라며 같은 그물을 한 번 더 던져** 같은 0 을 받고
   서로를 확인했다.」 ⇒ **불변: 한 그물이 0건을 내면 「없다」가 아니라 「이 그물로는 안 잡힌다」이다.**
2. **A 의 낡은 수를 N 에게 지시로 줬다** — 「올릴 수 있는 것은 **5**」라 적었는데 그것은 A 의 재측 **이전** 수였고,
   기계는 **3** 이라 했다. **N 이 기계가 낸 쪽을 썼다.**
   ★ **「지시도 입력이지 측정이 아니다」 — 내가 이 턴 내내 차선들에게 시킨 그 말에 두 턴 연속 걸렸다.**

★★ **차선이 나 또는 지시서를 고친 것이 이 턴에 열넷이다.** 그중 여섯은 **지시서의 전제**(§2),
둘은 **내 말**, 다섯은 **거짓 초록**(§0), 하나는 **내 병합 절차**(타입검사 부름)다.

---

## §12 불변 — 이 턴이 더하는 아홉

1. ★★ **분모가 줄어 올라간 수는 수가 아니다** — 「늘어 내려가면 첫 줄」의 거울상. **올라가면 아무도 안 본다.**
2. ★★ **분모를 손으로 넣는 자기시험은 분모를 안 재는 자기시험이다.**
3. ★★ **한 그물이 0건을 내면 「없다」가 아니라 「이 그물로는 안 잡힌다」이다** — **같은 그물을 두 번 던지면 두 번 같은 것을 놓친다.**
4. ★★ **타입검사는 `-p tsconfig.app.json` 으로만 잰다** — 그냥 부른 `rc 0` 은 통과가 아니라 **빈 검사**다.
5. ★ **「지시도 입력이지 측정이 아니다」는 조율자에게도 걸린다.**
6. ★ **로그인 창 없는 차선은 `--static`** — `--no-gates` 는 영역 ①의 게이트를 통해 **로그인한다**.
7. ★ **「잰 적이 없다」와 「잰 것이 늙었다」는 다르다** — 늙은 증거로 닫힌 절을 **안 내린다**.
8. ★ **동시 차선 수의 상한은 사람이 아니라 엔진이 정한다** — 전량 시험과 빌드를 **겹쳐 돌리지 않는다**.
9. ★ **거르지 못하면 말하게 한다** — 이름으로 못 거르고 이름도 못 바꾸는 자리는 **화면이 「훈련」이라고 말하게** 한다.

---

## §13 턴 AC 로 넘기는 것 — 순서대로

**① 첫 자리 — LAW-08 체인** (한 차선이 고침과 술어를 둘 다 쥔다 · P-219)
`evidence_chain.py:774` 의 비대칭. **쓸 때마다 는다**(이 턴에 0→6). 고치는 것보다 **안 남은 값이 더 무겁다**.

**② U4 사이드바 — 「화면은 있는데 가는 길이 없다」**
`menu.Menu` 에 `/dsm/reports`·`/dsm/audit` 행이 **0건**이라 `menuId` 를 지어낼 수 없어 이 턴에 안 넣었다.
**한 차선이 ① 두 행을 세울지 `menuId` 없는 갈래가 서는지 눌러서 가르고 ② `roleNav.ts` 두 줄 +
`product_menus.py` 의 `P61_NO_SCREEN_YET` 두 줄을 같은 변경에** 지운다.
값: **U4 온보딩 여덟 중 셋**(#5·#7·#16) + **K 의 온보딩 카드 U4③**.

**③ 청구 — 발급 순간 배선 다섯 자리** (안 달면 새 씨앗이 다시 청구에 든다)
게이트 탐침 카메라 · 온보딩 계측기 · 훈련 창 · 씨앗 계정 · 탐침 계정.

**④ 「시드(검수용)」 셋째 낱말** — 서버가 `live`/`drill` 둘만 보낸다.
★ **청구·보고서가 같은 낱말 밭을 읽는다**(B 의 `data_source`) — **한 차선이 둘 다 쥐어야 한다.**

**⑤ 캐시 A/A 눈금** — F 가 눈금을 다시 세웠고(양측 정확 이항검정 p ≤ 0.01 · `MIN_PAIRS` 8 ·
턴 AA 의 거짓 초록 표본이 이제 죽는다) **팔 둘을 못 세워 못 쟀다.** **제 창 + gunicorn 두 벌**이면 선다.
**PERF-04 는 안 올렸다.**

**⑥ 남은 회색 게이트 11** — 전부 **분모가 런타임에 생기는** 게이트다. **09-23 부터 빨강**(D-511).
필요한 것은 면제가 아니라 **술어** — 「분모를 지금은 모른다, 돌고 나서 말한다」를 정직하게 적는 갈래.

**⑦ V 가 48행 `--measure` 를 돌리면** A 가 깔아 둔 표로 **12절이 그대로 올라간다** — 상용을 움직이는 가장 싼 한 수.

**⑧ 못 잰 채로 남은 것** (회색 · 0 아님): 미디어 씨앗 몫 · 카메라 `GX-U5-01` 분류 · 표식 없는 옛 훈련 사건 ·
수집기 로그 만료 건수(크기 기반 회전) · 재부팅 아침 N/10 · 종이 0장(LAW-08 은 `gate_only` 그대로) ·
QR 을 인쇄해 리더로 읽어 보지 않았다 · 제8조② 시행일.

---

## §14 차선 열하나 — 한 줄씩

| 차선 | 닫힘 / 넘김 | 한 줄 |
|---|---|---|
| **A** | 도달 18 · 승격 근거 3 | **「도달 ≥ 24」를 반증하고 수를 만들지 않았다** · 제 턴 AA 판단이 틀렸음을 자진 |
| **U1** | 8 중 4 | **P-227 이 절반만 참** — 거르는 대신 **말하게** 했다(새 말 0 · 새 문 0) |
| **U24** | U2 2 · U4 3 / 넘김 7 | 지시서를 **두 번 재서 거절** · **상급 제출본이 훈련을 실재난으로** 올리던 것을 잡음 |
| **U3** | 4 중 3.5 | **VAPID 재적재 + 훈련 도달 1** · 정본 경로가 **없는 경로**였음을 먼저 쟀다 |
| **U56** | U5 4 · U6 0 | ★ **U5#2 는 인수 결함이 아니라 우리 퇴행**(턴 O) · P-229 의 「0」이 **8** |
| **B** | 닫힘 3/3 | **씨앗 청구 0** · 운영 면 **한 칸도 안 움직임** · 전/후 표를 **마이그레이션 전에** 떴다 |
| **K** | 4 중 3 | 카드 18 을 **기존 표에 이어** 세움(진행률 한 벌) · ★ **타입검사 거짓 초록** 신고 |
| **F** | 4 중 3 | 임계값 「지금 값」 고침 · **대장 `hand_why` 가 틀렸다** · **못 가르면 PERF-04 안 올림** |
| **Q** | 6 / 넘김 4 | 산출기 2 신설 · **MEASURED 69/80** · 기한을 **문서가 아니라 배선으로**(D-511) |
| **N** | 승격 3 · 등재 136 | **대장이 제 글을 조용히 버리던 것**(중복 키 둘) · **Q 의 분모 붕괴**를 잡음 |
| **S** | 새로 연 것 2 | **SEC-04 의 「0」은 분모 0** · **LAW-08 새 빨강** · **P-230 은 칸이 없는 문제** |

**규율 총계**: 삭제 **0** · `--no-verify` **0** · §0.4 금지구역 **0줄** · 비밀 출력 **0** ·
차선 커밋 **0**(조율자만) · 차선이 세운 서버 **0** · 차선이 누른 재기동 **0**.

---

## §15 V 단독 — **내 수 다섯을 고쳤고, 그중 하나는 내 오판이다**

| 수 | **V 실측 [정본]** | 내가 적었던 것 | 무엇이 달랐나 |
|---|---|---|---|
| **48행 「누른 뒤」** | **31/48** (초록 31 · 빨강 2 · 회색 15) | 0/48 회색 | V 가 **쟀다** |
| **온보딩 48행** | **34.0/48** · **회색 0** | 24.5(잰 43) | V 가 **48 전량**을 쟀다 |
| **손 안 도달율** | **72.3 %** (99/137) | **「회색 — 게이트 회차 필요」** | ★ **내 오판 ③** |
| FC | **62.5** [잰 칸 **2/3**] | 60.4 [1/3] | 칸이 하나 섰다 |
| PR | **50.0** [1/4] | 48.1 [1/4] | +1.9 |

**같은 수 넷**: 상용 **55.7** · 영역 ① 도달 **18** · 가중 **9.23** · CR **18.8[8/8]** · 기능명세 **26.6 %**.
★ **V 가 분모부터 봤다 — 291 그대로였고 수가 26.6 위로 안 올라갔다. Q 의 고침이 실물에서 섰다.**

### ★ 조율자 오판 ③ — **「손 안은 회색」이라 적었는데 아니었다**
나는 `verify_ga_readiness --static` 만 돌리고 **「게이트 회차가 필요하니 회색」**이라 보고에 적었다.
**게이트를 부르는 갈래는 그 수를 실제로 낸다** — `72.3 % = 99/137`. 내가 다시 돌려 확인했다.
★ **「내가 안 재서 모르는 것」을 「잴 수 없는 것」으로 적었다.** 이것도 **회색의 오용**이다 —
이 방이 한 턴 내내 잡아 온 그 병의 반대편이고, **회색을 방패로 쓴 자리**다.

### ★ 역할별 7줄 — **여섯 역할 전부 8/8 을 쟀다**(§9 불변 ③ 충족 · 회색 0)
```
U1 5.5   U2 3.5   U3 6.0   U4 5.0   U5 7.5   U6 6.5   = 34.0/48  (70.8 %)
48행 「누른 뒤」:  U1 7 · U2 5 · U3 5 · U4 4 · U5 4 · U6 6 = 31/48
```
**한 역할도 0 이 아니다.** 빨강 둘은 **둘 다 「요청은 나갔는데 값이 안 바뀐다」**(U3#2 주소 · U6#4 웹훅).

### ★★ V 가 연 것 — **우리는 고객의 로캘로 한 번도 안 쟀다**
V 의 첫 캡처가 로그인 **4/4 실패**했는데, **같은 순간 같은 자격**으로 온보딩 측정기는 6/6 들어갔다.
**격리 A/B 로 갈랐다**:
```
A  locale="ko-KR"   ok=False  posts=[200]        → /login 에 머문다
B  기본             ok=True   posts=[200,200]    → 들어간다
[AB2] 누른 뒤 단추: ['', '로그인', '', '확인', '취소']   ← 「세션 종료 … 다른 기기」 창
```
⇒ **제품은 옳다. 판정기가 틀렸다.** `measure_onboarding_t.login()` 이 확인창 단추를
**`Confirm` 영어 이름으로만** 찾아, 로캘이 `ko-KR` 이면 **못 누르고 로그인이 안 끝난다 → 그 회차가 통째로 회색.**
★★ **우리 고객 브라우저는 대부분 ko-KR 인데 게이트는 영어로만 재 왔다.**
⇒ **불변에 올린다: 판정기는 고객의 로캘로도 재야 한다.** 고칠 한 줄은 Q 자리(턴 AC).

### ★ 첫 10분 화면 12 — **시험 장치 3/12** (목표 0 · **빨강**)
**6 역할 첫 진입 6/6** 이 섰다 — 여섯 장 전부에 K 의 카드(`처음이세요 — 오늘 먼저 세 가지` · `onboarding-kick`).
보이는 자리 셋: U1 「지금 처리할 것」 · U2 「관제 현황」(최근 10줄 = 탐침 4 + 시드 4 + 훈련 2) ·
U3 「내게 온 이벤트」(9건 = 시드 7 + 훈련 2).
★ **U1 의 빨강을 V 가 눈으로 대서 참으로 확인했고, 한 겹을 더 찾았다**:
**모바일에는 「훈련」이라고 말하는 자리가 아예 없다** — `dataSourceBadge` 를 부르는 화면은 다섯이고
**`MobileInbox.tsx` 는 없다.** 지금은 카메라 이름에 `[훈련]` 이 박혀 있어 손해가 안 보이지만,
**이름에 그 글자가 없는 훈련 사건이 모바일로 가면 현장 사람이 실재난으로 읽는다.**

### ⚠ V 회차가 남긴 상태 넷 — **되돌리는 문이 없거나 실패했다**(삭제 0)
`gxprobe_onb_20260921T145138`(id 226) **비활성화 실패 · `is_active=True`** — 지난 회차 넷(118·119·224·225)은 전부 돌아갔다 ·
카메라 15→16 · 미입력 6→5 · 재시작 요청 4→5(지우는 문이 제품에 없고 **그것이 옳다**).
★★ **이것이 §6 의 「발급 순간 배선 다섯 자리」의 실물이다** — **이 회차가 만든 계정 1 · 카메라 1 이 지금 청구 면에 들어 있다.**

---

## §16 V 뒤 — N 이 미뤄 둔 판단을 갚았다 · **정본 수**

```
[조율자 실측 · 최종]
  상용 오픈 가중 합계   54.1 → 55.7 → **57.0 %**   (이 턴 **+2.9**)
  닫힌 절              68 → 71 → **73/155**  · 못 잼 77 → **71** · 게이트만 4 → **5**
  영역 ①               kind 13.00 → **15.50/39**(40 %) · 가중 6.67 → **7.95** · 도달 18
  기능명세 포함         26.6 → **27.5 %**  ← 분모 **291 안 움직임**. Q 의 고침이 실물에서 섰다
```

★ N 이 앞 회차에 **「잣대가 둘이다 — 안 내렸다. V 가 돌리면 그날 같은 경로로 초록이 된다」**고 적어 둔
그 자리를 **갚았다.** 내 수를 베끼지 않고 직접 다시 쟀고(`절 17 중 초록 16 · 빨강 0 · 회색 1`),
⇒ **잣대가 하나가 됐다 — 이 영역에 「경로 없는 `closed`」는 0개다.**
**옛 문장은 대장에 그대로 인용해 남겼다** — 미룬 것을 잊지 않았다는 증거로.

★ **N 이 내 목록에 없던 절 하나를 더 올렸다**(`F-10-c1`) — 앞 회차에 「도달만으로 닫힘을 주지 않는다」며
안 올린 절인데 **이제 두 행이 다 측정됐다.** **「지시도 입력이지 측정이 아니다」가 또 값을 했다.**

★★ **올리지 않은 하나가 더 값지다 — `F-11-c2` 를 `gate_only`(0.5)로 뒀다.**
`F-11-c2`(손입력 0)와 `F-11-c3`(치환이 돈다)이 **같은 한 행 `U2#6`** 에 기대는데,
그 행이 재는 것은 「보고서가 **만들어졌다**」 하나다. **「손입력 0」은 그 행이 안 잰다.**
⇒ **같은 한 행으로 두 절을 올렸으면 초록 하나가 두 번 세어졌을 것이다.**
★ 이 영역에 `gate_only` 가 **처음** 생겼고, 대장의 옛 문장(「이 영역엔 gate_only 0절」)은
**도달만 세던 동안 참이었다** — 지우지 않고 사연과 함께 남겼다.

★ **N 이 규율에서 하나를 자진했다**: 기계가 찍은 표로 갈아 끼우면서 **사람이 적어 둔 내력 셋이
지워질 뻔했다**(`F-12-c5`·`c6`·`c7`). `git diff` 로 삭제분을 한 줄씩 대 보고 `caveat` 로 되살렸다.
⇒ **불변에 올린다: 기계로 다시 찍는 것이 사람의 내력을 지우면 안 된다.**

★ **N 이 「가장 큰 발견」이라 본 것도 로캘이다**: 「한 절의 회색보다
**『우리 고객 브라우저는 대부분 ko-KR 인데 게이트는 여태 영어로만 재 왔다』**가 크다.
`F-12-c1` 하나가 아니라 **확인창을 지나야 하는 모든 측정**이 같은 그림자 아래 있었을 수 있다.」
⇒ Q 에게 「고친 뒤 **다른 절의 색도 함께 움직이는지** 봐 달라 — **움직이면 그것도 수다**」를 청했다.
