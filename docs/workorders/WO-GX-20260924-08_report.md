# WO-GX-20260924-08 보고 · 턴 AE — 「자를 쪼개니 두 사실이 제 색을 찾았다」

| 항목 | 내용 |
|---|---|
| 보고 | **영실(CPM · 조율자)** → 세종(CPO) · 참조 대표 |
| 지시서 | `docs/workorders/WO-GX-20260924-08_턴AE_…_20260924.md` · `docs/agent/RESUME_NEXT.md` |
| 기계 시각 | **2026-09-23 09:50 ~ 15:4x KST** · ⚠ 지시서는 **09-24 10:00~13:00** 을 적었다. **창을 연 것은 시계가 아니라 대표의 한 마디**(P-265) |
| 출발선 | `35ba9f3`(원격 차 0) · 상용 57.0 · 온보딩 ko-KR 31.5/48 · 「누른 뒤」 31/48 · CR 31.2 · 전량 2,123/0 |
| 차선 | 5 · 전원 가벼운 모델 · 동시 5 · **브라우저 0**(「누른 뒤」는 조율자가 한 창에서) |

---

## §0 한 줄

**오늘 가장 값나가는 일은 수를 올린 것이 아니라 자를 쪼갠 것이다.** 한 게이트가
두 절(OPS-04 「살아나는가」 · OPS-19 「저절로 도는가」)에 걸려 있었고, 차선 E 가 자를
둘째 물음으로 바꾸자 **첫째의 참인 답이 빨강으로 덮였다** — 게이트가 「복구할 것이 남아
있지 않다」고 말했는데 149 MB 덤프가 있고 0.6일 전에 232표를 살려 냈다.
자를 가르자 **둘 다 제 색을 찾았다**: OPS-04 초록 · OPS-19 **정직한 빨강**.

그리고 **밤사이 아무도 안 부른 덤프가 떴다**(창 2a 가 고친 것이 저절로 돌았다).
그래도 OPS-19 를 안 올렸다 — 호출자 칸이 어제 덤프엔 없다(P-264).

---

## §1 첫 표 열 줄

| # | 수 | 값 | 어떻게 |
|---|---|---|---|
| ① | 상용 **게이트 갈래** | **57.0 %**(안 움직임) | closed 73 · **measured_red 4**(OPS-19 가 「못 잼」에서 옮겨 왔다) · 못 잼 67 · ⚠ 목표 ≥59 **못 닿음** |
| ② | **beat 덤프 1 · 호출자 칸** | 덤프 **1**(149,701,407 B · 05:00 KST) · 호출자 칸 **배선됨** · 그 덤프엔 **아직 없음** | `evidence/OPS-19/자동덤프_첫건_20260923.md` |
| ③ | 웹푸시 | 단추 **503 → 200** · **도달 0(못 쟀다 · 도구 한계)** | §6 · 구독 기기는 0 이 아니라 **1**이었다(정정) |
| ④ | 재부팅 아침 | **1/10** · 준비 ㉠ **11/11 스스로 선다** | ⚠ **2/10 은 이 턴에 닿을 수 없다**(§5) |
| ⑤ | **온보딩 ko-KR** | **34.0/48 · 회색 0** (31.5 → +2.5) | 역할별 일곱 줄 §6-2 ③ · ⚠ 목표 ≥36 **못 닿음** |
| ⑥ | 「누른 뒤」 48 | **32/48** (빨강 2 · 회색 14) | 31 → 32 · `U2#3` 이 올라왔다 |
| ⑦ | 첫 10분 12 | **안 쟀다 — 0/12 그대로** | 창 4 시간에 안 들어갔다(§6-2 ⑥) |
| ⑧ | CR | **31.2** [잰 칸 8/8 · 두 벌 「같다」] | 어제 벌었고 오늘 유지 |
| ⑨ | **기능명세 포함 완료율** | **회색 → 27.5 %** | 분자 = 대장 kind 점수 80.0 ÷ 분모 291 |
| ⑩ | 전량 시험 | **2,135 passed · 0 failed** (18분 08초 · 한 회차) | 시험이 **열둘 늘었다**(음성 대조 2 · 청구 시험 10) |

---

## §2 ★ 색이 움직인 것 — **자를 쪼갠 자리**

### 무엇이 있었나
`verify_backup_recovery` 하나가 **두 절**에 걸려 있었다. 차선 E 가 P-264 대로 ㉡ 를
「**beat** 이 부른 덤프인가」로 바꿨고, 자기시험은 옳게 갈렸다. 그런데 그 게이트가 **동시에
답하던 다른 절**(OPS-04)의 마지막 줄이 이렇게 됐다:

```
[OPS-04] 빨강 — 복구할 것이 남아 있지 않다.
```

**사실이 아니다.** 금고에 149,701,407 바이트가 있고, 0.6일 전에 그것으로 232표를
살려 냈다(RTO 13.7초).

### 왜 이것이 그냥 넘길 일이 아닌가
그대로 두면 상용이 내려간다. 그런데 그 하락은 **제품이 나빠져서가 아니라 자가 바뀌어서**다.
「**내려간 수가 정본이다**」는 이 저장소의 좋은 규율이지만, 그것은 **제품이 내려갔을 때**
하는 말이지 자를 바꿔 놓고 할 말이 아니다. 그리고 **세상을 틀리게 말하는 게이트는
사람이 끈다** — 꺼진 게이트는 없는 게이트보다 나쁘다(D-311).

### 어떻게 고쳤나 — **술어를 약하게 만들지 않고 파일을 갈랐다**
| | OPS-04 `verify_backup_recovery.py` | OPS-19 `verify_backup_autonomy.py`(신규) |
|---|---|---|
| 묻는 것 | 백업이 있고 **살아나는가** | **저절로** 도는가(RPO) |
| ㉡/㉤ | 바이트 있는 덤프 ≤ 48h | 마지막 **beat** 덤프 ≤ 26h |
| 오늘 [조율자 실측] | **초록 · rc 0** | **빨강 · rc 1** |
| 마지막 줄 | 「뜬 것이 있고, 그것을 살려 봤다」 | 「백업이 **저절로** 돌았다는 것을 이 판정문으로는 아직 못 말한다」 |

- `judge_beat_dump()` 는 **지우지 않았다** — 새 파일이 `import` 해서 쓴다. 로직이 한 벌이다(D-369).
- **왜 `--clause` 플래그가 아니라 파일을 갈랐나**: E 가 코드를 읽고 찾았다 —
  `verify_ga_readiness.run_gate()` 는 `gate:` 경로를 **인자 없이** 부른다.
  플래그로는 두 절이 **같은 rc** 를 받는다. **길을 실측해서 고른 답이다.**
- 자기시험: `verify_backup_recovery` **18/18** · `verify_backup_autonomy` **12/12**
  (조율자가 지정한 네 표본 포함 — ㉡초록·㉤빨강 / 둘다초록 / 둘다빨강 / **㉤ 음성 대조**).

### ★ E 가 스스로 적은 오판 — **이 턴에서 가장 값나가는 한 줄**
> 「㉡ 를 바꾸면서 자기시험(초록/빨강이 옳게 갈리는가)만 확인했지, **그 게이트가 동시에
> 답하던 다른 절의 마지막 줄이 뭐라고 말하는지**를 다시 읽어보지 않았다.」

⇒ **새 불변**: **게이트를 고치면 그 게이트가 걸린 절을 전부 다시 읽는다.**
자기시험이 통과하는 것과 그 게이트가 세상을 옳게 말하는 것은 다른 일이다.

---

## §3 차선 다섯 — 받은 것

| 차선 | 한 것 | 닫는 조건 |
|---|---|---|
| **S** | `test_sec21` 계수 비우기 + **음성 대조 2** · 접속기록 절 설계(코드 0줄) | 섰다 — §3-S |
| **E** | 호출자 칸 배선 · 시간대 격리 · 기동 스크립트 · **자 쪼개기**(2회차) | 섰다 — §2 |
| **U온** | `POST /events/{id}/severity` 신설 + 화면 · **181 passed** | **빚 2** — §3-U온 |
| **U56** | 시드 계정 `gxseed_role_order` · **청구 20→20** | 브라우저는 조율자 몫 |
| **K+Q** | 판정기 둘 + **계측기 청구 배선** | 넷째 미착수(적었다) |

### §3-S — 「혼자 재면 통과」가 끝났다
음성 대조: 한 클라이언트로 계수를 다섯 태운 뒤 **같은 IP 의 새 클라이언트**로 첫 부름도 429.
시험이 덮은 캐시와 율제한이 읽는 캐시가 **다르면 이 시험이 빨강**이다 — 배선을 재는 시험이다.
별칭은 추측이 아니라 라이브러리 소스로 골랐다(`RATELIMIT_USE_CACHE` 가 저장소에 없음 → `"default"`).

**빨강도 실제로 났다** — `setUp` 의 `clear()` 를 빼자 「채우는 **첫 번째**부터 이미 429」.
★ **다만 그 빨강을 낸 것은 S 이고 조율자는 로그를 읽었다.** 내가 낸 수가 아니다.
(다시 안 건 이유 셋: 음성 대조 자체가 배선을 재고 · 실패 출력이 구체적이고 · 창 3 전량이
같은 파일을 내 측정으로 다시 잰다.)

⚠ S 가 곁들여 찾은 것: **접속기록 보존기간이 세 곳에서 다르다** —
선언 **730일** / 심긴 값 **365일** / 코드 기본값 **90일**.
**법이 요구하는 수를 세 곳이 다르게 말하면 어느 것도 지킨 것이 아니다.** 임자 배정부터다(턴 AF).

### §3-U온 — **갈랐다. 그리고 시드로 초록을 사지 않았다**
빨강 여섯을 「데이터 0건」/「문 없음」으로 갈랐다 —
U2#2·U2#6·U2#16·U4#15 는 전부 **데이터 0건**(비-probe `occurred` 사건이 **전 테넌트 0건**),
U4#11 은 소유표 밖, **U2#3 하나만 문 없음**(재판정 라우트가 backend 전체에 **0개**).
⇒ 그 하나를 끝까지 올렸다. 음성 대조 둘(`severity='bogus'`→400 · 없는 id→404 · 두 실패 뒤 원값 불변).

★ 오판 둘을 **스스로 뒤집었다**: U4#15 를 「화면 일」로 보다가 커밋 시각 대조로 정정 ·
U2#2 의 `occurred` 12건이 **전부 probe** 임을 개별 확인하고 「버그」 판정을 거뒀다.

**빚 둘**: ① 새 문을 **HTTP·브라우저로 안 눌렀다**(함수 직접 호출로 쟀다 — URL·메서드·`auth=`·
테넌트 관문은 그 경로로 한 번도 안 지나갔다) → 창 4. ② `api.ts` 가 소유표 밖이라
**URL 이 두 곳에 산다** → 턴 AF.

### §3-U56 — 순서를 지켰다
`unbillable` **먼저**, 역할 **나중**. 청구 `usage_snapshot()['users']` **20 → 20 → 20**(멱등).
프런트는 **0줄** — 턴 AD 코드가 이미 `order` 를 인수 6종에 넣어 둔 것을 회귀시험으로
확인하고 **안 고쳤다**. 고칠 것이 없다는 것을 재고 안 고친 것은 일을 안 한 것이 아니다.

### §3-K+Q — 가장 급한 것이 섰다
계측기가 만드는 계정·카메라에 `mark_unbillable` 배선. 닫는 조건을 「배선했다」가 아니라
**「청구 셈이 정확히 −1」**로 잡았고 넷을 더 쟀다(행 불변 · 화면 수 불변 · **실패를 조용히
안 삼킴**(음성 대조 2) · 중복 호출 이중 차감 없음). 시험 41 passed · **브라우저 0**.
⇒ 이것이 서서 **다음 회차부터 정본 ko-KR 전량을 다시 돌릴 수 있다**(어제 못 한 그 일).

넷째(카드 시험 6 · U4③)는 **미착수라고 적었다.** 지시서가 「1~3 먼저」라 했고 그대로 했다.

---

## §4 ★ 오늘 네 번 밟은 자리 — **머리글은 늙는다**

| 누가 | 무엇을 믿었나 | 실제 |
|---|---|---|
| 조율자(09-22) | `verify_seed_p20` 머리글 「호스트에서 위임한다」 | **위임 코드 0건** |
| 조율자(09-22) | 계측기에 자격 하나면 된다 | `.env.gates` 에 **있는데** 안 넘겨 U6 네 행 회색 · 정본 수 −2.5 |
| **U56**(09-23) | `seed_role_users.py` 머리글 「무인증 HTTP」 | D-343 이 그 사이 인증을 붙였다 → 401 |
| **K+Q**(09-23) | `verify_route_alive.delegate_to_container` 주석 「`-e NAME` 은 호스트에 없으면 **무해**」 | **A/B 실측: 길이 20 → 0 으로 덮인다.** 주석이 **정반대**다 |

⇒ **불변**: 부르기 전에 그 파일의 **코드**를 본다 — 특히 ① 부르는 자리 ② 필요한 환경 이름
③ 인증 전제. 넷째가 가장 나쁘다: 틀린 방향이 **「안전하다」쪽**이다.
⇒ **넘김**: 그 주석은 거짓이다. 임자가 고쳐야 한다(K 는 소유표 밖이라 재사용 안 하고
좁은 전용 위임을 새로 썼다 — 옳다).

### 곁들여 · U온이 찾은 것
`| tail` 뒤에서 종료 코드를 읽으면 **언제나 `tail` 의 것**이다(0). 파이프가 초록을 만든다.
⇒ `${PIPESTATUS[0]}` 를 쓰거나 파일로 받는다.

---

## §5 ⚠ 지시서 §0 의 한 칸은 **이 턴에 닿을 수 없다**

「재부팅 아침 1/10 → **2/10**」 — 판정기(`ops_reboot_morning.py`)는 **모든 컨테이너의
`StartedAt` 이 직전 기록보다 뒤**일 때만 한 줄을 늘린다. 재부팅 없이는 **절대로** 참이 안 된다.
그 자리는 애초에 **지어낼 수 없게** 만든 자리다(「아침이었다」고 사람이 적는 것으로는 한 줄도 안 는다).

오늘 실측: **아침 1/10** · 준비 ㉠ **컨테이너 11/11 전부 「스스로 선다」**.
⇒ **대표 손이 필요하다** — 오늘 중 재부팅 한 번이면 그 줄이 선다. 없으면 **1/10 으로 적는다.**

---

## §6 창 2b — **자격은 섰고, 도달은 못 쟀다**

| 닫는 조건 | 결과 |
|---|---|
| VAPID 새 쌍 · 금고 · 저장소에 0 | ✅ `.env.vapid`(0600 · `git check-ignore` 가 잡는다 · `git status` 에 안 보임) · 값 출력 **0**(이름·길이·sha256 앞 12자까지만) |
| 통 둘 재생성 | ✅ `gx-gunicorn-e`·`gx-celery-e` → `guardianx-backend:pg18` · 옛 통은 **이름만 비킴**(`-old-2b`) · **삭제 0** |
| 고객 「내 기기로 한 통」 **200** | ✅ **503 → 200** · `vapid-key` 200 |
| **도달 1** | ❌ **못 쟀다** — 아래 |

### ★ 도달을 못 잰 이유 — **제품이 아니라 도구다**
두 브라우저 다 푸시 **등록**에서 막혔다 [실측]:
- **크롬**(`channel="chromium"`) — `Registration failed - permission denied`.
  크롬의 푸시는 FCM 이고 FCM 등록에는 **구글 API 키**가 필요한데 Playwright 가
  내려받는 크로미움엔 그 키가 없다.
  ⚠ 곁들여 찾은 것: **기본 껍데기(`chrome-headless-shell`)는 `Notification.permission` 을
  언제나 `denied` 로 준다.** `channel="chromium"` 이라야 `granted` 다 [실측 A/B].
  화면은 그때 「이 기기에서 알림이 차단돼 있습니다」라고 **옳게** 말했다.
- **파이어폭스**(이 창에서 설치 · 모질라 autopush 는 키가 필요 없다) —
  `Error retrieving push subscription.` 구독 자체를 못 받았다.
  (`updates.push.services.mozilla.com:443` 은 **열려 있다** — 망이 아니다.)

⇒ **「보냈다」를 「닿았다」로 적지 않는다.** 도달은 **0** 이고, 그 0 은 회색이다(도구 한계).

### ★ 제품은 **못 보낼 때 못 보낸다고 말했다** — 이것이 오늘 선 것 중 하나다
```
못 보냄 — 발송 이력 #742 · 사유: 푸시 서비스가 거절했습니다(WebPushException)
          — 구독이 만료됐거나 자격이 맞지 않습니다. 해지 후 다시 등록하면 됩니다.
```
조용한 성공(D-284)이 아니다. 단추는 200 이고 **결과는 못 보냄**이며, 화면이 그 둘을 갈라 말한다.

### ★★ 조율자 정정 — **「구독 기기 0」은 틀렸다**
창 2a 보고와 대표께 드린 말에서 나는 이렇게 적었다: 「구독한 기기를 세니 **0** 이라
새 쌍을 만들어도 **끊길 것이 없습니다.**」

**기기는 1대였다.** 화면이 그렇게 말한다 — 「**등록된 기기 1대** · Win32 · 2026. 9. 17.」.
내가 0 으로 읽은 이유는 **구독이 표가 아니라 감사 기록으로 살기 때문**이다
(전용 모델이 없다 · `LOGGER_NAME = "guardianx.dsm.push_subscription"`). 나는 표를 찾다가
「표 없음」을 「0건」으로 읽었다 — **「안 잡혔다」를 「없다」로 읽은 그 자리**다(D-301).

⇒ 결론은 바뀌지 않는다. 그 기기는 **오늘 이전에 이미 닿지 않고 있었다** — 창 2a 가
VAPID 값이 **어디에도 없다**고 쟀고, 비밀키 없이는 서명을 못 해 한 통도 못 보낸다.
그러나 **근거가 틀렸다**: 「구독이 0이라서 끊길 것이 없다」가 아니라
**「그 구독이 이미 닿지 않고 있어서」** 다. 오늘 새 쌍이 그 기기를 **기록상으로도** 끊었다.

⇒ 남은 일(**대표 결정**): 그 09-17 행은 사람에게 「기기 1대」라고 말하는데 아무것도 닿지
않는다. 해지(「끄기」)하면 화면이 정직해진다. **해지는 지우는 일**이라 안 했다.

### 이 창에서 또 밟은 것 — **값을 넣은 자리가 어긋났다**
VAPID 를 `gx-gunicorn-e`·`gx-celery-e` 에 넣고 단추를 눌렀더니 화면이
「VAPID 환경변수가 비어 있습니다」라고 했다. 브라우저가 부르는 `localhost:8000` 은
그 통이 아니라 **gx-shell 안의 runserver**(어제부터 돌던 프로세스)였다.
`docker exec -d --env-file` 로 그 프로세스를 VAPID 와 함께 다시 띄우자 **503 → 200** 이 됐다.
⇒ 「부르는 자리」의 형제: **값을 넣는 자리도 틀린다.** 넣은 뒤 **그 값을 읽는 프로세스가
실제로 그것을 들고 있는지**(`/proc/<pid>/environ`) 본다.
⚠ **E 에게**: 이 runserver 는 내가 손으로 띄운 것이라 `gx_shell_bootstrap.py` 가
다시 띄우면 **VAPID 가 없다.** 그 스크립트가 금고 파일을 읽어야 한다.

### 되돌리기 · 삭제
옛 통 넷 살아 있음(`gx-gunicorn-e-old-2b` · `gx-celery-e-old-2b` · `gx-celery-e-old`) ·
비밀 담긴 임시 env 파일 **둘 다 지웠다** · **삭제 0건**.
⚠ 이 창에서 gx-shell 안에 **파이어폭스를 설치했다**(108 MB · 도달을 재려고).
되돌리려면 `/root/.cache/ms-playwright/firefox-1538` 을 지우면 된다. 제품과 무관하다.

---

## §6-2 창 4 — 조율자가 **브라우저를 독점하고 눌렀다**

### ① U온의 새 문 — **빚 ①을 갚았다**
```
누름   ▲ 경계  →  확인창 「등급 재판정」
나감   POST /api/dsm/events/4802/severity   200
화면   현재 등급  ■ 심각 → ▲ 경계   (새로고침 없이)
되돌림 ▲ 경계 → ■ 심각              (원래 값으로 · 사건은 다른 측정의 표본이다)
```
U온은 함수를 직접 불러 쟀고, **URL·메서드·`auth=`·테넌트 관문은 그 경로로 한 번도 안
지나갔었다.** 이제 지나갔다. 「누른 뒤」 네 칸이 다 섰다.

### ② U56 의 시드 계정 — ⚠ **「영어 줄 0」은 참인데, 분모가 0 이다**
`gxseed_role_order` 로 라이브에 들어가면 **`/profile` 로 떨어진다.**
사이드바 **0줄** · 영어 줄 **0** — 그런데 **사이드바가 아예 없어서 0** 이다.
그리고 그 사람이 실제로 보는 첫 화면의 본문은 이렇다:

    Personal Information · Add Profile Picture · Email * · ID * · First Name * ·
    Last Name * · Phone Number · Home Screen Setting * · Theme Dark Light ·
    Password Settings · Change Password · Address * · Search address · Language

**100 % 영어다.** 메뉴에서 영어를 치운 것은 맞지만 **사람이 서는 자리는 아직 영어**다.
⇒ **분모 0 인 초록은 초록이 아니다.** U56 의 ORM 수(121→0)는 **상한**으로 남고,
「누른 뒤」로는 **아직 안 선다.** 다음 일은 「메뉴를 비우기」가 아니라
**「그 사람이 떨어지는 화면을 우리 말로 만들기」**다. 턴 AF.

### ③ 온보딩 ko-KR — **34.0/48 · 회색 0** [정본]
| 역할 | 이번(자격 셋) | 지난 회차 | 갈림 |
|---|---|---|---|
| U1 | 5.5/8 | 5.5 | ±0 |
| U2 | 3.5/8 | 3.5 | ±0 |
| U3 | 6.0/8 | 6.0 | ±0 |
| U4 | 5.0/8 | 5.0 | ±0 |
| U5 | 7.5/8 | 7.5 | ±0 |
| **U6** | **6.5/8** | 4.0 | **+2.5** |
| **합** | **34.0/48** | 31.5 | **+2.5** |

움직인 것은 **U6 넷뿐**이고, 그것은 어제 조율자가 안 넘긴 자격이다. **회색이 0 이 됐다.**
⇒ **로캘이 수를 안 움직인다**가 이제 깨끗한 회차로 확인됐다: 영어 34.0 · **ko-KR 34.0 · 회색 0**.
⚠ 지시서 §0 목표는 **≥36** 이었다. **34.0 — 못 닿았다.**

#### ★ 제품은 한 행 더 서 있는데 판정기가 못 본다 — **손으로 더하지 않았다**
`U2#3` 은 여전히 빨강이다. 판정기의 술어는 **단추 자신이 「등급」이나 `severity` 라는
글자를 달고 있을 것**을 요구하는데, U온이 지은 단추는 「▲ 경계」이고 「등급 재판정」은
**옆 제목**이라 안 걸린다. 그런데 그 판정기가 **제 주석에 답을 미리 적어 뒀다**:

> 「0 이면 ○ … **1 이상이면 그것이 새 사실이고 그때 술어는 [서버 기록] severity 변경
>  + 감사 행 1 이 된다**」

그 「새 사실」이 오늘 왔고(위 ①), 술어는 아직 안 바뀌었다.
⇒ **34.0 + 1 = 35.0 을 적지 않는다.** 판정기를 고쳐서 올린 수가 아니라 **고친 뒤 다시
잰 수**가 정본이다. 새 술어는 지금 것보다 **더 세다**(서버 기록 + 감사 행) — 약화가 아니다.
**K+Q 의 파일이고 턴 AF 다.**

### ④ 「누른 뒤」 48 — **31 → 32** (빨강 2 · 회색 14)
올라온 한 행은 `U2#3` 이다 — 어제는 「'되돌리기 (사유 필수)'를 화면에서 못 찾았다」로
회색이었고 오늘 초록이다. 빨강 둘은 그대로: `U1#11`(요청은 나갔는데 `verdict` 가 그대로) ·
`U6#4`(구독 요청 뒤 `total` 이 1 → 1).
⚠ 곁의 표 「설정 5」는 여전히 **0/5 · 회색 5**(증거가 낡았다) — 48 밖의 표라 분모에 안 섞는다.

### ⑤ ★★ **U56 이 물어온 자리에 이제 잰 수가 있다 — 364자리 중 0건**
U56 이 「`_has_credentials` 가 헤더 **값을 검증하지 않는다**」고 적었다. 사실이고,
**소스가 이미 그 이유를 적어 뒀다**(유효성까지 보면 인증기가 둘이 되고, 둘이면 갈린다).
그러나 **그 경계가 얼마나 넓은지는 아무도 안 쟀다** — 턴 P 의 탐침은 **익명**(헤더 0)으로만
쟀다. 그래서 `scripts/probe_fake_bearer.py` 를 지어 **읽기 면 전수를 두 번** 때렸다
(익명 한 번 · **없는 토큰** 한 번):

```
[FAKE-BEARER] [입력] 읽기 자리 364 · 16.5초
[FAKE-BEARER] 가짜 헤더로 **자료가 나온 자리 0** (그중 익명으로는 막히던 자리 **0**)
[FAKE-BEARER] 초록 — 관문의 경계가 넓어도 그 뒤가 서 있다
```
**실계정·실토큰을 쓰지 않았다** — 로그인을 안 하므로 남의 세션을 빼앗지도, 잠금 계수기를
올리지도 않는다. ⇒ U56 의 물음은 **가정이 아니라 0 이라는 수로** 닫혔다.

### ⑥ 안 한 것 — **첫 10분 화면 12**
창 4 의 시간에 안 들어갔다. **0/12 그대로**이고, 이 턴에 안 쟀다고 적는다.

### ★ 조율자 오판 셋 — 전부 **추측한 낱말** 탓이다
1. **배치를 안 하고 눌렀다.** 「카드가 없다」를 봤는데 라이브 번들이 옛 것이었다 —
   「배치 전 본다 금지」가 정확히 이 자리다. 배치하니 카드가 있었다.
2. 등급 낱말을 「경고·정보」로 **추측**했다. 사전은 `severity.ts:22` —
   **심각 · 경계 · 주의**. 못 눌렀다.
3. 확인창 단추를 「확인」으로 찾았다. 실제는 **okText='등급 재판정'** 이라,
   한 회차가 **「눌렀는데 아무것도 안 나갔다」**로 보였다 — P-118 이 태어난 바로 그 모양인데
   **이번엔 제품이 아니라 탐침이 낸 거짓 빨강**이었다.
⇒ **새 불변**: **화면의 낱말은 사전에서 읽는다.** 「머리글은 늙는다」의 사촌이고,
  틀리면 **제품의 결함으로 읽히는 거짓 빨강**이 난다 — 거짓 초록만큼 나쁘다.

### 창 4 에서 조율자가 **잴 것** (차선이 브라우저를 안 잰 몫)
1. **U온의 새 문** — `gxseed_u2_manager` 로 `/dsm/events/4802` → 「등급 재판정」 단추 →
   새로고침 없이 배지가 서버 값으로 바뀌는가. ★ **HTTP 로도 두드린다**(함수 호출로만 잰 빚).
2. **U56 의 시드 계정** — `gxseed_role_order` 로 3002 로그인 → 최소 메뉴 · **영어 줄 0**.
3. **ko-KR 온보딩 재측** — 자격 **셋 다** 넘긴다(`GX_SEED_ROLE_PASSWORD` · `GX_PROBE_PASSWORD` ·
   `GX_PROBE_USER`). 어제 셋째를 빠뜨려 U6 네 행이 회색이었다.
4. ★ **안 재진 수 하나** — `common/access_gate.py` 는 자격증명의 **있음**만 본다(설계 ·
   소스에 적혀 있다 · 인증기가 둘이면 갈린다). 그러나 **그 경계가 얼마나 넓은지는 아무도
   안 쟀다**: 턴 P 의 탐침은 **익명**(헤더 0)으로 331자리를 쟀지 **가짜 헤더**로는 한 번도 안 쟀다.
   ⇒ `Authorization: Bearer <없는 토큰>` 으로 같은 331자리를 두드려 **자료를 내는 자리 수**를 센다.
   0 이면 경계는 설계대로 좁다. 0 이 아니면 **그 수가 새 빨강**이고, 고칠 자리는 관문이 아니라
   **그 라우트들의 `auth=`** 다. ⚠ 실계정·실토큰은 쓰지 않는다.

---

## §7 안 한 것 — **미룸**

| 무엇 | 왜 | 어디로 |
|---|---|---|
| OPS-19 절 올리기 | 호출자 칸이 **어제 덤프엔 없다**(P-264) · 게이트가 정직하게 빨강 | **내일 05:00 KST 뒤** 게이트 재실행 → 초록이면 승격 |
| 카드 완료 시험 6 · U4③ | K+Q 시간 부족(「1~3 먼저」 지시대로) | 턴 AF |
| `api.ts` URL 합치기 | 소유표 밖이라 U온이 자제 | 턴 AF |
| 접속기록 보존기간 세 갈래 | 임자 배정이 먼저 | 턴 AF |
| `delegate_to_container` 거짓 주석 | 소유표 밖 | 임자 |
| A 영역 ① 천장 19 · F 캐시 A/A · `/dsm/me` | 지시서가 턴 AF 로 적었다 | 턴 AF |

---

## §7-2 ★ 이 턴이 **수를 거의 안 올렸다** — 그리고 그것이 오늘의 사실이다

| 수 | 목표 | 결과 |
|---|---|---|
| 상용 | ≥ 59 | **57.0**(안 움직임) |
| 온보딩 ko-KR | ≥ 36 | **34.0**(+2.5 · 전부 회색 해제분) |
| 「누른 뒤」 빨강 | 0 | **2**(그대로) |
| 웹푸시 도달 | 1 | **0**(못 쟀다) |
| 아침 | 2/10 | **1/10**(재부팅 없이는 못 오른다) |
| 전량 실패 | 0 | **0** ✅ |
| 기능명세 포함 | 수 | **27.5 %** ✅(회색에서) |

**올린 것은 둘뿐이고 나머지는 제자리다.** 숨기지 않고 적는다.

그런데 오늘 한 일의 대부분은 **수를 올리는 일이 아니라 자를 바로 세우는 일**이었다 —
게이트 하나가 두 물음에 한 색을 내던 것을 갈랐고(§2), 판정기 하나가 여섯째 부류를 몰라
셈을 거부하던 것을 폈고(§3-K+Q), 시험 하나가 벽시계 위치로 색이 바뀌던 것을 음성 대조와
함께 닫았고(§3-S), 계측기가 돌 때마다 청구를 늘리던 것을 끊었고(§3-K+Q),
관문의 경계가 얼마나 넓은지를 **364자리 0건**으로 처음 쟀다(§6-2 ⑤).

**자가 틀린 채로 오르는 수는 오른 것이 아니다.** 오늘 자를 고쳤으니 내일 오르는 수는
오른 수다 — 그 말을 하려고 오늘 수를 안 올렸다고 적는다.

---

## §8 대표께 — **한 마디**

1. **재부팅 한 번** — 오늘 중 하시면 「아침 2/10」이 섭니다(§5). 안 하시면 1/10 로 적습니다.
2. **push** — 창 3 커밋 뒤 한 마디 주시면 올립니다.
3. **09-28 가격 숫자 4** — 그날 한 줄이 CR1 을 0 → 1 로 올립니다.
4. VAPID 는 세종이 판정했습니다(P-259) — **대표 손 없음.** 발급하면 「발급했다」 한 줄만 올립니다.

**이 턴 삭제·되돌리기 결정: 0건.**
