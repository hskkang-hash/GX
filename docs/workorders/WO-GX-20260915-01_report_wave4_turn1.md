# WO-GX-20260915-01 · 파 4 턴 1 「영역 ①을 연다」 — 턴 Z 보고

발행 세종(CPO) 2026-09-21 · 수신 대표 · 작성 영실(CPM · 조율자)
차선 9(A 신설 · N · Q · U1 · U56 · U24 · U3 · S · F) + V 단독

---

## §0 첫 줄

```
turn-z 시작 · 2026-09-21 12:0x KST · HEAD == 9ed7cf0 (턴 Y · **아직 push 안 됨**)
컨테이너 10/10 · 계정 4/4 · 태그 pre-turn-z · 살아 있는 차선 0/9 → 9/9
```

★ **턴 Y 커밋 `9ed7cf0` 은 아직 원격에 없습니다.** push 는 대표 한 줄을 기다립니다.

---

## §1 대표 결정 여섯 — 받은 그대로, 집행한 그대로

```
① DOCKER 자동시작 켰
② 씨앗 그대로 지워라 (4·42·4·3)
③ 창 2a 09-23 12:00 열어라. C0 에서 물어라.
④ 준비되면 대표가 다시 얘기한다
⑤ 넓혀라 — 접속 로그는 빼고
⑥ 훈련 구역으로 울려라
```

★ 별지 따라하기는 ⑤를 「이번 턴은 표만 · 다음 턴 결정」이라 적었는데 **대표가 이미 답을 줬습니다.**
그래서 U56 이 **표를 만들고 그 표대로 집행**했습니다. ③ 의 일시는 별지 예시(14:00)가 아니라
**대표가 준 12:00** 입니다.

### ① Docker 자동 시작 — **세 겹 중 둘만 섰습니다** [조율자 실측]

| 겹 | 지금 | 근거 |
|---|---|---|
| ① 컨테이너 재시작 정책 | **10/10** | `verify_autostart.py` |
| ② Docker Desktop 자동 시작 | **섰습니다** | `settings-store.json` 의 `AutoStart = True` · HKCU `Run` 에 등록 |
| ③ `com.docker.service` StartType | **`Manual` · Stopped** | `Get-Service` |

`verify_autostart.py` → **rc 1 (빨강).** 판정기 문장 그대로:
> Run 키에 Docker Desktop 이 있어도 **엔진 서비스가 수동이면 올라오지 않는다.**

**이것이 09-18 아침 0/10 의 뿌리였습니다.** 제 셸로 한 번 더 시도했고 **또 거절당했습니다**
(`Set-Service … → Access is denied`). **못 한 것을 못 했다고 적습니다.**
⚠ **재부팅이 아직 없어 「아침 N/10」 실측은 여전히 없습니다.** G5 는 ◐ 그대로 · **다섯 턴째.**

**대표께**: 관리자 PowerShell 에서 아래 한 줄이 아직 필요합니다.
```
Set-Service com.docker.service -StartupType Automatic
```

### ② 씨앗 삭제 — **집행했습니다** [조율자 실측]

**지우기 전에 다시 쟀더니 수가 달랐습니다.**

| 지워지는 것 | 대표께 올린 수 | 집행 시점 실측 |
|---|---|---|
| DetectionEvent (probe) | 4 | **4** — 같은 4건(#295402~295405) |
| DeliveryRecord (CASCADE) | 42 | **61** (+19) |
| EventClip (CASCADE) | 4 | **4** |
| DsmReportRun (연결만 끊김) | 3 | **4** (+1) |

발송이 +19 인 까닭은 **씨앗이 그 사이 19건을 더 낳았기 때문**이고, 그것이 대표가 삭제를
승인한 사유 그 자체입니다. **되돌릴 수 없는 일이라 그냥 넘기지 않고 다시 여쭀고**
「**같은 4건 그대로 지워라**」를 받아 집행했습니다.

**스냅샷 먼저**: `evidence/P-184/deleted_probe_snapshot_20260921.json` · **73행**.
되돌리진 못해도 **무엇이 있었는지는 남습니다.**

#### ★ 집행하다 제품의 가드에 막혀 두 가지를 알았습니다

```
core.base.DependencyCheckError: This record cannot be deleted,
  because there is already data in use on the system
  [DEBUG can_delete] DetectionEvent has 1 dependencies: dsm report runs.event: 1 records
```

1. **dj-core 가 지우기 전에 의존성을 봅니다.** `DsmReportRun.event` 는 `SET_NULL` 인데도
   가드는 그것을 「쓰이는 자료」로 세어 **거절합니다.**
2. **이 표들은 SafeDelete 입니다.** 그냥 `delete()` 는 **소프트 삭제**입니다.

dj-core 는 §0.4 금지구역이라 고치지 않고, **대표가 승인한 결과 그대로**
(「보고 링크는 연결만 끊고 행은 둔다」)를 **명시적으로 먼저** 한 뒤 하드 삭제했습니다.

```
LINKS_BROKEN 4                  (DsmReportRun.event → None · 행은 남음)
DELETED 69  {DeliveryRecord: 61, EventClip: 4, DetectionEvent: 4}
probe 사건  4 → **0**            (all_objects 로도 0 — 소프트 아님)
발송      286 → **225** (−61) · 클립 30 → **26** · 보고 행 9 → **9**(안 줄었다)
```

**㉡ 감사 체인 재심** — `verify_evidence_chain.py --db` **rc 0**:
감사 행 3320(체인 3031 · **등재된 끊김 23**) · **새로 난 어긋남 0건** ·
머리 sha256 앞 16자 `2a10f5d596474534`. **삭제가 사슬을 상하게 하지 않았습니다.**

⚠ **다음 턴이 알아야 할 것**: 발송 분모는 이제 **225** 입니다. 턴 X 가 끝난 자리와 같은 수인데
그 사이 씨앗이 61 을 낳아 286 이 됐다가 돌아온 것입니다. **줄어든 것이 결함이 아닙니다.**
U3 가 ORM·HTTP 둘 다에서 **225** 로 재확인했습니다.

### ③ 창 2a — **2026-09-23(수) 12:00 · C0 에서 대표께 묻습니다**

절차서(`evidence/P-178/RUNBOOK_창2a.md`) 머리에 일시를 박았고, **여는 날 순서 여덟 칸 표**를
세웠습니다(①§1 네 조건 → ②§2 기준선 → ③A0 보존 5분 → ④A1~A4 → ⑤B → **⑥★★C0 멈춤** →
⑦C1~C6 → ⑧되돌림 표). ★되돌릴 수 없는 자리 셋을 머리에 표로 올렸고
**⛔C7 `NOSUPERUSER` 는 이번 창에서 안 한다**를 머리에 두 번 + 본문에 적었습니다.
**본문은 한 줄도 안 고쳤습니다.** 이번 턴에 창을 열지 않았습니다(역할 0 · GRANT/REVOKE 0줄 ·
지운 DB 0 · 재생성 컨테이너 0).

⚠ U56 이 순서 제약 하나를 새로 올렸습니다: **C0-ⓒ(시험 DB 회수)가 C4 보다 먼저** — 미루면 막다른 길.

### ④ 스테이징 · 운영 주소 — 대기 (**일곱 턴째**)

「준비되면 대표가 다시 얘기한다」. 없으면 RC-1(09-25)에 `CR ①` 과 `P-90` 을 **회색 채로** 냅니다.
**회색을 초록으로 적지 않습니다.**

### ⑤ 로거 넓힘 — **집행했고, 대표 조건이 실제 누출을 막았습니다**

```
전     2채널 ·   1,698행
후    15채널 ·   3,369행   (×1.98)
안 엶(접속 로그) 5채널 · 152,884행
표 전체        20채널 · 156,253행
```

★★ **「접속 로그는 빼고」는 취향이 아니라 울타리였습니다.** `security` 채널의 `note`
**3,012행**이 그대로 *"User (계정) logged in successfully from (IP)"* 인데, 화면의 `read_page` 가
`note` 를 **`reason` 칸으로 그대로 냅니다.** 넣었으면 **계정명과 IP 가 감사 화면에 올라갔습니다.**
`db` 채널에도 사설 IP 225행이 있었습니다. `db`+`jwt` = **127,807** 이 대표께 「약 13만」이라 보고된 그것입니다.

**넣은 13채널**: 보존기간 파기(1,049) · 대응 상태 전이(254) · 시험이 남긴 행(219) ·
알림 규칙 변경(84) · 현장 회신(36) · 일일 맥박(14) · LAW-07 청구 처리(7) · 그 외 소수.
**누출 울타리 전수**: 새로 여는 13채널의 `note`·`api_name`·`username` 전부 →
**IP 0 · 이메일 0 · 전화 0 · 주민번호 0 · 금지 패턴 6종 0.**

**0행이라 실물을 못 본 채널 넷은 안 넣었습니다** — 못 본 것은 안 넣습니다(D-400).

★ 판정기의 `API_PATHS` 에 **`/api/dsm/audit` 을 더했습니다**(5→6).
**안 더하면 넓힌 면을 한 번도 안 본 초록**이었습니다. 그 라우트를 11쪽 2,066행 전수로 훑어 0건.
⚠ **첫 판은 거짓이었습니다**(채널 2) — `--noreload` runserver 가 옛 모듈을 들고 있어 **재기동 뒤** 다시 쟀습니다.

### ⑥ OPS-15 훈련 구역 — **울렸습니다. 제 손이 아니라 운영 beat 가.**

| 무엇 | 수 |
|---|---|
| 훈련 구역 | **1** — `Zone #338 「[훈련] 안양천 시험구역」` |
| drill 카메라 | **3** — `#5356~5358` |
| **경보** | **1회** — ★ **규칙이 스스로 냈습니다** |
| 웹푸시 도달 | **0 · 회색** (아래 ⚠) |

```
[2026-09-21 10:30:30 WARNING] [OPS][PULSE] 군집 두절 1건 — 구역 1 · 카메라 3
태스크 결과 {'zones': 1, 'cameras': 3, 'created': 1}
직전 회차 zones 0  ← 구역이 0개였다 = 이 결정의 이유
직후 회차 created 0 ← 제 5분 재발 억제가 들었다
사건 #326411 · camera_cluster_down · warning   (전 DB 통틀어 이 1건뿐)
```

세 대의 맥박을 같은 순간에 끊고 **기다렸고**, 1분 주기 beat 가 잡았습니다.
**「1회」는 우리가 센 1 이 아니라 규칙이 스스로 낸 1 입니다.**
표식 `data_source=drill` — `exclude_probe` 뒤 **1(제품이 셉니다)** · `exclude_unbillable` 뒤
**0(청구 제외)** · 화면 `event_data_source` → **`"drill"`**.

**뒤처리**: 구역·카메라 행은 **남겼습니다.** 다만 카메라 셋을 **비활성**으로 돌렸습니다 —
끊긴 채 두면 **5분마다 두절이 영원히 쌓입니다**(하루 288건). 그 결과 「활성 3대 이상 구역」은
**다시 0개**이고 **절 상태는 `rule_only` 유지**입니다. **초록으로 올리지 않았습니다.**
재무장 절차를 보고에 적어 뒀습니다.

⚠ **웹푸시 도달은 재지 못했습니다 — 회색이지 0 이 아닙니다.** 제품 경로(훈련 채널)를 실제로
때려 **`PushSendNotConfigured`**: `GX_VAPID_*` 세 이름이 **비어 있습니다**(이름만 · 값 0).
턴 T 에 금고에 둔 VAPID 쌍이 지금 gx-shell·gunicorn·celery **어디에도 없습니다**(재생성 창에서
사라진 듯). 구독 기기는 **살아 있고**(1건) 발송 행 0 · **운영 채널 발송 0**.
**→ 대표 자리입니다**(§5 ⑦) — 창 2b 항목이고 `.env*` 실제 값은 대표 결정입니다.

---

## §2 차선 아홉이 연 것

### A (신설 · 영역 ①) — 설정 한 장 · **짓기 전에 서버에 물었다**

`/dsm/settings/rules` 한 화면(탭 3: 구역 · 임계값 · 등급규칙 · U5 기관 관리자) · **805줄** ·
**새 서버 문 0개**(문 여섯은 전부 이미 서 있던 것).

★ **이 차선의 값은 짓기 전에 잰 것입니다.** 돌고 있는 서버의 `openapi.json` 에서 인자 자리를
읽었더니 — **임계·등급규칙은 인자가 전부 질의(query), 구역만 `camera_ids` 가 본문**.
**짐작했으면 셋 다 422 로 죽었을 자리**입니다.

세 갈래 모두 **저장 응답을 안 믿고 새 GET 으로 다시 읽어** 그리고, 다시 읽은 값이 나타나는 칸에
표식을 박았습니다 — 「누른 뒤를 본 것만 초록」을 술어가 지킬 수 있게.

**안 한 것을 안 했다고 적은 것 셋** — 이 차선에서 가장 값진 부분입니다:

- **`F-03-c3` 폴리곤은 지금 문으로는 화면이 못 합니다.** [A/B 실측] `geometry` 가 **질의에 실리는
  객체 칸**이라 JSON 을 글자로 보내면 `422 Input should be a valid dictionary`.
  **칸을 그리지 않고, 못 하는 일을 누르기 전에 한 줄로 적었습니다.**
- **`F-14-c2` 오탐 줄이 없습니다.** 자동본의 절은 여섯이고 오탐이 없습니다.
  ⚠ 넘어가는 `axes` 에는 **`verdict` 축이 들어 있습니다** — 자료는 종이 앞까지 와 있는데
  종이에 안 그려집니다. **없는 것을 있다고 적지 않았습니다.**
- **GX-COPY 에 없는 말은 만들지 않고 청했습니다.**

★ 곁다리로 **row_map 이 이미 낡아 있던 것**을 찾았습니다: `F-02-c3`·`F-12-c6` 의 사유가
「부르는 화면 0」인데 **`CameraTuning.tsx`(턴 T)가 벌써 그 문을 부릅니다.**

### N (대장) — 「초록」 한 글자가 삼키던 다섯 부류를 **154절 전수**로 갈랐다

| kind | 절 수 |
|---|---:|
| `closed` | **80** |
| `unmeasurable` | **64** |
| `gate_only` | **4** |
| `ratchet` | **3** |
| `rule_only` | **3** |

★★ **대장이 내는 「구현 91」과 「closed 80」이 다른 수입니다** — 갈리는 11절이 이름을 얻었습니다.
영역 요약이 이제 `24/39 = 62% (래칫 0 · 규칙만 0 · 못 잼 16 · 게이트만 2)` 처럼 **뜻을 같이 냅니다.**

★★ **미측정 후보가 엄밀히 0건이었습니다.** 미측정 8절은 「판정기가 회색」인 게 아니라
**`gate:` 가 아예 비어 있었습니다 — 돌릴 판정기가 없습니다.**
세 턴째 수가 안 움직인 또 하나의 얼굴입니다. 넓혀서 「구현이 아닌데 게이트가 있는」 셋을 전부
돌렸고(SEC-18 rc1 · OPS-10 rc1 · OPS-13a rc2) **셋 다 색이 같아 안 바꿨습니다.**
합쳐 **판정기 17벌을 실제로 불렀습니다.**

`rule_only` 에 **지시서가 몰랐던 셋째**가 나왔습니다 — **`SEC-16` 나가는 웹훅 0개.**
그리고 ★ **`OPS-15` 의 초록은 합성 구역 위에서 난 것**이었습니다.

**P-209 셋 집행**:

- **UX-16 쪼갬** — 재측 결과 **술어 7종에 지도·맥박이 0개**(분모 0인 초록). `a/b/c` 로 쪼개
  b·c 는 미측정 · **`gate:` 를 안 붙였습니다.** 절 **152 → 154** · 영역 ⑦ **−0.34**.
- **LAW-01 잠김** — `blocker: CLIP_EXTRACTION` 은 `DA-05` 에 **이미 있어 새로 안 만들었습니다.**
  ★ **`gate:` 를 뗐습니다** — 안 떼면 「게이트 초록 · 대장 잠김」이 **없는 갈림**으로 매번 빨강입니다.
  영역 ⑧ **−0.50**.
- **ISO-03 래칫** — 점수 안 바꿈. 면제 461 앱별 표를 냈고 ★ **「매 턴 −N」의 분모는 461 이 아니라
  312** 입니다(149건 32% 가 §0.4 금지구역이라 못 고칩니다). 상위 셋이 우리 몫의 49%.

★ **N 이 스스로 「확인 못 함」 26절을 표시했습니다** — `구현`이고 증명 파일도 실재하지만
**무엇을 분모 몇으로 쟀는지 대장이 말하지 않는** 절. `closed` 80 의 33% 입니다.

### Q (판정기) — **3002 은 API 가 아니었다**

★★ **이 턴에서 가장 넓게 퍼져 있던 거짓의 뿌리입니다.**

> `api_token()`/`rows_u6` 가 **`web`(3002)으로 API 를 때리고 있었습니다.**
> 3002 은 `SimpleHTTP/0.6 Python/3.11.14` **정적 SPA 서버**라 POST 에 **501 · `text/html`** 을 냅니다.
> `r.json()` 이 터져 토큰이 `None` → **회색 5.**

네 가설(계정 · 속도제한 · UX-24 · 라우트 HTML)을 **전부 실측으로 기각**했습니다.
그리고 **`U6#12·#14·#15` 의 어제 빨강 셋도 같은 뿌리의 거짓 빨강**이었습니다.
**재측: 시도 8 · 초록 0→6 · 빨강 3→2 · 회색 5→0.** 남은 빨강 둘도 이름으로 댔습니다
(`WEBHOOK_SIGNING_KEYS` 빈 표 — 제품이 옳게 닫은 것 · 씨앗 없음 — 없는 사건에 404 는 옳은 답).

★ **차선 U3 가 온보딩 쪽에서 같은 자리를 독립적으로 찾았고 두 실측이 글자까지 일치했습니다.**

- **`GX_V_SESSION_ID` 닫음** — 이름만 넣고 잠금을 잡은 채 A/B:
  **켬 `rc 0 · 62/62` · `109/109`** / **끔 둘 다 `rc 2`**. 턴 Y 의 V 가 바이트로 댄 것과 정확히 일치.
- **`inbound_key` 닫음** — 지역 변수만 개명. 제품 경로·응답 칸·계약 헤더는 그대로.
  `gx-homonyms` 기준선 **182 → 178.**
- **P-213 결정 훅** — `decisions.yaml` 이 바뀔 때만 돌고 빨강만 막습니다.
  ★ **깨뜨려 확인했습니다**: `D-505` 의 `enforced_by` 를 일부러 비우니 **Failed rc 1 · D-505 를
  이름으로 지목** → **백업에서 바이트 동일로 복원**(`git diff` 0줄).
  ⚠ 작업 중 배운 것: **`decisions.yaml` 은 CRLF 라 텍스트 모드 재작성이 줄끝을 통째로 바꿉니다.**
- **`MEASURED=` 12 → 32/79** — 목표 달성. **분모는 전부 머리글 시점에 지금 센 것**(손으로 적은 수 0).
  ⚠ **회색은 그대로입니다** — `verify_gate_header` 는 아직 rc 2(**47개가 분모를 안 말합니다**).

⚠ **Q 가 못 한 것을 못 했다고 적었습니다**: **A 의 술어 명세를 한 줄도 집행 못 했습니다.**
`verify_click_completes.py` 와 `row_map.json` 을 **손대지 않은 채** 넘깁니다.
**그래서 A 가 지은 화면의 다섯 절에 술어가 없습니다 — 영역 ① 이 A 가 세운 만큼 안 움직입니다.**
그리고 **FC 48 전량을 다시 안 돌려 「회색 15 → N」의 N 을 말할 수 없습니다.**

### U1 — 훈련 구역 · 억제 키 · 인구조사

⑥ 은 §1 에. 그 밖에:

- **F-04 억제 키 확정** — `_suppression_key(event)` 신설로 키가 **한 곳**에 삽니다.
  계약: **`event_id` 가 아니라 「같은 스트림 + 같은 유형」.** **동작은 안 바뀌었고**
  바뀐 것은 **어디에 적혀 있고 누가 지키는가**입니다. 시험 넷을 **그대로 두고** 정본 하나
  (`test_f04_suppression_key.py` · 10 초록)를 세워 각 파일이 정본을 가리키게 했습니다.
  정본은 **없던 대조 셋**을 더 잽니다 — ★ **스트림이 다르면 안 접힌다(키의 첫 칸을 아무도
  안 재고 있었습니다)** · 훈련 발송은 억제 근거 아님 · **키가 한 곳인가**(손으로 다시 적으면 빨강).
  ★ **탐지기 대조**: 키에 `event_id` 를 일부러 넣으니 **2 failed** → 되돌리니 **10 passed.**
- **`--census` rc 0 · 0건** — 빨강의 정체가 「하나를 놓쳤다」가 아니었습니다.
  **표식 0 vs 카메라 이름 4** 였고, 그 4건은 **탐침이 아니라 훈련(drill) 사건**이었습니다.
  **카메라 이름 갈래가 훈련을 탐침으로 세고 있었습니다.** 갈래를 폐지하고 상수를 삭제했으며,
  0일 때 **왜 0인지**를 판정기가 말하게 했습니다.

★ **U1 이 새 구멍을 찾았습니다**: `metering.py::_cameras` 는 살아 있는 카메라를 전부 세고
**카메라에는 표식 칸이 없습니다** → **훈련 카메라가 청구 「카메라 대수」에 듭니다.**
**「drill 은 청구에서 빠진다」는 사건·발송에만 참이었습니다.**

### U56 — 로거 넓힘(§1 ⑤) · 계량 · 창 2a(§1 ③)

**계량 — 빚 문구가 틀렸고 진짜 빚은 따로 있었습니다.**
「커널 면이 없다」는 주석이 **틀렸습니다** — `K6.usage_snapshot` 은 계약에 **이미 이름으로**
있었고 없던 것은 **구현**이었습니다. 그래서 새 커널을 세우지도 K1·K2 에 얹지도 않고
**K6 를 구현**했습니다. `_alive` 를 지웠습니다(`exclude_soft_deleted` 와 **두 벌**이었습니다).

```
AST offenders  {_alive,_cameras,_users,_storage} → **[]**
A/B 일치  카메라 15/15 · 계정 30/30 · 저장 1,493,917,814/동일 · 파일 115/115
test_k6_feedback_kernel  23 passed
  (「NotImplementedYet 을 던진다」 시험을 실제 규칙 4건으로 교체)
```

⚠ **닫힌 것이 아닙니다.** 세 표에 표식 칸이 **없어서 거를 자리가 없습니다** —
**살아 있는 카메라 12대 중 8대, 계정 105개 중 11개가 우리 씨앗인데 지금도 청구에 듭니다.**
**이름으로 거르지 않았습니다**(추측은 청구 근거가 못 됩니다 · D-280). **아는 빚**으로 수까지 적었습니다.

### U24 — 감사 화면 · 검수 DOCX

★★ **제가 시킨 글자를 거절했고 거절이 옳았습니다** — §4 ② 에 적습니다.

★★ **종이가 훈련이라 말하지 않습니다.** drill 사건으로 1쪽을 실물로 뽑아 확인:
완성 HTML 에 **「훈련」 0건 · `drill` 0건**, `incident_report.py`·`docx_export.py` 에 grep **0건**.
**청구는 훈련을 빼는데 종이는 훈련이라 말하지 않습니다.**
⇒ **파 4 검수(09-24~25)에서 drill 사건을 고르면 안 됩니다.** 정본 DOCX 는 **실사건 4798** 로 뽑았습니다.

**DOCX 에서 가려야 할 칸(실제로 찍힌 값)**: 수신자 메일 5건 · `drill:webpush:…` **기기 토큰** ·
판정자 계정명 · 발행자 계정명 · 설치 주소(번지).
**앞의 둘은 가리기 전에 밖으로 내면 안 됩니다.**

U56 의 넓힘이 U24 의 턴 **도중에** 일어나 둘이 전후를 갈라 쟀습니다:

| 잰 시점 | 채널 | 화면이 읽는 행 | 사건을 가리키는 행 | **화면이 침묵하는 행** |
|---|---|---|---|---|
| 넓힘 **전** | 2 | 1,678 | 2 | **0** |
| 넓힘 **후** | 15 | 2,066 | 273 | **271** |

**넓힘이 빚을 만든 것이 아니라 덮여 있던 빚을 드러냈습니다** — 온보딩이 내려간 것과 같은 모양입니다.
U24 가 **채널 이름 15개**에 우리말을 줬고(모르는 채널은 **지어내지 않고** 「우리말 이름 없음」),
**180행은 못 줬습니다** — 고칠 자리가 `audit.py`(U56 파일)이라 규약대로 안 건드렸습니다.

### U3 — 온보딩

**M 축 회색 5행은 세 부류 중 어느 것도 아니었습니다** — 「문을 잘못 두드렸다」였고
판정기가 적은 사유(「자격이 없다」)가 **거짓**이었습니다. 넷을 실물로 세워 자격 가설을 기각했고
Q 와 **글자까지 일치**했습니다.

**편리성 #5 — 못 닫았고 사유가 넷에서 하나로 줄었습니다.** 배선은 완전하고(고칠 것 없음)
막은 것은 자료 하나: `occurred` 사건이 **0건**. U1 이 하나 심었는데 **그 사건의 발송이 0행**이고
M1 은 사건 목록이 아니라 **발송 목록**이라 시계가 시작도 안 합니다. 필요한 것은
**`occurred` 사건 + 그 사건에 붙은 발송 1행**입니다.

**「실패만」 갈래가 왜 v1.2 인가** — 서버엔 이미 `succeeded=false` 가 있고 그 1건을 정확히 냅니다.
**못 만드는 게 아니라, 배지의 거짓말 하나를 닫는 턴에 새 변수를 얹지 않으려고 미룬 것**입니다.

### S — LAW-08 종이 절차 · fail-closed

**사람 절차 1쪽** (`docs/design/GX-LAW-08_일일종이앵커_사람절차_v1.0.md` · 166행) — 다섯 칸 전부.
아무도 못 한 날은 **「안 한 날」로 적고 서명**합니다(빈칸 금지 — 「했는데 안 적었다」와
「안 했다」를 가르기 위해). 주말·공휴일은 **건너뛰지 않고** 다음 영업일에 **하루 한 줄씩 전부**.

★★ **절차를 쓰다 실제 함정을 잡았습니다.** 같은 날 조율자의 머리 해시와 S 가 잰 것이
**달랐는데**, 어긋남이 아니라 **새 감사 행이 붙을 때마다 머리가 움직이기 때문**이었습니다.
**머리 해시를 대조 칸으로 삼으면 매일 「다름」이 나고 매일 나는 빨강은 아무도 안 봅니다.**
종이에 박는 값은 반드시 **그날의 앵커**여야 한다고 §3 에 박았습니다.

**「안 맞으면」이 10단계**입니다. 핵심 둘: **①먼저 종이에 적고 서명한다(다시 뜨기 전에)** ·
**②아무것도 고치지 않는다** — 재계산하면 체인이 다시 초록이 되고 **증거가 사라집니다.**
그리고 **(ㄴ) 판정기는 초록인데 종이와 다름 = 전체 재계산의 흔적, 코드가 영원히 못 잡는 유일한 경우.**

**fail-closed 회귀** (`test_law08_guard_fail_closed.py` · 신설 · 19건 · 남의 시험 복사 0):
과녁이 다릅니다 — 형제 시험은 「가드가 지금 막는가」, 이쪽은 **「가드가 무너지면 우리가 알아채는가」.**

| 판 | 결과 |
|---|---|
| **양성** | **19 passed · rc 0** |
| **음성 A** (가드 머리에 `return True`) | **6 failed · rc 1** |
| **음성 B** (가드 머리에 `return`) | **5 failed · rc 1** |
| 형제 넷과 함께 | **96 passed · rc 0** |

★ **시험이 S 를 한 번 빨갛게 했습니다**: `SimpleTestCase` 뿐인 실행에서는 시험 DB 가 아예 안 서서
`default` 가 그때까지 **운영 이름**을 가리킵니다 — **그 창이 정확히 턴 X 에 219행이 샌 자리**입니다.
**고칠 것은 제품이 아니라 제 주장이었고**, 그 실측을 시험 안에 적었습니다.

### F — OPS-18 성장률 · 설정 캐시

```
총량   155,243행 · 339.6 MiB (힙 171.2 + 인덱스 113.0 + TOAST 55.4) · 인덱스 35개
하루   중앙값 11,930행 (최소 8,363 ~ 최대 25,243 — 폭 3.0배)
연환산 약 435만 행 · 9.3 GiB/년 [추정]
```

★★ **무엇이 먼저 아픈가에 답이 나왔고 답이 예상과 달랐습니다 — 디스크가 아니라 조회 지연,
약 197일 뒤.** LAW-08 전건 검증이 **61.85 ms** 인데 `logger_name LIKE 'guardianx.%'` 가 인덱스를
못 타 **3,344행을 얻으려 155,243행을 훑습니다** → **하루 +4.7 ms.**
★ **턴 Y 에 넣은 인덱스는 이 자리를 안 덮습니다.** 디스크는 100년 뒤 —
**「자람 = 디스크 문제」가 이 표에선 틀렸습니다.**

★ **「어떻게 7일치를 쟀나」가 이 차선의 값입니다.** `created_at` 은 이 표에 **없었고**
(칸 46개 전수 확인) `create_datetime` 이었습니다. 그리고 **행이 0인 날을 빠뜨리지 않았습니다** —
`GROUP BY` 는 없는 날을 안 내므로 09-14(기계가 안 돈 날)를 **0으로 적고** 통계에서 뺐습니다.
**0을 평균에 넣으면 「기계가 꺼진 날까지 섞인 값」이 됩니다.**

⚠ **인덱스 35개의 쓰기 값은 못 쟀습니다** — 「먼저 아픈가」 후보 하나가 **순위 3위가 아니라
순위 밖**으로 남았습니다. **증가율 상한이 선언 안 돼** 판정기 한 갈래는 **회색**입니다(지어내지 않았습니다).

**설정 캐시 — 증명됐습니다.** 근거 셋이 같은 방향:
정방향 **5/6 자리** · **A/A 대조 전부 못 증명**(옳은 널) · **맞바꿈 부호 전부 뒤집힘**.
앱을 안 믿고 **DB 쪽 스캔 수를 따로 셌고 그것도 같이 뒤집혔습니다**(9.12↔2.26 → 2.72↔8.28).
**「−8 만」이 아닙니다.** ⚠ **다만 제 잘못으로 경합 창에서 쟀습니다** — §4 ⑥.
---

## §3 정본 수 — **[조율자 실측]** (차선 보고가 아니라 제가 병합 뒤 다시 잰 것)

| 수 | 지금 [정본] | 턴 Y | 뜻 |
|---|---|---|---|
| **상용 /100** | **66.5** (절 154) | 67.4 (152) | **−0.9** — 아래 사유 |
| **손 안 도달율** | **66.9%** (91/136) | 68.7% (92/134) | 분모 +2 · 구현 −1(LAW-01 잠김) |
| **영역 ①** | **10/39 = 26%** | 10/39 | ★ **네 턴째 그대로** — 아래 ★★ |
| 절 수 | **154** | 152 | UX-16 을 셋으로 쪼갬(+2) |
| 닫힌 절 | **80/154** | (셈이 없었음) | ★ 처음 나온 수 |
| 단위 시험 | **1903 passed · 10 skipped · 0 failed** (14분 7초) | 1,866/0/10 | +37 |
| 커밋 | (아래 §9) | `9ed7cf0` · **아직 push 안 됨** | |

### 수가 내려간 사유 — **전부 대장이 늘거나 거짓이 걷힌 것입니다**

```
UX-16 쪼갬   절 152 → 154   영역 ⑦  −0.34   (술어 7종에 지도·맥박이 0개였다)
LAW-01 잠김  구현 92 → 91    영역 ⑧  −0.50   (초록의 뜻이 「잠겨 있다」였다)
                                    합 −0.84 ≈ **−0.9**
```

**대장은 줄지 않았습니다.** 내려간 수가 정본입니다.

### ★★ 영역 ① 이 **네 턴째 10/39** — 이 턴의 본 목표를 **못 이뤘습니다**

목표는 **≥ 15/39** 였고 차선 하나를 통째로 썼습니다. 화면은 **섰습니다**
(`/dsm/settings/rules` · 805줄 · 탭 3 · 새 서버 문 0개 · 다시 읽어 그리는 술어 자리까지).
**그런데 절은 하나도 안 닫혔습니다.**

**왜**: A 는 화면을 짓고 **술어 명세를 썼고**, 그 명세를 판정기에 넣는 것은 **Q 의 일**이었는데
**Q 가 한 줄도 집행하지 못했습니다.** `verify_click_completes.py` 와 `row_map.json` 은
**손대지 않은 채** 다음 턴으로 갑니다.

⇒ **조율자 판정**: 이것은 차선의 실패가 아니라 **제 배분의 실패**입니다.
「한 파일은 한 차선」을 지키려고 **짓는 사람과 재는 사람을 갈랐는데**, 그러면 **둘이 다 끝나야
절이 닫힙니다.** Q 에게는 그 위에 여섯 가지 본 일이 더 있었습니다.
**다음 턴에는 한 차선이 화면과 술어를 둘 다 쥡니다.**

★ 다만 **화면은 실재하고 컴파일도 섭니다**(아래). 다음 턴에 술어만 넣으면 닫힙니다 —
**일은 유실되지 않았고 명세는 원문 그대로 `turn-z/Q.md` 에 있습니다.**

### 영역별 — **이제 「초록」이 뜻을 같이 냅니다**

| 영역 | 절 | % | 갈래 |
|---|---|---|---|
| ① 기능 완결성 | 10/39 | 26% | 못 잼 29 · **도달 10 · 못 이음(회색) 22 · 끊김 0 · 잠김 7** |
| ② 보안 | 16/20 | 80% | 래칫 1 · 규칙만 1 · 못 잼 4 |
| ③ 데이터 격리 | 4/4 | **100%** | **래칫 1** (면제 461/576) |
| ④ 운영 | 16/23 | 70% | 규칙만 1 · 못 잼 7 |
| ⑤ 성능·확장 | 5/7 | 71% | 못 잼 3 |
| ⑥ 품질 | 11/12 | 92% | 래칫 1 · 규칙만 1 · 못 잼 1 |
| ⑦ 사용성 | 24/39 | 62% | 못 잼 16 · **게이트만 2** |
| ⑧ 법·인증 | 5/10 | 50% | 못 잼 4 · **게이트만 2** |

★★ **닫힌 절은 80/154 이고 나머지 74절의 「초록」은 닫힘이 아닙니다** —
래칫 3(늘지 않았다) · 규칙만 3(현장이 비었다) · **못 잼 64** · 게이트만 4(제목이 게이트보다 넓다).
**이 한 줄이 이 턴의 값입니다.** 세 턴 동안 「67.x」 하나로 말하던 것이 이제 갈라집니다.

### 판정기 — 조율자가 병합 뒤 직접 부른 것

```
verify_ga_readiness            rc 2 (회색)  상용 66.5 · 손 안 66.9
  회색 사유: PERF-04 가 exit 2 — 못 쟀다. 회색은 초록이 아니다
verify_ui_secrets --api        rc 0  렌더 문자열 0건 · **API 6개 라우트 0건**
  ★ U56 이 `API_PATHS` 에 `/api/dsm/audit` 을 더해 5 → 6. 안 더했으면 **넓힌 면을 한 번도 안 본 초록**
verify_evidence_chain --db     rc 0  새 어긋남 0 · 등재 끊김 23 · 머리 sha256 앞 16자 2a10f5d596474534
verify_autostart               rc 1  ← ★ 빨강. ③ 겹이 아직 `Manual` (§1 ①)
forbidden-zone                 rc 0  금지구역 변경 0건 (입력 80건)
pytest tests (전량)             rc 0  **1903 passed · 10 skipped · 0 failed** · 14분 7초
```

### 프런트 컴파일 — **우리 변경이 더한 오류 0건** [조율자 실측]

아무도 이것을 못 했습니다. `frontend/node_modules` 가 **호스트에도 컨테이너에도 없고**
gx-shell 엔 node 자체가 없습니다. A 만 `gx-fe-build` 안에 격리 복사하는 길을 찾았습니다.

```
gx-fe-build · node 18.20.8 · /tmp/zcheck 격리 · tsc -p tsconfig.app.json --noEmit
  총 오류 줄 **3234**  ← A 가 잰 기준선과 같다
  우리가 만진 넷 = 17줄, **전부 App.tsx 의 rj-core import(15~36 · 121)**
  SettingsRules.tsx **0** · AuditLog.tsx **0** · routes.ts **0** · 우리가 만진 줄 **0**
```

★ **첫 판은 `TSC_EXIT=127`** 이었습니다 — 심링크가 끊겨 **명령이 아예 안 돌았는데**
출력은 「우리 파일 **0건**」이었습니다. **종료 코드를 안 찍었으면 속았습니다.**
★ **침묵을 안 믿고 A/B**: 우리 줄 옆(269행)에 일부러 형 오류를 심으니 **17 → 18**.
**판정기가 우리 줄을 실제로 봅니다.** 심은 것은 컨테이너 안 사본이고 되돌렸습니다
(호스트 파일은 애초에 안 만졌습니다).

⇒ **이것은 형 검사이고 화면이 실제로 뜨는지는 V 가 잽니다.**

### 수가 **아직 어제 판**인 것 둘 — 옮겨 적지 않았습니다

- **FC 32/48** · **온보딩 24.5/48** — Q 가 `U6` 여덟 중 **다섯을 뒤집었고 셋은 빨강에서** 왔습니다.
  **48행 전량을 다시 돌려야 정본이 됩니다.** Q 가 창을 안 뺏으려고 안 돌렸습니다.
  **바뀔 수는 예상하지 않았습니다** — 예상은 측정이 아닙니다.
- **PR · CR** — 세 턴째 빠졌습니다. 산출기는 그대로 있습니다.

### `[차선 보고]` 로만 적는 것 — 제가 다시 안 잰 수

- **설정 캐시 7~11 ms (14~19%)** — **경합 창에서 쟀습니다**(§4 ④). **방향은 서고 크기는 정본 아님.**
- **감사 표 성장률** 155,243행 · 하루 중앙값 11,930 · **조회 지연이 약 197일 뒤 먼저 아픔**
- **로거 넓힘** 2→15채널 · 1,698→3,369행 · 접속 로그 5채널 152,884행 안 엶
- **계량** AST offenders `[]` · A/B 네 축 일치 · **씨앗이 청구에 드는 것 카메라 8/12 · 계정 11/105**


---

## §4 조율자 자진 오판 — **아홉**

턴 Y 에도 아홉이었습니다. 안 줄었고 **①과 ②는 지난 턴의 것과 같은 병**입니다.

### ① 판정기를 **거꾸로** 불렀다 — 두 턴 연속 「어디서 부르는가」

`verify_evidence_chain.py` 를 gx-shell **안에서** 불러 회색(exit 2 · 「컨테이너에서 검증을 못
돌렸다」)을 받았습니다. 이 판정기는 **호스트에서** 부르는 것입니다 — 제가 스스로 `docker exec`
로 위임하는데 컨테이너 안엔 docker CLI 가 없습니다.

**턴 Y 오판 ⑨ 는 `gate_servers.sh` 를 호스트에서 불러 「서버가 죽었다」고 읽은 것**이었습니다.
**정확히 반대 방향의 같은 병입니다.** 기억에 **방향을 둘 다** 박았습니다:
소스에 `docker exec` 가 있으면 **밖에서**, 없고 HTTP 를 때리면 **안에서**. grep 한 번이면 갈립니다.

★ 차선 N 이 **같은 뿌리를 독립적으로 찾았습니다** — 같은 판정기가 부르는 자리에 따라 색이 다른 것 셋
(`route_inventory`·`tenant_scope`·`retention_declared`). 뿌리는 **컨테이너의 `/repo/docs` 가 비어 있다**는 것.
셋 다 지금은 호스트에서 돌아 다행이고, 누가 「Django 필요하니 컨테이너로」 옮기면 **셋이 동시에 색을 잃습니다.**

### ② ★ 없던 삭제를 만들 뻔했고 **차선이 막았다**

제가 U24 에게 시켰습니다:

> 감사 화면의 **「대상 없음」 2행**에 「삭제된 사건 · 대표 결정 2026-09-20 · 스냅샷 P-184」를 적어라.

**그 2행은 삭제된 사건이 아니었습니다.** U24 실측:
`upper_report:set:999999999` · **막힘 · HTTP 404** · 「사건이 없거나 남의 테넌트다」 —
**없는 번호를 가리킨 막힌 시도**입니다. 적었으면 **일어나지 않은 삭제를 감사 화면이 증언**하게 됩니다.
감사 화면에서 그것은 가장 나쁜 종류의 거짓말입니다. **U24 가 거절하고 원래 글자를 뒀습니다.**

**제 잘못의 모양**: 「2행」이라는 **수만 보고 그 행이 무엇인지 안 봤습니다.**
**턴 Y 오판 ④(「연쇄를 한 건만 재고 전부라 적었다」)와 같은 병입니다** — 수를 옮겨 적고 실물을 안 봤습니다.

U24 는 「**스냅샷 P-184**」도 거절했습니다(GX-COPY 가 절 ID·내부 경로를 사용자 본문에서 금합니다).
⚠ 그런데 **막은 것은 게이트가 아니라 사전**이었습니다 — `verify_ui_copy.py` 의 절 ID 정규식이
숫자 **1~2자리**만 봐서 `P-184` 를 **못 잡습니다.** 판정기가 놓친 자리입니다.

### ③ 차선 지시문이 틀렸다 — 「프런트 빌드는 호스트에서 됩니다」

`frontend/node_modules` 가 **호스트에도 컨테이너에도 없고** gx-shell 엔 node 자체가 없습니다.
A·U24·U3 세 차선에 그렇게 적어 보냈습니다. A 가 알아채고 `gx-fe-build` 안에 격리 복사해
돌리는 길을 찾았고, **U24 는 `npx tsc` 의 빈 출력을 한 번 「오류 0」으로 읽을 뻔했습니다** —
**거짓 초록 직전이었고 제 지시문이 그 자리를 만들었습니다.**

### ④ ★ 창 표를 안 세웠다 — **그래서 경합이 났다**

지시서 §3 은 창을 **시각으로 못 박아** 줬습니다(F 10:00~10:30 · V 11:45~12:25 · 「겹침 0」).
**저는 그것을 안 쓰고** 「잰 시각을 적어 내라, 겹침은 내가 대조한다」고만 했습니다.
**대조는 사후이고 경합은 사전에 막는 것입니다.**

| 시각 | 누가 | 무엇 |
|---|---|---|
| 12:21~12:23 | U3 | 로그인 4회 |
| ~12:23 · ~12:28 | N | `verify_feature_reach` ★ **`--no-gates` 를 줘도 영역 ① 게이트는 불립니다** |
| 12:25 | Q | 온보딩 재측 |
| **12:29~12:40** | **F** | **설정 캐시 벽시계 창 · 로그인 4회** |
| 12:34:05 | 알 수 없음 | `gxseed_u5_sysop` 로그인 (F 것이 아님) |
| 12:35 · 12:50 | U56 | **게이트 서버 재기동 2회** |

**F 의 세션이 창 안에서 세 번 끊겼습니다.** 「창이 열려 있으면 재기동이 먼저 멈춘다」도 깨졌습니다.
U3 도 「Q 와 겹쳤다 — 해는 없었지만 **운이었다**」고 자진했습니다.
**두 차선이 같은 병을 자진했고 원인은 제 창 표입니다.**

**그런데 F 의 결론은 살아남았습니다 — 설계가 경합을 견디게 돼 있었습니다.**
한 팔로 재지 않고 **같은 순간 두 벌을 짝지어** 재고 쌍마다 순서를 뒤집었으며, **A/A 대조**
(두 팔 다 켬 → 전부 못 증명 · 옳은 널)와 **맞바꿈**(부호 전부 뒤집힘)을 붙였습니다.
앱을 안 믿고 **DB 쪽 스캔 수를 따로 셌고 그것도 같이 뒤집혔습니다.**
경합은 **양쪽 팔에 똑같이** 들어오므로 쌍차를 체계적으로 밀지 못합니다.

⇒ **조율자 판정**: **방향은 섭니다.** **크기(7~11 ms · 14~19%)는 `[차선 보고 · 경합 창]` 으로 적고
정본으로 삼지 않습니다.** 다시 재는 것은 다음 턴이고 **그때는 창 표를 제가 먼저 세웁니다.**

### ⑤ 내가 세운 새 쪽지 규약이 **첫 턴에 사고를 냈다**

턴 Y 의 오배송 4통(보낸 차선 파일에 써서 받는 쪽이 못 봄)을 고치려
「**쪽지는 받는 차선 파일에 쓴다**」를 세웠는데 **반대쪽 사고**가 났습니다 —
**받는 차선이 제 보고를 같은 경로에 저장하면서 쪽지를 덮었습니다.**

| 무슨 일 |
|---|
| S → `N.md` : N 이 제 체크포인트를 저장해 **쪽지가 사라짐** → S 가 덧붙이기로 되살림(331행 무손상) |
| A → `Q.md` : A 가 통째로 덮음 → U3 가 A 의 340행을 지우지 않고 뒤에 다시 붙임 |
| F → `N.md` : F 도 한 번 덮었다가 `git show :` 로 **즉시 복원** 후 덧붙임 |
| U24 → `N.md` : 덮어썼다가 이후 전부 `append` 로 바꿈 |

**뿌리 둘**: ① 받는 차선이 같은 파일을 **쓴다**(읽기만 하는 것이 아니다)
② 보내는 차선이 여럿인데 파일이 하나다(`N.md` 의 보내는 쪽은 **다섯**).

★ **A 가 「지어내지 않고 못 되살렸다고 신고」한 것이 옳았습니다.** 그 원문이 U3 의 보고에
남아 있어 **제가 옮겨 적었습니다.** 그리고 **12:41 에 쪽지 파일 전부를 저장소 밖에 떴습니다.**

**다음 턴 규약**: `checkpoints/turn-<턴>/쪽지/<받는차선>_from_<보낸차선>.md`.
받는 차선은 제 보고 파일만 쓰고 **`쪽지/` 폴더를 읽습니다.** 덮을 파일이 없습니다.
「받는 차선 파일에 쓴다」는 **받는 차선이 보는 자리에 쓴다**는 뜻이었지 같은 파일이 아니었습니다.

### ⑥ 스냅샷을 뜨며 Django 내부 자료 구조를 **추측으로 세 번** 고쳤다

`Collector.field_updates` 의 모양이 판마다 달라 세 번 틀렸습니다.
세 번째에 **표에 직접 물어서** 한 번에 됐습니다. **내부를 읽지 말고 표에 물어라** —
처음부터 그랬어야 했습니다.

### ⑦ 첫 컴파일이 `TSC_EXIT=127` 이었는데 출력은 「우리 파일 **0건**」이었다

심링크가 끊겨 **명령이 아예 안 돌았습니다.** 종료 코드를 같이 안 찍었으면
**U24 가 빠질 뻔한 그 자리에 저도 빠졌습니다.** 그래서 **일부러 오류를 심어 A/B** 했고
17 → 18 로 올라 **판정기가 우리 줄을 실제로 본다**는 것을 확인했습니다.


### ⑧ ★ 「게이트도 옳다」고 적었는데 **세 자리 중 하나에만 맞았다** — 차선이 고쳐 줬다

커밋이 분류 게이트에서 막혔을 때 제가 세 자리를 읽고 이렇게 적었습니다:

> 세 자리 전부 `select`·`explain` 이다. **F 도 옳고 게이트도 옳다** — SQL 이 이어붙여져 있어
> AST 가 못 읽으니 「모르면 세는 쪽에 둔다」로 쓰기로 센 것이고, 그 보수성이 이 게이트의 값이다.

**F 가 실물로 고쳤고 F 가 맞습니다.** 「못 읽어서」는 **267 한 자리에만** 맞았습니다:

```
220  SQL CREATE   ← 게이트가 SQL 을 **읽었고** 그래도 쓰기라 했다
254  SQL CREATE   ← 같다
267  모호          ← 이것만 「못 읽었다」
```

220 의 해석된 SQL: `select (create_datetime at time zone %s)::date … from logger_auditlogs t`
`verb hit: ['CREATE']` — **칸 이름 `create_datetime` 안의 `CREATE`** 입니다.
`verify_classification.py:220` 이 `any(v in sql.upper() …)` 로 **부분 문자열**을 봅니다.
**보수성이 아니라 오독입니다.**

**제 잘못의 모양**: 「셋 다 select 다」까지는 실물로 봤는데, **게이트가 왜 그렇게 말했는지를
게이트 소스로 확인하지 않고** 그럴듯한 이야기(「못 읽어서 모호하니 보수적으로」)를 지었습니다.
**판정기와 갈리면 먼저 고쳐 본다**는 규약은 「판정기가 왜 그러는지 실물로 본다」를 포함합니다.

★ 그리고 이것은 F 혼자의 일이 아니었습니다 — 저장소 전체에서 해석된 `execute` 의 쓰기 판정이
**부분일치 11건 → 단어경계 5건**, **달라지는 6건이 전부 SELECT** 입니다.
범인은 식별자 셋(`deleted` · `created_by_id` · `create_datetime`).
**지금 세어지는 쓰기의 55%가 거짓 양성이었습니다.**

★★ **F 가 고침을 검증까지 해 놓고 적용하지 않았습니다** — 제 차선 파일이 아니고, 그 파일엔
이미 Q 의 MEASURED 머리글이 staged 돼 있었기 때문입니다. **규약대로였고 옳았습니다.**
그리고 F 는 통과시키는 나쁜 길 셋(SQL 쪼개기 · 일부러 모호하게 되돌려 면제 받기 ·
등록부 참조한 척)을 **전부 적어 놓고 아무것도 안 했습니다.**

★ 조율자 방향을 따른 결과가 **더 막혔다는 것도 F 가 숨기지 않았습니다** — 267 을 읽히게 고치니
그 안의 `create_datetime` 이 보여 **쓰기 3 → 5건**이 됐습니다(단어경계로 세면 0건).


★★ **그리고 Q 가 제가 넘긴 F 의 수를 그대로 안 믿고 다시 쟀습니다** — 게이트 자신의
`_const_str` 로 세어야 게이트와 같은 뜻이 되기 때문입니다. **수가 달랐습니다:**

| | F 가 낸 수 | **Q 가 다시 잰 수** |
|---|---|---|
| 부분일치 | 11건 | **14건** |
| 단어경계 | 5건 | 5건 (같음) |
| 달라지는 것 | 6건 | **9건** |
| 성격 | 「전부 SELECT」 | **SELECT 6 · EXPLAIN SELECT 3** |

⇒ 세어지던 SQL 쓰기의 **9/14(64%)가 거짓 양성**이었습니다(제가 옮겨 적은 55% 가 아닙니다).
Q 는 9건을 **하나씩 본문 끝까지 읽었고**, 특히 `backfill_owner_apply.py:274` 는 `WITH …` 로
시작해 **진짜 쓰기일 수 있는 모양**이었는데 CTE 가 `SELECT` 로 끝나고 집힌 `DELETE` 는
`deleted` 칸이었습니다. **모양만 보고 넘기지 않았습니다.**
그리고 **양성 13건도 F 를 믿지 않고 제 손으로 다시 재서 13/13 · 놓친 것 0** 을 확인했습니다.

★★★ **이 오독이 오래 산 이유가 나왔습니다 — 그 게이트에 자기시험이 아예 없었습니다.**
**아무도 그 그물에 표본을 대 본 적이 없습니다.** Q 가 양성 13 · 음성 8 을 박고
**판정 전에 그물부터 시험**하게 했습니다(자기시험이 깨지면 판정을 안 하고 멈춥니다).
판정은 **한 함수**로 모았습니다 — 두 벌이면 자기시험만 통과하고 판정은 다른 답을 냅니다.

★ **Q 가 지시에 없던 걸음 하나를 했고 조율자가 받아들였습니다 — 래칫을 내렸습니다(113 → 110).**
거짓 양성이 걷히자 게이트가 스스로 시킨 것입니다. **올린 것이 아니라 내린 것**이고 사유가 옳습니다:
**거짓 빚을 두면 그만큼이 슬랙이 되어 진짜 쓰기가 들어와도 조용합니다.**

### ⑨ 없는 게이트 이름을 줬다 — **rc 2 를 「통과 아님」으로 읽을 뻔한 자리**

F 에게 `bash docs/agent/verify_gates.sh --gate classification` 을 돌려 보라고 했는데
**`classification` 은 그 목록에 없는 이름**입니다(게이트 21개 확인). **rc 2** 가 났고
그것은 **아무것도 안 잰 회색**입니다. 이 검사는 pre-commit 훅으로만 섭니다.

**턴 Y 의 「`--all` 은 없는 플래그」와 같은 병**입니다 — 있는 줄 알고 준 이름이 회색을 내고,
그 회색이 색처럼 보입니다. F 가 **「rc 2 를 통과 아님으로 읽으면 안 된다」고 먼저 적어** 줬습니다.

---

## §5 대표 자리 — 일곱

| # | 무엇 | 지금 |
|---|---|---|
| ① | **Docker ③ `com.docker.service`** | **다섯 턴째.** 관리자 PowerShell 한 줄 + **재부팅 1회** → 그 다음 아침에 N/10 이 처음 생깁니다 |
| ④ | **스테이징 1대 · 운영 주소** | **일곱 턴째.** 없으면 RC-1(09-25)에 CR ① · P-90 을 **회색 채로** 냅니다 |
| ⑦ | ★ **VAPID 자격 셋이 사라졌습니다** | 훈련 웹푸시가 `PushSendNotConfigured` — 금고의 쌍이 gx-shell·gunicorn·celery 어디에도 없습니다. **자격만 다시 실으면 그 경로 그대로 1이 납니다.** ⚠ **새 쌍을 만들면 기존 구독 1건이 죽습니다**(기기에서 재등록 필요). 창 2b 항목 |
| ⑧ | ★ **씨앗이 지금도 청구에 듭니다** | 카메라 12대 중 **8대** · 계정 105개 중 **11개**. 세 표에 표식 칸이 없어 거를 자리가 없습니다. **이름으로 거르지 않았습니다** — 추측은 청구 근거가 못 됩니다. 칸을 더할지가 대표 자리입니다 |
| ⑨ | ★ **종이가 훈련이라 말하지 않습니다** | 사건 1쪽에 「훈련」 0건. **파 4 검수에서 drill 사건을 고르면 안 됩니다.** 배너 한 줄이면 닫힙니다 |
| ⑩ | **검수 DOCX 에 가릴 칸 다섯** | 수신자 메일 · **기기 토큰** · 판정자·발행자 계정명 · 설치 주소. **앞의 둘은 가리기 전에 밖으로 내면 안 됩니다** |
| ⑤ | 감사 로거 — **집행 끝** | 대표 조건 「접속 로그는 빼고」가 **실제 누출을 막았습니다**(계정명+IP 3,012행) |

그 뒤: 창 2b(VAPID · SMTP 5 · 시험 DB 회수 3) · 가격표 숫자 · 채널 계약서(CR ⑦).
---

## §6 세종 판정 청함 — 넷

### ① `MEASURED` 회색 47 — **「분모를 안 말하는 초록」을 언제부터 빨강으로 볼 것인가**

Q 가 12 → **32/79** 로 올렸지만 `verify_gate_header` 는 여전히 **rc 2** 입니다 —
**47개가 무엇을 분모 몇으로 쟀는지 말하지 않습니다.** P-204 는 「`exit 0` 은 이 호출이 통과일
뿐」이라 했고, 그 말이 옳다면 **47개의 초록은 지금 아무것도 보증하지 않습니다.**
회색으로 둘 것인지, 기한을 두고 **빨강으로 승격**할 것인지 판정을 청합니다.

### ② `rule_only` 셋 — **점수에서 어떻게 셀 것인가**

`OPS-15`(활성 구역 0 · 훈련으로 울렸음) · `QA-12`(7일 4건) · **`SEC-16`(나가는 웹훅 0개 · 이번 턴 새로 나옴)**.
셋 다 **규칙은 섰고 현장이 비었습니다.** 지금 점수는 이것들을 **구현으로 셉니다.**
「분모 0인 초록은 초록이 아니다」를 여기에도 적용하면 **상용 수가 또 내려갑니다.**
적용할지, 적용한다면 언제부터인지 — **점수식 변경은 판정기 목록 갱신과 한 커밋**이어야 하므로
세종 판정 없이는 안 건드렸습니다.

### ③ `closed` 80 중 **「확인 못 함」 26** — 이 26을 어느 부류로 셀 것인가

N 이 스스로 표시했습니다: `구현`이고 `proof` 파일도 실재하지만 **note 가 비고 gate 도 없어
무엇을 분모 몇으로 쟀는지 대장이 말하지 않는** 절. **closed 80 의 33%** 입니다.
`closed` 로 두면 「확인했다」가 되고, `unmeasurable` 로 내리면 **상용 수가 크게 내려갑니다.**
**내려간 수가 정본**이라면 내려야 합니다 — 판정을 청합니다.

### ④ **훈련은 청구에서 빠지는가** — 사건·발송에만 참이었습니다

U1 과 U56 이 **각자 독립적으로** 같은 것을 찾았습니다:
`metering` 의 카메라·계정·저장 셋은 **표식 칸이 없어 거를 자리가 없습니다.**
살아 있는 카메라 12대 중 **8대**, 계정 105개 중 **11개**가 우리 씨앗인데 **지금도 청구에 듭니다.**
「drill 은 청구에서 빠진다」는 **사건·발송에만 참**이었습니다.
표식 칸을 더하는 것은 **마이그레이션 + 세 표**이고 청구 근거를 바꾸므로 판정을 청합니다.
(**이름으로 거르지 않았습니다** — 추측은 청구 근거가 못 됩니다 · D-280.)

---

## §7 다음 턴으로 넘기는 것

### 한 차선이 **둘 다 쥐어야** 닫히는 것 셋

1. **감사 화면 180행의 침묵** — 고칠 자리는 `audit.py`(U56) 하나인데 그리는 자리는
   `api_u24.py`(U24)입니다. U56 이 `event_ref` 칸까지는 실어 뒀고(**292행이 값을 가짐**)
   **아직 아무것도 안 그립니다.** 둘을 갈라 놓으면 또 못 닫습니다.
2. **A 의 술어 다섯 + UX-16 술어 둘** — A 가 명세를 썼고 **Q 가 한 줄도 집행 못 했습니다.**
   화면은 섰는데 **술어가 없어 절이 안 닫힙니다.** 명세는 `turn-z/Q.md` 에 그대로 있습니다.
3. **종이의 「훈련」 배너** — `incident_report.py::build_html` 한 줄이고 **필요한 값은 이미 손에
   있습니다**(`EventView.track_id`). U1·U24 둘 다 자기 파일이 아니라 안 건드렸습니다.

### 재야 할 것

- **FC 48 전량 · 온보딩 48행 전량** — Q 가 U6 여덟 중 다섯을 뒤집었고 셋은 **빨강에서** 왔습니다.
  **대장에 옮기기 전에 다시 돌려야 합니다.** 바뀔 수는 **예상하지 않았습니다.**
- **설정 캐시** — 경합 없는 창에서 다시. 그리고 **동시 10(포화)** 에서.
- **인덱스 35개의 쓰기 값** — 「먼저 아픈가」 후보 하나가 아직 **순위 밖**입니다.
- **LAW-08 조회 지연** — `logger_name LIKE 'guardianx.%'` 가 인덱스를 못 탑니다. **하루 +4.7 ms.**

### 고칠 것

- `verify_ui_copy.py` 절 ID 정규식이 **숫자 1~2자리만** 봅니다(`P-184` 를 못 잡음)
- `measure_onboarding_t.py` 의 `PROBE_CAMERA_HINT` import (이번 턴 Q 가 고치는 중)
- `verify_route_inventory`·`verify_tenant_scope`·`verify_retention_declared` — 컨테이너의
  `/repo/docs` 가 비어 있어 **부르는 자리에 따라 색이 다릅니다.** 지금은 호스트에서 돌아 다행이지만
  누가 「Django 필요하니 컨테이너로」 옮기면 **셋이 동시에 색을 잃습니다.**
- **속도 제한** — 문서는 「5회/분/IP」인데 Q 의 관측은 **4번째부터 429**. 한 판만 본 것이라
  판정이 아니라 관측으로 적습니다.

### 자료가 없어 못 재는 것

- **편리성 #5** — `occurred` 사건 **+ 그 사건에 붙은 발송 1행**(수신자 = 재는 계정)이 필요합니다.
  U1 이 사건은 심었는데 **발송이 0행**이라 M1(발송 목록)에서 시계가 시작도 안 합니다.
- **W1 「미처리 ≥1」** · `WEBHOOK_SIGNING_KEYS` 빈 표 — Q 가 창을 안 뺏으려고 미뤘습니다.

---

## §8 불변 — 이 턴에 새로 배운 것

기존 전부 유지 + 이 턴이 실제로 겪어서 아는 것:

- **판정기는 부르는 방향이 반대다** — 서버를 때리는 것은 **gx-shell 안**, `docker exec` 로
  스스로 위임하는 것은 **호스트 밖**. 소스에서 `docker exec` 를 grep 하면 1초에 갈립니다.
  **두 턴 연속 이 방향을 틀렸습니다.**
- **창은 조율자가 시각으로 나눠 준다 — 차선이 고르지 않는다.** 창을 여는 차선은
  **열기 전에 「지금 로그인한 사람 있나」를 감사 표로 한 번 본다.**
- **쪽지는 `쪽지/<받는차선>_from_<보낸차선>.md` 로 가른다** — 받는 차선이 제 보고를 저장하며
  쪽지를 덮는다.
- **수를 옮겨 적기 전에 그 행이 무엇인지 본다** — 「2행」이라는 수만 보고 시키면
  **없던 삭제를 만든다.**
- **종료 코드를 출력과 함께 찍는다** — `TSC_EXIT=127` 인데 출력은 「0건」이었다.
  **안 돈 명령이 초록으로 보인다.**
- **머리 해시는 매일 움직인다** — 대조 칸으로 삼으면 매일 빨강이 나고 **매일 나는 빨강은
  아무도 안 본다.** 박을 값은 **그날의 앵커**다.
- **행이 0인 날을 빠뜨리지 않는다** — `GROUP BY` 는 없는 날을 안 낸다.
  **0을 평균에 넣지도 않는다**(기계가 꺼진 날이 섞인다).
- **넓히면 덮여 있던 빚이 드러난다** — 그것은 새로 생긴 빚이 아니다(감사 화면 0 → 271 ·
  온보딩이 내려간 것과 같은 모양).
- **「청구에서 빠진다」는 어느 표에서 참인지 묻는다** — 사건·발송에선 참이고 카메라·계정에선
  거짓이었다.
- **내부 자료 구조를 추측하지 말고 표에 묻는다.**

---

## §9 커밋 · push

```
85 files changed · 14,111 insertions(+) · 382 deletions(-)
훅 전부 돌았다 — `--no-verify` 안 썼다
브랜치 turn-q · 작업본 깨끗
```

**턴 Y 커밋 `9ed7cf0` 과 이 턴 커밋 둘 다 아직 원격에 없습니다. push 는 대표 한 줄을 기다립니다.**

### 커밋 자리에서 막힌 것 셋 — 전부 옳게 막았습니다

1. **`gitleaks`** — Q 가 A/B 증거를 적으며 `v_lock` 세션 id **32자를 통째로** 남겼습니다.
   비밀은 아니지만 「이름·길이·앞 12자까지만」 규약이 있어 **잘라 적었습니다.**
   게이트를 봐준 것이 아니라 규약대로 고쳤습니다.
2. **`gx-classification`** — §4 ⑧ 에. 게이트가 **칸 이름을 동사로 읽고 있었습니다.**
3. **`end-of-file-fixer`** — 새 증거 JSON·HTML 의 줄끝. 훅이 고쳤고 다시 담았습니다.

### ★ 메시지만 고치는 `--amend` 는 이 훅 묶음을 통과할 수 없습니다

제가 커밋 메시지를 셸로 넘기다 `COMMIT_EDITMSG` 를 망쳐 **턴 Y 메시지로 커밋됐고**,
`--amend` 로 고치려니 **변경 파일이 0건**이라 금지구역 게이트가

```
[입력] 0건 — 0건 사유: 작업 트리가 베이스라인과 같다. 금지구역 판정의 대상 자체가 없다
WAIT  볼 대상 0건 — 대기          (exit 2)
```

를 냈습니다. **분모 0이라 판정 자체가 없는 것이고 게이트가 옳습니다.**
`--no-verify` 는 금지이므로 **이 §9 를 채워 실제 변경을 만들어** 함께 넣었습니다.
⇒ **다음 턴 규약**: **긴 커밋 메시지는 셸에 태우지 말고 파일로 쓴다.**
