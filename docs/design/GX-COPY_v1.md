# GX-COPY v1 — GuardianX 사용자 언어 사전

- 작성: Code(조율자) · 2026-09-26 (P-29 초안 · 적용은 차선 C 의 UX-20)
- 강제: `python scripts/verify_ui_copy.py` — 렌더 문자열에 대장 언어가 있으면 잔여로 센다
- 정본: **이 문서 하나다.** 화면 코드의 라벨 표(`frontend/src/features/dsm/copy.ts` ·
  `severity.ts`)는 이 사전의 코드 쪽 반쪽이고, 두 곳이 갈리면 **이 문서가 옳다.**

---

## 0. 왜 사전이 생겼나 — 사고 한 건과 결함 서른한 건

화면 24장을 한 장씩 읽었더니 결함 31건이 나왔고, 그중 11장의 뿌리가 하나였다:
**화면이 우리 개발 대장의 말로 말한다.** D-번호 · 절 ID · 백틱 경로 · 마크다운 원문 ·
영문 열거값 · 개발 주석이 사용자 본문에 있었다. 가장 나쁜 한 장은 계약 상대사명과
조항이 관제요원 화면에 문단으로 뜬 것이었다(P-27 로 닫음).

**이것은 화면을 만든 쪽의 잘못이 아니다.** 「사유를 화면에 적어라」 「출처를 표기하라」가
시킨 결과다. 정정한다:

> 사유 · 출처 · 판정 근거는 **관리자 패널과 「?」 아이콘 뒤**에 산다.
> 사용자 본문에는 **사용자 언어만** 쓴다.

## 1. 규칙 넷

1. **사전에 없는 새 문구는 만들지 않는다.** 없으면 사전에 먼저 넣고 쓴다.
2. **표시만 바꾼다.** 계약 스키마 값(`severity` 의 info/warning/critical,
   `response_state` 의 occurred/… )은 그대로 두고 **라벨에서 매핑**한다 —
   값을 바꾸면 계약이 바뀐다.
3. **없는 것은 없다고 적는다.** 다만 **어디에 없는지**는 적지 않는다 —
   파일 경로 · 커널 이름 · 표식은 사용자에게 뜻이 없고, 읽는 사람에게는 우리 서랍의 지도다.
4. **한 화면에 축은 하나.** 「대응」 「상태」 「판정」을 나란히 세우지 않는다 —
   사람이 어느 축을 보는지 모른다.

---

## 2. 지금 화면의 말 → 바꿀 말

| 지금 화면 | 바꿀 말 | 왜 |
|---|---|---|
| 위험 · 경고 · 정보 | **심각 · 경계 · 주의** | 계약 3등급(F-04)이 그 세 낱말이다 |
| 대응 「발생(미처리)」 / 상태 「신규」 / 판정 「—」 **세 열** | **처리 단계 한 열**: 미처리 → 접수 → 조치 중 → 종결 · **판정 배지**: 실제 / 오탐 | 축이 셋이면 아무도 안 본다 |
| 기각(상태) · 오탐(판정) | **오탐** 하나 | 같은 뜻 두 낱말 |
| 종결(으)로 | **종결** | 프로그램이 만든 조사는 사람의 말이 아니다 |
| 규칙대로 발송 | **알림 보내기** | 「규칙대로」가 무엇인지 화면 앞에서는 모른다 |
| Network Error | **사진을 불러오지 못했습니다 · 다시 시도** | 원문 오류는 사용자의 말이 아니다 |
| 오탐률 잴 수 없음(판정 0건 — 분모가 없다) | **아직 판정한 이벤트가 없습니다** | 분모 이야기는 관리자 자리 |
| 표시 7건(요청 상한 50건) | **7건** | 상한은 「?」 뒤로 |
| 패널 상태 data 0/0 loading 0/0 … | (사용자 화면에서 **제거** · 관리자 패널로) | 개발 계수기 |
| 「UX-17 …」 「UX-18 …」 「W1 단일 초점 —」 | **훈련 모드** · **카메라 일괄 등록** · **지금 처리할 것** | 절 ID 는 제목이 아니다 |
| `data_source = live` 칩 | **실운영 / 시드(검수용) / 훈련** 배지 (시드·훈련일 때만 표시) | 영문 열거값 노출 |
| 안내 상자의 개발 문단(백틱 · D-번호 · 커널 경로) | **「?」 아이콘 → 팝오버 한 줄**(사용자 언어) · 원문은 관리자 패널 | 뿌리 ① 그 자체 |
| 연계 상태: `waiting` — 〈사유 문단〉 | **연계 대기** 한 단어 + 관리자 전용 **「자세히」** | P-27 에서 닫음 |
| 「역할 매핑을 찾지 못해…(K3_ROLE_PRESET_MAP)」 | **화면 구성이 아직 정해지지 않았습니다. 관리자에게 문의하십시오.** | 설정 이름은 관리자 자리 |

## 3. 상태·등급 표 (정본)

| 계약 값 | 화면 표시 | 비고 |
|---|---|---|
| `critical` | **심각** | 빨강은 이 등급 전용 |
| `warning` | **경계** | |
| `info` | **주의** | |
| `occurred` | **미처리** | 처리 단계 1축 |
| `acknowledged` | **접수** | |
| `in_progress` | **조치 중** | |
| `closed` | **종결** | |
| 판정 `confirmed` | **실제** | 배지 |
| 판정 `rejected` | **오탐** | 배지 (「기각」을 쓰지 않는다) |
| 연계 `normal` | **연계 정상** | |
| 연계 `waiting` | **연계 대기** | |
| 연계 `down` | **연계 끊김** | |

## 4. 금지 — 사용자 본문에 쓰지 않는 것

- 결정 번호 `D-000` · 절 ID(`UX-00` · `SEC-00` · `F-00` · `P-00` …)
- 백틱(`` ` ``) · 마크다운 강조(`**`) — 렌더되지 않고 그대로 보인다
- 내부 경로 · 파일 이름 · 커널 이름 · 설정 상수 이름 · 감사 채널 이름
- 영문 열거값(`occurred` · `acknowledged` · `live` · `data_source=` …)
- 계약 상대사명 · 계약번호 · 조항 (P-27 · 별도 판정기 `verify_ui_secrets.py`)

## 5. 아직 사전에 없는 것

새 문구가 필요하면 **여기에 먼저 한 줄을 적고** 쓴다. 비어 있는 것이 정상이고,
비어 있지 않은 채로 오래 있으면 그것은 사전이 뒤처졌다는 뜻이다.

### 2026-09-05 추가 — 온보딩·관리 화면·현장 회신

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **처음이세요?** | 로그인 화면 아래 한 줄 | 첫 근무일의 사람이 스스로 시작하는 문. 「도움말」은 이미 아는 사람의 말이다 |
| **처음 시작하기** | 온보딩 화면의 이름 | 「매뉴얼」·「가이드」는 읽을 것을 말하고, 이것은 **할 것**을 말한다 |
| **카메라 주소 채우기** | 카메라 한 대의 설치 주소를 넣는 화면 | 「등록」은 새 카메라를 만드는 말로 들린다. 여기서 하는 일은 **빈 칸을 채우는 것** |
| **표 먼저 보기 / 채우기** | 위 화면의 두 단추 | 일괄 등록과 **같은 말**을 쓴다. 같은 규약을 두 화면이 다르게 부르지 않는다 |
| **현장 회신 — 본 것을 한 줄로** | 휴대전화 상세의 회신 칸 | 「보고」는 서식을 떠올리게 한다. 여기서 받는 것은 한 줄이다 |
| **회신 보내기** | 위 칸의 단추 | 동사꼴. 「전송」은 기계의 말이다 |
| **관리자 전용 화면입니다. 아래 표기는 아직 영문입니다.** | 인수 화면 위의 한국어 헤더 한 줄 | 남는 인수 화면을 **고치지 않고 감싼다**. 사실을 먼저 말하면 영문 표기가 고장으로 읽히지 않는다 |

### 2026-09-16 추가 — UX-31′ 네 문장 · 큐 배지 · 종결 확인 (턴 S · 차선 F·U1)

**★ 이 사전의 코드 쪽 반쪽은 `frontend/src/features/dsm/copy.ts` 의 v1.1 절이다.**
문구를 고칠 때 한쪽만 고치면 두 벌이 되고, 두 벌은 반드시 어긋난다 — 여기와 그 파일은
**같은 말의 두 자리**이지 정본과 사본이 아니다.

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **지원 요청** | 초점 큐 카드의 등급 옆 배지 | 현장이 **혼자 못 한다**고 말한 것이다. 「도움 필요」는 상태를 적고, 이것은 **요청**을 적는다 — 누군가 답해야 하는 말이다 |
| **종결 확인 — 현장이 조치를 마쳤다고 알린 사건** | 종결 확인 카드의 제목 | 제목이 「무엇을 하는 칸인가」를 말한다. 「완료 목록」은 이미 끝난 것처럼 들리는데, 여기서 끝내는 사람은 **관제요원**이다 |
| **배선 대기** | 회신이 아직 종류 표시를 달고 오지 않을 때의 표시 | ★ **「데이터 없음」과 가르는 말이다.** 회신은 오는데 우리가 아직 못 읽는 상태가 실재한다. 그때 「없습니다」라고 적으면 **거짓**이다 — 없는 것이 아니라 못 읽는 것이다 |
| **현장 회신에 「지원 요청」·「조치 완료」 표시가 아직 붙지 않았습니다.** (턴 T: 둘째 문장 「검수용 예시」는 카드가 실자료로 이어지며 뗐다) | 위 표시 아래 한 줄 | 이유 없는 빈 칸은 고장으로 읽힌다. 그리고 **검수용 예시를 예시라고 말하지 않으면 그것은 시드 화면을 현장 화면으로 읽히게 하는 일**이다(D-284) |
| **지원 요청 표시는 현장 회신이 그 표시를 달고 올 때 이 자리에 뜹니다.** | 회신은 오는데 분류가 0건일 때 큐 머리 | 위와 같은 가름을 큐 쪽에서 한 번 더 한다 |
| **조치를 마쳤다고 알려 온 사건이 없습니다.** | 종결 확인 카드가 비었을 때 | 이쪽은 **진짜 없음**이다. 앞의 「배선 대기」와 **다른 문장을 쓴다** — 같은 문장을 쓰면 둘을 가른 뜻이 사라진다 |

**UX-31′ 네 문장** — 실패 자리의 말은 `copy.ts::failureSpeech` **한 표**에서만 나온다:
①무슨 일 ②왜 ③지금 무엇 ④누를 것. 종전의 한 줄짜리 `failureHint` 도 이제 그 표의
②+③을 이어 붙여 쓴다 — **표를 두 벌로 두지 않기 위해서다.**

⚠ **없는 것(404)에는 「다시 시도」를 그리지 않는다**(`retryLabel: null`). 다시 눌러도
영영 같은 답이 오는 자리에 단추를 두면, 그 단추는 사람에게 **거짓 약속**을 한다.
| **드론·로봇 장비 등록 · 항공 고시보 · 비행 기록 분석 · 조사 프로파일 · 역할 관리 · 메뉴 관리 · 보고서 서식 · 영상 자료** | 위 헤더가 부르는 여덟 화면의 우리말 이름 | 화면이 무엇인지 우리말로 먼저 말한다 |

### 2026-09-05 턴 D 추가 — 월 모드 · 카메라 격자 · 단축키/소리 · 보관 기간 · 청구

★ **조율자가 먼저 넣는다.** 이 턴에도 차선 넷이 동시에 돈다 — 넷이 같은 사전 파일
끝에 각자 줄을 붙이면 충돌하고, 충돌한 사전은 「사전에 없는 말을 쓴 화면」을 만든다.
그래서 규칙 1(「없으면 사전에 먼저 넣고 쓴다」)을 **턴 머리에서 한 번** 지킨다.

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **월 모드** | 관제실 대형 화면의 이름 | 「Wall」·「벽면」은 물건 이름이다. 이 화면이 하는 일은 **멀리서 보이게 하는 것** |
| **자동 갱신 중** / **마지막 갱신 N분 전** | 월 모드 머리 한 줄 | 멈춘 화면은 「사건이 없다」와 구별되지 않는다. **갱신 시각을 화면이 스스로 말한다** |
| **단축키** / **단축키 안내** | 큐 화면 「?」 뒤 | 「핫키」·「키맵」은 우리 말이다 |
| **소리 켜기** / **소리 끄기** | 큐 화면 오른쪽 위 단추 | 상태가 아니라 **누르면 일어나는 일**을 적는다 |
| **소리가 꺼져 있습니다** | 음소거 중 배지 | 심각이 떠도 안 울리는 이유를 화면이 먼저 말한다 |
| **카메라 격자** | 여러 대를 한눈에 보는 화면 | 「멀티 스트림」은 기계의 말이다 |
| **자동 순회** / **순회 멈춤** | 격자 위 단추 | 동사꼴. 「로테이션」을 쓰지 않는다 |
| **응답 없음** | 죽은 카메라 타일 | 「오프라인」·「다운」은 영문이고, 「고장」은 **우리가 모르는 것을 단정**한다 |
| **마지막 응답 N분 전** | 위 타일 아래 한 줄 | 「없다」와 「언제부터 없다」는 다른 사실이다 |
| **영상 보관 기간** / **N일** | 설정 화면 · 고지 화면 | 「보존 주기」·「retention」을 쓰지 않는다 |
| **보관 기간이 지난 영상은 자동으로 지워집니다** | 위 값 아래 한 줄 | **적어 둔 것**과 **그대로 지우는 것**은 다르다 — 화면이 뒤엣것을 말한다 |
| **자동 분석 안내** | 로그인 뒤 첫 화면 · 알림 본문 꼬리 | 사람이 최종 확인한다는 사실을 **읽는 자리에서** 말한다 |
| **이 알림은 자동 분석 결과이며, 관제요원이 최종 확인합니다.** | 위 안내의 본문 | 법이 요구하는 것은 고지이고, 고지는 **닿는 자리**에 있어야 한다 |
| **열람·삭제 청구** | 청구 접수 화면의 이름 | 법정 용어를 사용자 말로 그대로 쓴다 — 이 낱말은 청구인이 아는 말이다 |
| **청구 접수** / **접수 번호** | 위 화면의 단추와 결과 | 「등록」은 새 자료를 만드는 말로 들린다 |
| **마스킹본 보기** | 청구 상세의 단추 | 무엇을 보여 주는지 이름이 먼저 말한다 |
| **원본 영상은 제공되지 않습니다** | 위 단추 아래 한 줄 | 안 되는 일을 **누르기 전에** 적는다 |
| **회신 기록** | 청구 상세의 이력 | 「처리 이력」은 우리 말이고, 청구인이 받는 것은 **회신**이다 |

#### 턴 D 병합 중 추가 — 차선 C1 이 청한 것 (조율자 등재)

★ 차선이 **만들지 않고 청했다.** 규칙 1(「사전에 없는 새 문구는 만들지 않는다」)이
지켜진 첫 사례다 — 판정기 `verify_ui_copy` 는 이 문구들을 전부 통과시킨다(대장 언어가
아니므로). **판정기가 초록인데도 청한 것**이고, 그것이 규칙 1 이 노린 자리다.

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **지도** | 월 모드 왼쪽 칸 | 「위치도」·「맵」이 아니다 |
| **지도에 표시할 위치가 없습니다** | 좌표가 0건일 때 | 「데이터 없음」은 우리 말이다. 없는 것이 **무엇**인지 적는다 |
| **위치를 아는 카드 N장 · 전체 M장** | 지도 아래 한 줄 | 「전체」만 적으면 지도에 안 뜬 것이 고장으로 읽힌다 |
| **그 밖 N장** / **N대** | 묶음 꼬리 | 「기타」는 분류의 말이다 |
| **불러오는 중입니다** | 세 칸 각각 | 칸마다 따로 말한다 — 한 칸이 느린 것과 전부 죽은 것은 다르다 |
| **불러오지 못했습니다. 아래는 마지막으로 받은 내용입니다** | 갱신 실패 시 | **낡은 값을 새 값인 척 그리지 않는다.** 멈춘 화면이 「사건이 없다」로 읽히는 것을 막는 문장 |
| **이 항목에 대한 권한이 없습니다. 다시 로그인해 주십시오** | 401·403 일 때 | 사용자가 **다음에 할 일**을 적는다 |
| **단축키 R — 다시 시도** | 실패 상자 안 | 마우스가 없는 화면이라 되살리는 길도 키여야 한다 |
| **숫자 키는 가장 급한 하나에만 듭니다** | 단축키 안내 | 안 먹는 자리를 **누르기 전에** 말한다 |
| **소리는 심각에서만 납니다. 묶인 카드는 한 번만 납니다** | 단축키 안내 | 「왜 안 울렸나」를 사후에 묻지 않게 한다 |
| **아래로** / **위로** | 단축키 안내의 J·K | 「다음」·「이전」은 목록의 방향을 안 말한다 |

### 2026-09-05 턴 E 추가 — 「보기」 · 킥 문장 · 버전 · 계량 · 파기 · 다중 세션

★ 조율자가 턴 머리에서 한 번 넣는다(동시 여섯 · 같은 이유).

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **미처리 보기** / **지난 12시간 보기** / **내 담당 보기** / **시스템 보기** | 목록 위 단추 넷 (P-51) | 「프리셋」은 우리 말이다. 누르면 무엇이 보이는지 이름이 먼저 말한다 |
| **GuardianX는 대응 시간을 잽니다.** | 로그인 화면 · 지금 처리할 것 상단 · 월간 1쪽 첫 줄 (P-52) | **세 자리에 같은 글자.** 제품이 무엇을 하는 물건인지 한 문장으로 말한다 — 세 자리가 다른 말을 하면 그것은 문장이 아니라 장식이다 |
| **버전 3d40cf6** | 모든 화면 하단 오른쪽 (P-59) | 고객 지원이 「지금 무엇이 떠 있나」를 묻는 자리. 그리고 **낡은 번들이 뜬 것을 사람이 눈으로 본다** |
| **이번 달 사용량** | 계량 화면의 이름 (OPS-16) | 「계량」·「미터링」은 기계의 말이다. 관리자가 알고 싶은 것은 **이번 달에 얼마나 썼나**다 |
| **카메라 대수** / **쓰는 사람 수** / **이벤트 수** / **보낸 알림 수** / **저장 용량** | 위 화면의 다섯 칸 | 다섯 다 셀 수 있는 것의 이름이다. 「좌석」·「활성 사용자」는 계약서의 말이다 |
| **표 내려받기** | 위 화면의 단추 | 「CSV 내보내기」는 형식을 먼저 말한다. 사람이 하려는 일은 **가져가는 것** |
| **보관 기간이 지난 영상 지우기** | 관리자 파기 화면 (P-57) | 「파기」는 법의 말이고 「삭제」는 되돌릴 수 있는 것처럼 들린다. **지우기**가 하는 일 그대로다 |
| **먼저 표로 보기** | 위 단추 앞의 dry-run | 벌크 등록과 **같은 말**을 쓴다. 같은 규약을 두 화면이 다르게 부르지 않는다 |
| **지운 뒤에는 되돌릴 수 없습니다** | 위 단추 아래 한 줄 | 안 되는 일을 **누르기 전에** 적는다 |
| **지운 기록** | 파기 이력 | 「감사 로그」는 우리 자리의 이름이다 |
| **다른 기기에서 로그인되었습니다** | 세션이 밀려난 화면 (UX-24) | 「세션 만료」는 원인을 안 말한다. 사람이 알아야 할 것은 **누가 밀어냈나**다 |
| **이 계정으로 함께 쓸 수 있는 기기 수를 넘었습니다. 가장 오래 켜 둔 화면을 닫았습니다.** | 상한 초과 시 안내 | 무엇이 일어났는지와 **무엇이 닫혔는지**를 같이 말한다 |

#### 턴 E 병합 중 추가 — Frontend Agent 가 청한 것 (조율자 등재)

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **버전 알 수 없음** | 커밋을 못 읽고 만들어진 빌드의 하단 표시 | **지어낸 해시보다 「모른다」가 낫다.** 이 자리에 그럴듯한 값이 뜨면 고객 지원이 그 값을 믿고 엉뚱한 코드를 뒤진다 |

### 2026-09-06 턴 G — 차선 C 가 **청한** 것 (조율자 등재 대기)

★ 규칙 1(「사전에 없는 새 문구는 만들지 않는다」)을 지켜 **먼저 적고 쓴다.** 턴 D·E 와
달리 이번 턴에는 조율자가 머리에서 넣을 겨를이 없었다 — 그래서 차선이 이 절을 열되
**「등재 대기」라고 적어 둔다.** 조율자가 읽고 고치거나 승인한다. 이 절이 없는 채로
화면에 새 문장을 넣는 것이 사전을 뒤처지게 하는 유일한 길이다(§5 머리말).

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **월 표시 토큰으로 열림** / **로그인 세션으로 열림** | 월 모드 머리 한 줄 | 이 화면은 두 가지로 열리고 **겉이 똑같다**. 어느 쪽인지 화면이 말하지 않으면 「자리 화면이 왜 죽었나」를 나중에 아무도 못 가린다 |
| **월 표시 토큰이 만료되었거나 회수되었습니다. 관리자에게 새 토큰을 받으십시오.** | 월 모드 · 토큰이 거절될 때 | 「다시 로그인해 주십시오」는 **로그인 자리가 없는 화면**에서 갈 곳 없는 안내다. 사람이 다음에 할 일을 적는다 |
| **지금 처리할 것을 불러오지 못했습니다.** | 월 모드 큐 칸 · 첫 호출이 실패했을 때 | 종전에는 이 자리에 「평온합니다」가 떴다 — **못 가져온 밤에.** 빈 것과 못 가져온 것을 가르는 문장 |
| **지도에 표시할 위치를 불러오지 못했습니다.** | 월 모드 지도 칸 · 같은 자리 | 위와 같은 이유. 「위치가 없습니다」는 요청이 성공했을 때만 참이다 |
| **보존·백업 설정** | U5 사이드바 · 화면 이름 | 「보존 정책」·「리텐션」은 문서와 기계의 말이다. 이 화면에서 하는 일은 **정한 것을 보는 것**이다 |
| **보존·백업 설정 — 선언하지 않으면 돌지 않습니다** | 위 화면의 제목 줄 | 이 화면의 전부가 그 한 문장이다. 값이 비어 있는 것이 「아직 안 썼다」가 아니라 **「아무것도 안 일어난다」**임을 제목이 먼저 말한다 |
| **미선언** | 위 화면의 빨강 배지 | 세종 판정문의 낱말 그대로다. 「미설정」은 설정 화면의 말이고, 이것은 **정한 사람이 없다**는 뜻이다 |
| **서버 신호 대기** | 위 화면의 회색 배지 | **미선언과 절대 같은 그림이 되면 안 된다.** 미선언은 「아무도 안 정했다」이고 이것은 「우리가 못 읽는다」다. 회색인 이유가 그것이다 — 빨강으로 그리면 이미 정해 둔 값을 다시 정하러 가게 만든다 |
| **이 값을 읽는 자리가 아직 없습니다. 선언되지 않았다는 뜻이 아닙니다.** | 위 회색 배지 아래 한 줄 | 배지 하나로는 둘을 못 가른다. 가르는 문장을 옆에 둔다 |
| **보관 기간이 지난 영상 지우기가 실행되지 않습니다.** / **백업이 저장될 자리가 없어 백업이 실행되지 않습니다.** / **언제 뜰지 정해지지 않아 백업이 실행되지 않습니다.** / **오래된 백업을 언제 지울지 정해지지 않았습니다.** / **복구되는지 확인한 적이 없습니다.** | 빨강 배지마다 그 아래 한 줄 | 빨강만으로는 「고치면 뭐가 달라지나」를 모른다. **무엇이 안 일어나고 있는지**를 배지마다 적는다 |
| **돌지 않습니다** / **매일 자동으로 지웁니다** | 위 화면 「자동으로 도는가」 칸 | 「활성/비활성」은 스위치의 말이다. 알고 싶은 것은 **오늘 밤 무슨 일이 일어나는가**다 |
| **누가 정했나** | 위 화면의 출처 칸 | 「출처」·「소스」는 우리 말이다. 값만 보이면 다음 사람이 되짚지 못한다 |
| **백업 목적지** / **백업 일정** / **백업 보관 기간** / **복구 시험** | 위 화면의 네 칸 | 판정문의 낱말 그대로다. 「대상 경로」·「스케줄」·「RPO」를 쓰지 않는다 |

### 2026-09-06 턴 H — 차선 C 가 **청한** 것 (조율자 등재 대기)

★ 규칙 1 을 지켜 **먼저 적고 쓴다.** 이 절의 말은 전부 **실패를 말하는 자리**의 말이다 —
사전이 §2 에서 「Network Error」 한 줄만 이름으로 금지해 두었는데, 실제 화면에는 그 한 줄
말고도 **원문이 나올 자리가 여섯 곳 더** 있었다(P-78 실측). 원문을 지우고 나면 그 자리에
**무엇을 적을지**가 없어서 화면이 침묵한다 — 침묵은 원문보다 나쁘다. 그래서 적는다.

#### 불러오지 못한 자리 (`StateBoundary` 하나가 전부 그린다)

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **불러오지 못했습니다.** | 오류 상자의 제목 (종전 그대로) | 이 줄은 이미 있었다. 바뀌는 것은 **그 아래 줄**이다 |
| **잠시 뒤 다시 시도해 주십시오.** | 서버가 답은 했으나 실패했을 때(5xx) | 사용자가 할 수 있는 일이 기다림뿐인 갈래. **원인을 적지 않는다** — 서버 안의 사정은 사용자의 말이 아니다 |
| **서버에 연결하지 못했습니다. 연결을 확인한 뒤 다시 시도해 주십시오.** [2026-09-17 턴 T 정정 — `copy.ts::failureSpeech` ②+③ 실물과 같은 글자로] | 응답이 아예 없을 때(네트워크·서버 다운) | 「Network Error」가 태어난 그 자리. 5xx 와 **다른 문장**이어야 한다 — 하나는 우리 쪽이고 하나는 길 쪽이다 |
| **응답이 제때 오지 않았습니다. 잠시 뒤 다시 시도해 주십시오.** [2026-09-17 턴 T 정정 — `copy.ts::failureSpeech(timeout)` ②+③ 실물] | 화면이 정한 상한(10초)을 넘겼을 때 | 「실패」와 「늦다」는 다르다. 늦은 것은 다시 눌러 볼 값이 있다 |
| **이 항목에 대한 권한이 없습니다.** | 403·401 (종전 그대로) | |
| **다시 로그인하면 보일 수 있습니다.** | 위 줄 아래 한 줄 | 사용자가 **다음에 할 일**을 적는다. 종전에는 이 자리에 axios 원문이 있었다 |

#### 로그인이 실패하는 다섯 갈래

★ 이 다섯은 **서로 다른 문장이어야 한다.** 하나로 뭉치면 사용자가 「내가 틀렸나 · 서버가
죽었나 · 잠겼나」를 못 가르고, 그 셋은 **다음에 할 일이 각각 다르다.**

| 화면의 말 | 어느 갈래 | 왜 |
|---|---|---|
| **아이디를 입력해 주십시오.** / **비밀번호를 입력해 주십시오.** | ① 빈 값 | 칸 옆에 붙는다. 종전에는 영문 `This field is required.` 가 떴다 |
| **아이디 또는 비밀번호가 올바르지 않습니다.** | ② 틀림 | 둘 중 **어느 쪽이 틀렸는지 적지 않는다.** 「사용자 이름이 올바르지 않습니다」는 계정이 있다는 사실을 남에게 알려 준다 |
| **5회 틀리면 계정이 잠깁니다. (지금 N회)** | ② 틀림 · 위 줄 아래 | 잠기기 **전에** 말한다. 잠긴 뒤에 세는 것은 사용자에게 늦다 |
| **계정이 잠겼습니다. N분 M초 뒤에 다시 시도해 주십시오.** | ③ 잠금 | 「잠겼다」만 적으면 사람이 계속 누른다. **언제 풀리는지**가 이 문장의 값이다 |
| **지금 로그인할 수 없습니다. 잠시 뒤 다시 시도해 주십시오.** | ④ 서버 오류(5xx) | 사용자의 아이디·비밀번호 문제가 **아니라는 것**을 먼저 말한다 |
| **서버에 연결하지 못했습니다. 연결을 확인한 뒤 다시 시도해 주십시오.** [2026-09-17 턴 T 정정 — `loginCopy.ts::NETWORK` 실물] | ⑤ 네트워크 | 위와 같은 이유로 ④와 다른 문장이다 |
| **로그인 중입니다…** | 제출 중인 단추 | 눌린 단추가 아무 말도 안 하면 사람이 **한 번 더 누른다**(P-78 ③ 이 실측한 이중 제출의 절반) |

#### 두 번 눌린 자리

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **처리 중입니다. 잠시만 기다려 주십시오.** | 이미 보낸 요청을 또 누를 때 | 아무 일도 안 일어나는 단추는 고장으로 읽힌다. **삼킨 것을 삼켰다고 말한다** |

#### 월 모드 토큰을 받는 자리

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **이 주소로는 월 모드를 열 수 없습니다. 관리자에게 새 주소를 받으십시오.** | `/wall?token=` 로 들어왔을 때 | 종전 주소를 **조용히 무시하면** 대형 화면이 로그인 화면을 띄운 채 밤을 샌다. 왜 안 열리는지와 **누구에게 갈지**를 적는다 |

### 2026-09-15 턴 Q 추가 — 차선 U3 (모바일 M2 지도 링크)

★ 이 문구는 `frontend/src/features/dsm/copy.ts` 가 아니라 **`features/mobile/pages/MobileEventDetail.tsx`
안의 `MAP_LINK_LABEL` 상수**에 산다 — U3 차선의 소유 경계가 `features/mobile/**`뿐이고,
`copy.ts`는 F 차선의 파일이라 이번 턴에는 그 파일에 줄을 끼우지 않는다(§4.2 「충돌은
라우팅 침묵으로」와 같은 이유 — 같은 턴에 여러 차선이 한 파일을 고치면 충돌한다).

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **지도에서 보기** | M2 「어디로 가나」 카드 · 좌표 또는 주소가 있을 때만 | 「지도 앱으로 열기」·「Open in Maps」는 플랫폼의 말이다. 이 버튼이 하는 일은 하나 — 누르면 지도가 뜬다 |

### 2026-09-15 턴 Q 추가 — 차선 U1 (큐 카드 키 1 · 오탐 사유 3택)

★ `frontend/src/features/dsm/copy.ts` 끝에 같은 절을 올렸다(여러 차선이 같은 턴에
이 파일을 함께 읽어 끝에 붙이는 관례 그대로). 서버에 오탐 사유의 정해진 목록이
아직 없어(`/review` 의 `reason` 은 자유 텍스트) 아래 세 문장은 **화면이 미리
채우는 값**이지 새 계약이 아니다.

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **실제로 확인 · 접수** | 큐 카드 · 키 1 | 판정(확인)과 접수가 이제 한 번의 쓰기다(WO-01 §5 AC-2) — 사람에게 한 행동인 것을 이름도 하나로 말한다 |
| **오탐으로 판정** | 큐 카드 · 오탐 단추 | 판정 축의 다른 갈래. `EventDetail` 의 같은 말과 갈리지 않는다 |
| **사람이 아님(동물·사물)** / **카메라 오작동·화면 이상** / **기타** | 오탐 사유 3택 | 자유 텍스트 앞에 자주 쓰는 셋을 먼저 놓는다 — 매번 손으로 치지 않아도 되게 |

### 2026-09-15 턴 Q 추가 — 조율자 (사건 상세 · 알림 보내기의 결과)

★ P-129 대조표가 가른 것: 「알림을 보내도 발송 수가 그대로」는 응답 모양이 아니라 **화면이
응답을 버린 것**이었다(`EventDetail.tsx` 의 발송 단추 · 서버는 중복 억제 때 200 + `total: 0`).
문구는 그 화면 안에 산다 — 다른 차선이 같은 턴에 `copy.ts` 끝을 함께 고치고 있어서다.

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **새로 보낸 알림이 없습니다. 같은 사건의 알림은 5분 안에 다시 보내지 않습니다. 아래 발송 이력을 확인하십시오.** | 발송 응답 `total` 이 0 | 조용한 0 은 「고장」과 구별되지 않는다. 0 이라고 말하고 **왜 0 인지**(중복 억제 F-04)를 적는다 |
| **알림 N건 중 M건이 실패했습니다. 아래 발송 이력에서 사유를 확인하십시오.** | 발송 행 중 `succeeded=false` 가 있을 때 | 실패도 200 이다(F-10) — 실패 수를 그 자리에서 센다 |
| **알림 N건 발송을 요청했습니다. 결과는 아래 발송 이력에서 확인하십시오.** | 정상 | 「보냈다」가 아니라 「요청했다」 — 도착은 이력이 말한다(종전 문장에 수만 더했다) |

### 2026-09-17 턴 T 추가 — 차선 F (로그인 율제한 SEC-21 · 사전 v1.1 등재 마감)

★ **등재 실측(분모 먼저 · 2026-09-17)**: §5 의 굵은 문장 **130개(표 93행 · 이 절 2행 포함)** 중
화면 소스(`frontend/src/**/*.ts,tsx`)에 같은 글자로 있는 것 — 기계 대조 126 + 손 대조 4(`${n}` 틀
문장 「전체 M장」·「그 밖 N장」·「5회 틀리면 … (지금 N회)」·②+③ 두 문장 「응답이 제때 … 잠시 뒤 …」)
= **130/130**. 정정 전에는 셋이 어긋났다(「연결을 확인해 주십시오」 둘 · 「응답이 늦어 불러오지
못했습니다」 하나) — 위 표에서 **실물 쪽으로 정정**했다. 코드는 ②왜+③지금 두 문장을 잇는다.
★ 이 절의 문장은 `features/login/loginCopy.ts::rateLimitedLine` 과 서버
`backend/common/rate_limit_body.py::rate_limited_message` 에 **같은 글자**로 산다 — 서버가 처음
준 문장과 화면이 세어 내리며 다시 그리는 문장이 다르면 사람은 「다른 오류」로 읽는다.

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **요청이 너무 잦습니다. N초 뒤 다시 시도해 주십시오.** | ③′ 로그인 율제한(429 · 1분에 5회 초과) · 화면이 1초마다 N 을 내린다 | [실측 2026-09-17] 종전 답은 영문 HTML **403 Forbidden** 이었고 화면은 그것을 「접근 권한 없음 · 관리자 문의」로 읽었다. 사람이 할 일은 **기다림**이고, 얼마나 기다릴지가 이 문장의 값이다(잠금 문장과 같은 규약) |
| **지금 다시 시도할 수 있습니다.** | 위 카운트다운이 0 에 닿았을 때 | 기다림이 끝났음을 **말한다**. 문장이 그대로 남아 있으면 「아직 안 된다」로 읽힌다 |


### 2026-09-17 턴 T 추가 — 조율자 병합 (화면 제목 HEADLINE 6 · U24 축 이름 4 — 온보딩 48행 「정본 없음」을 줄이는 등재)

차선 Q 가 온보딩 48행에 「누를 문구」 칸을 채우다 **화면에는 있는데 사전에는 없는 제목 여섯**을 찾았고(각 화면
파일의 지역 HEADLINE), 차선 U24 가 D-479 로 갈라 보인 두 축 이름과 상급 보고 단추·감사 계측 문구를 청했다.
전부 **코드에 실재하는 글자 그대로**다(조율자가 grep 으로 대조 · 렌더 0 인 것은 없다).

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **사람·역할 — 계정 만들기 · 비활성화** | `/dsm/people` 제목(People.tsx) | 화면이 하는 두 일을 제목이 말한다 — 「사용자 관리」는 인수 화면(`/users`)의 이름이라 겹치지 않게 한다 |
| **알림 받는 사람·채널** | `/dsm/notify` 제목(NotifySettings.tsx) | 「알림 설정」은 M4(내 알림 설정)와 헷갈린다 — 이쪽은 **남에게** 가는 규칙이다 |
| **내 정보** | `/dsm/me` 제목(Me.tsx) | 인수 `/profile` 을 대신하지 않는 우리 층의 자리 — 짧은 말로 둔다 |
| **내게 온 이벤트** | M1 수신함 제목(mobile) · 온보딩 U3 행 | 「이벤트 목록」이 아니라 **나에게 발송된 것**만이다(M1 은 발송 기록이 정본) |
| **내 알림 설정** | M4 제목(Me.tsx 링크 · MobileSettings) | 위 「알림 받는 사람·채널」과 짝 — 이쪽은 **내가** 받는 조건이다 |
| **요원별 현황** | `/dsm/team-status` 제목(TeamStatus.tsx · Stats.tsx 링크) | 팀장이 사람 단위로 본다 — 「대시보드」와 가른다 |
| **시뮬 축 — 확신도 (0~1 · 모델이 낸 값 · 저장되지 않음)** | `/dsm/cameras/tuning` 슬라이더 위(CameraTuning.tsx) | D-479 — 확신도 축과 저장 축은 **다른 단위**다. 같은 말로 부르면 시뮬 값이 저장되는 줄 안다 |
| **저장 축 — 항목 단위 (서버가 정한 단위 · 사유를 적어야 저장됨)** | 같은 화면 저장 칸 위 | 위와 짝 — 저장은 서버 단위이고 사유 없이는 안 된다는 것을 칸이 먼저 말한다 |
| **보고 표시** / **보고함** | `/dsm/events` 행의 상급 보고 토글(EventList.tsx) | 누르기 전/후가 **다른 말**이어야 누른 뒤를 알 수 있다(서버 값으로만 바뀐다) |
| **N.N초 · 60초 안** | `/dsm/audit` 상태 칸(AuditLog.tsx) | 감사 화면의 요구(60초 안 도달)를 화면이 스스로 잰다 — 계측이지 토스트가 아니다 |

### 2026-09-18 턴 U 추가 — 조율자 병합 (기간 프리셋 1 · 차선 Q 의 등록 요청)

차선 Q 가 온보딩 U4#1(주간 상황 요약)을 채우려다 막혔다: 화면에는 **`7일`** 프리셋이 있는데 사전에 없어
「정본 없음」이었다. 짐작으로 채우지 않고 **등록 요청**으로 넘긴 것이 옳았고, 여기에 등재한다 —
글자는 `frontend/src/features/dsm/pages/EventList.tsx:223`(`PERIODS` 의 `d7`)에 **그대로 있는 것**이다.

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **7일** | `/dsm/events` 기간 프리셋(EventList.tsx `PERIODS.d7`) · 온보딩 U4#1 | 재난안전과의 「주간 상황 요약」이 누를 자리다. 서버는 `summary?hours=168` 을 받는데 프리셋이 없어 **누를 데가 없다**고 적혀 있었다(턴 T 착시 ⑨) — 그 사이 화면에 `7일` 이 섰다. 사전에 올려 두 칸이 채워진다 |

★ 위 등록 요청 뒤, 같은 병합에서 **차선 U24(보고서·감사)**·**차선 U56(관리자)**이 청한 문구를 조율자가
grep 으로 하나씩 코드에 대조했다 — 화면 소스에 **같은 글자로 실재하지 않는 것은 올리지 않았다**
(확인 못 한 것은 보고에 「코드에 없음」으로 남긴다). 「왜」는 차선의 설명을 그대로 옮기지 않고
조율자가 해당 파일·줄을 읽어 다시 썼다.

#### U24 축 — 보고서 · 감사 (화면 `Reports.tsx` · `AuditLog.tsx` · 서버 `monthly_report.py` · 렌더러 `incident_report.py`)

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **보고서** | `/dsm/reports` 제목(Reports.tsx:146) | 경로 이름공간(`/api/dsm/reports/*`)과 같은 말을 화면 제목에도 써서, 사람이 부르는 이름과 서버 문서의 이름이 갈리지 않는다(`routes.u24.ts:47` 주석) |
| **사건 보고서** | 서식 카드 이름(Reports.tsx `FORMS[0].label` · `monthly_report.KIND_LABEL['incident']`) | 서버가 내려받기 파일 이름(`KIND_LABEL.get(...)-{run_id}.{ext}`)에 쓰는 것과 같은 글자다 — 화면 카드명과 받은 파일명이 갈리지 않는다 |
| **이번 달 우리 센터** | 서식 카드 이름(Reports.tsx `FORMS[1].label` · `KIND_LABEL['monthly']`) | 자동본(매월 1일 배치 `run_monthly_all`)과 화면의 「만들기」가 같은 서식을 가리킨다는 것을 같은 이름으로 말한다 |
| **상급기관 제출용** | 서식 카드 이름(Reports.tsx `FORMS[2].label` · `KIND_LABEL['upper']`) | 화면 카드의 이름이다 — 종이 제목은 「…보고서」가 붙어 다르다(아래 종이 축에 별도 등재) |
| **만들기** | 서식 카드 단추(Reports.tsx:172) | 실행 기록 한 행을 새로 만드는 요청임을 가장 짧은 동사로 말한다 — 읽기 전용 계정(U4)은 이 단추가 403(플랫폼 문지기 `read_only_role`)이고, 그 사람의 자리는 아래 「DOCX 내려받기」다 |
| **DOCX 내려받기** | 실행 카드·표의 단추(Reports.tsx:207,296) | 정본 파일 형식을 이름에 밝힌다 — 한글(HWP)에서 열린다(결정 ⑤) |
| **PDF 내려받기** | 같은 자리의 병행 단추(Reports.tsx:211,300) | 정본이 아니라 같은 글자를 찍은 병행본임을 파일 형식으로 구분한다 |
| **특이사항 (자동본에 사람이 더하는 한 줄)** | 만들기 카드 아래 입력칸 제목(Reports.tsx:179) | 자동으로 채워진 종이에 사람이 한 줄을 더 얹는 자리임을 제목이 말한다 — 종이 쪽의 절 제목 「특이사항」과 짝이다(아래 종이 축) |
| **실행 기록** | 하단 표 카드 제목(Reports.tsx:237) | 무엇을 만들었는지(성공·실패 모두) 이력으로 남는 자리의 이름 |
| **자동** / **사람** | 실행 표 「만든 쪽」 칸의 값(`monthly_report.py:57` `TRIGGER_LABEL`) | PRD §7.4 가 묻는 수를 배치가 만든 것과 사람이 누른 것으로 가른다 — 사람이 누른 것을 자동이라 적으면 그 수가 거짓이 된다(Reports.tsx 머리말) |
| **생성됨** / **생성 실패** | 실행 표 「결과」 칸의 값(`monthly_report.py:58` `STATUS_LABEL`) | 실행이 파일까지 조립됐는지를 성공/실패로 가른다 — 조립이 실패해도 행은 남긴다(「시도가 없었다」와 「해 봤는데 안 됐다」를 가르려고) |
| **아직 만든 보고서가 없습니다. (요청은 성공했고 0건입니다)** | 실행 표 빈 상태 문구(Reports.tsx:244 `emptyText`) | 조용한 0을 「고장」과 구별한다(턴 Q 의 규약과 같다) |
| **보고서를 만들지 못했습니다.** | 만들기 실패 상태 칸 제목(Reports.tsx:111,191) | 토스트가 아니라 상태 칸에 남아 사라지지 않는다(P-173 · 새 토스트 0) |
| **파일을 받지 못했습니다.** | 내려받기 실패 상태 칸 제목(Reports.tsx:133,220) | 위와 같은 규약 — 「다시 시도」가 방금 그 요청을 그대로 다시 낸다 |
| **해시 체인** | `/dsm/audit` 상태 칸 요약 라벨(AuditLog.tsx:262) | 표의 행들이 앞 행과 이어져 있는지 요약해 보여주는 자리 이름 |
| **체인 이어짐** | 행별 체인 라벨(AuditLog.tsx:71 `CHAIN_WORD.linked`) | 그 행이 위조되지 않고 앞 행과 연결됐음을 말한다 |
| **체인 끊김** | 행별 체인 라벨(AuditLog.tsx:72 `CHAIN_WORD.broken`) | 체인이 끊어져 있음을 빨강으로 말한다 — 회색 「체인 모름」(관측 불가)과는 다른 사실이다(AuditLog.tsx 머리말) |
| **감사 기록 표를 내려받지 못했습니다.** | 감사 CSV 실패 상태 칸 제목(AuditLog.tsx:224) | 표 내려받기 실패도 토스트가 아니라 상태 칸이다(P-173) — 「표 내려받기」 단추 자체는 아래를 본다 |

⚠ **「표 내려받기」는 이미 있음** — 178행(계량 화면 CSV 단추)이 정본이다. `AuditLog.tsx:216` 이 같은 글자를
그대로 쓴다(재등재하지 않는다).

**종이(서식) — `docx_export.py` 가 부르는 렌더러 `apps/dsm/incident_report.py`**

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **상급기관 제출용 보고서** | 종이 제목(`incident_report.py:382` `UPPER_TITLE`) | 화면 카드 이름 「상급기관 제출용」과 다르게 종이에는 「보고서」가 붙는다 — 화면과 종이가 같은 말이 아님을 분명히 하려고 별개로 등재한다 |
| **특이사항** | 종이 절 제목(`incident_report.py:411` `<h2>특이사항</h2>`) | 화면 입력칸 제목 「특이사항 (자동본에 사람이 더하는 한 줄)」과 짝이 되는, 종이 위의 제목 |
| **기재된 특이사항이 없습니다.** | 특이사항 절이 빈 채로 만들어질 때(`incident_report.py:410`) | 칸을 지우지 않고 「없음」을 적어, 「특이사항이 없었다」와 「아무도 안 적었다」를 가른다(머리말 그대로) |
| **이 기간에 상급기관 보고 대상으로 표시된 사건이 없습니다.** | 상급 제출용 종이의 대상 사건 표가 0행일 때(`incident_report.py:548-549`) | 빈 표를 내지 않고 문장으로 0을 말한다(함수 머리말: 「체크가 0건이면 빈 표를 내지 않는다」) |

#### U56 축 — 관리자 (화면 `CameraAddress.tsx` · `SystemSettings.tsx`)

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **등록된 카메라 — 행마다 한 대씩 채웁니다** | 카메라 표 카드 제목(CameraAddress.tsx:226) | 이 표의 각 행이 하는 일(한 대씩 주소 채우기)을 제목이 먼저 말한다 |
| **이 한 대 채우기** | 표 행의 단추(CameraAddress.tsx:297) | 이름으로 찾는 일괄 문의 함정(이름을 바꾼 카메라는 못 찾고 **새로 만들어진다**)을 피해, **이 행의 id** 로 전용 문(`POST /cameras/{id}/address`)을 부른다는 것을 단추 이름이 말한다 |
| **그만두기** | 행 편집 취소 단추(CameraAddress.tsx:285) | 이 행의 편집을 시작한 뒤 되돌리는 자리 |
| **아직 없는 카메라를 이름으로 넣습니다** | 하단 카드 제목(CameraAddress.tsx:310) | 위 표(이미 있는 카메라)와 달리 이 카드는 **없는** 카메라를 새로 등록하는 자리임을 가른다 — 이 길만 여전히 일괄 등록 문을 탄다 |
| **백업 회수증 — 마지막으로 남은 것** | 카드 제목(SystemSettings.tsx:321) | 「뜬 것」이 아니라 「남은 것」을 본다는 화면의 규약을 제목이 그대로 말한다(D-301) |
| **회수증 없음 — 여기서 읽히지 않습니다** | 회수증 없음 표시(SystemSettings.tsx:68 `NO_RECEIPT`) | 「백업 0건」이라 적지 않는다 — 이 화면이 아는 것은 「여기서 안 읽힌다」뿐이고, 둘은 다른 사실이다 |
| **재시작 요청 — 기록만 남습니다** | 카드 제목(SystemSettings.tsx:406) | 이 단추가 서버를 내리지 않고 요청만 기록한다는 것을 제목이 먼저 말한다 |
| **재시작을 요청합니다** | 단추(SystemSettings.tsx:425) | 「재시작합니다」가 아니라 「요청합니다」 — 실제 재시작은 점검 창에서 사람이 한다(본문 그대로) |

#### 2026-09-18 턴 U · 커밋 자리에서 등재된 한 줄 (조율자 · 게이트가 먼저 잡았다)

| 화면의 말 | 어디에 쓰나 | 왜 |
|---|---|---|
| **이 표는 켜져 있는 카메라만 보여 줍니다. 꺼 둔 카메라는 여기 없습니다 — 「없다」가 아니라 「이 표의 밖」입니다.** | `/dsm/cameras/address` 표 아래 주석(CameraAddress.tsx) | 「주소 미입력 N대」의 **분모가 무엇인지**를 화면이 스스로 말한다 — 꺼 둔 카메라를 세지 않는 표에서 0 을 보고 「다 채웠다」로 읽으면 그 0 은 거짓이다(분모를 손으로 적지 않는다는 규약의 화면 쪽 짝) |

⚠ 이 문장은 원래 `**켜져 있는**` 으로 쓰여 있었다. 화면은 마크다운을 렌더하지 않으므로 **별 두 개가 그대로 보인다** —
`scripts/verify_ui_copy.py` 의 「마크다운 강조」 규칙이 커밋 자리에서 잡았고(사전 미등재도 함께), 별을 떼고 여기 등재했다.
