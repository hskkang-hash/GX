# DA-04 공유 커널 6종 API 스펙 (K1~K6) — F 계약 AC × U 상품 AC 이중 태깅

**티켓** DA-04 (WP-DA1) · **버전** v1.0 · **작성** 2026-08-25
**근거** 상위 지시서 v2.1 "초효율 병행 전략(공유 커널)" · `docs/agent/GuardianX_상품화_개발방안_20260822.pdf` §2·§3
· 계약 [별첨 1] 기능명세서 · `docs/design/DA-01_요구사항정의서_v1.0.md`
**승인** ☐ 대표 (human_gate)

> **이 문서의 주장 하나**: 재난안전 App(F-01~14)과 상품화 2단계(U1~U4)는 **별개 프로젝트가 아니다.**
> 같은 부품 6개의 두 포장이다. 그 6개를 여기서 정의하고, **하나의 구현이 두 AC 를 동시에 충족한다**는
> 것을 표로 강제한다. 강제하지 않으면 반드시 두 벌이 만들어진다 — 그때 비용은 두 배가 아니라
> 두 배 + 데이터 이원화다.

---

## 0. U1~U4 의 정의 — 원본을 따른다

★ **먼저 정정**: 상위 지시서 §1 은 "U1↔F-11 보고서 · U2↔F-10 알림"이라 적었으나
정의 원본(`GuardianX_상품화_개발방안_20260822.pdf` §2)은 다음과 같다. 한 칸 밀렸다.
**같은 지시서의 v2.1 커널 절은 원 정의와 맞다** — 이 문서는 원 정의를 따른다(D-210: 실측 우선).

| U | 정의 | 해결 Painpoint | 고객이 얻는 것 |
|---|---|---|---|
| **U1** 이벤트 증거화 | 알람을 3클릭으로 재검색·증거 다운로드, 정탐/오탐 기록으로 오탐률 수치화 | PP-1 · PP-2 | "3클릭 증거화" |
| **U2** 보고 자동화 | 사건·통계 자동 취합 → 보고서 생성. 주당 수 시간 → 10분 | PP-3 | 감시에 집중 |
| **U3** 3계층 대시보드 | 담당자 / 부서장 / 지자체장 각 뷰 | PP-4 | 역할별 관제 |
| **U4** 격리·계측 봉인 상용화 | 격리를 테스트 출력으로 봉인, 사용량을 시스템 집계로 | PP-5 · PP-6 | 증거 기반 신뢰 |

또한 **U1~U4 는 티켓 ID 가 아니라 WP 묶음**이다 — D-246 이 이미 "AI 이벤트 센터(WP-3 · 상품화 2단계 U1)"로
그렇게 쓰고 있다. 지시서 v2.1 의 "U1~U4 티켓에 kernel 필드 추가" 지시는 근사치이며,
실측대로 **해당 WP 의 개별 티켓 18건**에 `kernel:` 필드를 넣었다(§4).
> **[2026-08-27 갱신 · D-257]** K2 GAP 이 닫혔다 — 티켓 `DA2-21` 등재로 **19건**이 됐다. 아래 §2·§3·§4 의 GAP 표기를 함께 정정했다.

---

## 1. 커널 3원칙 (지시서 v2.1 규칙 — 문서로 못박는다)

1. **커널은 L3 Platform 에만 산다.** App(L4)·어댑터(L2)는 커널 API 를 **소비만** 한다 — 얇게.
   → 이것은 재사용 편의가 아니라 **IP 방어**다. 커널 로직이 App 으로 새면
   "본체=가이온 / 신규 연계=공동"(계약 8조)의 경계가 코드에서 소멸한다.
2. **커널 티켓은 이중 태깅한다** — `[F-xx 계약 AC]` + `[U-x 상품 AC]`.
   **충돌 시 계약 AC 가 우선**한다. 계약 AC 는 검수 기준이고(7조3항) 상품 AC 는 우리 목표다.
3. **코드 착수는 WP-2 EXIT(누출 0건) 후.** 설계·Mock·커널 스펙은 지금 병행한다.

**추가로 이 문서가 정하는 것 하나 —**
4. **커널의 공개 면(public surface)은 서비스 함수다.** App 이 커널의 **모델을 직접 import 하지 않는다.**
   모델을 직접 만지면 커널을 바꿀 때 App 이 깨지고, 그러면 "한 번 개발"이 거짓이 된다.

---

## 2. 커널 6종 스펙

표기 — **소유**: 구현이 사는 계층 / **상태**: 지금 저장소에 얼마나 있는가

### K1 이벤트 커널 — 소유 L3 · 상태 **계약 고정 완료, 구현 일부**

이벤트를 만들고·모으고·찾고·판정하는 한 벌. **이 App 의 심장이다.**

| 공개 면 | 입력 | 출력 | 비고 |
|---|---|---|---|
| `record_detection(...)` | stream, type, severity, occurred_at, bbox, confidence, snapshot | Event | **중복 억제 내장**(동일 stream+type 10초 → `last_seen_at` 갱신) |
| `query_events(...)` | 기간·유형·등급·상태·구역·스트림 · 페이지 | Event[] | **테넌트 스코프 강제** — 인자로 group 을 명시로 받는다 |
| `get_event(id)` | id | Event | 남의 것이면 **404** (200+빈 응답 금지 · W0-18 교훈) |
| `review_event(id, verdict, reason)` | confirmed / rejected | Event | K6 오탐 통계의 입력 |
| `close_event(id)` | | Event | F-11 보고서 트리거 지점 |
| `subscribe(webhook)` | URL · 필터 · 서명키 참조 | Subscription | F-05 Webhook |
| `advance_response(id, to_state, reason)` | 대응 진행 한 칸 | {from,to,allowed_next,audit_id} | ★ **D-399.** `status` 와 **다른 축**이다 — 아래 설명 |
| `response_state(id)` | id | {response_state, allowed_next} | 화면이 자기 전이표를 들지 않게 서버가 낸다 |

★ **왜 축이 둘인가** (D-399 · 2026-09-14 추가). 지시서가 「이벤트를 4값으로」를 냈고
실측하니 `status` 는 **이미 4값**이었다. 두 4값은 **묻는 것이 다르다**:

| 칸 | 묻는 것 | 값 |
|---|---|---|
| `status` | 이 탐지가 진짜인가 | 신규 → 확인/기각 → 종료 |
| `response_state` | 사람이 어디까지 했나 | 발생 → 접수 확인 → 조치중 → 종결 |

한 칸에 두 뜻을 넣으면 **마지막에 쓴 사람이 앞사람의 뜻을 덮는다.** 그것이 D-293 이
`status` 와 `verdict` 를 가른 이유이고, 섞여 있던 동안 종료가 쌓일수록 오탐률이
저절로 좋아졌다. 전이 규칙: **앞으로만 간다.** 되돌림은 「종결 → 조치중」 하나뿐이고,
관제팀장(K3 MANAGER 이상)만, **사유 필수**. 전이 기록은 새 표가 아니라
`logger.AuditLogs` 에 `common/audit_writer.py` 로 남긴다 (D-333).

- **저장 계약**: `docs/contracts/detection-event.md` v1.0 (고정 2026-08-13). **이 문서가 계약을 바꾸지 않는다.**
- 확장 필요: 침수 계열 · 통신두절 계열 열거값 추가 → 계약 문서 + W2-3 색 규칙 동시 갱신(DA-01 OPEN-05).
- **오류 계약**: 거부는 HTTP 4xx 로 낸다. `200 + {"success":false}` 를 새로 만들지 않는다.

| 이중 AC | 계약 (F) | 상품 (U) |
|---|---|---|
| | **F-04**: 동일 이벤트 5분 내 중복 알림 0건 / **F-05**: OpenAPI 제공 · p95 500ms | **U1**: 증거 확보 **클릭 수 ≤ 3** · 오탐률 수치화 |
| **하나의 구현** | 중복 억제는 **기록 단계**(10초·이벤트)와 **알림 단계**(5분·K2)를 나눈다. 이벤트를 접으면 U1 의 오탐률 분모가 거짓이 되므로 **이벤트는 남기고 알림만 접는다.** 조회 API 하나가 F-05 와 U1 3클릭을 함께 만족한다 — **3클릭이 되려면 조회 API 가 필터를 서버에서 처리해야 한다**(클라이언트 필터는 클릭을 늘린다). |

**대응 정본 티켓**: W2-1(모델·done 여부 확인) · W2-2(수집·중복억제) · W2-3(이벤트 센터 화면)

---

### K2 알림 커널 — 소유 L3 · 상태 **커널 구현됨 (2026-08-30 · `backend/kernels/k2_notify/`)**

등급별 수신자에게 보내고, 보낸 사실을 남기는 한 벌.

| 공개 면 | 입력 | 출력 |
|---|---|---|
| `resolve_recipients(severity, group, zone)` | 등급·테넌트·구역 | Recipient[] |
| `send(event, recipients, channels)` | SMS · 이메일 · 앱 푸시 · Webhook | DeliveryRecord[] |
| `suppress?(event)` | | bool — **5분 중복 억제 판정은 여기다** (K1 이 아니다) |
| `list_deliveries(...)` | 기간·이벤트·수신자 | DeliveryRecord[] |

- **DeliveryRecord 필수 필드**: 이벤트 · 수신자 · 채널 · `sent_at` · 성공여부 · 실패사유 · **재시도 횟수**.
  `occurred_at → sent_at` 이 F-10 의 30초 AC 를 재는 두 점이다.
- 외부 발송 업체 호출은 **타임아웃 필수**(`common/external_http.py`) — 발송이 느리다고 관제가 멈추면 안 된다.
- 수신자 0명 저장 금지(DA-03 §3-2) — 조용한 무력화를 막는다.

| 이중 AC | 계약 (F) | 상품 (U) |
|---|---|---|
| | **F-10**: 심각 등급 발생 후 **30초 내 발송 기록** | **U2** 보고 자동화의 입력 · **U1** 오탐 흐름의 시작점 |
| **하나의 구현** | 발송 기록이 곧 보고서의 "조치 이력" 행이다 — K4 가 이 레코드를 그대로 읽는다. 알림용·보고서용 두 벌로 적재하지 않는다. |

> ★ **GAP 닫힘 (2026-08-27 · D-257)**: 알림 **발송** 티켓 `DA2-21`(P0 · WP-DA2b) 이 등재됐다.
> 등재 전 상태를 지우지 않고 남긴다 — 이 GAP 은 커널 매핑을 **기계로** 대조했기 때문에 드러났다.
> `NotificationsList` 는 여전히 화면단 목록이며 발송 기능이 아니다.
> F-10 은 계약 필수(M)이므로 **WP-DA2 에서 신규 티켓 등재가 필요하다**(DA-01 OPEN-07).

---

### K3 역할별 대시보드 프레임 — 소유 L3 · 상태 **커널 구현됨 (2026-08-30 · `backend/kernels/k3_dashboard/`)** — 프리셋 매핑은 설정 대기(P-K3-1)

"누가 보느냐에 따라 다른 화면"을 만드는 프레임. **위젯이 아니라 프레임이 커널이다.**

| 공개 면 | 입력 | 출력 |
|---|---|---|
| `get_preset(user)` | 사용자 | Preset (OPERATOR / MANAGER / EXECUTIVE) |
| `resolve_layout(preset, app)` | 프리셋 · 앱(감시/재난) | Panel[] — `dashboard.DashboardPanel` 재사용 |
| `widget_permission(user, widget)` | | visible / hidden — DA-03 §3-4 의 서버측 판정 |

- 프리셋 정의는 **W3-2 가 정본**이다. 재난용 프리셋을 따로 만들지 않는다(그러면 U3 수렴이 깨진다).
- `Dashboard` · `DashboardPanel` 은 이미 `BaseModelWithGroup` 이다 — 격리 기반이 있다.
- ✅ **해소 (2026-08-30)** — `dashboard.DashboardPanel` 을 격리 시험 레지스트리에 등재했다(K3 커널과 같은 커밋).
  reach=`via_parent`(단건 pk 라우트 0 · 목록 2) 로 목록 응답의 중첩 패널을 친다.
  ★ 조건을 기억에 맡기지 않는다 — `test_da04_precondition_dashboardpanel_is_isolation_registered`
  가 K3 시험 안에서 이 등재를 붙들고 있다. 빠지면 K3 가 멈춘다 (D-286).

| 이중 AC | 계약 (F) | 상품 (U) |
|---|---|---|
| | **F-09**: 5상태(기본/로딩/빈/오류/권한없음) 100% · 클릭→영상 3초 | **U3**: 역할별 대시보드 **도달 클릭 ≤ 2** |
| **하나의 구현** | 프리셋이 **로그인 직후 기본 화면**을 결정하면 도달 클릭이 0~1 이 된다. 즉 U3 의 ≤2 는 프리셋 라우팅으로 달성되고, F-09 의 5상태는 그 프레임이 **상태를 위젯에 주입**하는 방식으로 한 번에 처리된다 — 위젯마다 5상태를 손으로 짜지 않는다. |

**대응 정본 티켓**: W3-1(라우트 6→1 + 프리셋 통합) · W3-2(프리셋 정의)

---

### K4 보고서 엔진 확장 — 소유 L3 · 상태 **커널 구현됨 (2026-08-30 · `backend/kernels/k4_report/`)** — 기간 보고서는 미구현(선행 대기)

템플릿에 값을 꽂아 PDF 를 만드는 한 벌.

| 공개 면 | 입력 | 출력 |
|---|---|---|
| `render(template, context)` | 템플릿 + 치환 컨텍스트 | PDF |
| `build_context(mission \| event \| period)` | 임무 / 이벤트 / 기간 | context — **이벤트·조치이력·캡처** 포함 |
| `list_templates(group)` | | Template[] |

- 엔진: `backend/report_template/` (`ReportTemplate` · `api.py` · 폰트 다운로더까지 실재).
- 치환 변수는 **계약이 지정한 3종을 반드시 포함**한다: 이벤트 · 조치 · 캡처(F-11 AC).
  "조치"는 K2 의 DeliveryRecord + I-4 의 SDN 처리 결과다.
- ✅ **OPEN-03 해소 (2026-08-30)** — 전제가 틀렸다. 실행 중인 dj-core 의 `BaseModel` 이
  이미 `group` FK 와 `CustomManagerGroup` 을 갖고, `BaseModelWithGroup` 은 **DEPRECATED** 이며
  필드를 더 정의하지 않는다. 살아 있는 13행은 전부 소유가 채워져 있다.
  **기저 클래스 변경·마이그레이션·백필 모두 불필요** — `evidence/W2-K4/open03_premise.md`.

| 이중 AC | 계약 (F) | 상품 (U) |
|---|---|---|
| | **F-11**: 템플릿 변수(이벤트·조치·캡처) 치환 및 PDF 출력 | **U2**: 보고서 생성 시간 **≤ 10분** |
| **하나의 구현** | 10분은 사람의 작업 시간이지 렌더 시간이 아니다. 줄이는 것은 **`build_context` 의 자동 취합 범위**다 — 사람이 손으로 옮겨 적는 항목이 0 이면 10분이 된다. 즉 U2 의 KPI 가 F-11 의 치환 변수 목록을 결정한다. |

**대응 정본 티켓**: W1-1~W1-4 (임무 리포트·템플릿 시드·생성 API·인수인계 요약) · W3-3 · W3-4

---

### K5 신뢰 커널 — 소유 L3 · 상태 **WP-2 에서 제작 중** ★ 이 App 의 전제

격리·감사·저하운전. **팔 수 있게 만드는 부품이고, 없으면 나머지 다섯이 무의미하다.**

| 공개 면 | 무엇 |
|---|---|
| `tenant_roles.is_global_admin(user)` | **경계 판정 한 곳** — 이 판정식을 복사하지 않는다(D-212) |
| `tenant_filters` / `@tenant_scoped` / `assert_scoped()` | 뷰·서비스 레벨 스코프 강제(주 통제 — ORM 매니저는 §0.4) |
| 감사로그 기록 | 설정 변경 · 권한 변경 · 제어 요청 전건 |
| 저하 운전 | 외부 의존 다운 시 핵심 API 200 유지 (W0-17 · W0-19) |
| `common/api_contract.py` | 거부의 HTTP 상태 승격(플래그 기본 OFF · W0-18) |

| 이중 AC | 계약 (F) | 상품 (U) |
|---|---|---|
| | **F-12**: 무권한 차단 + 설정 감사로그 / 검수 §7: **보안 점검(인증·권한·감사로그·시크릿 분리) 통과** | **U4**: 격리를 **테스트 출력으로 봉인** · 외부 의존 장애 시 핵심 기능 가용률 ≥ 99% |
| **하나의 구현** | U4 가 요구하는 "테스트 출력"이 곧 검수의 "보안 점검 통과 증거"다. **격리 시험의 green 출력 하나가 두 곳에 제출된다** — 에스비 검수와 상용판 신뢰 자료. 그래서 시험을 스킵·비활성화하지 않는다(금지 #4). |

**대응 정본 티켓**: W0-14 · W0-16 · W0-17 · W0-18 (+W0-13 · W0-19)
**★ WP-2 EXIT(누출 0건)가 WP-DA2 코드 착수 조건인 이유가 이것이다** — K5 없이 K1~K4 를 얹으면
새 표면마다 격리를 다시 뚫는다.

---

### K6 피드백·계측 커널 — 소유 L3 · 상태 **커널 구현됨 (2026-08-30 · `backend/kernels/k6_feedback/`)** — `usage_snapshot`·`kpi_series` 는 미구현(선행 대기)

오탐을 모으고, 사용량을 세는 한 벌.

| 공개 면 | 입력 | 출력 |
|---|---|---|
| `record_feedback(event, verdict, reason)` | K1 `review_event` 가 호출 | — |
| `false_positive_rate(period, filters)` | | 비율 + 분모·분자 |
| `usage_snapshot(group, period)` | | 채널수·이벤트수·저장량 등 (W4-1 `UsageSnapshot`) |
| `kpi_series(metric, period)` | 3초·30초 실측 분포 포함 | 시계열 |

- 오탐률의 분모·분자는 **`DetectionEvent.status` 하나에서 나온다.** 별도 집계 테이블을 만들지 않는다
  — 두 벌로 세면 숫자가 갈리고, 갈린 숫자는 고객 앞에서 못 쓴다.
- DA-03 §2-4 의 `t0/t1` 계측을 여기 적재한다. **검수는 "재봤더니 빨랐다"가 아니라 p50/p95 로 한다.**

| 이중 AC | 계약 (F) | 상품 (U) |
|---|---|---|
| | **F-14**: 월간 오탐률 리포트 생성 | **U1**: 오탐률 추이 **월 하락** / **U4**: 요금 = 시스템 집계 |
| **하나의 구현** | 같은 `status` 필드가 월간 리포트(F-14)와 추이 지표(U1)와 과금 근거(U4)를 동시에 먹인다. **집계 경로를 하나로 유지하는 것 자체가 요구사항이다.** |

**대응 정본 티켓**: W2-4(오탐 피드백 통계) · W4-1(계측 모델) · W4-4(사용량 화면·CSV)

---

## 3. 커널 × 기능 × U 교차표 (한 장 요약)

| 커널 | 소유 | 계약 기능 (F) | 상품 가치 (U) | 지금 상태 |
|---|---|---|---|---|
| K1 이벤트 | L3 | F-04 · F-05 (+F-01~03 입력) | U1 | **커널 done (D-287)** · 계약 고정 |
| **K2 알림** | L3 | F-10 | U1 · U2 | `DA2-21` (2026-08-30 커널 구현 · in_progress) |
| K3 대시보드 프레임 | L3 | F-09 | U3 | **커널 구현 (2026-08-30)** · 프리셋 매핑 설정 대기(P-K3-1) |
| K4 보고서 엔진 | L3 | F-11 | U2 | **커널 구현 (2026-08-30)** · ~~격리 결함(OPEN-03)~~ **전제 오류로 해소** |
| K5 신뢰 | L3 | F-12 + 검수 보안점검 | U4 | **WP-2 제작 중 — 전제** |
| K6 피드백·계측 | L3 | F-14 | U1 · U4 | `W2-4` 커널 구현 (화면 미완) |

**F 14건 중 커널로 덮이는 것**: F-04·05·09·10·11·12·14 = **7건**.
나머지 7건은 커널이 아니다 — F-01~03(탐지 모델 확장) · F-06~08(SDN 어댑터, L2) · F-13(정찰 연계).
즉 **App 기능의 절반이 상품화 2단계와 같은 부품**이다.

> ⚠ 지시서 v2.1 은 "F 14개 중 9개가 U와 겹침 · 공수 ~40% 절감"이라 적었고 스스로 **추정**이라 표시했다.
> 실측 대조 결과 **7건**이다(F-06·07 은 SDN 어댑터로 U 와 겹치지 않는다). 공수 절감률은
> 티켓 수가 아니라 **WP-DA2 착수 시 티켓별 공수 추정으로 갱신**한다(D-210 · 지시서 자체 단서와 동일).

---

## 4. `kernel:` 필드 — 정본 반영 결과

지시서 v2.1 Code 지시대로 `tickets.yaml` 에 `kernel:` 매핑 필드를 넣었다. **20건** (2026-08-30 · `W2-2b` 추가 · D-287).

| 커널 | 티켓 |
|---|---|
| K1 | W2-1 · W2-2 · W2-2b · W2-3 |
| K2 | DA2-21 |
| K3 | W3-1 · W3-2 |
| K4 | W1-1 · W1-2 · W1-3 · W1-4 · W3-3 · W3-4 |
| K5 | W0-14 · W0-16 · W0-17 · W0-18 |
| K6 | W2-4 · W4-1 · W4-4 |

- **ID 충돌 검사**: 신규 ID `DA-01`~`DA-04` 는 기존 정본 ID 집합과 충돌 없음
  (`python scripts/gen_manifest.py` 통과 · total 75). D-246 의 교훈을 절차로 반영했다.
- 이 표와 `tickets.yaml` 의 일치는 `python scripts/verify_kernel_map.py` 가 판정한다.
- **2026-08-30 (D-287)**: `W2-2b`(AI 응답 스키마 실측) 등재. 판정문은 ID 를 `W2-3` 으로
  지정했으나 그 ID 는 이미 "AI 이벤트 센터 화면"이 쓰고 있어 실측 우선(D-210)으로 바꿔 달았다.
  같은 판정에서 **커널 done 요건이 개정**됐다 — "이중 AC green + 호출처 2곳" 에서
  **"이중 AC green + 산출물을 만드는 경로 1곳 + 계약 AC 충족"** 으로. 재사용의 증거는
  AI 호출처 개수가 아니라 **K2·K3·K4 가 K1 을 소비하는 것**이고, 그 소비 관계가 위 표다.

---

## 5. KPI 추가 (지시서 v2.1)

| 지표 | 정의 | 어떻게 재나 |
|---|---|---|
| **커널 재사용률** | 이중 AC 를 **동시에** 충족한 커널 수 ÷ 6 · 목표 90%+ | 커널마다 F AC 시험과 U AC 시험을 **각각** 돌려 둘 다 green 인지 본다. 한쪽만 green 이면 재사용 실패로 센다 |
| **중복 개발 0건** | 같은 능력을 App 과 Platform 에 두 번 구현한 건수 | 규칙 4(App 이 커널 모델 직접 import 금지)를 **정적 검사**로 건다 — 사람이 코드리뷰로 잡지 않는다 |

★ 두 KPI 는 **선언이 아니라 게이트여야 한다.** 재사용률을 사람이 세면 항상 90% 가 나온다.
WP-DA2 착수 시 위 두 검사를 CI 잡으로 등재한다.

---

## 6. 미정 2건

| # | 무엇 | 언제 |
|---|---|---|
| D4-1 | K2 알림 커널의 **외부 발송 업체**(SMS·푸시) 선정 | **WP-DA2b ENTRY(2026-10) 까지** — 계약·비용 사안. 미정 시 메일 채널만으로 착수하고 어댑터 자리만 비워 둔다 (`DA2-21` blocker) |
| D4-2 | K4 의 `ReportTemplate` **격리 결함 처리 범위** — WP-2 안인가 WP-DA2 인가 | WP-2 EXIT 전 대표 판정 (DA-01 OPEN-03) |

---

**다음 단계**: DA-01 · DA-03 · DA-04 대표 승인 → (DA-02 는 명세 수령 대기) → WP-2 EXIT → WP-DA2.
