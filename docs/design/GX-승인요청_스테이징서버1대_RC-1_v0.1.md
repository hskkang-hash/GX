# 승인 요청 — 스테이징 서버 1대 (RC-1의 열쇠)

- 요청: 세종(CPO) · 2026-09-05(기계) · 승인자: 대표 · 집행: 영실(CPM) `scripts/deploy.sh` 한 명령(사람 손 0)

## 무엇을
공개 주소 1개(도메인 · TLS 인증서) + 소형 클라우드 서버 1대(GPU 없음 · 탐지는 시드/재생으로) [추정 사양: 4 vCPU · 16GB · 200GB · 월 수만 원대 — 견적은 영실이 실측]

## 왜 지금
1. **상용 기준 G1 「신규 사용자가 URL로 들어간다」가 0**이다 — 앞단(gunicorn+nginx)은 섰지만 회사 밖에서 아무도 못 들어간다. 운영 서버 작업 창(대표 ②)을 기다리면 그때까지 상용은 0이다.
2. **빈 환경 설치 재현(OPS-02)을 사내 git 없이 잰다** — 저장소 미러 → 매뉴얼대로 세움 → 손 밖 절이 손 안으로 돌아온다.
3. **RC-1 정의**: 그 URL에서 신규 계정으로 로그인 → 첫 근무일 시나리오(S1·S2·S3 · 데스크+휴대전화 390px) 완주 캡처 1회. 이것이 「상용 오픈 후보」다.
4. 파일럿·시연·무료 대응 시간 진단(영업)이 전부 이 URL을 쓴다.

## 무엇이 아닌가
- 운영 서버가 아니다(고객 데이터 0 · 시드·훈련 데이터만 · `data_source` 표기).
- 개인정보 없음 · 원본 영상 없음(11조) · 허용 도메인 밖 실발송 0(P-41).

## 되돌리기
서버 삭제 한 번. 코드·데이터는 저장소에 있다.

## 승인 칸
☐ 승인 ☐ 보류(사유: ) · 서명: ________ · 일자: ________
