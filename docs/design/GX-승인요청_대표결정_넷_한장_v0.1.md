# 대표 결정 — **넷을 한 장으로** (30초 안에 ☐ 만 치시면 됩니다)

- 작성: 세종(CPO) · 2026-09-06(기계) · 턴 G · 수신: 대표 · 집행: 영실(CPM)
- 읽는 법: **막힌 것 / 승인되면 열리는 것 / 승인이 없으면 포기하는 것** — 줄마다 한 줄씩입니다.
- 시간 칸은 전부 **[추정]**입니다. 재 본 적 없는 수를 확정으로 적지 않았습니다.

---

## ⓪ 먼저 — **둘째 항목은 이미 끝났습니다** [실측 2026-09-06]

    직전 지시서 §3 의 「② 삭제 `tasks/record_task.py`」는 **턴 B 에 승인·집행되었습니다.**
    커밋 `32d0953` — "대표 승인 1 — stream_monitors/tasks/record_task.py **삭제**(328줄)".
    지금 저장소에 그 파일은 **없습니다**(`git ls-files` 0건).

**결정 목록이 낡아 있었습니다.** 같은 결정을 두 번 여쭙는 것은 결정을 싸게 만듭니다.
그 자리에 **지금 실제로 승인을 기다리는 삭제**를 대신 올립니다(아래 ②).

---

## ① SMTP 자격증명 + **대표 수신 주소**

| | |
|---|---|
| **막힌 것** | **사람에게 도달한 경보 0건**. `EMAIL_HOST_USER` 기본값이 `your-email@gmail.com`(자리 표시자). 오늘 차선 E 가 **발송→도착을 기계로** 쟀지만(mailpit) 받은 것은 **우리가 세운 통**이고 **TLS·SMTP 인증 경로는 못 쟀다** |
| **승인되면** | 값 넣고 **실제 릴레이로 발송 → 사람 수신함 캡처 1장** → 대장 **OPS-10 닫힘** · SLA 의 「경보 5분 도달」이 **약속으로 돌아옴** [추정 2~3시간 · 값을 받은 뒤] |
| **없으면 포기** | 상용 다섯 문장 중 **③「밤에 죽지 않는다」**. 새벽 3시 장애를 **아무도 모릅니다**. SLA 에서 경보 약속을 뺐습니다(GX-LAW-05 v0.2 §3) |
| **되돌리기** | 자격증명 회수 1회 |

☐ 승인 (주소: ________________)  ☐ 보류(사유: __________)

---

## ② 삭제 — **녹화 상태 함수 4개**(`stream_monitors/utils/redis_client.py`)

| | |
|---|---|
| **막힌 것** | `set_/update_/delete_recording_status` · `list_recording_statuses` — **부르는 곳 0곳** [실측 2026-09-06: 각 이름이 저장소에 **1회씩만** 등장 = 정의뿐]. 턴 B 에 지운 `record_task.py` 가 유일한 사용처였습니다 |
| **승인되면** | 지우고 **단위·E2E 재측정** — 읽을 것과 시험할 것이 줍니다 [추정 1시간] |
| **없으면 포기** | 잃는 기능 **없음**. 다만 **죽은 코드가 살아 있는 척** 계속 남습니다(GS 유지보수성 항목에 불리) |
| **되돌리기** | `git revert` 1회 (§0.4 금지구역 아님) |

☐ 승인  ☐ 보류(사유: __________)

---

## ③ VLM · **GPU 1장**

| | |
|---|---|
| **막힌 것** | 대장 **PERF-07**(VLM 사유 시간 예산 p95 ≤ 2s) 미착수 — **대표 결정 전에는 착수하지 않기로** 정해져 있습니다 |
| **승인되면** | 모델 후보 **2종 실측 표**(라이선스 · 오프라인 동작 · 한국어) → 예산 측정 [추정 1일 이상 · GPU 도착 뒤] |
| **없으면 포기** | 이벤트의 **한 줄 사유**(「무엇이 보였는가」). 알림은 그대로 나갑니다 — **알림은 사유를 기다리지 않습니다**(F-10 30초가 먼저) |
| **되돌리기** | 도입 안 함 = 지금 상태 |

☐ 승인  ☐ 보류(사유: __________)

---

## ④ **스테이징 서버 1대** — 넷 중 **가장 많이 막고 있는 것**

| | |
|---|---|
| **막힌 것** | 공개 URL **0** → G1 미달 · **RC-1 불가**. 빈 기계 설치 **0회** · 밖에서 두드리는 자리 **0** |
| **승인되면** | `scripts/deploy.sh` 한 명령(사람 손 0) → URL·TLS → 신규 계정 → **첫 근무일 완주 캡처 = RC-1** [추정 반나절] · **동시에 열리는 것 넷**: GS 대조표의 미충족 3칸(설치·호환성·이식성) · SLA 가용성 측정 시작 · 무료 진단/시연이 쓸 주소 · 파일럿 레퍼런스의 전제 |
| **없으면 포기** | **상용 오픈 후보(RC-1) 자체.** 회사 밖 누구도 제품을 못 봅니다. 파일럿·시연·영업이 전부 여기 걸려 있습니다 |
| **되돌리기** | 서버 삭제 1회(고객 데이터 0 · 시드·훈련만) |
| **상세** | `docs/design/GX-승인요청_스테이징서버1대_RC-1_v0.1.md` |

☐ 승인  ☐ 보류(사유: __________)

---

## 한 줄 요약

    넷 중 **④가 가장 많이 막고 있습니다** — 상용 준비도(CR)의 절반, GS 미충족의 3칸,
    SLA 의 가용성 약속이 전부 이 한 대에 걸려 있습니다.
    ①은 「밤에 죽지 않는다」, ③은 「사유 한 줄」, ②는 코드 정리입니다.
    **②는 되돌리기가 가장 쉽고, ④는 되돌리기가 서버 삭제 한 번입니다.**

서명: ________________   일자: ____________
