# -*- coding: utf-8 -*-
"""L4 App 층 — **커널을 소비만 한다.**

계층 (DA-04 §1-1)
-----------------
    L1 common     공용 유틸 · 테넌트 문지기
    L2 adapters   외부 시스템 번역기
    L3 kernels    커널. 도메인 로직이 사는 곳
    L4 apps       ← **여기.** 커널 API 를 **소비만** 한다

왜 "소비만" 인가 — 재사용 편의가 아니라 **IP 방어**다
-----------------------------------------------------
계약 8조3항이 **"본체=가이온 / 신규 연계=공동"** 으로 소유를 나눈다. 그 경계가
코드에 남으려면 도메인 로직이 커널(본체) 안에 있어야 한다. 커널 로직이 App 으로
새는 순간 **그 경계가 코드에서 소멸한다** (DA-04 §1-1 · D-278).

그래서 이 층의 파일은 얇다. 얇은 것이 게으름이 아니라 **설계**다:

    App 이 하는 일   커널 함수를 부르고, 그 결과를 HTTP 로 옮긴다
    App 이 안 하는 일 판정 · 집계 · 중복 억제 · 소유 상속 — 전부 커널이 한다

두 벌을 두면 어긋나고, 그 어긋남은 아무도 못 본다.

게이트가 지킨다 — `scripts/verify_layers.py`
--------------------------------------------
    from kernels.k1_event import record_detection      # 이렇게 쓴다
    from kernels.k1_event.models import DetectionEvent # 이러면 CI 가 exit 1
"""
