# -*- coding: utf-8 -*-
"""DSM — 재난안전 모니터링 App (L4). **F-09 · F-10 · F-11 · F-12**.

    F-09 재난 대시보드   5상태 프레임 · 연계 상태 3표시 · 이벤트 목록
    F-10 알림 발송       규칙대로 보내고, 보낸 것을 기록으로 남긴다
    F-11 상황 보고서     템플릿 치환 PDF
    F-12 관리자 설정     무권한 차단 + 감사로그 전건

Django 앱이 아니다 — `INSTALLED_APPS` 에 넣지 않는다
----------------------------------------------------
모델이 없기 때문이다. 커널을 소비할 뿐이고, 소비에는 표가 필요 없다.
`INSTALLED_APPS` 를 건드리는 것은 설정 변경이고 되돌리기가 더 크다(4원칙 ②·④) —
얻는 것이 없는데 치를 이유가 없다. 라우트는 `config/urls.py` 가 `include` 한다.

이 App 이 만지는 것 / 안 만지는 것
----------------------------------
    만진다   `kernels.<k>` 의 **공개 면** · `adapters.sdn` · `common` 의 문지기
    안 만진다 커널 내부 모듈 · Django 모델 · 다른 App 의 내부

    `scripts/verify_layers.py` 가 이것을 exit 1 로 지킨다 (D-278).
"""
